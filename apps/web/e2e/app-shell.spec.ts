import { test } from '@playwright/test';

// Phase 05A Plan 02 owns the app shell assertions.
test.describe.configure({ mode: 'serial' });

test.skip('app shell renders persistent authenticated chrome', async () => {});
