import { test } from '@playwright/test';

// Phase 05A Plan 03 owns triage audit assertions.
test.describe.configure({ mode: 'serial' });

test.skip('triage audit renders unavailable and fixture-backed populated states', async () => {});
