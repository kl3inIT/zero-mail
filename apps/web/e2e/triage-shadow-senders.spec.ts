import { test } from '@playwright/test';

// Phase 05A Plan 03 owns shadow mode and sender safety net assertions.
test.describe.configure({ mode: 'serial' });

test.skip('shadow mode and sender safety net flows are reachable', async () => {});
