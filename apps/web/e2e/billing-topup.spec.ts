import { test } from '@playwright/test';

// Phase 05A Plan 04 owns top-up route assertions.
test.describe.configure({ mode: 'serial' });

test.skip('top-up route creates an intent and watches for credited balance', async () => {});
