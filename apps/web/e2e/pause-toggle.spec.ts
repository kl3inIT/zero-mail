import { test } from '@playwright/test';

// Phase 05A Plan 02 owns the pause toggle assertions.
test.describe.configure({ mode: 'serial' });

test.skip('pause toggle uses the shared triage pause query key', async () => {});
