@ApplicationModule(
        displayName = "Referral",
        allowedDependencies = {"tenant", "shared :: lang", "shared :: persistence"})
package com.zeromail.core.referral;

import org.springframework.modulith.ApplicationModule;
