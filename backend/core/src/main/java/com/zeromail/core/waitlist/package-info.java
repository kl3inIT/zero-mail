@ApplicationModule(
        displayName = "Waitlist",
        allowedDependencies = {"account", "shared.persistence", "shared.lang", "shared.exception"})
package com.zeromail.core.waitlist;

import org.springframework.modulith.ApplicationModule;
