/**
 * Business exceptions raised by the master-key admin use cases. Each one carries a stable error
 * code consumed by the admin frontend's localization layer and the audit log.
 */
package com.zeromail.core.admin.mkey.exception;
