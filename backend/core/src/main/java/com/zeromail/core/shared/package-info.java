/** Cross-cutting primitives exposed through narrow named interfaces. */
@ApplicationModule(
        displayName = "Shared",
        allowedDependencies = {})
package com.zeromail.core.shared;

import org.springframework.modulith.ApplicationModule;
