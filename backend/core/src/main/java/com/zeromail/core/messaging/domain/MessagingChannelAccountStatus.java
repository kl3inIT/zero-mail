package com.zeromail.core.messaging.domain;

/** Lifecycle state of a tenant's connected channel account. */
public enum MessagingChannelAccountStatus {
    CONNECTED,
    BLOCKED,
    DISCONNECTED
}
