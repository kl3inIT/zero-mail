/**
 * Account domain services: concrete @Service classes only (D-B3 — no interfaces). Cross-domain
 * reads MUST go through the other domain's service (D-D1).
 */
package com.zeromail.core.account.service;
