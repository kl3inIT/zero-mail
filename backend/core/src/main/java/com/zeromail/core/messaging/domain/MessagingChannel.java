package com.zeromail.core.messaging.domain;

/** External messaging channels that can host the mobile assistant surface. */
public enum MessagingChannel {
    TELEGRAM,
    ZALO,
    WHATSAPP,
    SLACK
}
