/**
 * Rules-domain HTTP wire DTOs for compile, CRUD, reorder, preview, and template materialization.
 *
 * <p>Exposed as part of the {@code dto} application module's public API so rules controllers can
 * reach these records across Spring Modulith module boundaries.
 */
@org.springframework.modulith.NamedInterface("rules")
package com.zeromail.api.dto.rules;
