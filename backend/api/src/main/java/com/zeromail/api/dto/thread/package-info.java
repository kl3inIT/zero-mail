/**
 * Thread-domain HTTP wire DTOs for draft generation and needs-reply inbox endpoints.
 *
 * <p>Exposed as part of the {@code dto} application module's public API so thread controllers can
 * reach records in this nested package.
 */
@org.springframework.modulith.NamedInterface("thread")
package com.zeromail.api.dto.thread;
