/**
 * Admin scheduler-catalog HTTP wire DTOs. Read-only descriptors of the system's background
 * schedulers (cron + fixed-delay loops) surfaced to the operator console.
 */
@org.springframework.modulith.NamedInterface("admin.scheduler")
package com.zeromail.api.dto.admin.scheduler;
