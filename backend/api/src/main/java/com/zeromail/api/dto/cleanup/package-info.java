/**
 * Cleanup-domain HTTP wire DTOs for the bulk-unsubscribe campaign and sender suppression endpoints
 * (Phase 8).
 *
 * <p>Exposed as part of the {@code dto} application module's public API so cleanup controllers can
 * reach records in this nested package.
 */
@org.springframework.modulith.NamedInterface("cleanup")
package com.zeromail.api.dto.cleanup;
