/**
 * Worker notification runtime configuration shared by notification adapters and worker-side
 * dispatchers that need sender/app URL settings.
 */
@org.springframework.modulith.NamedInterface("notification-config")
package com.zeromail.worker.notification.config;
