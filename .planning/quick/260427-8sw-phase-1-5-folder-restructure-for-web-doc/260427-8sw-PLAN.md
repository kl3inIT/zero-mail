# Quick Task 260427-8sw: Web Folder Restructure

**Date:** 2026-04-27
**Mode:** quick

## Goal

Move low-level web content/i18n folders now so Phase 1.5 can focus on heavier OAuth and primitive deflation work.

## Tasks

### 1. Move files

- Move `apps/web/content/docs/*.mdx` to `apps/web/docs/*.mdx`.
- Move `apps/web/messages/{vi,en}.json` to `apps/web/i18n/messages/{vi,en}.json`.
- Move `LanguageSwitcher` to `apps/web/i18n/components/LanguageSwitcher.tsx`.
- Remove the `apps/web/features/i18n` feature root entirely.

### 2. Update references

- Update next-intl request config and root layout dynamic imports.
- Update LanguageSwitcher imports and feature-folder architecture tests.
- Update `global.d.ts` message typing import.
- Update docs loader paths/comments and MDX tests.
- Update i18n checker paths and message contract/error-render/component tests.

### 3. Verify

- Run `pnpm --dir apps/web run i18n:check`.
- Run relevant Vitest suites for i18n and MDX.
- Run `pnpm --dir apps/web exec tsc --noEmit`.
