# Quick Task 260507-4lb: Refactor Frontend Feature API, Hooks, Query Keys, And Tests

## Goal

Clean up the frontend feature structure so TanStack Query keys, API functions, hooks, and E2E tests have clearer ownership.

## Tasks

1. Move per-feature TanStack Query key factories out of `features/*/api/keys.ts` into `features/*/query-keys.ts`.
2. Consolidate small endpoint API files into per-feature API files where that reduces fragmentation.
3. Keep TanStack Query hooks as one hook per file, updating imports to the new API/query-key layout.
4. Move Playwright specs out of `__tests__/e2e/` into top-level `e2e/` and simplify Playwright/Vitest config around that split.
5. Update architecture tests and CI/i18n workflow details that currently reflect old paths or incomplete feature coverage.
6. Remove stale `RED-by-design` comments from tests that are now green.

## Verification

Run frontend lint, typecheck, Vitest tests, Playwright test listing, and build. Run Playwright E2E if local dev-server startup is available within the task window.
