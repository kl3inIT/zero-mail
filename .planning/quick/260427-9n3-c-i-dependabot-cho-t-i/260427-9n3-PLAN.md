# Quick Task 260427-9n3: Add Dependabot

**Date:** 2026-04-27
**Mode:** quick

## Goal

Enable GitHub Dependabot version updates for the monorepo dependency surfaces that exist today.

## Tasks

1. Add `.github/dependabot.yml` using current Dependabot v2 syntax.
2. Cover Gradle, the pnpm/npm workspace, and GitHub Actions workflows.
3. Validate the YAML/config formatting locally and commit the configuration separately from GSD artifacts.

## Rules

- Do not touch unrelated dirty files, especially the untracked `REVIEW.md`.
- Keep schedules conservative so dependency PRs are useful without creating excessive noise.
- Prefer one root pnpm/npm entry because the repo uses a root `pnpm-lock.yaml` and workspace manifest.
