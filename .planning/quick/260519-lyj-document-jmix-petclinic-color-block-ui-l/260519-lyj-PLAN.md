# Quick Task 260519-lyj: Document Jmix Petclinic Website Colors

## Goal

Create a concise Markdown reference that shows the colors used by Jmix Petclinic, using the GitHub screenshots/source and the local `jmix-scratch` reference.

## Tasks

1. Inspect Jmix Petclinic visual source.
   - Use GitHub `jmix-framework/jmix-petclinic` screenshots and theme files.
   - Use local `jmix-scratch` (`jmix-petclinic-2`) theme files for Flow UI comparison.

2. Write a viewable Markdown color reference.
   - Include palette swatches, exact hex values, screenshot embeds, and source pointers.
   - Keep it as a visual reference, not production implementation.

3. Verify references.
   - Ensure links point to stable GitHub raw assets.
   - Ensure file is easy to open in Markdown preview.
