---
status: complete
quick_id: 260427-02m
date: 2026-04-27
commit: uncommitted
---

# Quick Task 260427-02m Summary

## Result

Refactored Spring application-property reads away from direct `@Value` injection and into typed `@ConfigurationProperties` records.

## Changes

- Added `ZeroMailApiProperties` in `backend/api` for API-owned `zeromail.web` and `zeromail.cors` settings.
- Added `ZeroMailCoreProperties` in `backend/core` for core-owned `zeromail.crypto` settings, with the crypto key masked in `toString()`.
- Updated OAuth success redirect, CORS configuration, and refresh-token cipher wiring to inject typed properties.
- Enabled configuration property scanning in API, worker, and core integration-test application entrypoints.
- Added configuration metadata processors for API and core modules.
- Moved CORS defaults into `backend/api/src/main/resources/application.yml`.
- Added the core crypto property placeholder to worker `application.yml` so worker boots with the same core config contract.

## Verification

- `rg --line-number --glob '!**/build/**' '@Value\\(' backend` returned no matches.
- `./gradlew.bat :backend:core:compileJava :backend:api:compileJava :backend:worker:compileJava` passed.
- `./gradlew.bat :backend:api:test --tests com.zeromail.api.security.GoogleOAuthSuccessHandlerTest` passed.
- `./gradlew.bat :backend:api:test --tests com.zeromail.api.security.CorsIntegrationTest` passed.

## Commit

No commit was created because the worktree already contained unrelated and concurrent user changes, including modified files touched by this task and untracked files. Changes are left in the shared working tree for user review.
