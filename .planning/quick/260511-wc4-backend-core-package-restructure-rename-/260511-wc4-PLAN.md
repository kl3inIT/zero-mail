---
phase: quick-260511-wc4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/src/main/java/com/zeromail/core/**
  - backend/api/src/main/java/com/zeromail/api/**
  - backend/api/src/test/java/com/zeromail/api/**
  - backend/core/src/test/java/com/zeromail/core/**
  - backend/worker/src/main/java/com/zeromail/worker/**
  - backend/worker/src/test/java/com/zeromail/worker/**
  - CLAUDE.md
  - CONVENTIONS.md
  - package.json
  - lint-staged.config.mjs
autonomous: true
requirements: [QUICK-260511-wc4]
must_haves:
  truths:
    - "`./gradlew check` passes: all-module compileJava + compileTestJava, the updated ArchUnit suite, the NEW domain/-framework-ban rule, Spring Modulith verification, and the full backend test suite."
    - "No `com.zeromail.core.*.service` package exists anywhere under `backend/core/src/main` after the refactor."
    - "No `com.zeromail.core.*.service` package exists under `backend/core/src/test` either (the `rules/service` arch-test package is relocated)."
    - "No empty `com.zeromail.core.*.model` package directory exists under `backend/core`."
    - "`git grep -n 'com\\.zeromail\\.core\\.\\(rules\\|llm\\|triage\\)\\.application' -- backend` returns nothing — every old `application` import is rewritten to `usecases`."
    - "No class in any `com.zeromail.core.<context>.domain..` package depends on `org.springframework..`, `tools.jackson..`, `jakarta.persistence..`, or `org.hibernate..` (bright-line ArchUnit rule, no whitelist)."
    - "`lint-staged.config.mjs` exists at repo root; the `package.json` `lint-staged` block is removed; `.husky/pre-commit` (`pnpm exec lint-staged`) picks up the config file and the `backend/**/*.java` entry runs `gradlew.bat spotlessApply -q` on Windows."
    - "CLAUDE.md Conventions §2 and CONVENTIONS.md §2 describe `usecases/` (not `application/`), omit `service/` from the valid list, keep `model/` in the anti-pattern list, and carry the layering-doctrine sentence."
  artifacts:
    - path: lint-staged.config.mjs
      provides: "Cross-platform lint-staged config (function-based backend/**/*.java entry; web glob entries unchanged)"
      contains: "spotlessApply"
    - path: backend/core/src/test/java/com/zeromail/core/arch/DomainPurityArchTest.java
      provides: "New bright-line framework-free-domain ArchUnit rule (sibling to DomainBoundaryArchTests)"
      contains: "domain"
    - path: backend/core/src/main/java/com/zeromail/core/rules/usecases/package-info.java
      provides: "Renamed rules use-case package marker"
      contains: "use-case"
    - path: backend/core/src/main/java/com/zeromail/core/llm/usecases/package-info.java
      provides: "Renamed llm use-case package marker"
      contains: "use-case"
    - path: backend/core/src/main/java/com/zeromail/core/triage/usecases/package-info.java
      provides: "Renamed triage use-case package marker"
      contains: "use-case"
    - path: backend/core/src/main/java/com/zeromail/core/config/package-info.java
      provides: "Modulith package marker for core.config"
      min_lines: 1
    - path: backend/core/src/main/java/com/zeromail/core/tenant/concurrency/package-info.java
      provides: "Modulith package marker for core.tenant.concurrency"
      min_lines: 1
    - path: backend/core/src/main/java/com/zeromail/core/billing/persistence/package-info.java
      provides: "Modulith package marker for core.billing.persistence"
      min_lines: 1
    - path: backend/core/src/main/java/com/zeromail/core/llm/byok/package-info.java
      provides: "Modulith package marker for core.llm.byok"
      min_lines: 1
    - path: backend/core/src/main/java/com/zeromail/core/gmail/gateway/package-info.java
      provides: "Modulith package marker for the new gmail external-I/O adapter package"
      min_lines: 1
    - path: backend/core/src/test/java/com/zeromail/core/rules/usecases/RulePreviewWriteBoundaryTest.java
      provides: "Relocated rule-preview write-boundary arch test (was core.rules.service), package + imports + FQN string literals updated"
      contains: "Preview"
    - path: CLAUDE.md
      provides: "Conventions §2 updated to usecases/ + layering doctrine"
      contains: "usecases"
    - path: CONVENTIONS.md
      provides: "§2 updated to usecases/ + layering doctrine"
      contains: "usecases"
  key_links:
    - from: ".husky/pre-commit"
      to: "lint-staged.config.mjs"
      via: "pnpm exec lint-staged auto-discovers lint-staged.config.mjs"
      pattern: "lint-staged"
    - from: "backend/api ZeroMailApiApplicationModulesTest"
      to: "renamed core.<ctx>.usecases packages + dissolved service packages"
      via: "ApplicationModules.of(...).verify() walks every core package; no leftover service/ or empty model/"
      pattern: "ApplicationModules"
    - from: "backend/core/src/test/.../arch/DomainPurityArchTest (framework-free-domain rule)"
      to: "every core.<ctx>.domain.. class"
      via: "ArchUnit noClasses().that().resideInAPackage('..com.zeromail.core.*.domain..').should().dependOnClassesThat().resideInAnyPackage('org.springframework..','tools.jackson..','jakarta.persistence..','org.hibernate..')"
      pattern: "domain"
---

<objective>
Restructure `backend/core` package layout to match the locked DDD + Clean Architecture + Hexagonal doctrine: delete leftover empty `model/` packages, backfill missing Modulith `package-info.java`, rename `application/` → `usecases/` in the three contexts that have it (`llm`, `rules`, `triage`), dissolve every context's `service/` package by re-homing each class to `usecases/` (injected beans, keep `@Service`) or `domain/` (pure logic, drop the annotation) or `gateway/` (external-I/O adapters), relocate the stray `core.rules.service` arch *test* class, add a bright-line ArchUnit rule banning framework imports from `domain/`, update all existing ArchUnit/Modulith tests, sync `CLAUDE.md` + `CONVENTIONS.md`, and replace the Windows-broken `lint-staged` `package.json` block with a cross-platform `lint-staged.config.mjs`.

Purpose: The `domain/` packages must be provably framework-free (tactical DDD), `usecases/` must be the single Clean-Architecture use-case layer, and `service/` (an ambiguous catch-all that lets framework imports drift into would-be domain code) must not exist anywhere — main *or* test. The lint-staged fix removes a Windows pre-commit hook failure.
Output: Restructured `backend/core` packages, rewritten imports across `backend/{core,api,worker}` (main + test), a new ArchUnit rule, updated tests, updated docs, and `lint-staged.config.mjs`. Delivered in four commits — Commit 1 cleanup, Commit 2 `application/`→`usecases/` rename, Commit 3 dissolve `service/` (incl. test relocation + arch-test edits), Commit 4 new ArchUnit rule + docs — so each is a self-contained green compile and the big rename is isolated from the bigger dissolve.

LOCKED context — do NOT re-litigate any design decision below. Re-homing rules, the no-whitelist domain rule, the doctrine sentence, and the commit grouping are all decided.

Branch note: the executor runs on `gsd/phase-04-triage-convergence-hero` (no worktree isolation in this env). A parallel agent may be on the same tree — `git add` ONLY the explicit paths listed per commit; NEVER `git add -A` / `git add .`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@CONVENTIONS.md

# Codebase facts gathered during planning (use these — do NOT re-survey from scratch):
#
# Contexts with an `application/` package today: llm, rules, triage. Contexts WITHOUT one
#   (account, billing, gmail, onboarding): create `usecases/` ONLY if Task 3 moves classes there;
#   never create an empty `usecases/`.
#
# Empty `model/` dirs to delete (confirmed empty during planning — re-confirm before `git rm`):
#   core/account/model, core/billing/model, core/gmail/model, core/llm/model,
#   core/onboarding/model, core/rules/model
#
# `backend/core/src/main` sub-packages WITHOUT a package-info.java (add one to each, matching
# existing one-line `/** ... */` style): core/config, core/tenant/concurrency,
# core/billing/persistence, core/billing/service (the latter only matters until Task 3 deletes it
# — do NOT add a package-info to billing/service; it's going away), core/llm/byok.
#   So: add package-info.java to core/config, core/tenant/concurrency, core/billing/persistence,
#   core/llm/byok. (billing/service is intentionally skipped — it's dissolved in Task 3.)
#
# `@ApplicationModule(allowedDependencies = {...})` arrays use bare context names
# ("llm", "rules", "tenant", "shared.lang", ...) — NONE reference `.application`, so NONE need
# changing for the rename. The only doc that mentions old sub-package names is the JavaDoc block in
# `backend/core/src/main/java/com/zeromail/core/llm/package-info.java` ("`model` - public records...",
# "`service` - public service contract...") — update that prose to mention `usecases/`, `domain/`,
# `gateway/`, drop `model`.
#
# Files importing `com.zeromail.core.{llm,rules,triage}.application.*` (rewrite ALL — main + test):
#   backend/api: controllers/{llm,rules,triage}, ~18 dto/{llm,rules,triage} files; tests:
#     ByokControllerIntegrationTest, RulesControllerIntegrationTest, TriageTenantControllerContractTest,
#     TriageUndoControllerContractTest
#   backend/core main: the 3 application/ packages themselves; llm/gateway/sanitization/*,
#     llm/gateway/springai/*, llm/service/* (ByokService, LlmGateway, LlmGatewayImpl, AllowListedTools,
#     ByokLlmModelClient, LlmModelClient, RuleCompileToolValidator, SemanticIntentEvaluator),
#     rules/service/* (RuleCompileResultValidator, RuleCompilerService, RuleManagementService,
#     RulePreviewService, RuleTemplateMaterializationService), triage/service/SenderSafetyNetService
#   backend/core test: llm/application/*, llm/gateway/sanitization/*, llm/gateway/springai/*,
#     llm/observability/*, llm/service/* (many), rules/ai/RuleCompileReferenceDatasetTest,
#     rules/privacy/RulePreviewPrivacyTest, rules/service/* (many — INCLUDING the arch test
#     RulePreviewWriteBoundaryTest, see below), triage/* (several)
#   backend/worker: main worker/llm/DriftDetectionJob; tests worker/llm/* + worker/triage/*
#   ALSO: test SOURCE directories named `application`/`service` exist and must be `git mv`'d to
#     `usecases` / re-homed: src/test/.../llm/application, src/test/.../rules/service,
#     src/test/.../billing/service, src/test/.../gmail/service, src/test/.../onboarding/service,
#     src/test/.../llm/service. (The test classes inside follow their production targets:
#     llm/application tests → llm/usecases; the *behavioural* service tests follow each moved class
#     to its new home — see the per-class table. The lone *arch* test in rules/service —
#     RulePreviewWriteBoundaryTest — has a dedicated relocation rule below, since it maps to no
#     single moved class.)
#
# RELOCATION OF THE STRAY ARCH TEST (LOCKED — Task 3):
#   `backend/core/src/test/java/com/zeromail/core/rules/service/RulePreviewWriteBoundaryTest.java`
#   currently: `package com.zeromail.core.rules.service;`, imports `com.zeromail.core.gmail.service.GmailPreviewReadService`,
#   references types `RulePreviewService`, `RulePreviewDataService`, `GmailPreviewReadService` (all of which
#   move to `usecases/` per the table below), and contains the FQN string literals
#   `"..core.rules.service.."` and `"..core.gmail.service.."` in an ArchUnit rule.
#   → MOVE it to `backend/core/src/test/java/com/zeromail/core/rules/usecases/RulePreviewWriteBoundaryTest.java`
#     (next to where `RulePreviewService`/`RulePreviewDataService` land — `rules/usecases/`).
#   → Change its declaration to `package com.zeromail.core.rules.usecases;`.
#   → Change `import com.zeromail.core.gmail.service.GmailPreviewReadService;` →
#     `import com.zeromail.core.gmail.usecases.GmailPreviewReadService;` (after that class moves in Task 3).
#     `RulePreviewService` / `RulePreviewDataService` then need no import (same package).
#   → Change the two ArchUnit string literals: `"..core.rules.service.."` → `"..core.rules.usecases.."`,
#     `"..core.gmail.service.."` → `"..core.gmail.usecases.."`.
#   → After this move, NO `com.zeromail.core.*.service` package may remain under `backend/core/src/test`
#     either (the `rules/service` test dir is now empty — delete it).
#
# `service/` re-homing decisions (LOCKED — executor MUST open each class and confirm the injection
# signal before acting; the calls below were verified during planning):
#
#   account/service:
#     AccountService            → usecases/  (keep @Service; injects UserRepository)
#     OAuthProvisioningService  → usecases/  (keep @Service; injects repos + TransactionTemplate)
#     package-info.java         → delete (package gone) ; add account/usecases/package-info.java
#   billing/service:
#     BillingTopupService       → usecases/  (keep @Service; injects repos + TransactionTemplate)
#     CreditLedger (interface)  → usecases/  (public port — callers depend on this)
#     CreditLedgerService       → usecases/  (keep @Service; package-private impl of CreditLedger)
#     SepayApiKeyVerifier       → usecases/  (keep @Component; injects ZeroMailCoreProperties; HMAC
#                                  compare only, no HTTP — usecases/ is correct, NOT gateway/)
#     TopupCodeGenerator        → domain/    (REMOVE @Component; only field is a `new SecureRandom()`,
#                                  not injected → pure domain. Few callers — BillingTopupService;
#                                  swap to `new TopupCodeGenerator()` or static. Document the call-site
#                                  swap in the summary.)
#   gmail/service:
#     GmailApiClientFactory     → gateway/   (keep @Component; external Gmail OAuth/token I/O adapter.
#                                  Create core/gmail/gateway/ package + package-info.java.)
#     GmailConnectionService    → usecases/  (keep @Service)
#     GmailDeliveryProcessingService → usecases/ (keep @Service)
#     GmailPreviewReadService   → usecases/  (keep @Service; injects JdbcTemplate + factory)
#     PubSubIngestionService    → usecases/  (keep @Service)
#     IngestResult / InvalidGrantException → usecases/ (result + exception records used by the above)
#     package-info.java         → delete; add gmail/usecases/package-info.java + gmail/gateway/package-info.java
#   llm/service:
#     LlmGateway (interface)    → usecases/  (public port; callers import verbatim — keep FQN style)
#     LlmGatewayImpl            → usecases/  (keep @Service; package-private impl)
#     ByokService               → usecases/  (keep @Service)
#     ByokLlmModelClient / LlmModelClient (interfaces) → usecases/ (model-client seams)
#     SemanticIntentEvaluator (the INTERFACE in llm/service) → usecases/ ; the SpringAI impl already
#       lives in llm/gateway/springai/SemanticIntentEvaluator.java — leave it.
#     ActionValidator           → domain/    (REMOVE @Component; no injected fields → pure domain.
#                                  Caller: LlmGatewayImpl — swap to `new ActionValidator()` or static.)
#     AllowListedTools          → domain/    (REMOVE @Component; only static tool config → pure domain.
#                                  Caller: LlmGatewayImpl. Document swap.)
#     RuleCompileToolValidator  → domain/    (REMOVE @Component; no injected fields → pure domain.
#                                  Caller: LlmGatewayImpl. Document swap.)
#     package-info.java         → delete; add llm/usecases/package-info.java (the `application/`
#       package-info is renamed to `usecases/` in Task 2 — reuse/update that one). Update
#       llm/package-info.java JavaDoc prose.
#   onboarding/service:
#     OnboardingService         → usecases/  (keep @Service; injects repo + AccountService)
#     package-info.java         → delete; add onboarding/usecases/package-info.java
#   rules/service:
#     RuleCompilerService               → usecases/ (keep @Service)
#     RuleManagementService             → usecases/ (keep @Service)
#     RulePreviewService                → usecases/ (keep @Service)
#     RulePreviewDataService            → usecases/ (keep @Service)
#     RuleTemplateCatalogService        → usecases/ (keep @Service)
#     RuleTemplateMaterializationService→ usecases/ (keep @Service)
#     RuleEvaluator                     → domain/   (plain class, no annotation, no injection → domain.
#                                          Callers: RulePreviewService, ActionProposalMerger — already
#                                          `new RuleEvaluator()` in places. Document.)
#     ActionProposalMerger              → domain/   (plain class, no @Component; default ctor does
#                                          `this(new RuleEvaluator())` → pure domain. Caller:
#                                          RulePreviewService. Keep both ctors. Document.)
#     RuleCompileResultValidator        → domain/   (REMOVE @Component; NO injected fields, only an
#                                          inner record → pure domain. Caller: RuleCompilerService —
#                                          swap to `new RuleCompileResultValidator()`. Document.)
#     package-info.java                 → the `application/` one is renamed to `usecases/` in Task 2;
#                                          delete the `service/` one. Update `rules/usecases/package-info.java`
#                                          doc text to "Use-case services, commands, and operation
#                                          results for the rules context."
#     (ALSO: the arch test RulePreviewWriteBoundaryTest in src/test/.../rules/service — relocate per
#      the dedicated rule above; it is NOT one of these production classes.)
#   tenant/service:
#     TenantService             → usecases/  (keep @Service; injects TenantRepository).
#                                  Create core/tenant/usecases/package-info.java; delete tenant/service/.
#   triage/service:
#     SenderSafetyNetService    → usecases/  (keep @Component; injects factory + repos + Redis + Clock)
#     TriageGmailWriter         → usecases/  (keep @Component; injects GmailApiClientFactory — it IS
#                                  the triage Gmail-write entry bean, currently called from triage
#                                  application/. usecases/ is correct.)
#     TriageSafetyPolicy        → domain/    (REMOVE @Component; NO injected fields, pure gate logic →
#                                  domain. Callers: triage usecases/orchestrator — swap to
#                                  `new TriageSafetyPolicy()` or static. Document.)
#     package-info.java         → delete; the `application/` one renames to `usecases/` in Task 2.
#                                  Update its doc text. (TriageGmailWriter/SenderSafetyNetService land
#                                  in triage/usecases/.)
#
#   After Task 3: NO `com.zeromail.core.<ctx>.service` package may remain under backend/core/src/main
#   OR backend/core/src/test. Also re-grep backend/api and backend/worker (main + test) — no
#   `com.zeromail.<module>.<ctx>.service` package decls or FQN refs should survive (none expected, but
#   verify).
#   When genuinely torn between usecases/ and domain/: pick usecases/ (never risk a framework import
#   in domain/). If removing an annotation would cascade into many call-site edits that bloat the
#   task, instead move that class to usecases/ keeping the annotation — but the verified cases above
#   all have ≤ a few callers, so the domain/ choice stands. Document any deviation in the summary.
#
# Existing ArchUnit / Modulith tests to update (Task 3 unless noted):
#   - DomainBoundaryArchTests — no `..application..`/`..service..` literals today; the cross-domain-
#     repo rules reference `..core.<ctx>.persistence..` (unchanged). The NEW framework-free-domain
#     rule does NOT go here — it gets its own sibling class `DomainPurityArchTest` in the same `arch`
#     package (Task 4) for legibility.
#   - TriageGmailWriteBoundaryTest — references `"com.zeromail.core.triage.service.TriageGmailWriter"`
#     → update to `"com.zeromail.core.triage.usecases.TriageGmailWriter"`.
#   - BillingDomainBoundaryArchTest (in package com.zeromail.core.billing) — references
#     `"..core.billing.service.."` and `"com.zeromail.core.billing.service.CreditLedgerService"`
#     → update to `"..core.billing.usecases.."` and `"com.zeromail.core.billing.usecases.CreditLedgerService"`.
#   - RulePreviewWriteBoundaryTest (relocated from rules/service → rules/usecases) — update its two
#     `"..core.rules.service.."` / `"..core.gmail.service.."` literals to `usecases` and its
#     `GmailPreviewReadService` import — see the dedicated relocation rule above.
#   - RulesBoundaryArchTest, LlmGatewayBoundaryTest, SafetyContractArchTests, TenantIsolationArchTests,
#     TriageAuditRepositoryBoundaryArchTest, LlmRepositoryContentBanTest, ZeroMailApiApplicationModulesTest
#     — no `..application..`/`..service..` literals found during planning, but the executor MUST
#     re-grep ALL of `backend/**/src/main` AND `backend/**/src/test` for
#     `\.application\.|\.service\.|application\.\.|service\.\.|package com\.zeromail\.[a-z.]+\.service;`
#     after the rename/dissolve and fix anything that slipped through (test class package decls, FQN
#     string literals, `@ComponentScan`/`@Import` references). `./gradlew check` is the backstop.
#
# lint-staged (Task 1): package.json currently has top-level `"lint-staged": { "backend/**/*.java":
#   "./gradlew spotlessApply -q", "apps/web/**/*.{ts,tsx,js,jsx}": [eslint --fix, prettier --write],
#   "apps/web/messages/*.json": [...], "apps/web/!(messages)/**/*.{md,mdx,json,css}": [...],
#   "apps/web/*.{md,json,css}": [...] }`. The `backend/**/*.java` string is broken on Windows.
#   Fix: create `lint-staged.config.mjs` at repo root that `export default`s an object where:
#     - "backend/**/*.java" maps to a FUNCTION `(_files) => process.platform === 'win32' ?
#         'gradlew.bat spotlessApply -q' : './gradlew spotlessApply -q'` (ignores its files arg →
#         lint-staged won't append staged filenames as Gradle task names);
#     - the four `apps/web/...` keys keep their EXACT current array-of-string values (those entries
#       DO receive filenames — preserve verbatim).
#   Then DELETE the entire top-level `"lint-staged"` block from package.json. Leave the
#   `"lint-staged": "16.4.0"` devDependency line untouched. `.husky/pre-commit` already runs
#   `pnpm exec lint-staged` and auto-discovers `lint-staged.config.mjs` — verify, do NOT edit the hook.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cleanup — delete empty model/ packages, add missing package-info, cross-platform lint-staged (Commit 1)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/{account,billing,gmail,llm,onboarding,rules}/model/ (delete),
    backend/core/src/main/java/com/zeromail/core/config/package-info.java (new),
    backend/core/src/main/java/com/zeromail/core/tenant/concurrency/package-info.java (new),
    backend/core/src/main/java/com/zeromail/core/billing/persistence/package-info.java (new),
    backend/core/src/main/java/com/zeromail/core/llm/byok/package-info.java (new),
    lint-staged.config.mjs (new), package.json
  </files>
  <action>
    1. For each of `core/{account,billing,gmail,llm,onboarding,rules}/model/`: confirm the directory contains NO files (planning verified all six empty). If any actually has content, STOP — surface the file(s) and ask rather than guessing. For the confirmed-empty ones, remove the directory (`git rm -r --` on the empty dir; if git refuses because it's untracked/empty, just `rmdir` it — empty dirs aren't in git, so this may already be a no-op).
    2. Add a `package-info.java` to `core/config`, `core/tenant/concurrency`, `core/billing/persistence`, `core/llm/byok`, each with the `package` declaration and a single-line `/** ... */` JavaDoc in the style of existing one-liners (e.g. `core/account/projection/package-info.java`). Suggested text: config → "Core-wide configuration: typed `@ConfigurationProperties` and shared `@Bean` factories used by every runnable module."; tenant/concurrency → "Tenant-aware structured-concurrency primitives (`TenantAwareTaskScope`) that propagate `TenantContext` across virtual threads."; billing/persistence → "Billing persistence: JPA entities and Spring Data repositories for credit ledger and top-up intents."; llm/byok → "BYOK endpoint validation helpers for tenant-supplied LLM provider credentials." Do NOT add a package-info to `core/billing/service` — that package is dissolved in Task 3.
    3. Create `lint-staged.config.mjs` at repo root: `export default` an object where key `"backend/**/*.java"` is a function `(_files) => (process.platform === 'win32' ? 'gradlew.bat spotlessApply -q' : './gradlew spotlessApply -q')`, and the four `apps/web/...` keys carry the EXACT current array values from `package.json` (`"apps/web/**/*.{ts,tsx,js,jsx}"` → `["pnpm --filter web exec eslint --fix", "pnpm --filter web exec prettier --write"]`; `"apps/web/messages/*.json"` → `["pnpm --filter web exec prettier --write", "pnpm --filter web run i18n:check"]`; `"apps/web/!(messages)/**/*.{md,mdx,json,css}"` → `["pnpm --filter web exec prettier --write"]`; `"apps/web/*.{md,json,css}"` → `["pnpm --filter web exec prettier --write"]`).
    4. In `package.json`, DELETE the entire top-level `"lint-staged": { ... }` block (keep the `"lint-staged": "16.4.0"` devDependency line and trailing comma/brace structure valid). Do NOT touch `.husky/pre-commit`.
    5. JetBrains MCP `get_file_problems` on the new `package-info.java` files (if MCP unavailable, `./gradlew compileJava` is the substitute parse gate — note the substitution in the summary). `pnpm exec lint-staged --help` or a dry `node -e "import('./lint-staged.config.mjs').then(m=>console.log(typeof m.default))"` to confirm the config file parses.
    6. Stage ONLY these explicit paths (no `git add -A`): the deleted model dirs (if tracked), the four new `package-info.java` files, `lint-staged.config.mjs`, `package.json`. Commit: `chore(core): remove empty model/ packages, add missing package-info, fix cross-platform lint-staged java config`.
  </action>
  <verify>
    `git status --porcelain` shows only the intended paths staged; `find backend/core/src/main -type d -name model` returns nothing; `node -e "import('./lint-staged.config.mjs').then(m=>{const c=m.default; if(typeof c['backend/**/*.java']!=='function') process.exit(1); console.log('ok')})"` prints `ok`; `grep -c '"lint-staged"' package.json` returns 1 (the devDependency line only); `./gradlew compileJava -q` succeeds.
  </verify>
  <done>The six empty `model/` dirs are gone, four `package-info.java` files added in the existing style, `lint-staged.config.mjs` exists with a function-based `backend/**/*.java` entry and verbatim web globs, `package.json` no longer has a top-level `lint-staged` block, `compileJava` passes, and Commit 1 is made staging only the listed paths.</done>
</task>

<task type="auto">
  <name>Task 2: Rename application/ → usecases/ in llm, rules, triage + rewrite all imports (Commit 2)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/{llm,rules,triage}/application/ → .../usecases/ (git mv),
    backend/core/src/test/java/com/zeromail/core/{llm,rules,triage}/application/ → .../usecases/ (git mv, where present),
    backend/core/src/main/java/com/zeromail/core/{llm,rules,triage}/usecases/package-info.java (renamed from application/, doc text updated),
    backend/core/src/main/java/com/zeromail/core/llm/package-info.java (JavaDoc prose updated),
    backend/api/src/main/java/com/zeromail/api/** (imports), backend/api/src/test/java/com/zeromail/api/** (imports),
    backend/core/src/test/java/com/zeromail/core/** (imports), backend/worker/src/main/java/com/zeromail/worker/** (imports), backend/worker/src/test/java/com/zeromail/worker/** (imports)
  </files>
  <action>
    1. For each `<ctx>` in (`llm`, `rules`, `triage`): `git mv backend/core/src/main/java/com/zeromail/core/<ctx>/application backend/core/src/main/java/com/zeromail/core/<ctx>/usecases` (and the same for the matching test source dir `backend/core/src/test/.../<ctx>/application` → `.../<ctx>/usecases` if it exists).
    2. In every moved file (main + test), change `package com.zeromail.core.<ctx>.application;` → `package com.zeromail.core.<ctx>.usecases;`.
    3. Repo-wide, rewrite every `import com.zeromail.core.<ctx>.application.` → `import com.zeromail.core.<ctx>.usecases.` and any FQN string literal `com.zeromail.core.<ctx>.application.X` → `...usecases.X` (search `backend/{core,api,worker}/src/{main,test}`). Use the planning file list as the starting set but verify with `git grep -l 'core\.<ctx>\.application'` until it returns nothing.
    4. Update each moved `usecases/package-info.java` doc text to e.g. "Use-case services, commands, and operation results for the <ctx> context."
    5. `@ApplicationModule(allowedDependencies=...)` arrays use bare context names — confirm none mention `.application` (planning says none do; re-grep all `package-info.java` to be sure). Update `llm/package-info.java`'s JavaDoc prose: change the `model` / `service` sub-package bullets to `domain` / `usecases` / `gateway`, drop `model`.
    6. Run `./gradlew compileJava compileTestJava` (all modules) — MUST be green. JetBrains MCP `get_file_problems` on a sample of the heavily-touched files (the moved `usecases/` classes, the api `controllers/{llm,rules,triage}` + `dto/*`); if MCP unavailable, `compileJava compileTestJava` is the substitute — note it.
    7. Stage ONLY the explicit moved/edited paths (`git add <paths>`), never `git add -A`. Commit: `refactor(core): rename application/ to usecases/ in llm, rules, triage`.
  </action>
  <verify>
    `find backend/core/src/main -type d -name application` returns nothing; `find backend/core/src/test -type d -name application` returns nothing; `git grep -n 'com\.zeromail\.core\.\(llm\|rules\|triage\)\.application' -- backend` returns nothing; `git grep -nE 'package com\.zeromail\.core\.(llm|rules|triage)\.application;' -- backend` returns nothing; `./gradlew compileJava compileTestJava` (all modules) succeeds.
  </verify>
  <done>`application/` is renamed to `usecases/` in llm/rules/triage (main + matching test dirs) with all imports/FQNs/package decls rewritten; the three moved `usecases/package-info.java` files have updated doc text; `llm/package-info.java` JavaDoc updated to `domain`/`usecases`/`gateway`; `compileJava + compileTestJava` green across all modules; Commit 2 staged only the explicit paths.</done>
</task>

<task type="auto">
  <name>Task 3: Dissolve service/ into usecases+domain (incl. test relocations + arch-test edits) (Commit 3 — may split per-context)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/{account,billing,gmail,llm,onboarding,rules,tenant,triage}/service/ → dissolved into .../usecases/ and .../domain/ (and gmail/gateway/),
    backend/core/src/main/java/com/zeromail/core/{account,gmail,onboarding,tenant}/usecases/package-info.java (new),
    backend/core/src/main/java/com/zeromail/core/gmail/gateway/package-info.java (new),
    backend/core/src/main/java/com/zeromail/core/llm/package-info.java (JavaDoc prose — only if Task 2 left anything),
    backend/api/src/main/java/com/zeromail/api/** (imports), backend/api/src/test/java/com/zeromail/api/** (imports),
    backend/core/src/test/java/com/zeromail/core/** (imports + test src dir renames: rules/service, billing/service, gmail/service, onboarding/service, llm/service → re-homed to follow moved targets; rules/service/RulePreviewWriteBoundaryTest → rules/usecases/),
    backend/worker/src/main/java/com/zeromail/worker/** (imports), backend/worker/src/test/java/com/zeromail/worker/** (imports),
    backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java,
    backend/core/src/test/java/com/zeromail/core/billing/BillingDomainBoundaryArchTest.java,
    backend/core/src/test/java/com/zeromail/core/rules/usecases/RulePreviewWriteBoundaryTest.java (relocated from rules/service)
  </files>
  <action>
    Work context-by-context (`llm`, then `rules`, then `triage`, then `account`, `billing`, `gmail`, `onboarding`, `tenant`) so each commit (you MAY make one commit per context, or one big Commit 3 — your call, but EVERY commit must leave `./gradlew compileJava compileTestJava` green) is self-contained.

    A. DISSOLVE `service/` per the LOCKED re-homing table in <context> (RE-READ it; the executor MUST open each `service/` class and confirm the injection signal before acting):
       - For each class going to `usecases/`: `git mv` it into `<ctx>/usecases/`, change its `package` decl, KEEP `@Service`/`@Component` and the `Service` suffix. (For contexts with no prior `usecases/` — account, gmail, onboarding, tenant — this creates the dir; add a `usecases/package-info.java` to each new one matching the style, e.g. "Use-case services, commands, and operation results for the <ctx> context.")
       - For each class going to `domain/`: `git mv` it into `<ctx>/domain/`, change its `package` decl, REMOVE the `@Service`/`@Component` annotation (and its import), and fix call sites to `new X()` (or convert pure methods to `static` if the class has no state — author's call; prefer the smaller diff). The verified call-site counts are small (≤ a few callers each) — see the table. If a removal would cascade into many edits, instead leave the class in `usecases/` with its annotation and note the deviation in the summary.
       - `GmailApiClientFactory` → create `core/gmail/gateway/` with a `package-info.java` ("Gmail external-I/O adapters: OAuth token refresh and Gmail API client construction.") and `git mv` the class there; change its `package` decl; keep `@Component`.
       - Move accompanying result/exception records that travel with an entry bean (e.g. `gmail/service/IngestResult.java`, `gmail/service/InvalidGrantException.java`) into the same `usecases/` package.
       - Delete each now-empty `<ctx>/service/package-info.java` and the (empty) `<ctx>/service/` directory. After this step, `find backend/core/src/main -type d -name service` MUST return nothing.

    B. MOVE & FIX BEHAVIOURAL TEST CLASSES that follow their production targets (`git mv` + update `package` decls + imports): e.g. `src/test/.../billing/service/CreditLedgerConcurrentReserveTest` → `.../billing/usecases/`; `src/test/.../gmail/service/...` → `.../gmail/usecases/`; `src/test/.../llm/service/...` → `.../llm/usecases/`; `src/test/.../onboarding/service/...` → `.../onboarding/usecases/`; `src/test/.../rules/service/...` (except the arch test, see C) → `.../rules/usecases/`. After moving, no `com.zeromail.core.*.service` test package should remain except temporarily the `rules/service` dir which step C empties.

    C. RELOCATE THE STRAY ARCH TEST `RulePreviewWriteBoundaryTest` (LOCKED — see the dedicated rule in <context>):
       - `git mv backend/core/src/test/java/com/zeromail/core/rules/service/RulePreviewWriteBoundaryTest.java backend/core/src/test/java/com/zeromail/core/rules/usecases/RulePreviewWriteBoundaryTest.java`.
       - Change its declaration `package com.zeromail.core.rules.service;` → `package com.zeromail.core.rules.usecases;`.
       - Change `import com.zeromail.core.gmail.service.GmailPreviewReadService;` → `import com.zeromail.core.gmail.usecases.GmailPreviewReadService;`. `RulePreviewService` / `RulePreviewDataService` are now same-package — drop any explicit import / use the simple name.
       - Change the two ArchUnit string literals: `"..core.rules.service.."` → `"..core.rules.usecases.."` and `"..core.gmail.service.."` → `"..core.gmail.usecases.."`.
       - Delete the now-empty `backend/core/src/test/java/com/zeromail/core/rules/service/` directory.

    D. UPDATE the remaining arch/Modulith tests:
       - `TriageGmailWriteBoundaryTest`: `"com.zeromail.core.triage.service.TriageGmailWriter"` → `"com.zeromail.core.triage.usecases.TriageGmailWriter"`.
       - `BillingDomainBoundaryArchTest`: `"..core.billing.service.."` → `"..core.billing.usecases.."`; `"com.zeromail.core.billing.service.CreditLedgerService"` → `"com.zeromail.core.billing.usecases.CreditLedgerService"`. (`credit_ledger_service_not_instantiated_outside_billing_usecases` is the new intent — rename the test method too.)
       - Re-grep ALL of `backend/**/src/main` AND `backend/**/src/test` for `\.service\.`, `service\.\.`, `\.\.service\.\.`, and `package com\.zeromail\.[a-z.]+\.service;` and fix any remaining package decls, FQN string literals, `@Import`/`@ComponentScan` refs. `ZeroMailApiApplicationModulesTest`, `SafetyContractArchTests`, `TenantIsolationArchTests`, `TriageAuditRepositoryBoundaryArchTest`, `RulesBoundaryArchTest`, `LlmGatewayBoundaryTest`, `LlmRepositoryContentBanTest` had no such literals at planning time — verify (in main *and* test).

    E. After A–D: run `./gradlew compileJava compileTestJava` (all modules) — MUST be green before any commit in this task. JetBrains MCP `get_file_problems` on a sample of the heavily-touched files (LlmGatewayImpl, RulePreviewService, RuleCompilerService, TriageOrchestratorService, the moved `domain/` classes, the relocated `RulePreviewWriteBoundaryTest`); if MCP unavailable, `compileJava compileTestJava` is the substitute — note it. Then `./gradlew :backend:core:test --tests 'com.zeromail.core.arch.*' --tests 'com.zeromail.core.billing.BillingDomainBoundaryArchTest' --tests 'com.zeromail.core.rules.usecases.RulePreviewWriteBoundaryTest'` (modulo the documented OOM-retry caveat).

    F. Commit(s): either one `refactor(core): dissolve service/ into usecases+domain; relocate rule-preview arch test` covering A–D, OR split per-context (`refactor(core/llm): ...`, `refactor(core/rules): ...`, ...) — each commit must compile. Stage ONLY the explicit moved/edited paths (`git add <paths>`), never `git add -A`. Include the arch-test edits + new `package-info.java` files in the relevant commit(s).
  </action>
  <verify>
    `find backend/core/src/main -type d -name service` returns nothing; `find backend/core/src/test -type d -name service` returns nothing; `git grep -n 'com\.zeromail\.core\.[a-z]*\.service\.' -- backend` returns nothing (no remaining FQN refs to a `core.<ctx>.service` package); `git grep -nE 'package com\.zeromail\.core\.[a-z]+\.service;' -- backend` returns nothing; `./gradlew compileJava compileTestJava` (all modules) succeeds; `./gradlew :backend:core:test --tests 'com.zeromail.core.arch.*' --tests 'com.zeromail.core.billing.BillingDomainBoundaryArchTest' --tests 'com.zeromail.core.rules.usecases.RulePreviewWriteBoundaryTest'` passes (modulo the documented OOM-retry caveat).
  </verify>
  <done>Every `service/` package under `backend/core/src/main` is gone, its classes re-homed per the locked table (`@Service` kept on usecases beans, removed on domain classes with call sites fixed, `GmailApiClientFactory` in `gmail/gateway/`); behavioural test classes moved to follow their targets; the stray arch test `RulePreviewWriteBoundaryTest` relocated to `rules/usecases/` with package + `GmailPreviewReadService` import + the two ArchUnit string literals updated, and the old `rules/service` test dir deleted; new `usecases/`/`gateway/` `package-info.java` files added; arch/Modulith tests updated (`TriageGmailWriteBoundaryTest`, `BillingDomainBoundaryArchTest`, the relocated test, any others found in main or test); `compileJava + compileTestJava` green across all modules; commit(s) staged only the explicit paths; any deviation from the re-homing table documented in the summary.</done>
</task>

<task type="auto">
  <name>Task 4: Enforce framework-free domain/ (new ArchUnit rule) + sync docs (Commit 4)</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/arch/DomainPurityArchTest.java (new),
    CLAUDE.md, CONVENTIONS.md
  </files>
  <action>
    1. Add a new ArchUnit test class `backend/core/src/test/java/com/zeromail/core/arch/DomainPurityArchTest.java` (package `com.zeromail.core.arch`, style of the other arch tests — `ClassFileImporter().withImportOption(new ImportOption.DoNotIncludeTests()).importPackages("com.zeromail")`). The rule: `noClasses().that().resideInAPackage("..com.zeromail.core.*.domain..").should().dependOnClassesThat().resideInAnyPackage("org.springframework..","tools.jackson..","jakarta.persistence..","org.hibernate..").because("Domain layer (tactical DDD) MUST be framework-free — if a class needs a framework dependency it belongs in usecases/, not domain/. NO WHITELIST, NO EXEMPTION.")`. Do NOT add `allowEmptyShould(true)` — billing/domain, gmail/domain, llm/domain, onboarding/domain, rules/domain, triage/domain all exist with classes (and Task 3 adds more), so the rule has subjects. The pattern `..com.zeromail.core.*.domain..` matches `<context>/domain/` only — it does NOT match `core.shared.persistence.*` (not in a `*.domain.*` segment), which is intended. Do not add `core.shared.*` to the rule.
       - If after Task 3 any class in a `domain/` package can't satisfy this rule, that class was mis-homed — move it to `usecases/` (keeping its annotation if it had one). Do NOT weaken the rule.
       - Teeth check (do, then revert): temporarily add `import org.springframework.stereotype.Service;` to one `domain/` class (e.g. `rules/domain/RuleEvaluator.java`), run `./gradlew :backend:core:test --tests 'com.zeromail.core.arch.DomainPurityArchTest'`, confirm it FAILS, then revert the import. Note the result in the summary.
    2. `CLAUDE.md` "Conventions" section item 2 ("Backend domain package layout"): replace `application/` with `usecases/`; remove the `service/` mention; keep `model/` out (already not listed as valid); list per-context folders as `domain/` · `usecases/` · `projection/` · `exception/` · `persistence/` (+ `lowlevel/`) · `gateway/` (now `llm` AND `gmail`, for external-service adapters). Add this exact doctrine sentence: *"Zero Mail's backend follows DDD strategic design (Spring Modulith bounded contexts) + DDD tactical patterns in `domain/` (framework-free, ArchUnit-enforced) + a Clean Architecture use-case layer in `usecases/` + Hexagonal ports/adapters (a port interface lives next to its owner; adapters are role-named packages — `persistence/` for DB, `gateway/` for external services); the Dependency Rule is enforced by ArchUnit. Deliberate, documented deviations from textbook: CQRS-lite (Spring Data JPA for writes, Spring Data JDBC for reads), and repository-per-entity rather than repository-per-aggregate-root."*
    3. `CONVENTIONS.md` §2 "Backend domain package layout": same updates — `usecases/` not `application/`; remove `service/` from the valid list; keep `model/` in the anti-pattern list; update example paths (e.g. `core.rules.application.RuleCompileResult` → `core.rules.usecases.RuleCompileResult`, and similar); add the same doctrine sentence near the top of §2.
    4. JetBrains MCP `get_file_problems` on `DomainPurityArchTest.java` (or `./gradlew compileTestJava` as substitute). Run `./gradlew :backend:core:test --tests 'com.zeromail.core.arch.*'` — green.
    5. Stage ONLY: `DomainPurityArchTest.java`, `CLAUDE.md`, `CONVENTIONS.md`. Commit: `arch(core): enforce framework-free domain/; document layering doctrine`.
  </action>
  <verify>
    `./gradlew :backend:core:test --tests 'com.zeromail.core.arch.DomainPurityArchTest'` passes; the documented teeth-check shows the rule FAILS when a framework import is added to a `domain/` class (then reverted); `grep -n 'usecases' CLAUDE.md CONVENTIONS.md` shows the layout sections updated; `grep -n 'application/' CLAUDE.md CONVENTIONS.md` no longer shows the old `core.<ctx>.application` package layout references (`@ApplicationModule` and unrelated text are fine); `grep -c 'DDD strategic design' CLAUDE.md` and `... CONVENTIONS.md` each return 1.
  </verify>
  <done>`DomainPurityArchTest` exists, runs as part of `./gradlew check`, has confirmed teeth (fails on a framework import in `domain/`), and passes against the post-Task-3 tree; `CLAUDE.md` §2 and `CONVENTIONS.md` §2 use `usecases/`, drop `service/`, keep `model/` as an anti-pattern, list `gateway/` for `llm` and `gmail`, and carry the verbatim layering-doctrine sentence; Commit 4 staged only the three listed paths.</done>
</task>

</tasks>

<verification>
- `./gradlew check` passes end-to-end: `compileJava` + `compileTestJava` across `backend/core`, `backend/api`, `backend/worker`; the updated ArchUnit suite (`DomainBoundaryArchTests`, `BillingDomainBoundaryArchTest`, `TriageGmailWriteBoundaryTest`, `RulePreviewWriteBoundaryTest` (relocated), `RulesBoundaryArchTest`, `LlmGatewayBoundaryTest`, `SafetyContractArchTests`, `TenantIsolationArchTests`, `TriageAuditRepositoryBoundaryArchTest`, `LlmRepositoryContentBanTest`); the NEW `DomainPurityArchTest`; Spring Modulith verification (`ZeroMailApiApplicationModulesTest`); and the full backend test suite. OOM caveat: if the test-worker JVM OOMs because a parallel agent is also running on this machine, retry with `--no-daemon --max-workers=1 -Dorg.gradle.jvmargs=-Xmx768m` — that is NOT a real failure and NOT a reason to weaken anything.
- `find backend/core/src/main -type d \( -name service -o -name model -o -name application \)` returns nothing.
- `find backend/core/src/test -type d \( -name service -o -name application \)` returns nothing.
- `git grep -n 'com\.zeromail\.core\.\(rules\|llm\|triage\)\.application' -- backend` returns nothing.
- `git grep -n 'com\.zeromail\.core\.[a-z]*\.service\.' -- backend` returns nothing.
- `git grep -nE 'package com\.zeromail\.core\.[a-z]+\.service;' -- backend` returns nothing.
- `node -e "import('./lint-staged.config.mjs').then(m=>{if(typeof m.default['backend/**/*.java']!=='function')process.exit(1)})"` exits 0; `package.json` has no top-level `lint-staged` block.
- JetBrains MCP `get_file_problems` on touched Java/build files is clean (if MCP unavailable, `./gradlew compileJava compileTestJava` is the substitute — note the substitution).
- Teeth check on `DomainPurityArchTest` performed and reverted; documented in the summary.
- Four commits exist (Commit 3 optionally split per-context), each staged only its explicit paths, the rename isolated from the dissolve and both isolated from docs/arch.
</verification>

<success_criteria>
- All `must_haves.truths` hold.
- `./gradlew check` green (or green under the documented OOM-retry flags).
- No `core.*.service` (main or test), `core.*.model` (empty), or `core.*.application` package directories remain under `backend/core`.
- Re-homing followed the locked table; the stray `rules/service` arch test relocated to `rules/usecases/`; deviations (if any) documented in the summary with rationale (call-site churn).
- Docs (`CLAUDE.md` §2, `CONVENTIONS.md` §2) and `lint-staged.config.mjs` are in place; `package.json` `lint-staged` block removed.
- Commit structure: Commit 1 (cleanup + lint-staged) / Commit 2 (`application/`→`usecases/` rename) / Commit 3 (dissolve `service/` + test relocations + arch-test edits, optionally per-context) / Commit 4 (new ArchUnit rule + docs). Each commit compiles. `git add` used explicit paths only.
</success_criteria>

<output>
After completion, create `.planning/quick/260511-wc4-backend-core-package-restructure-rename-/260511-wc4-SUMMARY.md` documenting: the final re-homing decision for every former `service/` class (and any deviation from the locked table + why), the relocation of `RulePreviewWriteBoundaryTest`, the `package-info.java` files added, the new ArchUnit rule + its teeth-check result, the docs edits, the `lint-staged.config.mjs` content, the commit SHAs, and any OOM-retry that was needed for `./gradlew check`.
</output>
