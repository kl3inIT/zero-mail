# Quick Task 260523-m46: Recent Inbox

**Date:** 2026-05-23
**Status:** In progress

## Goal

Add an Inbox surface to Zero Mail based on the reference UI in `E:\Project\Zero`, but scoped to Gmail's 100 most recent inbox messages with lazy loading. Do not sync or persist the user's full mailbox.

## Tasks

1. Backend recent inbox API
   - Add core Gmail read use case that lists Gmail inbox messages directly via Gmail API.
   - Return metadata page data with a Gmail `nextPageToken`, capped to 100 total messages per session/request flow.
   - Add detail endpoint that fetches one message body on demand without persisting raw content.

2. Frontend inbox UI
   - Add `/inbox` route and sidebar nav item.
   - Build a two-pane inbox: lazy-loaded message list and selected message detail.
   - Use TanStack Query infinite query and existing OpenAPI client patterns.

3. Verification
   - Regenerate OpenAPI/i18n if needed.
   - Run focused backend/frontend checks and inspect UI behavior where feasible.
