# Quick Task 260427-8qe: Phase 1.5 quick cleanup

**Date:** 2026-04-26
**Mode:** quick

## Goal

Remove low-risk Phase 1.5 cleanup work from the heavy OAuth/folder/primitive refactor phase.

## Tasks

### 1. Frontend runtime polish

- Fix the Tailwind font token self-reference in `apps/web/app/globals.css`.
- Add TanStack Query defaults in `apps/web/lib/query-client.tsx` to reduce refetch storms.
- Replace `DeleteAccountDialog` local busy state with the owning mutation's pending state.

### 2. Small backend cleanup

- Make Gmail OAuth additional parameters immutable at the builder boundary.
- Fail loudly on unknown OAuth success registration IDs while the Phase 1.4 dispatcher still exists.

### 3. Verify

- Run targeted frontend checks for the touched web files.
- Run targeted backend compile/problem checks for touched Java files.

## Deferred

- Root layout `/me` cache strategy: keep for Phase 1.5 planning because authenticated RSC caching needs an explicit stale/freshness decision.
- OAuth bundling, folder relocation, and primitive deflation: handled as separate quick/phase work.
