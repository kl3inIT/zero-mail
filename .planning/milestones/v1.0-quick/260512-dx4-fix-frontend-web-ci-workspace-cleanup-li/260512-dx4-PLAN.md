---
quick_id: 260512-dx4
status: ready
created: 2026-05-12
---

# Quick Task 260512-dx4: Fix Frontend Web CI Workspace Cleanup Test

## Goal

Fix PR #29 `Frontend Web` CI by updating the stale workspace cleanup Vitest contract that still expects inline `lint-staged` config in root `package.json`.

## Tasks

1. Update `apps/web/__tests__/workspace/workspace-cleanup.test.ts` to assert the current root-level `lint-staged.config.mjs` file instead of `package.json["lint-staged"]`.
2. Run the failing frontend Vitest file locally.
3. Commit the test fix, quick-task summary, and state update, then push to PR #29.

## Verification

- `pnpm --filter web exec vitest run __tests__/workspace/workspace-cleanup.test.ts`
