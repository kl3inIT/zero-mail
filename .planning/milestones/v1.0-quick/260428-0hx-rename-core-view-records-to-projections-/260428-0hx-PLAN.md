# Quick Task 260428-0hx: Rename Core Views To Projections

**Date:** 2026-04-28
**Mode:** quick

## Goal

Rename backend core read-side `*View` records to `*Projection` and move small controller response mapping helpers into API DTO static factories.

## Tasks

1. Rename `CurrentUserView` to `CurrentUserProjection`.
2. Rename `GmailConnectionView` to `GmailConnectionProjection`.
3. Add `from(...)` factories to response DTOs so controllers do not repeat small `toResponse(...)` mappers.
4. Verify backend compile/tests.

## Rules

- Keep dependency direction `backend/api -> backend/core`; never import API DTOs from core.
- Do not introduce mapper framework or builder boilerplate for two-field/simple DTOs.
- Leave unrelated dirty files untouched, especially untracked `REVIEW.md`.
