---
quick_id: 260514-gy3
slug: fix-github-copilot-review-comments-on-pr
status: in_progress
---

# Quick Task 260514-gy3: Fix GitHub Copilot Review Comments on PR #35

## Goal

Fix the actionable GitHub Copilot review findings before merging Phase 05C.

## Scope

- Count distinct triaged Gmail messages for the analytics "messages triaged" metric while preserving per-action counts for time-saved.
- Keep transient digest dispatch failures retryable instead of marking them as terminal `FAILED`.
- Add/update focused tests that prove both behaviors.
- Do not change the Phase 05C locked digest content window unless test evidence shows it is wrong; the spec already defines prior 24h closed at tenant send hour.

## Verification

- Run focused backend tests for analytics aggregation and digest dispatch retry behavior.
- Run relevant formatter/problem checks for touched Java files.
