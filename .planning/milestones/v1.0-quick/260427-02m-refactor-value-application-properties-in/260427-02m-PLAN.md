# Quick Task 260427-02m: Refactor @Value application properties into @ConfigurationProperties

**Date:** 2026-04-26
**Mode:** quick

## Goal

Replace direct Spring `@Value` injections for application configuration with typed `@ConfigurationProperties` classes.

## Tasks

### 1. Introduce typed configuration properties

Files:
- `backend/api/src/main/java/com/zeromail/api/config/ZeroMailApiProperties.java`
- `backend/core/src/main/java/com/zeromail/core/config/ZeroMailCoreProperties.java`

Action:
- Add one root typed properties holder per owning module.
- Keep API-owned `web` and `cors` nested records in `backend/api`.
- Keep core-owned `crypto` nested record in `backend/core`.
- Keep existing external property names intact while moving defaults out of `@Value` strings.
- Mask crypto secrets in the nested record `toString()`.

Verify:
- Properties bind through Spring Boot configuration property infrastructure.

Done:
- No new `@Value` usage is introduced.

### 2. Replace current `@Value` injection sites

Files:
- `backend/api/src/main/java/com/zeromail/api/security/GoogleOAuthSuccessHandler.java`
- `backend/api/src/main/java/com/zeromail/api/config/CorsConfig.java`
- `backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCryptoConfig.java`
- `backend/api/src/main/java/com/zeromail/api/Application.java`
- `backend/worker/src/main/java/com/zeromail/worker/WorkerApplication.java`
- `backend/core/src/test/java/com/zeromail/core/support/CoreTestApplication.java`
- `backend/api/src/main/resources/application.yml`

Action:
- Inject the module-owned typed properties into the OAuth success handler, CORS config, and refresh-token cipher config.
- Enable configuration properties scanning in the app/test entrypoints that boot core components.

Verify:
- Existing handler behavior still strips trailing slashes and redirects to `/onboarding`.

Done:
- `rg '@Value\\(' backend` finds no production config injections.

### 3. Build/test the affected modules

Files:
- `backend/api/build.gradle.kts`
- `backend/core/build.gradle.kts`
- `backend/api/src/test/java/com/zeromail/api/security/GoogleOAuthSuccessHandlerTest.java`

Action:
- Add Spring Boot configuration processor metadata support where new properties live.
- Update direct unit construction in tests.

Verify:
- Run focused tests or module compile tasks for `backend:api` and `backend:core`.

Done:
- Relevant Gradle verification passes, or any environmental blocker is reported.
