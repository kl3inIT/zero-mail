---
phase: 01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r
verified: 2026-04-27T17:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/5
  gaps_closed:
    - "Gmail provisioning + token persistence run in the SAME transaction as user provisioning (atomic, no separate connect Gmail step)"
    - "Onboarding routes to template selection after provisioning; consent-denied uses /login?error=...; reconnect uses single google registration with prompt=consent"
    - "Surviving REVIEW cleanup is closed or explicitly documented (CLAUDE.md conventions, deferred ceremonies, stack constraints honored)"
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "Production cookie secure: true profile override + REFRESH_TOKEN_KEY_BASE64 deployment-secret resolution"
    addressed_in: "Phase 6 (launch hardening)"
    evidence: "STATE.md deferred ceremonies entry (c) — added in Plan 05 with verification protocol covering prod profile cookie override + deployment-secret fail-fast behavior."
  - truth: "Refresh-token key rotation drill (multi-version decrypt, key v1 to v2 swap)"
    addressed_in: "Phase 2C (or dedicated security-ceremony phase)"
    evidence: "STATE.md deferred ceremonies entry (b) — added in Plan 05; deployment-secret-source-agnostic protocol per HIGH-6 fix."
  - truth: "Pub/Sub OIDC verification ceremony (push receiver authentication)"
    addressed_in: "Phase 2A (push-receiver subsystem)"
    evidence: "STATE.md deferred ceremonies entry (a) — added in Plan 05 with JWKS / aud / email-claim assertions."
human_verification: []
---

# Phase 01.5: Inbox-Zero Alignment Verification Report

**Phase Goal:** Finish the remaining heavy Phase 1.5 work after the quick-task sweep: align with Inbox Zero's bundled Google OAuth model, remove the now-unnecessary two-registration Gmail mismatch architecture, simplify onboarding and consent-denied UX, deflate custom frontend primitives back to raw shadcn/token-aware composition, polish the current landing/login/onboarding/settings/reconnect surfaces, and close surviving REVIEW cleanup that still applies.

**Verified:** 2026-04-27T17:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plans 01.5-06, 01.5-07, 01.5-08)

## Re-verification Summary

Previous status was `gaps_found` (2/5). Three gap-closure plans executed:

- **Plan 06** (CR-01): Removed second `bundledTx.executeWithoutResult` from race-loser catch block in `OAuthProvisioningService`. Added `OAuthProvisioningRaceAtomicityTest` regression test.
- **Plan 07** (CR-02 + CR-03): Changed `ConnectGmailController` from `@PostMapping` to `@GetMapping`. Added `removeAuthorizedClient("google", token.getName())` before both throws in `GoogleOAuthSuccessHandler`. Replaced settings `<form method="post">` with `<Button onClick>` GET navigation.
- **Plan 08** (CR-04): Removed `spring-cloud-gcp-starter-secretmanager` from `build.gradle.kts`, BOM from `buildSrc/zeromail.spring-boot-conventions.gradle.kts`, and `springCloudGcp` version entry from `libs.versions.toml`. Replaced dead `sm://` nested-placeholder with `:?` fail-fast required-env-var pattern.

All three blockers verified closed by direct source inspection. No regressions detected in must-haves #1 and #4.

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Primary Google OAuth registration requests bundled scopes upfront, persists Gmail tokens during login, and removes `google-gmail` registration + mismatch/revocation handlers | ✓ VERIFIED | `application.yml:25-34` — single `google` registration with `openid`, `profile`, `email`, `gmail.modify`, `gmail.settings.basic`. No regression from plans 06-08 (only `OAuthProvisioningService`, `GoogleOAuthSuccessHandler`, `ConnectGmailController`, `settings/page.tsx`, build files touched). |
| 2 | Gmail provisioning + token persistence run in the SAME transaction as user provisioning (atomic, no separate "connect Gmail" step) | ✓ VERIFIED | `OAuthProvisioningService.java` lines 156-167: race-loser catch block contains ONLY `log.warn("event=oauth_provisioning_race")` + `users.findByGoogleSubject(googleSubject).orElseThrow(() -> race)` + `return new BundledProvisioningResult(...)`. Zero `bundledTx.executeWithoutResult` calls inside the catch block. Single-tx contract confirmed. Regression test `OAuthProvisioningRaceAtomicityTest` added (commit `39d0120`). |
| 3 | Onboarding routes to template selection after provisioning; consent-denied uses `/login?error=...`; reconnect uses single `google` registration with `prompt=consent` | ✓ VERIFIED | (a) `ConnectGmailController.java:5` — `@GetMapping("/tenant/connect-gmail")`, not `@PostMapping`; GET requires no CSRF token. (b) `GoogleOAuthSuccessHandler.java:96-102` — `removeAuthorizedClient("google", token.getName())` called before `gmail_scope_required` throw. Lines 116-118 — same cleanup before `consent_denied` throw. (c) `settings/page.tsx:57-59` — `reconnect = () => { window.location.href = getApiUrl('/tenant/connect-gmail') }` is GET navigation. Lines 94-103 — `NOT_CONNECTED` path uses `<Button onClick>` GET navigation; no `<form method="post">` remains. Reconnect path is now end-to-end reachable. |
| 4 | Phase 1.4 frontend wrapper primitives are deflated; retained primitives justify real composition; affected surfaces use raw shadcn + token-aware className | ✓ VERIFIED | `settings/page.tsx` — raw `Card`/`CardHeader`/`CardTitle`/`CardContent`/`CardFooter` chains throughout. Plan 07 replacement of `<form>` with `<Button onClick>` preserves raw shadcn usage (no new wrappers introduced). No regression detected. |
| 5 | Surviving REVIEW cleanup is closed or explicitly documented (`/me` cache, `TemplateCard` cn(), retained `ReconnectPrompt` token variant, CLAUDE.md conventions, deferred verification ceremonies) | ✓ VERIFIED | (a) `backend/api/build.gradle.kts` — no `spring-cloud-gcp` or `secretmanager` anywhere; confirmed by direct file read (10 starters, all standard Spring Boot). (b) `gradle/libs.versions.toml` — no `springCloudGcp` entry; confirmed by direct file read (16 lines total, no GCP). (c) `buildSrc/zeromail.spring-boot-conventions.gradle.kts` — only `spring-boot-dependencies:4.0.6` BOM; no `spring-cloud-gcp-dependencies` BOM. (d) `application.yml:73` — `refresh-token-key-base64: ${REFRESH_TOKEN_KEY_BASE64:?REFRESH_TOKEN_KEY_BASE64 must be supplied...}` — `:?` fail-fast confirmed, no `sm://`, no `spring.cloud.gcp` block anywhere in the 104-line file. CLAUDE.md "No GCP hosting baseline" lock is now honored end-to-end. |

**Score:** 5/5 truths verified

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Production cookie secure: true profile override | Phase 6 (launch hardening) | STATE.md deferred ceremonies entry (c) |
| 2 | Refresh-token key rotation drill | Phase 2C / security-ceremony phase | STATE.md deferred ceremonies entry (b) |
| 3 | Pub/Sub OIDC verification ceremony | Phase 2A (push-receiver subsystem) | STATE.md deferred ceremonies entry (a) |

### Required Artifacts (Re-verification Pass)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `OAuthProvisioningService.java` | No second `bundledTx.executeWithoutResult` in race-loser catch | ✓ VERIFIED | Lines 156-167: catch block is read-only (warn log + findByGoogleSubject + return). CR-01 closed. |
| `GoogleOAuthSuccessHandler.java` | `removeAuthorizedClient("google", token.getName())` before both throws | ✓ VERIFIED | Lines 96-102 (gmail_scope_required path) and lines 116-118 (consent_denied path). CR-02 closed. |
| `ConnectGmailController.java` | `@GetMapping` not `@PostMapping` | ✓ VERIFIED | Line 5: `@GetMapping("/tenant/connect-gmail")`. CR-03 closed. |
| `apps/web/app/(protected)/settings/page.tsx` | No `<form method="post">` for reconnect; GET navigation used | ✓ VERIFIED | Lines 57-59: `window.location.href = getApiUrl('/tenant/connect-gmail')` (DISCONNECTED path). Lines 96-99: `<Button onClick={() => { window.location.href = getApiUrl('/tenant/connect-gmail') }}>` (NOT_CONNECTED path). No `<form>` tag in file. CR-03 closed. |
| `backend/api/build.gradle.kts` | No `spring-cloud-gcp-starter-secretmanager` | ✓ VERIFIED | File has 10 `implementation`/`testImplementation` entries; none contain `cloud`, `gcp`, or `secretmanager`. CR-04 closed. |
| `gradle/libs.versions.toml` | No `springCloudGcp` version or library entry | ✓ VERIFIED | File is 32 lines; no GCP entry found. CR-04 closed. |
| `buildSrc/zeromail.spring-boot-conventions.gradle.kts` | No GCP BOM import | ✓ VERIFIED | File is 16 lines; only BOM is `spring-boot-dependencies:4.0.6`. CR-04 closed. |
| `backend/api/src/main/resources/application.yml` | No `sm://`, no `spring.cloud.gcp`, has `:?` fail-fast | ✓ VERIFIED | Line 73 uses `:?` pattern with clear error message. No `sm://` or `spring.cloud.gcp` in 104-line file. CR-04 closed. |

### Key Link Verification (Re-verification Pass)

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `GoogleOAuthSuccessHandler` throw sites (2x) | `authorizedClients.removeAuthorizedClient` | `token.getName()` (CR-02 fix) | ✓ WIRED | Both throw paths clean up partial AuthorizedClient under correct principal name. |
| Settings page reconnect handlers (2x) | `ConnectGmailController` GET endpoint | `window.location.href` GET navigation | ✓ WIRED | DISCONNECTED path (line 58) and NOT_CONNECTED path (line 98) both emit GET to `/tenant/connect-gmail`. |
| `ConnectGmailController` GET | `/oauth2/authorization/google?reconnect=true` | `res.sendRedirect` (line 21) | ✓ WIRED | No CSRF barrier on GET; redirect reaches `GoogleAuthorizationRequestResolver` which injects `prompt=consent`. |
| Race-loser catch block | Winner state (read-only) | `users.findByGoogleSubject` + return | ✓ WIRED | No write; single-tx invariant preserved. |

### Anti-Patterns Check (Remaining Warnings — not blockers)

The following warnings from the initial verification remain unchanged. None block the phase goal.

| File | Pattern | Severity | Status |
|------|---------|----------|--------|
| `OAuthProvisioningService.java:174-198` | `findOrCreateGoogleUser` retained `@Deprecated(forRemoval=true)` with no live callers | ⚠ Warning | Deferred to Phase 02 per WR-01 comment in source |
| `GoogleOAuthSuccessHandler.java:75` | Default target URL built via string concat | ⚠ Warning | Deferred per WR-04 |
| `LoginRedirectAuthenticationFailureHandler.java:51,61,73` | Login URL via string concat not `UriComponentsBuilder` | ⚠ Warning | Deferred per WR-05 |
| `SecurityConfig.java:13` | `@Profile("!test")` excludes filter chain from integration tests | ⚠ Warning | Documented as WR-06 deferred |
| `apps/web/__tests__/features/account/me-cache-dedupe.test.ts:80-83` | Weak dedupe assertion (`toBeGreaterThanOrEqual(1)`) | ⚠ Warning | Deferred per WR-03 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status |
|-------------|-------------|-------------|--------|
| (no new requirements) | — | Phase 01.5 pivots AUTH-01/AUTH-02 behavior; no new product requirements declared. | n/a |

### Human Verification Required

None. All five must-haves verified by direct source inspection.

### Gaps Summary

No gaps remain. All three previously identified BLOCKER defects (CR-01, CR-02+CR-03, CR-04) are closed and confirmed by direct file reads:

- **CR-01 (single-tx violation):** Closed by Plan 06. Race-loser catch block in `OAuthProvisioningService.java` has zero `bundledTx.executeWithoutResult` calls; it is a pure read returning winner state.
- **CR-02 (cleanup with anonymous principal):** Closed by Plan 07. `GoogleOAuthSuccessHandler.java` calls `removeAuthorizedClient("google", token.getName())` on both throw paths before the exception propagates.
- **CR-03 (CSRF defect / POST verb):** Closed by Plan 07. `ConnectGmailController` is now `@GetMapping`. Both reconnect callers in `settings/page.tsx` use `window.location.href` GET navigation. No `<form method="post">` remains.
- **CR-04 (GCP starter contradicts CLAUDE.md):** Closed by Plan 08. `spring-cloud-gcp-starter-secretmanager` removed from `build.gradle.kts`. GCP BOM removed from `buildSrc`. `springCloudGcp` entry removed from `libs.versions.toml`. `sm://` placeholder replaced with `:?` fail-fast in `application.yml`. CLAUDE.md "No GCP hosting baseline" lock honored end-to-end.

---

_Verified: 2026-04-27T17:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes — after gap closure plans 01.5-06, 01.5-07, 01.5-08_
