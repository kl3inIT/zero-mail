---
phase: "01.5"
plan: 8
subsystem: backend-config
tags: [gcp-removal, security, build-cleanup, cr-04]
dependency_graph:
  requires: []
  provides: [clean-vps-build, fail-fast-secret-resolution]
  affects: [backend/api, buildSrc, gradle/libs.versions.toml]
tech_stack:
  removed:
    - "spring-cloud-gcp-starter-secretmanager (com.google.cloud:spring-cloud-gcp-starter-secretmanager)"
    - "spring-cloud-gcp-dependencies BOM 8.0.2 (from buildSrc conventions)"
    - "springCloudGcp version entry (libs.versions.toml)"
  patterns:
    - ":? Spring Boot fail-fast required-property pattern for REFRESH_TOKEN_KEY_BASE64"
key_files:
  modified:
    - backend/api/build.gradle.kts
    - buildSrc/src/main/kotlin/zeromail.spring-boot-conventions.gradle.kts
    - gradle/libs.versions.toml
    - backend/api/src/main/resources/application.yml
decisions:
  - "Removed spring-cloud-gcp-starter-secretmanager and BOM entirely — CLAUDE.md 'No GCP hosting baseline' lock honored"
  - "REFRESH_TOKEN_KEY_BASE64 now uses :? fail-fast — missing env var gives clear startup error instead of opaque base64 failure"
  - "buildSrc BOM removal scoped as Rule 2 (missing critical cleanup) — leaving the BOM would silently keep GCP in dependency management"
metrics:
  duration: "12min"
  completed: "2026-04-27T16:49:00Z"
  tasks: 2
  files_modified: 4
---

# Phase 01.5 Plan 08: Remove spring-cloud-gcp + sm:// Placeholder Summary

**One-liner:** Removed `spring-cloud-gcp-starter-secretmanager` dependency and BOM entirely, replaced dead `sm://` nested-placeholder with `:?` fail-fast required-env-var pattern (CR-04 closure).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Remove spring-cloud-gcp from build + BOM | f4917fb | backend/api/build.gradle.kts, buildSrc/zeromail.spring-boot-conventions.gradle.kts, gradle/libs.versions.toml |
| 2 | Replace sm:// placeholder with :? fail-fast | b5d1d82 | backend/api/src/main/resources/application.yml |

## Verification Results

| Check | Result |
|-------|--------|
| `grep "spring-cloud-gcp" backend/api/build.gradle.kts` | 0 matches |
| `grep "secretmanager" backend/api/build.gradle.kts` | 0 matches |
| `grep "spring-cloud-gcp" gradle/libs.versions.toml` | 0 matches |
| `grep "sm://" application.yml` | 0 matches |
| `grep "spring.cloud.gcp" application.yml` | 0 matches |
| `grep "REFRESH_TOKEN_KEY_BASE64:?" application.yml` | 1 match |
| `grep -rn "org.springframework.cloud.gcp" backend/` | 0 matches |
| GCP jars in runtimeClasspath | 0 (gcp/secretmanager/opencensus/conscrypt all gone) |
| `./gradlew :backend:api:compileJava` | BUILD SUCCESSFUL |
| `./gradlew :backend:api:test` | 106 tests, 1 pre-existing failure (CorsIntegrationTest Redis-health) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Cleanup] Removed spring-cloud-gcp BOM from buildSrc + libs.versions.toml**
- **Found during:** Task 1 scan — plan said "No version catalog change needed" but grep found `spring-cloud-gcp-bom` entry in `libs.versions.toml` dòng 21 và `mavenBom("com.google.cloud:spring-cloud-gcp-dependencies:8.0.2")` dòng 11 trong `buildSrc/src/main/kotlin/zeromail.spring-boot-conventions.gradle.kts`
- **Issue:** Leaving the BOM import in buildSrc would keep GCP dependency management active for ALL backend modules even after removing the starter, allowing future accidental re-introduction without explicit version declarations
- **Fix:** Removed `springCloudGcp = "8.0.2"` + `spring-cloud-gcp-bom` library entry from `libs.versions.toml`; removed `mavenBom("com.google.cloud:spring-cloud-gcp-dependencies:8.0.2")` from `buildSrc/src/main/kotlin/zeromail.spring-boot-conventions.gradle.kts`
- **Files modified:** gradle/libs.versions.toml, buildSrc/src/main/kotlin/zeromail.spring-boot-conventions.gradle.kts
- **Commit:** f4917fb

## W-4 Mitigation Confirmation

`ApiPostgresTestBase.java` already supplies `zeromail.crypto.refresh-token-key-base64` via `@DynamicPropertySource` (dòng 48-49) before the `:?` was applied. The test environment supplies the dummy key `AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=`, so ApplicationContext startup never reaches the `:?` fail-fast — confirmed by 105/106 tests passing (1 pre-existing `CorsIntegrationTest` Redis-health failure unchanged).

## Known Stubs

None — all wiring is real (env-var pattern, no placeholder data).

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. Changes are build-config and property-file only.

## Self-Check: PASSED

- [x] `f4917fb` exists in git log
- [x] `b5d1d82` exists in git log
- [x] `backend/api/build.gradle.kts` — no spring-cloud-gcp
- [x] `gradle/libs.versions.toml` — no springCloudGcp
- [x] `buildSrc/src/main/kotlin/zeromail.spring-boot-conventions.gradle.kts` — no GCP BOM
- [x] `backend/api/src/main/resources/application.yml` — no sm://, no spring.cloud.gcp block, has :? pattern
