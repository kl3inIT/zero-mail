---
phase: 01.3
plan: 08
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md
autonomous: false
requirements: []
success_criterion_refs: [1, 2, 3, 4, 5, 6, 7]
must_haves:
  truths:
    - "All 6 Wave 0 test files exit 0 (FULL GREEN)."
    - "tsc --noEmit, vitest, i18n:check, Playwright e2e all pass."
    - "proxy.ts as unknown as cast STILL compiles after the full refactor."
    - "Husky pre-commit hook fires on a deliberate broken-i18n commit attempt — verified on a DISPOSABLE BRANCH (REVIEWS Revision 7), no real history pollution."
    - "Tailwind 4 class sorting via prettier-plugin-tailwindcss applies on a staged TSX file — verified via lint-staged dry-run or disposable branch."
    - "Visual smoke of /, /docs, /docs/getting-started passes manually."
    - "All 7 ROADMAP success criteria literally satisfied."
    - "VALIDATION.md frontmatter flipped to nyquist_compliant: true and wave_0_complete: true."
    - "generate:api round-trip produces empty diff against committed apps/web/lib/api/schema.d.ts (REVIEWS Revision 7) — Phase 1.3 doesn't touch backend OpenAPI surface."
  artifacts:
    - path: ".planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md"
      provides: "Updated frontmatter + verification map populated with green status; schema-diff evidence logged"
  key_links:
    - from: "Phase 1.3 closure"
      to: "/gsd-verify-work"
      via: "All success criteria satisfied; ready for verifier subagent"
      pattern: "/gsd-verify-work 01.3"
---

> **Shell:** bash (Git Bash on Windows). Tasks use `git checkout -b`, `git reset --hard`, `git branch -D`. PowerShell users: invoke via `bash -c '...'` or use Git Bash directly. (REVIEWS Revision 8 — Codex MEDIUM shell portability)

<objective>
Wave 6 — Final verification + cleanup. Run the full automated suite (tsc, vitest, i18n,
Playwright); execute the manual smokes (Husky hook fires on disposable branch — REVIEWS
Revision 7; Tailwind class sort applies; visual landing/docs render); literally verify
each of the 7 ROADMAP success criteria; gate `nyquist_compliant: true` on empty
schema.d.ts diff (REVIEWS Revision 7); flip VALIDATION.md frontmatter.

**Reviews-mode revisions applied:**
- Revision 7 (Codex LOW + OpenCode LOW): replace the manual real-commit hook tests with
  DISPOSABLE-BRANCH tests (`git checkout -b gsd/phase-01.3-hook-test`, deliberate edit,
  expect blocked commit, `git reset --hard`, `git checkout` back, `git branch -D`).
  Document the temp-branch convention. Alternative: use `pnpm exec lint-staged --debug
  --shell` against staged-but-not-committed files for a dry-run.
- Revision 7 (OpenCode LOW): strengthen `nyquist_compliant: true` gate. If
  `pnpm generate:api` produces a NON-EMPTY diff against `apps/web/lib/api/schema.d.ts`,
  surface diff size + first 10 lines in `01.3-08-SUMMARY.md` AND BLOCK the
  `nyquist_compliant: true` flip until human acks. Expected state is empty diff.
- Revision 9 (OpenCode MEDIUM): relax brittle "byte-identical" criteria where the
  literal is implementation phrasing. Keep grep-verifiable invariants where the literal
  IS the contract (e.g., `as unknown as`, `compileMDX`, `D-D1 MANDATE`, regex literals).

Purpose: Single source-of-truth verification gate. Plan 08 fails closed if anything from
Plans 01-07 regressed.

Output: Phase 1.3 closure-ready; VALIDATION.md updated with all evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-CONTEXT.md
@.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md
@.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-REVIEWS.md
@apps/web/proxy.ts
@apps/web/__tests__/architecture/route-groups.test.ts
@apps/web/__tests__/architecture/feature-folders.test.ts
@apps/web/__tests__/api/isomorphic-shape.test.ts
@apps/web/__tests__/api/server-safe-client.test.ts
@apps/web/__tests__/docs/mdx-pipeline.test.ts
@apps/web/__tests__/workspace/workspace-cleanup.test.ts
@apps/web/__tests__/e2e/route-smoke.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite + literal-verify all 7 ROADMAP success criteria + schema-diff gate</name>
  <files>.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md</files>
  <read_first>
    .planning/ROADMAP.md (lines 89-101 — Phase 1.3 7 success criteria),
    .planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md (current frontmatter + verification map),
    apps/web/proxy.ts (full — confirm `as unknown as` cast still present),
    .planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-REVIEWS.md (Revision 7 schema-diff gate)
  </read_first>
  <action>
**Step A — Run full automated suite** from repo root:

```bash
pnpm --filter zeromail-web exec tsc --noEmit
pnpm --filter zeromail-web test -- --run
pnpm --filter zeromail-web run i18n:check
pnpm --filter zeromail-web run test:e2e
```

All four must exit 0. If any fail, surface as deviation; do NOT proceed.

**Step B — Run each Wave 0 test file individually** to confirm GREEN (now 6 files
including `server-safe-client.test.ts` from REVIEWS Revision 1):

```bash
pnpm --filter zeromail-web test apps/web/__tests__/architecture/route-groups.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/architecture/feature-folders.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/api/isomorphic-shape.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/api/server-safe-client.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/docs/mdx-pipeline.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/workspace/workspace-cleanup.test.ts -- --run
```

All six must exit 0.

**Step C — Confirm proxy.ts cast preservation** (D-E5 invariant — load-bearing literal,
REVIEWS Revision 9 keeps grep enforcement):

```bash
grep -c "as unknown as" apps/web/proxy.ts
```

Must return >= 2.

**Step D — Literal-check each ROADMAP success criterion**. For each, confirm the required
artifact:

1. Route groups + clean URLs — verify directories `(public)/`, `(auth)/`, `(protected)/` exist; clean URL routing verified by Plan 05 Task 4 route-smoke (re-run if needed).
2. Feature folders with api/components/hooks subfolders — verify all 5 feature roots exist with the 3 subdirs each AND with the per-endpoint api/ files (status, disconnect, deleteAccount, selectTemplate, complete) plus their hooks (REVIEWS Revision 1 expansion).
3. Shared UI primitives in components/ui — verify directory still exists with shadcn primitives.
4. Duplicate workspace artifacts removed — verify apps/web/pnpm-lock.yaml + apps/web/pnpm-workspace.yaml absent; root pnpm-workspace.yaml has ignoredBuiltDependencies.
5. Prettier + Husky + lint-staged configured — verify .husky/pre-commit, .prettierrc.json, .prettierignore exist; root package.json has prepare:"husky" + lint-staged config; lint-staged i18n:check is in STRICT mode (NO `|| true`) per Plan 07 Task 3 (REVIEWS Revision 3).
6. lib/api singleton + per-feature api/ — verify lib/api/client.ts is server-safe (REVIEWS Revision 1 server-safe-client test); features/<name>/api/ contain ALL endpoint-specific calls (REVIEWS Revision 1 — Codex HIGH #1); route pages no longer call api.GET/POST/DELETE inline.
7. Public landing + multi-page docs — verify (public)/page.tsx, (public)/docs/page.tsx, (public)/docs/[slug]/page.tsx exist; 4 sample MDX files; (public)/page.tsx uses `<Link className={buttonVariants(...)}>` per REVIEWS Revision 2.

**Step E — Schema-diff gate (REVIEWS Revision 7).** Run:

```bash
pnpm --filter zeromail-web run generate:api
git diff --stat apps/web/lib/api/schema.d.ts
git diff apps/web/lib/api/schema.d.ts | head -20
```

Expected: `git diff --stat` reports `0 files changed` OR an empty diff. If non-empty:

1. Capture the diff size (lines added + removed) and first 10 lines in
   `01.3-08-SUMMARY.md`.
2. **BLOCK the `nyquist_compliant: true` flip** until a human acknowledges that the
   schema diff is intentional (e.g., backend OpenAPI surface changed independently of
   Phase 1.3). Surface as a checkpoint in Task 2.

If empty: proceed.

**Step F — Update `01.3-VALIDATION.md`**:
- Flip frontmatter `nyquist_compliant: false` -> `true` (ONLY if schema-diff gate passed in Step E).
- Flip frontmatter `wave_0_complete: false` -> `true`.
- Update the `## Validation Sign-Off` section: tick all 6 boxes.
- Update `**Approval:**` line to `approved YYYY-MM-DD by /gsd-execute-phase 01.3 Plan 08`.
- Populate the per-task verification map table with one row per Wave 0 test file (status `green`).
- Add a row for the Plan 05 route-smoke (status `green`).
- Add a `## Schema Diff` sub-section with the result of Step E (empty diff / non-empty + ack).
  </action>
  <verify>
    <automated>pnpm --filter zeromail-web exec tsc --noEmit && pnpm --filter zeromail-web test -- --run && pnpm --filter zeromail-web run i18n:check && pnpm --filter zeromail-web run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - All 6 Wave 0 test files exit 0 individually (REVIEWS Revision 1 added server-safe-client.test.ts; REVIEWS Revision 4 fixed mdx-pipeline.test.ts skipIf gating).
    - Full suite (`pnpm --filter zeromail-web test -- --run`) exits 0.
    - `pnpm --filter zeromail-web exec tsc --noEmit` exits 0.
    - `pnpm --filter zeromail-web run i18n:check` exits 0.
    - `pnpm --filter zeromail-web run test:e2e` exits 0 (Playwright LanguageSwitcher smoke + route-smoke from Plan 05 both GREEN).
    - `apps/web/proxy.ts` contains literal `as unknown as` (REVIEWS Revision 9 keeps load-bearing literal grep).
    - All 7 ROADMAP success-criteria artifacts verified present.
    - `pnpm generate:api` produces EMPTY diff against committed `apps/web/lib/api/schema.d.ts` (REVIEWS Revision 7) — OR diff is non-empty AND human acknowledged via Task 2 checkpoint AND SUMMARY.md captures diff size + first 10 lines.
    - `01.3-VALIDATION.md` frontmatter has `nyquist_compliant: true` (only if schema-diff gate passed) and `wave_0_complete: true`.
    - `01.3-VALIDATION.md` Validation Sign-Off section has all 6 boxes ticked.
    - `01.3-VALIDATION.md` includes Schema Diff sub-section logging the Step E result.
  </acceptance_criteria>
  <done>Full automated suite green; ROADMAP criteria verified; schema-diff gate passed; VALIDATION.md updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke — Husky hook fires on DISPOSABLE BRANCH (REVIEWS Revision 7) + Tailwind class sort + visual landing/docs</name>
  <what-built>
    Phase 1.3 architecture refactor complete. Three manual checks per
    01.3-VALIDATION.md §"Manual-Only Verifications". REVIEWS Revision 7 changes the hook
    test convention from "real commits on the working branch" to "disposable branch
    with explicit reset". This avoids polluting working-branch history with deliberate
    drift commits.
  </what-built>
  <how-to-verify>
    > **Shell:** all `git` commands below are bash. Use Git Bash on Windows.

    **Check A — Husky pre-commit hook fires on broken i18n (REVIEWS Revision 7 — disposable branch convention):**

    1. From repo root, capture current branch and create disposable branch:
       ```bash
       CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
       git checkout -b gsd/phase-01.3-hook-test-A
       ```

    2. Run `pnpm install` (idempotent).
    3. Open `apps/web/messages/vi.json` and DELETE one key that exists in en.json
       (e.g., remove `landing.continueSetupCta`).
    4. `git add apps/web/messages/vi.json`
    5. `git commit -m "test: deliberate i18n parity drift"` — EXPECTED: commit BLOCKED
       with `pnpm i18n:check` failure output. (Plan 07 Task 3 already exercised this on
       its own temp branch; this is a final confirmation against the merged HEAD.)
    6. Cleanup:
       ```bash
       git reset --hard HEAD
       git checkout "$CURRENT_BRANCH"
       git branch -D gsd/phase-01.3-hook-test-A
       ```

    **Check B — Tailwind class sort applies via prettier-plugin-tailwindcss (REVIEWS Revision 7 — disposable branch OR lint-staged dry-run):**

    Option B1 (preferred — dry-run, no git state):
    ```bash
    # Pick any TSX file in apps/web/app/(public)/. Add a deliberately misordered
    # className: <div className="p-4 m-2 flex">test</div>. DO NOT commit.
    git add <that-file>
    pnpm exec lint-staged --debug --shell 2>&1 | grep -i "prettier\|tailwind" | head -20
    # Confirm Prettier ran on the file. Inspect the file post-run: classes should be
    # sorted (e.g., "m-2 flex p-4").
    git restore --staged --worktree <that-file>
    ```

    Option B2 (disposable branch fallback):
    ```bash
    git checkout -b gsd/phase-01.3-hook-test-B
    # Make the misordered className edit, git add, git commit.
    # The commit should SUCCEED but Prettier rewrites the file in the commit.
    # Verify: git show HEAD -- <file> shows sorted classes.
    git checkout "$CURRENT_BRANCH"
    git branch -D gsd/phase-01.3-hook-test-B
    ```

    **Check C — Visual smoke of landing + docs (ROADMAP #7):**
    1. From repo root: `pnpm --filter zeromail-web dev`.
    2. Open http://localhost:3000/ — confirm landing renders with header (Zero Mail logo + Docs link + Sign in link + LanguageSwitcher), heading "Zero Mail", tagline (Vietnamese by default), and "Đăng nhập" CTA.
    3. Click LanguageSwitcher -> English. Page swaps; tagline becomes English; CTA reads "Sign in".
    4. Open http://localhost:3000/docs — confirm list of 2 docs (Getting Started, Privacy in current locale) sorted by order.
    5. Click "Getting Started". Confirm MDX article renders with h1 + body + "Back to docs" link.
    6. Switch language while on /docs/getting-started — confirm content swaps.
    7. Open http://localhost:3000/docs/non-existent — confirm 404 not crash.
    8. Open http://localhost:3000/docs/../etc/passwd (URL-encoded if needed) — confirm 404 (slug regex blocks).
    9. **REVIEWS Revision 6 consistency check**: temporarily edit one of the 4 sample MDX files' frontmatter slug to a different value, save, hit the URL — confirm 404. Restore the file.

    **Check D — schema-diff acknowledgment (REVIEWS Revision 7), only if Task 1 Step E found non-empty diff:**

    1. Inspect the diff in SUMMARY.md (size + first 10 lines).
    2. If the diff is intentional (backend OpenAPI surface changed in a parallel phase), acknowledge by typing "schema-diff acknowledged: <reason>" in the resume signal.
    3. If the diff is NOT intentional, type "schema-diff blocked: investigate before flipping nyquist_compliant" — do NOT proceed.

    Type "approved" if all relevant checks pass; otherwise describe the failure.
  </how-to-verify>
  <resume-signal>Type "approved", "schema-diff acknowledged: <reason>", or describe the failing check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer -> running app | Manual visual confirmation depends on dev server boot. |
| Disposable branch -> main branch history | Task 2 Check A + B use disposable branches with explicit reset to avoid history pollution (REVIEWS Revision 7). |
| Backend OpenAPI surface -> apps/web/lib/api/schema.d.ts | generate:api round-trip may dirty the tree; Phase 1.3 is supposed to be a no-op (REVIEWS Revision 7 schema-diff gate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.3.08-01 | R (Repudiation) | Phase declared complete with regressed automated check | mitigate | Task 1 acceptance criterion enumerates each automated check; if any exits non-zero the plan fails closed. |
| T-1.3.08-02 | T (Tampering) | Pre-commit hook bypassed via --no-verify | accept | --no-verify is a documented escape hatch for emergencies. CI re-runs the same checks server-side as a backstop (Phase 1.1 P07 contract). |
| T-1.3.08-03 | T (Tampering) | proxy.ts cast silently rewritten across 8 plans | mitigate | Task 1 Step C grep enforces `as unknown as` literal count >= 2 (REVIEWS Revision 9 — load-bearing literal kept). |
| T-1.3.08-04 | A (Availability) | Polluting working-branch history with deliberate drift commits during Task 2 verification (REVIEWS Revision 7) | mitigate | Disposable-branch convention with explicit `git reset --hard` + `git checkout` back + `git branch -D`. Alternative: `pnpm exec lint-staged --debug --shell` dry-run for non-destructive verification. |
| T-1.3.08-05 | T (Tampering) | nyquist_compliant flipped despite unintended schema.d.ts churn (REVIEWS Revision 7) | mitigate | Task 1 Step E surfaces non-empty diff; Task 2 Check D requires explicit human acknowledgment before the flip proceeds. |
</threat_model>

<verification>
- All 6 Wave 0 tests GREEN.
- All 4 automated suites (tsc, vitest, i18n, Playwright) GREEN.
- 3 manual checks (Husky on disposable branch, Tailwind sort via dry-run, visual) PASS via human checkpoint.
- proxy.ts cast preserved.
- Schema-diff gate either empty OR explicitly acknowledged.
- VALIDATION.md frontmatter flipped (only after gate passes).
</verification>

<success_criteria>
Phase 1.3 closes when:
- All 7 ROADMAP success criteria verified literally.
- Automated suite green.
- Manual checkpoints approved (including schema-diff acknowledgment if non-empty).
- VALIDATION.md frontmatter `nyquist_compliant: true` + `wave_0_complete: true`.
- All 9 REVIEWS revisions (1-9) have evidence trail in SUMMARY.md.
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-08-SUMMARY.md` with the verification matrix (each ROADMAP criterion + which artifact + which command), schema-diff log, and disposable-branch test outcomes.
2. The orchestrator will tick the Phase 1.3 entry in ROADMAP.md and update STATE.md.
3. Recommend `/gsd-verify-work 01.3` for cross-AI review before proceeding to Phase 2A.
</output>
