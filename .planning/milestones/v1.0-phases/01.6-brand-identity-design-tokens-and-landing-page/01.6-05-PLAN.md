---
id: 01.6-05
title: Wave 3 — Sign-in 2-col reskin (AuthTopBar + TrustPanel + LegalFooter) + /terms /privacy stubs
phase: 01.6
plan: 05
type: execute
wave: 3
depends_on: [01.6-00, 01.6-01, 01.6-02, 01.6-03]
files_modified:
  - apps/web/features/auth/components/AuthTopBar.tsx
  - apps/web/features/auth/components/TrustPanel.tsx
  - apps/web/features/auth/components/LegalFooter.tsx
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(public)/terms/page.tsx
  - apps/web/app/(public)/privacy/page.tsx
  - apps/web/i18n/messages/en.json
  - apps/web/i18n/messages/vi.json
autonomous: true
tdd: false
requirements: [1.6-6, 1.6-8, 1.6-9]
must_haves:
  truths:
    - "(auth)/login renders 2-column shell at viewport ≥768px (TrustPanel left desktop-only via hidden md:flex)."
    - "(auth)/login renders single-column form at viewport <768px (TrustPanel hidden, legal footer visible)."
    - "Legal footer (Terms + Privacy + Google API Limited Use disclosure) renders at every viewport beneath OAuth button."
    - "OAuth integration UNCHANGED: <a href={getApiUrl('/oauth2/authorization/google')}> preserved (Phase 1.5 D-A1)."
    - "Error rendering UNCHANGED: KNOWN_ERROR_CODES closed-enum guard preserved (Phase 1.5 D-B3)."
    - "RSC searchParams reading UNCHANGED (Phase 1.5 D-B3)."
    - "auth.login.error.{code}.{title,body} keys UNCHANGED."
    - "/terms and /privacy stub routes return HTTP 200 with non-empty placeholder body."
    - "TrustPanel uses RSC + getTranslations (informational, no client interactivity)."
    - "LegalFooter uses t.rich(...) for inline <Link> + <a> substitution (next-intl rich-text pattern)."
    - "i18n keys for legal.terms.body, legal.googleApiPolicy.body, legal.privacy.placeholderTitle, legal.terms.placeholderTitle land in lock-step VI/EN."
    - "e2e/login-shell.spec.ts + e2e/legal-stubs.spec.ts contracts satisfied (verified manually + via Wave 4 visual sweep)."
  artifacts:
    - path: "apps/web/features/auth/components/AuthTopBar.tsx"
      provides: "Auth shell chrome — logo + theme + lang + back link"
      exports: ["default"]
    - path: "apps/web/features/auth/components/TrustPanel.tsx"
      provides: "Sign-in TrustPanel desktop-only with 3 pillars (trust.p1..p3)"
      exports: ["TrustPanel"]
    - path: "apps/web/features/auth/components/LegalFooter.tsx"
      provides: "CASA-compliant legal footer (Terms + Privacy + Google API Limited Use)"
      exports: ["LegalFooter"]
    - path: "apps/web/app/(public)/terms/page.tsx"
      provides: "/terms stub route"
    - path: "apps/web/app/(public)/privacy/page.tsx"
      provides: "/privacy stub route"
  key_links:
    - from: "apps/web/app/(auth)/login/page.tsx"
      to: "Spring Security /oauth2/authorization/google"
      via: "<a href={getApiUrl(...)}> — UNCHANGED Phase 1.5 D-A1 contract"
      pattern: "/oauth2/authorization/google"
    - from: "apps/web/features/auth/components/LegalFooter.tsx"
      to: "/terms + /privacy + https://developers.google.com/terms/api-services-user-data-policy"
      via: "t.rich Link + a substitution"
      pattern: "t\\.rich\\('legal\\."
threat_model:
  trust_boundaries:
    - "browser ↔ Spring Security OAuth endpoint (cross-origin if API host differs)"
    - "(public)/{terms,privacy} ↔ public internet (unauthenticated, no input)"
  threats:
    - id: T-1.6-05-01
      category: Repudiation
      component: "Phase 1.5 OAuth integration"
      disposition: mitigate
      mitigation: "DO NOT change OAuth href, form action, error code keys, or searchParams reading. Visual reskin only — wrap existing form/Card in 2-col shell. Wave 4 visual sweep + git diff scope verification confirms backend integration unchanged."
    - id: T-1.6-05-02
      category: "Information Disclosure (CASA compliance)"
      component: "legal footer visibility"
      disposition: mitigate
      mitigation: "Legal footer (Terms + Privacy + Google API Limited Use disclosure) MUST render at every viewport (320–1280 px). e2e/login-shell.spec.ts asserts this across 5 widths. Required for Phase 6 Google OAuth verification (CASA)."
    - id: T-1.6-05-03
      category: Tampering
      component: "/terms /privacy stub content"
      disposition: accept
      mitigation: "Stubs explicitly mark 'Real legal content lands in Phase 6 launch hardening'. No PII, no input, no data path. Threat surface is zero — public unauthenticated routes."
    - id: T-1.6-05-04
      category: "Information Disclosure (privacy logging)"
      component: "TrustPanel pillar copy"
      disposition: mitigate
      mitigation: "Copy aligned with PROJECT.md privacy contract: no email content stored / auto-send forbidden / BYOK ready. Trust copy is the user-facing manifestation of the privacy posture; tone audit is part of Wave 4 visual sweep."
---

## Plan rationale

This plan reskins `(auth)/login` from utilitarian single-column to 2-column desktop / 1-column mobile, adds the always-visible CASA legal footer, and ships the `/terms` + `/privacy` stub routes the legal links point to. It MUST NOT touch backend integration — OAuth href, error rendering, searchParams reading, error code keys are all Phase 1.5 invariants.

3 tasks: (1) i18n keys (`legal.*`) lock-step + stub route copy keys; (2) AuthTopBar + TrustPanel + LegalFooter + stub routes; (3) login page 2-col reskin + verification.

<objective>
Reskin `(auth)/login` to 2-column desktop / 1-column mobile with TrustPanel (left desktop) + AuthTopBar + form Card + LegalFooter (always visible). Ship `/terms` and `/privacy` stub routes (HTTP 200, placeholder content). Add `legal.*` i18n keys lock-step VI/EN. Preserve every Phase 1.5 backend invariant.

Purpose: CASA-compliant Google OAuth verification baseline. Earnest trust signal on desktop sign-in. Legal links never broken.
Output: 3 new auth components + 2 new stub routes + modified login page + i18n extensions. e2e/login-shell.spec.ts + e2e/legal-stubs.spec.ts contracts satisfied at visual sweep time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-SPEC.md
@.planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-CONTEXT.md
@.planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-RESEARCH.md
@.planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-PATTERNS.md
@apps/web/AGENTS.md
@apps/web/app/(auth)/login/page.tsx
@apps/web/app/(public)/docs/page.tsx
@apps/web/i18n/messages/en.json
@apps/web/i18n/messages/vi.json
@D:/study-materials-summer-2026/EXE202/inbox-zero/apps/web/app/(landing)/login/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Extend i18n with legal.* namespace + stub copy keys</name>
  <files>apps/web/i18n/messages/en.json, apps/web/i18n/messages/vi.json</files>
  <read_first>
    - apps/web/i18n/messages/en.json (current)
    - apps/web/i18n/messages/vi.json (current)
    - .planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-RESEARCH.md §"i18n key shape recommendation" (lines 994–1024) and §"CASA Compliance Footer Recommended Zero Mail port" (lines 948–992)
    - D:/study-materials-summer-2026/EXE202/inbox-zero/apps/web/app/(landing)/login/page.tsx (CASA reference, lines 62–90 — port wording brand-swapped)
  </read_first>
  <action>
DO NOT invoke `frontend-design` skill — JSON-only.

**A. Extend `apps/web/i18n/messages/en.json`** — add `legal` namespace top-level (preserve all existing keys):

```json
"legal": {
  "terms": {
    "body": "By clicking continue, you agree to our <terms>Terms of Service</terms> and <privacy>Privacy Policy</privacy>.",
    "placeholderTitle": "Terms of Service",
    "placeholderBody": "Real legal content will land in Phase 6 launch hardening. This stub exists so the legal-footer link target resolves and Google OAuth verification (CASA) requirements are unblocked."
  },
  "privacy": {
    "placeholderTitle": "Privacy Policy",
    "placeholderBody": "Real privacy policy content will land in Phase 6 launch hardening. PROJECT.md describes the binding privacy contract: no long-term storage of email content, prompts, completions, or embeddings."
  },
  "googleApiPolicy": {
    "body": "Zero Mail's use and transfer of information received from Google APIs to any other app will adhere to <link>Google API Services User Data</link> Policy, including the Limited Use requirements."
  }
}
```

**B. Extend `apps/web/i18n/messages/vi.json`** — must mirror EN structure:

```json
"legal": {
  "terms": {
    "body": "Khi nhấn tiếp tục, bạn đồng ý với <terms>Điều khoản dịch vụ</terms> và <privacy>Chính sách bảo mật</privacy> của chúng tôi.",
    "placeholderTitle": "Điều khoản dịch vụ",
    "placeholderBody": "Nội dung pháp lý đầy đủ sẽ có trong Phase 6 (giai đoạn hardening trước khi launch). Trang stub này tồn tại để các liên kết trong footer pháp lý phân giải đúng và để Zero Mail không bị chặn yêu cầu xác minh Google OAuth (CASA)."
  },
  "privacy": {
    "placeholderTitle": "Chính sách bảo mật",
    "placeholderBody": "Chính sách bảo mật đầy đủ sẽ có trong Phase 6 (giai đoạn hardening trước khi launch). PROJECT.md mô tả cam kết ràng buộc về bảo mật: không lưu trữ lâu dài nội dung email, prompt, câu trả lời, hay embedding."
  },
  "googleApiPolicy": {
    "body": "Việc Zero Mail sử dụng và chuyển giao thông tin nhận được từ Google API sang bất kỳ ứng dụng nào khác sẽ tuân thủ <link>Chính sách bảo mật dữ liệu người dùng của Google API</link>, bao gồm các yêu cầu Sử dụng có giới hạn (Limited Use)."
  }
}
```

The `<terms>...</terms>`, `<privacy>...</privacy>`, `<link>...</link>` placeholders are next-intl rich-text markup — components are passed in code via `t.rich(...)`.

Run i18n:check:
```
pnpm --filter zeromail-web i18n:check
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm i18n:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"legal"' apps/web/i18n/messages/en.json` returns ≥ 1
    - `grep -c '"legal"' apps/web/i18n/messages/vi.json` returns ≥ 1
    - `grep -c '"googleApiPolicy"' apps/web/i18n/messages/en.json` returns ≥ 1
    - `grep -c '"googleApiPolicy"' apps/web/i18n/messages/vi.json` returns ≥ 1
    - `grep -c "<terms>Terms of Service</terms>" apps/web/i18n/messages/en.json` returns ≥ 1
    - `grep -c "<terms>Điều khoản dịch vụ</terms>" apps/web/i18n/messages/vi.json` returns ≥ 1
    - `pnpm --filter zeromail-web i18n:check` exits 0
    - JSON validates: `node -e "JSON.parse(require('fs').readFileSync('apps/web/i18n/messages/en.json','utf8')); JSON.parse(require('fs').readFileSync('apps/web/i18n/messages/vi.json','utf8'))"` exits 0
  </acceptance_criteria>
  <done>legal.* namespace lock-step VI/EN.</done>
</task>

<task type="auto">
  <name>Task 5.2: Create AuthTopBar + TrustPanel + LegalFooter components and /terms /privacy stub routes</name>
  <files>apps/web/features/auth/components/AuthTopBar.tsx, apps/web/features/auth/components/TrustPanel.tsx, apps/web/features/auth/components/LegalFooter.tsx, apps/web/app/(public)/terms/page.tsx, apps/web/app/(public)/privacy/page.tsx</files>
  <read_first>
    - .planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-PATTERNS.md §"AuthTopBar.tsx" (lines 421–433), §"TrustPanel.tsx" (lines 436–467), §"LegalFooter.tsx" (lines 470–547), §"app/(public)/terms/page.tsx and app/(public)/privacy/page.tsx" (lines 617–642)
    - .planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-RESEARCH.md §"Recommended Zero Mail port" (lines 948–992) — verbatim component code for LegalFooter
    - apps/web/i18n/components/LanguageSwitcher.tsx (LanguagesIcon + reuse pattern, D-B2)
    - apps/web/features/landing/components/{TopBar,ZMLogoMark,ThemeToggle}.tsx (Plan 01.6-03 outputs — reuse via deep import)
    - apps/web/app/(public)/docs/page.tsx (stub-style RSC + max-w container — analog for /terms /privacy)
  </read_first>
  <action>
Invoke the `frontend-design` skill BEFORE writing JSX (4 of 5 files have JSX).

**A. Create `apps/web/features/auth/components/AuthTopBar.tsx`** — RSC, similar to TopBar but no Sign-in CTA + back-link instead:

```tsx
import { getLocale, getTranslations } from 'next-intl/server';
import { cookies } from 'next/headers';
import Link from 'next/link';

import { buttonVariants } from '@/components/ui/button';
import ZMLogoMark from '@/features/landing/components/ZMLogoMark';
import { ThemeToggle } from '@/features/landing/components/ThemeToggle';
import { LanguageSwitcher } from '@/i18n/components/LanguageSwitcher';
import { cn } from '@/lib/utils';

import type { AppLocale } from '@/i18n/routing';

type Props = { backHref?: string; children?: React.ReactNode };

export default async function AuthTopBar({ backHref = '/', children }: Props) {
  const t = await getTranslations();
  const locale = (await getLocale()) as AppLocale;
  const cookieStore = await cookies();
  const theme: 'light' | 'dark' = cookieStore.get('zm-theme')?.value === 'dark' ? 'dark' : 'light';

  return (
    <header className="border-border border-b" style={{ background: 'var(--nav-bg)' }}>
      <nav className="mx-auto flex max-w-5xl items-center justify-between gap-3 px-4 py-3">
        <Link href={backHref} aria-label={t('nav.logoLabel')} className="text-foreground hover:text-accent flex items-center gap-2 transition-colors">
          <ZMLogoMark size={20} className="text-accent" />
          <span className="text-base">
            <span className="font-medium">zero</span>
            <span className="font-light">mail</span>
          </span>
        </Link>
        <div className="flex items-center gap-1 sm:gap-2">
          <ThemeToggle currentTheme={theme} />
          <LanguageSwitcher currentLocale={locale} authenticated={false} variant="compact" />
        </div>
      </nav>
      {children ? <div className="border-border bg-background border-t px-4 py-3">{children}</div> : null}
    </header>
  );
}
```

Notes:
- `children` slot below the chrome row hosts `<StepIndicator>` on onboarding pages (Plan 06).
- No Sign-in CTA (user is in auth flow already).
- `backHref` defaults to `/` (landing); onboarding pages can omit / override.

**B. Create `apps/web/features/auth/components/TrustPanel.tsx`** — RSC, desktop-only via `hidden md:flex`:

```tsx
import { getTranslations } from 'next-intl/server';

export async function TrustPanel() {
  const t = await getTranslations();
  const pillars = ['p1', 'p2', 'p3'] as const;

  return (
    <aside
      className="hidden md:flex md:flex-col md:gap-8 md:px-12 md:py-16 lg:px-16"
      style={{ background: 'var(--trust-bg)', color: 'var(--trust-text)' }}
    >
      <div className="flex flex-col gap-2">
        <span className="text-xs font-medium uppercase tracking-wider opacity-70">
          {t('trust.eyebrow')}
        </span>
      </div>
      <ul className="flex flex-col gap-8">
        {pillars.map((p) => (
          <li key={p} className="flex flex-col gap-2">
            <CheckIcon className="size-5 shrink-0" />
            <h3 className="text-lg font-semibold">{t(`trust.${p}.title` as never)}</h3>
            <p className="text-sm leading-relaxed opacity-80">{t(`trust.${p}.body` as never)}</p>
          </li>
        ))}
      </ul>
    </aside>
  );
}

function CheckIcon({ className }: { className?: string }) {
  return (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2.5} strokeLinecap="round" strokeLinejoin="round" className={className} aria-hidden="true">
      <path d="M5 13l4 4L19 7" />
    </svg>
  );
}
```

Notes:
- `hidden md:flex` enforces desktop-only (D from CONTEXT — TrustPanel desktop only on sign-in).
- Inline `style={{ background: 'var(--trust-bg)' }}` consumes the chrome helper token.
- Inline CheckIcon (no lucide-react — LM-3 family).
- Uses `trust.p1..p3` (sign-in pillars from PROJECT.md privacy contract).
- DO NOT import `<Alert>` — TrustPanel is informational, not status (PATTERNS.md note).

**C. Create `apps/web/features/auth/components/LegalFooter.tsx`** — RSC with `t.rich(...)`:

```tsx
import { getTranslations } from 'next-intl/server';
import Link from 'next/link';

export async function LegalFooter() {
  const t = await getTranslations();
  return (
    <div className="text-muted-foreground space-y-3 text-center text-xs leading-relaxed">
      <p>
        {t.rich('legal.terms.body', {
          terms: (chunks) => (
            <Link href="/terms" className="hover:text-foreground underline underline-offset-4">
              {chunks}
            </Link>
          ),
          privacy: (chunks) => (
            <Link href="/privacy" className="hover:text-foreground underline underline-offset-4">
              {chunks}
            </Link>
          ),
        })}
      </p>
      <p>
        {t.rich('legal.googleApiPolicy.body', {
          link: (chunks) => (
            <a
              href="https://developers.google.com/terms/api-services-user-data-policy"
              className="hover:text-foreground underline underline-offset-4"
              target="_blank"
              rel="noopener noreferrer"
            >
              {chunks}
            </a>
          ),
        })}
      </p>
    </div>
  );
}
```

Notes:
- `t.rich(...)` substitutes `<terms>` / `<privacy>` / `<link>` placeholders with React components.
- External link uses `target="_blank" rel="noopener noreferrer"` (security baseline).
- Token classes only: `text-muted-foreground`, `hover:text-foreground`.
- ALWAYS visible at every viewport (no `md:` class) — REQ-1.6-8 hard requirement.

**D. Create `apps/web/app/(public)/terms/page.tsx`** — stub:

```tsx
import { getTranslations } from 'next-intl/server';

export default async function TermsPage() {
  const t = await getTranslations();
  return (
    <main className="mx-auto max-w-3xl px-4 py-8 sm:py-12">
      <h1 className="text-foreground mb-6 text-3xl font-semibold tracking-tight">
        {t('legal.terms.placeholderTitle')}
      </h1>
      <p className="text-muted-foreground leading-relaxed">
        {t('legal.terms.placeholderBody')}
      </p>
    </main>
  );
}
```

**E. Create `apps/web/app/(public)/privacy/page.tsx`** — stub:

```tsx
import { getTranslations } from 'next-intl/server';

export default async function PrivacyPage() {
  const t = await getTranslations();
  return (
    <main className="mx-auto max-w-3xl px-4 py-8 sm:py-12">
      <h1 className="text-foreground mb-6 text-3xl font-semibold tracking-tight">
        {t('legal.privacy.placeholderTitle')}
      </h1>
      <p className="text-muted-foreground leading-relaxed">
        {t('legal.privacy.placeholderBody')}
      </p>
    </main>
  );
}
```

Both stubs live in `(public)/` group → inherit TopBar + Footer chrome from `(public)/layout.tsx` (Plan 03 Task 3.3) — confirmed acceptable per LM-6 (intended behavior). DO NOT redefine `<html>`/`<body>`. DO NOT skip the route group.

After saving all 5 files, run:
```
pnpm --filter zeromail-web i18n:check && pnpm --filter zeromail-web tsc --noEmit
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm i18n:check && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/features/auth/components/AuthTopBar.tsx` exists
    - File `apps/web/features/auth/components/TrustPanel.tsx` exists
    - File `apps/web/features/auth/components/LegalFooter.tsx` exists
    - File `apps/web/app/(public)/terms/page.tsx` exists
    - File `apps/web/app/(public)/privacy/page.tsx` exists
    - `grep -c "hidden md:flex" apps/web/features/auth/components/TrustPanel.tsx` returns ≥ 1 (desktop-only)
    - `grep -c "trust.p1\|trust.p2\|trust.p3" apps/web/features/auth/components/TrustPanel.tsx` returns ≥ 1 (sign-in pillars, not landing c1..c4)
    - `grep -c "t.rich" apps/web/features/auth/components/LegalFooter.tsx` returns ≥ 2 (terms.body + googleApiPolicy.body)
    - `grep -c "developers.google.com/terms/api-services-user-data-policy" apps/web/features/auth/components/LegalFooter.tsx` returns ≥ 1
    - `grep -c "rel=\"noopener noreferrer\"" apps/web/features/auth/components/LegalFooter.tsx` returns ≥ 1
    - `grep -c "lucide-react" apps/web/features/auth/components/TrustPanel.tsx` returns 0
    - `grep -c "<Button" apps/web/features/auth/components/AuthTopBar.tsx` returns 0 (LM-3)
    - `grep -cE "text-stone-|bg-amber-|border-blue-|text-gray-" apps/web/features/auth/components/` returns 0 (no raw colors)
    - `grep -c "legal.terms.placeholderTitle" apps/web/app/\(public\)/terms/page.tsx` returns ≥ 1
    - `grep -c "legal.privacy.placeholderTitle" apps/web/app/\(public\)/privacy/page.tsx` returns ≥ 1
    - `pnpm --filter zeromail-web i18n:check` exits 0
    - `pnpm --filter zeromail-web tsc --noEmit` exits 0
    - `git diff --stat backend/` reports zero lines changed
  </acceptance_criteria>
  <done>3 auth components + 2 stub routes shipped; legal.* keys wired; no raw colors; no Button; no lucide-react.</done>
</task>

<task type="auto">
  <name>Task 5.3: Reskin (auth)/login/page.tsx to 2-col desktop / 1-col mobile (preserve OAuth + error rendering)</name>
  <files>apps/web/app/(auth)/login/page.tsx</files>
  <read_first>
    - apps/web/app/(auth)/login/page.tsx (current 99 lines — full source — PRESERVE searchParams handling, KNOWN_ERROR_CODES guard, Alert variant=destructive, OAuth href, error code keys)
    - .planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-PATTERNS.md §"app/(auth)/login/page.tsx — 2-col reskin (preserve OAuth)" (lines 862–917)
    - .planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-CONTEXT.md (memory: bundled OAuth flow Phase 1.5 D-A1 — UNCHANGED)
    - apps/web/features/auth/components/AuthTopBar.tsx (Task 5.2 output)
    - apps/web/features/auth/components/TrustPanel.tsx (Task 5.2 output)
    - apps/web/features/auth/components/LegalFooter.tsx (Task 5.2 output)
  </read_first>
  <action>
Invoke the `frontend-design` skill BEFORE writing JSX.

Open `apps/web/app/(auth)/login/page.tsx`. The current shape (Phase 1.5):
- Imports + KNOWN_ERROR_CODES tuple + isKnownError helper
- `export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> })`
- Awaits searchParams, gets translators
- Returns `<main className="flex min-h-screen items-center justify-center p-6">` with Alert + Card + OAuth `<a>` + safety bullet list

PRESERVE THESE VERBATIM (NO TEXT CHANGES):
- All imports (KNOWN_ERROR_CODES, isKnownError, getApiUrl, Alert primitive, getTranslations, etc.)
- `KNOWN_ERROR_CODES` tuple
- `isKnownError(error)` helper
- `searchParams: Promise<{ error?: string }>` signature
- The `await searchParams` destructuring
- The `auth.login.error.{code}.{title,body}` translator usage and the `tError` cast
- The `<Alert variant="destructive"><AlertTitle>{tError(...)}</AlertTitle><AlertDescription>{tError(...)}</AlertDescription></Alert>` block
- The `<a href={getApiUrl('/oauth2/authorization/google')} className={cn(buttonVariants({ size: 'lg' }), 'w-full')}>` OAuth CTA
- The `tLogin('googleButton')` button label
- The safety bullet list inside Card (whatever the current copy is — leave it alone)

WRAP the existing content in a 2-column shell. Specifically replace the outer `<main className="flex min-h-screen items-center justify-center p-6">...</main>` with:

```tsx
import AuthTopBar from '@/features/auth/components/AuthTopBar';
import { TrustPanel } from '@/features/auth/components/TrustPanel';
import { LegalFooter } from '@/features/auth/components/LegalFooter';

// ... existing imports, KNOWN_ERROR_CODES, isKnownError stay above ...

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const { error } = await searchParams;
  const tLogin = await getTranslations('auth.login');
  const tAuthError = await getTranslations('auth.error');
  const tError = tAuthError as unknown as (key: string) => string;
  // ... any existing helper logic stays ...

  return (
    <main className="grid min-h-screen md:grid-cols-2">
      <TrustPanel />
      <div className="bg-background flex flex-col">
        <AuthTopBar />
        <div className="flex flex-1 items-center justify-center p-6">
          <div className="flex w-full max-w-md flex-col gap-6">
            {isKnownError(error) && (
              <Alert variant="destructive">
                <AlertTitle>{tError(`${error}.title`)}</AlertTitle>
                <AlertDescription>{tError(`${error}.body`)}</AlertDescription>
              </Alert>
            )}
            <Card className="w-full">
              {/* PRESERVE existing CardHeader / CardContent / safety bullets / OAuth <a> verbatim */}
              {/* ... existing card body content here unchanged ... */}
            </Card>
            <LegalFooter />
          </div>
        </div>
      </div>
    </main>
  );
}
```

CRITICAL DO-NOTs:
- DO NOT modify the safety bullet list copy or its i18n keys (Phase 1.5 D-B3 contract).
- DO NOT modify the OAuth `<a href>` — `/oauth2/authorization/google` stays.
- DO NOT touch `KNOWN_ERROR_CODES` or `isKnownError` — closed-enum guard (T-01.5-02-01).
- DO NOT remove the Alert error block — Phase 1.5 D-B3 invariant.
- DO NOT add a new Sign-in CTA — the OAuth `<a>` is the only CTA.
- DO NOT make any backend changes — the `getApiUrl()` helper is preserved as-is.

After editing, manually verify dev render:
```
pnpm --filter zeromail-web dev
```
Open `http://localhost:3000/login` at viewport widths 320 / 768 / 1024 / 1280. Confirm:
- 320 / 480 px: Single column, TrustPanel hidden, LegalFooter visible below OAuth button.
- 768 / 1024 / 1280 px: 2-col grid, TrustPanel visible left, LegalFooter visible below OAuth button.
- Click OAuth button — should still POST to `/oauth2/authorization/google`.

Document any rendering anomaly in the SUMMARY for Wave 4 visual sweep input.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && pnpm i18n:check && grep -c "/oauth2/authorization/google" app/\(auth\)/login/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/oauth2/authorization/google" apps/web/app/(auth)/login/page.tsx` returns ≥ 1 (OAuth href preserved)
    - `grep -c "KNOWN_ERROR_CODES\|isKnownError" apps/web/app/(auth)/login/page.tsx` returns ≥ 1 (closed-enum guard preserved)
    - `grep -c "Alert variant=\"destructive\"\|<Alert variant='destructive'" apps/web/app/(auth)/login/page.tsx` returns ≥ 1 (error rendering preserved)
    - `grep -c "searchParams: Promise" apps/web/app/(auth)/login/page.tsx` returns ≥ 1 (RSC searchParams reading preserved — Phase 1.5 D-B3)
    - `grep -c "auth.error\|auth.login.error" apps/web/app/(auth)/login/page.tsx` returns ≥ 1 (error keys preserved)
    - `grep -c "TrustPanel" apps/web/app/(auth)/login/page.tsx` returns ≥ 1
    - `grep -c "AuthTopBar" apps/web/app/(auth)/login/page.tsx` returns ≥ 1
    - `grep -c "LegalFooter" apps/web/app/(auth)/login/page.tsx` returns ≥ 1
    - `grep -cE "md:grid-cols-2" apps/web/app/(auth)/login/page.tsx` returns ≥ 1 (2-col shell)
    - `grep -c "@/features/auth/components/(AuthTopBar|TrustPanel|LegalFooter)" apps/web/app/(auth)/login/page.tsx` (deep imports — D-B5)
    - `pnpm --filter zeromail-web tsc --noEmit` exits 0
    - `pnpm --filter zeromail-web i18n:check` exits 0
    - `git diff --stat backend/` reports zero lines changed
    - `git diff apps/web/app/(auth)/login/page.tsx` shows additions to wrap + new imports + LegalFooter render — NO deletions of safety bullet list, error keys, OAuth href, or KNOWN_ERROR_CODES
  </acceptance_criteria>
  <done>(auth)/login reskinned to 2-col desktop / 1-col mobile; legal footer always visible; every Phase 1.5 backend invariant preserved.</done>
</task>

</tasks>

<verification>
- All 3 task acceptance criteria pass
- `pnpm --filter zeromail-web build` succeeds (dev render compiles)
- Manual viewport sweep at 320 / 768 / 1024 / 1280 px shows correct layout (deferred to Wave 4 visual sweep)
- `git diff --stat backend/` is empty
- e2e/login-shell.spec.ts + e2e/legal-stubs.spec.ts contracts: structurally satisfied (run requires dev server — Wave 4)
</verification>

<success_criteria>
Sign-in shell reskinned without backend regression. CASA-compliant legal footer always visible. /terms + /privacy stubs serve HTTP 200. TrustPanel honors PROJECT.md privacy contract on desktop sign-in.
</success_criteria>

<output>
After completion, create `.planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-05-SUMMARY.md`. Note: include a "Phase 1.5 invariants preserved" checklist confirming OAuth href, error code keys, KNOWN_ERROR_CODES, and searchParams reading were not modified.
</output>
