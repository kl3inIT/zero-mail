---
phase: 01.6-brand-identity-design-tokens-and-landing-page
plan: 02
subsystem: ui
tags: [nextjs, next-font, typography, vietnamese, vitest]

requires:
  - phase: 01.6-00
    provides: Wave 0 root layout font contract test
provides:
  - Be Vietnam Pro next/font loader with vietnamese and latin subsets
  - Instrument Serif next/font loader with latin subset, normal and italic style
  - Root html className containing Geist Sans, Geist Mono, Be Vietnam Pro, and Instrument Serif CSS variable classes
affects: [landing-page, typography, visual-sweep, theme-cookie-plan]

tech-stack:
  added: []
  patterns:
    - Direct module-scope next/font/google calls for production extraction
    - Spec-local next/font/google mock for Vitest render tests

key-files:
  created:
    - .planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-02-SUMMARY.md
  modified:
    - apps/web/app/layout.tsx
    - apps/web/test/root-layout-fonts.test.tsx

key-decisions:
  - "Use Be Vietnam Pro weights 400, 500, and 600 with the vietnamese subset for full Vietnamese diacritic coverage."
  - "Keep Instrument Serif latin-only with normal and italic style because it has no vietnamese subset."
  - "Keep production font loaders as direct module-scope calls; mock next/font/google only inside the Vitest spec."

patterns-established:
  - "next/font loaders stay as direct const assignments in app/layout.tsx so Next 16/Turbopack can statically extract font assets."
  - "Vitest specs that import app/layout.tsx mock next/font/google locally instead of adding test-only branches to production layout code."

requirements-completed: [1.6-2]

duration: 16min
completed: 2026-04-28
---

# Phase 01.6 Plan 02: Typography Stack Summary

**Four-font Next.js typography stack with Be Vietnam Pro Vietnamese coverage and Instrument Serif display support**

## Performance

- **Duration:** 16 min
- **Started:** 2026-04-28T09:17:18Z
- **Completed:** 2026-04-28T09:33:01Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Added Be Vietnam Pro via `next/font/google` with `subsets: ['vietnamese', 'latin']`, weights `400/500/600`, and `display: 'swap'`.
- Added Instrument Serif via `next/font/google` with `subsets: ['latin']`, weight `400`, normal and italic styles, and `display: 'swap'`.
- Preserved the existing `reassertServerLocale(cookieLocale)` flow and extended the root `<html>` class list to include all four font CSS-variable classes.
- Turned `root-layout-fonts.test.tsx` green with a spec-local `next/font/google` mock that leaves production layout code direct and build-compatible.

## Task Commits

1. **Task 2.1: Add Be Vietnam Pro + Instrument Serif to app/layout.tsx** - `ef13413` (feat)

## Files Created/Modified

- `apps/web/app/layout.tsx` - Adds Be Vietnam Pro and Instrument Serif font loaders, `display: 'swap'` on all four font loaders, and all four font variables on `<html>`.
- `apps/web/test/root-layout-fonts.test.tsx` - Adds a Vitest-local `next/font/google` mock so the root layout font contract can render outside Next's compiler.
- `.planning/phases/01.6-brand-identity-design-tokens-and-landing-page/01.6-02-SUMMARY.md` - Execution record and verification results.

## Decisions Made

- Kept `zm-theme` cookie reads out of `app/layout.tsx`; that remains Plan 01.6-03 scope.
- Kept direct module-scope font loader calls after confirming `pnpm --filter zeromail-web build` rejects conditional/test-fallback wrappers for `next/font`.
- Used a test-local font mock rather than production test branches because Next 16 requires direct font loader const assignments for Turbopack extraction.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added Vitest next/font/google mock**
- **Found during:** Task 2.1
- **Issue:** The Wave 0 spec imported `app/layout.tsx`, but `next/font/google` exports are compiler-handled by Next and are empty under bare Vitest, causing `TypeError: Geist is not a function` before assertions could run.
- **Fix:** Added a spec-local mock returning the configured `variable` value for `Geist`, `Geist_Mono`, `Be_Vietnam_Pro`, and `Instrument_Serif`.
- **Files modified:** `apps/web/test/root-layout-fonts.test.tsx`
- **Verification:** `pnpm --filter zeromail-web test -- --run test/root-layout-fonts.test.tsx` passed.
- **Committed in:** `ef13413`

**2. [Rule 3 - Blocking] Repaired stale local React install**
- **Found during:** Task 2.1 verification
- **Issue:** `apps/web/node_modules/react` and `apps/web/node_modules/react-dom` were stale physical folders at `19.2.4`, while `package.json`, `pnpm-lock.yaml`, and root `node_modules` were on `19.2.5`; this produced an invalid-hook-call failure in the render test.
- **Fix:** Removed only the generated stale `apps/web/node_modules/react` and `apps/web/node_modules/react-dom` folders after path checks, then ran `pnpm install --frozen-lockfile`.
- **Files modified:** none tracked
- **Verification:** `pnpm --filter zeromail-web exec node -e "..."` resolved React and React DOM from the root install at `19.2.5`; the root layout font spec passed.
- **Committed in:** N/A, local dependency repair only

**3. [Rule 3 - Blocking] Used package-relative Vitest path**
- **Found during:** Task 2.1 verification
- **Issue:** The plan's root-relative test path (`apps/web/test/root-layout-fonts.test.tsx`) is interpreted relative to the `zeromail-web` package because `pnpm --filter` runs the script in `apps/web`, so Vitest found no files.
- **Fix:** Ran the same spec with the package-relative path `test/root-layout-fonts.test.tsx`.
- **Files modified:** none
- **Verification:** `pnpm --filter zeromail-web test -- --run test/root-layout-fonts.test.tsx` passed.
- **Committed in:** N/A, command correction only

---

**Total deviations:** 3 auto-fixed (3 blocking)
**Impact on plan:** All deviations were required to verify the planned layout change. Production font wiring stayed direct and build-compatible; no backend or theme-cookie scope was added.

## Issues Encountered

- First `pnpm --filter zeromail-web build` attempt was blocked by a concurrent/stale Next build lock while Plan 01.6-01 was still finishing. Retrying after the parallel work settled succeeded.
- First task commit collided with Plan 01.6-01's metadata commit while lint-staged was applying changes. The tree was checked, the plan changes were preserved on top of the new HEAD, verification was rerun, and the task was committed as `ef13413`.
- `gsd-sdk query requirements.mark-complete "1.6-2"` returned `not_found` because Phase 01.6 uses phase-internal requirement IDs in `01.6-SPEC.md`; the root `.planning/REQUIREMENTS.md` does not define `1.6-2`.

## Verification

- `pnpm --filter zeromail-web test -- --run test/root-layout-fonts.test.tsx` - passed, 1 file / 1 test.
- `pnpm --filter zeromail-web exec tsc --noEmit` - passed.
- `pnpm --filter zeromail-web build` - passed.
- Fixed-string acceptance checks for `Be_Vietnam_Pro`, `Instrument_Serif`, Vietnamese subset, both CSS variables, Be Vietnam Pro weights, `display: 'swap'`, and `reassertServerLocale` - passed.
- `git diff --stat -- backend/` - empty output, no backend changes.

## Known Stubs

None.

## Threat Flags

None. This plan only adds build-time `next/font/google` loaders; Next self-hosts the downloaded font assets and no runtime browser request to Google Fonts is introduced.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 01.6-03 can add `zm-theme` cookie reading and theme toggle behavior without revisiting the font stack. Visual confirmation of Vietnamese diacritic rendering remains deferred to the Wave 4 visual sweep, as planned.

## Self-Check: PASSED

- Summary file exists.
- Task commit `ef13413` exists in git history.
- Modified task files exist: `apps/web/app/layout.tsx`, `apps/web/test/root-layout-fonts.test.tsx`.
- Stub scan found no TODO/FIXME/placeholder patterns in files touched by this plan.
- Threat-surface scan found no new endpoints, cookie writes, file access, or network calls in files touched by this plan.

---
*Phase: 01.6-brand-identity-design-tokens-and-landing-page*
*Completed: 2026-04-28*
