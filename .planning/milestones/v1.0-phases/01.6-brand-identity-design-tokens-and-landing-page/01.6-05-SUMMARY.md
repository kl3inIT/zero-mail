---
phase: 01.6-brand-identity-design-tokens-and-landing-page
plan: 05
subsystem: ui
tags: [nextjs, auth, login, legal, casa, i18n]

requires:
  - phase: 01.6-03
    provides: Public/auth chrome building blocks and theme toggle
  - phase: 01.6-04
    provides: trust.p1..p3 copy namespace for sign-in TrustPanel
provides:
  - Two-column desktop sign-in shell with desktop-only TrustPanel
  - Always-visible legal footer with Terms, Privacy, and Google API Limited Use disclosure
  - Public /terms and /privacy placeholder routes returning 200
  - legal.* i18n namespace mirrored in EN and VI
affects: [login, legal-routes, casa-verification, i18n]

tech-stack:
  added: []
  patterns:
    - Auth shell reuses landing ZMLogoMark, ThemeToggle, and compact LanguageSwitcher
    - Legal footer uses next-intl t.rich placeholders for internal and external links

key-files:
  created:
    - apps/web/features/auth/components/AuthTopBar.tsx
    - apps/web/features/auth/components/TrustPanel.tsx
    - apps/web/features/auth/components/LegalFooter.tsx
    - apps/web/app/(public)/terms/page.tsx
    - apps/web/app/(public)/privacy/page.tsx
  modified:
    - apps/web/app/(auth)/login/page.tsx
    - apps/web/i18n/messages/en.json
    - apps/web/i18n/messages/vi.json

key-decisions:
  - "Preserve the Phase 1.5 OAuth href exactly: getApiUrl('/oauth2/authorization/google')."
  - "Keep TrustPanel desktop-only with hidden md:flex; mobile gets the form and legal footer only."
  - "Keep the legal footer outside the Card and always visible beneath the OAuth form content."

patterns-established:
  - "AuthTopBar exposes a children slot for Plan 06 StepIndicator while keeping theme/lang controls reusable."
  - "Public legal stubs live inside the (public) route group and inherit the public chrome."

requirements-completed: [1.6-6, 1.6-8, 1.6-9]

duration: 18min
completed: 2026-04-28
---

# Phase 01.6 Plan 05: Sign-In Shell and Legal Stubs Summary

Reskinned `/login` into a desktop two-column trust shell and mobile single-column form while preserving the Phase 1.5 OAuth and error-handling invariants.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 5.1 | Add legal.* EN/VI i18n copy | `3e51310` | 2 modified |
| 5.2 | Add AuthTopBar, TrustPanel, LegalFooter, /terms, /privacy | `8f16e52` | 5 created |
| 5.3 | Reskin login auth shell | `558b6c8` | 1 modified |

## Phase 1.5 Invariants Preserved

- OAuth href remains `getApiUrl('/oauth2/authorization/google')`.
- `KNOWN_ERROR_CODES` and `isKnownError` closed-enum guard remain in `login/page.tsx`.
- `Alert variant="destructive"` error rendering remains present.
- RSC `searchParams: Promise<{ error?: string }>` shape remains present.
- `auth.error` / dynamic error translator usage remains present.
- No backend files changed.

## Verification

- `pnpm --filter zeromail-web exec tsc --noEmit` - PASS.
- `pnpm --filter zeromail-web i18n:check` - PASS, 157 leaf keys.
- `pnpm --filter zeromail-web build` - PASS; routes include `/login`, `/terms`, and `/privacy`.
- Literal acceptance probes passed for OAuth href, error guard, destructive alert, `searchParams`, auth component imports, `md:grid-cols-2`, `hidden md:flex`, `trust.p1..p3`, `t.rich`, Google API Limited Use URL, and `rel="noopener noreferrer"`.
- Raw-color scan over `features/auth/components/*.tsx` found 0 matches for `text-stone-`, `bg-amber-`, `border-blue-`, or `text-gray-`.
- `git diff --stat c49363b..HEAD -- backend/` returned no output.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking Verification] Used workspace-valid verification commands**
- **Found during:** Task verification
- **Issue:** `zeromail-web` has no `tsc` script, and PowerShell requires literal quoting for route-group paths containing parentheses.
- **Fix:** Used `pnpm --filter zeromail-web exec tsc --noEmit` and literal-path PowerShell probes.
- **Files modified:** None
- **Verification:** TypeScript, i18n, build, and literal acceptance probes passed.
- **Committed in:** N/A

**Total deviations:** 1 verification-only adjustment.
**Impact:** No product behavior change; source implementation stayed within plan scope.

## Known Stubs

- `/terms` and `/privacy` intentionally contain placeholder legal copy. Full legal content is deferred to Phase 6 launch hardening per plan.

## Threat Flags

None. This plan adds public static legal stubs and presentational auth chrome only. OAuth routing remains unchanged.

## User Setup Required

None.

## Next Phase Readiness

Plan 06 can reuse `AuthTopBar` and its `children` slot for the onboarding `StepIndicator`. Wave 4 can verify the login shell across mobile/desktop breakpoints and confirm the legal links return HTTP 200.

## Self-Check: PASSED

- Summary file exists.
- Task commits exist in git history: `3e51310`, `8f16e52`, `558b6c8`.
- Created files exist: `AuthTopBar.tsx`, `TrustPanel.tsx`, `LegalFooter.tsx`, `/terms/page.tsx`, `/privacy/page.tsx`.
- No tracked backend changes.
