---
phase: 01.4
plan: 03
type: execute
wave: 1
depends_on: ["01.4-01"]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java
  - backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "GmailConnectionService.upsert(...) idempotently creates or updates the single GmailConnectionEntity row per tenant"
    - "Upsert resets disconnectedAt to null and status to CONNECTED on re-grant after disconnect"
    - "GmailScopeRequestResolver injects login_hint=<email> when an authenticated session principal is available"
    - "GmailScopeRequestResolver omits login_hint silently (no exception, no redirect) when session is anonymous"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java"
      provides: "New upsert(UUID, String, String, byte[]) method; existing methods (currentStatus, disconnect, deleteForCurrentTenant) preserved verbatim"
    - path: "backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java"
      provides: "Extended customize() with login_hint key in additionalParameters when SecurityContextHolder yields an OidcUser email"
  key_links:
    - from: "GmailOAuthSuccessHandler (Plan 02)"
      to: "GmailConnectionService.upsert"
      via: "transactional call within ScopedValue-bound block"
      pattern: "connections\\.upsert"
    - from: "GmailScopeRequestResolver.customize"
      to: "Google authorization URL"
      via: "additionalParameters.put('login_hint', ...)"
      pattern: "login_hint"
---

<objective>
Land the two backend extensions whose absence would block Plan 02's compile (`GmailConnectionService.upsert`) and would defeat the same-account-default UX (`GmailScopeRequestResolver` `login_hint`). Single-domain, tenant-scoped, idempotent upsert per D-A4; graceful-degrade login_hint per D-A2.

Purpose: Plan 02's `GmailOAuthSuccessHandler` calls `connections.upsert(...)` — without this method that handler does not compile. The resolver extension makes Google's chooser default to the same account the user logged in with — pre-empting most identity-mismatch errors before they happen. Required by Phase 1.4 success criteria #2 (idempotent upsert) and #3 (login_hint is part of the permission UX contract).

Output: 1 new method on `GmailConnectionService`, 1 modified `customize()` block on `GmailScopeRequestResolver`. No new files. Plan 02 + Plan 03 together unblock the Wave 0 success/failure handler tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-RESEARCH.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-PATTERNS.md
@backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java
@backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java
@backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java
@backend/core/src/main/java/com/zeromail/core/gmail/model/GmailConnectionStatus.java
@backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java
@backend/core/src/main/java/com/zeromail/core/account/persistence/UserRepository.java

<interfaces>
- GmailConnectionRepository.findByTenantId(UUID) -> Optional<GmailConnectionEntity>
- GmailConnectionRepository.save(GmailConnectionEntity) -> GmailConnectionEntity
- GmailConnectionRepository.count() -> long
- GmailConnectionEntity setters available: setStatus, setRefreshTokenEncrypted, setScopesGranted, setConnectedAt, setDisconnectedAt
- GmailConnectionEntity constructor: (UUID id, UUID tenantId, String googleEmail, GmailConnectionStatus initialStatus)
- GmailConnectionStatus.CONNECTED enum constant
- @Transactional from org.springframework.transaction.annotation
- SecurityContextHolder.getContext().getAuthentication() -> Authentication (or null/AnonymousAuthenticationToken)
- OAuth2AuthenticationToken.getPrincipal() -> OidcUser (when present)
- OidcUser.getEmail() -> String (may be null)
- OAuth2AuthorizationRequest.from(req).additionalParameters(Map).build()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GmailConnectionService.upsert(...) idempotent method</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java (full — preserve currentStatus / disconnect / deleteForCurrentTenant unchanged; the upsert method goes alongside them)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java (full — confirm setter availability for status/refreshTokenEncrypted/scopesGranted/connectedAt/disconnectedAt; check if setGoogleEmail exists — RESEARCH §"Open Questions Q4" recommends NOT adding a setter since subject check guarantees email equality)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java (confirm findByTenantId signature)
    - backend/core/src/main/java/com/zeromail/core/account/service/OAuthProvisioningService.java lines 60-86 (race-handling + REQUIRES_NEW pattern — adapt the discipline; upsert does not need REQUIRES_NEW because the caller already opens the tx)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md §D-A4 (upsert signature and idempotency contract)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-RESEARCH.md §"Code Examples — Backend: GmailConnectionService.upsert" (lines 580-596 reference impl)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-PATTERNS.md §"GmailConnectionService.upsert(...)"
  </read_first>
  <action>
Add a new `upsert(UUID tenantId, String googleEmail, String scopesGranted, byte[] refreshTokenEncrypted)` method to `GmailConnectionService.java`. Place it alongside the existing methods (do NOT modify `currentStatus`, `disconnect`, or `deleteForCurrentTenant`).

Behavior contract (from CONTEXT D-A4 + RESEARCH §"Code Examples"):

```java
@Transactional
public void upsert(UUID tenantId, String googleEmail, String scopesGranted, byte[] refreshTokenEncrypted) {
    GmailConnectionEntity row = connections.findByTenantId(tenantId)
            .orElseGet(() -> new GmailConnectionEntity(
                    UUID.randomUUID(), tenantId, googleEmail, GmailConnectionStatus.CONNECTED));
    row.setStatus(GmailConnectionStatus.CONNECTED);
    row.setRefreshTokenEncrypted(refreshTokenEncrypted);
    row.setScopesGranted(scopesGranted);
    row.setConnectedAt(Instant.now());
    row.setDisconnectedAt(null);
    connections.save(row);
}
```

**Notes:**
- `googleEmail` is set on insert via the constructor; it is NOT explicitly written on update. Per RESEARCH Open Question Q4 + CONTEXT D-A4 ("defensive — should always equal stored value post-A1 check"), the subject check in Plan 02 guarantees email equality, so writing it on update is unnecessary. Do NOT add `setGoogleEmail` to the entity unless future scope demands it.
- `setDisconnectedAt(null)` is critical — re-grant after disconnect must clear the disconnected timestamp (CONTEXT D-A4 explicit requirement).
- The method is `@Transactional` (Spring's default propagation `REQUIRED`). The caller (`GmailOAuthSuccessHandler` from Plan 02) wraps it in `ScopedValue.where(TenantContext.TENANT, tenantId.toString()).run(() -> tx.executeWithoutResult(s -> connections.upsert(...)))` — so the JPA session captures the right tenant before this method runs (Pitfall 6 / FND-05).
- The auditing listener (Phase 1.2.1) updates `version` and `updated_at` automatically on save.
- Privacy: NEVER log googleEmail, scopesGranted contents, or refreshTokenEncrypted bytes in this method or its callers. The success-handler emits the only log line and uses opaque tenantId only.

**JetBrains check:** After saving, run `mcp__jetbrains__get_file_problems` on `GmailConnectionService.java`. Resolve any blocker issues before declaring done. If MCP unavailable, `./gradlew :backend:core:compileJava :backend:core:test --tests "*GmailConnection*"` substitutes — clean compile + Wave 0 upsert test (`GmailConnectionServiceUpsertTest`) GREEN is the proof.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -10; ./gradlew :backend:core:test --tests "*GmailConnectionServiceUpsert*" 2>&1 | tail -20; grep -E 'public void upsert\(UUID tenantId, String googleEmail, String scopesGranted, byte\[\] refreshTokenEncrypted\)' backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java | wc -l | grep -q "^1$" && echo "OK signature exact"; grep -E 'setDisconnectedAt\(null\)' backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java | wc -l | grep -q "^1$" && echo "OK D-A4 disconnect-reset present"; if grep -E '(log\.|logger\.).*\b(googleEmail|scopesGranted|refreshTokenEncrypted)\b' backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java | grep -v '^\s*//' | grep -v '^\s*\*'; then echo "FAIL: PII in log"; exit 1; fi</automated>
  </verify>
  <done>
    `GmailConnectionService.upsert(...)` exists with the EXACT signature `void upsert(UUID tenantId, String googleEmail, String scopesGranted, byte[] refreshTokenEncrypted)` (per D-A4). `setDisconnectedAt(null)` and `setStatus(CONNECTED)` are both present. Existing methods (`currentStatus`, `disconnect`, `deleteForCurrentTenant`) are byte-identical to before. The Wave 0 `GmailConnectionServiceUpsertTest` from Plan 01 turns GREEN. No PII appears in any log statement of this file. JetBrains problem check (or substitute) clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend GmailScopeRequestResolver.customize() with login_hint injection</name>
  <files>
    backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java
  </files>
  <read_first>
    - backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java (full — preserve existing prompt/access_type/include_granted_scopes additions; line 34-43 is the `customize` body to extend)
    - backend/core/src/main/java/com/zeromail/core/account/persistence/UserRepository.java (findByGoogleSubject signature — fallback if SecurityContextHolder principal is not an OidcUser)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md §D-A2 (login_hint contract; graceful-degrade requirement)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-RESEARCH.md §"Pattern 3" (login_hint code excerpt lines 369-389) + §"Common Pitfalls 7" (login_hint is advisory) + §Finding #4 (`prompt=consent` interaction; do NOT add `prompt=select_account`)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-PATTERNS.md §"GmailScopeRequestResolver.customize()"
  </read_first>
  <action>
Modify the `customize(OAuth2AuthorizationRequest r)` method inside `GmailScopeRequestResolver.java`. The existing structure is (verified read of file lines 34-43):

```java
private OAuth2AuthorizationRequest customize(OAuth2AuthorizationRequest r) {
    if (r == null) return null;
    Object regId = r.getAttribute("registration_id");
    if (regId == null || !GMAIL_REGISTRATION_ID.equals(regId.toString())) return r;
    var extra = new HashMap<>(r.getAdditionalParameters());
    extra.put("include_granted_scopes", "true");
    extra.put("prompt", "consent");
    extra.put("access_type", "offline");
    return OAuth2AuthorizationRequest.from(r).additionalParameters(extra).build();
}
```

Insert a `login_hint` graceful-degrade block AFTER the existing three `extra.put(...)` lines and BEFORE the `return` statement:

```java
// D-A2: pre-fill the Google chooser with the current user's email so leg-2 defaults to the same
// account they signed in with. Graceful-degrade: silently omit if no authenticated session principal
// or no email available — the subject check in GmailOAuthSuccessHandler is still the authoritative guard.
Authentication current = SecurityContextHolder.getContext().getAuthentication();
if (current instanceof OAuth2AuthenticationToken oauthToken
        && oauthToken.getPrincipal() instanceof OidcUser oidc
        && oidc.getEmail() != null
        && !oidc.getEmail().isBlank()) {
    extra.put("login_hint", oidc.getEmail());
}
```

**Imports to add** (if not already present):
- `org.springframework.security.core.Authentication`
- `org.springframework.security.core.context.SecurityContextHolder`
- `org.springframework.security.oauth2.client.authentication.OAuth2AuthenticationToken`
- `org.springframework.security.oauth2.core.oidc.user.OidcUser`

**KEY POINTS:**
- Do NOT throw if SecurityContextHolder yields null/anonymous — Google chooser will simply show without pre-selection (CONTEXT D-A2 explicit; Pitfall 7 — login_hint is advisory anyway).
- Do NOT add `prompt=select_account` (RESEARCH Finding #4 — would defeat login_hint's account-suppression behavior). The existing `prompt=consent` stays.
- The email value is URL-encoded by Spring Security's URI builder (Threat T-login-hint-xss mitigated by construction).
- Privacy: do NOT log the email. The flow is fire-and-forget URL construction; no logging needed in this method.

**JetBrains check:** After saving, run `mcp__jetbrains__get_file_problems` on `GmailScopeRequestResolver.java`. Resolve blocker issues. If MCP unavailable, `./gradlew :backend:api:compileJava :backend:api:test --tests "*GmailScopeRequestResolver*"` is the substitute proof — clean compile + Wave 0 resolver test GREEN.
  </action>
  <verify>
    <automated>./gradlew :backend:api:compileJava 2>&1 | tail -10; ./gradlew :backend:api:test --tests "*GmailScopeRequestResolver*" 2>&1 | tail -20; grep -E 'extra\.put\("login_hint"' backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java | wc -l | grep -q "^1$" && echo "OK login_hint emitted"; grep -E "select_account" backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java | wc -l | grep -q "^0$" && echo "OK no select_account regression"; grep -E "prompt.*consent" backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java | wc -l | grep -q "^1$" && echo "OK prompt=consent preserved"; if grep -E '(log\.|logger\.).*\b(getEmail|email|login_hint)\b' backend/api/src/main/java/com/zeromail/api/security/GmailScopeRequestResolver.java | grep -v '^\s*//' | grep -v '^\s*\*'; then echo "FAIL: PII in log"; exit 1; fi</automated>
  </verify>
  <done>
    `customize(OAuth2AuthorizationRequest)` adds `login_hint` to `additionalParameters` ONLY when SecurityContextHolder yields a non-anonymous OidcUser with a non-blank email (graceful-degrade per D-A2). Existing `prompt=consent`, `access_type=offline`, `include_granted_scopes=true` parameters are byte-identical to before. No `select_account` parameter introduced (RESEARCH Finding #4). The Wave 0 `GmailScopeRequestResolverTest` from Plan 01 (both `loginHintInjected_whenAuthenticatedSession` and `loginHintOmittedGracefully_whenAnonymousSession` cases) turns GREEN. No PII in log statements. JetBrains problem check (or substitute) clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GmailConnectionService.upsert → DB | Tenant-scoped JPA write; relies on caller to have already bound `TenantContext.TENANT` ScopedValue (Plan 02's success handler does this) |
| GmailScopeRequestResolver → Google authorization URL | Outbound URL containing `login_hint=<email>` query parameter; URL-encoded by Spring Security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.4-03-token-leak | Information Disclosure | GmailConnectionService.upsert | mitigate | No log statements reference googleEmail / scopesGranted / refreshTokenEncrypted; auditing listener handles version+updated_at without logging |
| T-1.4-03-tenant-bleed | Tampering | GmailConnectionService.upsert | mitigate | `@Transactional` with default REQUIRED propagation joins caller's tx; caller (Plan 02 handler) binds ScopedValue before opening tx so Hibernate `@TenantId` filter resolves correctly (Pitfall 6 / FND-05) |
| T-1.4-03-login-hint-xss | XSS | GmailScopeRequestResolver | mitigate | Email value URL-encoded by Spring Security's URI builder; safe by construction (Threat per RESEARCH §Security Domain) |
</threat_model>

<verification>
- `./gradlew :backend:core:compileJava :backend:api:compileJava` GREEN end-to-end (Plan 02's `GmailOAuthSuccessHandler` now compiles because `connections.upsert(...)` exists)
- After Plan 02 + Plan 03 both land, the Wave 0 backend tests from Plan 01 (`GmailOAuthSuccessHandlerIntegrationTest`, `GmailOAuthFailureHandlerIntegrationTest`, `GmailScopeRequestResolverTest`, `GmailConnectionServiceUpsertTest`) all turn GREEN
- No PII in any log statement of either modified file
- `prompt=consent` preserved (no regression to `select_account`)
</verification>

<success_criteria>
- 1 new public method on `GmailConnectionService`; existing methods byte-identical
- 1 modified `customize()` block on `GmailScopeRequestResolver`; existing 3 additionalParameters preserved
- Wave 0 backend tests from Plan 01 turn GREEN
- JetBrains problem check (or compile substitute) clean for both files
</success_criteria>

<output>
After completion, create `.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-03-SUMMARY.md`
</output>
