---
phase: 01.2
slug: domain-owned-persistence-restructuring
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-26
---

# Phase 01.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Phase 1.2 is a structural refactor — no new behavior. The validation contract is **byte-identical behavior preservation** plus **new module-boundary enforcement**.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 5 + Spring Boot Test + Testcontainers 1.21.3 + ArchUnit 1.3.0 + Spring Modulith 2.0.7-SNAPSHOT verifier |
| **Config file** | `backend/core/build.gradle.kts`, `backend/api/build.gradle.kts`, convention plugins in `buildSrc/` |
| **Quick run command** | `./gradlew :backend:core:compileJava :backend:api:compileJava :backend:api:test --tests "ApplicationModulesTest"` |
| **Full suite command** | `./gradlew clean check` |
| **Estimated runtime** | quick ~30 s · full ~4–8 min (Testcontainers boot dominates) |

---

## Sampling Rate

- **After every task commit:** Run `./gradlew :backend:core:compileJava :backend:api:compileJava :backend:api:test --tests "ApplicationModulesTest"` (≈30 s — Modulith verify catches package-info / allowedDependencies drift fast)
- **After every plan wave:** Run `./gradlew clean check` (≈4–8 min — Testcontainers boots, full ArchUnit, Liquibase migration, integration + E2E)
- **Before `/gsd-verify-work`:** Full suite must be green PLUS byte-identical contract verified — zero diff in `gradle/libs.versions.toml`, `apps/web/openapi/*.json`, and `backend/core/src/main/resources/db/changelog/**`
- **Max feedback latency:** 30 s (quick) · 8 min (full)

---

## Per-Task Verification Map

> Task IDs are placeholders — planner emits final IDs in PLAN.md frontmatter. Each row maps a phase invariant to the test that proves it survived the move.

| Plan | Wave | Behavior to prove | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|------|------|-------------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.2-01 | 1 | Sensitive marker still scrubs in logs after package move | T-01.2-A (logback FQN drift) | Logback filter scrubs `Sensitive(...)` literals from synthetic traffic | integration | `./gradlew :backend:api:test --tests "LogScrubSyntheticTrafficTest"` | ✅ | ⬜ pending |
| 01.2-01 | 1 | ArchUnit safety contract enforces `Sensitive<T>` (post-FQN-update) | T-01.2-B (ArchUnit literal drift) | Build fails if `body`/`prompt`/`completion`/`refreshToken` not `Sensitive<String>` | arch | `./gradlew :backend:core:test --tests "SafetyContractArchTests"` | ✅ | ⬜ pending |
| 01.2-01 | 1 | `Sensitive.toString()` redacts | — | Returns `"[REDACTED]"`, never raw value | unit | `./gradlew :backend:core:test --tests "SensitiveToStringTest"` | ✅ | ⬜ pending |
| 01.2-01 | 1 | logback-spring.xml class string updated (Pitfall 1) | T-01.2-A | App boots with scrub filter active (no silent removal) | integration | `./gradlew :backend:api:test --tests "LogScrubSyntheticTrafficTest"` | ✅ | ⬜ pending |
| 01.2-01 | 1 | Modulith verify after `shared.privacy` introduced + Modulith naming probe | — | `ApplicationModulesTest.verify()` green; locked naming form documented in package-info | arch | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | ✅ | ⬜ pending |
| 01.2-02 | 2 | Tenant entity persistence works | — | Liquibase migration + Hibernate scan preserve `tenants` table | integration | `./gradlew :backend:core:test --tests "LiquibaseMigrationTest"` | ✅ | ⬜ pending |
| 01.2-02 | 2 | Multi-tenant leak still impossible (FND-05) | T-01.2-C (tenant filter regression) | `@TenantId` discriminator filter applies after entity package move | integration | `./gradlew :backend:api:test --tests "MultiTenantLeakIntegrationTest"` | ✅ | ⬜ pending |
| 01.2-02 | 2 | Modulith verify after `tenant` allowedDependencies update | — | `ApplicationModulesTest.verify()` green | arch | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | ✅ | ⬜ pending |
| 01.2-03 | 3 | First-login + delete cascade still work end-to-end (post `AccountService` reshape per CL-2) | T-01.2-D (delete-cascade cross-domain) | Cascade order children → tenant → user; no orphan rows | E2E | `./gradlew :backend:api:test --tests "AccountDeletionE2ETest"` | ✅ | ⬜ pending |
| 01.2-03 | 3 | `/me` and `PATCH /me/language` (Phase 1.1 contract) survive | — | Response shape + ProblemDetail unchanged | integration | `./gradlew :backend:api:test --tests "MeLanguageIntegrationTest"` | ✅ | ⬜ pending |
| 01.2-03 | 3 | Onboarding state machine still works | — | UserEntity↔OnboardingStep mapping preserved | integration | `./gradlew :backend:api:test --tests "OnboardingStateMachineTest"` | ✅ | ⬜ pending |
| 01.2-03 | 3 | Modulith verify with `account → {tenant, onboarding, shared.privacy}` | — | No `account → gmail` edge present | arch | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | ✅ | ⬜ pending |
| 01.2-04 | 4 | Onboarding state machine | — | Selection persistence + step transitions intact | integration | `./gradlew :backend:api:test --tests "OnboardingStateMachineTest"` | ✅ | ⬜ pending |
| 01.2-04 | 4 | OnboardingStep enum serializes byte-identical (`name()`-based) | T-01.2-E (enum FQN persistence drift) | `OnboardingStep.SIGNED_IN.name() == "SIGNED_IN"` after package move | unit | `./gradlew :backend:core:test --tests "OnboardingStepEnumPersistenceTest"` | ❌ Wave 0 | ⬜ pending |
| 01.2-04 | 4 | Modulith verify with `onboarding → {tenant, account, shared.privacy}` | — | Cycle detection passes | arch | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | ✅ | ⬜ pending |
| 01.2-05 | 5 | Refresh-token cipher round-trip works after package move | T-01.2-F (cipher key-version envelope drift) | AES-GCM encrypt → decrypt yields plaintext bytes | unit | `./gradlew :backend:core:test --tests "RefreshTokenCipherTest"` | ✅ | ⬜ pending |
| 01.2-05 | 5 | Cipher nonce uniqueness | T-01.2-F | 1000 encrypts → 1000 distinct nonces | unit | `./gradlew :backend:core:test --tests "NonceUniquenessTest"` | ✅ | ⬜ pending |
| 01.2-05 | 5 | Gmail connection unique-per-tenant constraint still works | — | `(tenant_id, google_email)` unique index honored | integration | `./gradlew :backend:core:test --tests "GmailConnectionUniquenessTest"` | ✅ | ⬜ pending |
| 01.2-05 | 5 | Disconnect on `invalid_grant` flips status (renamed service still wired) | — | `GmailConnectionStatus.DISCONNECTED` after revocation | integration | `./gradlew :backend:api:test --tests "DisconnectOnInvalidGrantTest"` | ✅ | ⬜ pending |
| 01.2-05 | 5 | Session cookie e2e still works (post-renames) | — | OAuth flow → Spring Session Redis → `/me` 200 | E2E | `./gradlew :backend:api:test --tests "SessionCookieE2ETest"` | ✅ | ⬜ pending |
| 01.2-05 | 5 | `@EntityScan(basePackages = "com.zeromail.core")` discovers all 4 entities | T-01.2-G (silent entity-scan miss) | Boot fails fast if any entity is missing from scan | smoke | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | ✅ | ⬜ pending |
| 01.2-05 | 5 | Modulith verify with `gmail → {tenant, shared.privacy}` | — | No `gmail → account` edge | arch | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | ✅ | ⬜ pending |
| 01.2-06 | 6 | Cross-domain repo ban active | T-01.2-H (architectural drift over time) | ArchUnit fails if any `core.<A>.*` imports `core.<B>.*Repository` | arch (NEW) | `./gradlew :backend:core:test --tests "DomainBoundaryArchTests"` | ❌ Wave 0 | ⬜ pending |
| 01.2-06 | 6 | Native-SQL allow-list regex spans all 4 domains | T-01.2-B | `..persistence.lowlevel..` matches each domain's lowlevel/ | arch | `./gradlew :backend:core:test --tests "TenantIsolationArchTests"` | ✅ | ⬜ pending |
| 01.2-06 | 6 | TenantStatus controller still serves `/tenant/status` (post `toResponse(view)` refactor + post-CL-2 delete-cascade orchestration) | — | Response payload + status code unchanged | integration | `./gradlew :backend:api:test --tests "SessionCookieE2ETest"` | ✅ | ⬜ pending |
| 01.2-06 | 6 | Full safety-contract test class passes after FQN updates | T-01.2-B | All ArchUnit safety rules green | arch | `./gradlew :backend:core:test --tests "SafetyContractArchTests"` | ✅ | ⬜ pending |
| ALL | every | Phase 1.1 i18n contract intact | — | ProblemDetail extension contract + locale resolution survive | integration | `./gradlew :backend:api:test --tests "ProblemDetailContractTest" --tests "LocaleResolutionTest" --tests "ApiErrorSchemaTest"` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

These are net-new tests Phase 1.2 must add (one-time, small):

- [ ] `backend/core/src/test/java/com/zeromail/core/onboarding/OnboardingStepEnumPersistenceTest.java` — 5-line test asserting `OnboardingStep.SIGNED_IN.name() == "SIGNED_IN"` (post-package-move). Covers Pitfall 5 (enum FQN persistence drift). **Owner: Plan 01.2-04**.
- [ ] `backend/core/src/test/java/com/zeromail/core/arch/DomainBoundaryArchTests.java` — 4 ArchRules (one per domain) enforcing the cross-domain repository import ban (D-D1). **Owner: Plan 01.2-06**.
- [ ] `backend/api/src/test/java/com/zeromail/api/controllers/AccountDeletionE2ETest.java` — verifies the post-CL-2 controller-level delete orchestration cascades correctly (children → tenant → user) when `AccountService` no longer holds cross-domain repos. May extend existing E2E if one exists; otherwise net-new. **Owner: Plan 01.2-03 (controller scaffold) + Plan 01.2-06 (final orchestration assembly)**.

**Framework install:** none needed — JUnit 5, ArchUnit 1.3.0, Testcontainers 1.21.3, Spring Modulith verifier all already wired in `buildSrc/zeromail.archunit-conventions` and module-level `build.gradle.kts`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Modulith nested-module naming probe (CL-3) | Architectural | Empirical — three candidate forms (`"shared.privacy"`, `"privacy"`, `"shared :: privacy"`); pick whichever makes `ApplicationModulesTest.verify()` pass and lock across all package-info files | Plan 01.2-01 task: try `allowedDependencies = {"shared.privacy"}` first; if `IllegalArgumentException: Module ... does not exist!`, try `"privacy"`, then `"shared :: privacy"`. Document the working form in `core/shared/privacy/package-info.java` JavaDoc. |
| Byte-identical Liquibase changelog | Success Criterion #4 | Must inspect git diff to confirm no changelog file modified | `git diff --stat backend/core/src/main/resources/db/changelog/` MUST show zero changes after final plan |
| Byte-identical OpenAPI doc | Success Criterion #4 | Must regenerate and diff to confirm no API surface drift | `./gradlew :backend:api:openApiGenerate` then `git diff apps/web/openapi/*.json` MUST show zero changes |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify command or Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (every plan's wave gates on `ApplicationModulesTest` at minimum)
- [ ] Wave 0 covers all 3 MISSING test files (enum persistence, DomainBoundaryArchTests, AccountDeletionE2ETest)
- [ ] No watch-mode flags (Gradle `--continuous` / `-t` forbidden in CI)
- [ ] Quick feedback latency < 30 s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner confirms every plan task references a row above)

**Approval:** pending
