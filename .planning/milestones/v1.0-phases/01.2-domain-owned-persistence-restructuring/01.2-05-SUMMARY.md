---
phase: 01.2-domain-owned-persistence-restructuring
plan: 05
subsystem: persistence
tags: [spring-modulith, java-25, refactor, package-rename, gmail, jpa, entity-scan, cl-2, cl-3, d-d4]

# Dependency graph
requires:
  - phase: 01.2-domain-owned-persistence-restructuring
    plan: 04
    provides: "OnboardingService at core.onboarding.service with deleteSelectionsForCurrentTenant; AccountDeletionController bridge with 1 service call + 2 cross-domain repos remaining; transitional 4-element @EntityScan array"
  - phase: 01.2-domain-owned-persistence-restructuring
    plan: 01
    provides: "Locked CL-3 form `\"shared.privacy\"`"
  - phase: 01-foundation-safety-infrastructure
    provides: "GmailConnectionEntity/Repository/Status, RefreshTokenCipher/CryptoConfig, TenantConnectionService/View, AccountDeletionController, GmailAccessGuard, RefreshTokenCipherTest, NonceUniquenessTest, GmailConnectionUniquenessTest, DisconnectOnInvalidGrantTest, SessionCookieE2ETest, AccountDeletionE2ETest, ApplicationModulesTest"
provides:
  - "GmailConnectionEntity + GmailConnectionRepository at com.zeromail.core.gmail.persistence"
  - "GmailConnectionStatus enum at com.zeromail.core.gmail.model"
  - "RefreshTokenCipher + RefreshTokenCryptoConfig at com.zeromail.core.gmail.persistence.crypto (domain-internal per D-C2)"
  - "GmailConnectionService at com.zeromail.core.gmail.service (renamed from TenantConnectionService) with deleteForCurrentTenant(UUID) per CL-2"
  - "GmailConnectionView at com.zeromail.core.gmail.model (renamed from TenantConnectionView)"
  - "TenantService at com.zeromail.core.tenant.service (first occupant of tenant/service/) with deleteCurrentTenant(UUID) per CL-2"
  - "core.gmail/package-info.java with @ApplicationModule(displayName=\"Gmail\", allowedDependencies={\"tenant\", \"shared.privacy\"}) — D-D4 honored, NO account edge"
  - "core.crypto/ and core.persistence/ packages fully deleted from main source tree (and test tree)"
  - "@EntityScan + @EnableJpaRepositories collapsed to single root \"com.zeromail.core\" in BOTH Application.java AND CoreTestApplication.java"
  - "AccountDeletionController bridge complete: 4 single-domain service calls in FK-safe order, zero direct repo injections"
affects: [01.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CL-2 single-domain delete pattern completed for the gmail + tenant domains: GmailConnectionService.deleteForCurrentTenant + TenantService.deleteCurrentTenant mirror the AccountService.deleteCurrentUser / OnboardingService.deleteSelectionsForCurrentTenant shape from Plans 03 + 04"
    - "Final entity-scan collapse: arrays {legacy, tenant.persistence, account.persistence, onboarding.persistence} → single root \"com.zeromail.core\". Forward-compatible for all future Phase 2A/2B/2C/3/4 domains (RESEARCH.md primary recommendation)"
    - "Pitfall 1 closure protocol applied identically to Plans 02-04: every stale FQN reference (7 import sites + 2 intra-package test files) updated alongside the git mv"
    - "Atomic D-D4 boundary declaration: gmail allowedDependencies = {tenant, shared.privacy} (NO account edge) lands in same commit as the new module's package-info.java"

key-files:
  created:
    - "backend/core/src/main/java/com/zeromail/core/gmail/persistence/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/gmail/persistence/lowlevel/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/gmail/model/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/gmail/service/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/gmail/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java"
    - "backend/core/src/main/java/com/zeromail/core/tenant/service/package-info.java"
  modified:
    - "backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java (relocated; explicit GmailConnectionStatus import added across packages)"
    - "backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java (relocated; package decl)"
    - "backend/core/src/main/java/com/zeromail/core/gmail/model/GmailConnectionStatus.java (relocated; package decl)"
    - "backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCipher.java (relocated; package decl)"
    - "backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCryptoConfig.java (relocated; package decl)"
    - "backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java (renamed from TenantConnectionService; package + imports updated; new deleteForCurrentTenant method)"
    - "backend/core/src/main/java/com/zeromail/core/gmail/model/GmailConnectionView.java (renamed from TenantConnectionView; package decl + record name)"
    - "backend/api/src/main/java/com/zeromail/api/Application.java (@EntityScan + @EnableJpaRepositories collapsed to single root)"
    - "backend/core/src/test/java/com/zeromail/core/support/CoreTestApplication.java (same collapse)"
    - "backend/api/src/main/java/com/zeromail/api/controllers/TenantStatusController.java (TenantConnectionService/View → GmailConnectionService/View)"
    - "backend/api/src/main/java/com/zeromail/api/controllers/DisconnectController.java (same flip)"
    - "backend/api/src/main/java/com/zeromail/api/controllers/AccountDeletionController.java (final bridge: 2 cross-domain repos replaced with GmailConnectionService + TenantService; FK-safe delete order preserved; @Transactional preserved)"
    - "backend/api/src/main/java/com/zeromail/api/security/GmailAccessGuard.java (GmailConnectionRepository/Status import flips)"
    - "backend/api/src/test/java/com/zeromail/api/security/DisconnectOnInvalidGrantTest.java (same flips)"
    - "backend/api/src/test/java/com/zeromail/api/AccountDeletionE2ETest.java (same flips)"
    - "backend/api/src/test/java/com/zeromail/api/LogScrubSyntheticTrafficTest.java (same flips)"
    - "backend/core/src/test/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCipherTest.java (relocated intra-package — Rule 3 blocking sweep)"
    - "backend/core/src/test/java/com/zeromail/core/gmail/persistence/crypto/NonceUniquenessTest.java (relocated intra-package — Rule 3 blocking sweep)"
    - "backend/core/src/test/java/com/zeromail/core/gmail/persistence/GmailConnectionUniquenessTest.java (relocated; package decl from core.persistence to core.gmail.persistence)"
    - "backend/core/src/test/java/com/zeromail/core/support/LiquibaseMigrationTest.java (relocated; package decl from core.persistence to core.support)"
  deleted:
    - "backend/core/src/main/java/com/zeromail/core/account/TenantConnectionService.java (renamed via git mv to gmail/service/GmailConnectionService.java)"
    - "backend/core/src/main/java/com/zeromail/core/account/TenantConnectionView.java (renamed via git mv to gmail/model/GmailConnectionView.java)"
    - "backend/core/src/main/java/com/zeromail/core/persistence/GmailConnectionEntity.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/persistence/GmailConnectionRepository.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/persistence/GmailConnectionStatus.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/persistence/package-info.java (last .java in package — package now empty)"
    - "backend/core/src/main/java/com/zeromail/core/persistence/lowlevel/package-info.java (superseded by per-domain lowlevel/ markers)"
    - "backend/core/src/main/java/com/zeromail/core/crypto/RefreshTokenCipher.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/crypto/RefreshTokenCryptoConfig.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/crypto/package-info.java (last .java in package — package now empty)"

key-decisions:
  - "Pitfall 1 sweep folded into Task 1 commit (Rule 3): TenantConnectionService import flip + 2 crypto test files moved intra-package alongside the git mv. Without this, :backend:core:compileJava would fail at the Task 1 verify gate. Same protocol as Plans 02-04."
  - "Two leftover legacy-package test files cleaned up in Task 3: GmailConnectionUniquenessTest moved to core.gmail.persistence (domain-aligned); LiquibaseMigrationTest moved to core.support (no domain — schema-wide test). Closes the success_criterion 'core.persistence package fully deleted (no .java files anywhere)'."
  - "Pre-flight bean-name safety check (Pitfall 8): zero matches for `tenantConnectionService` qualifier or bean-name autowire across backend/. Rename was safe. Documented in Task 2 commit message."
  - "Entity-scan collapse to single root chosen over explicit 4-element array (RESEARCH.md primary recommendation): forward-compatible for Phase 2A/2B/2C/3/4 — every future domain auto-discovered without further Application.java edits. Trade-off accepted: future @Component scans pick up everything under com.zeromail.core, which matches the @SpringBootApplication scanBasePackages already in place."
  - "core.gmail/package-info.java allowedDependencies = {tenant, shared.privacy} — explicitly NO account edge per D-D4. Verified by ApplicationModulesTest gate. Cross-domain access from API tier (AccountDeletionController) is permissible because backend/api transitively depends on every core domain — the boundary it would violate (gmail-internal repos called by account services) is honored."
  - "RefreshTokenCipher kept domain-internal at core.gmail.persistence.crypto (D-C2): single consumer = gmail's refresh_token_encrypted column. Promotion to shared/crypto/ deferred until a second consumer materializes (potentially Phase 2C BYOK)."
  - "All write-action invariants preserved: AES-GCM envelope shape unchanged (Task 1 was a pure package move with byte-identical class body), env-var name `zeromail.crypto.refresh-token-key-base64` unchanged, RefreshTokenCipherTest + NonceUniquenessTest round-trip green."

patterns-established:
  - "Per-domain `service/` package convention completed (4 of 4 domains): account/service, onboarding/service, gmail/service, tenant/service all present and populated. Plan 06 only needs to add the ArchUnit DomainBoundaryArchTests to enforce the cross-domain repo ban (D-D1)."
  - "AccountDeletionController final bridge shape: 4 single-domain service calls in FK-safe order (children → tenant → user → tenant), controller-level @Transactional. Plan 06 cleanup is JavaDoc-only (remove TRANSITIONAL note + add canonical Pattern 8 comment)."
  - "Domain-internal crypto helper convention: when a cipher/key is consumed by exactly one domain, place at <domain>/persistence/crypto/. Promote to shared/crypto/ only when a second consumer arrives (lazy promotion)."

requirements-completed: ["N/A — structural refactor, no Phase 1 requirement IDs apply"]

# Metrics
duration: ~11min
completed: 2026-04-26
---

# Phase 01.2 Plan 05: Move gmail domain + crypto helpers + collapse entity-scan + finalize AccountDeletionController bridge

**The gmail domain (entity, repo, enum, view, service) and refresh-token AES-GCM crypto helpers now live at `com.zeromail.core.gmail.{persistence,model,service,persistence/crypto}.*`; `TenantConnectionService` renamed to `GmailConnectionService` with the new single-domain `deleteForCurrentTenant`; new `TenantService` first occupant of `core.tenant.service` with `deleteCurrentTenant`; legacy `core.crypto` and `core.persistence` packages fully deleted from both main and test trees; `@EntityScan`/`@EnableJpaRepositories` collapsed to single root `com.zeromail.core` in both Application files; `AccountDeletionController` on final 4-service-call bridge shape (Plan 06 will only need a JavaDoc cleanup pass).**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-04-26T09:23Z
- **Completed:** 2026-04-26T09:34Z
- **Tasks:** 3 (all completed)
- **Files modified:** 30 (5 source relocations + 2 service renames + 1 net-new TenantService + 6 new package-info markers + 3 deleted package-info markers + 7 consumer reference updates + 4 test relocations + 2 Application.java entity-scan edits)
- **Commits:** 3 (2 by executor — 761fb72, f136f9f; Task 3 changes captured by user's concurrent docs commits 03b5652 + eabbdca — see Deviations)

## Accomplishments

- **GmailConnectionEntity + Repository + Status** moved to `com.zeromail.core.gmail.{persistence,model}.*` (rename history preserved via `git mv`).
- **RefreshTokenCipher + RefreshTokenCryptoConfig** moved to `com.zeromail.core.gmail.persistence.crypto.*` — domain-internal per D-C2. Env-var `zeromail.crypto.refresh-token-key-base64` preserved (string lookup, not Java FQN). AES-GCM envelope behavior byte-identical (verified by RefreshTokenCipherTest + NonceUniquenessTest green).
- **TenantConnectionService → GmailConnectionService** at `com.zeromail.core.gmail.service.*`. `currentStatus` + `disconnect` semantics preserved; `deleteForCurrentTenant(UUID)` added per CL-2.
- **TenantConnectionView → GmailConnectionView** at `com.zeromail.core.gmail.model.*`. `notConnected()` static helper preserved; only the type names changed.
- **TenantService** created at `com.zeromail.core.tenant.service.*` with `deleteCurrentTenant(UUID)` — first occupant of the tenant domain's `service/` package per CL-2 + Pattern 5.
- **core.gmail/package-info.java** declared with `@ApplicationModule(displayName="Gmail", allowedDependencies={"tenant", "shared.privacy"})`. Explicit absence of `account` edge per D-D4 — verified by ApplicationModulesTest green.
- **core.crypto and core.persistence packages fully deleted** from filesystem (main + test). Three legacy `package-info.java` files removed; two leftover legacy-namespaced test classes (GmailConnectionUniquenessTest + LiquibaseMigrationTest) relocated to `core.gmail.persistence` and `core.support` respectively.
- **@EntityScan + @EnableJpaRepositories collapsed to single root `"com.zeromail.core"`** in BOTH `backend/api/Application.java` AND `backend/core/.../CoreTestApplication.java`. Forward-compatible for all future Phase 2A/2B/2C/3/4 domains.
- **AccountDeletionController final bridge complete.** Both `GmailConnectionRepository` and `TenantRepository` direct-repo injections dropped; replaced with `GmailConnectionService.deleteForCurrentTenant` + `TenantService.deleteCurrentTenant`. Handler body now reads:
  ```java
  onboardingService.deleteSelectionsForCurrentTenant(tenantId);
  gmailConnectionService.deleteForCurrentTenant(tenantId);
  accountService.deleteCurrentUser(tenantId);
  tenantService.deleteCurrentTenant(tenantId);
  ```
  FK-safe delete order preserved; controller-level `@Transactional` preserved per CL-2. Plan 06 cleanup is JavaDoc-only.
- **Four consumers updated** with new gmail-domain imports: `TenantStatusController`, `DisconnectController`, `AccountDeletionController`, `GmailAccessGuard`. Three test files updated: `DisconnectOnInvalidGrantTest`, `AccountDeletionE2ETest`, `LogScrubSyntheticTrafficTest`.
- **All 7 validation gates green:** `RefreshTokenCipherTest`, `NonceUniquenessTest`, `GmailConnectionUniquenessTest`, `DisconnectOnInvalidGrantTest`, `SessionCookieE2ETest`, `AccountDeletionE2ETest`, `ApplicationModulesTest`. Build green: `:backend:core:compileJava`, `:backend:core:compileTestJava`, `:backend:api:compileJava`, `:backend:api:compileTestJava`.
- **Zero stale references** anywhere: `grep -rn 'com\.zeromail\.core\.persistence\.\|com\.zeromail\.core\.crypto\.\|com\.zeromail\.core\.account\.TenantConnection' backend/ apps/` returns no matches (one harmless JavaDoc reference in OnboardingStepEnumPersistenceTest comment text remains — Plan 04 artifact, not a code import).

## Task Commits

Each task verified compile-green; final tests-green run combined Task 3 with concurrent-user commits.

1. **Task 1: move gmail entity/repo/enum + crypto helpers to per-tier sub-packages** — `761fb72` (refactor) by executor
2. **Task 2: rename TenantConnectionService → GmailConnectionService + add TenantService** — `f136f9f` (refactor) by executor
3. **Task 3: package-info, entity-scan collapse, controllers, AccountDeletionController bridge, delete empty packages** — split between `03b5652` and `eabbdca` (committed by user's concurrent activity — see Deviations section)

All Task 3 code changes are present in the working tree and verified by passing tests; the unusual commit attribution is purely a git-history artifact.

## Files Created/Modified

(See frontmatter `key-files` block above for the full breakdown — 8 created, 19 modified, 10 deleted.)

## Decisions Made

- **CL-2 single-domain delete pattern completed across all 4 domains.** Plan 03 added AccountService.deleteCurrentUser; Plan 04 added OnboardingService.deleteSelectionsForCurrentTenant; this plan added GmailConnectionService.deleteForCurrentTenant + TenantService.deleteCurrentTenant. Each method deletes ONLY its own domain's rows; multi-domain orchestration lives at the API tier (AccountDeletionController).
- **D-D4 boundary respected: `gmail` does NOT depend on `account`.** Verified by ApplicationModulesTest. Cross-domain reads from gmail to account (none currently) would surface as a deviation rather than be silently allowed.
- **RefreshTokenCipher kept at gmail/persistence/crypto (D-C2).** Single consumer = gmail. Lazy promotion to shared/crypto/ deferred until Phase 2C BYOK actually needs encryption-at-rest for user keys (and only if it's not request-scoped).
- **Entity-scan single-root collapse:** chose `"com.zeromail.core"` (RESEARCH.md primary recommendation) over a 5-element explicit array. Future Phase 2A/2B/2C/3/4 domains auto-discovered without further Application.java edits.
- **Pre-flight bean-name safety check (Pitfall 8):** zero `tenantConnectionService` qualifier references — rename was safe to apply globally without bean wiring breakage.
- **Two leftover legacy-namespaced tests relocated proactively:** GmailConnectionUniquenessTest (→ `core.gmail.persistence`) and LiquibaseMigrationTest (→ `core.support`). Their package decls were the last residue of `core.persistence`. Without this move, Plan 06's stale-ref sweep grep would still match the `package` lines, which is technically not a stale import but visually is. Cleaner to fully evict the legacy namespace now.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Pitfall 1 sweep — TenantConnectionService import flip + intra-package crypto test moves folded into Task 1 commit**
- **Found during:** Task 1 verify gate preflight.
- **Issue:** Task 1 only listed gmail entity/repo/enum/crypto source files. But moving GmailConnectionRepository/Status broke TenantConnectionService's imports immediately, and moving RefreshTokenCipher broke 2 intra-package tests (NonceUniquenessTest + RefreshTokenCipherTest). Plan said `:backend:core:compileJava` should pass at Task 1 boundary; reality required 3 additional file edits.
- **Fix:** TenantConnectionService imports flipped + 2 test files git-mv'd into `core.gmail.persistence.crypto` package alongside the source moves. Same protocol as Plans 02-04.
- **Files modified:** `backend/core/src/main/java/com/zeromail/core/account/TenantConnectionService.java`, `backend/core/src/test/java/com/zeromail/core/gmail/persistence/crypto/{NonceUniquenessTest,RefreshTokenCipherTest}.java`
- **Verification:** Bundled into Task 1's commit `761fb72`; subsequent `:backend:core:compileJava` PASS.
- **Committed in:** `761fb72`

**2. [Rule 3 - Blocking] Test-import sweep folded into Task 3**
- **Found during:** Task 3 implementation.
- **Issue:** Plan listed only the controller updates (`TenantStatusController`, `DisconnectController`, `ConnectGmailController`, `AccountDeletionController`) but `GmailAccessGuard` (production) + 3 tests (`DisconnectOnInvalidGrantTest`, `AccountDeletionE2ETest`, `LogScrubSyntheticTrafficTest`) also imported `core.persistence.GmailConnection*`. Without updating these, `:backend:api:compileJava` and `:backend:api:compileTestJava` would fail.
- **Fix:** Updated all 4 additional files (1 production + 3 tests) with new gmail-domain imports.
- **Files modified:** `backend/api/src/main/java/com/zeromail/api/security/GmailAccessGuard.java`, `backend/api/src/test/java/com/zeromail/api/security/DisconnectOnInvalidGrantTest.java`, `backend/api/src/test/java/com/zeromail/api/AccountDeletionE2ETest.java`, `backend/api/src/test/java/com/zeromail/api/LogScrubSyntheticTrafficTest.java`
- **Verification:** Compile + 7 validation tests all green.

**3. [Rule 1 - Cleanup] Two leftover legacy-namespaced test files relocated**
- **Found during:** Final stale-ref sweep before commit.
- **Issue:** `GmailConnectionUniquenessTest` and `LiquibaseMigrationTest` still had `package com.zeromail.core.persistence;` declarations even though no other classes lived in that test namespace and the corresponding main package was being deleted. Plan success_criterion required "core.persistence package fully deleted (no more .java files anywhere)" — this includes test tree.
- **Fix:** `GmailConnectionUniquenessTest` moved to `core.gmail.persistence` (domain-aligned, native SQL test for `gmail_connections` table); `LiquibaseMigrationTest` moved to `core.support` (schema-wide test, no specific domain).
- **Files modified:** Both test files relocated via `git mv` + package decl update.

**4. [External] Concurrent user activity captured Task 3 changes in unrelated commits**
- **Found during:** Final `git status` check before Task 3 commit.
- **Issue:** While the executor was completing Task 3 work in the working tree, the user committed `.planning/phases/01.3-...` documentation files using a `git add` mode that picked up the executor's pending Java edits. As a result:
  - Commit `03b5652` (titled `docs(01.3): add phase research`) includes the executor's deletion of 3 legacy `package-info.java` files (`core.crypto`, `core.persistence`, `core.persistence.lowlevel`).
  - Commit `eabbdca` (titled `docs(01.3): add validation strategy`) includes the executor's modifications to Application.java, AccountDeletionController, DisconnectController, TenantStatusController, GmailAccessGuard, CoreTestApplication.java, AccountDeletionE2ETest, LogScrubSyntheticTrafficTest, DisconnectOnInvalidGrantTest, the new `core.gmail/package-info.java`, and the 2 leftover test relocations.
- **Decision:** Do NOT attempt to rewrite git history. The Java code is correct, all tests pass, all acceptance criteria are met. The unusual commit attribution is a cosmetic git-history quirk only. Splitting the history would require destructive operations (rebase + force push) that would lose the user's 01.3 docs work. Documented as a deviation here for traceability.
- **Net effect on plan correctness:** Zero. All Task 3 success criteria verified post-commit:
  - `core.gmail/package-info.java` exists with correct `allowedDependencies = {"tenant", "shared.privacy"}`
  - 3 legacy package-info files deleted; both legacy main packages empty/auto-removed
  - Both Application.java + CoreTestApplication.java collapsed to `@EntityScan(basePackages = "com.zeromail.core")`
  - AccountDeletionController has zero direct repo injections (4 single-domain service calls only)
  - All 7 validation gate tests pass

---

**Total deviations:** 4 (3 auto-fixed Rule 3 + Rule 1 closures; 1 external concurrent-activity event with zero functional impact)
**Impact on plan:** Net-positive correctness — every commit compile-green; no functional regressions; all gates pass. Concurrent-activity collision is documented but does not affect the delivered code.

## Issues Encountered

- **Concurrent user activity captured Task 3 changes in unrelated commits.** See Deviation 4 — purely a commit-attribution quirk; functional outcome identical.
- **CRLF line-ending warnings on Windows** for every staged file. Cosmetic; `.gitattributes` handles normalization.
- **No real issues.** The plan executed cleanly: 2 explicit task commits + 1 auto-captured Task 3 state via concurrent commits, all gates green on first run, no probes needed (CL-3 form locked from Plan 01, bean-name check passed Pitfall 8 trivially).

## Verification Gate Results

| Gate | Command | Result |
|------|---------|--------|
| Task 1 compile | `./gradlew :backend:core:compileJava` | PASS |
| Task 2 compile | `./gradlew :backend:core:compileJava` | PASS |
| Task 3 cross-tier compile | `./gradlew :backend:core:compileJava :backend:core:compileTestJava :backend:api:compileJava :backend:api:compileTestJava` | PASS |
| Task 3 — RefreshTokenCipherTest | `./gradlew :backend:core:test --tests "RefreshTokenCipherTest"` | PASS |
| Task 3 — NonceUniquenessTest | `./gradlew :backend:core:test --tests "NonceUniquenessTest"` | PASS |
| Task 3 — GmailConnectionUniquenessTest | `./gradlew :backend:core:test --tests "GmailConnectionUniquenessTest"` | PASS |
| Task 3 — DisconnectOnInvalidGrantTest | `./gradlew :backend:api:test --tests "DisconnectOnInvalidGrantTest"` | PASS |
| Task 3 — SessionCookieE2ETest | `./gradlew :backend:api:test --tests "SessionCookieE2ETest"` | PASS |
| Task 3 — AccountDeletionE2ETest | `./gradlew :backend:api:test --tests "AccountDeletionE2ETest"` | PASS |
| Task 3 — ApplicationModulesTest | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | PASS |

## Stale-reference sweep

```
$ grep -rn 'com\.zeromail\.core\.persistence\.\|com\.zeromail\.core\.crypto\.\|com\.zeromail\.core\.account\.TenantConnection' backend/ apps/
(no matches)

$ ls backend/core/src/main/java/com/zeromail/core/persistence/ 2>/dev/null
(no such directory)

$ ls backend/core/src/main/java/com/zeromail/core/crypto/ 2>/dev/null
(no such directory)
```

Both legacy main packages fully deleted from filesystem. Test packages also empty. Only one harmless JavaDoc text reference remains in `OnboardingStepEnumPersistenceTest` (Plan 04 artifact, comment text describing the move history — not an FQN import).

## Notes for Plan 06 Executor

Plan 06 is the final cleanup pass. Specific follow-ups remaining:

1. **AccountDeletionController JavaDoc cleanup.** The class-level Javadoc still says "TRANSITIONAL ORCHESTRATION (Plan 01.2-03 through 01.2-05)". Now that the bridge is fully on per-domain service calls, replace with the canonical Pattern 8 comment: "Multi-domain delete orchestration. Calls four single-domain services in FK-safe order under a controller-level @Transactional boundary per CL-2 + D-D1."

2. **TenantStatusController toResponse(view) helper extraction (D-B5).** Replace the inline `new TenantStatusResponse(view.status(), view.googleEmail())` with a private static helper to match `MeController`'s style.

3. **New ArchUnit DomainBoundaryArchTests (D-D1).** Add a rule that forbids any class in `core.<domainA>` from importing any `*Repository` from `core.<domainB>`. Concrete reachable targets are now: `core.{account,onboarding,gmail,tenant}.*`. The `service/` and `persistence/` packages of the same domain may both touch that domain's repositories.

4. **TenantIsolationArchTests update.** The existing `..core.persistence.lowlevel..` literal needs to become a regex pattern `..persistence.lowlevel..` to span all 4 domains' lowlevel/ packages.

5. **Final verify pass.** Run `./gradlew :backend:core:test :backend:api:test` end-to-end to confirm the full Phase 1 + 1.1 + 1.2 test matrix is still green after the new ArchUnit rules.

No blockers. The structural refactor is done — Plan 06 is purely additive (new ArchUnit class) + cosmetic (JavaDoc cleanup, controller helper extraction).

## Self-Check: PASSED

All claimed files exist at the asserted paths; all claimed commit hashes resolve in `git log`.

- `backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/persistence/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/persistence/lowlevel/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCipher.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCryptoConfig.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/model/GmailConnectionStatus.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/model/GmailConnectionView.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/model/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java` — FOUND (with `deleteForCurrentTenant(UUID)`)
- `backend/core/src/main/java/com/zeromail/core/gmail/service/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/gmail/package-info.java` — FOUND with `allowedDependencies = {"tenant", "shared.privacy"}`
- `backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java` — FOUND (with `deleteCurrentTenant(UUID)`)
- `backend/core/src/main/java/com/zeromail/core/tenant/service/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/persistence/` — DELETED (no such directory)
- `backend/core/src/main/java/com/zeromail/core/crypto/` — DELETED (no such directory)
- `backend/core/src/main/java/com/zeromail/core/account/TenantConnectionService.java` — DELETED
- `backend/core/src/main/java/com/zeromail/core/account/TenantConnectionView.java` — DELETED
- Commits `761fb72`, `f136f9f`, `03b5652`, `eabbdca` — all resolve in `git log`.
- AccountDeletionController has zero `GmailConnectionRepository` or `TenantRepository` references (`grep` exit=1).
- Both Application.java + CoreTestApplication.java contain `@EntityScan(basePackages = "com.zeromail.core")` (single-root form).

---

*Phase: 01.2-domain-owned-persistence-restructuring*
*Plan: 05*
*Completed: 2026-04-26*
