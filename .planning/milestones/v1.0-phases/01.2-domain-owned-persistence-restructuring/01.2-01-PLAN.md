---
phase: 01.2-domain-owned-persistence-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/src/main/java/com/zeromail/core/shared/privacy/Sensitive.java
  - backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveJacksonModule.java
  - backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveMarkerScrubFilter.java
  - backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java
  - backend/core/src/main/java/com/zeromail/core/privacy/package-info.java
  - backend/core/src/main/resources/logback-spring.xml
  - backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java
  - backend/core/src/test/java/com/zeromail/core/privacy/SensitiveToStringTest.java
  - backend/core/src/test/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilterTest.java
autonomous: true
requirements: ["N/A — structural refactor"]
must_haves:
  truths:
    - "All three privacy classes (Sensitive, SensitiveJacksonModule, SensitiveMarkerScrubFilter) reside under com.zeromail.core.shared.privacy"
    - "logback-spring.xml references the new FQN for SensitiveMarkerScrubFilter (no ClassNotFoundException at boot)"
    - "ApplicationModulesTest.verify() is green with the working naming form documented"
    - "SafetyContractArchTests' SENSITIVE FQN literal matches the new package"
    - "LogScrubSyntheticTrafficTest passes (proves filter is wired and scrubs Sensitive markers)"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/shared/privacy/Sensitive.java"
      provides: "Sensitive<T> redaction marker at canonical FQN"
    - path: "backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveMarkerScrubFilter.java"
      provides: "Logback filter scrubbing Sensitive markers"
    - path: "backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java"
      provides: "@ApplicationModule(displayName=\"Privacy\", allowedDependencies={}) at new path; JavaDoc documents the working CL-3 naming form"
      contains: "@ApplicationModule"
  key_links:
    - from: "backend/core/src/main/resources/logback-spring.xml"
      to: "com.zeromail.core.shared.privacy.SensitiveMarkerScrubFilter"
      via: "<filter class=\"...\"/> string-FQN reference (lines 14, 27)"
      pattern: "com\\.zeromail\\.core\\.shared\\.privacy\\.SensitiveMarkerScrubFilter"
    - from: "backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java"
      to: "com.zeromail.core.shared.privacy.Sensitive"
      via: "private static final String SENSITIVE literal"
      pattern: "com\\.zeromail\\.core\\.shared\\.privacy\\.Sensitive"
---

<objective>
Move the privacy module from `com.zeromail.core.privacy.*` to `com.zeromail.core.shared.privacy.*` (per CL-1 — `core.shared` is the canonical parent, NOT `shared` as a sibling of `core`). Update every string-FQN reference (logback XML + ArchUnit fixture) inside the SAME commit as the Java class moves so the silent-failure mode in Pitfall 1 cannot occur. Empirically lock the Spring Modulith naming form for nested packages (CL-3 probe) before downstream plans depend on it.

Purpose: This is the lowest-risk move and the foundation every other plan builds on. Plan 02-05 will all reference `shared.privacy` (or whatever CL-3 probe locks) in their `allowedDependencies`. Locking the naming form here prevents 5 plans from re-discovering the same Modulith verification failure.
Output: Three privacy classes at new package; logback wired to new FQN; ArchUnit fixture updated; CL-3 working form documented in JavaDoc on `core/shared/privacy/package-info.java`; old `core/privacy/package-info.java` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.2-domain-owned-persistence-restructuring/01.2-CONTEXT.md
@.planning/phases/01.2-domain-owned-persistence-restructuring/01.2-RESEARCH.md
@.planning/phases/01.2-domain-owned-persistence-restructuring/01.2-PATTERNS.md
@.planning/phases/01.2-domain-owned-persistence-restructuring/01.2-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing files this plan moves; bodies are byte-identical, only `package` decl + import lines change. -->

backend/core/src/main/java/com/zeromail/core/privacy/Sensitive.java — wrapper record `public record Sensitive<T>(T value)` with `toString()` returning `"[REDACTED]"`. Body unchanged.

backend/core/src/main/java/com/zeromail/core/privacy/SensitiveJacksonModule.java — Jackson module that serializes `Sensitive<T>` as `"[REDACTED]"`. Body unchanged.

backend/core/src/main/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilter.java — Logback `Filter<ILoggingEvent>` (per-event filter, NOT TurboFilter). Class body unchanged.

backend/core/src/main/java/com/zeromail/core/privacy/package-info.java — current annotation `@ApplicationModule(displayName = "Privacy", allowedDependencies = {})`.

backend/core/src/main/resources/logback-spring.xml — lines 14, 27 each contain `<filter class="com.zeromail.core.privacy.SensitiveMarkerScrubFilter"/>`. Both must update.

backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java line 20 — `private static final String SENSITIVE = "com.zeromail.core.privacy.Sensitive";`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move privacy classes + package-info to com.zeromail.core.shared.privacy</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/shared/privacy/Sensitive.java,
    backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveJacksonModule.java,
    backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveMarkerScrubFilter.java,
    backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java,
    backend/core/src/main/java/com/zeromail/core/privacy/package-info.java (delete),
    backend/core/src/main/java/com/zeromail/core/privacy/Sensitive.java (delete),
    backend/core/src/main/java/com/zeromail/core/privacy/SensitiveJacksonModule.java (delete),
    backend/core/src/main/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilter.java (delete)
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/privacy/Sensitive.java (verbatim body — copy unchanged to new path)
    - backend/core/src/main/java/com/zeromail/core/privacy/SensitiveJacksonModule.java (verbatim body)
    - backend/core/src/main/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilter.java (verbatim body)
    - backend/core/src/main/java/com/zeromail/core/privacy/package-info.java (annotation analog)
    - backend/core/src/main/java/com/zeromail/core/tenant/package-info.java (annotation pattern analog — Pattern 1)
    - .planning/phases/01.2-domain-owned-persistence-restructuring/01.2-PATTERNS.md (Plan 01.2-01 row + Pattern 1)
    - .planning/phases/01.2-domain-owned-persistence-restructuring/01.2-VALIDATION.md (rows for plan 01.2-01)
  </read_first>
  <action>
    1. Use `git mv` to relocate the three Java source files (preserving git history):
       - `git mv backend/core/src/main/java/com/zeromail/core/privacy/Sensitive.java backend/core/src/main/java/com/zeromail/core/shared/privacy/Sensitive.java`
       - `git mv backend/core/src/main/java/com/zeromail/core/privacy/SensitiveJacksonModule.java backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveJacksonModule.java`
       - `git mv backend/core/src/main/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilter.java backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveMarkerScrubFilter.java`
    2. In each of the three relocated files, update the package declaration from `package com.zeromail.core.privacy;` to `package com.zeromail.core.shared.privacy;`. Class bodies remain byte-identical.
    3. Create new file `backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java` with EXACT content (CL-3 working form will be patched in Task 3 once probed):
       ```java
       /**
        * Cross-cutting Sensitive&lt;T&gt; marker contract: redaction wrapper, Jackson module, Logback scrub filter.
        * The only inhabitant of {@code core.shared/} in Phase 1.2.
        *
        * <p>Spring Modulith naming form for cross-sibling references: TBD by Plan 01.2-01 Task 3 probe.
        * (CL-3 — the working form will be inserted here and applied identically across all package-info files.)
        */
       @ApplicationModule(displayName = "Privacy", allowedDependencies = {})
       package com.zeromail.core.shared.privacy;

       import org.springframework.modulith.ApplicationModule;
       ```
    4. Delete the old `backend/core/src/main/java/com/zeromail/core/privacy/package-info.java` (the old `core.privacy` package becomes empty after the moves above; deleting `package-info.java` removes its Modulith module registration).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/core/src/main/java/com/zeromail/core/shared/privacy/Sensitive.java` (new location exists)
    - `test -f backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveJacksonModule.java`
    - `test -f backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveMarkerScrubFilter.java`
    - `test -f backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java`
    - `! test -f backend/core/src/main/java/com/zeromail/core/privacy/Sensitive.java` (old location gone)
    - `! test -f backend/core/src/main/java/com/zeromail/core/privacy/SensitiveJacksonModule.java`
    - `! test -f backend/core/src/main/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilter.java`
    - `! test -f backend/core/src/main/java/com/zeromail/core/privacy/package-info.java`
    - `grep -F 'package com.zeromail.core.shared.privacy;' backend/core/src/main/java/com/zeromail/core/shared/privacy/Sensitive.java` returns a line
    - `grep -F 'package com.zeromail.core.shared.privacy;' backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveJacksonModule.java` returns a line
    - `grep -F 'package com.zeromail.core.shared.privacy;' backend/core/src/main/java/com/zeromail/core/shared/privacy/SensitiveMarkerScrubFilter.java` returns a line
    - `grep -F '@ApplicationModule(displayName = "Privacy", allowedDependencies = {})' backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java` returns a line
  </acceptance_criteria>
  <done>Three Java sources + new package-info exist under `com.zeromail.core.shared.privacy`; old `core.privacy` package contains zero `.java` files; `./gradlew :backend:core:compileJava` succeeds (compile is the gate — runtime/Modulith verification is Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Update string-FQN references (logback XML, ArchUnit, two test files) in the same commit</name>
  <files>
    backend/core/src/main/resources/logback-spring.xml,
    backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java,
    backend/core/src/test/java/com/zeromail/core/privacy/SensitiveToStringTest.java,
    backend/core/src/test/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilterTest.java
  </files>
  <read_first>
    - backend/core/src/main/resources/logback-spring.xml (current state — both `<filter class="..."/>` lines)
    - backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java (line 20 SENSITIVE constant)
    - backend/core/src/test/java/com/zeromail/core/privacy/SensitiveToStringTest.java (current package + imports)
    - backend/core/src/test/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilterTest.java (current package + imports)
    - .planning/phases/01.2-domain-owned-persistence-restructuring/01.2-RESEARCH.md (Pitfall 1 — silent-removal failure mode)
    - .planning/phases/01.2-domain-owned-persistence-restructuring/01.2-PATTERNS.md (Pattern 2a)
  </read_first>
  <action>
    1. **logback-spring.xml** — replace BOTH occurrences (lines 14 and 27) of:
       ```
       <filter class="com.zeromail.core.privacy.SensitiveMarkerScrubFilter"/>
       ```
       with EXACTLY:
       ```
       <filter class="com.zeromail.core.shared.privacy.SensitiveMarkerScrubFilter"/>
       ```
       (Pitfall 1 — Logback class strings are NOT compile-checked; missing this leaves the app running with no scrub filter.)

    2. **SafetyContractArchTests.java line 20** — replace EXACTLY:
       ```java
       private static final String SENSITIVE = "com.zeromail.core.privacy.Sensitive";
       ```
       with EXACTLY (per CL-1 — `core.shared.privacy`, NOT `shared.privacy`):
       ```java
       private static final String SENSITIVE = "com.zeromail.core.shared.privacy.Sensitive";
       ```

    3. **SensitiveToStringTest.java** — update its `package` declaration from `package com.zeromail.core.privacy;` to `package com.zeromail.core.shared.privacy;`, then physically move the file with `git mv backend/core/src/test/java/com/zeromail/core/privacy/SensitiveToStringTest.java backend/core/src/test/java/com/zeromail/core/shared/privacy/SensitiveToStringTest.java`. Update any internal imports of `com.zeromail.core.privacy.Sensitive` → `com.zeromail.core.shared.privacy.Sensitive`.

    4. **SensitiveMarkerScrubFilterTest.java** — same treatment as Task 2.3: `git mv` to `backend/core/src/test/java/com/zeromail/core/shared/privacy/SensitiveMarkerScrubFilterTest.java`, update package decl, update imports of moved classes.

    5. After moves, the test directory `backend/core/src/test/java/com/zeromail/core/privacy/` should be empty — no further action (git tracks deletion automatically).
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "SafetyContractArchTests" --tests "SensitiveToStringTest" --tests "SensitiveMarkerScrubFilterTest" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'com.zeromail.core.privacy.SensitiveMarkerScrubFilter' backend/core/src/main/resources/logback-spring.xml` returns `0` (no stale references)
    - `grep -c 'com.zeromail.core.shared.privacy.SensitiveMarkerScrubFilter' backend/core/src/main/resources/logback-spring.xml` returns `2` (both appender filters updated)
    - `grep -F 'private static final String SENSITIVE = "com.zeromail.core.shared.privacy.Sensitive";' backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java` returns a line
    - `! grep -F 'com.zeromail.core.privacy.Sensitive' backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java`
    - `test -f backend/core/src/test/java/com/zeromail/core/shared/privacy/SensitiveToStringTest.java`
    - `test -f backend/core/src/test/java/com/zeromail/core/shared/privacy/SensitiveMarkerScrubFilterTest.java`
    - `! test -f backend/core/src/test/java/com/zeromail/core/privacy/SensitiveToStringTest.java`
    - `! test -f backend/core/src/test/java/com/zeromail/core/privacy/SensitiveMarkerScrubFilterTest.java`
    - The verify command exits 0 (3 tests green)
  </acceptance_criteria>
  <done>All three string-FQN references updated; both privacy tests moved + repackaged; `./gradlew :backend:core:test --tests "SafetyContractArchTests" --tests "SensitiveToStringTest" --tests "SensitiveMarkerScrubFilterTest"` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Modulith naming probe + LogScrubSyntheticTrafficTest gate (CL-3 + Pitfall 1 closure)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java (current state from Task 1)
    - backend/core/src/main/java/com/zeromail/core/tenant/package-info.java (analog)
    - backend/api/src/test/java/com/zeromail/api/ApplicationModulesTest.java (the verification gate)
    - .planning/phases/01.2-domain-owned-persistence-restructuring/01.2-RESEARCH.md (Pitfall 2 — three candidate forms)
    - .planning/phases/01.2-domain-owned-persistence-restructuring/01.2-VALIDATION.md (Plan 01.2-01 rows + manual probe row)
  </read_first>
  <action>
    Empirical CL-3 probe — in this exact order, stopping at the first form that makes `ApplicationModulesTest.verify()` green:

    1. **Probe A — `"shared.privacy"` (dotted nested form):** Update `core.tenant/package-info.java` to declare `allowedDependencies = {"shared.privacy"}` (temporary single-source probe). Run:
       `./gradlew :backend:api:test --tests "ApplicationModulesTest" --no-daemon`
       - **If green:** the working form is `"shared.privacy"`. Skip to step 4.
       - **If red with `IllegalArgumentException: Module ... does not exist!`:** revert tenant/package-info.java to `allowedDependencies = {}` and proceed to Probe B.

    2. **Probe B — `"privacy"` (local simple-name form):** Update `core.tenant/package-info.java` to `allowedDependencies = {"privacy"}`. Run the same Modulith verify command.
       - **If green:** working form is `"privacy"`.
       - **If red:** revert to `{}` and proceed to Probe C.

    3. **Probe C — named-interface form `"shared :: privacy"`:** Add `@ApplicationModule(displayName = "Shared")` to a NEW file `backend/core/src/main/java/com/zeromail/core/shared/package-info.java` (parent module marker). Update `core.tenant/package-info.java` `allowedDependencies = {"shared :: privacy"}`. Run verify.
       - **If green:** working form is `"shared :: privacy"`. Keep the new `core/shared/package-info.java` file.
       - **If red:** STOP. Surface as a checkpoint — none of the three documented forms work; planner must decide between (a) keeping privacy at original `com.zeromail.core.privacy` location (revert all moves) or (b) escalating to user for fresh CL-3 guidance.

    4. **Lock the working form** in the JavaDoc of `backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java`. Replace its current TBD JavaDoc with a concrete record, e.g.:
       ```java
       /**
        * Cross-cutting Sensitive&lt;T&gt; marker contract: redaction wrapper, Jackson module, Logback scrub filter.
        * The only inhabitant of {@code core.shared/} in Phase 1.2.
        *
        * <p><b>Spring Modulith naming form (CL-3 probe locked 2026-04-26):</b>
        * Cross-sibling modules MUST reference this module as {@code "<WORKING_FORM>"} in their
        * {@code allowedDependencies} array. (Probe results: A=&lt;pass|fail&gt;, B=&lt;pass|fail&gt;, C=&lt;pass|fail&gt;.)
        */
       @ApplicationModule(displayName = "Privacy", allowedDependencies = {})
       package com.zeromail.core.shared.privacy;

       import org.springframework.modulith.ApplicationModule;
       ```
       Replace `<WORKING_FORM>` with the literal string that worked (`shared.privacy`, `privacy`, or `shared :: privacy`). Replace `<pass|fail>` with the actual probe outcomes.

    5. **Restore tenant/package-info.java to the locked working form** (so the probe modification is not left behind):
       ```java
       @ApplicationModule(displayName = "Tenant", allowedDependencies = {"<WORKING_FORM>"})
       package com.zeromail.core.tenant;

       import org.springframework.modulith.ApplicationModule;
       ```

    6. **Pitfall 1 closure** — run the integration test that proves the Logback filter is wired:
       `./gradlew :backend:api:test --tests "LogScrubSyntheticTrafficTest" --no-daemon`
       - Must exit 0. If it fails with `ClassNotFoundException` for `SensitiveMarkerScrubFilter`, the Task 2 logback XML update is incomplete — go fix it and re-run.

    7. **Final Modulith re-verify** with the locked form across both updated package-info files:
       `./gradlew :backend:api:test --tests "ApplicationModulesTest" --no-daemon`
  </action>
  <verify>
    <automated>./gradlew :backend:api:test --tests "ApplicationModulesTest" --tests "LogScrubSyntheticTrafficTest" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'CL-3 probe locked' backend/core/src/main/java/com/zeromail/core/shared/privacy/package-info.java` returns a line (working form documented)
    - `grep -E '@ApplicationModule.*allowedDependencies = \{"(shared\.privacy|privacy|shared :: privacy)"\}' backend/core/src/main/java/com/zeromail/core/tenant/package-info.java` returns a line (tenant uses one of the three documented forms)
    - The verify command exits 0 (both Modulith verify and synthetic-traffic scrub test pass)
    - `grep -c 'com.zeromail.core.privacy' backend/core/src/main/resources/logback-spring.xml` returns `0`
    - The logback XML still contains `com.zeromail.core.shared.privacy.SensitiveMarkerScrubFilter` exactly twice (idempotency from Task 2)
  </acceptance_criteria>
  <done>Working CL-3 form is documented in `core/shared/privacy/package-info.java` JavaDoc; `core/tenant/package-info.java` references it; ApplicationModulesTest.verify() green; LogScrubSyntheticTrafficTest green (proving filter wiring + scrub behavior survived the move).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application logs → operator/SRE eyes | Sensitive markers must be scrubbed before any log stream leaves the JVM |
| build-time ArchUnit invariants → runtime | FQN literals in test fixtures are the only enforcement of `Sensitive<T>` discipline; literal drift silently disables the rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-A | Information Disclosure | logback-spring.xml `<filter class="..."/>` string FQN | mitigate | Update both occurrences (lines 14, 27) in the same commit as the Java class moves (Task 2). LogScrubSyntheticTrafficTest in Task 3 is the runtime gate — fails if Logback can't instantiate the class (silent removal of scrub filter). |
| T-01.2-B | Tampering (silent guardrail removal) | SafetyContractArchTests SENSITIVE FQN literal | mitigate | Update line 20 to canonical `com.zeromail.core.shared.privacy.Sensitive` per CL-1 in same commit (Task 2). SafetyContractArchTests run in Task 2 verify command catches mismatch via `ClassNotFoundException` on rule evaluation. |
</threat_model>

<verification>
- `./gradlew :backend:core:compileJava` succeeds after Task 1
- `./gradlew :backend:core:test --tests "SafetyContractArchTests" --tests "SensitiveToStringTest" --tests "SensitiveMarkerScrubFilterTest"` green after Task 2
- `./gradlew :backend:api:test --tests "ApplicationModulesTest" --tests "LogScrubSyntheticTrafficTest"` green after Task 3
- `grep -rn 'com.zeromail.core.privacy' backend/ apps/ 2>/dev/null` returns ZERO matches (full sweep — no stale references anywhere)
- The CL-3 working form is documented exactly once in `core/shared/privacy/package-info.java` and used exactly once in `core/tenant/package-info.java` (downstream plans will reuse it)
</verification>

<success_criteria>
- Three privacy classes (Sensitive, SensitiveJacksonModule, SensitiveMarkerScrubFilter) live at `com.zeromail.core.shared.privacy.*`
- Old `com.zeromail.core.privacy` package no longer exists in the source tree
- logback-spring.xml references the new FQN at both appender positions
- SafetyContractArchTests SENSITIVE constant updated (CL-1 canonical form)
- CL-3 Modulith naming form probed, locked, and documented
- LogScrubSyntheticTrafficTest green — proves filter is wired AND scrubs Sensitive markers (Pitfall 1 closure)
- ApplicationModulesTest.verify() green with new shared.privacy module + tenant→privacy edge
- Two privacy tests relocated to `core/shared/privacy/` with updated package decls
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-domain-owned-persistence-restructuring/01.2-01-SUMMARY.md` documenting:
- The CL-3 working form locked (one of `shared.privacy` / `privacy` / `shared :: privacy`)
- Probe outcomes (which probes passed/failed) for traceability
- Confirmation that `grep -rn 'com.zeromail.core.privacy' backend/ apps/` returns zero hits
- LogScrubSyntheticTrafficTest result + ApplicationModulesTest result
</output>
