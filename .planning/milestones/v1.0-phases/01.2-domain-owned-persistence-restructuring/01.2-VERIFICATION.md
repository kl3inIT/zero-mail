---
phase: 01.2-domain-owned-persistence-restructuring
verified: 2026-04-26T10:01:21Z
status: passed
score: 5/5 roadmap success criteria verified (plus all 6 plan must_haves)
overrides_applied: 0
---

# Phase 01.2: Domain-Owned Persistence Restructuring — Verification Report

**Phase Goal (ROADMAP):** Refactor `backend/core` away from a global persistence bucket into domain-owned `service`, `persistence`, and `model` packages, with a small `shared` package reserved only for stable cross-cutting infrastructure. Preserve existing schema and Phase 1 privacy/safety constraints while making module boundaries explicit and enforceable.
**Verified:** 2026-04-26T10:01:21Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|---|---|---|
| SC1 | Account, onboarding, gmail, tenant, privacy, and crypto code organized by domain ownership rather than a single global `core.persistence` package | VERIFIED | `backend/core/.../com/zeromail/core/` contains exactly `{account, gmail, onboarding, shared, tenant}`. Legacy `core.persistence/`, `core.crypto/`, `core.privacy/` directories are **completely absent** (verified via `ls`). |
| SC2 | Entities and repositories live under their owning domain's `persistence` subpackage unless genuinely shared infrastructure | VERIFIED | `UserEntity/UserRepository` → `core.account.persistence`; `TenantEntity/TenantRepository` → `core.tenant.persistence`; `OnboardingSelectionEntity/Repository` → `core.onboarding.persistence`; `GmailConnectionEntity/Repository` → `core.gmail.persistence`; `RefreshTokenCipher` → `core.gmail.persistence.crypto`. |
| SC3 | A small `shared` package exists only for stable cross-cutting infrastructure; not a business-logic dumping ground | VERIFIED | `core.shared/` contains only `privacy/` (Sensitive, SensitiveJacksonModule, SensitiveMarkerScrubFilter, package-info). Per D-C1, that is the only inhabitant after Phase 1.2. |
| SC4 | Existing table names, Liquibase changelog semantics, tenant isolation, log-scrub rules, and API behavior preserved | VERIFIED | `./gradlew :backend:core:check :backend:api:check` BUILD SUCCESSFUL — all integration/E2E tests including FND-05 multi-tenant leak, MeLanguageIntegrationTest, AccountDeletionE2E, SessionCookieE2E, RefreshTokenCipherTest, LogScrubSyntheticTrafficTest pass. Liquibase changelogs untouched (Plan 06 must_have asserts byte-identical diff). |
| SC5 | Modulith and/or ArchUnit tests enforce the new boundaries, including restrictions on cross-domain access to persistence internals | VERIFIED | `ApplicationModulesTest.verify()` green; `DomainBoundaryArchTests` (4 ArchRules — one per domain) bans cross-domain `*Repository` imports; `TenantIsolationArchTests` regex updated to `..persistence.lowlevel..`; `SafetyContractArchTests` SENSITIVE FQN updated to `com.zeromail.core.shared.privacy.Sensitive`. |

**Score:** 5/5 ROADMAP success criteria verified. All 6 plans' must_haves independently verified below.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `backend/core/.../core/shared/privacy/{Sensitive, SensitiveJacksonModule, SensitiveMarkerScrubFilter, package-info}.java` | Privacy classes at canonical FQN with `@ApplicationModule(displayName="Privacy", allowedDependencies={})` | VERIFIED | All 4 files present; package-info documents CL-3 probe result A (`shared.privacy` form) PASS. |
| `backend/core/.../core/tenant/persistence/{TenantEntity, TenantRepository, package-info, lowlevel/package-info}.java` | Tenant persistence module with proactive lowlevel marker | VERIFIED | All 4 files present. |
| `backend/core/.../core/account/persistence/{UserEntity, UserRepository}.java` + `account/model/{CurrentUserView, CurrentUserNotFoundException}.java` + `account/service/{AccountService, OAuthProvisioningService}.java` | Account domain reshaped per D-D1 + CL-2 | VERIFIED | All present. `AccountService` injects ONLY `UserRepository` (verified at lines 27–31). `deleteCurrentUser(UUID)` exists at line 89 and only deletes the User row. |
| `backend/core/.../core/onboarding/{persistence,model,service}/...` | Onboarding domain (entity/repo + enum + service) | VERIFIED | `OnboardingSelectionEntity/Repository` in `persistence/`, `OnboardingStep` enum in `model/`, `OnboardingService` in `service/`. `deleteSelectionsForCurrentTenant(UUID)` exists at line 56. |
| `backend/core/.../core/gmail/{persistence,persistence/crypto,model,service}/...` | Gmail domain with renamed view/service + relocated cipher | VERIFIED | `GmailConnectionEntity/Repository`, `GmailConnectionStatus`, `GmailConnectionView`, `GmailConnectionService`, `RefreshTokenCipher`, `RefreshTokenCryptoConfig` all at expected paths. `deleteForCurrentTenant(UUID)` exists at line 57. |
| `backend/core/.../core/tenant/service/TenantService.java` | First occupant of tenant/service with `deleteCurrentTenant` + `createTenant` | VERIFIED | Present (49 lines). `deleteCurrentTenant(UUID)` at line 45. |
| `backend/api/.../api/Application.java` + `backend/core/.../support/CoreTestApplication.java` | Both collapse `@EntityScan`/`@EnableJpaRepositories` to single root `com.zeromail.core` | VERIFIED | `Application.java:11–12` and `CoreTestApplication.java:18–19` both use `basePackages = "com.zeromail.core"`. |
| `backend/api/.../controllers/AccountDeletionController.java` | Zero direct repository injections; orchestrates 4 service calls in FK-safe order under `@Transactional` | VERIFIED | Constructor injects only 4 services (`OnboardingService, GmailConnectionService, AccountService, TenantService`); `deleteAccount()` calls them in order children → user → tenant; method-level `@Transactional`. |
| `backend/api/.../controllers/TenantStatusController.java` | Uses `private static toResponse(GmailConnectionView)` helper (D-B5) | VERIFIED | `toResponse` at line 29; matches `MeController` pattern. |
| `backend/core/.../arch/DomainBoundaryArchTests.java` | 4 ArchRules (one per domain) banning cross-domain repository imports | VERIFIED | `account_no_cross_domain_repos`, `onboarding_no_cross_domain_repos`, `gmail_no_cross_domain_repos`, `tenant_no_cross_domain_repos` — 4 rules total. |
| `backend/core/.../arch/SafetyContractArchTests.java` | `SENSITIVE` FQN literal at `com.zeromail.core.shared.privacy.Sensitive` (CL-1 corrected form) | VERIFIED | Line 20: `private static final String SENSITIVE = "com.zeromail.core.shared.privacy.Sensitive";` |
| `backend/core/.../arch/TenantIsolationArchTests.java` | Native-SQL allow-list regex `..persistence.lowlevel..` spans all 4 domains | VERIFIED | Line 34: `.resideOutsideOfPackage("..persistence.lowlevel..")`. |
| `backend/core/.../onboarding/OnboardingStepEnumPersistenceTest.java` | Pitfall 5 / T-01.2-E test exists | VERIFIED | Wave 0 test asserts the 4 enum constants (`SIGNED_IN`, `GMAIL_CONNECTED`, `TEMPLATE_SELECTED`, `COMPLETE`) preserve their persisted `name()` strings. |
| `backend/core/src/main/resources/logback-spring.xml` | References new FQN `com.zeromail.core.shared.privacy.SensitiveMarkerScrubFilter` | VERIFIED | Lines 14, 27 reference the new FQN; no stale `core.privacy.*` references. |
| Per-domain `persistence/lowlevel/package-info.java` markers | 4 domains each have a `lowlevel/` package-info (D-D3 proactive option) | VERIFIED | All 4 (`account`, `gmail`, `onboarding`, `tenant`) present. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `logback-spring.xml` | `core.shared.privacy.SensitiveMarkerScrubFilter` | `<filter class="...">` (lines 14, 27) | WIRED | New FQN; verified by LogScrubSyntheticTrafficTest passing in `./gradlew :backend:core:check`. |
| `SafetyContractArchTests` | `com.zeromail.core.shared.privacy.Sensitive` | `private static final String SENSITIVE` literal | WIRED | Updated FQN; rule passes (`./gradlew :backend:core:test --tests *SafetyContractArchTests` green). |
| `tenant/package-info` | `shared.privacy` module | `@ApplicationModule(allowedDependencies={"shared.privacy"})` | WIRED | CL-3 form A (`shared.privacy` dotted nested) confirmed; ApplicationModulesTest green. |
| `account/package-info` | `{tenant, onboarding, shared.privacy}` | `@ApplicationModule(allowedDependencies={...})` | WIRED | Exactly per D-D4. |
| `gmail/package-info` | `{tenant, shared.privacy}` | `@ApplicationModule(allowedDependencies={...})` | WIRED | Exactly per D-D4 — explicitly NO account edge. |
| `onboarding/package-info` | `{tenant, account, shared.privacy}` | `@ApplicationModule(allowedDependencies={...})` | WIRED | Exactly per D-D4. |
| `Application.java` | All entities under `com.zeromail.core` | `@EntityScan(basePackages = "com.zeromail.core")` | WIRED | Single root scan covers all 4 per-domain `persistence/` packages. |
| `CoreTestApplication.java` | All entities under `com.zeromail.core` | `@EntityScan(basePackages = "com.zeromail.core")` | WIRED | Test-side parity matches main `Application.java`. |
| `AccountDeletionController.deleteAccount()` | 4 single-domain service deletes | DI of `{OnboardingService, GmailConnectionService, AccountService, TenantService}` | WIRED | FK-safe order: onboarding → gmail → user → tenant; method-level `@Transactional`. |
| `OnboardingService.selectTemplate/complete` | `AccountService.advanceOnboardingStep` | constructor DI of `AccountService` | WIRED | D-D1: onboarding no longer injects `UserRepository` directly. |
| `TenantStatusController.status()` | `GmailConnectionView` mapping | `private static TenantStatusResponse toResponse(GmailConnectionView view)` | WIRED | D-B5 pattern matches MeController. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Compile main + test sources for backend modules | `./gradlew :backend:core:compileJava :backend:api:compileJava :backend:core:compileTestJava :backend:api:compileTestJava` | BUILD SUCCESSFUL in 17s; all UP-TO-DATE | PASS |
| Gate test suite (Modulith + ArchUnit + Pitfall-5 enum test) | `./gradlew :backend:core:test --tests "*DomainBoundaryArchTests" --tests "*TenantIsolationArchTests" --tests "*SafetyContractArchTests" --tests "*OnboardingStepEnumPersistenceTest" :backend:api:test --tests "*ApplicationModulesTest"` | BUILD SUCCESSFUL in 13s | PASS |
| Full backend check (Plan 06 final gate) | `./gradlew :backend:core:check :backend:api:check` | BUILD SUCCESSFUL in 50s | PASS |
| Legacy package absence | `ls backend/core/.../com/zeromail/core/persistence` and `.../core/crypto` and `.../core/privacy` | All three return "No such file or directory" | PASS |
| Old class-name references | grep `TenantConnectionService\|TenantConnectionView` across `backend/` | Zero matches | PASS |

### Requirements Coverage

| Plan | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| (none — all 6 plans declare `requirements: ["N/A — structural refactor"]`) | All 6 PLANs | Phase 1.2 is architectural restructuring; ROADMAP explicitly says "_(architecture restructuring; no new product requirements yet)_" | N/A | `grep "Phase 1.2" .planning/REQUIREMENTS.md` returns zero results — confirmed no new REQ-IDs scoped to this phase. |

No orphaned requirements. ROADMAP and REQUIREMENTS.md are consistent: Phase 1.2 ships zero new product requirements.

### Anti-Patterns Found

None. Spot scans for stubs were unnecessary — this is a refactor phase moving existing classes; full `./gradlew check` exercising all integration tests passing is the strongest signal that no behavior was broken or stubbed.

### Plan-Level Must-Have Roll-up

| Plan | Must-haves | Status | Notes |
|---|---|---|---|
| 01.2-01 (privacy → shared.privacy) | 5 truths + 3 artifacts + 2 key_links | PASSED | All 3 privacy classes at new FQN; logback updated lines 14/27; CL-3 probe A passed; ArchUnit SENSITIVE FQN updated. |
| 01.2-02 (tenant persistence) | 6 truths + 4 artifacts + 2 key_links | PASSED | TenantEntity/Repository moved; lowlevel/ marker present; allowedDependencies = {shared.privacy}. |
| 01.2-03 (account reshape) | 8 truths + 3 artifacts + 2 key_links | PASSED | UserEntity/Repository at new path; AccountService injects only UserRepository; `deleteCurrentUser(UUID)` present. |
| 01.2-04 (onboarding) | 9 truths + 4 artifacts + 2 key_links | PASSED | All onboarding artifacts moved; `deleteSelectionsForCurrentTenant` present; UserEntity imports `OnboardingStep` from new package; Pitfall-5 test exists. |
| 01.2-05 (gmail + crypto + cleanup) | 10 truths + 4 artifacts + 2 key_links | PASSED | Gmail rename complete; cipher relocated; legacy `core.crypto`/`core.persistence` deleted; @EntityScan collapsed to single root. |
| 01.2-06 (final boundaries + cleanup) | 6 truths + 2 artifacts + 2 key_links | PASSED | DomainBoundaryArchTests has 4 rules; TenantIsolationArchTests regex updated; TenantStatusController uses toResponse helper; `./gradlew check` green. |

### Pitfall Coverage

| Pitfall (from RESEARCH.md) | Status | Evidence |
|---|---|---|
| Pitfall 1 — string-FQN drift | COVERED | logback `<filter class>` and ArchUnit string literals updated to new FQNs; verified by passing tests. |
| Pitfall 5 — `@Enumerated(EnumType.STRING)` persistence drift on enum move | COVERED | `OnboardingStepEnumPersistenceTest` (Wave 0 net-new) asserts 4 enum names. |

### Human Verification Required

None. All five ROADMAP success criteria verifiable programmatically; no UI/UX or external-service behaviors changed (refactor preserves byte-identical schema and API contracts). `./gradlew check` is the integration-level proof.

### Gaps Summary

No gaps. All 5 ROADMAP success criteria, all 6 plans' must_haves, all key links, and both targeted pitfalls are independently verified against the codebase. The full backend `check` task (Modulith verify + ArchUnit suite + integration + E2E + Phase 1.1 contract) is BUILD SUCCESSFUL.

The domain-owned restructuring is complete: legacy `core.persistence/`, `core.crypto/`, and `core.privacy/` packages are entirely absent; the four business domains (`account`, `gmail`, `onboarding`, `tenant`) own their `service/`, `persistence/`, and `model/` sub-packages with `lowlevel/` markers; `shared/privacy/` is the sole cross-cutting subpackage; Modulith `allowedDependencies` graph matches D-D4 exactly (gmail explicitly has no account edge); CL-2 single-domain delete methods exist on all 4 domain services; `AccountDeletionController` orchestrates the FK-safe cascade with zero direct repository injections; `@EntityScan`/`@EnableJpaRepositories` are collapsed to a single root in both main and test applications; renames `TenantConnectionService → GmailConnectionService` and `TenantConnectionView → GmailConnectionView` are complete with no leftover references.

---

_Verified: 2026-04-26T10:01:21Z_
_Verifier: Claude (gsd-verifier)_
