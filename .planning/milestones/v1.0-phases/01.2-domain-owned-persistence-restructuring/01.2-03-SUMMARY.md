---
phase: 01.2-domain-owned-persistence-restructuring
plan: 03
subsystem: persistence
tags: [spring-modulith, java-25, refactor, package-rename, account, jpa, entity-scan, cl-2]

# Dependency graph
requires:
  - phase: 01.2-domain-owned-persistence-restructuring
    plan: 02
    provides: "Tenant entity/repo at core.tenant.persistence; transitional 2-element @EntityScan array; Pitfall-1 closure protocol confirmed; OAuthProvisioningService + AccountService already had Tenant* imports updated by Plan 02 sweep"
  - phase: 01.2-domain-owned-persistence-restructuring
    plan: 01
    provides: "Locked CL-3 form `\"shared.privacy\"`"
  - phase: 01-foundation-safety-infrastructure
    provides: "AccountService + OAuthProvisioningService originals; AccountDeletionController; MeController; AccountDeletionE2ETest; MeLanguageIntegrationTest; OnboardingStateMachineTest; ApplicationModulesTest"
provides:
  - "UserEntity + UserRepository at the domain-owned path com.zeromail.core.account.persistence"
  - "CurrentUserView + CurrentUserNotFoundException at com.zeromail.core.account.model"
  - "AccountService + OAuthProvisioningService at com.zeromail.core.account.service"
  - "AccountService reshaped per CL-2: 4 cross-domain repos → 1 (UserRepository only); deleteCurrentTenantAccount → deleteCurrentUser (single-domain)"
  - "AccountDeletionController on TRANSITIONAL bridge with FK-safe delete order + controller-level @Transactional (Plan 04/05/06 progressively replace direct repo calls with per-domain services)"
  - "core.account/package-info.java updated to allowedDependencies = {\"tenant\", \"shared.privacy\"} (forward-deferring \"onboarding\" to Plan 04 when that module actually exists)"
  - "Widened transitional 3-element @EntityScan arrays in Application.java + CoreTestApplication.java"
affects: [01.2-04, 01.2-05, 01.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CL-2 reshape pattern: domain service drops cross-domain repo injections; multi-domain orchestration moves to API-tier controller with controller-level @Transactional + FK-safe delete order"
    - "Transitional bridge controller idiom: API controller temporarily holds cross-domain repos for orchestration; per-domain services replace each repo call across subsequent plans"
    - "Pitfall 1 closure protocol applied identically to Plan 02: every stale FQN reference (16 import sites this plan) updated alongside the git mv"

key-files:
  created:
    - "backend/core/src/main/java/com/zeromail/core/account/persistence/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/account/persistence/lowlevel/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/account/model/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/account/service/package-info.java"
  modified:
    - "backend/core/src/main/java/com/zeromail/core/account/persistence/UserEntity.java (relocated; package decl updated; explicit OnboardingStep import added)"
    - "backend/core/src/main/java/com/zeromail/core/account/persistence/UserRepository.java (relocated; package decl updated)"
    - "backend/core/src/main/java/com/zeromail/core/account/model/CurrentUserView.java (relocated)"
    - "backend/core/src/main/java/com/zeromail/core/account/model/CurrentUserNotFoundException.java (relocated)"
    - "backend/core/src/main/java/com/zeromail/core/account/service/AccountService.java (relocated + CL-2 reshape: 4 repos → 1; deleteCurrentTenantAccount → deleteCurrentUser)"
    - "backend/core/src/main/java/com/zeromail/core/account/service/OAuthProvisioningService.java (relocated; @Lazy self-injection idiom preserved verbatim)"
    - "backend/core/src/main/java/com/zeromail/core/account/package-info.java (allowedDependencies updated to {tenant, shared.privacy})"
    - "backend/core/src/main/java/com/zeromail/core/account/OnboardingService.java (User*/CurrentUserNotFoundException import path updates)"
    - "backend/api/src/main/java/com/zeromail/api/Application.java (@EntityScan + @EnableJpaRepositories widened to 3 packages)"
    - "backend/core/src/test/java/com/zeromail/core/support/CoreTestApplication.java (same widening as Application.java)"
    - "backend/api/src/main/java/com/zeromail/api/controllers/MeController.java (AccountService + CurrentUserView import flips)"
    - "backend/api/src/main/java/com/zeromail/api/controllers/AccountDeletionController.java (TRANSITIONAL bridge — holds cross-domain repos; FK-safe delete order; @Transactional)"
    - "backend/api/src/main/java/com/zeromail/api/security/GoogleOAuthSuccessHandler.java (OAuthProvisioningService import flip)"
    - "backend/api/src/main/java/com/zeromail/api/security/TenantBindingFilter.java (UserRepository import path)"
    - "backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java (CurrentUserNotFoundException import path)"
    - "backend/api/src/test/java/com/zeromail/api/AccountDeletionE2ETest.java (User* imports)"
    - "backend/api/src/test/java/com/zeromail/api/LogScrubSyntheticTrafficTest.java (User* imports)"
    - "backend/api/src/test/java/com/zeromail/api/OnboardingStateMachineTest.java (User* imports)"
    - "backend/api/src/test/java/com/zeromail/api/controllers/MeLanguageIntegrationTest.java (User* imports)"
    - "backend/api/src/test/java/com/zeromail/api/error/GlobalExceptionHandlerSafetyTest.java (CurrentUserNotFoundException import)"
    - "backend/api/src/test/java/com/zeromail/api/i18n/LocaleResolutionTest.java (User* imports)"
    - "backend/api/src/test/java/com/zeromail/api/security/MultiTenantLeakIntegrationTest.java (User* imports)"
    - "backend/api/src/test/java/com/zeromail/api/security/SessionCookieE2ETest.java (User* imports)"
    - "backend/api/src/test/java/com/zeromail/api/security/TestSessionSupport.java (User* imports)"
  deleted:
    - "backend/core/src/main/java/com/zeromail/core/persistence/UserEntity.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/persistence/UserRepository.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/account/CurrentUserView.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/account/CurrentUserNotFoundException.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/account/AccountService.java (moved via git mv to service/ + reshaped)"
    - "backend/core/src/main/java/com/zeromail/core/account/OAuthProvisioningService.java (moved via git mv to service/)"

key-decisions:
  - "CL-2 reshape applied: AccountService now injects ONLY UserRepository (was: 4 cross-domain repos). New deleteCurrentUser(UUID) is single-domain (deletes the User row only). The multi-domain orchestration moves to AccountDeletionController as a transitional bridge — Plans 04/05/06 progressively replace each direct repo call with the proper domain service."
  - "Forward-deferred \"onboarding\" allowedDependencies edge: core.account/package-info.java declares {\"tenant\", \"shared.privacy\"} for now — Plan 04 amends to add \"onboarding\" once that module actually exists. Adding it prematurely would make ApplicationModulesTest.verify() fail with \"Module ... does not exist!\"."
  - "Pitfall 1 sweep folded into Task 1 commit: 16 stale FQN import sites (User*, CurrentUserView, CurrentUserNotFoundException) updated alongside the git mv to keep every commit compile-green. Plan 02 lesson — never leave a commit with stale imports just to honor task boundaries."
  - "AccountDeletionController bridge landed in Task 2's commit (not Task 3): AccountService's reshape removed deleteCurrentTenantAccount, so the controller call site broke at the same compile boundary as the service relocation. Bundling the bridge into Task 2 made the package move and the controller fix atomic; Task 3 stays a metadata-only commit (package-info + entity-scan)."
  - "OnboardingService stays at core.account/ for this plan: Plan 04 will move it to core.onboarding.service. Its import sweep landed here only because User* imports broke, not because it's being relocated yet."

patterns-established:
  - "CL-2 reshape pattern: when a service holds cross-domain repository injections, drop them and move the orchestration up to the API controller (with controller-level @Transactional). Future delete-cascades-across-domains use this exact shape."
  - "Forward-decl deferral protocol for Modulith allowedDependencies: never declare an edge to a module that does not yet exist on disk — Modulith verifies module existence eagerly during ApplicationModules.verify(). Add the edge in the same plan that creates the target module."
  - "Transitional bridge controller pattern: when a multi-plan refactor must keep an integration test green throughout, the API controller temporarily holds the cross-domain dependencies and is progressively cleaned up across the chain of plans."

requirements-completed: ["N/A — structural refactor, no Phase 1 requirement IDs apply"]

# Metrics
duration: 9min
completed: 2026-04-26
---

# Phase 01.2 Plan 03: Move account domain to per-tier sub-packages + reshape AccountService per CL-2

**The account domain now lives at `com.zeromail.core.account.{service,persistence,model}.*`; AccountService injects only UserRepository (4 → 1 repos); the multi-domain delete-cascade orchestration moves to AccountDeletionController as a TRANSITIONAL bridge that keeps AccountDeletionE2ETest green during Plans 03→06; ApplicationModulesTest verifies the new `account → {tenant, shared.privacy}` Modulith edge.**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-04-26T08:55:33Z
- **Completed:** 2026-04-26T09:04:39Z
- **Tasks:** 3 (all completed)
- **Files modified:** 27 (6 source relocations + 4 new package-info markers + 17 reference updates)
- **Commits:** 3 (one per task — clean linear history, no fix-up commits)

## Accomplishments

- `UserEntity` + `UserRepository` moved to `com.zeromail.core.account.persistence.*` (rename history preserved via git mv).
- `CurrentUserView` + `CurrentUserNotFoundException` moved to `com.zeromail.core.account.model.*`.
- `AccountService` + `OAuthProvisioningService` moved to `com.zeromail.core.account.service.*`.
- **AccountService CL-2 reshape complete:** dropped 3 cross-domain repository injections (`OnboardingSelectionRepository`, `GmailConnectionRepository`, `TenantRepository`). The remaining surface is `requireCurrentUser`, `updateCurrentUserLanguage`, and the new single-domain `deleteCurrentUser(UUID)`.
- **`OAuthProvisioningService.@Lazy self` self-injection idiom preserved verbatim** (Pitfall 6 — first-login race convergence depends on the proxy resolution).
- **AccountDeletionController on transitional bridge:** holds `OnboardingSelectionRepository`, `GmailConnectionRepository`, `TenantRepository` directly + injects `AccountService`. `@Transactional` at the controller method boundary keeps the cascade atomic. FK-safe delete order: children (onboarding, gmail) → user → tenant. Plans 04/05/06 will progressively replace each direct repo call with per-domain service calls; Plan 06 then cleans up the remaining repository imports.
- **`core.account/package-info.java`** declares `allowedDependencies = {"tenant", "shared.privacy"}`. The `"onboarding"` edge is forward-deferred to Plan 04 — adding it now would fail `ApplicationModulesTest.verify()` because the `onboarding` module does not exist on disk yet (it carves out of `core.account/OnboardingService` in Plan 04).
- **Transitional 3-element `@EntityScan` + `@EnableJpaRepositories` arrays** in both `Application.java` and `CoreTestApplication.java` — `{core.persistence, core.tenant.persistence, core.account.persistence}`. Plan 05 collapses to single root `"com.zeromail.core"` after gmail/onboarding entities move.
- All Pitfall 1 silent-failure surfaces closed: 16 import sites (1 controller + 1 OAuth handler + 1 servlet filter + 1 exception handler + 1 OnboardingService transitional + 1 deletion controller + 9 integration/E2E tests) all updated in the same commit window as the source moves.
- Zero stale `com.zeromail.core.persistence.User*` or `com.zeromail.core.account.{AccountService,CurrentUserView,OAuthProvisioningService,CurrentUserNotFoundException}` references in `backend/`.
- Validation gates green: `AccountDeletionE2ETest`, `MeLanguageIntegrationTest`, `OnboardingStateMachineTest`, `ApplicationModulesTest`. Phase 1.1 contract suite still green: `ProblemDetailContractTest`, `ApiErrorSchemaTest`, `LocaleResolutionTest`.

## Task Commits

Each task committed atomically — no fix-up commits, every commit compiles cleanly:

1. **Task 1: move User*/CurrentUser* into per-tier sub-packages + sweep stale imports** — `d7fbe69` (refactor)
2. **Task 2: move account services to account.service + reshape AccountService per CL-2** — `300e51b` (refactor)
3. **Task 3: finalize account package-info + widen entity-scan to account.persistence** — `254772e` (refactor)

## Files Created/Modified

**Created (4 package-info markers):**
- `backend/core/src/main/java/com/zeromail/core/account/persistence/package-info.java` — intra-domain marker, plain JavaDoc only (no `@ApplicationModule`)
- `backend/core/src/main/java/com/zeromail/core/account/persistence/lowlevel/package-info.java` — proactive native-SQL allow-list marker (per D-D3); empty body
- `backend/core/src/main/java/com/zeromail/core/account/model/package-info.java` — domain model types (records, exceptions, enums) — explicitly NOT wire DTOs (D-B2)
- `backend/core/src/main/java/com/zeromail/core/account/service/package-info.java` — concrete @Service classes only (D-B3 — no interfaces); cross-domain reads via other domain's service (D-D1)

**Relocated via git mv (rename history preserved, package decls updated):**
- `core/persistence/UserEntity.java` → `core/account/persistence/UserEntity.java` (added explicit `import com.zeromail.core.persistence.OnboardingStep;` — was intra-package before; Plan 04 flips it)
- `core/persistence/UserRepository.java` → `core/account/persistence/UserRepository.java`
- `core/account/CurrentUserView.java` → `core/account/model/CurrentUserView.java`
- `core/account/CurrentUserNotFoundException.java` → `core/account/model/CurrentUserNotFoundException.java`
- `core/account/AccountService.java` → `core/account/service/AccountService.java` (+ CL-2 reshape)
- `core/account/OAuthProvisioningService.java` → `core/account/service/OAuthProvisioningService.java`

**Modified (transitional widening + Modulith update):**
- `backend/core/src/main/java/com/zeromail/core/account/package-info.java` — `allowedDependencies` updated from `{"persistence", "tenant"}` to `{"tenant", "shared.privacy"}` (forward-deferring `"onboarding"` to Plan 04)
- `backend/api/src/main/java/com/zeromail/api/Application.java` — `@EntityScan` + `@EnableJpaRepositories` widened from 2 to 3 packages
- `backend/core/src/test/java/com/zeromail/core/support/CoreTestApplication.java` — identical widening

**Modified (TRANSITIONAL bridge — Plans 04/05/06 will progressively clean up):**
- `backend/api/src/main/java/com/zeromail/api/controllers/AccountDeletionController.java` — full rewrite: holds `OnboardingSelectionRepository`, `GmailConnectionRepository`, `TenantRepository` injections + `AccountService`. `@Transactional` at controller boundary. FK-safe delete order: `onboardingRepo.deleteAll(...)` → `gmailRepo.delete(...)` → `accountService.deleteCurrentUser(...)` → `tenantRepo.delete(...)`.

**Modified (consumer import flips — Pitfall 1 sweep):**
- Production: `MeController`, `GoogleOAuthSuccessHandler`, `TenantBindingFilter`, `GlobalExceptionHandler`, `OnboardingService` (still at `core.account/` for this plan — moved in Plan 04)
- Tests: `AccountDeletionE2ETest`, `LogScrubSyntheticTrafficTest`, `OnboardingStateMachineTest`, `MeLanguageIntegrationTest`, `GlobalExceptionHandlerSafetyTest`, `LocaleResolutionTest`, `MultiTenantLeakIntegrationTest`, `SessionCookieE2ETest`, `TestSessionSupport`

## Decisions Made

- **CL-2 reshape applied verbatim** — `AccountService` now injects only `UserRepository`. `deleteCurrentTenantAccount(UUID)` removed; `deleteCurrentUser(UUID)` is single-domain (deletes the User row only via `users.findFirstByTenantId(...).ifPresent(users::delete)`). Multi-domain orchestration moves to `AccountDeletionController` per CONTEXT CL-2 + D-D1.
- **Forward-deferred `"onboarding"` allowedDependencies edge.** Plan instructions explicitly told me to use `{"tenant", "<CL-3 form>"}` (sans `"onboarding"`) and amend in Plan 04. Modulith verifies module existence eagerly — declaring an edge to a non-existent module would fail `ApplicationModulesTest.verify()` with "Module 'onboarding' does not exist!". The CL-3 form locked from Plan 01 is `"shared.privacy"`.
- **AccountDeletionController bridge landed in Task 2 (not Task 3) per blocking-issue Rule 3.** AccountService's reshape removed `deleteCurrentTenantAccount`, so the controller call site broke at the same compile boundary as the service relocation. Bundling the bridge into Task 2's commit kept the per-task `compileJava` invariant intact and avoided a fix-up commit (Plan 02 lesson). Task 3 then stays a metadata-only commit (package-info + entity-scan).
- **`OnboardingService` import sweep now, relocation later.** `OnboardingService` lives at `core.account/` for this plan and breaks compile when `User*`/`CurrentUserNotFoundException` paths flip. Updating its imports in Task 1 is Pitfall 1 closure, not a relocation — Plan 04 owns the move.
- **Three task commits, zero fix-ups.** Plan 02's discipline carried — staged the package-decl + import edits BEFORE staging the renames so each commit's working tree matches its index. Verified after each task by re-running compile.
- **OAuthProvisioningService imports already correct from Plan 02.** The `Tenant*` imports updated by Plan 02's sweep stayed correct after the Task 2 move; only the package decl line changed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Pitfall 1 sweep folded into Task 1 commit**
- **Found during:** Task 1 verify gate (`./gradlew :backend:core:compileJava` failed with "cannot find symbol" on `CurrentUserNotFoundException` references in `OnboardingService` after the model relocation; same shape as Plan 02's stale-import sweep).
- **Issue:** Plan 03 Task 1's `<files>` block listed only the relocated entity/repo/model files plus the new package-info markers. But moving `User*` out of `com.zeromail.core.persistence` and `CurrentUserView`/`CurrentUserNotFoundException` out of `com.zeromail.core.account` leaves 16 sites still importing the old FQNs — production (`MeController`, `GlobalExceptionHandler`, `TenantBindingFilter`, `OnboardingService`) and tests (9 `*Test.java` files in `backend/api`). Without sweeping them, `compileJava` fails immediately, blocking the plan's own verify step.
- **Fix:** Updated all 16 import sites to point at the new locations. The `OnboardingService` (which stays at `core.account/` for this plan) needed to add an explicit `import com.zeromail.core.account.model.CurrentUserNotFoundException;` because the exception was previously intra-package.
- **Files modified (production):** `MeController.java`, `GlobalExceptionHandler.java`, `TenantBindingFilter.java`, `OnboardingService.java`, `AccountService.java` (transitional cross-package import — replaced in Task 2)
- **Files modified (tests):** `AccountDeletionE2ETest`, `LogScrubSyntheticTrafficTest`, `OnboardingStateMachineTest`, `MeLanguageIntegrationTest`, `GlobalExceptionHandlerSafetyTest`, `LocaleResolutionTest`, `MultiTenantLeakIntegrationTest`, `SessionCookieE2ETest`, `TestSessionSupport`
- **Verification:** Bundled into Task 1's commit `d7fbe69`; subsequent `compileJava :backend:core:compileJava` + `:backend:api:compileJava` + `:backend:api:compileTestJava` all green.
- **Committed in:** `d7fbe69`

**2. [Rule 3 - Blocking] AccountDeletionController transitional bridge folded into Task 2 commit**
- **Found during:** Task 2 verify gate (`./gradlew :backend:api:compileJava` failed: `accountService.deleteCurrentTenantAccount(tenantId)` no longer compiles because Task 2's CL-2 reshape removed that method).
- **Issue:** Plan 03 placed the AccountDeletionController bridge into Task 3 step 4, but Task 2's verify command (`compileJava`) cannot pass with a broken caller. Bundling the bridge into Task 2 made the service reshape and the consumer fix atomic.
- **Fix:** Replaced the entire `AccountDeletionController` body with the plan-specified transitional bridge: holds `OnboardingSelectionRepository` + `GmailConnectionRepository` + `TenantRepository` directly, injects `AccountService` for the User row delete, applies controller-level `@Transactional`, calls in FK-safe order (children → user → tenant). Plus the `MeController` import flip (`com.zeromail.core.account.AccountService` → `com.zeromail.core.account.service.AccountService`) and the `GoogleOAuthSuccessHandler` flip (`com.zeromail.core.account.OAuthProvisioningService` → `com.zeromail.core.account.service.OAuthProvisioningService`).
- **Files modified:** `AccountDeletionController.java`, `MeController.java`, `GoogleOAuthSuccessHandler.java`
- **Verification:** Bundled into Task 2's commit `300e51b`; `:backend:api:compileJava` + `:backend:api:compileTestJava` green; Task 3's full validation gate (4 tests) green afterward.
- **Committed in:** `300e51b`

---

**Total deviations:** 2 auto-fixed (both Rule 3 — fold blocking-issue fixes into the same commit as the move that surfaced them; same discipline as Plan 02)
**Impact on plan:** Net-positive — kept every commit compile-green, avoided fix-up commits, shrank Task 3 to a clean metadata-only change. AccountDeletionE2ETest stays green throughout the chain via the transitional bridge.

## Issues Encountered

- **CRLF line-ending warnings on Windows** for every staged file. Cosmetic; `.gitattributes` handles normalization.
- **No real issues.** Plan 03 was a clean execution: 3 commits, each compile-green, all gates green on first run, no probes, no surprises. The CL-2 reshape and the transitional bridge both landed exactly as the plan dictated.

## Verification Gate Results

| Gate | Command | Result |
|------|---------|--------|
| Task 1 compile | `./gradlew :backend:core:compileJava` | PASS |
| Task 1 + Task 2 cross-tier compile | `./gradlew :backend:core:compileJava :backend:api:compileJava :backend:api:compileTestJava :backend:core:compileTestJava` | PASS |
| Task 2 compile | `./gradlew :backend:core:compileJava :backend:api:compileJava :backend:api:compileTestJava :backend:core:compileTestJava` | PASS |
| Task 3 — AccountDeletionE2ETest | `./gradlew :backend:api:test --tests "AccountDeletionE2ETest"` | PASS |
| Task 3 — MeLanguageIntegrationTest | `./gradlew :backend:api:test --tests "MeLanguageIntegrationTest"` | PASS |
| Task 3 — OnboardingStateMachineTest | `./gradlew :backend:api:test --tests "OnboardingStateMachineTest"` | PASS |
| Task 3 — ApplicationModulesTest | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | PASS |
| Phase 1.1 contract suite | `./gradlew :backend:api:test --tests "ProblemDetailContractTest" --tests "ApiErrorSchemaTest" --tests "LocaleResolutionTest"` | PASS |

## Stale-reference sweep

```
$ grep -rn 'com\.zeromail\.core\.persistence\.User(Entity|Repository)' backend/
(no matches)

$ grep -rn 'com\.zeromail\.core\.account\.(AccountService|CurrentUserView|OAuthProvisioningService|CurrentUserNotFoundException)\b' backend/
(no matches)
```

Zero stale references in source code. Other `com.zeromail.core.persistence.*` references (Onboarding/Gmail) intentionally remain — those domains relocate in Plans 04 and 05.

## Self-Check: PASSED

All claimed files exist at the asserted paths; all claimed commit hashes resolve in `git log`.

- `backend/core/src/main/java/com/zeromail/core/account/persistence/UserEntity.java` — FOUND (`grep` confirms `package com.zeromail.core.account.persistence;`)
- `backend/core/src/main/java/com/zeromail/core/account/persistence/UserRepository.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/account/persistence/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/account/persistence/lowlevel/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/account/model/CurrentUserView.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/account/model/CurrentUserNotFoundException.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/account/model/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/account/service/AccountService.java` — FOUND (`grep` confirms `private final UserRepository users;` AND `public void deleteCurrentUser(UUID tenantId)`)
- `backend/core/src/main/java/com/zeromail/core/account/service/OAuthProvisioningService.java` — FOUND (`grep` confirms `@Lazy OAuthProvisioningService self`)
- `backend/core/src/main/java/com/zeromail/core/account/service/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/account/package-info.java` — FOUND with `allowedDependencies = {"tenant", "shared.privacy"}`
- `backend/core/src/main/java/com/zeromail/core/persistence/UserEntity.java` — DELETED (confirmed)
- `backend/core/src/main/java/com/zeromail/core/persistence/UserRepository.java` — DELETED (confirmed)
- `backend/core/src/main/java/com/zeromail/core/account/CurrentUserView.java` — DELETED (confirmed)
- `backend/core/src/main/java/com/zeromail/core/account/CurrentUserNotFoundException.java` — DELETED (confirmed)
- `backend/core/src/main/java/com/zeromail/core/account/AccountService.java` — DELETED (confirmed)
- `backend/core/src/main/java/com/zeromail/core/account/OAuthProvisioningService.java` — DELETED (confirmed)
- Commits `d7fbe69`, `300e51b`, `254772e` — all resolve in `git log`.

## Notes for Plan 04 Executor

Plan 04 will move the onboarding domain (`OnboardingSelectionEntity` + `OnboardingSelectionRepository` + `OnboardingStep` enum + `OnboardingService`) to `com.zeromail.core.onboarding.{persistence,model,service}.*`. Two specific follow-ups from this plan:

1. **Add `"onboarding"` to `core.account/package-info.java` allowedDependencies.** This plan declared `{"tenant", "shared.privacy"}` (forward-deferring the onboarding edge). Plan 04 must amend to `{"tenant", "onboarding", "shared.privacy"}` once the onboarding module exists on disk. Add this as part of the same commit that creates `core.onboarding/package-info.java` so `ApplicationModulesTest.verify()` stays green.

2. **Replace the OnboardingSelectionRepository call in `AccountDeletionController`.** Add `OnboardingService.deleteSelectionsForCurrentTenant(UUID)` to the new onboarding service and swap the controller's `onboardingRepo.deleteAll(onboardingRepo.findByTenantId(tenantId));` line with `onboardingService.deleteSelectionsForCurrentTenant(tenantId);`. Drop the `OnboardingSelectionRepository onboardingRepo` field + constructor argument from the controller. Plan 05 will do the same for `gmailRepo` (replace with `GmailConnectionService.deleteForCurrentTenant`); Plan 06 finalizes the orchestration (chain of 4 service calls).

3. **Move `OnboardingService` itself.** It lives at `core.account/OnboardingService.java` after this plan (its imports were updated for compile-green but the file did not move). Plan 04 moves it to `core.onboarding/service/OnboardingService.java` and updates all import sites.

4. **Entity-scan widening pattern continues.** Plan 04 should add `"com.zeromail.core.onboarding.persistence"` to the `@EntityScan` and `@EnableJpaRepositories` arrays in BOTH `Application.java` AND `CoreTestApplication.java`. Plan 05 then collapses to single root `"com.zeromail.core"` after gmail entities also move.

No blockers, no concerns. The transitional bridge in `AccountDeletionController` is the central living artifact across Plans 04/05/06 — each subsequent plan removes one direct repo call and replaces it with a per-domain service call.

---

*Phase: 01.2-domain-owned-persistence-restructuring*
*Plan: 03*
*Completed: 2026-04-26*
