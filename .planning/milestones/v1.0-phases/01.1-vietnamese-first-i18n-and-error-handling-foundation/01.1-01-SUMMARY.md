---
phase: 1.1
plan: 01
subsystem: i18n-foundation
tags: [liquibase, jpa, testcontainers, archunit, vitest, wave-0]
provides:
  - "users.preferred_language column (varchar(2) NOT NULL DEFAULT 'vi', CHECK in ('vi','en'))"
  - "UserEntity.preferredLanguage field + getter/setter"
  - "CurrentUserView 5th component preferredLanguage"
  - "MeResponse 5th component preferredLanguage (null placeholder until Plan 04)"
  - "PreferredLanguageMigrationTest (BLOCKING green)"
  - "Wave 0 RED stubs: ProblemDetailContractTest, LocaleResolutionTest, AllowedParamScalarsTest, I18nArchUnitTest"
  - "apps/web Vitest config + setup + 2 RED stubs"
requires:
  - "Phase 1 baseline schema (tenants, users, gmail_connections, onboarding_selections)"
  - "PostgresContainerTest base class from Phase 1"
affects:
  - backend/core
  - backend/api
  - apps/web
key-files:
  created:
    - backend/core/src/main/resources/db/changelog/changes/006-users-preferred-language.yaml
    - backend/core/src/test/java/com/zeromail/core/migration/PreferredLanguageMigrationTest.java
    - backend/api/src/test/java/com/zeromail/api/i18n/ProblemDetailContractTest.java
    - backend/api/src/test/java/com/zeromail/api/i18n/LocaleResolutionTest.java
    - backend/core/src/test/java/com/zeromail/core/i18n/AllowedParamScalarsTest.java
    - backend/core/src/test/java/com/zeromail/core/architecture/I18nArchUnitTest.java
    - apps/web/vitest.config.ts
    - apps/web/__tests__/setup.ts
    - apps/web/__tests__/i18n/messages.contract.test.ts
    - apps/web/__tests__/i18n/error-render.test.tsx
  modified:
    - backend/core/src/main/java/com/zeromail/core/persistence/UserEntity.java
    - backend/core/src/main/java/com/zeromail/core/account/CurrentUserView.java
    - backend/core/src/main/java/com/zeromail/core/account/AccountService.java
    - backend/api/src/main/java/com/zeromail/api/dto/MeResponse.java
    - backend/api/src/main/java/com/zeromail/api/controllers/MeController.java
    - apps/web/package.json
    - pnpm-lock.yaml
decisions:
  - "Used existing PostgresContainerTest base for migration test (Phase 1 pattern), not the standalone @Testcontainers shape from the plan body"
  - "Pinned vitest 4.1.5 + jsdom 29.0.2 + @vitejs/plugin-react 6.0.1 (current stable per npm registry 2026-04-26)"
  - "I18nArchUnitTest located at src/test/java/com/zeromail/core/architecture/ (not src/test/architecture/ — that path is not a Gradle source set)"
  - "varchar(2) per plan body (not varchar(8) from prompt orientation summary)"
metrics:
  duration: ~25min
  completed: 2026-04-26
  tasks_total: 4
  tasks_completed: 4
  files_created: 10
  files_modified: 7
---

# Phase 1.1 Plan 01: Vietnamese-first i18n Wave 0 Foundation Summary

**One-liner:** [BLOCKING] Adds `users.preferred_language` column with provable Liquibase migration, extends `UserEntity` / `CurrentUserView` / `MeResponse` to carry it, and lays down 6 RED test stubs (4 backend + 2 frontend) that lock the contracts Plans 02/04/05/06/08 must satisfy.

## Tasks Completed

| Task | Status | Commit | Notes |
|------|--------|--------|-------|
| T1 — Liquibase 006 + JPA + CurrentUserView | ✓ green | `463400d` | Compile clean for backend:core + backend:api |
| T2 — [BLOCKING] PreferredLanguageMigrationTest | ✓ green | `aeacf53` | Booted Spring against Postgres 17 Testcontainer; both column-shape + CHECK-constraint assertions pass |
| T3 — Backend Wave 0 RED stubs (4 files) | ✓ green | `15554c9` | All 4 classes compile and report SKIPPED |
| T4 — Vitest config + FE RED stubs | ✓ green | `250a70d` | `pnpm --filter zeromail-web run test` exits 0 with 6 skipped tests |

## Verification Status

| Verify command | Result |
|----------------|--------|
| `./gradlew :backend:core:compileJava :backend:api:compileJava` | ✓ BUILD SUCCESSFUL |
| `./gradlew :backend:core:test --tests "*PreferredLanguageMigrationTest*"` | ✓ BUILD SUCCESSFUL (Liquibase applied 006-, both assertions passed) |
| `./gradlew :backend:core:test :backend:api:test --tests "*ProblemDetailContract*" --tests "*LocaleResolution*" --tests "*AllowedParamScalars*" --tests "*I18nArchUnit*"` | ✓ BUILD SUCCESSFUL (skipped, no failures) |
| `pnpm --filter zeromail-web run test` (vitest run) | ✓ Test Files 2 skipped, Tests 6 skipped, exit 0 |

## Deviations from Plan

### Path / Layout Deviations (auto-applied)

**1. [Rule 3 — Blocking] UserEntity package**
- **Plan said:** `backend/core/src/main/java/com/zeromail/core/user/UserEntity.java`
- **Actual repo:** `backend/core/src/main/java/com/zeromail/core/persistence/UserEntity.java`
- **Action:** Modified the entity at its actual location. The frontmatter `files_modified` path in the plan was stale relative to Phase 1's `persistence` package layout.

**2. [Rule 3 — Blocking] I18nArchUnitTest source set**
- **Plan said:** `backend/core/src/test/architecture/I18nArchUnitTest.java`
- **Issue:** `src/test/architecture/` is not a configured Gradle source set; only `src/test/java/` is compiled.
- **Action:** Placed the file at `backend/core/src/test/java/com/zeromail/core/architecture/I18nArchUnitTest.java` (mirrors existing convention `SafetyContractArchTests` in `src/test/java/com/zeromail/core/arch/`). The test runs in the standard test source set and is picked up by the `--tests "*I18nArchUnit*"` filter.

**3. [Rule 3 — Blocking] Migration test base class**
- **Plan said:** Author a standalone `@SpringBootTest @Testcontainers` class with its own `@Container static PostgreSQLContainer`.
- **Actual repo:** Phase 1 already established `com.zeromail.core.support.PostgresContainerTest` (singleton container, dynamic datasource props, ddl-auto=validate, redis disabled, refresh-token-key seeded).
- **Action:** Extended `PostgresContainerTest` instead of duplicating container plumbing (avoids per-class container churn the base class explicitly warns about). This matches the existing `LiquibaseMigrationTest` pattern.

**4. MeResponse 5th component**
- The plan instructed adding the 5th component to `MeResponse` "if it does not yet declare a `preferredLanguage` component". It did not. Added it now as `String preferredLanguage`; `MeController.me` passes `null` unconditionally — Plan 04 will swap to `user.preferredLanguage()`.

### Width Decision

The orientation summary in the prompt referenced `varchar(8)`, but the plan body, the example YAML inside the plan, the JPA snippet (`length = 2`), and CONTEXT.md decision D-B2 all specify `varchar(2)`. **Implemented `varchar(2)`** per the locked plan body. If a future plan needs a wider column for additional locale tags (e.g. `vi-VN`), it must amend D-B2 first.

### Auth gates / blocked verifications
None. Docker was available; Testcontainers brought up Postgres 17.6; Liquibase applied; Vitest ran. No environment deferrals needed.

## What's Now Unblocked

| Downstream plan | Status before | Status after |
|---|---|---|
| Plan 02 — GlobalExceptionHandler + AllowedParamScalars | gated on Wave 0 stubs | can flip `ProblemDetailContractTest` and `AllowedParamScalarsTest` from `@Disabled` to GREEN; the `ResponseEntityExceptionHandler` superclass assertion is already worded |
| Plan 04 — `/me` + `PATCH /me/language` | gated on column + CurrentUserView shape | can finalize `MeController.me` (replace `null` placeholder with `user.preferredLanguage()`), implement `PATCH /me/language`, and turn `LocaleResolutionTest` GREEN |
| Plan 05 — vi/en dictionaries + parity script | gated on Vitest config | can author `messages/vi.json` + `messages/en.json` and lift `it.skip` on `messages.contract.test.ts` |
| Plan 06 — FE error localization helper | gated on Vitest config | can author the helper + flip `error-render.test.tsx` to GREEN |
| Plan 08 — ArchUnit i18n bans | gated on stub | can lift `@Disabled` on `I18nArchUnitTest` and wire `@AnalyzeClasses` |

## Threat Mitigations Recorded

| Threat ID | Mitigation landed in this plan |
|---|---|
| T-1.1.01-01 (Tampering: bogus locale persisted) | DB CHECK `users_preferred_language_check IN ('vi','en')` is provably present (asserted in Task 2) |
| T-1.1.01-02 (Information Disclosure: changelog) | YAML contains only column name + literal locale codes — no secrets |
| T-1.1.01-03 (Tampering: cross-tenant locale write) | Existing `@TenantId` on `UserEntity` carries forward; `LocaleResolutionTest` stub explicitly notes the cross-tenant-safety assertion Plan 04 must add |

## Privacy Invariants Preserved

- No PII / email body / LLM prompt / completion in any test fixture, log line, or comment.
- All sentinel values use the obvious-marker convention (`<<SENTINEL_HTML>>`, `<<SENTINEL/TOKEN>>`, `"zz"` for invalid locale).
- `preferredLanguage` is a non-sensitive scalar (`'vi'`/`'en'`); not wrapped in `Sensitive<T>` per Phase 1's `SafetyContractArchTests` deny-list semantics.

## Self-Check: PASSED

- [x] `006-users-preferred-language.yaml` exists with 3+ `preferred_language` references
- [x] `users_preferred_language_check` and `defaultValue: vi` present
- [x] `UserEntity.preferredLanguage` field + accessor present
- [x] `CurrentUserView.preferredLanguage` 5th component present
- [x] `PreferredLanguageMigrationTest` exists and PASSES against real Postgres
- [x] 4 backend Wave 0 stubs exist with `@Disabled` + downstream-plan annotations
- [x] `vitest.config.ts` has `environment: 'jsdom'`
- [x] `__tests__/setup.ts` imports `@testing-library/jest-dom`
- [x] `messages.contract.test.ts` has 2 `it.skip` + parity assertions worded
- [x] `error-render.test.tsx` has 4 `it.skip` cases (vi default, en after switch, unknown fallback, no raw diagnostics)
- [x] `package.json` scripts include `"test": "vitest run"`
- [x] `pnpm --filter zeromail-web run test` exits 0
- [x] All 4 commits exist on `gsd/phase-1.1-vietnamese-first-i18n-and-error-handling-foundation-inserted`

## Commits

```
463400d  feat(01.1-01-T1): add Liquibase 006 + UserEntity.preferredLanguage
aeacf53  test(01.1-01-T2): [BLOCKING] verify Liquibase 006 applies to Postgres
15554c9  test(01.1-01-T3): Wave 0 backend RED stubs (Disabled, Plans 02/04/08)
250a70d  test(01.1-01-T4): Vitest config + Wave 0 frontend RED stubs (it.skip)
```
