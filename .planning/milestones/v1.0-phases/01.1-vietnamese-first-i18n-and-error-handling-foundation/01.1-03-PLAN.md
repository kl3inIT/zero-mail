---
phase: 1.1
plan: 03
type: execute
wave: 3
depends_on: [01.1-02]
files_modified:
  - backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java
  - backend/api/src/test/java/com/zeromail/api/openapi/ApiErrorSchemaTest.java
autonomous: true
requirements: [REQ-3]
must_haves:
  truths:
    - "Generated OpenAPI document contains a reusable ApiError schema component"
    - "Every Phase 1 operation declares 4xx/5xx default response with content-type application/problem+json referencing ApiError"
    - "ApiError schema lists code, params, fieldErrors, message — not auto-omitted because they are setProperty extension members"
    - "openapi-typescript regeneration in apps/web produces a typed ApiError interface FE can switch on"
  artifacts:
    - path: "backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java"
      provides: "GlobalOpenApiCustomizer registering ApiError + FieldErrorDto schemas + default 4xx/5xx response on every operation"
    - path: "backend/api/src/test/java/com/zeromail/api/openapi/ApiErrorSchemaTest.java"
      provides: "Snapshot test that asserts /v3/api-docs JSON contains ApiError component with required fields"
  key_links:
    - from: "OpenApiConfig.GlobalOpenApiCustomizer"
      to: "Components.addSchemas(\"ApiError\", ...)"
      via: "io.swagger.v3.oas.models.media.Schema"
      pattern: "addSchemas\\(\"ApiError\""
    - from: "Each operation 4xx/5xx response"
      to: "$ref: '#/components/schemas/ApiError'"
      via: "Operation.responses defaulting via customizer"
      pattern: "ApiError"
---

<objective>
Register the `ApiError` and `FieldErrorDto` OpenAPI schemas via a `GlobalOpenApiCustomizer` so the generated spec documents the locked error contract from Plan 02. Default every Phase 1 operation's 4xx and 5xx responses to `application/problem+json` referencing `#/components/schemas/ApiError`. Bump OpenAPI `version` to `0.1.1`. Add a snapshot test asserting the schema appears in `/v3/api-docs`.

Purpose: REQ-3 acceptance criterion "generated OpenAPI includes the reusable error schema for Phase 1 endpoints" + Pitfall 6 (`openapi-typescript` produces `unknown` for properties added via `setProperty` unless the schema is explicitly registered). Plans 05/06 depend on the regenerated typed client containing `components['schemas']['ApiError']`.

Output: 1 modified config + 1 new test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1.1-vietnamese-first-i18n-and-error-handling-foundation/01.1-PATTERNS.md
@.planning/phases/1.1-vietnamese-first-i18n-and-error-handling-foundation/01.1-RESEARCH.md
@backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java
@backend/api/src/main/java/com/zeromail/api/error/ApiError.java
@backend/api/src/main/java/com/zeromail/api/error/FieldErrorDto.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GlobalOpenApiCustomizer registering ApiError + default 4xx/5xx response, plus snapshot test</name>
  <files>backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java, backend/api/src/test/java/com/zeromail/api/openapi/ApiErrorSchemaTest.java</files>
  <read_first>
    - backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java (current OpenApiCustomizer bean — extend, don't replace)
    - backend/api/src/main/java/com/zeromail/api/error/ApiError.java (Plan 02 record under test)
    - backend/api/src/main/java/com/zeromail/api/error/FieldErrorDto.java (Plan 02 record)
    - PATTERNS.md §"OpenApiConfig.java (config, springdoc customizer)" (use GlobalOpenApiCustomizer NOT OpenApiCustomizer)
    - RESEARCH.md §"Architecture Patterns — Pattern 2: springdoc global ApiError schema" (full code excerpt at lines 315-365)
    - RESEARCH.md §"Common Pitfalls — Pitfall 6" (reflection misses setProperty members; explicit registration required)
    - CONTEXT.md decision D-C4 (single OpenApiCustomiser, no per-controller @ApiResponse)
  </read_first>
  <behavior>
- A new `@Bean` of type `GlobalOpenApiCustomizer` (NOT `OpenApiCustomizer`):
  1. Adds `ApiError` schema to `Components.schemas` with explicit properties: `type` (string), `title` (string), `status` (integer), `detail` (string), `code` (string, required), `params` (object with additionalProperties), `fieldErrors` (array of `$ref: #/components/schemas/FieldErrorDto`), `message` (string, deprecated=true).
  2. Adds `FieldErrorDto` schema with `field` (string, required), `code` (string, required), `params` (object).
  3. For every operation in `paths`, ensures responses for status `400`, `401`, `403`, `404`, `409` exist with `content: application/problem+json: schema: $ref: #/components/schemas/ApiError`. Do not overwrite existing 200/201 responses.
- Existing `phase1Info` bean updated to set `version("0.1.1")`.
- Snapshot test `ApiErrorSchemaTest`:
  - `@SpringBootTest(webEnvironment = RANDOM_PORT)` + `TestRestTemplate` GET `/v3/api-docs`
  - Parse JSON, assert `$.components.schemas.ApiError.properties.code.type == "string"`, `$.components.schemas.ApiError.required` contains `"code"`, `$.components.schemas.FieldErrorDto.required` contains `"field"` and `"code"`, `$.components.schemas.FieldErrorDto.properties` does NOT contain `message` (JHipster reject).
  - Pick one Phase 1 operation (e.g., `/me` GET) and assert `$.paths["/me"].get.responses["401"].content["application/problem+json"].schema.$ref == "#/components/schemas/ApiError"`.
  - Assert `$.info.version == "0.1.1"`.
  </behavior>
  <action>
Modify `OpenApiConfig.java`. Keep the existing `phase1Info` bean (update version literal to `0.1.1`). Add a second `@Bean` returning `GlobalOpenApiCustomizer`. Use `io.swagger.v3.oas.models.media.{Schema, ObjectSchema, StringSchema, IntegerSchema, ArraySchema, MapSchema}` and `io.swagger.v3.oas.models.responses.{ApiResponse, ApiResponses}` and `io.swagger.v3.oas.models.media.{Content, MediaType}`. Use code shape from RESEARCH.md Pattern 2 verbatim, adapted to register both `ApiError` and `FieldErrorDto`.

Status codes to default with `application/problem+json` + ApiError $ref: 400, 401, 403, 404, 409, 500. Skip if a response is already explicitly declared on the operation (do not overwrite). Use `paths.values().forEach(pathItem -> pathItem.readOperations().forEach(op -> ...))`.

Create `ApiErrorSchemaTest.java`. Drive it via `WebTestClient` or `TestRestTemplate`. Parse via Jackson `ObjectMapper`. Use AssertJ for the JSON assertions; if you prefer JsonPath, declare its dependency only in test scope.

If Phase 1 has no `/me` operation yet (it does — Plan 01 of Phase 1 created `MeController`), pick `/me` GET. Otherwise pick any documented path.
  </action>
  <acceptance_criteria>
    - `grep -n "GlobalOpenApiCustomizer" backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java` returns ≥ 1 line.
    - `grep -n "addSchemas(\"ApiError\"" backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java` returns 1 line.
    - `grep -n "addSchemas(\"FieldErrorDto\"" backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java` returns 1 line.
    - `grep -n "application/problem+json" backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java` returns ≥ 1 line.
    - `grep -n "0.1.1" backend/api/src/main/java/com/zeromail/api/config/OpenApiConfig.java` returns 1 line.
    - `./gradlew :backend:api:test --tests "*ApiErrorSchemaTest*"` exits 0 with PASSED, no SKIPPED.
    - Manual sanity: `curl -s http://localhost:8080/v3/api-docs | jq '.components.schemas.ApiError.properties.code'` returns a string-typed object after running the app.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :backend:api:test --tests "*ApiErrorSchemaTest*"</automated>
  </verify>
  <done>OpenAPI doc carries the ApiError schema and defaults all Phase 1 operation 4xx/5xx responses to it; snapshot test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAPI doc consumer (apps/web codegen) | The generated typed client is the FE contract; missing schema → `unknown` types → FE cannot switch on `code` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.1.03-01 | Tampering | OpenAPI schema reflection misses setProperty members | mitigate | Explicit `GlobalOpenApiCustomizer` registration (Pitfall 6); snapshot test asserts presence |
| T-1.1.03-02 | Information Disclosure | OpenAPI doc exposing internal paths | accept | Phase 1 already publishes /v3/api-docs publicly; no new exposure here |
</threat_model>

<verification>
- `./gradlew :backend:api:test --tests "*ApiErrorSchemaTest*"` exits 0
- `./gradlew :backend:api:bootRun` followed by `curl -s http://localhost:8080/v3/api-docs | jq '.components.schemas | keys'` lists `ApiError` and `FieldErrorDto`
</verification>

<success_criteria>
- ApiError + FieldErrorDto registered as named OpenAPI schema components
- Every Phase 1 operation gains 4xx/5xx responses keyed `application/problem+json` referencing ApiError
- Info.version bumped to 0.1.1 (FE codegen will see the change)
- Snapshot test passes
</success_criteria>

<output>
After completion, create `.planning/phases/1.1-vietnamese-first-i18n-and-error-handling-foundation/01.1-03-SUMMARY.md`
</output>
