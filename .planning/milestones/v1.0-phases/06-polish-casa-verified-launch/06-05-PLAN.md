---
phase: 06-polish-casa-verified-launch
plan: 05
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - .planning/LAUNCH-GO-NOGO.md
  - .planning/seeds/SEED-012-casa-restricted-scope-verification.md
  - .planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md
  - .planning/todos/pending/2026-04-28-worker-application-yml-fail-fast-parity.md
# MED-10: Task 4 also mutates a git tag (`v1.0.0-rc1`, annotated, single push). See Task 4 <git_artifacts> block.
autonomous: false
requirements:
  - SPEC-06-R4
  - SPEC-06-R5
  - SPEC-06-R6
tags: [launch-decision, casa-seed, housekeeping, sign-off]

must_haves:
  truths:
    - "`.planning/LAUNCH-GO-NOGO.md` exists at the planning-tree root (NOT inside the phase directory) per D-14; the launch decision spans the whole project."
    - "LAUNCH-GO-NOGO.md contains exactly 8 pass/fail checkboxes labelled (a) through (h) per SPEC Req #5 + D-14, with the wording matching SPEC.md verbatim."
    - "Item (g) restates the trust story with THREE verbatim phrases — `auto-send forbidden`, `no stored bodies / prompts / completions`, `every triage action undoable` — copied from CLAUDE.md, REQUIREMENTS.md, and PROJECT.md authoritative sources; no paraphrase (PATTERNS lines 596-599)."
    - "Item (h) records launch mode = OAuth `Testing` (Production move deferred to `SEED-012-casa-restricted-scope-verification.md`) (SPEC AC #6, D-14)."
    - "Item (a) links to the green `Release Gates Summary` job URL from Plan 06-04's `release.yml`; item (b) links to `06-LOAD-TEST-RESULT.md` from Plan 06-02; items (c)–(f) link to the same release-gates run."
    - "A sign-off line of the exact shape `✓ signed-off by @<user> on <ISO date>` is committed as the last edit on the file (D-14, SPEC AC #5)."
    - "`.planning/seeds/SEED-012-casa-restricted-scope-verification.md` exists with the same frontmatter shape as SEED-011 (`id`, `status: dormant`, `planted`, `planted_during`, `trigger_when`, `scope: large`); H2 sections include `Required Evidence Package`, `Candidate CASA Labs`, `Closure Trigger`, and Breadcrumbs to REQUIREMENTS.md FND-07 + PITFALLS.md §Pitfall 1 + LAUNCH-GO-NOGO.md item (h) (D-09, PATTERNS lines 549-585)."
    - "The resolved Phase 1.5 todo `2026-04-28-worker-application-yml-fail-fast-parity.md` is MOVED from `.planning/todos/pending/` to `.planning/todos/done/` (creating `done/` if it does not exist) — body content unchanged, only path (D-15)."
    - "codex HIGH-10 closure (two-tag pattern): the operator cuts `v1.0.0-rc1-candidate` FIRST on the current main HEAD (matches the existing `'v*.*.*-rc*'` glob in release.yml — `*` is greedy across `-`). The candidate run produces the gate URL. The operator fills the URL into `LAUNCH-GO-NOGO.md` item (a), checks all 8 boxes, commits the sign-off line. ONLY THEN does the operator cut the FINAL annotated `v1.0.0-rc1` tag on the signed commit (pushed exactly once, no delete-and-re-push of the final tag). The candidate tag may be deleted as housekeeping. MED-7 is preserved (the launch tag itself is single-push)."
    - "codex HIGH-9 closure: the operator runs the full load test LOCALLY before cutting either tag, commits `.planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md` to main, and the tags are cut on that committed-evidence SHA. The CI `release.yml` `loadtest` job re-runs the same test against the tagged SHA for record (uploads to artifacts, does NOT overwrite the committed file)."
    - "LOW-10: the operator runbook ends with a STATE.md update step that commits the closure of Phase 6 in the planning state via `gsd-sdk query state planned-phase --phase 6 --name polish-casa-verified-launch --plans 5`."
    - "The plan does NOT cut the `v1.0.0-rc1` tag — tagging is a documented operator action in the plan's checkpoint (D-12)."
  artifacts:
    - path: ".planning/LAUNCH-GO-NOGO.md"
      provides: "Single launch decision artifact: 8 checkboxes (a–h) + sign-off line"
      min_lines: 40
      contains: "auto-send forbidden"
    - path: ".planning/seeds/SEED-012-casa-restricted-scope-verification.md"
      provides: "Post-launch CASA tracking seed mirroring SEED-011 structure"
      contains: "id: SEED-012"
    - path: ".planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md"
      provides: "Archived Phase-1.5 follow-up (already-fixed per D-15)"
  key_links:
    - from: ".planning/LAUNCH-GO-NOGO.md item (a)"
      to: ".github/workflows/release.yml release-gates-summary job"
      via: "GitHub check URL (filled in by operator after rc1 tag push, see Task 4)"
      pattern: "release-gates-summary"
    - from: ".planning/LAUNCH-GO-NOGO.md item (b)"
      to: ".planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md"
      via: "relative path link"
      pattern: "06-LOAD-TEST-RESULT.md"
    - from: ".planning/LAUNCH-GO-NOGO.md item (h)"
      to: ".planning/seeds/SEED-012-casa-restricted-scope-verification.md"
      via: "relative path link"
      pattern: "SEED-012-casa-restricted-scope-verification.md"
---

<objective>
Land the three artifacts that close out Phase 6: the launch decision document, the post-launch CASA tracking seed, and the housekeeping move of the already-resolved Phase-1.5 todo. This plan does NOT cut the `v1.0.0-rc1` tag — that is an operator action documented in a checkpoint. The tag is pushed exactly ONCE with a pre-enriched annotation (MED-7); the gate-run URL is recorded only in `LAUNCH-GO-NOGO.md` item (a), not retro-amended into the tag message.

Purpose: D-14 fixes the exact 8-checkbox structure + sign-off requirement; the three trust-story phrases in item (g) are LOAD-BEARING (PATTERNS line 596 lists their authoritative sources and explicitly forbids paraphrase). D-09 fixes SEED-012's location and required H2 sections. D-15 archives the already-fixed Phase-1.5 follow-up. MED-7 removes the dangerous delete-and-re-push tag dance. LOW-10 makes the planning-state closure step explicit.

Output: 3 new/moved files. Plan is NOT autonomous — Task 4 is a `checkpoint:human-action` for the RC tag cut + sign-off line, because the gate URL is only known after the live `release.yml` run completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/phases/06-polish-casa-verified-launch/06-SPEC.md
@.planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md
@.planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md
@.planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md
@.planning/phases/06-polish-casa-verified-launch/06-04-PLAN.md
@.planning/seeds/SEED-011-admin-support-and-compliance-console.md
@.planning/todos/pending/2026-04-28-worker-application-yml-fail-fast-parity.md

<interfaces>
<!-- Authoritative sources for trust-story phrases (PATTERNS lines 596-600). Copy verbatim, do NOT paraphrase. -->

Phrase 1: `auto-send forbidden` — source: CLAUDE.md (Constraints block "**Auto-send is forbidden.**") + REQUIREMENTS.md DRFT-04.
Phrase 2: `no stored bodies / prompts / completions` — source: CLAUDE.md (Privacy constraint) + REQUIREMENTS.md LLM-09.
Phrase 3: `every triage action undoable` — source: .planning/PROJECT.md line 102.

SEED-011 frontmatter shape (for SEED-012 mirror):
```
---
id: SEED-011
status: dormant
planted: 2026-05-14
planted_during: Phase 6 launch-readiness discussion
trigger_when: "when planning Milestone 1.1 admin UI, support tooling, or CASA operations"
scope: medium
---
```
SEED-012 deltas (PATTERNS lines 579-585):
- `scope: large` (CASA is 4–12 weeks).
- `trigger_when: "when Vietnam beta exceeds 100 users OR when the CASA submission is funded/scheduled"`.
- Replace H2 `## Candidate Product Shape` with `## Required Evidence Package`.
- Add `## Candidate CASA Labs`.
- Add `## Closure Trigger`.
- Breadcrumbs MUST link REQUIREMENTS.md FND-07, .planning/research/PITFALLS.md §"Pitfall 1", .planning/LAUNCH-GO-NOGO.md item (h).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author `.planning/LAUNCH-GO-NOGO.md` — 8-checkbox launch decision artifact with verbatim trust-story phrases</name>
  <files>.planning/LAUNCH-GO-NOGO.md</files>
  <read_first>
    - .planning/phases/06-polish-casa-verified-launch/06-SPEC.md Req #5 (lines 47-50 — the 8-item checklist (a)–(h) authoritative wording)
    - .planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md D-14 (full wording + sign-off line shape)
    - .planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md lines 589-602 (authoritative sources for the THREE verbatim trust-story phrases — DO NOT paraphrase)
    - CLAUDE.md (the "Auto-send is forbidden." block + the Privacy constraint about "no long-term storage of raw email bodies, LLM prompts/completions, or embeddings")
    - .planning/PROJECT.md line 102 (the "every triage action undoable" phrasing)
    - .planning/REQUIREMENTS.md (DRFT-04 + LLM-09 entries — confirm the phrases match)
    - .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §4.2 (skeleton if present — verbatim recommended)
  </read_first>
  <action>
    Create `.planning/LAUNCH-GO-NOGO.md` at the PLANNING-TREE ROOT (NOT inside `phases/06-...`). D-14 is explicit on this — the launch decision spans the whole project and outlives the phase.

    Document structure (markdown):

    ```markdown
    # Launch Go/No-Go — Zero Mail v1.0.0-rc1

    *Decision artifact for the v1.0.0-rc1 release candidate. Owned by the project owner. Items checked once the corresponding evidence resolves.*

    ## Launch Mode

    OAuth consent screen: **Testing** (100-user cap). Production move is gated on CASA, deferred to a post-launch track (see item (h)).

    ## Decision Checklist

    - [ ] **(a) Playwright golden-path spec green on RC tag.** Link to CI run: _(operator fills in after `release-gates-summary` reports green — see Task 4)_
    - [ ] **(b) 50-tenant load test invariants all PASS.** Link to result: [`.planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md`](./phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md)
    - [ ] **(c) Prompt-injection regression suite green on RC tag.** Re-verified by the `Gates / backend` job in `Release Gates` (it runs `./gradlew check`, which includes the prompt-injection suite from Phase 2C).
    - [ ] **(d) ArchUnit suite green on RC tag.** Re-verified by the same `Gates / backend` job (includes `DraftPathArchUnitTest`, `I18nArchUnitTest`, `LaunchProfileArchUnitTest` from Plan 06-01).
    - [ ] **(e) Spring Modulith `ApplicationModulesTest` green on RC tag.** Re-verified by `Gates / backend` (`ZeroMailApiApplicationModulesTest` runs as part of `check`).
    - [ ] **(f) LLM golden-set drift check green on RC tag.** Re-verified by `Gates / ai-eval` (`:backend:core:aiEval -PdeterministicOnly`).
    - [ ] **(g) Trust story re-affirmed in writing:** **auto-send forbidden**, **no stored bodies / prompts / completions**, **every triage action undoable**.
    - [ ] **(h) Launch mode = OAuth "Testing" (Production move deferred).** Tracking: [`.planning/seeds/SEED-012-casa-restricted-scope-verification.md`](./seeds/SEED-012-casa-restricted-scope-verification.md)

    ## Evidence

    | Item | Artifact | Authoritative source |
    |------|----------|----------------------|
    | (a) | `Release Gates / golden-path` job | `.github/workflows/release.yml` |
    | (b) | `06-LOAD-TEST-RESULT.md` (committed) + `Release Gates / loadtest` job | `.planning/phases/06-polish-casa-verified-launch/06-02-PLAN.md` Task 4 |
    | (c) | `Gates / backend` job (includes prompt-injection regression) | Phase 2C |
    | (d) | `Gates / backend` job (ArchUnit rules) | Phase 1 + 1.2 + Plan 06-01 |
    | (e) | `Gates / backend` job (`ApplicationModulesTest`) | Phase 1.2 |
    | (f) | `Gates / ai-eval` job (`aiEval -PdeterministicOnly`) | Phase 2C |
    | (g) | This document | CLAUDE.md + REQUIREMENTS.md DRFT-04 + LLM-09 + PROJECT.md |
    | (h) | This document + SEED-012 | D-09 |

    ## Sign-off

    *Operator adds the sign-off line below as the LAST EDIT before pushing the rc1 tag. Format is exact and committed verbatim.*

    <!-- ✓ signed-off by @<user> on <ISO date> -->
    ```

    CRITICAL — verbatim trust-story rules (PATTERNS line 596):
    - Item (g) MUST contain `auto-send forbidden` (4 chars hyphen + space), `no stored bodies / prompts / completions` (spaces around `/`), and `every triage action undoable` — all in lowercase, as written here, inside the same checklist item. Plan 06-04 `release.yml` enforces these phrases on every RC tag push (release.yml job `trust-story-grep`); a missing phrase fails the gate. For this plan: the phrases ARE the load-bearing committed evidence; treat them as immutable strings.
    - DO NOT change to title case ("Auto-Send Forbidden"), DO NOT remove the spaces around `/`, DO NOT add em-dashes between phrases.

    Other rules:
    - The file is committed UNCHECKED (every `- [ ]`). Boxes are checked AFTER the operator verifies the evidence (Task 4).
    - Sign-off line is a markdown comment placeholder `<!-- ✓ signed-off by ... -->` so a fresh checkout shows a clean "not yet signed" state; the operator promotes it to a real line (without the `<!--` wrapper) when checking the boxes.
    - Use relative markdown links from `.planning/LAUNCH-GO-NOGO.md`'s vantage (so `./phases/...`, `./seeds/...`).
  </action>
  <acceptance_criteria>
    - File exists at `.planning/LAUNCH-GO-NOGO.md` (NOT inside `phases/`).
    - File is ≥ 40 lines: `wc -l .planning/LAUNCH-GO-NOGO.md` returns ≥ 40.
    - Exactly 8 unchecked checkboxes labelled (a)–(h): `grep -c '^- \\[ \\] \\*\\*([a-h])' .planning/LAUNCH-GO-NOGO.md` returns 8.
    - Three verbatim trust-story phrases co-occur INSIDE item (g) (stricter than three independent file-wide greps):
      - Node verifier asserts the line beginning `^- \[[ x]\] \*\*\(g\)` plus its continuation lines (up to the next `^- \[`) contains ALL THREE phrases.
      - Verifier: `node -e "const s=require('fs').readFileSync('.planning/LAUNCH-GO-NOGO.md','utf8').split(/\r?\n/);let g=-1;for(let i=0;i<s.length;i++){if(/^-\s+\[[ x]\]\s+\*\*\(g\)/.test(s[i])){g=i;break;}}if(g<0){console.error('item (g) not found');process.exit(1);}let end=g+1;while(end<s.length&&!/^-\s+\[/.test(s[end]))end++;const block=s.slice(g,end).join('\n');for(const p of ['auto-send forbidden','no stored bodies / prompts / completions','every triage action undoable']){if(!block.includes(p)){console.error('MISSING in item (g):',p);process.exit(1);}}console.log('ok');"` exits 0.
      - (Belt-and-suspenders) file-wide presence still asserted: `grep -Fq 'auto-send forbidden' .planning/LAUNCH-GO-NOGO.md`, `grep -Fq 'no stored bodies / prompts / completions' .planning/LAUNCH-GO-NOGO.md`, `grep -Fq 'every triage action undoable' .planning/LAUNCH-GO-NOGO.md` all exit 0.
    - Item (b) links to the load-test result: `grep -c '06-LOAD-TEST-RESULT.md' .planning/LAUNCH-GO-NOGO.md` returns ≥ 1.
    - Item (h) links to SEED-012: `grep -c 'SEED-012-casa-restricted-scope-verification.md' .planning/LAUNCH-GO-NOGO.md` returns ≥ 1.
    - Sign-off placeholder present: `grep -c '<!-- ✓ signed-off by' .planning/LAUNCH-GO-NOGO.md` returns 1.
    - No sign-off line is yet active (no uncommented `✓ signed-off by`): `grep -c '^✓ signed-off by' .planning/LAUNCH-GO-NOGO.md` returns 0.
    - File is at the planning-tree root, not the phase dir: `test -f .planning/LAUNCH-GO-NOGO.md` AND `test ! -f .planning/phases/06-polish-casa-verified-launch/LAUNCH-GO-NOGO.md`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/LAUNCH-GO-NOGO.md','utf8').split(/\r?\n/);let g=-1;for(let i=0;i<s.length;i++){if(/^-\s+\[[ x]\]\s+\*\*\(g\)/.test(s[i])){g=i;break;}}if(g<0){console.error('item (g) not found');process.exit(1);}let end=g+1;while(end<s.length&&!/^-\s+\[/.test(s[end]))end++;const block=s.slice(g,end).join('\n');for(const p of ['auto-send forbidden','no stored bodies / prompts / completions','every triage action undoable']){if(!block.includes(p)){console.error('MISSING in item (g):',p);process.exit(1);}}console.log('ok');"</automated>
  </verify>
  <done>Launch decision artifact lands at the planning-tree root with 8 unchecked boxes and the 3 verbatim trust-story phrases.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author `.planning/seeds/SEED-012-casa-restricted-scope-verification.md` — post-launch CASA tracking seed mirroring SEED-011</name>
  <files>.planning/seeds/SEED-012-casa-restricted-scope-verification.md</files>
  <read_first>
    - .planning/seeds/SEED-011-admin-support-and-compliance-console.md (entire file — EXACT structural template: frontmatter + H2 headings + Breadcrumbs style)
    - .planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md lines 549-585 (full SEED-012 pattern with deltas: scope: large, trigger_when, replace `Candidate Product Shape` with `Required Evidence Package`, add `Candidate CASA Labs` + `Closure Trigger`)
    - .planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md D-09 (seed location + required content: evidence package, CASA lab options, 4–12 week timeline, FND-07 → status change trigger)
    - .planning/research/PITFALLS.md (Pitfall 1 — restricted-scope OAuth verification; SEED-012 Breadcrumbs links here)
    - .planning/REQUIREMENTS.md (FND-07 entry — currently Pending; the seed Breadcrumbs links to this)
  </read_first>
  <action>
    Create `.planning/seeds/SEED-012-casa-restricted-scope-verification.md`.

    Frontmatter (mirror SEED-011 shape with D-09 deltas):
    ```yaml
    ---
    id: SEED-012
    status: dormant
    planted: 2026-05-14
    planted_during: Phase 6 launch-readiness — CASA deferred to post-launch track
    trigger_when: "when Vietnam beta exceeds 100 users OR when the CASA submission is funded/scheduled"
    scope: large
    ---
    ```

    Title: `# SEED-012: CASA Restricted-Scope Verification (Post-Launch Track)`

    H2 sections in this exact order (mirror SEED-011 ordering with the three swaps):

    `## Why This Matters` — one paragraph. Sample shape:
    "Google's CASA (Cloud Application Security Assessment) restricted-scope verification is required to move the OAuth consent screen from Testing to Production. Phase 6 launched into Testing mode (100-user cap), which is sufficient for the Vietnam beta but caps growth. CASA is a 4–12 week external process: lab engagement, evidence package, Letter of Assessment, then the consent-screen move. Until CASA closes, FND-07 stays `Pending` in REQUIREMENTS.md and Zero Mail cannot accept user 101."

    `## When to Surface` — one paragraph: trigger condition mirroring `trigger_when` frontmatter.

    `## Scope Estimate` — `**Large**.` plus rationale: external CASA lab fees + 4–12 week wall-clock + evidence preparation effort.

    `## Required Evidence Package` — bulleted list (per D-09 + research §4.1):
    - Privacy policy URL (published, stable, addresses restricted scopes specifically).
    - Demo video showing every restricted Gmail scope in use (read messages, modify labels, save drafts).
    - Data-flow diagram showing how Gmail data flows through the system + privacy boundaries.
    - MFA evidence: screenshots/policy showing MFA on Google Cloud Console + OpenRouter + Sepay + repo admin accounts.
    - Key-rotation evidence: AES-GCM refresh-token-key rotation procedure + last-rotated date.
    - Employee-access policy: who can access prod data + how (likely "single operator, no third-party access" for the v1 beta).
    - Penetration test report (CASA tier-dependent — clarify with the chosen lab).
    - Software bill of materials (SBOM) — likely generated by `./gradlew dependencies` + frontend `pnpm list`.

    `## Candidate CASA Labs` — bulleted list of options:
    - CREST-accredited lab (e.g., Bishop Fox, Leviathan Security).
    - Bishop Fox specifically called out in research §4.1 as a common pick.
    - "TBD — final pick when budget + timeline are committed (D-09 Claude's Discretion)."

    `## Safety Rules` — preserve SEED-011 H2 ordering. Bulleted list:
    - No data shared with the CASA lab includes real user emails / prompts / completions.
    - Demo accounts use synthetic data only.
    - Lab access to source code is read-only.

    `## Closure Trigger` — paragraph:
    "Seed closes when (1) Letter of Assessment is received from the CASA lab AND (2) Google OAuth consent screen status flips from `Testing` to `Production` in the Google Cloud Console AND (3) FND-07 in `.planning/REQUIREMENTS.md` is updated from `Pending` to `Complete` with the LoA date. At that point this seed file moves from `.planning/seeds/` to an archive location (TBD) and `LAUNCH-GO-NOGO.md` item (h) is updated to reflect launch mode = `Production`."

    `## Breadcrumbs` — bulleted list (PATTERNS line 585):
    - `.planning/REQUIREMENTS.md` — FND-07 (currently Pending; flips to Complete on closure).
    - `.planning/research/PITFALLS.md` §"Pitfall 1: Restricted-scope OAuth verification" — explains the verification surface.
    - `.planning/LAUNCH-GO-NOGO.md` item (h) — the launch decision artifact that this seed exists to eventually unblock.

    `## Notes` — optional paragraph: implementation notes, timeline pressure, lab-pick deferral rationale.

    Rules:
    - Mirror SEED-011's H2 ordering (Why / When / Scope / Candidate Product Shape / Safety / Breadcrumbs / Notes) but with the three swaps: `Candidate Product Shape` → `Required Evidence Package`, then ADD `Candidate CASA Labs` BEFORE `Safety Rules`, then ADD `Closure Trigger` AFTER `Safety Rules`. Result: 8 H2 sections in this order — Why / When / Scope / Required Evidence Package / Candidate CASA Labs / Safety Rules / Closure Trigger / Breadcrumbs / Notes.
    - Frontmatter MUST be valid YAML (no trailing spaces on the `---` lines).
    - No emoji.
    - No backticks around `Testing` / `Production` outside of when they refer to the literal OAuth status string.
  </action>
  <acceptance_criteria>
    - File exists at `.planning/seeds/SEED-012-casa-restricted-scope-verification.md`.
    - Frontmatter has `id: SEED-012`: `grep -c '^id: SEED-012' .planning/seeds/SEED-012-casa-restricted-scope-verification.md` returns 1.
    - Frontmatter has `status: dormant`: `grep -c '^status: dormant' .planning/seeds/SEED-012-casa-restricted-scope-verification.md` returns 1.
    - Frontmatter has `scope: large`: `grep -c '^scope: large' .planning/seeds/SEED-012-casa-restricted-scope-verification.md` returns 1.
    - All required H2 sections present: `grep -E '^## (Why This Matters|When to Surface|Scope Estimate|Required Evidence Package|Candidate CASA Labs|Safety Rules|Closure Trigger|Breadcrumbs)' .planning/seeds/SEED-012-casa-restricted-scope-verification.md | wc -l` returns 8.
    - Breadcrumbs link to FND-07, PITFALLS.md, and LAUNCH-GO-NOGO.md item (h): `grep -c 'FND-07\\|PITFALLS.md\\|LAUNCH-GO-NOGO.md' .planning/seeds/SEED-012-casa-restricted-scope-verification.md` returns ≥ 3.
    - YAML frontmatter parses (no trailing whitespace, balanced `---`): `head -10 .planning/seeds/SEED-012-casa-restricted-scope-verification.md | grep -c '^---$'` returns 2.
    - Counterpart consistency: the path referenced by `LAUNCH-GO-NOGO.md` item (h) resolves to this file.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/seeds/SEED-012-casa-restricted-scope-verification.md && grep -q '^id: SEED-012' .planning/seeds/SEED-012-casa-restricted-scope-verification.md && grep -q '^scope: large' .planning/seeds/SEED-012-casa-restricted-scope-verification.md</automated>
  </verify>
  <done>SEED-012 lands with the 8 H2 sections + correct frontmatter + Breadcrumbs to the three required references.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Archive the resolved Phase-1.5 follow-up todo (D-15 housekeeping)</name>
  <files>.planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md, .planning/todos/pending/2026-04-28-worker-application-yml-fail-fast-parity.md</files>
  <read_first>
    - .planning/todos/pending/2026-04-28-worker-application-yml-fail-fast-parity.md (entire file — current body; this task only moves the file, never edits the body)
    - .planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md D-15 (already-fixed verification at `backend/worker/src/main/resources/application.yml:63`; this task is housekeeping only)
    - backend/worker/src/main/resources/application.yml line 63 (verify the `:?` fail-fast is in place; do NOT edit — verification only)
  </read_first>
  <action>
    D-15 housekeeping. Two operations.

    1. Create the directory `.planning/todos/done/` if it does not exist. Verify with `ls .planning/todos/` before the operation.

    2. Move (git mv) `.planning/todos/pending/2026-04-28-worker-application-yml-fail-fast-parity.md` to `.planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md`. The body is NOT changed. The git history is preserved by using `git mv` (NOT `cp` + `rm`).

    3. If the executor wants to record the resolution context, append (do not modify existing content) a final section to the file:
    ```markdown

    ---

    ## Resolution (2026-05-14, Phase 6 D-15)

    Verified already fixed at `backend/worker/src/main/resources/application.yml:63` during the Phase 6 discuss-phase. The `:?` fail-fast placeholder for `REFRESH_TOKEN_KEY_BASE64` is present and crashes worker boot if the env var is missing — exactly the property D-15 requires. Phase 6 implementation work: none.

    See also: `.planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md` D-15.
    ```

    DO NOT touch the two other pending todos:
    - `2026-04-28-wr-06-test-profile-securityconfig-slice.md` (deferred per CONTEXT.md Reviewed Todos).
    - `2026-05-12-make-backend-core-context-api-surfaces-explicit-with-namedin.md` (deferred per CONTEXT.md Reviewed Todos).

    DO NOT create `.planning/todos/archive/` or any other parent — D-15 says "or equivalent archive path the project uses"; this plan uses `done/` per the file_modified path in the frontmatter. If a different archive directory already exists at higher precedence (e.g., `.planning/todos/closed/`), use that and update the SUMMARY to note the deviation.
  </action>
  <acceptance_criteria>
    - `.planning/todos/done/` directory exists: `test -d .planning/todos/done/` exits 0.
    - The file is in the new location: `test -f .planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md` exits 0.
    - The file is no longer in the old location: `test ! -f .planning/todos/pending/2026-04-28-worker-application-yml-fail-fast-parity.md` exits 0.
    - Git history preserved: `git log --follow .planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md` shows the original 2026-04-28 commit.
    - The two other pending todos are untouched: `test -f .planning/todos/pending/2026-04-28-wr-06-test-profile-securityconfig-slice.md` AND `test -f .planning/todos/pending/2026-05-12-make-backend-core-context-api-surfaces-explicit-with-namedin.md`.
    - If the resolution section was appended: `grep -c 'Resolution (2026-05-14, Phase 6 D-15)' .planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md` returns 1; otherwise skip this assertion.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md && test ! -f .planning/todos/pending/2026-04-28-worker-application-yml-fail-fast-parity.md</automated>
  </verify>
  <done>Resolved todo archived via `git mv`; git history preserved; two other deferred todos untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Operator action — cut `v1.0.0-rc1` annotated tag (single push, pre-enriched annotation per MED-7), fill in evidence URL, check boxes, commit sign-off line, close STATE.md (LOW-10)</name>
  <files>.planning/LAUNCH-GO-NOGO.md</files>
  <action>This is a checkpoint:human-action — Claude does NOT execute these steps. Pause and present the operator runbook in <how-to-verify> to the user, then wait for the <resume-signal>. The runbook reflects MED-7 (single tag push with pre-enriched annotation; NO delete-and-re-push) and LOW-10 (STATE.md update step at the end). See <how-to-verify> for the 8-step runbook.</action>
  <verify>
    <automated>git tag -l v1.0.0-rc1 | grep -q v1.0.0-rc1 && git cat-file -t v1.0.0-rc1 | grep -q tag && grep -q "^✓ signed-off by" .planning/LAUNCH-GO-NOGO.md && test -f .planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md</automated>
  </verify>
  <done>v1.0.0-rc1 annotated tag exists on main (pushed exactly once on the SIGNED commit per MED-7 + codex HIGH-10); LAUNCH-GO-NOGO.md has all 8 boxes checked, a committed sign-off line, and the Release Gates URL in item (a); 06-LOAD-TEST-RESULT.md is committed under .planning/phases/06-.../ (codex HIGH-9); STATE.md records Phase 6 closure; release-gates-summary is green for the FINAL tag commit.</done>
  <what-built>
    Plans 06-01 through 06-04 plus Tasks 1-3 of this plan have landed:
    - Test-only `e2e-stub` + `loadtest` profile infrastructure (Plan 06-01).
    - 50-tenant docker-compose load harness + `:backend:api:loadtestVerify` Gradle task + `wait-for-worker-drain.sh` (Plan 06-02).
    - Playwright golden-path spec under `e2e-stub` profile (Plan 06-03).
    - Reusable `gates.yml` + tag-triggered `release.yml` with `release-gates-summary` (Plan 06-04).
    - `LAUNCH-GO-NOGO.md` (8 unchecked boxes) + SEED-012 + archived todo.

    The remaining acts are operator-only because they require live evidence URLs from a real `release.yml` run:
    1. Run the load test locally + commit `06-LOAD-TEST-RESULT.md` to main (codex HIGH-9 — committed evidence pre-tag).
    2. Cut the `v1.0.0-rc1-candidate` annotated tag (codex HIGH-10 — two-tag pattern: candidate runs the gate to produce the URL).
    3. Wait for the candidate run's `Release Gates Summary` to report green.
    4. Fill the gate URL into `LAUNCH-GO-NOGO.md` item (a), check the 8 boxes.
    5. Commit the sign-off line `✓ signed-off by @<user> on <ISO date>`.
    6. Cut the FINAL `v1.0.0-rc1` annotated tag on the signed commit (MED-7 — single push, never delete-and-re-push the launch tag).
    7. Wait for the final-tag gate run to confirm green; optionally delete the candidate tag as housekeeping.
    8. Close Phase 6 in `.planning/STATE.md` via `gsd-sdk query state planned-phase` (LOW-10).
  </what-built>
  <how-to-verify>
    Run these steps from the repo root in order. STOP if any step fails — that is a launch blocker.

    Step 1 — Confirm working tree is clean and on `main`:
    ```
    git status
    git branch --show-current   # expect: main
    git pull --ff-only
    ```

    Step 2 — Identify the RC commit SHA + capture today's ISO date:
    ```
    SHA=$(git rev-parse HEAD)
    ISO=$(date -u +%Y-%m-%d)
    echo "SHA=$SHA ISO=$ISO"
    ```

    Step 2.5 — codex HIGH-9: run the load test LOCALLY and commit the result file BEFORE cutting any tag. Follow `loadtest/README.md` `## Pre-tag commit step` exactly. Summary:
    ```
    docker compose -f loadtest/compose.loadtest.yml down -v || true
    ./gradlew --no-daemon :backend:api:bootBuildImage --imageName=zeromail-api:loadtest
    ./gradlew --no-daemon :backend:worker:bootBuildImage --imageName=zeromail-worker:loadtest
    export REFRESH_TOKEN_KEY_BASE64=$(openssl rand -base64 32)
    docker compose -f loadtest/compose.loadtest.yml up -d --wait
    PGPASSWORD=zeromail psql -h localhost -p 15433 -U zeromail -d zeromail -f loadtest/scripts/seed-tenants.sql
    export LOADTEST_TENANT_UUIDS=$(PGPASSWORD=zeromail psql -h localhost -p 15433 -U zeromail -d zeromail -tAc "SELECT string_agg(tenant_id::text, ',' ORDER BY slug) FROM loadtest_tenant")
    API_BASE_URL=http://localhost:8080 k6 run loadtest/scripts/golden-path.js
    bash loadtest/scripts/wait-for-worker-drain.sh
    mkdir -p loadtest/run && docker compose -f loadtest/compose.loadtest.yml logs --no-color --no-log-prefix api worker > loadtest/run/run.log
    ./gradlew --no-daemon :backend:api:loadtestVerify
    docker compose -f loadtest/compose.loadtest.yml down -v
    git add .planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md
    git commit -m "docs(06): commit load-test result for v1.0.0-rc1"
    git push origin main
    SHA=$(git rev-parse HEAD)   # refresh SHA after the result-file commit
    ```
    If any invariant fails, STOP — do not proceed to tag cut. Fix forward via a hotfix plan + repeat Step 2.5.

    Step 3 — codex HIGH-10: cut the CANDIDATE tag first. It matches release.yml's `'v*.*.*-rc*'` trigger (the `*` is greedy across `-`):
    ```
    git tag -a v1.0.0-rc1-candidate "$SHA" -m "Zero Mail v1.0.0-rc1 CANDIDATE — SHA: $SHA — Date: $ISO. Produces gate URL for LAUNCH-GO-NOGO.md item (a). Final v1.0.0-rc1 tag will be cut on the signed commit AFTER sign-off."
    git push origin v1.0.0-rc1-candidate
    ```
    This triggers `release.yml` against the result-file commit; the candidate run produces the URL captured in Step 5.

    Step 4 — Watch the candidate `Release Gates` workflow:
    ```
    gh run watch --workflow=release.yml
    # or:
    gh run list --workflow=release.yml --limit 1 --json url,status,conclusion
    gh run view <run-id> --log
    ```

    The candidate's `release-gates-summary` job MUST report `success`. If ANY upstream gate failed, do NOT proceed. Fix forward, repeat Step 2.5 (re-run load test, commit fresh result), and cut a new candidate tag (e.g., `v1.0.0-rc1-candidate-b`) — never delete-and-re-push the same candidate. If `v1.0.0-rc2` is needed instead, follow the same two-tag pattern (`v1.0.0-rc2-candidate` → sign-off → `v1.0.0-rc2`). No workflow edits per D-12.

    Step 5 — Capture the green check URL into a shell var for the next step:
    ```
    GATE_URL=$(gh run view <run-id> --json url -q .url)
    echo "$GATE_URL"
    ```

    Step 6 — Edit `.planning/LAUNCH-GO-NOGO.md`:
    - Replace the `_(operator fills in after release-gates-summary reports green — see Task 4)_` placeholder in item (a) with `[Release Gates run #<run-id>]($GATE_URL)`.
    - Flip each of the 8 `- [ ]` boxes to `- [x]`. ONE PASS — do not check selectively; if you can't check a box, the launch is no-go.
    - Promote the sign-off comment from `<!-- ✓ signed-off by @<user> on <ISO date> -->` to a real line (remove the `<!--` and `-->` wrappers, fill in your handle + today's ISO date).
    - Save.

    Step 7 — Commit the sign-off (single commit on main):
    ```
    git add .planning/LAUNCH-GO-NOGO.md
    git commit -m "docs(06): sign off v1.0.0-rc1 launch decision"
    git push origin main
    SIGNED_SHA=$(git rev-parse HEAD)
    ```

    Step 7.5 — codex HIGH-10: cut the FINAL annotated `v1.0.0-rc1` tag on the SIGNED commit. Push exactly once (MED-7). The launch tag now points at a commit that contains the signed launch decision:
    ```
    git tag -a v1.0.0-rc1 "$SIGNED_SHA" -m "Zero Mail v1.0.0-rc1 — SHA: $SIGNED_SHA — Date: $(date -u +%Y-%m-%d) — Launch mode: OAuth Testing. CASA deferred per SEED-012. Signed-off launch decision: .planning/LAUNCH-GO-NOGO.md. Candidate gate run: $GATE_URL."
    git push origin v1.0.0-rc1
    ```
    The push triggers `release.yml` again against the signed commit. Wait for it to complete + verify `release-gates-summary` is green; if green, the tag IS the canonical launch checkpoint. Optional housekeeping AFTER the final-tag run is green:
    ```
    git tag -d v1.0.0-rc1-candidate
    git push origin :refs/tags/v1.0.0-rc1-candidate
    ```

    Step 8 — LOW-10: close Phase 6 in planning state and commit the STATE.md change:
    ```
    gsd-sdk query state planned-phase --phase 6 --name polish-casa-verified-launch --plans 5
    git add .planning/STATE.md
    git commit -m "docs(06): close Phase 6 — v1.0.0-rc1 launched in OAuth Testing mode"
    git push origin main
    ```
    If the SDK is not on `PATH`, use the same handler argv with the project-local `gsd-sdk` shim; if no shim exists, document the deviation in the SUMMARY and update STATE.md by hand to reflect Phase 6 closure (5 plans, status=complete).

    Step 9 — Verify the launch state end-to-end:
    ```
    git tag -l 'v1.0.0-rc*'                              # expect: v1.0.0-rc1 (candidate optionally cleaned up in Step 7.5)
    git cat-file -t v1.0.0-rc1                           # expect: tag (annotated)
    git merge-base --is-ancestor v1.0.0-rc1 main && echo "tag on main"
    git rev-list -n 1 v1.0.0-rc1                         # confirms tag points at the SIGNED commit ($SIGNED_SHA)
    grep '✓ signed-off by' .planning/LAUNCH-GO-NOGO.md   # expect: one match, no <!-- wrapper
    test -f .planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md && echo "load-test result committed"
    grep -q 'polish-casa-verified-launch' .planning/STATE.md && echo "STATE.md closed Phase 6"
    ```
  </how-to-verify>
  <resume-signal>Type `signed-off` once steps 1–9 complete and `release-gates-summary` is green for the FINAL `v1.0.0-rc1` tag commit (the one on the signed commit per codex HIGH-10). Type `no-go: <reason>` if any candidate gate failed — the executor will route to a hotfix plan, repeat Step 2.5 (rerun load test + commit fresh result), cut a new candidate tag, and re-attempt; no workflow edits per D-12.</resume-signal>
  <git_artifacts>
    <!-- MED-10: surface mutations that are NOT files. -->
    - `git tag v1.0.0-rc1-candidate` (annotated; pushed once in Step 3, then optionally deleted in Step 7.5 — housekeeping only, never the canonical launch tag).
    - `git tag v1.0.0-rc1` (annotated; pushed EXACTLY ONCE in Step 7.5 on the SIGNED commit per MED-7 + codex HIGH-10). This IS the canonical launch checkpoint and the canonical trigger for `.github/workflows/release.yml`.
    - Committed file mutation: `.planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md` is committed in Step 2.5 BEFORE either tag is cut (codex HIGH-9 — committed evidence requirement).
    - No delete-and-re-push of the final `v1.0.0-rc1` tag is performed at any point.
  </git_artifacts>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `LAUNCH-GO-NOGO.md` ↔ launch decision | Single signed artifact; tampering with the sign-off line or the 8 checkboxes is detectable in git history. |
| SEED-012 ↔ CASA-deferred work | Tracks the post-launch obligation; FND-07 stays Pending until the seed closes. |
| RC tag annotation ↔ evidence | Tag message embeds SHA + date + launch mode (pre-enriched before the single push); the gate URL lives in `LAUNCH-GO-NOGO.md` item (a) only. MED-7: no delete-and-re-push window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.05-01 | Tampering | Trust-story phrases in item (g) silently paraphrased | mitigate | grep gates in this plan's Task 1 acceptance criteria + the `trust-story-grep` job in Plan 06-04's `release.yml` (literal grep on every RC tag push). |
| T-06.05-02 | Repudiation | Sign-off line removed or backdated post-hoc | mitigate | The line is committed; git history (commit author, date, content) is the audit trail. |
| T-06.05-03 | Information Disclosure | SEED-012 evidence list reveals system specifics to a future leak | accept | The list is best-practice CASA requirements (public via Google docs); not sensitive. |
| T-06.05-04 | Elevation of Privilege | OAuth consent screen accidentally moved to Production before CASA closes | mitigate | Outside repo control — Google Cloud Console; item (h) of LAUNCH-GO-NOGO.md is the contractual reminder. |
| T-06.05-05 | Tampering | Stale tag pulled during a delete-and-re-push window | mitigate | MED-7 + codex HIGH-10: the FINAL `v1.0.0-rc1` tag is pushed exactly once on the signed commit; no delete-and-re-push of the launch tag exists. Only the candidate tag (`v1.0.0-rc1-candidate`) may be deleted as housekeeping. |
| T-06.05-06 | Tampering | Cycle-1 flow committed the sign-off AFTER cutting the tag, so the tag did not point at the signed decision (codex HIGH-10) | mitigate | Two-tag pattern: candidate produces the gate URL; sign-off is committed on main; FINAL `v1.0.0-rc1` tag is cut on the signed SHA. |
| T-06.05-07 | Repudiation | Cycle-1 generated `06-LOAD-TEST-RESULT.md` in CI AFTER the tag push, so committed evidence lagged the tag (codex HIGH-9) | mitigate | Pre-tag local-run in Step 2.5; result file committed on main BEFORE either tag is cut; CI re-runs the test against the tag for record. |
</threat_model>

<verification>
- `.planning/LAUNCH-GO-NOGO.md` exists with 8 unchecked boxes and the 3 verbatim trust-story phrases (pre-Task 4 state).
- `.planning/seeds/SEED-012-casa-restricted-scope-verification.md` exists with 8 H2 sections + correct frontmatter.
- `.planning/todos/done/2026-04-28-worker-application-yml-fail-fast-parity.md` exists; pending counterpart deleted.
- After Task 4: `git tag -l 'v1.0.0-rc1'` returns `v1.0.0-rc1`; `git cat-file -t v1.0.0-rc1` returns `tag`; `grep '✓ signed-off by' .planning/LAUNCH-GO-NOGO.md` returns one line; `.planning/STATE.md` reflects Phase 6 closure (LOW-10).
</verification>

<success_criteria>
- 3 new files (`LAUNCH-GO-NOGO.md`, `SEED-012-...md`, archived todo) committed before Task 4.
- Task 4 is the only operator-action step in Phase 6; no other plan blocks on it.
- Phase 6 closes when the sign-off line is committed, the gate URL is in `LAUNCH-GO-NOGO.md` item (a), and STATE.md records the closure.
- The `v1.0.0-rc1` tag is pushed exactly once (MED-7) with a pre-enriched annotation; no delete-and-re-push step exists.
- FND-07 remains `Pending` in REQUIREMENTS.md (unchanged in Phase 6); SEED-012 tracks the obligation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-casa-verified-launch/06-05-SUMMARY.md` documenting:
- The exact tag annotation message used in Step 3 (so v1.0.0-rc2, if cut, can mirror the format).
- The `Release Gates` run-id + URL that was recorded in `LAUNCH-GO-NOGO.md` item (a).
- Any failed gates that required a hotfix + the path taken (forward-fix + rc2 with same single-push pattern, or roll back).
- D-15 archive path used (default `.planning/todos/done/` OR project-pre-existing path).
- The ISO date of sign-off + the operator handle (for the project decision log).
- LOW-10 confirmation: the `gsd-sdk query state planned-phase ...` command exit code, and the resulting STATE.md diff.
- MED-7 confirmation: the tag was pushed exactly once (no delete-and-re-push in the operator session).
</output>
