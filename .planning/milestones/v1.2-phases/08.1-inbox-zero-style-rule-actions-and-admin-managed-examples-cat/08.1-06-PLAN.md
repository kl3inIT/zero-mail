---
phase: 08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat
plan: 08.1-06
type: execute
wave: 4
depends_on:
  - 08.1-01
  - 08.1-02
  - 08.1-05
files_modified:
  - backend/core/src/main/java/com/zeromail/core/outbound/
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageSafetyPolicy.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageOrchestratorService.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java
  - backend/core/src/main/java/com/zeromail/core/rules/domain/
  - backend/core/src/test/java/com/zeromail/core/arch/
  - backend/core/src/test/java/com/zeromail/core/triage/
  - backend/api/src/test/java/com/zeromail/api/
  - apps/web/e2e/
  - docs/uat/phase-08.1-rule-actions.md
autonomous: false
requirements:
  - RACT-07
  - RACT-08
  - RACT-09
  - RACT-10
  - RACT-11
  - RACT-12
user_setup:
  - service: gmail-test-account
    why: "Final UAT for real send/reply/forward behavior should use a controlled Gmail account and safe recipient allow-list."
    dashboard_config:
      - task: "Provide test Gmail tenant with OAuth scopes for drafts and send, plus safe recipient addresses."
        location: "Local dev secrets / Google OAuth test user"
must_haves:
  truths:
    - "D-13 is authoritative: when auto-send is OFF or any safety gate fails, runtime saves a Gmail draft instead of sending."
    - "D-14 is authoritative: no separate needs_review UI in this phase."
    - "D-15 is authoritative: blocked sends must not silently no-op."
    - "D-16/D-17/D-18/D-19 are authoritative: one shared OutboundSendGateway, one allowed direct Gmail send boundary, no RuleSendExecutor bypass."
    - "D-21 allows exact gateway names as long as they are domain-neutral and boundary-clear."
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendGateway.java"
      provides: "Domain-neutral send API consumed by chat confirmations and rule runtime."
    - path: "backend/core/src/test/java/com/zeromail/core/arch/OutboundGmailSendBoundaryTest.java"
      provides: "ArchUnit rule forbidding direct Gmail send outside the gateway implementation."
    - path: "docs/uat/phase-08.1-rule-actions.md"
      provides: "Manual UAT covering examples, admin catalog, global toggle, fallback-to-draft, and no-bypass architecture tests."
  key_links:
    - from: "TriageOrchestratorService"
      to: "OutboundSendGateway"
      via: "outbound action runtime dispatch"
      pattern: "send_reply|forward_email|send_email"
    - from: "AssistantSendExecutor"
      to: "OutboundSendGateway"
      via: "chat confirmation preserved through shared gateway"
      pattern: "execute\\("
---

<objective>
Enable safe runtime execution for expanded rule actions, including send reply, forward, and send email, while preserving Phase 7 chat send behavior. All actual Gmail sends go through one shared outbound gateway. Any blocked outbound rule action falls back to saving a Gmail draft and records an audit reason.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@CONVENTIONS.md
@TESTING.md
@docs/architecture/outbound-gateway-boundary.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-CONTEXT.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-RESEARCH.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-01-SUMMARY.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-02-SUMMARY.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-05-SUMMARY.md
@backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java
@backend/core/src/main/java/com/zeromail/core/chat/confirm/send/GmailMessageBuilder.java
@backend/core/src/main/java/com/zeromail/core/triage/domain/TriageSafetyPolicy.java
@backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageOrchestratorService.java
@backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java
@backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 08.1-06-01: Extract shared OutboundSendGateway and preserve chat send behavior</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/outbound/,
    backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java,
    backend/core/src/main/java/com/zeromail/core/chat/confirm/send/GmailMessageBuilder.java,
    backend/core/src/test/java/com/zeromail/core/chat/confirm/send/,
    backend/core/src/test/java/com/zeromail/core/outbound/
  </files>
  <behavior>
    - Introduce domain-neutral `OutboundSendGateway` command/result records.
    - Move actual Gmail `messages().send(...)` call into one gateway implementation.
    - Keep message building, recipient hashing, safety-net checks, idempotency/audit hooks explicit.
    - Refactor `AssistantSendExecutor` to call the gateway and preserve existing audit state transitions.
    - Do not introduce a second send executor for rules.
  </behavior>
  <action>
    Create the shared outbound gateway API and implementation under the package agreed in plan 01. Move the Gmail send call into that implementation, then refactor `AssistantSendExecutor` to build a gateway command and handle the result while preserving its current lease release and confirmation state-machine transitions.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*AssistantSend*" --tests "*Outbound*"</automated>
  </verify>
  <done>
    Chat assistant sends still work through the shared gateway.
  </done>
  <acceptance_criteria>
    - Existing assistant send tests remain green.
    - Exactly one production class contains direct Gmail `messages().send`.
    - That class is under the shared outbound gateway package and annotated/marked as the allowed call site.
    - Gateway result exposes Gmail message/thread IDs without logging recipient/body content.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-06-02: Runtime gate matrix and fallback-to-draft for outbound rule actions</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/triage/domain/TriageSafetyPolicy.java,
    backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageOrchestratorService.java,
    backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java,
    backend/core/src/main/java/com/zeromail/core/outbound/,
    backend/core/src/test/java/com/zeromail/core/triage/
  </files>
  <behavior>
    - Runtime checks before send: global auto-send rules setting, sender-risk/static guard, sender safety net, daily/rate cap, idempotency, OAuth scope, tenant binding, audit reservation.
    - If any check fails, create a Gmail draft equivalent and audit the fallback reason.
    - If draft creation also fails, audit failure explicitly.
    - Non-outbound actions (`mark_read`, `star`, `add_to_digest`, `mark_spam`) execute through existing or new Gmail write adapters without using send gateway.
    - Sender-protected behavior remains conservative.
  </behavior>
  <action>
    Extend triage dispatch to classify action intents as non-outbound, outbound-sendable, or outbound-draft-fallback. Wire the global setting and gate checks ahead of the gateway call. For each failure path, construct an equivalent draft intent, call the existing draft write path, and persist a bounded audit reason.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*Triage*" --tests "*Outbound*"</automated>
  </verify>
  <done>
    Rules can safely dispatch expanded actions and blocked outbound sends degrade to drafts.
  </done>
  <acceptance_criteria>
    - Global auto-send OFF -> send_reply/forward_email/send_email creates draft and audit reason `AUTO_SEND_DISABLED`.
    - Sender safety net hit -> draft fallback, no send.
    - Low-trust/static demo sender pattern -> validation/runtime blocks send and saves draft.
    - Idempotency prevents duplicate sends for the same message/rule/action key.
    - OAuth missing send scope -> draft fallback or explicit audited failure, no send.
    - Tenant context mismatch throws/fails before Gmail call.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-06-03: ArchUnit, privacy, and grep gates for no-bypass send boundary</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/arch/OutboundGmailSendBoundaryTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java,
    backend/core/src/test/java/com/zeromail/core/privacy/
  </files>
  <behavior>
    - Replace assistant-package-specific expectation with shared gateway boundary rule.
    - Ban direct Gmail send methods from controllers, rules, triage orchestration, admin packages, worker packages, and compiler packages.
    - Keep privacy gates proving no Gmail-read body/prompt/completion persistence or logs.
    - Add fixture that fails if a fake direct send call appears in forbidden package.
  </behavior>
  <action>
    Rewrite the send-boundary tests around the shared gateway package and add explicit forbidden-package checks. Add privacy regression tests for rule action JSON and audit records, preserving the project's Gmail-read-content versus user-authored-draft-data distinction.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*Arch*" --tests "*Privacy*"</automated>
  </verify>
  <done>
    CI catches boundary bypasses before runtime tests.
  </done>
  <acceptance_criteria>
    - ArchUnit reports direct Gmail send outside gateway as failure.
    - Grep/count gate expects exactly one direct Gmail send call site and it is the gateway implementation.
    - Admin packages remain forbidden from Gmail send methods.
    - Privacy tests distinguish Gmail-read content (forbidden) from user-authored/action draft data (allowed).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 08.1-06-04 (CHECKPOINT): Safe Gmail UAT for outbound rule actions</name>
  <files>
    docs/uat/phase-08.1-rule-actions.md
  </files>
  <what-built>
    Expanded rule runtime with shared outbound gateway, global auto-send setting, fallback-to-draft, admin/user examples catalog, and no-bypass architecture tests.
  </what-built>
  <how-to-verify>
    1. Start API/worker/web with a controlled Gmail test tenant and safe recipient allow-list.
    2. Confirm settings `Auto-send rules` is ON by default.
    3. Create rule from a Founder/Developer example that sends a reply to a safe test message.
    4. Trigger matching incoming message; confirm Gmail Sent contains exactly one sent reply and audit row records gateway send.
    5. Turn `Auto-send rules` OFF.
    6. Trigger same rule on a new message; confirm no sent email and a Gmail draft exists with audit reason `AUTO_SEND_DISABLED`.
    7. Add safe recipient to sender safety net/protected list.
    8. Trigger outbound rule; confirm draft fallback and no send.
    9. Run architecture tests and confirm direct Gmail send boundary is green.
    10. Confirm no logs contain raw recipient body, Gmail body, prompt, completion, or draft body text beyond allowed IDs/hashes.
  </how-to-verify>
  <action>
    Pause for human verification against a controlled Gmail test tenant. Do not run this checkpoint against a real personal inbox or unapproved recipients. Record the user's approval or failed step in the plan summary.
  </action>
  <verify>
    <manual>Follow the ten-step safe Gmail UAT checklist above.</manual>
  </verify>
  <done>
    User types `approved` or provides the failed step and observed output.
  </done>
  <resume-signal>Type "approved" if all checks pass; otherwise describe the failed step and observed output.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 08.1-06-05: UAT/runbook documentation</name>
  <files>
    docs/uat/phase-08.1-rule-actions.md
  </files>
  <behavior>
    - Document safe test account setup, seed catalog check, admin edit check, user example flow, global toggle flow, fallback-to-draft flow, and no-bypass test commands.
    - Include exact commands for backend tests, frontend tests, and grep gate.
  </behavior>
  <action>
    Write the UAT document from the checkpoint flow and include copy-paste commands for local backend tests, frontend tests, architecture tests, and the direct Gmail send grep gate. Add a short safety note that production-like send tests require controlled recipients.
  </action>
  <verify>
    <automated>Test-Path docs/uat/phase-08.1-rule-actions.md; rg -n "AUTO_SEND_DISABLED|OutboundGmailSendBoundaryTest|Auto-send rules|fallback" docs/uat/phase-08.1-rule-actions.md</automated>
  </verify>
  <done>
    Future deployment smoke can repeat the same safety checks.
  </done>
  <acceptance_criteria>
    - UAT doc includes all RACT-12 scenarios.
    - UAT doc explicitly says real-send test must use controlled Gmail test tenant and safe recipients.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-081-01 | Elevation of privilege | Direct Gmail send bypass | mitigate | ArchUnit + grep: direct send only in gateway implementation. |
| T-081-02 | Information disclosure | Gmail-read body in action/prompt persistence | mitigate | Existing privacy sanitizer/ArchUnit/DB trigger distinction remains; tests cover Gmail-read vs user-authored draft data. |
| T-081-03 | Tampering | Duplicate sends on retry | mitigate | Idempotency key per tenant/message/rule/action; audit reserve before Gmail call. |
| T-081-04 | Spoofing | Wrong tenant sends | mitigate | TenantContext check before gateway call and OAuth client resolution. |
| T-081-05 | Repudiation | Send/fallback not explainable | mitigate | Append-only triage audit row with gate/fallback reason. |
| T-081-06 | Safety | Demo/static example sends to real person | mitigate | Low-trust/static guard equivalent to Inbox Zero; compile/save/runtime tests. |

</threat_model>

<verification>

```powershell
./gradlew :backend:core:test --tests "*AssistantSend*" --tests "*Outbound*" --tests "*Triage*" --tests "*Arch*" --tests "*Privacy*"
./gradlew :backend:api:test
pnpm --filter @zeromail/web typecheck
pnpm --filter @zeromail/web e2e -- --grep "rules examples"
rg -n "gmail\\.users\\(\\)\\.messages\\(\\)\\.send|messages\\(\\)\\.send\\(" backend/core/src/main/java
```

</verification>

<success_criteria>
- [ ] Chat assistant send path still works through shared gateway.
- [ ] Rule outbound actions execute only through shared gateway.
- [ ] Global auto-send OFF causes draft fallback.
- [ ] Runtime safety gate failures cause draft fallback with audit reason.
- [ ] Idempotency prevents duplicate sends.
- [ ] Direct Gmail send boundary tests pass.
- [ ] Privacy tests pass.
- [ ] Human Gmail UAT checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-06-SUMMARY.md` when done.
</output>
