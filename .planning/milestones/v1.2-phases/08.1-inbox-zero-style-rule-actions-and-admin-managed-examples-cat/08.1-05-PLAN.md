---
phase: 08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat
plan: 08.1-05
type: execute
wave: 3
depends_on:
  - 08.1-01
  - 08.1-02
files_modified:
  - backend/core/src/main/java/com/zeromail/core/llm/domain/Action.java
  - backend/core/src/main/java/com/zeromail/core/rules/domain/RuleActionType.java
  - backend/core/src/main/java/com/zeromail/core/rules/domain/ActionIntent.java
  - backend/core/src/main/java/com/zeromail/core/rules/usecases/ActionIntentJsonValidator.java
  - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleCompileResultValidator.java
  - backend/core/src/main/java/com/zeromail/core/llm/usecases/
  - backend/core/src/test/java/com/zeromail/core/rules/
  - apps/web/features/rules/lib/rule-structure.ts
  - apps/web/features/rules/lib/rule-structure-copy.ts
  - apps/web/features/rules/components/RuleComposer.tsx
  - apps/web/messages/
autonomous: true
requirements:
  - RACT-04
  - RACT-06
  - RACT-10
  - RACT-11
  - RACT-12
must_haves:
  truths:
    - "D-01/D-02 remain constraints: schema changes must preserve current composer/manual editor flow."
    - "D-13 matters for schema: outbound action intents need enough structured data to fallback to draft without losing useful output."
    - "D-18 matters for schema: compiler/runtime must not include direct Gmail send implementation details."
    - "Rule authoring policy remains authoritative: natural language is only compiler input; saved rule is structured When/Then schema."
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/rules/domain/ActionIntent.java"
      provides: "Sealed action intent hierarchy for expanded action set."
    - path: "apps/web/features/rules/lib/rule-structure.ts"
      provides: "Manual editor can build the same structured JSON as compiler output."
  key_links:
    - from: "RuleCompileResultValidator"
      to: "ActionIntentJsonValidator"
      via: "same allowed action payload contract"
      pattern: "send_reply|forward_email|send_email|mark_read|star|add_to_digest|mark_spam"
---

<objective>
Expand the rule action contract end-to-end so both the AI compiler and the manual editor persist the same structured `When/Then` action schema for label/archive/draft plus mark/read/star/digest/spam/outbound actions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONVENTIONS.md
@TESTING.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-CONTEXT.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-RESEARCH.md
@backend/core/src/main/java/com/zeromail/core/llm/domain/Action.java
@backend/core/src/main/java/com/zeromail/core/rules/domain/RuleActionType.java
@backend/core/src/main/java/com/zeromail/core/rules/domain/ActionIntent.java
@backend/core/src/main/java/com/zeromail/core/rules/usecases/ActionIntentJsonValidator.java
@backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleCompileResultValidator.java
@apps/web/features/rules/lib/rule-structure.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 08.1-05-01: Backend action enum, intent payloads, and JSON validation</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/llm/domain/Action.java,
    backend/core/src/main/java/com/zeromail/core/rules/domain/RuleActionType.java,
    backend/core/src/main/java/com/zeromail/core/rules/domain/ActionIntent.java,
    backend/core/src/main/java/com/zeromail/core/rules/usecases/ActionIntentJsonValidator.java,
    backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleCompileResultValidator.java,
    backend/core/src/test/java/com/zeromail/core/rules/
  </files>
  <behavior>
    - Add action IDs: `mark_read`, `star`, `add_to_digest`, `mark_spam`, `send_reply`, `forward_email`, `send_email`.
    - Define explicit payload records:
      - no-value actions for archive/mark_read/star/add_to_digest/mark_spam unless local domain requires a parameter.
      - label requires `labelName`.
      - save_draft and send_reply require `instruction` or draft body intent.
      - forward_email requires recipient(s) plus optional note/instruction.
      - send_email requires to/cc/bcc/subject/body or structured draft fields.
    - Validate bounds, required fields, safe recipient syntax shape, and no unknown fields.
    - Do not store Gmail-read body content in action JSON.
  </behavior>
  <action>
    Extend the Java enums and sealed action-intent hierarchy first, then update all JSON validators and compile-result validators to recognize the new explicit payload records. Add regression tests for each action's valid and invalid JSON shape before touching compiler/UI code.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*ActionIntent*" --tests "*RuleCompile*"</automated>
  </verify>
  <done>
    Backend accepts only well-formed expanded action intents.
  </done>
  <acceptance_criteria>
    - Every required action type round-trips through enum `fromId`.
    - Validator rejects unknown action types and unknown action fields.
    - Outbound payload tests cover missing recipient/subject/body/instruction cases.
    - Privacy test proves action JSON cannot contain Gmail-read body/snippet fields.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-05-02: AI compiler schema and prompt contract update</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/llm/usecases/,
    backend/core/src/test/java/com/zeromail/core/llm/,
    backend/core/src/test/java/com/zeromail/core/rules/
  </files>
  <behavior>
    - Update tool/schema contracts so the LLM compiler extracts action fields through structured output.
    - Follow prompt rewrite policy: outcome-first, output contract, success criteria, constraints, clarification policy, examples.
    - Do not add regex/keyword post-processing to infer user-meaning fields.
    - Include examples for VI and EN prompts with outbound actions.
    - Low-trust/static demo patterns must compile to safe drafts or validation errors, not direct send rules.
  </behavior>
  <action>
    Update the compiler's structured output schema/tool definitions and system prompt together so action fields are extracted directly by the model. Add tests around representative VI/EN prompts for reply, forward, and send email. If a prompt is unsafe or too ambiguous, make the compiler return clarification/invalid rather than backend semantic cleanup.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*RuleCompiler*" --tests "*AllowListedTools*" --tests "*Action*"</automated>
  </verify>
  <done>
    Compiler tests prove natural language can produce expanded structured action intents without backend semantic cleanup hacks.
  </done>
  <acceptance_criteria>
    - Compiler structured schema includes all required action types.
    - Prompt includes representative examples for reply/forward/send.
    - Tests assert no semantic regex cleanup was added for display/action field extraction.
    - Static/demo outbound examples are rejected or downgraded by validation.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-05-03: Frontend manual builder schema and summaries</name>
  <files>
    apps/web/features/rules/lib/rule-structure.ts,
    apps/web/features/rules/lib/rule-structure-copy.ts,
    apps/web/features/rules/components/RuleComposer.tsx,
    apps/web/messages/
  </files>
  <behavior>
    - Extend `MANUAL_ACTION_TYPES` and zod schemas.
    - Add field controls for recipient/subject/body/instruction where needed.
    - Keep action rows stable with responsive dimensions; no text overlap.
    - Summaries and compiled review understand every expanded action.
    - Outbound actions render `Will auto-send` copy when settings toggle is ON, but saving is not blocked by UI acknowledgement.
  </behavior>
  <action>
    Update the TypeScript manual rule schema, action-to-intent conversion, JSON parsing, summaries, labels, and form rows. Add unit tests for round-tripping compiled action JSON into manual draft rows and back into saved structured JSON for every supported action type.
  </action>
  <verify>
    <automated>pnpm --filter @zeromail/web test:unit -- --run rule-structure && pnpm --filter @zeromail/web typecheck</automated>
  </verify>
  <done>
    Manual editor builds the same JSON shape as compiler output.
  </done>
  <acceptance_criteria>
    - Manual builder can create each required action type.
    - `manualDraftFromCompiledRule` parses expanded action JSON back into form rows.
    - `summarizeActionIntents` produces readable labels for expanded actions.
    - UI does not show old "unsafe auto-send disabled" badges as hard bans.
  </acceptance_criteria>
</task>

</tasks>

<verification>

```powershell
./gradlew :backend:core:test --tests "*ActionIntent*" --tests "*RuleCompile*" --tests "*RuleCompiler*" --tests "*AllowListedTools*"
pnpm --filter @zeromail/web test:unit -- --run rule-structure
pnpm --filter @zeromail/web typecheck
```

</verification>

<success_criteria>
- [ ] Backend action enum/model supports all required actions.
- [ ] Validator rejects unknown/unsafe action JSON.
- [ ] Compiler schema/prompt supports expanded actions through structured output.
- [ ] Manual builder supports the same action JSON.
- [ ] Low-trust demo outbound examples are blocked or downgraded before runtime send.
- [ ] Privacy tests protect Gmail-read body content.
</success_criteria>

<output>
Create `.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-05-SUMMARY.md` when done.
</output>
