---
phase: 08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat
plan: 08.1-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - AGENTS.md
  - CLAUDE.md
  - .planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-SPEC.md
  - docs/architecture/outbound-gateway-boundary.md
  - backend/core/src/main/java/com/zeromail/core/**/package-info.java
  - backend/core/src/test/java/com/zeromail/core/arch/
  - backend/api/src/test/java/com/zeromail/api/ZeroMailApiApplicationModulesTest.java
  - backend/api/build/spring-modulith-docs/
autonomous: true
requirements:
  - RACT-05
  - RACT-07
  - RACT-09
  - RACT-10
  - RACT-12
must_haves:
  truths:
    - "D-09/D-10/D-11 are authoritative: Phase 08.1 uses one global `Auto-send rules` setting, default ON, with no per-action toggles and no rule-level acknowledgement checkbox/modal."
    - "D-12 is authoritative: user UI may show `Will auto-send` copy/badges but must not block saving."
    - "D-13/D-14/D-15 are authoritative: global toggle OFF or runtime safety failure falls back to Gmail draft; separate needs_review UI is out of scope."
    - "D-16/D-17/D-18/D-19 are authoritative: all Gmail send calls must move behind one shared outbound gateway boundary; no separate `RuleSendExecutor`."
    - "D-20/D-21 are authoritative: named-interface/API-surface work is folded into this phase and package/class names may be chosen if domain-neutral."
    - "All stale docs/requirements/SPEC/agent instructions are reconciled before code plans 02-06 execute."
    - "Spring Modulith verification passes and docs are regenerated after named interfaces are added."
  artifacts:
    - path: "docs/architecture/outbound-gateway-boundary.md"
      provides: "Contract for shared OutboundSendGateway, allowed call sites, audit/fallback obligations, and direct Gmail send ban."
    - path: "backend/core/src/main/java/com/zeromail/core/**/package-info.java"
      provides: "Named interfaces such as `rules::api`, `outbound::api`, `tenant::api`, `triage::api` where this phase needs cross-module calls."
    - path: "backend/api/build/spring-modulith-docs/all-docs.adoc"
      provides: "Regenerated Modulith documentation showing no cycles and explicit public surfaces."
  key_links:
    - from: "backend/core/outbound/*"
      to: "backend/core/gmail/gateway/GmailApiClientFactory"
      via: "single gateway implementation"
      pattern: "gmail\\.users\\(\\)\\.messages\\(\\)\\.send|@AllowedSendCallSite"
---

<objective>
Resolve the Phase 08.1 contract before implementation: update stale requirements/docs to the locked user-first decisions, define the shared outbound gateway boundary, and make Spring Modulith public API surfaces explicit enough that plans 02-06 cannot bypass module boundaries.

Output: future executors see one consistent rule: global `Auto-send rules` default ON, no per-action toggles, no rule-level acknowledgement, fallback-to-draft on blocked runtime sends, and Gmail sends only through a shared outbound gateway.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@CONVENTIONS.md
@TESTING.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-CONTEXT.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-SPEC.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-RESEARCH.md
@.planning/todos/pending/2026-05-12-make-backend-core-context-api-surfaces-explicit-with-namedin.md
@backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java
@backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java
</context>

<documentation_lookup>
Before editing package metadata, verify current Spring Modulith named-interface syntax against official docs. Context7 may be quota-limited; fallback official source used during planning: https://docs.spring.io/spring-modulith/reference/1.4/fundamentals.html. Executor should confirm `@NamedInterface` and `allowedDependencies = "module :: api"` syntax before coding.
</documentation_lookup>

<tasks>

<task type="auto" tdd="false">
  <name>Task 08.1-01-01: Reconcile stale product/security docs and requirement text</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/PROJECT.md,
    AGENTS.md,
    CLAUDE.md,
    .planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-SPEC.md
  </files>
  <read_first>
    .planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-CONTEXT.md,
    .planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-RESEARCH.md
  </read_first>
  <behavior>
    - Replace per-action toggle/default-OFF language with one global `Auto-send rules` setting default ON.
    - Remove rule-level acknowledgement checkbox/modal as a requirement.
    - Preserve runtime gates: sender-risk guard, sender safety net, daily/rate cap, idempotency, OAuth scope, tenant isolation, audit, and fallback-to-draft.
    - Rewrite RACT-05 and RACT-07 to match D-09..D-15.
    - Keep `needs_review` only as a future/deferred concept; Phase 08.1 fallback is save draft.
  </behavior>
  <action>
    Edit each listed planning/agent file to replace stale outbound-policy wording with the locked context decisions. Keep the requirement IDs unchanged, but rewrite their body text so future traceability still points to RACT-05 and RACT-07. After editing, run the grep verification and manually inspect any remaining hits to confirm they are explicit out-of-scope/deferred notes rather than active requirements.
  </action>
  <verify>
    <automated>rg -n "per-action|rule-level acknowledgement|defaults? OFF|risk acknowledgement|needs_review" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md AGENTS.md CLAUDE.md .planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-SPEC.md</automated>
  </verify>
  <done>
    No stale policy text remains except explicit "out of scope/deferred" notes.
  </done>
  <acceptance_criteria>
    - RACT-05 says exactly one global auto-send rules setting, default ON.
    - RACT-07 lists runtime gates but does not mention per-action setting or rule-level acknowledgement.
    - ROADMAP success criteria 5 and 7 match RACT-05/07.
    - AGENTS.md and CLAUDE.md rule authoring policy match the new global-toggle wording.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-01-02: Define outbound gateway boundary contract and named interfaces</name>
  <files>
    docs/architecture/outbound-gateway-boundary.md,
    backend/core/src/main/java/com/zeromail/core/**/package-info.java,
    backend/api/src/test/java/com/zeromail/api/ZeroMailApiApplicationModulesTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/
  </files>
  <read_first>
    .planning/todos/pending/2026-05-12-make-backend-core-context-api-surfaces-explicit-with-namedin.md,
    backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java
  </read_first>
  <behavior>
    - Add named interfaces only for packages that must be consumed across contexts; keep persistence/domain internals hidden.
    - Document the gateway API as domain-neutral: chat and rules both send through it, but no controller/rule/compiler/runtime class can call Gmail send directly.
    - Update/add tests so future cross-module imports into `..persistence..`, `..domain..` internals, or direct Gmail send methods fail CI.
    - Do not move runtime code yet beyond package metadata/test scaffolding unless needed to keep tests compiling; actual gateway extraction belongs in plan 06.
  </behavior>
  <action>
    Create the outbound boundary doc first, then add `package-info.java` named interfaces for only the exported API packages needed by this phase. Adjust module dependency declarations and architecture tests so violations point developers to the named API or gateway boundary. Keep behavioral send extraction out of this task unless a compile error forces a minimal placeholder interface.
  </action>
  <verify>
    <automated>./gradlew :backend:api:test --tests "*ApplicationModules*" && ./gradlew :backend:core:test --tests "*Arch*"</automated>
  </verify>
  <done>
    Spring Modulith can verify named API surfaces and architecture tests encode the intended send boundary before runtime work begins.
  </done>
  <acceptance_criteria>
    - `@NamedInterface("api")` package-info files exist for the exported surfaces used by Phase 08.1.
    - No named interface is added to persistence packages.
    - Gateway boundary doc names allowed callers, forbidden direct callers, fallback behavior, and audit obligations.
    - Architecture tests still fail if direct Gmail send appears outside the future shared gateway package.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 08.1-01-03: Regenerate Spring Modulith docs and clear folded todo</name>
  <files>
    backend/api/build/spring-modulith-docs/all-docs.adoc,
    backend/api/build/spring-modulith-docs/components.puml,
    .planning/todos/pending/2026-05-12-make-backend-core-context-api-surfaces-explicit-with-namedin.md
  </files>
  <behavior>
    - Run the existing Modulith documentation generation test/task after metadata changes.
    - Record the generated docs locations in the summary.
    - Move or mark the folded todo as completed according to local GSD todo convention only after verification passes.
  </behavior>
  <action>
    Execute the repo's existing Modulith documentation generation path, inspect the generated docs for the new named-interface surfaces, and move the folded todo out of pending only after the docs and tests are green. In the summary, include exact Windows paths and the command used to regenerate them.
  </action>
  <verify>
    <automated>Test-Path backend/api/build/spring-modulith-docs/all-docs.adoc; Test-Path backend/api/build/spring-modulith-docs/components.puml</automated>
  </verify>
  <done>
    Modulith docs exist locally and folded todo is no longer pending.
  </done>
  <acceptance_criteria>
    - `all-docs.adoc` and `components.puml` are generated.
    - Summary explains how to open the docs on Windows.
    - Folded todo is removed from pending only after tests/docs generation pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>

```powershell
rg -n "per-action|rule-level acknowledgement|defaults? OFF|risk acknowledgement" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md AGENTS.md CLAUDE.md .planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-SPEC.md
./gradlew :backend:api:test --tests "*ApplicationModules*"
./gradlew :backend:core:test --tests "*Arch*"
Test-Path backend/api/build/spring-modulith-docs/all-docs.adoc
Test-Path backend/api/build/spring-modulith-docs/components.puml
```

</verification>

<success_criteria>
- [ ] Docs/requirements contain no stale per-action toggle/default-OFF/rule-level acknowledgement requirements.
- [ ] Global `Auto-send rules` default ON is documented consistently.
- [ ] Runtime gate/fallback/audit obligations remain documented.
- [ ] Named interfaces expose only intended API packages.
- [ ] Spring Modulith verification passes.
- [ ] Send-boundary architecture tests are ready for plan 06 extraction.
- [ ] Modulith docs generated.
</success_criteria>

<output>
Create `.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-01-SUMMARY.md` when done.
</output>
