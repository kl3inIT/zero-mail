---
phase: 08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat
plan: 08.1-04
type: execute
wave: 3
depends_on:
  - 08.1-02
files_modified:
  - apps/web/features/rules/api/
  - apps/web/features/rules/hooks/
  - apps/web/features/rules/query-keys.ts
  - apps/web/features/rules/components/RuleComposer.tsx
  - apps/web/features/rules/components/RulesWorkspace.tsx
  - apps/web/features/rules/components/AvailableActionsPanel.tsx
  - apps/web/app/(protected)/(app)/settings/SettingsClient.tsx
  - apps/web/messages/
  - apps/web/e2e/
autonomous: true
requirements:
  - RACT-01
  - RACT-02
  - RACT-04
  - RACT-05
  - RACT-12
must_haves:
  truths:
    - "D-01 is authoritative: keep the current RuleComposer and do not build a new wizard/page."
    - "D-02 is authoritative: `Choose from examples` lives inside Describe tab and examples render where current hard-coded chips live."
    - "D-03 is authoritative: clicking an example replaces the full textarea."
    - "D-04 is authoritative: persona list matches Inbox Zero."
    - "D-08 is authoritative: locale-specific prompt display with EN fallback."
    - "D-09/D-10/D-11/D-12 are authoritative: settings expose one default-ON global auto-send toggle and UI shows clear `Will auto-send` copy without blocking saves."
  artifacts:
    - path: "apps/web/features/rules/components/AvailableActionsPanel.tsx"
      provides: "User-facing list of supported/disabled actions with explanations."
    - path: "apps/web/features/rules/components/RuleComposer.tsx"
      provides: "Persona selector and example grid below existing prompt box."
    - path: "apps/web/app/(protected)/(app)/settings/SettingsClient.tsx"
      provides: "Global Auto-send rules toggle default ON."
---

<objective>
Wire the user-facing Phase 08.1 experience: persona examples inside the existing rule composer, an Available Actions panel, and a single global `Auto-send rules` setting. This plan intentionally does not introduce a new rule wizard.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONVENTIONS.md
@TESTING.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-CONTEXT.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-RESEARCH.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-02-SUMMARY.md
@apps/web/features/rules/components/RuleComposer.tsx
@apps/web/features/rules/components/RulesWorkspace.tsx
@apps/web/features/rules/lib/rule-structure.ts
@apps/web/app/(protected)/(app)/settings/SettingsClient.tsx
@../inbox-zero/apps/web/app/(app)/[emailAccountId]/assistant/AvailableActionsPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 08.1-04-01: Web catalog API hooks for examples/actions</name>
  <files>
    apps/web/features/rules/api/rule-catalog-api.ts,
    apps/web/features/rules/hooks/use-rule-examples.ts,
    apps/web/features/rules/hooks/use-rule-actions-catalog.ts,
    apps/web/features/rules/query-keys.ts
  </files>
  <behavior>
    - Use generated public OpenAPI schema types.
    - Query enabled examples and actions by locale.
    - Cache keys include locale.
    - No frontend hard-coded seed examples remain except loading/empty placeholders.
  </behavior>
  <action>
    Add a rules catalog API module and hooks using the generated public schema. Thread the active UI locale into the query keys and API calls, and remove the old hard-coded examples from data ownership so the composer receives examples through props/hooks.
  </action>
  <verify>
    <automated>pnpm --filter @zeromail/web typecheck</automated>
  </verify>
  <done>
    Rule composer can consume catalog data without mirror DTOs.
  </done>
  <acceptance_criteria>
    - API module imports generated `paths`/`components` types.
    - Hooks expose loading/error/data states.
    - Query keys include locale.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-04-02: Persona examples inside existing RuleComposer</name>
  <files>
    apps/web/features/rules/components/RuleComposer.tsx,
    apps/web/features/rules/components/RulesWorkspace.tsx,
    apps/web/messages/
  </files>
  <behavior>
    - Replace the three hard-coded suggestion chips with `Choose from examples`.
    - Render persona selector using the required 14 persona keys.
    - Render selected persona examples below the textarea.
    - Clicking an example calls `onSourceTextChange(prompt)` and replaces the textarea.
    - Keep existing Convert to rule, clarification, review, and manual tabs intact.
  </behavior>
  <action>
    Update `RulesWorkspace` to load catalog data and pass it into `RuleComposer`. Replace the old suggestion chip block with a compact persona selector plus responsive example grid inside the Describe tab, preserving existing compile/save/refine props and state flow. Add localized empty/loading/error copy through message files.
  </action>
  <verify>
    <automated>pnpm --filter @zeromail/web test:unit -- --run RuleComposer && pnpm --filter @zeromail/web typecheck</automated>
  </verify>
  <done>
    Existing composer flow is extended with DB-backed examples.
  </done>
  <acceptance_criteria>
    - No new rule creation page/wizard is introduced.
    - Persona selection changes the examples under the textarea.
    - Example click replaces textarea content exactly.
    - Loading and empty states are compact and do not shift layout.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-04-03: Available Actions panel and global auto-send setting UI</name>
  <files>
    apps/web/features/rules/components/AvailableActionsPanel.tsx,
    apps/web/features/rules/components/RulesWorkspace.tsx,
    apps/web/app/(protected)/(app)/settings/SettingsClient.tsx,
    apps/web/messages/
  </files>
  <behavior>
    - Available Actions panel lists Label, Archive, Save draft, Mark read/unread, Star/unstar, Add to digest, Mark spam, Send reply, Forward, Send email.
    - Disabled/unavailable actions have visible reasons from descriptor data.
    - Outbound actions show `Will auto-send` / Vietnamese equivalent when global toggle is ON.
    - Settings shows one `Auto-send rules` switch default ON; no per-action toggles.
    - If toggle is OFF, copy explains rules will save Gmail drafts instead of sending.
  </behavior>
  <action>
    Build `AvailableActionsPanel` from catalog descriptor data and place it near the rule composer/list where users review available automation. Extend the settings client to read/update the single global auto-send flag and remove/avoid any per-action toggle UI. Ensure outbound action copy changes based on the global setting but does not add a blocking acknowledgement flow.
  </action>
  <verify>
    <automated>pnpm --filter @zeromail/web test:unit -- --run AvailableActionsPanel && pnpm --filter @zeromail/web typecheck</automated>
  </verify>
  <done>
    User can understand available actions and adjust the global outbound switch with minimal friction.
  </done>
  <acceptance_criteria>
    - The action panel contains all required actions.
    - No copy says Zero Mail never auto-sends after this phase.
    - Only one auto-send rules setting exists in UI.
    - Toggling OFF persists and changes explanatory copy to draft fallback.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-04-04: Browser verification for user rule flow</name>
  <files>
    apps/web/e2e/rules-examples.spec.ts
  </files>
  <behavior>
    - Mock catalog endpoints with bilingual persona examples and action descriptors.
    - Walk rule composer: open, choose persona, click example, convert, review.
    - Walk settings: global auto-send toggle ON/OFF and copy changes.
  </behavior>
  <action>
    Add a Playwright spec that mocks the public catalog and settings endpoints, drives the composer at desktop and mobile viewport sizes, and verifies the settings toggle state and copy. Capture console errors and fail the test on unexpected frontend exceptions.
  </action>
  <verify>
    <automated>pnpm --filter @zeromail/web e2e -- --grep "rules examples"</automated>
  </verify>
  <done>
    Real browser covers the user-facing flow.
  </done>
  <acceptance_criteria>
    - Playwright sees examples below the existing source textarea.
    - Example click populates textarea and does not append.
    - No overlapping/truncated text at desktop and mobile viewport.
    - Browser console has no errors.
  </acceptance_criteria>
</task>

</tasks>

<verification>

```powershell
pnpm --filter @zeromail/web typecheck
pnpm --filter @zeromail/web test:unit -- --run RuleComposer
pnpm --filter @zeromail/web test:unit -- --run AvailableActionsPanel
pnpm --filter @zeromail/web e2e -- --grep "rules examples"
```

</verification>

<success_criteria>
- [ ] Existing RuleComposer remains the flow.
- [ ] Persona examples are DB-backed and locale-aware.
- [ ] Example click replaces textarea content.
- [ ] Available Actions panel lists all required actions and disabled reasons.
- [ ] Settings has one default-ON global auto-send rules toggle.
- [ ] Browser verification passes.
</success_criteria>

<output>
Create `.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-04-SUMMARY.md` when done.
</output>
