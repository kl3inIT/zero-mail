---
phase: 08-bulk-unsubscribe-campaign
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/gmail/usecases/GmailPreviewReadService.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/projection/UnsubscribeCandidateProjection.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/projection/SenderSuppressionProjection.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/projection/CampaignStatusProjection.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/projection/PerSenderAttemptProjection.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CandidateQueryService.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/usecases/SuppressionCrudService.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/usecases/SuppressionAutoAddService.java
autonomous: true
requirements: [UNS-01, UNS-02]
must_haves:
  truths:
    - "`GmailPreviewReadService` extract đầy đủ 3 trường: list_unsubscribe_url (HTTPS-only, D-11 parse-time guard), list_unsubscribe_mailto, list_unsubscribe_one_click flag"
    - "`CandidateQueryService.findCandidates(tenantId, window, limit)` trả candidate filter (list_unsubscribe_url IS NOT NULL OR list_unsubscribe_mailto IS NOT NULL) + anti-join sender_suppression"
    - "`SuppressionCrudService` CRUD endpoint underlying — add/remove/list với XOR sender_email vs sender_domain"
    - "`SuppressionAutoAddService` heuristic — sender đã reply ≥1 lần trong 90d → tự động add với reason=REPLIED"
    - "Wave 0 `CandidateQueryServiceTest` + `SuppressionServiceTest` flip GREEN"
  artifacts:
    - path: backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CandidateQueryService.java
      provides: "UNS-01 read-side query với JdbcTemplate @Transactional(readOnly=true)"
    - path: backend/core/src/main/java/com/zeromail/core/cleanup/usecases/SuppressionCrudService.java
      provides: "UNS-02 CRUD service"
    - path: backend/core/src/main/java/com/zeromail/core/gmail/usecases/GmailPreviewReadService.java
      provides: "Extended extraction: parse List-Unsubscribe header → 3 column persistence"
  key_links:
    - from: backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CandidateQueryService.java
      to: backend/core/src/main/java/com/zeromail/core/analytics/usecases/AnalyticsSummaryQueryService.java
      via: "Same pattern (@Service + @Transactional(readOnly=true) + JdbcTemplate + multi-SQL constants)"
      pattern: "JdbcTemplate"
    - from: backend/core/src/main/java/com/zeromail/core/gmail/usecases/GmailPreviewReadService.java
      to: backend/core/src/main/resources/db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml
      via: "Extracted values persist vào 3 column mới list_unsubscribe_url/mailto/one_click"
      pattern: "list_unsubscribe_url"
---

<objective>
Wave 2: Extract `List-Unsubscribe` URL/mailto/one-click flag từ Gmail headers + persist vào `mail_message_observed` (Liquibase 041). Add `CandidateQueryService` (read-side filter — UNS-01), `SuppressionCrudService` (UNS-02 CRUD), `SuppressionAutoAddService` (auto-add heuristic). 4 projection record.

Purpose: Unlock candidate list endpoint cho `GET /api/unsubscribe/candidates`. Wave 0 tests CandidateQueryServiceTest + SuppressionServiceTest flip GREEN. Wave 3 ingest pipeline tự động bắt đầu thu data cho `mail_message_observed.list_unsubscribe_*`.
Output: 8 file (1 modify + 7 new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-SPEC.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-PATTERNS.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-RESEARCH.md
@backend/core/src/main/java/com/zeromail/core/gmail/usecases/GmailPreviewReadService.java
@backend/core/src/main/java/com/zeromail/core/analytics/usecases/AnalyticsSummaryQueryService.java
@backend/core/src/main/java/com/zeromail/core/analytics/projection/TopSenderProjection.java
@backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java
@backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SenderSuppressionRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GmailPreviewReadService — extract list_unsubscribe_* fields + persist</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/gmail/usecases/GmailPreviewReadService.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/gmail/usecases/GmailPreviewReadService.java,
    backend/core/src/main/resources/db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml,
    .planning/phases/08-bulk-unsubscribe-campaign/08-RESEARCH.md
  </read_first>
  <action>
    Modify `GmailPreviewReadService.java`. Locate line ~515 (current `listUnsubscribePresent` extraction điểm — đã có sẵn).

    Required changes:
    1. Add 3 local variable trong message-row mapping: `String listUnsubscribeUrl`, `String listUnsubscribeMailto`, `boolean listUnsubscribeOneClick`.
    2. Parse Gmail header `List-Unsubscribe` value (format: `<https://...>, <mailto:...>`):
       - Iterate comma-separated values bọc trong `<>`.
       - Strip `<` và `>`.
       - For each URI:
         - If `startsWith("https://")` → set `listUnsubscribeUrl` (only first HTTPS match; D-11 parse-time guard — drop `http://` silently).
         - Else if `startsWith("mailto:")` → set `listUnsubscribeMailto` (keep raw value including `?subject=...&body=...` params).
         - Else (e.g., `http://`) → drop, log NO sentinel (privacy invariant — log only domain via `extractDomainFromUri(uri)` helper).
    3. Parse Gmail header `List-Unsubscribe-Post` (format `"List-Unsubscribe=One-Click"`):
       - Set `listUnsubscribeOneClick = (postHeaderValue != null && postHeaderValue.equalsIgnoreCase("List-Unsubscribe=One-Click") && listUnsubscribeUrl != null)`.
       - One-click flag CHỈ true khi cả URL HTTPS + post header present (RESEARCH §"RFC 2369 — List-Unsubscribe Header Format" + RFC 8058 trigger pair).
    4. Update insert/upsert SQL trong cùng class (locate persist statement cho `mail_message_observed`) — add 3 column với binding:
       - `list_unsubscribe_url = ?`
       - `list_unsubscribe_mailto = ?`
       - `list_unsubscribe_one_click = ?`
       Verify schema column count match — Liquibase 041 đã ship 3 cột với defaults NULL/false (forward-only D-10).
    5. Logging: privacy invariant. Log line `event=gmail_preview_list_unsubscribe_extracted tenantId={} hasUrl={} hasMailto={} oneClick={}` — KHÔNG log url value, KHÔNG log mailto value, KHÔNG log sender email full. Boolean only.
    6. Boundary check: nếu `List-Unsubscribe` header absent → cả 3 column = NULL/false (giữ behavior cũ + D-10 forward-only).

    KHÔNG move `listUnsubscribePresent` boolean field (giữ tương thích phase 2A — `presentFlag = (listUnsubscribeUrl != null || listUnsubscribeMailto != null)`).

    KHÔNG dùng regex để parse — dùng `java.net.URI.create(value)` cho mailto parsing (D-23). Cho HTTPS URI: simple `startsWith("https://")` + length cap 2048 (match VARCHAR(2048)).

    Naming: full identifiers `listUnsubscribeUrl`, `listUnsubscribeMailto`, `listUnsubscribeOneClick`, `postHeaderValue`, `extractDomainFromUri`. KHÔNG `url/uri/ex/cfg`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*GmailPreviewReadServiceTest*" --tests "*GmailReadServiceIntegrationTest*"</automated>
  </verify>
  <acceptance_criteria>
    - `GmailPreviewReadService.java` chứa identifier `listUnsubscribeUrl`, `listUnsubscribeMailto`, `listUnsubscribeOneClick`.
    - File chứa literal `"https://"` (HTTPS-only check, D-11).
    - File chứa literal `"List-Unsubscribe=One-Click"` (RFC 8058 trigger pair).
    - File `event=gmail_preview_list_unsubscribe_extracted` log line — tìm bằng grep.
    - Insert/upsert SQL contains `list_unsubscribe_url`, `list_unsubscribe_mailto`, `list_unsubscribe_one_click` exactly.
    - Existing Wave 2A test `GmailPreviewReadServiceTest` exits 0 (no regression).
    - New extraction logic: fixture với header `List-Unsubscribe: <https://example.com/u/a>, <mailto:unsub@example.com?subject=u>` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click` → persisted row có `list_unsubscribe_url='https://example.com/u/a'`, `list_unsubscribe_mailto='mailto:unsub@example.com?subject=u'`, `list_unsubscribe_one_click=true`.
    - Fixture với header `http://...` only → cả 3 column NULL/false (drop http silently).
    - `./gradlew :backend:core:test --tests "*GmailPreviewReadServiceTest*"` exits 0.
    - Privacy grep: `grep -nE "log\.(info|debug|warn|error).*listUnsubscribeUrl|listUnsubscribeMailto" backend/core/src/main/java/com/zeromail/core/gmail/usecases/GmailPreviewReadService.java` trả 0 match (chỉ boolean flag, KHÔNG raw URL/mailto trong log).
  </acceptance_criteria>
  <done>
    GmailPreviewReadService extract + persist 3 trường mới. No regression. D-11 parse-time guard enforced. Privacy invariant pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: 4 projection records + CandidateQueryService (UNS-01)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/UnsubscribeCandidateProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/SenderSuppressionProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/CampaignStatusProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/PerSenderAttemptProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CandidateQueryService.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/analytics/usecases/AnalyticsSummaryQueryService.java,
    backend/core/src/main/java/com/zeromail/core/analytics/projection/TopSenderProjection.java,
    backend/core/src/main/java/com/zeromail/core/triage/projection/AuditLogRow.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeMethod.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeAttemptState.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/CampaignStatus.java,
    backend/core/src/main/resources/db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml,
    backend/core/src/main/resources/db/changelog/changes/043-sender-suppression.yaml,
    .planning/phases/08-bulk-unsubscribe-campaign/08-PATTERNS.md
  </read_first>
  <action>
    Tạo 4 projection record + `CandidateQueryService`.

    1. **`UnsubscribeCandidateProjection`** (record):
       ```
       public record UnsubscribeCandidateProjection(
           String senderEmail,
           String senderDomain,
           long messageCount,
           Instant lastSeenAt,
           UnsubscribeMethod unsubscribeMethod,
           boolean suppressed
       ) {}
       ```
       (Java 25 record — full identifier names, no abbreviation. `suppressed` always `false` cho candidate output — anti-join đã loại trong query. Vẫn giữ field cho future use khi enrichment query.)
    2. **`SenderSuppressionProjection`** (record):
       ```
       public record SenderSuppressionProjection(
           UUID id,
           String senderEmail,
           String senderDomain,
           SuppressionReason reason,
           Instant createdAt
       ) {}
       ```
       (`senderEmail` OR `senderDomain` non-null XOR.)
    3. **`CampaignStatusProjection`** (record):
       ```
       public record CampaignStatusProjection(
           UUID id,
           UUID jobId,
           CampaignStatus status,
           Instant appliedAt,
           Instant revertedAt,
           int totalSenderCount,
           int totalHistoryMessageCount,
           Instant createdAt,
           List<PerSenderAttemptProjection> perSender
       ) {
           public CampaignStatusProjection {
               perSender = List.copyOf(perSender);
           }
       }
       ```
    4. **`PerSenderAttemptProjection`** (record):
       ```
       public record PerSenderAttemptProjection(
           String senderEmail,
           String senderDomain,
           UnsubscribeMethod unsubscribeMethod,
           UnsubscribeAttemptState state,
           String failureReason,
           int archivedMessageCount,
           Instant startedAt,
           Instant finishedAt
       ) {}
       ```

    5. **`CandidateQueryService`** (UNS-01, D-21 — `core.cleanup.usecases` package, KHÔNG share `AnalyticsSummaryQueryService`):
       - `@Service`, `@Transactional(readOnly = true)` ở class level.
       - Constructor inject `JdbcTemplate jdbcTemplate`, `Clock clock`.
       - Method `public List<UnsubscribeCandidateProjection> findCandidates(UUID tenantId, Duration window, int limit)`:
         - SQL constant `CANDIDATE_SQL`:
           ```sql
           SELECT
               mmo.sender_email,
               split_part(mmo.sender_email, '@', 2) AS sender_domain,
               COUNT(*) AS message_count,
               MAX(mmo.observed_at) AS last_seen_at,
               CASE
                   WHEN BOOL_OR(mmo.list_unsubscribe_one_click) THEN 'ONE_CLICK'
                   WHEN BOOL_OR(mmo.list_unsubscribe_mailto IS NOT NULL) THEN 'MAILTO'
                   ELSE 'NONE'
               END AS unsubscribe_method
           FROM mail_message_observed mmo
           WHERE mmo.tenant_id = ?
             AND mmo.observed_at >= ?
             AND mmo.observed_at < ?
             AND mmo.sender_email IS NOT NULL
             AND (mmo.list_unsubscribe_url IS NOT NULL OR mmo.list_unsubscribe_mailto IS NOT NULL)
             AND NOT EXISTS (
                 SELECT 1 FROM sender_suppression ss
                 WHERE ss.tenant_id = mmo.tenant_id
                   AND (
                       ss.sender_email = mmo.sender_email
                       OR ss.sender_domain = split_part(mmo.sender_email, '@', 2)
                   )
             )
           GROUP BY mmo.sender_email
           ORDER BY message_count DESC, mmo.sender_email ASC
           LIMIT ?
           ```
         - Bind: `tenantId`, `now.minus(window)`, `now`, `limit`.
         - Use `jdbcTemplate.query(...)` với `RowMapper<UnsubscribeCandidateProjection>` mapping resultSet → record. `UnsubscribeMethod method = UnsubscribeMethod.fromId(resultSet.getString("unsubscribe_method"))`.
         - Log: `log.info("event=cleanup_candidates_queried tenantId={} window={} limit={} count={}", tenantId, window, limit, results.size())` — KHÔNG log sender list.

    Anti-join via `NOT EXISTS` (PostgreSQL efficient với index `ux_sender_suppression_email` + `ux_sender_suppression_domain`).

    Limit hard-capped `Math.min(limit, MAX_SENDERS_PER_CAMPAIGN)` từ `UnsubscribeCampaignPolicy.MAX_SENDERS_PER_CAMPAIGN` (25 per SPEC.md UNS-01).

    Naming: `tenantId`, `window`, `limit`, `jdbcTemplate`, `resultSet`, `senderEmail`, `senderDomain`, `messageCount`, `lastSeenAt`, `unsubscribeMethod`. KHÔNG `rs/cfg/svc/ctx`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*CandidateQueryServiceTest*"</automated>
  </verify>
  <acceptance_criteria>
    - 4 projection record file tồn tại với `public record` declaration.
    - `UnsubscribeCandidateProjection` chứa 6 component (senderEmail, senderDomain, messageCount, lastSeenAt, unsubscribeMethod, suppressed).
    - `CampaignStatusProjection` chứa compact constructor `perSender = List.copyOf(perSender);`.
    - `CandidateQueryService.java` chứa `@Service` + `@Transactional(readOnly = true)` annotation.
    - File chứa SQL literal substring `NOT EXISTS` + `sender_suppression` + `list_unsubscribe_url IS NOT NULL OR mmo.list_unsubscribe_mailto IS NOT NULL` (anti-join + filter).
    - File chứa literal `BOOL_OR` (aggregate cho one-click detection per-sender).
    - Log line `event=cleanup_candidates_queried` exists (privacy-safe — chỉ count, no sender list).
    - `./gradlew :backend:core:test --tests "*CandidateQueryServiceTest*"` exits 0 (Wave 0 stub flip GREEN). Acceptance: 4 test method pass:
      - `returnsCandidatesWithOneClickAndMailto`: 2 candidate trả về.
      - `excludesSenderWithoutListUnsubscribeHeader`: third sender absent.
      - `excludesSenderInSuppressionList`: suppressed sender absent.
      - `excludesSuppressedDomain`: domain match absent.
    - `grep -E "log\.(info|debug|warn).*senderEmail|sender_email.*log" backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CandidateQueryService.java` trả 0 (no PII in logs).
  </acceptance_criteria>
  <done>
    4 projection record + CandidateQueryService lands. UNS-01 contract satisfied. Wave 0 CandidateQueryServiceTest GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: SuppressionCrudService + SuppressionAutoAddService (UNS-02)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/SuppressionCrudService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/SuppressionAutoAddService.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SenderSuppressionEntity.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SenderSuppressionRepository.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/SenderSuppressionProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/SuppressionReason.java,
    backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java,
    backend/core/src/main/resources/db/changelog/changes/043-sender-suppression.yaml,
    .planning/phases/08-bulk-unsubscribe-campaign/08-SPEC.md
  </read_first>
  <action>
    Tạo 2 file service:

    1. **`SuppressionCrudService`** (UNS-02 CRUD):
       - `@Service`, `@Transactional` ở class level.
       - Constructor inject `SenderSuppressionRepository senderSuppressionRepository`, `Clock clock`.
       - Methods:
         - `public SenderSuppressionProjection addManual(UUID tenantId, String senderEmailOrDomain)`:
           - Validate input non-empty + length ≤ 320.
           - Determine if input has `@` → it's sender_email; otherwise it's sender_domain. (Basic heuristic — fronted validates format too via Wave 5b form.)
           - Check existing entry (idempotency): `findByTenantIdAndSenderEmail` HOẶC `findByTenantIdAndSenderDomain` — nếu tồn tại → throw `IllegalArgumentException("Duplicate suppression entry")` (controller maps 409).
           - Persist new `SenderSuppressionEntity(tenantId, senderEmail, null, SuppressionReason.MANUAL)` HOẶC `(tenantId, null, senderDomain, SuppressionReason.MANUAL)`.
           - Log: `log.info("event=cleanup_suppression_added tenantId={} reason={} target={}", tenantId, "manual", senderEmailOrDomain.contains("@") ? "email" : "domain")` — KHÔNG log raw value.
           - Return `SenderSuppressionProjection.from(entity)` (add static factory in projection file Task 2 nếu cần — OR map inline ở đây).
         - `public List<SenderSuppressionProjection> list(UUID tenantId)`:
           - `senderSuppressionRepository.findByTenantIdOrderByCreatedAtDesc(tenantId)` → map to projection list.
         - `public void remove(UUID tenantId, UUID suppressionId)`:
           - `findByIdAndTenantId(suppressionId, tenantId).orElseThrow(NoSuchElementException::new)` → delete.
           - Log: `event=cleanup_suppression_removed tenantId={} suppressionId={}`.

    2. **`SuppressionAutoAddService`** (UNS-02 heuristic: reply ≥1/90d):
       - `@Service`, `@Transactional`.
       - Constructor inject `SenderSuppressionRepository senderSuppressionRepository`, `JdbcTemplate jdbcTemplate`, `Clock clock`.
       - Method `public int scanAndAutoAdd(UUID tenantId)`:
         - SQL: find sender_email mà user đã reply (`triage_audit` rows với `decision = 'APPLIED'` AND `action = 'SAVE_DRAFT'` followed by Gmail send-as-self trong 90d cuối). RESEARCH §"Specifics > Auto-add suppression heuristic" ghi rõ pattern: `mail_message_observed.label_ids` chứa `SENT` label + sender_email = current user's Gmail address.
         - **SIMPLIFIED HEURISTIC for v1**: pull sender_email từ những `mail_message_observed` row có `'SENT' = ANY(label_ids)` trong 90d cuối (= user sent message). Đối với mỗi row đó, extract `to_recipient` (HÌNH NHƯ schema chưa có cột này). FALLBACK: pull sender_email từ `triage_audit` với `action = 'SAVE_DRAFT'` AND `decision = 'APPLIED'` trong 90d. Documented ASSUMPTION inline trong service Javadoc.
         - **PRACTICAL SQL** (use what schema has — `triage_audit` actions):
           ```sql
           SELECT DISTINCT mmo.sender_email
           FROM triage_audit ta
           JOIN mail_message_observed mmo ON mmo.gmail_message_id = ta.gmail_message_id
                                          AND mmo.tenant_id = ta.tenant_id
           WHERE ta.tenant_id = ?
             AND ta.action = 'SAVE_DRAFT'
             AND ta.decision = 'APPLIED'
             AND ta.decided_at >= ?
             AND mmo.sender_email IS NOT NULL
             AND NOT EXISTS (
                 SELECT 1 FROM sender_suppression ss
                 WHERE ss.tenant_id = ta.tenant_id
                   AND ss.sender_email = mmo.sender_email
             )
           ```
         - Cho mỗi `senderEmail` result → insert `SenderSuppressionEntity(tenantId, senderEmail, null, SuppressionReason.REPLIED)`.
         - Log: `event=cleanup_suppression_auto_added tenantId={} count={} reason=replied`.
         - Return inserted count.
       - **Trigger**: Wave 5a controller hoặc Wave 4 worker batch sẽ call. Method bare service — no scheduling annotation in this file.

    Naming hygiene strict. Banned abbreviations check.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*SuppressionServiceTest*"</automated>
  </verify>
  <acceptance_criteria>
    - File `SuppressionCrudService.java` chứa 3 method: `addManual`, `list`, `remove` (grep method signatures).
    - File chứa `SuppressionReason.MANUAL` literal usage.
    - File chứa log line `event=cleanup_suppression_added` + `event=cleanup_suppression_removed`.
    - File `SuppressionAutoAddService.java` chứa `SuppressionReason.REPLIED`.
    - File chứa SQL với `triage_audit` join + `decision = 'APPLIED'` + `action = 'SAVE_DRAFT'`.
    - File chứa duration `90` (days) — implicit qua `Duration.ofDays(90)` HOẶC literal `INTERVAL '90 days'`.
    - File chứa `NOT EXISTS` clause to avoid duplicate insert.
    - `./gradlew :backend:core:test --tests "*SuppressionServiceTest*"` exits 0 — 6 test method từ Wave 0:
      - `addSenderEmail_isPersisted`
      - `addSenderDomain_isPersisted`
      - `add_rejectsBothNull`
      - `add_rejectsBothNonNull` (XOR enforced trong entity constructor)
      - `remove_byId_softCheckRowAbsent`
      - `autoAddAfterUserRepliedOnce_within90d_appearsAsRepliedReason`
    - `grep -E "log\.(info|warn).*senderEmail" backend/core/src/main/java/com/zeromail/core/cleanup/usecases/Suppression*.java` không có raw email — chỉ `target="email"` hoặc count.
  </acceptance_criteria>
  <done>
    SuppressionCrudService + SuppressionAutoAddService lands. UNS-02 contract satisfied. Wave 0 SuppressionServiceTest GREEN.
  </done>
</task>

</tasks>

<verification>
- 8 file (1 modify + 7 new) lands.
- Wave 0 test `CandidateQueryServiceTest` flip GREEN.
- Wave 0 test `SuppressionServiceTest` flip GREEN.
- Existing Phase 2A Gmail preview tests no regression.
- Privacy invariant pass — log lines KHÔNG chứa raw URL/email/mailto.
</verification>

<success_criteria>
- [ ] `GmailPreviewReadService` extracts + persists 3 column mới với D-11 HTTPS-only guard.
- [ ] `CandidateQueryService` SQL với anti-join + BOOL_OR (one-click detection) ships.
- [ ] `SuppressionCrudService` 3 method (add/list/remove) ships.
- [ ] `SuppressionAutoAddService` heuristic 90d ships.
- [ ] 4 projection record (`UnsubscribeCandidateProjection` + 3 status-related) lands.
- [ ] Wave 0 tests flip GREEN: `CandidateQueryServiceTest` + `SuppressionServiceTest`.
- [ ] Privacy invariant — no PII in log lines từ Wave 2 services.
</success_criteria>

<output>
Tạo `.planning/phases/08-bulk-unsubscribe-campaign/08-04-SUMMARY.md` khi done.
</output>
