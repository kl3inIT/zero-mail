---
phase: 10-gmail-mailbox-foundation-and-account-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/src/main/resources/db/changelog/changes/119-gmail-connections-multi-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java
  - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java
autonomous: true
requirements: [WSP-02, WSP-03, GMA-03, GMA-06, VER-01]
must_haves:
  truths:
    - "Migration 119 drops the one-Gmail-per-tenant unique constraint and adds duplicate-active + at-most-one-primary partial unique indexes (a partial unique index enforces AT MOST one primary, not exactly one; the service/backfill is what creates one when a CONNECTED mailbox exists)"
    - "An existing single-Gmail tenant is backfilled to exactly one primary mailbox with byte-identical encrypted tokens"
    - "The entity exposes is_primary + a display label; the repository can resolve a connection by (id, tenantId) for ownership"
    - "Two CONNECTED mailboxes with different emails coexist for one tenant; a duplicate active email is rejected at the DB"
  artifacts:
    - path: "backend/core/src/main/resources/db/changelog/changes/119-gmail-connections-multi-mailbox.yaml"
      provides: "Raw-SQL changeset: drop uq_gmail_connections_tenant_id, add uq_gmail_conn_active_email + is_primary + uq_gmail_conn_primary + label, with preConditions + rollback"
      contains: "119-gmail-connections-multi-mailbox"
    - path: "backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java"
      provides: "is_primary boolean column + label/display_purpose column with getters/setters"
      contains: "is_primary"
    - path: "backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java"
      provides: "findByIdAndTenantId + tenant-ordered list query"
      contains: "findByIdAndTenantId"
  key_links:
    - from: "db.changelog-master.yaml"
      to: "changes/119-gmail-connections-multi-mailbox.yaml"
      via: "include"
      pattern: "119-gmail-connections-multi-mailbox"
    - from: "GmailConnectionEntity"
      to: "gmail_connections.is_primary"
      via: "@Column(name = \"is_primary\")"
      pattern: "is_primary"
---

<objective>
Break the one-Gmail-per-tenant invariant at the schema + entity + repository tier. Ship Liquibase changeset 119 (raw SQL, paired rollback, preConditions dedupe guard) that drops `uq_gmail_connections_tenant_id`, adds the duplicate-active partial unique index, the `is_primary` marker (backfilled deterministically) + exactly-one-primary partial unique, and a display label column. Add the entity fields and the `findByIdAndTenantId` ownership lookup + tenant-ordered list query the rest of the phase builds on. Tokens/scopes/watch columns are NEVER read or written — ciphertext stays byte-identical (D-00b / D-11).

Purpose: This is the migration foundation (D-07, D-09, WSP-02/03, GMA-03/06, VER-01). Every later plan (factory, service seam, OAuth split, controllers) depends on these columns and this lookup existing.
Output: 1 new changeset + master include + entity fields + 2 repository methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-RESEARCH.md
@.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
NEW symbols this plan creates (consumed by Plans 03-06):
- Changeset id `119-gmail-connections-multi-mailbox` (author `zeromail`)
- Indexes: `uq_gmail_conn_active_email` (partial unique on `(tenant_id, lower(google_email)) WHERE status='CONNECTED'`), `uq_gmail_conn_primary` (partial unique on `(tenant_id) WHERE is_primary=true`)
- Dropped constraint: `uq_gmail_connections_tenant_id`
- Columns: `is_primary boolean NOT NULL DEFAULT false`, `display_purpose varchar` (the display label per D-07 fold)
- Entity fields: `GmailConnectionEntity.isPrimary` (+ `isPrimary()`/`setPrimary(...)`), `GmailConnectionEntity.displayPurpose` (+ getter/setter)
- Repository methods: `GmailConnectionRepository.findByIdAndTenantId(UUID id, UUID tenantId)`, `findByTenantIdOrderByIsPrimaryDesc(UUID tenantId)`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Liquibase changeset 119 (raw SQL + preConditions + rollback) + master include</name>
  <files>backend/core/src/main/resources/db/changelog/changes/119-gmail-connections-multi-mailbox.yaml, backend/core/src/main/resources/db/changelog/db.changelog-master.yaml</files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/changes/042-chat-message-and-body-ban-trigger.yaml (EXACT analog: single changeSet, `sql:` with `splitStatements: false`, paired `rollback: sql:`, author `zeromail`, "single changeSet by design" comment)
    - backend/core/src/main/resources/db/changelog/changes/003-create-gmail-connections.yaml (the drop target: `uq_gmail_connections_tenant_id` is an `addUniqueConstraint` at lines 17-20 — a CONSTRAINT, not a bare index)
    - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml (append-only include block; last entry is 118)
    - .planning/phases/10-gmail-mailbox-foundation-and-account-management/10-RESEARCH.md (§ Liquibase Migration — Changeset 119, lines 208-283: the exact ordered SQL + preConditions sqlCheck + DISTINCT ON backfill + rollback)
    - CLAUDE.md (Conventions §10 Liquibase discipline: append-only, immutable applied changesets, raw `sql:` with explicit rollback, `preConditions onFail` for data-dependent DDL)
  </read_first>
  <action>
    Create `119-gmail-connections-multi-mailbox.yaml` as a SINGLE changeSet (id `119-gmail-connections-multi-mailbox`, author `zeromail`) with the "single changeSet by design — atomic, no window where a duplicate active email can slip in" comment mirroring 042. Use the exact ordered raw SQL from RESEARCH lines 216-275 with `splitStatements: false`:
    1. `ALTER TABLE gmail_connections DROP CONSTRAINT uq_gmail_connections_tenant_id;` (DROP CONSTRAINT, NOT `DROP INDEX` — it is an addUniqueConstraint per 003, RESEARCH Pitfall 3).
    2. `CREATE UNIQUE INDEX uq_gmail_conn_active_email ON gmail_connections (tenant_id, lower(google_email)) WHERE status = 'CONNECTED';`
    3. `ALTER TABLE gmail_connections ADD COLUMN is_primary boolean NOT NULL DEFAULT false;`
    4. `ALTER TABLE gmail_connections ADD COLUMN display_purpose varchar;` (the D-07 display label, folded here so entity+migration land atomically per RESEARCH line 277 / A2).
    5. The `DISTINCT ON (tenant_id)` backfill `UPDATE ... SET is_primary = true` ordered `(status='CONNECTED') DESC, connected_at NULLS LAST, id` (RESEARCH lines 256-261) — deterministic single primary per tenant.
    6. `CREATE UNIQUE INDEX uq_gmail_conn_primary ON gmail_connections (tenant_id) WHERE is_primary = true;`
    Add the `preConditions` block with `onFail: HALT` + `onFailMessage` + the `sqlCheck expectedResult: 0` dedupe guard from RESEARCH lines 228-240 (no tenant has >1 CONNECTED row for the same `lower(email)`). **REVIEW FIX (codex HIGH #1): this MUST be `onFail: HALT`, NOT `MARK_RAN`.** Changeset 119 is required structural DDL — it adds the `NOT NULL` `is_primary` column, the `display_purpose` column, and the two partial unique indexes that Task 2's entity maps with `nullable = false`. With `MARK_RAN`, a failed dedupe precondition would record changeset 119 as applied in `DATABASECHANGELOG` while none of those columns/indexes exist, so the next application boot starts against a missing-column schema (Hibernate schema validation failure / runtime insert failure on `is_primary`). `HALT` instead stops the deployment loudly with the `onFailMessage`, the operator dedupes the offending legacy CONNECTED rows, then re-runs Liquibase — the changeset never falsely marks itself applied. Set `onFailMessage` to a clear operator instruction (e.g. "Phase 10 migration halted: a tenant has more than one CONNECTED gmail_connections row for the same lowercased email. Dedupe the legacy duplicates, then re-run the migration."). Document the halt-dedupe-then-rerun semantics in the changeset comment.
    Add the paired `rollback: sql:` block from RESEARCH lines 266-275: drop `uq_gmail_conn_primary`, drop column `is_primary`, drop column `display_purpose`, drop `uq_gmail_conn_active_email`, then `ADD CONSTRAINT uq_gmail_connections_tenant_id UNIQUE (tenant_id)`.
    Append the include for `changes/119-gmail-connections-multi-mailbox.yaml` to `db.changelog-master.yaml` after the 118 entry (`relativeToChangelogFile: true`).
    Tokens/scopes/watch columns are NEVER referenced by this changeset (byte-identical guarantee, D-00b/D-11). Downstream tables are NOT touched (D-00a — no nullable mailbox columns anywhere else).
  </action>
  <verify>
    <automated>cd backend && ./gradlew :backend:core:test --tests "*Migration119*" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `119-gmail-connections-multi-mailbox.yaml` exists; grep finds `DROP CONSTRAINT uq_gmail_connections_tenant_id` (NOT `DROP INDEX uq_gmail_connections_tenant_id`).
    - grep finds all three: `uq_gmail_conn_active_email`, `uq_gmail_conn_primary`, `is_primary`, plus `display_purpose`.
    - The changeset has a `preConditions` with `onFail: HALT` (NOT `MARK_RAN`) plus an `onFailMessage` and a `sqlCheck`, and a non-empty `rollback:` block. grep confirms `onFail: HALT` and the absence of `MARK_RAN` in this changeset.
    - `db.changelog-master.yaml` includes `changes/119-gmail-connections-multi-mailbox.yaml` exactly once, appended after 118.
    - `Migration119Test` (Plan 01) goes GREEN: backfill = exactly one primary, ciphertext byte-identical.
    - grep confirms the changeset SQL never names `refresh_token_encrypted`, `scopes_granted`, or any `watch_*` column.
  </acceptance_criteria>
  <done>Migration 119 applies on a real Postgres (Testcontainers) against an old-single-account fixture, backfills exactly one primary, preserves ciphertext byte-identical, and rolls forward; the dedupe preCondition guards legacy data.</done>
</task>

<task type="auto">
  <name>Task 2: GmailConnectionEntity is_primary + display_purpose fields</name>
  <files>backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java (the `@Column` + getter/setter pattern at lines 18-23, 54-59, 118-160; entity extends AbstractTenantOwnedEntity, stays a class, Lombok-free)
    - .planning/phases/10-gmail-mailbox-foundation-and-account-management/10-PATTERNS.md (§ GmailConnectionEntity, lines 143-153: the exact field shape)
    - CLAUDE.md (Conventions §3: entities are classes not records; no Lombok; enterprise naming)
  </read_first>
  <action>
    Add to `GmailConnectionEntity`: a `@Column(name = "is_primary", nullable = false) private boolean isPrimary = false;` with `isPrimary()` getter and `setPrimary(boolean)` setter; and a `@Column(name = "display_purpose") private String displayPurpose;` with `getDisplayPurpose()`/`setDisplayPurpose(String)`. Use full enterprise names (no abbreviations). Do not touch the constructor signature or the existing token/watch fields.
  </action>
  <verify>
    <automated>cd backend && ./gradlew :backend:core:compileJava 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `GmailConnectionEntity` declares `isPrimary` (mapped to column `is_primary`, `nullable = false`, default `false`) with `isPrimary()`/`setPrimary(boolean)`.
    - `GmailConnectionEntity` declares `displayPurpose` (mapped to `display_purpose`) with getter/setter.
    - `./gradlew :backend:core:compileJava` is GREEN; entity remains a `class`, no Lombok, no abbreviations.
  </acceptance_criteria>
  <done>The entity maps the two new columns from changeset 119; later plans can read/write `isPrimary` and `displayPurpose`.</done>
</task>

<task type="auto">
  <name>Task 3: GmailConnectionRepository ownership lookup + tenant-ordered list query</name>
  <files>backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java (derived-query style at line 14-16; native-query methods at lines 18-39 do NOT inherit @TenantId filtering)
    - .planning/phases/10-gmail-mailbox-foundation-and-account-management/10-PATTERNS.md (§ GmailConnectionRepository, lines 129-139: `findByIdAndTenantId` + `findByTenantIdOrderByIsPrimaryDesc`)
    - .planning/phases/10-gmail-mailbox-foundation-and-account-management/10-RESEARCH.md (§ D-04 ownership lookup; D-09 note native SQL must include tenant_id explicitly)
  </read_first>
  <action>
    Add two derived-query methods to `GmailConnectionRepository`:
    - `Optional<GmailConnectionEntity> findByIdAndTenantId(UUID id, UUID tenantId);` — the D-04 ownership lookup the service seam uses (fail-closed: empty ⇒ caller throws 404).
    - `List<GmailConnectionEntity> findByTenantIdOrderByIsPrimaryDesc(UUID tenantId);` — the GMA-02 list endpoint source (primary first). Use derived-query naming (no `@Query` needed); both inherit Hibernate `@TenantId` filtering since they are JPQL derived queries, but the method bodies still pass `tenantId` explicitly so they are tenant-safe regardless.
    Do not add native SQL here; any future set-primary native UPDATE (Plan 04) will include `tenant_id` explicitly per D-09.
  </action>
  <verify>
    <automated>cd backend && ./gradlew :backend:core:compileJava 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `GmailConnectionRepository` declares `findByIdAndTenantId(UUID id, UUID tenantId)` returning `Optional<GmailConnectionEntity>`.
    - `GmailConnectionRepository` declares `findByTenantIdOrderByIsPrimaryDesc(UUID tenantId)` returning `List<GmailConnectionEntity>`.
    - `./gradlew :backend:core:compileJava` GREEN; both are derived queries (no hand-written native SQL added in this task).
  </acceptance_criteria>
  <done>The ownership lookup and the list-by-tenant query exist; Plan 04's seam and Plan 06's list endpoint can compile against them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → existing production data | Changeset 119 mutates the live `gmail_connections` table; legacy duplicate data could abort index creation |
| repository derived query → tenant isolation | A lookup that forgets `tenantId` would allow cross-tenant connection reads (IDOR) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | duplicate-active mailbox race | mitigate | `uq_gmail_conn_active_email` partial unique index is DB-enforced and race-proof (RESEARCH Don't Hand-Roll); app-level pre-check is only for the friendly message (Plan 04) |
| T-10-02-02 | Information Disclosure | byte-identical token preservation | mitigate | Changeset never reads/writes `refresh_token_encrypted`/`scopes_granted`/`watch_*`; AAD stays `tenantId` (D-11); `RefreshTokenCipherContinuityTest` (Plan 01) proves it |
| T-10-02-03 | Elevation of Privilege | cross-tenant connection lookup | mitigate | `findByIdAndTenantId` requires both ids; later seam fails closed on not-owned (Plan 04) |
| T-10-02-04 | Availability / Integrity | migration aborts on legacy dupe | mitigate | `preConditions onFail=HALT` + `onFailMessage` + sqlCheck: on legacy duplicate CONNECTED rows the changeset HALTS loudly (it is NEVER recorded as applied), the operator dedupes, then re-runs. `MARK_RAN` is explicitly rejected here (codex HIGH #1) because marking required structural DDL as applied-without-applying boots the app against a missing `is_primary`/`display_purpose` schema |
| T-10-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero new deps) |
</threat_model>

<verification>
- `./gradlew :backend:core:test --tests "*Migration119*" --tests "*DuplicateActiveEmail*" --tests "*SetPrimary*" --tests "*RefreshTokenCipherContinuity*"` — the Plan 01 Testcontainers scaffolds turn GREEN against the new schema (except set-primary's transactional behavior, which is Plan 04).
- `./gradlew :backend:core:compileJava` GREEN after entity + repository changes.
</verification>

<success_criteria>
- Changeset 119 exists, is included from master, drops the constraint (not an index), adds both partial unique indexes + `is_primary` + `display_purpose`, backfills one primary, and has preConditions + rollback.
- Entity maps the two new columns; repository exposes the ownership + list queries.
- `Migration119Test`, `DuplicateActiveEmailTest` (coexistence + duplicate rejection), `RefreshTokenCipherContinuityTest` GREEN.
- No downstream table touched; no token/scope/watch column referenced by the migration.
</success_criteria>

<output>
Create `.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-02-SUMMARY.md` when done, recording the verified changeset number, the exact index/constraint names, and which Plan 01 tests flipped green.
</output>
