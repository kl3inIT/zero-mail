---
phase: quick-260522-37g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/LlmRouter.java
  - backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultTierService.java
  - backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultProviderService.java
  - backend/core/src/main/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminService.java
  - backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/admin/ProviderMasterKeyResolver.java
  - backend/core/src/main/java/com/zeromail/core/admin/cat/persistence/lowlevel/ProviderCatalogLookupRepository.java
  - apps/admin/src/routes/_authenticated/master-keys.index.tsx
  - apps/admin/src/routes/_authenticated/master-keys.$provider.tsx
  - apps/admin/src/components/RoutingMatrixCard.tsx
  - apps/admin/src/components/TierPickerDialog.tsx
  - apps/admin/src/components/AddProviderKeyDialog.tsx
  - apps/admin/src/components/EditProviderKeyDialog.tsx
  - apps/admin/src/components/AddCatalogModelDialog.tsx
  - apps/admin/src/features/master-keys/**
  - .planning/quick/260522-37g-review-trang-quan-ly-llm-targeted-tests/REVIEW-FINDINGS.md
  - backend/core/src/test/java/com/zeromail/core/admin/cat/usecases/LlmRouterWalkOrderTest.java
  - backend/core/src/test/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultTierServiceValidationTest.java
  - backend/core/src/test/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminServiceListMaskedDedupTest.java
  - backend/core/src/test/java/com/zeromail/core/admin/cat/persistence/lowlevel/ProviderCatalogLookupRepositoryFindAllFeatureDefaultPairsIT.java
  - backend/core/src/test/java/com/zeromail/core/admin/mkey/persistence/lowlevel/LlmProviderMasterKeyWriteRepositoryReorderKeysIT.java
autonomous: true
requirements: [MKEY-01, MKEY-02, MKEY-03, MKEY-04, CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06, CAT-07]

must_haves:
  truths:
    - "All deferred-defect findings are captured in REVIEW-FINDINGS.md with file:line + severity"
    - "Fix-now findings are corrected in code (Java + TSX) and the project still type-checks + builds"
    - "LlmRouter no longer issues N+1 modelCatalogRepository.findById per tier model"
    - "LlmRouter does not re-fetch the active provider key when the same provider repeats across tiers"
    - "Dead method ProviderMasterKeyResolver.isFeatureDefaultProvider is either documented as used or removed"
    - "Doc-comment terminology in LlmRouter matches the code (highest-priority ACTIVE key, not lowest)"
    - "Five new invariant tests exist and all pass via ./gradlew :backend:core:test"
    - "Integration tests use Testcontainers Postgres (not H2, not @Mock for repositories)"
  artifacts:
    - path: ".planning/quick/260522-37g-review-trang-quan-ly-llm-targeted-tests/REVIEW-FINDINGS.md"
      provides: "Defer list — every non-fix-now finding with severity + rationale"
    - path: "backend/core/src/test/java/com/zeromail/core/admin/cat/usecases/LlmRouterWalkOrderTest.java"
      provides: "A1 router walk order invariant"
    - path: "backend/core/src/test/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultTierServiceValidationTest.java"
      provides: "Tier-model validation invariants (empty/dup/mismatch/ineligible)"
    - path: "backend/core/src/test/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminServiceListMaskedDedupTest.java"
      provides: "listMasked dedup invariant"
    - path: "backend/core/src/test/java/com/zeromail/core/admin/cat/persistence/lowlevel/ProviderCatalogLookupRepositoryFindAllFeatureDefaultPairsIT.java"
      provides: "Batch findAllFeatureDefaultPairs perf-regression integration test"
    - path: "backend/core/src/test/java/com/zeromail/core/admin/mkey/persistence/lowlevel/LlmProviderMasterKeyWriteRepositoryReorderKeysIT.java"
      provides: "reorderKeys positive-shift constraint integration test"
  key_links:
    - from: "LlmRouter.appendTierSteps"
      to: "ModelCatalogRepository"
      via: "single batch findAllById call per resolve (not per-model findById)"
      pattern: "findAllById"
    - from: "LlmRouter.resolve"
      to: "LlmProviderMasterKeyWriteRepository.pickHighestPriorityActiveKey"
      via: "memoized per-provider lookup across tiers within one resolve call"
      pattern: "computeIfAbsent|HashMap"
    - from: "ProviderCatalogLookupRepository.findAllFeatureDefaultPairs"
      to: "feature_default_provider table"
      via: "single SQL query returning Set<String> of pairKey(provider,feature)"
      pattern: "SELECT.*FROM feature_default_provider"
---

<objective>
Code review + targeted invariant tests for the Quản lý LLM admin page (commits cb559749..ce6af65a).

Purpose: catch smells/perf/dead-code regressions introduced by the recent master-keys + curated-catalog work, and pin five high-value invariants in tests so they cannot silently regress. This is NOT a coverage exercise — only the five invariants listed in `<tasks>` are tested.

Output:
- In-place fixes (Java + TSX) for fix-now findings
- `REVIEW-FINDINGS.md` capturing deferred findings
- Five new test files (3 unit, 2 @DataJpaTest IT) in `backend/core/src/test/java/...`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@TESTING.md
@CONVENTIONS.md
@.planning/STATE.md

<!-- Backend review targets — read with mcp__jetbrains__get_file_text_by_path for symbol-aware results -->
@backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/LlmRouter.java
@backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultTierService.java
@backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultProviderService.java
@backend/core/src/main/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminService.java
@backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/admin/ProviderMasterKeyResolver.java
@backend/core/src/main/java/com/zeromail/core/admin/cat/persistence/lowlevel/ProviderCatalogLookupRepository.java
@backend/core/src/main/java/com/zeromail/core/admin/cat/persistence/lowlevel/CuratedCatalogReadRepository.java
@backend/core/src/main/java/com/zeromail/core/admin/cat/persistence/lowlevel/ModelCatalogWriteRepository.java
@backend/core/src/main/java/com/zeromail/core/admin/mkey/persistence/lowlevel/LlmProviderMasterKeyWriteRepository.java
@backend/core/src/main/java/com/zeromail/core/admin/cat/persistence/FeatureTierModelEntity.java
@backend/core/src/main/java/com/zeromail/core/admin/cat/persistence/FeatureTierModelRepository.java
@backend/core/src/main/resources/db/changelog/changes/082-feature-tier-multi-model.yaml

<!-- Frontend review targets -->
@apps/admin/src/routes/_authenticated/master-keys.index.tsx
@apps/admin/src/routes/_authenticated/master-keys.$provider.tsx
@apps/admin/src/components/RoutingMatrixCard.tsx
@apps/admin/src/components/TierPickerDialog.tsx
@apps/admin/src/components/AddProviderKeyDialog.tsx
@apps/admin/src/components/EditProviderKeyDialog.tsx
@apps/admin/src/components/AddCatalogModelDialog.tsx

<!-- Existing test scaffolding patterns to follow -->
@backend/core/src/test/java/com/zeromail/core/admin/mkey/usecases/
@backend/core/src/test/java/com/zeromail/core/admin/mkey/persistence/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review sweep + fix-now corrections across full Quản lý LLM page</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/LlmRouter.java,
    backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultTierService.java,
    backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultProviderService.java,
    backend/core/src/main/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminService.java,
    backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/admin/ProviderMasterKeyResolver.java,
    backend/core/src/main/java/com/zeromail/core/admin/cat/persistence/lowlevel/ProviderCatalogLookupRepository.java,
    apps/admin/src/routes/_authenticated/master-keys.index.tsx,
    apps/admin/src/routes/_authenticated/master-keys.$provider.tsx,
    apps/admin/src/components/RoutingMatrixCard.tsx,
    apps/admin/src/components/TierPickerDialog.tsx,
    apps/admin/src/components/AddProviderKeyDialog.tsx,
    apps/admin/src/components/EditProviderKeyDialog.tsx,
    apps/admin/src/components/AddCatalogModelDialog.tsx,
    apps/admin/src/features/master-keys/**,
    .planning/quick/260522-37g-review-trang-quan-ly-llm-targeted-tests/REVIEW-FINDINGS.md
  </files>
  <action>
    Walk the recent commits cb559749..ce6af65a and review the Quản lý LLM page end-to-end (backend usecases/persistence/migrations + apps/admin routes + dialogs + hooks). Use JetBrains MCP for all Java reads/diagnostics (`mcp__jetbrains__get_file_text_by_path`, `mcp__jetbrains__search_symbol`, `mcp__jetbrains__get_symbol_info`, `mcp__jetbrains__get_file_problems`) — fall back to Read/Grep only for `.tsx`, `.yaml`, and config files.

    Classify every finding with one of three severities:
    - `block` — correctness or privacy bug; MUST fix before tests are written
    - `fix` — non-blocking smell that is cheap to fix in this sweep
    - `note` — deferred; record in REVIEW-FINDINGS.md and move on

    Fix-now scope (apply directly):

    1. **LlmRouter perf — N+1 on model lookup.** `appendTierSteps` calls `modelCatalogRepository.findById(...)` once per model id inside the tier loop (up to 9 single-row SELECTs per resolve for 3 tiers × 3 models). Refactor to a single `findAllById(allModelIds)` call at the top of `resolve(...)`, build a `Map<UUID, ModelCatalogEntity>`, and look up from the map inside `appendTierSteps`. Preserve walk order (PRIMARY → FALLBACK → LAST_RESORT outer, position 1..N inner) — order comes from `FeatureTierModelRepository`, not the map.
    2. **LlmRouter perf — redundant active-key fetch across tiers.** When PRIMARY + FALLBACK + LAST_RESORT share the same provider (common case: all OpenRouter), `pickHighestPriorityActiveKey(provider)` runs three times and returns the same row. Memoize per resolve with a `Map<LlmProvider, Optional<LlmProviderMasterKeyRow>>` populated via `computeIfAbsent`. Local to the method — no shared state, no @Cacheable.
    3. **LlmRouter doc/terminology mismatch.** Class-level javadoc says "lowest-priority ACTIVE key" but the helper is `pickHighestPriorityActiveKey` and the `provider_key.priority ASC` ordering means lowest-number = highest priority. Rewrite the doc-comment to say "highest-priority ACTIVE key (lowest priority number)" so terminology is consistent.
    4. **ProviderMasterKeyResolver dead code.** Use JetBrains symbol search to confirm `isFeatureDefaultProvider(LlmProvider)` (single-row variant) has zero callers outside the resolver class itself after the batch fix that switched callers to `findAllFeatureDefaultPairs`. If unused, delete it. If still used in any callsite, leave a one-line javadoc explaining the divergence from the batch path.
    5. **ProviderMasterKeyResolver FQN cleanup.** `toMaskedRow` uses `com.zeromail.core.admin.cat.persistence.lowlevel.ProviderCatalogLookupRepository.pairKey` three times. Add the static import (`import static ...ProviderCatalogLookupRepository.pairKey;`) and remove the FQN. Same treatment for `MasterKeyAdminService.listMasked` where `java.util.Map<com.zeromail.core.admin.mkey.domain.LlmProvider, ...>` appears — add the import and use bare `Map<LlmProvider, ...>`.
    6. **MasterKeyAdminService.listMasked comment vs behavior.** The javadoc claims "prefer ACTIVE over PENDING/REVOKED" but the dedup logic only checks `maskedKey != null`. Pick one: either tighten the code to filter by `status == ACTIVE` first, or rewrite the comment to "prefer rows with a masked key value present (active or pending with key material)". Match the actual product intent — when in doubt, match behavior to the comment because dedup is meant to keep only resolvable keys. Flag the choice in the commit body.
    7. **MasterKeyMaskedRow magic 0L.** `activeKeyCount = 0L` literal sprinkled inline. Replace with a named constant `private static final long NO_ACTIVE_KEY = 0L;` in `MasterKeyAdminService` or use `Optional.empty()`-style sentinel via the row factory.
    8. **Frontend enterprise readability.** Sweep apps/admin route/component/hook files for banned abbreviations from CLAUDE.md (`req`, `res`, `cfg`, `ctx`, `msg`, `err`, `ex`, `e`, `conn`, `tx`, `svc`, `repo`, single-letter locals). Rename to domain-revealing names (`request`, `response`, `configuration`, `tenantContext`, `gmailMessage`, `authenticationException`, etc.). Exception: ignored lambda params may stay `_`.
    9. **Frontend unused imports + any/unknown casts.** Run TypeScript compiler to surface unused imports. Replace `as any`/`as unknown` in hook files with types derived from `apps/admin/src/lib/api/admin-schema.d.ts` (generated OpenAPI). Do NOT hand-edit `admin-schema.d.ts` (CONVENTIONS rule #10) — if a backend DTO is missing a field, log it in REVIEW-FINDINGS.md as a note and use a local view-model type instead.
    10. **Frontend hardcoded hex / non-shadcn styling.** Grep `apps/admin/src/components/*.tsx` and the new routes for hex color literals (`#[0-9a-fA-F]{3,8}`). Replace with token classes (`bg-card`, `border-border`, `text-foreground`, `bg-primary/10`, `bg-accent text-accent-foreground`). Where a custom wrapper duplicates a shadcn primitive without composition value, inline the primitive (per user pref `feedback_raw_shadcn_first`).
    11. **TierPickerDialog provider dropdown polish.** Dropdown renders raw enum `OPENROUTER` / `OPENAI` / `ANTHROPIC` / `DEEPSEEK` / `GOOGLE_GENAI`. Map to friendly labels (`OpenRouter`, `OpenAI`, `Anthropic`, `DeepSeek`, `Google GenAI`) via a `PROVIDER_LABEL: Record<LlmProvider, string>` constant — colocate with the dialog or in `features/master-keys/provider-labels.ts` if also needed by `AddProviderKeyDialog` and `RoutingMatrixCard`.

    Everything else (UX nits, broader refactors, deferred OPS-INFRA hardening, suggestions for follow-up commits) → write to `REVIEW-FINDINGS.md` with this schema per entry:

    - `### Finding NN: <short title>`
    - `**File:**` path:line
    - `**Severity:**` block | fix | note
    - `**Observation:**` one paragraph, what the code currently does
    - `**Why it matters / Why deferred:**` one paragraph
    - `**Suggested fix:**` concrete next step (link to a phase or "ad-hoc later")

    After all edits, run `mcp__jetbrains__get_file_problems` on every touched `.java` file. Zero errors required; warnings noted in commit body. Then run `pnpm --filter @zeromail/admin run typecheck` for frontend. Commit with message `refactor(quick-37g): review sweep + fix-now corrections on Quản lý LLM page` plus a body listing each fix-now ID applied.
  </action>
  <verify>
    <automated>
      cd backend &amp;&amp; ./gradlew :core:compileJava :core:checkstyleMain --no-daemon
      pnpm --filter @zeromail/admin run typecheck
      test -f .planning/quick/260522-37g-review-trang-quan-ly-llm-targeted-tests/REVIEW-FINDINGS.md
      grep -L '#[0-9a-fA-F]\{3,8\}' apps/admin/src/components/TierPickerDialog.tsx apps/admin/src/components/RoutingMatrixCard.tsx
      grep -q 'findAllById' backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/LlmRouter.java
      grep -q 'computeIfAbsent' backend/core/src/main/java/com/zeromail/core/admin/cat/usecases/LlmRouter.java
    </automated>
  </verify>
  <done>
    Java compiles with zero new warnings on touched files (verify via JetBrains `get_file_problems`); apps/admin typechecks clean; REVIEW-FINDINGS.md exists with deferred findings; LlmRouter uses `findAllById` (no per-model `findById` in `appendTierSteps`) and memoizes the active-key lookup; doc-comments match code; dead `isFeatureDefaultProvider` resolved (removed or documented); frontend has zero hex color literals in touched files and zero `as any` casts in master-keys hook files; commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Five targeted invariant tests for Quản lý LLM page</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/admin/cat/usecases/LlmRouterWalkOrderTest.java,
    backend/core/src/test/java/com/zeromail/core/admin/cat/usecases/FeatureDefaultTierServiceValidationTest.java,
    backend/core/src/test/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminServiceListMaskedDedupTest.java,
    backend/core/src/test/java/com/zeromail/core/admin/cat/persistence/lowlevel/ProviderCatalogLookupRepositoryFindAllFeatureDefaultPairsIT.java,
    backend/core/src/test/java/com/zeromail/core/admin/mkey/persistence/lowlevel/LlmProviderMasterKeyWriteRepositoryReorderKeysIT.java
  </files>
  <action>
    Write exactly five test files pinning the five invariants from the task scope. Follow TESTING.md slice ladder: plain JUnit + Mockito for pure use-case logic (tests 1, 2, 4); `@DataJpaTest` + Testcontainers Postgres + `@ServiceConnection` for repository slices (tests 3, 5). NEVER H2. Use `@MockitoBean` if Spring context is loaded (not `@MockBean`). Backend enterprise naming applies — no `req`/`res`/`cfg` etc. — test files are also Java code.

    **Conflict note with task scope and TESTING.md §5:** scope suggests Mockito mocks for tests (1), (2), (4) "with repos mocked". TESTING.md says "Never mock internal services or repositories — use Testcontainers instead." Resolve as follows: tests (1), (2), (4) target use-case classes whose collaborators are passed in via constructor — these collaborators are domain services/repositories defined in the same `backend/core` module. Per TESTING.md, those become @DataJpaTest if they touch the DB. BUT: `LlmRouter`, `FeatureDefaultTierService`, and `MasterKeyAdminService` accept REPOSITORY INTERFACES that are pure data lookups with no in-flight DB-side validation (no triggers/CHECK constraints exercised in these specific tests). For these three, stub the repository interfaces with hand-rolled fakes (in-memory `Map`-backed implementations of the repository interface) — this is a TESTING.md-compliant pattern because it is NOT a mock of an internal service, it is a deterministic test double that records calls. If Mockito is materially simpler, document the deviation in the commit body. Repository-layer tests (3, 5) must use Testcontainers — no exceptions.

    Test-by-test directives:

    1. **`LlmRouterWalkOrderTest`** — plain JUnit, no Spring. Construct `LlmRouter` with hand-rolled in-memory fakes for `FeatureTierModelRepository`, `ModelCatalogWriteRepository`, `LlmProviderMasterKeyWriteRepository`. Seed: feature `TRIAGE` has PRIMARY=[modelA, modelB], FALLBACK=[modelC], LAST_RESORT=[modelD, modelE]. All five models map to OpenRouter. One ACTIVE key exists for OpenRouter. Call `router.resolve("TRIAGE")`. Assert: returned `List<ResolvedRoute>` has size 5 and the order is exactly `[(PRIMARY,1,modelA), (PRIMARY,2,modelB), (FALLBACK,1,modelC), (LAST_RESORT,1,modelD), (LAST_RESORT,2,modelE)]`. Also assert the `pickHighestPriorityActiveKey` recorder fake was called at most once per distinct provider (= 1 total here) — pins the Task 1 memoization fix.

    2. **`FeatureDefaultTierServiceValidationTest`** — plain JUnit. Four nested classes (`@Nested EmptyList`, `@Nested DuplicateIds`, `@Nested ProviderMismatch`, `@Nested IneligibleModel`), each with one `@Test`. Wire `FeatureDefaultTierService` with a fake `ModelCatalogReadRepository` seeded with models `m1@OpenRouter@OK`, `m2@OpenRouter@OK`, `m3@OpenAI@OK`, `m4@OpenRouter@FAILED`, `m5@OpenRouter@UNTESTED`. Assertions: `assign("TRIAGE", PRIMARY, OpenRouter, [])` throws `EmptyTierModelListException`; `assign(..., [m1, m1])` throws `DuplicateTierModelException`; `assign("TRIAGE", PRIMARY, OpenRouter, [m1, m3])` throws `ModelProviderMismatchException`; `assign(..., [m1, m4])` and `assign(..., [m1, m5])` both throw `ModelNotEligibleException`. Verify message content includes the offending model id for ops debugging — but do NOT assert exact message wording (TESTING.md §2).

    3. **`LlmProviderMasterKeyWriteRepositoryReorderKeysIT`** — `@DataJpaTest` + `@ServiceConnection` Testcontainers Postgres. Seed `provider_master_key` table with 3 ACTIVE keys for OpenRouter at priorities 1, 2, 3. Call `reorderKeys(OpenRouter, [keyB.id, keyA.id, keyC.id])` (swap A↔B). Assert: after commit, keyB.priority=1, keyA.priority=2, keyC.priority=3 AND the SQL audit shows no row ever held a negative or zero priority during the operation (use a `@Sql` or a manual `JdbcTemplate` snapshot read against the table mid-operation if possible; otherwise rely on the `ck_priority_positive` constraint — if the two-pass shift goes through 0 or negative, the test fails with a constraint violation. The PASS condition is: no constraint violation thrown + final priorities equal expected). Add a second `@Test` for the edge case of 2 keys swapping (covers minimum size); assert final priorities are 1,2 with positions swapped. Use `@Transactional(propagation = NOT_SUPPORTED)` if the shift is committed in chunks; otherwise default rollback is fine.

    4. **`MasterKeyAdminServiceListMaskedDedupTest`** — plain JUnit. Stub `ProviderMasterKeyResolver` to return two rows for `OpenRouter`: row1 with `maskedKey=null` (REVOKED), row2 with `maskedKey="sk-...abcd"` (ACTIVE). Call `service.listMasked()`. Assert: result contains exactly one entry for OpenRouter, and that entry's `maskedKey` equals `"sk-...abcd"` (the non-null one wins). Then add a `@Nested ProviderWithNoKeyAtAll` case: resolver returns zero rows for `Anthropic` → result still contains an `Anthropic` entry with `maskedKey=null` AND `activeKeyCount=0` (pins the "list every provider, even without keys" behavior).

    5. **`ProviderCatalogLookupRepositoryFindAllFeatureDefaultPairsIT`** — `@DataJpaTest` + Testcontainers. Seed `feature_default_provider` with three pairs: (OpenRouter, TRIAGE), (OpenAI, DRAFT), (OpenRouter, RULES). Call `findAllFeatureDefaultPairs()`. Assert: returned `Set<String>` has size 3 and contains exactly `pairKey(OpenRouter, "TRIAGE")`, `pairKey(OpenAI, "DRAFT")`, `pairKey(OpenRouter, "RULES")`. For the query-count assertion, use Hibernate `Statistics` (enable via `spring.jpa.properties.hibernate.generate_statistics=true` in `application-test.yml` or `@TestPropertySource`): grab `sessionFactory.getStatistics().getPrepareStatementCount()` before/after the call, assert delta == 1. This pins the perf regression — if someone reverts to N round-trips, the count jumps and the test fails. Use `EntityManagerFactory` injection to reach the Hibernate `SessionFactory`.

    Common rules:
    - All tests live under `backend/core/src/test/java/com/zeromail/core/admin/...` mirroring main source layout
    - Use `@DisplayName` on each class and test method for readability in CI output (Vietnamese OK per user preference)
    - Use AssertJ (`assertThat`) — already on classpath via Spring Boot Test starter
    - No `@Disabled` placeholders, no skipped assertions, no commented-out code
    - Run `./gradlew :backend:core:test --tests '*LlmRouterWalkOrderTest' --tests '*FeatureDefaultTierServiceValidationTest' --tests '*MasterKeyAdminServiceListMaskedDedupTest' --tests '*reorderKeysIT' --tests '*findAllFeatureDefaultPairsIT'` to confirm all pass before commit
    - After all green, also run the full `./gradlew :backend:core:test` to confirm no regression in the existing suite

    Commit message: `test(quick-37g): pin 5 invariants on Quản lý LLM page (router walk, tier validation, reorder priorities, listMasked dedup, batch pairs perf)`
  </action>
  <verify>
    <automated>
      cd backend &amp;&amp; ./gradlew :core:test --tests '*LlmRouterWalkOrderTest' --tests '*FeatureDefaultTierServiceValidationTest' --tests '*MasterKeyAdminServiceListMaskedDedupTest' --tests '*LlmProviderMasterKeyWriteRepositoryReorderKeysIT' --tests '*ProviderCatalogLookupRepositoryFindAllFeatureDefaultPairsIT' --no-daemon
      cd backend &amp;&amp; ./gradlew :core:test --no-daemon
    </automated>
  </verify>
  <done>
    Five test files exist at the paths in `files_modified`; each contains the invariant described above; all five pass via Gradle; the full `:core:test` suite still passes (no regression in existing tests); integration tests use Testcontainers Postgres (verified by `@ServiceConnection` annotation present and no H2 import); commit landed.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. `cd backend && ./gradlew :core:test --no-daemon` — full core test suite green
2. `pnpm --filter @zeromail/admin run typecheck` — admin app typechecks clean
3. JetBrains `mcp__jetbrains__get_file_problems` on every touched `.java` file returns zero errors
4. `git log --oneline cb559749..HEAD` shows exactly 2 new commits (refactor + test)
5. `REVIEW-FINDINGS.md` exists and contains at least the deferred items not covered by Task 1 fix-now scope
</verification>

<success_criteria>
- LlmRouter no longer issues N+1 `findById` calls during `resolve(...)`
- LlmRouter does not re-fetch the active provider key when tiers repeat the same provider
- Dead/inconsistent code in `ProviderMasterKeyResolver`, `MasterKeyAdminService`, `LlmRouter` cleaned up
- Frontend has no hardcoded hex colors, no `as any`, no banned abbreviations in touched files
- TierPickerDialog renders friendly provider labels
- Five new invariant tests exist and all pass
- Integration tests use Testcontainers Postgres
- Deferred findings captured in `REVIEW-FINDINGS.md`
</success_criteria>

<output>
Create `.planning/quick/260522-37g-review-trang-quan-ly-llm-targeted-tests/260522-37g-SUMMARY.md` when both tasks complete. Summary must include:
- List of fix-now items applied (Java + TSX) with file:line
- List of deferred findings (link to REVIEW-FINDINGS.md)
- Five test file paths + the invariant each pins
- Any conflict resolutions (e.g. Mockito-vs-Testcontainers deviation rationale)
</output>
