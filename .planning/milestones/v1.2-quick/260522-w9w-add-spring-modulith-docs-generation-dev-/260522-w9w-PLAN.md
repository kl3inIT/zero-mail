# Quick Task 260522-w9w: Add Spring Modulith Docs Generation, Dev Actuator Endpoint, and Scenario Module Tests

**Date:** 2026-05-22
**Status:** Boundary violations found

## Goal

Bring Zero Mail closer to the Spring Modulith talk/demo best practices without changing product behavior.

## Scope

- Generate Spring Modulith documentation from the existing module verification test.
- Expose the Modulith actuator endpoint only where the project already exposes all actuator endpoints for development.
- Add a focused Modulith `@ApplicationModuleTest`/`Scenario` test if the current Spring Modulith version and module shape support it cleanly.

## Verification

- Run the existing application modules verification test.
- Run the new focused module test, if added.
- Run the relevant Gradle test slice for touched backend modules.

## Result

- API-level `ApplicationModules.verify()` still passes and now generates Spring Modulith documentation.
- `spring-modulith-actuator` was added to `backend/api`; production actuator exposure remains constrained by existing config.
- A core-root `@ApplicationModuleTest`/`Scenario` test was added without disabling automatic verification.
- Core module verification currently fails with real Spring Modulith boundary violations. Do not bypass with `verifyAutomatically = false`; fix the graph instead.
