# PR 59 CI Fix

## Goal
Fix the two failing PR #59 CI jobs and monitor until all checks pass.

## Failures
- `gates / Backend Gradle`: `:backend:core:spotlessJavaCheck` format violation in `LlmGatewayByokRoutingTest.java`.
- `gates / Frontend Web`: Vitest coverage statement threshold `29.96% < 30%`.

## Plan
- Apply Java formatting/import cleanup.
- Add focused web test coverage without lowering thresholds.
- Run local CI-equivalent gates before push.
- Push `nhat` and monitor PR #59 checks to green.
