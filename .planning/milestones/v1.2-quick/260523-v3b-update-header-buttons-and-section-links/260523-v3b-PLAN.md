# Quick Task 260523-v3b: Update header buttons and section links

## Goal

Update the public landing header so its navigation buttons point to sections that actually exist on the current landing page.

## Tasks

1. Replace stale landing header links with current section anchors.
   - Files: `apps/web/features/landing/components/TopBar.tsx`, `apps/web/features/landing/components/Hero.tsx`
   - Verify: header links target `/#features`, `/#pricing`, `/#testimonials`, and `/#faq`; hero secondary CTA targets `/#features`.

2. Update localized labels for new header links.
   - Files: `apps/web/i18n/messages/en.json`, `apps/web/i18n/messages/vi.json`
   - Verify: English and Vietnamese labels render without missing translation keys.

3. Run focused frontend checks.
   - Verify: type/lint or available web checks pass, then browser-check the landing page and header anchor behavior.
