---
phase: 11-mailbox-scoped-ingestion-automation-ui-and-verification
plan: 06
type: execute
wave: 5
depends_on: ["11-05"]
files_modified:
  - apps/web/lib/api/schema.d.ts
  - apps/web/features/mailbox/api/mailbox-api.ts
  - apps/web/features/mailbox/query-keys.ts
  - apps/web/features/mailbox/hooks/useMailboxList.ts
  - apps/web/features/mailbox/hooks/useActiveMailbox.ts
  - apps/web/features/mailbox/hooks/useSetActiveMailbox.ts
  - apps/web/components/shell/AppSidebar.tsx
  - apps/web/features/rules/components/CopyRulesDialog.tsx
  - apps/web/e2e/mailbox-switch.spec.ts
  - apps/web/e2e/mailbox-rules.spec.ts
  - apps/web/e2e/mailbox-send-from.spec.ts
  - apps/web/__tests__/mailbox-hooks.test.ts
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, VER-02, VER-04]
user_setup:
  - service: google-gmail
    why: "VER-04 real-Gmail multi-mailbox smoke (connect → ingest → switch → send-from) needs two real Gmail grants on the dev VPS"
    dashboard_config:
      - task: "Connect a SECOND real Gmail mailbox on the dev environment (first is already connected)"
        location: "Zero Mail dev app → footer AccountMenu → Add Gmail → Google consent"
must_haves:
  truths:
    - "schema.d.ts is regenerated from the running backend (never hand-edited) and the mailbox feature API derives its types from it"
    - "The footer AccountMenu shows the workspace/Google identity (top) AND a separate mailbox list (active marker + primary badge + status + Switch + Add Gmail) — they are distinct concepts"
    - "Switching the active mailbox calls the set endpoint then invalidates all mailbox-scoped query keys so stale mailbox-A data never renders under mailbox B"
    - "Send/write previews show the source + executing mailbox (UX-06); rules UI shows the active mailbox's rules + a copy-rules action (UX-03)"
    - "D-02: every read surface (inbox, needs-reply, rules, audit, analytics) renders the single active mailbox by default; NO unified/all-mailboxes roll-up view is built in v1.3 — the ING-04/UX-04 read-only roll-up constraints stay future guardrails only"
    - "Playwright covers connect/list/switch/mailbox-owned-rules/send-from/audit; a real-Gmail smoke checkpoint validates the live flow"
  artifacts:
    - path: "apps/web/features/mailbox/hooks/useSetActiveMailbox.ts"
      provides: "Set-active mutation that invalidates mailbox-scoped query keys on success"
      contains: "invalidateQueries"
    - path: "apps/web/components/shell/AppSidebar.tsx"
      provides: "Mailbox switcher merged into AccountMenu (D-01)"
      contains: "Add Gmail"
    - path: "apps/web/e2e/mailbox-switch.spec.ts"
      provides: "Playwright active-mailbox switch flow (VER-04)"
      contains: "test("
  key_links:
    - from: "features/mailbox/api/mailbox-api.ts"
      to: "lib/api/schema.d.ts"
      via: "components['schemas']['MailboxSummaryResponse'] + typed api.GET/PUT"
      pattern: "components\\['schemas'\\]"
    - from: "useSetActiveMailbox"
      to: "inbox/needs-reply/rules/audit/analytics query keys"
      via: "queryClient.invalidateQueries on switch"
      pattern: "invalidateQueries"
---

<objective>
Surface the multi-mailbox model in the web app: regenerate OpenAPI types (VER-02), build the `features/mailbox` feature triad (api + query-keys + hooks), merge the active-mailbox switcher into the footer `AccountMenu` (D-01/UX-02) as a SEPARATE concept from the workspace identity, render the active mailbox by default on inbox/needs-reply/rules/audit/analytics (UX-02/04), expose the copy-rules action in the rules UI (UX-03), show source + executing mailbox on write/send previews (UX-06), keep onboarding one-Gmail-simple with add-more after first connect (UX-05), and verify connect/list/switch/rules/send-from/audit in a real browser plus a real-Gmail smoke (VER-04).

Purpose: The user-facing integration that makes fast mailbox switching real. This is the final phase plan; it has a human-verify checkpoint for the real-Gmail smoke (autonomous: false).
Output: Regenerated schema, mailbox feature triad, AccountMenu switcher, copy-rules dialog, active-default rendering, Playwright specs + real-Gmail smoke checkpoint.
</objective>

<artifacts_this_phase_produces>
NEW symbols (exclude from drift verification):
- `features/mailbox/api/mailbox-api.ts`, `features/mailbox/query-keys.ts` (NEW)
- `useMailboxList`, `useActiveMailbox`, `useSetActiveMailbox` hooks (NEW)
- `CopyRulesDialog` component (NEW — consumes Plan 04 CopyRulesService endpoint)
- Mailbox switcher section inside `AccountMenu` (NEW UI in existing component)
- New `data-testid`s: `mailbox-switch-{id}`, `mailbox-add-gmail`, `mailbox-active-marker`, `copy-rules-button` (NEW Playwright handles)
- `e2e/mailbox-switch.spec.ts`, `mailbox-rules.spec.ts`, `mailbox-send-from.spec.ts`, `__tests__/mailbox-hooks.test.ts` (NEW)
- Regenerated `schema.d.ts` entries for the new/changed DTOs (MailboxSummaryResponse fields, ActiveMailboxResponse, mailbox provenance on read DTOs)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-05-SUMMARY.md
@apps/web/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate OpenAPI + build the features/mailbox triad (api + query-keys + hooks) (VER-02, UX-01/02)</name>
  <files>apps/web/lib/api/schema.d.ts, apps/web/features/mailbox/api/mailbox-api.ts, apps/web/features/mailbox/query-keys.ts, apps/web/features/mailbox/hooks/useMailboxList.ts, apps/web/features/mailbox/hooks/useActiveMailbox.ts, apps/web/features/mailbox/hooks/useSetActiveMailbox.ts, apps/web/__tests__/mailbox-hooks.test.ts</files>
  <read_first>
    - apps/web/features/gmail/api/gmail-api.ts (typed api.GET/POST client; throw on error || !response.ok || data === undefined — the exact shape to mirror)
    - apps/web/features/gmail/query-keys.ts (key factory shape)
    - apps/web/features/gmail/hooks/useTenantStatus.ts + useDisconnectGmail.ts (useQuery / useMutation + onSuccess invalidate patterns)
    - apps/web/lib/api/client.ts (api client; meta toasts via QueryClient cache callbacks — AGENTS.md rule 12)
    - apps/web/scripts/generate-api.ts (regen pipeline: boot backend → fetch /v3/api-docs → openapi-typescript)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md (§ features/mailbox/{api,query-keys,hooks}; § Open Question 1 — switch must invalidate all mailbox-scoped keys)
    - apps/web/AGENTS.md (generated schema.d.ts never hand-edited; meta.successMessage/errorMessage not local toast; token classes)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-05-SUMMARY.md (active-mailbox endpoint paths from Plan 05)
  </read_first>
  <action>
    Boot the backend (dev DB via SSH tunnel :5555 + crypto env per MEMORY notes), then run `pnpm --filter web run generate:api` to regenerate `schema.d.ts` from the live `/v3/api-docs` (NEVER hand-edit — CLAUDE.md rule 11). Create `features/mailbox/api/mailbox-api.ts` deriving types from `components['schemas']['MailboxSummaryResponse']` + `ActiveMailboxResponse`, using the typed `api.GET`/`api.PUT` client (`/api/gmail/mailboxes` list, `/api/gmail/active-mailbox` get, `/api/gmail/active-mailbox/{gmailConnectionId}` set), throwing on `error || !response.ok || data === undefined`. Create `features/mailbox/query-keys.ts` with `mailboxQueryKeys` (all/list/active). Create hooks: `useMailboxList`/`useActiveMailbox` (useQuery mirroring useTenantStatus) and `useSetActiveMailbox` (useMutation mirroring useDisconnectGmail) whose `onSuccess` invalidates ALL mailbox-scoped query keys — inbox, needs-reply, rules, audit, analytics — so stale mailbox-A data does not render under mailbox B (RESEARCH Open Question 1); use `meta.successMessage`/`meta.errorMessage`, not local toast. Add `__tests__/mailbox-hooks.test.ts` (Vitest) covering list/active fetch and the switch-invalidation behavior.
  </action>
  <verify>
    <automated>pnpm --filter web test -- mailbox-hooks && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `schema.d.ts` regenerated from the live backend (git diff shows the new mailbox/active-mailbox DTOs); no hand-edit
    - `mailbox-api.ts` derives types from `components['schemas']` and uses the typed `api.*` client
    - `useSetActiveMailbox` `onSuccess` calls `invalidateQueries` for inbox/needs-reply/rules/audit/analytics keys
    - Hooks use `meta` toasts, not local `toast`
    - `pnpm --filter web test -- mailbox-hooks` green; `tsc --noEmit` clean
  </acceptance_criteria>
  <done>The mailbox feature triad exists with generated types and a switch that resets all mailbox-scoped caches.</done>
</task>

<task type="auto">
  <name>Task 2: Switcher in AccountMenu + copy-rules dialog + active-default + send/write provenance (UX-02/03/04/05/06)</name>
  <files>apps/web/components/shell/AppSidebar.tsx, apps/web/features/rules/components/CopyRulesDialog.tsx</files>
  <read_first>
    - apps/web/components/shell/AppSidebar.tsx (read AccountMenu lines 118-222 + ReconnectRow lines 224-248 — raw shadcn DropdownMenu imports lines 27-35; top identity lines 176-191 from useCurrentUser; ReconnectRow hard-navigates to /api/tenant/connect-gmail line 238)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md (§ AppSidebar — add a DropdownMenuGroup below the separator: connected mailboxes with active marker + primary badge + status + Switch + Add Gmail; do NOT conflate logged-in user with active mailbox; token classes only; data-testid handles)
    - apps/web/features/rules/ (existing rules UI — When/Then mental model; add the copy-rules action without replacing it, UX-03)
    - apps/web/AGENTS.md + CLAUDE.md rule 13 (raw shadcn first, no custom wrapper; tokens not hex; no global UI skill)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-04-SUMMARY.md (CopyRulesService endpoint contract) + 11-05-SUMMARY.md (active-mailbox endpoint)
  </read_first>
  <action>
    In `AppSidebar.tsx` `AccountMenu`: keep the top identity line as the workspace/Google user (`useCurrentUser` — unchanged), and BELOW the separator add a new `DropdownMenuGroup` (raw shadcn `DropdownMenu` primitives, no custom wrapper) that lists connected mailboxes from `useMailboxList` with an active marker (from `useActiveMailbox`), primary badge, and status; a Switch affordance calling `useSetActiveMailbox`; and an "Add Gmail" entry. Do NOT conflate the logged-in user with the active mailbox (D-01). Make `ReconnectRow` mailbox-aware (use the Phase 10 add/reconnect intent endpoint, not the tenant-singular `/api/tenant/connect-gmail`). Token classes only (`bg-sidebar-accent`, `text-destructive`, etc. — never hex). Add `data-testid`s `mailbox-switch-{id}`, `mailbox-add-gmail`, `mailbox-active-marker`. Create `features/rules/components/CopyRulesDialog.tsx` exposing the bulk "Copy rules from [mailbox A]" action (UX-03) that calls the Plan 04 copy-rules endpoint (clones into the active mailbox, enabled=false) with a `copy-rules-button` testid. Ensure inbox/needs-reply/rules/audit/analytics render the ACTIVE mailbox by default (they already resolve via MailboxContext server-side; the UI just refetches on switch). Ensure send/reply/forward + risky-write previews show the source + executing mailbox (UX-06) by surfacing the provenance fields now on the read/preview DTOs. Verify in a real browser via Playwright MCP (login → open AccountMenu → switch → confirm inbox refetches; open rules → copy-rules; open a send preview → confirm mailbox labels).
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit && pnpm --filter web exec eslint apps/web/components/shell/AppSidebar.tsx apps/web/features/rules/components/CopyRulesDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `AppSidebar.tsx` AccountMenu has a separate mailbox-list group (active marker + primary badge + status + Switch + Add Gmail) below the workspace identity line; raw shadcn primitives; token classes only
    - `ReconnectRow` no longer navigates to `/api/tenant/connect-gmail`; uses the mailbox-aware add/reconnect endpoint
    - `CopyRulesDialog.tsx` exists, calls the copy-rules endpoint, clones into the active mailbox enabled=false
    - Send/reply/forward + risky-write previews display source + executing mailbox (UX-06)
    - New `data-testid`s present; `tsc --noEmit` + eslint clean; browser-verified via Playwright MCP
  </acceptance_criteria>
  <done>The footer switcher distinguishes workspace identity from active mailbox, copy-rules is surfaced, previews show provenance, and all active-default surfaces refetch on switch.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright specs for connect/list/switch/rules/send-from/audit (VER-04)</name>
  <files>apps/web/e2e/mailbox-switch.spec.ts, apps/web/e2e/mailbox-rules.spec.ts, apps/web/e2e/mailbox-send-from.spec.ts</files>
  <read_first>
    - apps/web/e2e/ (existing Playwright specs — chrome-test-utils mocking conventions; reference_ai_page_e2e_and_hydration MEMORY note about mocking ALL endpoints)
    - apps/web/components/shell/AppSidebar.tsx (the data-testid handles added in Task 2: mailbox-switch-{id}, mailbox-add-gmail, mailbox-active-marker, copy-rules-button)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md (§ Validation Architecture VER-04 row; § Environment Availability — real-Gmail smoke is the manual checkpoint, browser flows are automatable with mocks)
    - reference about Rules tab e2e flake at 320px (MEMORY) — keep the switcher reachable at all viewports; do not gate tab visibility on overflow
  </read_first>
  <action>
    Create three Playwright specs against the running dev server, mocking the mailbox/active-mailbox/inbox/rules/audit endpoints (mock ALL endpoints a page reads, per the AI-page e2e MEMORY note, to avoid rethrow-on-error blanks): `mailbox-switch.spec.ts` (open AccountMenu → list mailboxes → switch active → assert inbox/needs-reply refetch and the active marker moves), `mailbox-rules.spec.ts` (rules UI shows the active mailbox's rules + copy-rules dialog clones into the active mailbox enabled=false), `mailbox-send-from.spec.ts` (a send/reply/forward preview shows source + executing mailbox labels). Use the `data-testid` handles from Task 2. Keep the switcher reachable at mobile 320px (do not gate on overflow — MEMORY flake note). Connect/list flow is covered by the switch spec's setup. These are mock-driven and CI-runnable; the live real-Gmail run is the Task 4 checkpoint.
  </action>
  <verify>
    <automated>pnpm --filter web exec playwright test e2e/mailbox-switch.spec.ts e2e/mailbox-rules.spec.ts e2e/mailbox-send-from.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Three specs exist and pass against the dev server with mocked endpoints
    - Switch spec asserts active-marker move + refetch; rules spec asserts copy-rules enabled=false; send-from spec asserts source + executing mailbox labels
    - Switcher reachable at 320px viewport (no overflow gating)
    - `pnpm --filter web exec playwright test e2e/mailbox-*.spec.ts` exits 0
  </acceptance_criteria>
  <done>Browser flows for connect/list/switch/mailbox-owned-rules/send-from/audit are automated and green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Real-Gmail multi-mailbox smoke on dev VPS (VER-04, folded Phase 8 todo)</name>
  <files>(human-driven; no source files — live dev VPS verification of the Plan 01-05 backend + Plan 06 web work)</files>
  <action>HUMAN CHECKPOINT (no autonomous code): the executor pauses here. The human performs the six how-to-verify steps below on the dev VPS with two real Gmail mailboxes connected, confirming live multi-mailbox ingestion, active-mailbox switching, send-from correctness, no cross-account bleed, and AUD-07 privacy-clean logging. Resume only on "approved".</action>
  <what-built>Multi-mailbox ingestion + active-mailbox switching + send-from across two real Gmail mailboxes, end to end on the dev environment. All automated backend invariant tests + mock Playwright flows are green; this verifies the live Google OAuth + real Pub/Sub push + real send path that cannot run in CI.</what-built>
  <how-to-verify>
    1. Ensure the dev backend is running (SSH tunnel :5555 up, crypto env set) and a FIRST Gmail mailbox is already connected.
    2. In the dev web app, open the footer AccountMenu → "Add Gmail" → complete Google consent for a SECOND real Gmail account. Confirm it appears in the mailbox list (status CONNECTED, secondary, not primary).
    3. Send a fresh email to EACH connected Gmail address. Confirm each message is ingested under the correct mailbox (check the inbox view after switching active mailbox to each; confirm no cross-account bleed — mailbox A does not show mailbox B's mail).
    4. Switch the active mailbox between the two; confirm inbox/needs-reply/rules/audit re-render for the active mailbox and stale data does not persist.
    5. Trigger a send/reply (rule-triggered or chat-confirmed) from EACH mailbox; confirm the Gmail "Sent" folder of the correct account receives it and the preview showed the right source + executing mailbox.
    6. Confirm app logs emit no raw email/subject/body/token (AUD-07) — only tenantId + gmailConnectionId + technical status.
  </how-to-verify>
  <verify><human-check>All six how-to-verify steps confirmed on the dev VPS with two real Gmail mailboxes.</human-check></verify>
  <done>Live two-mailbox connect → ingest → switch → send-from verified with no cross-account bleed and AUD-07-clean logs; human typed "approved".</done>
  <resume-signal>Type "approved" if all six steps pass, or describe the failure (which mailbox, which step, what leaked).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → set-active-mailbox PUT | server re-validates ownership; the UI must not assume a switch succeeded without the 200 |
| switch → cached query data | stale mailbox-A data must not render under mailbox B after a switch |
| send preview → user | the executing mailbox shown must match the mailbox that will actually send (UX-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Info disclosure | stale mailbox-A inbox/audit data renders under active mailbox B | mitigate | useSetActiveMailbox invalidates all mailbox-scoped query keys on switch (Open Question 1) |
| T-11-06-02 | Spoofing | preview shows mailbox A but the send executes from mailbox B | mitigate | preview reads the same executing-mailbox provenance the gateway uses (UX-06) |
| T-11-06-03 | Info disclosure | hand-edited schema.d.ts drifts from backend, hiding a contract mismatch | mitigate | regenerate via generate:api from the live backend; never hand-edit (rule 11) |
| T-11-06-04 | Info disclosure | real-Gmail smoke leaks raw email/subject to logs | mitigate | checkpoint step 6 confirms AUD-07 logging on the live path |
| T-11-06-05 | Elevation | UI conflates logged-in user with active mailbox, enabling a wrong-mailbox action | mitigate | AccountMenu keeps workspace identity and active mailbox as distinct sections (D-01) |
| T-11-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new packages; feature uses already-vendored deps (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `pnpm --filter web test -- mailbox-hooks` + `pnpm --filter web exec tsc --noEmit` + eslint clean
- `pnpm --filter web exec playwright test e2e/mailbox-*.spec.ts` green
- `git diff apps/web/lib/api/schema.d.ts` shows regenerated (not hand-edited) mailbox DTOs
- Browser-verified via Playwright MCP (switch + copy-rules + send-preview provenance)
- Real-Gmail smoke checkpoint approved
</verification>

<success_criteria>
- OpenAPI regenerated; mailbox feature API uses generated types (VER-02)
- AccountMenu switcher distinguishes workspace identity from active mailbox (UX-02, D-01)
- Active-default rendering on inbox/needs-reply/rules/audit/analytics; copy-rules in rules UI (UX-02/03/04)
- Onboarding stays one-Gmail-simple with add-more after first connect (UX-05)
- Send/write previews show source + executing mailbox (UX-06)
- Playwright flows green + real-Gmail smoke approved (VER-04)
</success_criteria>

<output>
Create `.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-06-SUMMARY.md` when done. Record the regenerated schema diff scope, the new data-testids, and the real-Gmail smoke result (approved / issues).
</output>
