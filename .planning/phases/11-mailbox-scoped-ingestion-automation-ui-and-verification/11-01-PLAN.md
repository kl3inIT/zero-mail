---
phase: 11-mailbox-scoped-ingestion-automation-ui-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/src/test/java/com/zeromail/core/gmail/PubSubMailboxLookupTest.java
  - backend/core/src/test/java/com/zeromail/core/gmail/ObservedMailboxPkTest.java
  - backend/core/src/test/java/com/zeromail/core/inbox/ProjectionAadContinuityTest.java
  - backend/core/src/test/java/com/zeromail/core/migration/Migration12xBackfillTest.java
  - backend/core/src/test/java/com/zeromail/core/rules/MailboxOwnedRulesRuntimeTest.java
  - backend/core/src/test/java/com/zeromail/core/outbound/OutboundMailboxRoutingTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/GmailClientLookupBoundaryTest.java
  - backend/api/src/test/java/com/zeromail/api/security/CrossAccountIsolationTest.java
  - backend/core/src/test/java/com/zeromail/core/migration/OldTwoMailboxFixture.java
autonomous: true
requirements: [VER-03, AUD-05, AUD-06, ING-01, ING-03, ING-06, AUTO-04, AUTO-06, AUD-02]
must_haves:
  truths:
    - "Every Phase 11 invariant (migration backfill, same-msg-id collision, AAD continuity, mailbox-owned rules, outbound routing, cross-account isolation) has a focused RED test that compiles and fails for the right reason"
    - "The ArchUnit boundary test carries a NEW rule forbidding GmailConnectionRepository.findByTenantId in mailbox-scoped packages, complementing the existing buildClientForTenant rule"
    - "A two-CONNECTED-mailbox fixture exists so later isolation tests can craft cross-account ids (covers the Phase 10 CR-01 shim gap)"
  artifacts:
    - path: "backend/api/src/test/java/com/zeromail/api/security/CrossAccountIsolationTest.java"
      provides: "Two-CONNECTED-mailbox RestClient+@LocalServerPort isolation harness for AUD-06"
      contains: "class CrossAccountIsolationTest"
    - path: "backend/core/src/test/java/com/zeromail/core/arch/GmailClientLookupBoundaryTest.java"
      provides: "ArchUnit allow-list drain target + new findByTenantId-forbidding rule"
      contains: "findByTenantId"
    - path: "backend/core/src/test/java/com/zeromail/core/migration/OldTwoMailboxFixture.java"
      provides: "Reusable two-mailbox seed fixture for migration + isolation tests"
      contains: "class OldTwoMailboxFixture"
  key_links:
    - from: "Wave 0 RED tests"
      to: "Wave 1-4 production classes"
      via: "FQN references / reflection that compile but fail until production lands"
      pattern: "gmail_connection_id|buildClientForMailbox|MailboxContext"
---

<objective>
Establish the Phase 11 validation spine BEFORE any production code (Nyquist). Create focused, RED-by-design test scaffolds — one per phase invariant from RESEARCH.md § "Validation Architecture" → "Phase Requirements → Test Map" — plus the two-CONNECTED-mailbox fixture and the NEW ArchUnit rule. **Every Wave 0 test MUST leave `compileTestJava` GREEN (HIGH-1 from 11-REVIEWS.md): compile-broken intermediate waves are forbidden because they make per-wave verification unreliable and block parallel/autonomous execution.** Achieve RED-by-design WITHOUT a broken compile by one of these mechanisms only: (a) the test references production symbols that ALREADY exist (e.g. `MailboxRef`, `buildClientForMailbox`, `GmailConnectionRepository` — all shipped in Phase 10) and fails at ASSERTION/RUNTIME against the not-yet-migrated behavior; (b) for symbols this phase has not yet created (e.g. the `gmail_connection_id` PK column, `findConnectedMailboxByEmail`, `OutboundSendCommand.mailboxRef`, `MailboxContext`), the test reaches them via a FQN string + reflection / `Class.forName` / a SQL `information_schema` probe / a Liquibase-applied-schema query — so the file COMPILES today and FAILS at runtime with the documented reason until Waves 1-4 land the symbol. Do NOT write a test that names a non-existent Java type as a compile-time reference. `compileTestJava` for both `:backend:core` and `:backend:api` MUST exit 0 at the end of this plan with zero compilation errors.

Purpose: Every later task has an automated verify command or a Wave 0 dependency it satisfies. This closes the VALIDATION.md `nyquist_compliant` and `wave_0_complete` gaps and gives the cross-account isolation harness (AUD-06) that covers the Phase 10 CR-01 multi-row-primary shim gap called out in 10-REVIEW-FIX.md.
Output: 8 new/extended test files + 1 fixture, all RED-by-design with documented acceptance contracts.
</objective>

<artifacts_this_phase_produces>
This plan creates NEW test symbols only (excluded from drift/source-grounding verification):
- `PubSubMailboxLookupTest`, `ObservedMailboxPkTest`, `ProjectionAadContinuityTest`, `Migration12xBackfillTest`, `MailboxOwnedRulesRuntimeTest`, `OutboundMailboxRoutingTest`, `CrossAccountIsolationTest` (NEW test classes)
- `OldTwoMailboxFixture` (NEW test fixture)
- NEW ArchUnit rule method inside `GmailClientLookupBoundaryTest` forbidding `findByTenantId` in mailbox-scoped packages
Symbols Waves 1-4 create that these tests are RED against (`MailboxContext`, `gmail_connection_id` columns, `findConnectedMailboxByEmail`, `TenantMailboxRef`, `OutboundSendCommand.mailboxRef`) are reached via FQN-string + reflection / `information_schema` / applied-schema probes so the test files COMPILE today and fail at runtime — never as compile-time type references (HIGH-1 compile-green rule). Symbols that already exist from Phase 10 (`MailboxRef`, `buildClientForMailbox`, `GmailConnectionRepository`) may be referenced directly.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-VALIDATION.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GmailClientLookupBoundaryTest with the findByTenantId-forbidding rule (AUD-05)</name>
  <files>backend/core/src/test/java/com/zeromail/core/arch/GmailClientLookupBoundaryTest.java</files>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/arch/GmailClientLookupBoundaryTest.java (read all 85 lines — the existing buildClientForTenant rule, ALLOWED_TENANT_LOOKUP_CALLERS lines 19-32, isTenantLookupCall/getMethodCallsFromSelf condition shape lines 38-84, allowEmptyShould(false) line 78)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionRepository.java (the findByTenantId shim — lines 27-29 delegate to findPrimaryMailboxCandidatesByTenantId(..., Limit.of(1)) returning the primary)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md (§ GmailClientLookupBoundaryTest)
  </read_first>
  <action>
    Add a SECOND ArchUnit rule method `findByTenantId_isForbiddenInMailboxScopedFlows` mirroring the existing `isTenantLookupCall`/`getMethodCallsFromSelf` ArchCondition shape but matching `methodName.equals("findByTenantId")` with target owner `GmailConnectionRepository` (rather than `buildClientForTenant` on `GmailApiClientFactory`). Reuse the same allow-list mechanism with a separate constant `ALLOWED_PRIMARY_SHIM_CALLERS` seeded with the CURRENT callers of `findByTenantId` (find them via Grep on `findByTenantId` across backend/core), and keep `allowEmptyShould(false)` so the rule bites immediately and Phase 11 drains it. Leave the existing `buildClientForTenant` rule and its `ALLOWED_TENANT_LOOKUP_CALLERS` list UNCHANGED in this task (Wave 3 drains it). Document in a class-level Javadoc note that this rule guards Pitfall 2 (shim silently routing a mailbox-scoped write to primary).
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*GmailClientLookupBoundary*"</automated>
  </verify>
  <acceptance_criteria>
    - GmailClientLookupBoundaryTest.java contains a method/condition referencing `findByTenantId` AND a constant `ALLOWED_PRIMARY_SHIM_CALLERS`
    - `./gradlew :backend:core:test --tests "*GmailClientLookupBoundary*"` exits 0 (rule passes because the allow-list lists current callers; it will be drained in Wave 3)
    - The existing `buildClientForTenant` rule and `ALLOWED_TENANT_LOOKUP_CALLERS` are byte-unchanged
  </acceptance_criteria>
  <done>Two ArchUnit boundary rules exist (buildClientForTenant + findByTenantId), both green against current callers, both with non-empty allow-lists to drain.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two-mailbox fixture + RED migration/collision/AAD/rules/outbound test scaffolds (backend/core)</name>
  <files>backend/core/src/test/java/com/zeromail/core/migration/OldTwoMailboxFixture.java, backend/core/src/test/java/com/zeromail/core/gmail/PubSubMailboxLookupTest.java, backend/core/src/test/java/com/zeromail/core/gmail/ObservedMailboxPkTest.java, backend/core/src/test/java/com/zeromail/core/inbox/ProjectionAadContinuityTest.java, backend/core/src/test/java/com/zeromail/core/migration/Migration12xBackfillTest.java, backend/core/src/test/java/com/zeromail/core/rules/MailboxOwnedRulesRuntimeTest.java, backend/core/src/test/java/com/zeromail/core/outbound/OutboundMailboxRoutingTest.java</files>
  <read_first>
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md (§ Validation Architecture → Phase Requirements→Test Map, § Wave 0 Gaps, § Common Pitfalls 3/4/5)
    - backend/core/src/test/java/com/zeromail/core/PostgresContainerTest.java (singleton Testcontainers base — extend this, do NOT spin new contexts; ~15-context ceiling)
    - .claude/skills/spring-jpa-testing/SKILL.md (persistAndFlush + em.clear() before conflict assertions — Pitfall 5)
    - backend/core/src/main/java/com/zeromail/core/inbox/usecases/InboxProjectionCipher.java (AAD `tenantId:gmailMessageId:field` lines ~112-115 — proves AAD stays tenant-based, projection PK change must NOT touch it)
    - backend/core/src/main/resources/db/changelog/changes/119-gmail-connections-multi-mailbox.yaml (backfill ordering analog for Migration12xBackfillTest expectations)
  </read_first>
  <action>
    Create `OldTwoMailboxFixture` as a reusable helper that seeds one tenant with TWO CONNECTED `gmail_connections` rows (one `is_primary=true`, one secondary) via raw JDBC/`TestEntityManager`, returning both `gmailConnectionId` UUIDs and the tenant id — so later tests can craft same-message-id-in-both-mailboxes scenarios. Then create six RED-by-design `@DataJpaTest`/integration scaffolds extending `PostgresContainerTest`, each with the focused name from the RESEARCH test map: `PubSubMailboxLookupTest` (ING-01/HIGH-2 — asserts the to-be-built `findConnectedMailboxByEmail` returns a single `(tenantId, gmailConnectionId)`, empty→drop, AND that the global `uq_gmail_conn_active_email_global` index from changeset 127 rejects a second CONNECTED row for the same email in a different tenant — probe the index via `information_schema` and assert an insert of a cross-tenant duplicate CONNECTED row fails), `ObservedMailboxPkTest` (ING-03/06 — same gmail_message_id in two mailboxes yields TWO `mail_message_observed` rows after PK includes `gmail_connection_id`; use persistAndFlush+clear), `ProjectionAadContinuityTest` (ING-06 — a projection row encrypted before the PK change still decrypts after, AAD UNCHANGED), `Migration12xBackfillTest` (VER-03 — old rows backfill to the tenant's primary mailbox, HALT-on-tenant-without-connection), `MailboxOwnedRulesRuntimeTest` (AUTO-04 — runtime loads only `WHERE gmail_connection_id = :sourceMailbox AND enabled=true`), `OutboundMailboxRoutingTest` (AUTO-06/AUD-02 — `OutboundSendCommand` carries an executing `MailboxRef`, undo targets the same mailbox, and the `triage_audit` idempotency index is keyed on `executing_mailbox_id` per HIGH-3 — probe `ux_triage_audit_idem` columns via `information_schema`, NOT a `gmail_connection_id` column). **COMPILE-GREEN MECHANISM (HIGH-1):** every reference to a symbol Waves 1-4 have NOT yet created must be reached so the file compiles today — a not-yet-existing repository method or command field via reflection (`Class.forName` / `getDeclaredMethod` / `getDeclaredField` wrapped to throw a documented `AssertionError` until present), a not-yet-existing column (`gmail_connection_id` on the PK) via an `information_schema.columns` / `information_schema.table_constraints` probe against the Liquibase-applied schema, and same-id collision via an actual insert that the test asserts on the row count. Symbols already shipped in Phase 10 (`MailboxRef`, `buildClientForMailbox`, `GmailConnectionRepository`, `InboxProjectionCipher`) may be referenced directly as compile-time types. Add a class-level Javadoc to EACH stating the exact production symbol/column it waits on, the probe mechanism, and why it is RED (the acceptance contract). Do NOT add production stubs, and do NOT write any compile-time reference to a type/method/field that does not yet exist.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test files + `OldTwoMailboxFixture` exist under the listed packages
    - `OldTwoMailboxFixture.java` seeds TWO CONNECTED rows for one tenant and exposes both connection ids + tenant id
    - Each test class Javadoc names the specific production symbol/column it is RED against AND the compile-green probe mechanism (reflection / information_schema / row-count assertion)
    - `./gradlew :backend:core:compileTestJava` exits 0 with ZERO compilation errors (HIGH-1 — no compile-broken seam; not-yet-existing symbols are reached via reflection/probe, never as compile-time references)
    - Tests are RED at runtime/assertion only (record in the SUMMARY which runtime assertion each waits on as its Waves 1-4 acceptance contract)
  </acceptance_criteria>
  <done>Six focused backend invariant tests + the two-mailbox fixture exist as documented RED scaffolds; the Phase 11 invariant suite is now addressable by `--tests` name.</done>
</task>

<task type="auto">
  <name>Task 3: Create the CrossAccountIsolationTest harness (backend/api, AUD-06, CR-01 gap)</name>
  <files>backend/api/src/test/java/com/zeromail/api/security/CrossAccountIsolationTest.java</files>
  <read_first>
    - backend/api/src/test/java/com/zeromail/api/ApiPostgresTestBase.java (RestClient + @LocalServerPort base — reuse so filters/ScopedValue bind; MockMvc would skip the new MailboxBindingFilter)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md (§ Security Domain threat table, § Pitfall 4 — ONE @SpringBootTest with two CONNECTED mailboxes, not many contexts)
    - .planning/phases/10-gmail-mailbox-foundation-and-account-management/10-CONTEXT.md (D-04 ownership seam 404 not-owned / 409 disconnected contract)
    - backend/core/src/test/java/com/zeromail/core/migration/OldTwoMailboxFixture.java (created in Task 2 — reuse to seed two CONNECTED mailboxes)
  </read_first>
  <action>
    Create `CrossAccountIsolationTest` as a SINGLE `@SpringBootTest(webEnvironment = RANDOM_PORT)` extending `ApiPostgresTestBase`, using `RestClient + @LocalServerPort` and `OldTwoMailboxFixture`. Add RED-by-design test methods asserting that with two CONNECTED mailboxes for one tenant: (a) a crafted/foreign `gmailConnectionId` on a `/api/gmail/mailboxes/{gmailConnectionId}/...` management call returns 404 (not-owned), (b) a disconnected mailbox returns 409, (c) read/inbox surfaces resolve only the ACTIVE mailbox's data (no cross-account read leak), (d) outbound send executes only under the active/executing mailbox. **COMPILE-GREEN MECHANISM (HIGH-1):** drive every assertion over HTTP through the `RestClient` against the planned endpoint PATHS (`PUT /api/gmail/active-mailbox/{gmailConnectionId}`, `GET /api/gmail/active-mailbox`) as plain strings — do NOT import or name the not-yet-built `MailboxBindingFilter` / `ActiveMailboxController` as compile-time Java types. The test compiles today (only `ApiPostgresTestBase`, `RestClient`, `OldTwoMailboxFixture` are referenced as types) and is RED at runtime (the active-mailbox endpoints 404 / behavior wrong) until Wave 4. Class Javadoc states it covers the Phase 10 CR-01 multi-row-primary shim gap. Keep it to ONE Spring context (Pitfall 4 connection ceiling).
  </action>
  <verify>
    <automated>./gradlew :backend:api:compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - `CrossAccountIsolationTest.java` exists, extends `ApiPostgresTestBase`, uses a single `@SpringBootTest` context + `RestClient` + `@LocalServerPort`
    - Test methods cover 404 not-owned, 409 disconnected, active-only read, executing-mailbox send (RED at runtime until Wave 4 via string endpoint paths, not compile-time type references)
    - Class Javadoc references the Phase 10 CR-01 shim gap and AUD-06
    - `./gradlew :backend:api:compileTestJava` exits 0 with ZERO compilation errors (HIGH-1)
  </acceptance_criteria>
  <done>A single-context two-CONNECTED-mailbox isolation harness exists, addressable as `*CrossAccountIsolation*`, ready for Wave 3/4 to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (mailbox-scoped endpoints) | client may supply a crafted `gmailConnectionId`; ownership must be re-validated server-side |
| test fixture → DB | fixture seeds two CONNECTED mailboxes; tests must prove DB-level isolation, not L1-cache illusions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Elevation/Info disclosure | crafted gmailConnectionId reading another mailbox | mitigate | CrossAccountIsolationTest asserts 404 not-owned via resolveOwnedConnectionOrThrow seam |
| T-11-01-02 | Tampering | same gmail_message_id collides across mailboxes, silently dropping mail | mitigate | ObservedMailboxPkTest asserts TWO rows after gmail_connection_id enters the PK |
| T-11-01-03 | Info disclosure | tests assert against L1 cache, masking a real cross-account leak | mitigate | persistAndFlush + em.clear() before conflict/read assertions (Pitfall 5) |
| T-11-01-04 | Tampering | findByTenantId shim silently routes a mailbox-scoped write to primary | mitigate | NEW ArchUnit rule forbids findByTenantId in mailbox-scoped packages with a draining allow-list |
| T-11-01-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs this plan (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `./gradlew :backend:core:test --tests "*GmailClientLookupBoundary*"` green (both rules)
- `./gradlew :backend:core:compileTestJava` AND `./gradlew :backend:api:compileTestJava` BOTH exit 0 with ZERO compilation errors (HIGH-1 — no compile-broken seam; future symbols reached via reflection/information_schema/string-path probes)
- Each test class Javadoc names its acceptance contract (the runtime assertion it waits on) + its compile-green probe mechanism
</verification>

<success_criteria>
- All 8 test files + fixture exist with documented RED acceptance contracts
- `compileTestJava` is GREEN for both `:backend:core` and `:backend:api` (HIGH-1 — Wave 0 leaves no compile-broken seam)
- Two ArchUnit boundary rules present (buildClientForTenant + findByTenantId), both green with non-empty draining allow-lists
- Cross-account isolation harness uses a single @SpringBootTest context (respects the ~15-context ceiling)
- VALIDATION.md Wave 0 requirements satisfied; nyquist spine in place for Waves 1-4
</success_criteria>

<output>
Create `.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-01-SUMMARY.md` when done. Record which referenced production symbols are still RED (the acceptance contract Waves 1-4 must satisfy) and the current contents of both ArchUnit allow-lists.
</output>
