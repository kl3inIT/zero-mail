---
phase: 11-mailbox-scoped-ingestion-automation-ui-and-verification
plan: 04
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleService.java
  - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleRepository.java
  - backend/core/src/main/java/com/zeromail/core/rules/usecases/CopyRulesService.java
  - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleManagementService.java
  - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleTemplateMaterializationService.java
  - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageOrchestratorService.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageDispatchContext.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java
  - backend/core/src/main/java/com/zeromail/core/outbound/usecases/GmailOutboundSendGateway.java
  - backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendCommand.java
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/usecases/UnsubscribeMailtoSender.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageUndoService.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java
  - backend/core/src/main/java/com/zeromail/core/outbound/usecases/ForwardMessageAssembler.java
  - backend/core/src/main/java/com/zeromail/core/draft/usecases/DraftReplySourceLoader.java
  - backend/core/src/main/java/com/zeromail/core/draft/usecases/ToneContextBuilder.java
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05, AUTO-06, AUD-01, AUD-02, AUD-03, AUD-07]
must_haves:
  truths:
    - "Runtime triage evaluates ONLY rules WHERE gmail_connection_id = source mailbox AND enabled = true"
    - "Copy-rules clones When/Then (matcher_ast + action_intents) into the target mailbox with enabled = false for user review"
    - "Triage dispatch carries source + executing mailbox; all Gmail label/archive/draft/star/spam/digest actions resolve via buildClientForMailbox"
    - "Outbound send executes under the executing MailboxRef on the command; ALL three callers (TriageAuditSaga, UnsubscribeMailtoSender, AssistantSendExecutor) populate it; undo/revert (TriageUndoService) targets the same mailbox via triage_audit.executing_mailbox_id; triage_audit records source + executing mailbox"
    - "Sender safety-net stays tenant-owned and records triggering-mailbox metadata without leaking content"
    - "Forward/draft SOURCE reads (ForwardMessageAssembler original-message fetch, DraftReplySourceLoader thread fetch, ToneContextBuilder sent-history fetch) resolve via buildClientForMailbox(executing/source MailboxRef), NOT buildClientForTenant — the source read and the send/draft both use the SAME mailbox, closing the wrong-mailbox read path (HIGH-4 cycle-2)"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/rules/usecases/CopyRulesService.java"
      provides: "Bulk copy-rules action (D-04) cloning When/Then into a target mailbox, enabled=false"
      contains: "enabled"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageDispatchContext.java"
      provides: "sourceMailboxId + executingMailboxId carried through triage"
      contains: "MailboxId"
    - path: "backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendCommand.java"
      provides: "Executing MailboxRef on the send command"
      contains: "MailboxRef"
  key_links:
    - from: "RuleRepository runtime load"
      to: "rules.gmail_connection_id"
      via: "WHERE gmail_connection_id = :sourceMailbox AND enabled = true"
      pattern: "gmail_connection_id|GmailConnectionId"
    - from: "GmailOutboundSendGateway"
      to: "GmailApiClientFactory.buildClientForMailbox"
      via: "command.mailboxRef()"
      pattern: "buildClientForMailbox"
    - from: "TriageGmailWriter"
      to: "GmailApiClientFactory.buildClientForMailbox"
      via: "executing MailboxRef from dispatch context"
      pattern: "buildClientForMailbox"
---

<objective>
Make automation mailbox-owned end to end: rules belong to one mailbox (AUTO-01/02), runtime triage evaluates only the source mailbox's rules (AUTO-04), preview/test sample only the owning mailbox (AUTO-03), Gmail write actions + outbound send resolve against the executing mailbox via `buildClientForMailbox` (AUTO-05/06), the audit saga records source + executing mailbox with mailbox-aware idempotency (AUD-01/02), and the sender safety-net stays tenant-owned with triggering-mailbox metadata (AUD-03). Add the explicit bulk copy-rules action (D-04).

Purpose: This is the automation half of the runtime threading, parallel to ingestion (Plan 03) but in disjoint packages (rules/triage/outbound). It migrates `TriageGmailWriter` and `GmailOutboundSendGateway` off `buildClientForTenant` onto `buildClientForMailbox`; their ArchUnit allow-list entries are drained by Plan 05 (which owns the shared boundary test), so this plan and Plan 03 stay file-disjoint in Wave 3.
Output: Mailbox-owned rules + copy-rules, mailbox-carrying triage dispatch, mailbox-aware writes/sends, mailbox-provenance audit.
</objective>

<artifacts_this_phase_produces>
NEW symbols (exclude from drift verification):
- `CopyRulesService` (NEW — D-04 bulk copy)
- `TriageDispatchContext.sourceMailboxId` + `executingMailboxId` (NEW fields, or NEW record if not present)
- `OutboundSendCommand.mailboxRef` (NEW field — executing MailboxRef; new record arity `(tenantId, mailboxRef, gmailMessage)`)
- Migrated callers: `TriageAuditSaga`, `UnsubscribeMailtoSender`, `AssistantSendExecutor` (transitional primary-shim MailboxRef), `TriageUndoService` (executing-mailbox-from-audit-row resolution)
- `RuleRepository` mailbox-scoped runtime load method (NEW)
- `RuleCreateCommand` / `RuleUpdateCommand` structured `gmailConnectionId` arg (NEW)
- Migrated `new RuleEntity(...)` call sites in `RuleManagementService` + `RuleTemplateMaterializationService` to the mailbox-carrying constructor; REMOVED the transitional OLD-arity `RuleEntity` constructor Plan 02 opened (HIGH-1 cycle-2)
- `TriageAuditWriter` source/executing mailbox params (NEW)
- copy-rules application service entry consumed by the rules UI in Plan 06
- Migrated read-side source loaders: `ForwardMessageAssembler`, `DraftReplySourceLoader`, `ToneContextBuilder` (source MIME/thread/sent-history reads switched from `buildClientForTenant` to `buildClientForMailbox`; their `ALLOWED_TENANT_LOOKUP_CALLERS` entries are drained by Plan 05 — HIGH-4 cycle-2 wrong-mailbox reads)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mailbox-owned rules runtime load + copy-rules (AUTO-01/02/03/04, D-04)</name>
  <files>backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleService.java, backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleRepository.java, backend/core/src/main/java/com/zeromail/core/rules/usecases/CopyRulesService.java, backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleManagementService.java, backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleTemplateMaterializationService.java, backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java</files>
  <behavior>
    - Runtime rule load returns only rules WHERE gmail_connection_id = :sourceMailbox AND enabled = true (rules from another mailbox never evaluate)
    - Rule create/manual-edit persists the owning gmailConnectionId as structured input; sourceText stays metadata only (no NL inference of mailbox)
    - Copy-rules clones matcher_ast + action_intents JSONB from source mailbox into target mailbox with enabled = false; duplicate template_key per target mailbox is allowed (widened unique index from Plan 02)
    - Preview/test sampling is scoped to the owning mailbox and reports the active mailbox context
  </behavior>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java (gmailConnectionId field + transitional OLD-arity constructor added in Plan 02; this plan migrates the call sites to the mailbox-carrying constructor and REMOVES the transitional constructor; replaceDefinition lines ~172-189 reused by copy)
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleRepository.java (existing tenant-scoped load methods to extend with a mailbox-scoped runtime query)
    - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleService.java (create/edit/preview/test entry points — add gmailConnectionId as structured input)
    - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleManagementService.java (line ~72-84 `new RuleEntity(...)` in create(RuleCreateCommand) — migrate to the mailbox-carrying constructor passing the structured gmailConnectionId from the command)
    - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleTemplateMaterializationService.java (line ~193-205 `new RuleEntity(...)` in materializeTemplateOnce — first-login default-rule seeding; pass the tenant's PRIMARY mailbox as the owning gmailConnectionId via the GmailConnectionRepository primary seam)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md (§ RuleEntity + 12x-rules-mailbox-ownership — runtime load WHERE gmail_connection_id = :sourceMailbox; copy clones JSONB enabled=false)
    - CLAUDE.md (rule compiler AI boundary: gmailConnectionId is structured input, NOT inferred from NL; backend only validates schema/safety/bounds)
    - backend/core/src/test/java/com/zeromail/core/rules/MailboxOwnedRulesRuntimeTest.java (Plan 01 RED test this task turns green)
  </read_first>
  <action>
    Add a mailbox-scoped runtime load to `RuleRepository`: `findEnabledByTenantIdAndGmailConnectionId(...)` (or extend the existing runtime loader) returning only `WHERE gmail_connection_id = :sourceMailbox AND enabled = true`. In `RuleService`, accept `gmailConnectionId` as a structured argument on create/manual-edit/preview/test paths (the compiler MUST NOT infer it from natural language — `sourceText` stays metadata); scope preview/test sampling to the owning mailbox and include the active mailbox context in results. Create `CopyRulesService` exposing a bulk `copyRules(UUID sourceMailboxId, UUID targetMailboxId)` that loads the source mailbox's rules and clones each rule's `matcher_ast` + `action_intents` JSONB into the target mailbox with `enabled = false` (reuse `RuleEntity.replaceDefinition(...)`), ownership re-validated for both mailboxes. Default `enabled = false` so the user reviews before activation (D-04); never create an all-mailbox runtime rule. NOTE: this plan creates the CORE `CopyRulesService` only; its backend API surface (controller `POST /api/rules/copy` + `CopyRulesRequest`/`CopyRulesResponse` DTOs + OpenAPI) is added in Plan 05 Task 3 (HIGH-4), and the frontend consumes it in Plan 06. Also make `RuleCreateCommand`/`RuleUpdateCommand` accept the structured `gmailConnectionId` arg (the controller in Plan 05 passes it through) — keep it structured input, NOT inferred from `sourceText`.

    **HIGH-1 cycle-2 — close the transitional RuleEntity constructor seam Plan 02 opened:** Plan 02 added a transitional OLD-arity `RuleEntity` constructor (defaulting `gmailConnectionId = null`) so the two construction sites compiled in Wave 2. This plan migrates BOTH sites to the mailbox-carrying constructor and DELETES the transitional constructor so the null-mailbox seam never reaches runtime (the `rules.gmail_connection_id` column is NOT NULL post-changeset-126, so a null insert would fail): (1) `RuleManagementService.create(RuleCreateCommand)` (line ~72) — pass `command.gmailConnectionId()` (the structured arg now on `RuleCreateCommand`) into the new constructor; (2) `RuleTemplateMaterializationService.materializeTemplateOnce(...)` (line ~193, first-login default-rule seeding) — resolve the tenant's PRIMARY mailbox (inject `GmailConnectionRepository` and use the `findByTenantId` primary seam, or accept the owning `gmailConnectionId` from the materialization entry point) and pass it. After both call sites use the new constructor, remove the `@deprecated` transitional `RuleEntity` constructor. `:backend:core` `compileJava` + `compileTestJava` MUST stay GREEN (HIGH-1).
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*MailboxOwnedRules*"</automated>
  </verify>
  <acceptance_criteria>
    - `RuleRepository` runtime load filters by `gmail_connection_id` + `enabled = true`
    - `RuleService` accepts `gmailConnectionId` as structured input; no NL-derived mailbox inference
    - `CopyRulesService.java` clones into the target mailbox with `enabled = false`
    - `RuleManagementService` + `RuleTemplateMaterializationService` `new RuleEntity(...)` sites use the mailbox-carrying constructor (structured gmailConnectionId / tenant-primary for seeding); the transitional OLD-arity `RuleEntity` constructor from Plan 02 is REMOVED (HIGH-1 cycle-2 — null-mailbox seam never ships)
    - `:backend:core` `compileJava` + `compileTestJava` GREEN after the constructor migration + transitional-ctor removal
    - `MailboxOwnedRulesRuntimeTest` green: a rule owned by mailbox B does not evaluate for mailbox A
  </acceptance_criteria>
  <done>Rules are mailbox-owned at runtime and in CRUD/preview; copy-rules gives an explicit, review-gated cross-mailbox reuse path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mailbox-carrying triage dispatch + mailbox-aware Gmail write actions (AUTO-04/05, AUD-01/03)</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageOrchestratorService.java, backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageDispatchContext.java, backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java</files>
  <behavior>
    - TriageDispatchContext carries sourceMailboxId + executingMailboxId derived from the MailMessageObserved event's gmailConnectionId
    - Orchestrator loads rules for the source mailbox and rebinds MailboxContext (+ TenantContext) on the async path
    - TriageGmailWriter resolves label/archive/draft/read/star/spam/digest actions via buildClientForMailbox(executing MailboxRef), never the tenant shim
    - TriageAuditWriter persists source_mailbox_id + executing_mailbox_id; idempotency hash includes the executing mailbox
    - Sender safety-net lookup stays tenant-owned but records triggering-mailbox metadata without content
  </behavior>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageOrchestratorService.java (the @ApplicationModuleListener consuming MailMessageObserved — now carries gmailConnectionId from Plan 02; rebind pattern)
    - backend/core/src/main/java/com/zeromail/core/tenant/TenantContext.java (runWith(UUID, Runnable) async rebind — mirror for MailboxContext.runWith in Plan 05; for THIS plan, if MailboxContext is not yet built, pass executing MailboxRef explicitly through TriageGmailWriter args and document the seam for Plan 05)
    - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java (the sole triage Gmail write adapter — currently on the buildClientForTenant allow-list; switch to buildClientForMailbox(MailboxRef))
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java (the validation/canonicalization seam before native triage_audit inserts; add source/executing mailbox params; tenant_protected_sender_observation records triggering mailbox metadata)
    - backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java (buildClientForMailbox lines 107-128)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md (§ System Architecture Diagram triage block; § Pitfall 2 shim)
  </read_first>
  <action>
    Add `sourceMailboxId` + `executingMailboxId` to `TriageDispatchContext` (extend the record/class), populated from the `MailMessageObserved.gmailConnectionId` (Plan 02). In `TriageOrchestratorService`, load enabled rules via the Plan-04-Task-1 mailbox-scoped query for the source mailbox, and pass the executing `MailboxRef` down to writes. Change `TriageGmailWriter` to resolve every Gmail action (label/archive/draft/read-unread/star/spam/digest) through `buildClientForMailbox(new MailboxRef(tenantId, executingMailboxId))` instead of `buildClientForTenant`; keep it send-free (sends go through the outbound gateway in Task 3). In `TriageAuditWriter`, add `source_mailbox_id` + `executing_mailbox_id` to the native insert (columns from Plan 02) and key the idempotency ON-CONFLICT target on the EXACTLY-NAMED `executing_mailbox_id` column matching the Plan 02 `ux_triage_audit_idem (tenant_id, executing_mailbox_id, gmail_message_id, rule_id, action_type, args_hash)` index (HIGH-3 — there is NO `gmail_connection_id` column on triage_audit; the dedup axis is `executing_mailbox_id`); the sender safety-net / `tenant_protected_sender_observation` stays tenant-owned but records triggering-mailbox metadata with NO content. DO NOT edit `GmailClientLookupBoundaryTest.java` or remove `TriageGmailWriter` from the `buildClientForTenant` (`ALLOWED_TENANT_LOOKUP_CALLERS`) allow-list in this plan: although `TriageGmailWriter` no longer calls the shim after this change, its allow-list entry is intentionally drained by Plan 05 (the single owner of the shared boundary test). This keeps Wave 3 Plans 03 + 04 disjoint in `files_modified` so they cannot collide on the shared ArchUnit test during a merge; the entry stays until Plan 05, so the rule does not go red (an allow-listed-but-no-longer-calling class is permitted, not required).
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*MailboxOwnedRules*"</automated>
  </verify>
  <acceptance_criteria>
    - `TriageDispatchContext` carries `sourceMailboxId` + `executingMailboxId`
    - `TriageGmailWriter.java` calls `buildClientForMailbox` and no longer `buildClientForTenant`
    - `TriageAuditWriter` inserts `source_mailbox_id` + `executing_mailbox_id` and its ON-CONFLICT idempotency target names `executing_mailbox_id` (matches Plan 02 `ux_triage_audit_idem`; no `gmail_connection_id` on triage_audit — HIGH-3)
    - Sender safety-net stays tenant-owned with triggering-mailbox metadata, no content leak
    - `GmailClientLookupBoundaryTest.java` is NOT modified by this plan (verify `git diff` shows it untouched); the `TriageGmailWriter` allow-list entry is intentionally drained by Plan 05
  </acceptance_criteria>
  <done>Triage dispatch carries mailbox provenance, Gmail write actions execute under the executing mailbox, audit records source + executing mailbox with mailbox-aware idempotency; the writer no longer calls the shim, with its allow-list entry left for Plan 05 to drain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Outbound send gateway routes by executing mailbox; undo targets the same mailbox (AUTO-06/AUD-02)</name>
  <files>backend/core/src/main/java/com/zeromail/core/outbound/usecases/GmailOutboundSendGateway.java, backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendCommand.java</files>
  <behavior>
    - OutboundSendCommand carries an executing MailboxRef
    - The single send gateway builds the Gmail client via buildClientForMailbox(command.mailboxRef()), preserving @AllowedSendCallSite and the InvalidGrant/IllegalState → OutboundSendException mapping
    - With two CONNECTED mailboxes, a send executes under exactly the command's executing mailbox (no >1-connected crash)
    - Undo/revert resolves the same executing mailbox as the original send
    - Blocked/failed outbound outcomes are recorded as failed audits and do NOT fall back to a Gmail draft (product decision)
  </behavior>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/outbound/usecases/GmailOutboundSendGateway.java (read all ~44 lines — line 28 buildClientForTenant(command.tenantId()) to replace; @AllowedSendCallSite + mapping lines 32-34)
    - backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendCommand.java (record (tenantId, gmailMessage) — add executing MailboxRef as a SECOND field)
    - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java (line 213 `new OutboundSendCommand(command.tenantId(), gmailMessage)` — rule-triggered send; executing mailbox = the triage dispatch executing MailboxRef, available in THIS plan)
    - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java (line 105 `new OutboundSendCommand(command.tenantId(), gmailMessage)` — chat-confirmed send; executing mailbox = the user's ACTIVE mailbox, which only exists after Plan 05's ActiveMailboxResolver/MailboxContext — see transitional-path note in the action)
    - backend/core/src/main/java/com/zeromail/core/cleanup/usecases/UnsubscribeMailtoSender.java (line 119 `new OutboundSendCommand(tenantId, gmailMessage)` — cleanup send-as-self; executing mailbox = the cleanup's source mailbox)
    - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageUndoService.java (undo/revert — must resolve the SAME executing mailbox as the original action, read from the audit row's executing_mailbox_id, AUD-02)
    - backend/core/src/main/java/com/zeromail/core/gmail/gateway/MailboxRef.java + GmailApiClientFactory.java:107-128 (buildClientForMailbox)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md (§ Pitfall 6 outbound still using buildClientForTenant; § Security Domain "Outbound send from wrong mailbox")
    - CLAUDE.md (all Gmail send goes through the single OutboundSendGateway; blocked/failed → failed audit, never a surprise draft)
    - backend/core/src/test/java/com/zeromail/core/outbound/OutboundMailboxRoutingTest.java (Plan 01 RED test this task turns green)
  </read_first>
  <action>
    Add an executing `MailboxRef mailboxRef` field to `OutboundSendCommand` (now `(UUID tenantId, MailboxRef mailboxRef, Message gmailMessage)`). In `GmailOutboundSendGateway.send`, replace `gmailApiClientFactory.buildClientForTenant(command.tenantId())` with `buildClientForMailbox(command.mailboxRef())`. Keep `@AllowedSendCallSite` and the `InvalidGrantException | IllegalStateException → OutboundSendException` mapping. Preserve the product rule: a blocked/failed outbound action records a failed audit with reason and does NOT create a Gmail draft.

    **HIGH-5 — enumerate and migrate ALL THREE OutboundSendCommand call sites + the undo path, with a compile-green transitional path so this plan does NOT depend on Plan 05's MailboxContext (which lands in Wave 4):**
    1. `TriageAuditSaga` (line 213, rule-triggered send) — populate `mailboxRef` from the triage dispatch EXECUTING `MailboxRef` (the `executingMailboxId` added to `TriageDispatchContext` in Task 2 of THIS plan). Fully available now; no transitional value.
    2. `UnsubscribeMailtoSender` (line 119, cleanup send-as-self) — populate `mailboxRef` from the cleanup's source mailbox (the `gmailConnectionId` of the message being unsubscribed). Available now.
    3. `AssistantSendExecutor` (line 105, chat-confirmed send) — the correct executing mailbox is the user's ACTIVE mailbox, which is only resolvable after Plan 05 builds `ActiveMailboxResolver`/`MailboxContext` (Wave 4). TRANSITIONAL PATH (keeps Wave 3 compile-green and correct for the single-mailbox status quo): construct the `MailboxRef` from the tenant's PRIMARY mailbox via the existing `GmailConnectionRepository.findByTenantId` primary shim, and add a `// TODO(Plan 05): replace primary-shim MailboxRef with MailboxContext.currentOrThrow() once the active-mailbox filter binds` comment. Plan 05 (which lists `AssistantSendExecutor` is NOT in its files — so add it there) swaps this to the active mailbox. **To make this explicit and ordered, add `AssistantSendExecutor` to Plan 05's `files_modified` and its Task 2 scope** (this plan documents the seam; Plan 05 closes it). Because `AssistantSendExecutor` legitimately uses the primary shim transitionally, it stays on the `ALLOWED_TENANT_LOOKUP_CALLERS` / `ALLOWED_PRIMARY_SHIM_CALLERS` allow-list until Plan 05 migrates it — note this in the SUMMARY.
    4. `TriageUndoService` (undo/revert) — resolve the SAME executing mailbox as the original action by reading `executing_mailbox_id` off the `triage_audit` row (column added in Plan 02), build the `MailboxRef` from it, and route the undo's Gmail call through the executing mailbox (AUD-02). No active-mailbox dependency — fully available now.

    DO NOT edit `GmailClientLookupBoundaryTest.java` or remove `GmailOutboundSendGateway` from the `buildClientForTenant` (`ALLOWED_TENANT_LOOKUP_CALLERS`) allow-list in this plan: although the gateway no longer calls the shim after this change, its allow-list entry is intentionally drained by Plan 05 (the single owner of the shared boundary test), keeping Wave 3 Plans 03 + 04 file-disjoint. The entry stays until Plan 05, so the boundary rule does not go red. **`:backend:core` `compileJava` + `compileTestJava` MUST be GREEN at the end of this plan (HIGH-1) — every OutboundSendCommand caller is updated to the new arity, including the transitional chat path.**
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*OutboundMailbox*"</automated>
  </verify>
  <acceptance_criteria>
    - `OutboundSendCommand` carries an executing `MailboxRef`
    - `GmailOutboundSendGateway.java` calls `buildClientForMailbox(command.mailboxRef())`, not `buildClientForTenant`; `@AllowedSendCallSite` + exception mapping preserved
    - ALL THREE callers updated to the new arity: `TriageAuditSaga` (triage executing mailbox), `UnsubscribeMailtoSender` (cleanup source mailbox), `AssistantSendExecutor` (transitional primary-shim MailboxRef + TODO(Plan 05))
    - `TriageUndoService` resolves the executing mailbox from the `triage_audit.executing_mailbox_id` row and undo targets the same mailbox (AUD-02)
    - `:backend:core` `compileJava` + `compileTestJava` GREEN (HIGH-1) — no caller left at the old arity
    - `AssistantSendExecutor` added to Plan 05 Task 2 scope to swap its transitional primary-shim MailboxRef to MailboxContext (ordering: active-mailbox-dependent migration after MailboxContext exists)
    - `GmailClientLookupBoundaryTest.java` is NOT modified by this plan (verify `git diff` shows it untouched); the `GmailOutboundSendGateway` allow-list entry is intentionally drained by Plan 05
  </acceptance_criteria>
  <done>Outbound send and undo execute under the command's executing mailbox; the single-gateway + failed-audit (no draft fallback) invariants are preserved; the gateway no longer calls the shim, with its allow-list entry left for Plan 05 to drain.</done>
</task>

<task type="auto">
  <name>Task 4: Migrate forward/draft SOURCE reads off the tenant primary shim to the executing mailbox (HIGH-4 cycle-2 wrong-mailbox reads, AUD-02/AUTO-05/06)</name>
  <files>backend/core/src/main/java/com/zeromail/core/outbound/usecases/ForwardMessageAssembler.java, backend/core/src/main/java/com/zeromail/core/draft/usecases/DraftReplySourceLoader.java, backend/core/src/main/java/com/zeromail/core/draft/usecases/ToneContextBuilder.java, backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/outbound/usecases/ForwardMessageAssembler.java (line ~124-126 `fetchOriginalMimeBytes(UUID tenantId, String sourceMessageId)` calls `buildClientForTenant(tenantId)` to read the ORIGINAL message MIME — this reads from the tenant primary, so a forward of a message that lives in mailbox B is fetched from mailbox A; thread the executing MailboxRef through and call `buildClientForMailbox`)
    - backend/core/src/main/java/com/zeromail/core/draft/usecases/DraftReplySourceLoader.java (line ~44-47 `load(UUID tenantId, String gmailThreadId)` calls `buildClientForTenant(tenantId)` to read the SOURCE thread for a reply draft — same wrong-mailbox hazard; add a MailboxRef/gmailConnectionId param and call `buildClientForMailbox`)
    - backend/core/src/main/java/com/zeromail/core/draft/usecases/ToneContextBuilder.java (line ~221 inner `GmailSentMailSource` calls `buildClientForTenant(tenantId, fetchBudget)` to read SENT history for tone — must read the executing mailbox's sent history, not the primary's; thread the MailboxRef and call the mailbox-scoped client; note the allow-list entry is the nested class `ToneContextBuilder$GmailSentMailSource`)
    - backend/core/src/main/java/com/zeromail/core/draft/usecases/DraftBodyGenerator.java + GenerateThreadDraftService.java (the callers of DraftReplySourceLoader/ToneContextBuilder — they have the source mailbox in context; pass the executing MailboxRef down)
    - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageAuditSaga.java (the forward/draft rule-action path that invokes ForwardMessageAssembler/draft generation — pass the triage executing MailboxRef from TriageDispatchContext, same value used for the OutboundSendCommand in Task 3)
    - backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java (buildClientForMailbox lines 107-128 + any buildClientForMailbox(MailboxRef, fetchBudget) overload for ToneContextBuilder's budgeted read)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-REVIEWS.md (§ "Forward/draft mailbox reads are still wrong-mailbox capable" — the source-message read and the send execution MUST use the same concrete mailbox)
  </read_first>
  <action>
    Migrate the three read-side source loaders so the SOURCE read uses the SAME mailbox as the eventual send/draft, not the tenant primary. (1) `ForwardMessageAssembler.fetchOriginalMimeBytes`: change its signature to accept the executing `MailboxRef` (or `gmailConnectionId`) and call `gmailApiClientFactory.buildClientForMailbox(mailboxRef)` instead of `buildClientForTenant(tenantId)`; thread the value from the assembler's public entry point. (2) `DraftReplySourceLoader.load`: add a `MailboxRef`/`gmailConnectionId` parameter and call `buildClientForMailbox` for the source-thread read. (3) `ToneContextBuilder` inner `GmailSentMailSource`: read the EXECUTING mailbox's sent history via `buildClientForMailbox(mailboxRef, fetchBudget)` (add/confirm the budgeted mailbox overload on `GmailApiClientFactory`) instead of `buildClientForTenant(tenantId, fetchBudget)`. Update the immediate callers (`DraftBodyGenerator`, `GenerateThreadDraftService`, and the `TriageAuditSaga` forward/draft action path) to pass the executing `MailboxRef` — for the triage path this is the `executingMailboxId` already on `TriageDispatchContext` (Task 2); for the chat path it is the same source/active mailbox used by the send. DO NOT edit `GmailClientLookupBoundaryTest.java` in this plan — these three classes (`ForwardMessageAssembler`, `DraftReplySourceLoader`, `ToneContextBuilder$GmailSentMailSource`) stay on `ALLOWED_TENANT_LOOKUP_CALLERS` until Plan 05 drains their entries (single-owner rule keeps Wave 3 plans file-disjoint); once migrated they no longer call the shim, which is permitted-but-not-required until Plan 05. `:backend:core` `compileJava` + `compileTestJava` MUST stay GREEN (HIGH-1) — update every caller to the new signatures. Add `ForwardMessageAssembler`, `DraftReplySourceLoader`, and `ToneContextBuilder$GmailSentMailSource` to the list Plan 05 must drain (record in SUMMARY).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava :backend:core:compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - `ForwardMessageAssembler` reads the original message MIME via `buildClientForMailbox`, not `buildClientForTenant`
    - `DraftReplySourceLoader.load` accepts a mailbox argument and reads the source thread via `buildClientForMailbox`
    - `ToneContextBuilder$GmailSentMailSource` reads sent history via `buildClientForMailbox(mailboxRef, fetchBudget)`, not `buildClientForTenant`
    - All immediate callers (`DraftBodyGenerator`, `GenerateThreadDraftService`, `TriageAuditSaga` forward/draft path) pass the executing `MailboxRef`
    - `:backend:core` `compileJava` + `compileTestJava` GREEN (no caller left at the old signature)
    - `GmailClientLookupBoundaryTest.java` is NOT modified by this plan; the three loaders' allow-list entries are recorded for Plan 05 to drain
  </acceptance_criteria>
  <done>Forward/draft source reads and the send/draft execution now use the SAME executing mailbox, closing the wrong-mailbox read path (HIGH-4 cycle-2); allow-list draining deferred to Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| triage event → Gmail write | executing mailbox must come from the event's gmailConnectionId, not a tenant shim |
| send command → Gmail send | command's executing MailboxRef is the only authority for which mailbox sends |
| copy-rules → target mailbox | ownership of both source + target re-validated; cloned rules land disabled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Spoofing | outbound send executes from the wrong mailbox | mitigate | OutboundSendCommand.mailboxRef + single gateway buildClientForMailbox; OutboundMailboxRoutingTest |
| T-11-04-02 | Elevation | another mailbox's rules evaluate for the source mailbox | mitigate | runtime load WHERE gmail_connection_id = :sourceMailbox AND enabled=true (AUTO-04) |
| T-11-04-03 | Repudiation | audit cannot attribute which mailbox executed an action | mitigate | triage_audit source_mailbox_id + executing_mailbox_id (AUD-01); undo same mailbox (AUD-02) |
| T-11-04-04 | Tampering | copy-rules silently creates an active all-mailbox rule | mitigate | clone with enabled=false; ownership re-validated; never all-mailbox runtime rule (D-04/AUTO-01) |
| T-11-04-05 | Info disclosure | safety-net observation leaks email content | mitigate | tenant-owned safety-net records triggering-mailbox metadata only, no content (AUD-03) |
| T-11-04-06 | Tampering | mailbox-aware idempotency collisions across mailboxes re-execute/dedupe wrongly | mitigate | idempotency key includes executing mailbox (Plan 02 index) |
| T-11-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `./gradlew :backend:core:test --tests "*MailboxOwnedRules*" --tests "*OutboundMailbox*"` green
- `GmailClientLookupBoundaryTest.java` is NOT touched in this plan — the `TriageGmailWriter` + `GmailOutboundSendGateway` allow-list entries are drained by Plan 05; do NOT run/assert against `*GmailClientLookupBoundary*` here
- Grep confirms no `buildClientForTenant` in TriageGmailWriter or GmailOutboundSendGateway
- `mcp__jetbrains__get_file_problems` clean on touched Java files
</verification>

<success_criteria>
- Rules mailbox-owned + copy-rules (AUTO-01..04, D-04)
- Gmail writes + outbound send execute under the executing mailbox (AUTO-05/06)
- Audit records source + executing mailbox, mailbox-aware idempotency, same-mailbox undo (AUD-01/02)
- Safety-net stays tenant-owned with triggering-mailbox metadata (AUD-03)
- TriageGmailWriter + GmailOutboundSendGateway no longer call the shim; their allow-list entries are left for Plan 05 to drain (Wave 3 plans stay file-disjoint)
</success_criteria>

<output>
Create `.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-04-SUMMARY.md` when done. Record: (1) that `TriageGmailWriter` + `GmailOutboundSendGateway` no longer call the shim and their `ALLOWED_TENANT_LOOKUP_CALLERS` entries are left for Plan 05 to drain; (2) the full OutboundSendCommand caller list migrated (`TriageAuditSaga`, `UnsubscribeMailtoSender`, `AssistantSendExecutor`) + `TriageUndoService`; (3) the TRANSITIONAL primary-shim MailboxRef in `AssistantSendExecutor` and that it is added to Plan 05's scope to swap to MailboxContext (HIGH-5 ordering); (4) any MailboxContext rebind seam left for Plan 05 (triage async path); (5) confirmation that `:backend:core` compileJava + compileTestJava are GREEN (HIGH-1).
</output>
</content>
</invoke>
