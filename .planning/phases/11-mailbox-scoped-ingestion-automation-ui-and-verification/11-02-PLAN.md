---
phase: 11-mailbox-scoped-ingestion-automation-ui-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - backend/core/src/main/resources/db/changelog/changes/120-pubsub-delivery-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/121-mail-message-observed-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/122-gmail-inbox-projection-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/123-gmail-inbox-sync-state-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/124-processing-job-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/125-triage-audit-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/126-rules-mailbox-ownership.yaml
  - backend/core/src/main/resources/db/changelog/changes/127-gmail-conn-global-active-email-unique.yaml
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  - backend/core/src/main/java/com/zeromail/core/gmail/event/MailMessageObserved.java
  - backend/core/src/main/java/com/zeromail/core/gmail/event/MailOutboundObserved.java
  - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java
  - backend/core/src/main/java/com/zeromail/core/rules/projection/RuleStatusProjection.java
  - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleManagementService.java
  - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleTemplateMaterializationService.java
autonomous: true
requirements: [WSP-03, ING-02, ING-03, ING-06, AUD-01, AUTO-01, AUTO-02, VER-03]
must_haves:
  truths:
    - "A Gmail account can be CONNECTED in at most ONE tenant at a time (global partial unique index on lower(google_email) WHERE status='CONNECTED'), so Pub/Sub email→tenant resolution is unambiguous (HIGH-2)"
    - "Every downstream mailbox-scoped table carries a NOT NULL gmail_connection_id after nullable→backfill-to-primary→NOT NULL, with no orphaned tenants (HALT precondition)"
    - "mail_message_observed and gmail_inbox_projection PKs include gmail_connection_id so the same Gmail message id in two mailboxes never collides"
    - "triage_audit idempotency index is keyed on executing_mailbox_id (NOT a gmail_connection_id column, which triage_audit does not have); rules template-key unique index includes gmail_connection_id"
    - "Existing tenants' rows backfill to the tenant's primary mailbox deterministically; existing projection ciphertext stays decryptable (AAD untouched)"
  artifacts:
    - path: "backend/core/src/main/resources/db/changelog/changes/121-mail-message-observed-mailbox.yaml"
      provides: "Observed-message PK swap to include gmail_connection_id"
      contains: "gmail_connection_id"
    - path: "backend/core/src/main/resources/db/changelog/changes/126-rules-mailbox-ownership.yaml"
      provides: "Rules gmail_connection_id column + widened template-key unique index"
      contains: "uq_rules_tenant"
    - path: "backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java"
      provides: "RuleEntity.gmailConnectionId ownership field"
      contains: "gmailConnectionId"
  key_links:
    - from: "120-126 changesets"
      to: "db.changelog-master.yaml"
      via: "include file entries"
      pattern: "12[0-6]-.*mailbox"
    - from: "RuleEntity.gmailConnectionId"
      to: "rules.gmail_connection_id column"
      via: "@Column(name = \"gmail_connection_id\")"
      pattern: "gmail_connection_id"
---

<objective>
Land all Phase 11 schema migrations as append-only changesets 120-127 (D-00a deferred these to Phase 11) and thread the new `gmail_connection_id` axis onto the in-core domain events and the `RuleEntity`. Each downstream table gets the nullable → backfill-to-primary → NOT NULL + PK/index-swap shape mirroring changeset 119, with HALT preconditions and explicit rollback (CLAUDE.md rule 10). The inbox-projection AAD is NOT touched (D-00b — AAD stays tenant-based; only the PK/indexes change).

Purpose: Foundation for every Wave 2/3 runtime change. After this plan the schema can represent two mailboxes per tenant without id collisions, rules are mailbox-owned, and audit/idempotency disambiguate by mailbox. Backfill points old rows at the tenant's primary mailbox using the exact deterministic ordering Phase 10 uses.
Output: 7 new Liquibase changesets included from master + 3 threaded Java domain artifacts.
</objective>

<artifacts_this_phase_produces>
NEW symbols (exclude from drift verification):
- Changesets `120-pubsub-delivery-mailbox` through `126-rules-mailbox-ownership` (NEW)
- `gmail_connection_id` columns on `pubsub_delivery`, `mail_message_observed`, `gmail_inbox_projection`, `gmail_inbox_sync_state`, `processing_job`, `rules` (NOTE: `triage_audit` does NOT get a `gmail_connection_id` column — its mailbox axes are the dedicated `source_mailbox_id` + `executing_mailbox_id` columns below; HIGH-3)
- `triage_audit.source_mailbox_id` + `triage_audit.executing_mailbox_id` (AUD-01); the idempotency index `ux_triage_audit_idem` is keyed on `executing_mailbox_id` (NOT `gmail_connection_id` — triage_audit has no such column)
- Global active-email unique index `uq_gmail_conn_active_email_global` on `lower(google_email) WHERE status='CONNECTED'` (changeset 127 — HIGH-2 cross-tenant duplicate-grant rejection)
- Widened `uq_rules_tenant_template_key_present` → `(tenant_id, gmail_connection_id, template_key)`
- `RuleEntity.gmailConnectionId` field + constructor parameter + `toStatusProjection()` wiring
- `RuleStatusProjection.gmailConnectionId` record component (new field surfaced by `toStatusProjection()`)
- Updated `new RuleEntity(...)` call sites in `RuleManagementService` and `RuleTemplateMaterializationService` (pass the owning `gmailConnectionId`)
- `MailMessageObserved.gmailConnectionId`, `MailOutboundObserved.gmailConnectionId` event fields
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md
@.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gmail_connection_id to ingestion tables (changesets 120-124, backfill-to-primary, PK swaps)</name>
  <files>backend/core/src/main/resources/db/changelog/changes/120-pubsub-delivery-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/121-mail-message-observed-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/122-gmail-inbox-projection-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/123-gmail-inbox-sync-state-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/124-processing-job-mailbox.yaml, backend/core/src/main/resources/db/changelog/db.changelog-master.yaml</files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/changes/119-gmail-connections-multi-mailbox.yaml (read all 67 lines — HALT precondition + sqlCheck expectedResult:0 + raw sql splitStatements:false + DISTINCT ON deterministic primary pick + explicit rollback — the exact shape to mirror)
    - backend/core/src/main/resources/db/changelog/changes/108-gmail-inbox-projection.yaml (projection table PK + its 3 indexes that must be rebuilt to include gmail_connection_id)
    - backend/core/src/main/resources/db/changelog/changes/081-processing-job-tenant-scope.yaml (processing_job shape + the INBOX_PROJECTION_BACKFILL idempotency_key — RESEARCH A4: suffix it with mailbox so a 2nd mailbox backfill is not deduped away)
    - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml (include-order; add 120-124 in numeric order)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md (§ Code Examples — the 3-changeset add/backfill/notnull-pk split; § Pitfall 1 HALT-on-tenant-without-connection)
  </read_first>
  <action>
    Create five append-only changeset files. Each uses the 119 pattern: `addColumn gmail_connection_id type:uuid` (nullable), then a backfill changeset with `preConditions onFail: HALT` + `sqlCheck expectedResult: 0` (fails if any target tenant has projection/observed/delivery/job/sync rows but ZERO gmail_connections rows), then `addNotNullConstraint` + PK/index swap. Backfill ordering MUST be `DISTINCT ON (tenant_id) ... ORDER BY tenant_id, is_primary DESC, (status='CONNECTED') DESC, connected_at NULLS LAST, id` (matches `findPrimaryMailboxCandidatesByTenantId` shim — Pitfall 1). Per table: `pubsub_delivery` (add column + extend dedup key), `mail_message_observed` (swap PK to `tenant_id, gmail_connection_id, gmail_message_id`), `gmail_inbox_projection` (swap PK to include `gmail_connection_id` AND rebuild its 3 indexes via raw `sql:` — e.g. the list index becomes `(tenant_id, gmail_connection_id, received_at DESC, gmail_message_id DESC) WHERE inbox_state='INBOX'`; do NOT touch `InboxProjectionCipher` AAD — D-00b), `gmail_inbox_sync_state` (PK was `tenant_id` → becomes `(tenant_id, gmail_connection_id)` so cursor is per-mailbox), `processing_job` (add column + suffix the `INBOX_PROJECTION_BACKFILL` idempotency_key with the mailbox id per A4). Every changeset has an explicit roll-forward-safe `rollback`. Use raw `sql: splitStatements: false` for partial/expression indexes. Include each file from `db.changelog-master.yaml` in numeric order. **MEDIUM deployment-locking note (from 11-REVIEWS.md): PK/index swaps on `gmail_inbox_projection`, `mail_message_observed`, and `triage_audit` take ACCESS EXCLUSIVE locks on hot tables and the `processing_job` idempotency-key change affects in-flight rows.** Add the deploy runbook as YAML-native comments — use `#`-prefixed comment lines at the top of the changeset file AND/OR a Liquibase `comment:` key on the changeset (NOT XML-style `<!-- ... -->` blocks, which are invalid inside YAML changelogs and can break parsing — HIGH cycle-2 YAML comment item). Put a `# DEPLOY:`-prefixed comment block at the top of changeset 124 (processing_job) and the projection/observed changesets, and/or set the changeset's `comment:` field, documenting the runbook: (1) PREFLIGHT — before deploy, run a read-only check for open `processing_job` rows (`status IN ('PENDING','PROCESSING')`) and in-flight deliveries; (2) DRAIN — pause/quiesce the worker (stop the SKIP-LOCKED poller) and let Pub/Sub buffer (it retries) so no row is mid-flight against the OLD idempotency key during the swap; (3) APPLY — run the migration during low traffic; new `INBOX_PROJECTION_BACKFILL` jobs use the mailbox-suffixed idempotency key, so any pre-existing open job completes under its old key and new jobs dedupe correctly post-swap; (4) RESUME — restart the worker. State this is operational guidance (not executable SQL) and that the dev VPS single-node deploy makes the drain a short worker-stop. Do NOT attempt `CREATE INDEX CONCURRENTLY` inside a Liquibase transaction (it cannot run transactionally); the brief lock is acceptable for the dev VPS scale per the runbook.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*Migration12*"</automated>
  </verify>
  <acceptance_criteria>
    - Files 120-124 exist and each is `include`d from db.changelog-master.yaml
    - Each backfill changeset has `preConditions: onFail: HALT` + `sqlCheck expectedResult: 0` and the DISTINCT ON ordering with `is_primary DESC, (status='CONNECTED') DESC, connected_at NULLS LAST, id`
    - `mail_message_observed` and `gmail_inbox_projection` PKs include `gmail_connection_id`; sync-state PK is `(tenant_id, gmail_connection_id)`
    - processing_job INBOX_PROJECTION_BACKFILL idempotency_key includes the mailbox id (A4)
    - No edit to changeset 119 or any pre-120 changeset; no change to InboxProjectionCipher
    - The hot-table PK/index-swap changesets + 124 carry the preflight/drain/apply/resume runbook as YAML-native `# DEPLOY:` comment lines and/or a Liquibase `comment:` key — NO XML-style `<!-- ... -->` blocks anywhere in the YAML changelogs (MEDIUM migration-locking item + HIGH cycle-2 YAML comment item)
    - `Migration12xBackfillTest` (from Plan 01) runs against the applied changesets (Liquibase boot succeeds)
  </acceptance_criteria>
  <done>Ingestion-side tables carry NOT NULL gmail_connection_id backfilled to primary, with PKs/indexes/idempotency keys disambiguating by mailbox; projection ciphertext unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Add triage_audit provenance + rules ownership (changesets 125-126) and thread RuleEntity</name>
  <files>backend/core/src/main/resources/db/changelog/changes/125-triage-audit-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/126-rules-mailbox-ownership.yaml, backend/core/src/main/resources/db/changelog/db.changelog-master.yaml, backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java, backend/core/src/main/java/com/zeromail/core/rules/projection/RuleStatusProjection.java, backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleManagementService.java, backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleTemplateMaterializationService.java</files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/changes/086-triage-audit-source.yaml (addColumn + CHECK + partial index + rollback — the shape for source_mailbox_id/executing_mailbox_id)
    - backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml (lines ~127-129 — the idempotency index `ux_triage_audit_idem (tenant_id, gmail_message_id, rule_id, action_type, args_hash) NULLS NOT DISTINCT` to drop + recreate with the `executing_mailbox_id` column added as the new mailbox dedup axis (NOT a `gmail_connection_id` column — triage_audit has none; HIGH-3); keep NULLS NOT DISTINCT for nullable rule_id)
    - backend/core/src/main/resources/db/changelog/changes/021-rules-engine-schema.yaml (lines ~212-217 — `uq_rules_tenant_template_key_present (tenant_id, template_key) WHERE template_key IS NOT NULL` + GIN indexes; widen to include gmail_connection_id per A2)
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java (read all 226 lines — extends AbstractTenantOwnedEntity; existing public constructor lines 75-93 takes (ruleId, tenantId, displayName, sourceText, sourceLanguage, schemaVersion, matcherAst, actionIntents, orderIndex, templateKey, templateVersion); add gmailConnectionId; surface through constructor + toStatusProjection() lines 153-164; replaceDefinition lines ~172-189 reused by copy-rules in Plan 04)
    - backend/core/src/main/java/com/zeromail/core/rules/projection/RuleStatusProjection.java (read all — record with 15 components; add a gmailConnectionId component so toStatusProjection() can surface it)
    - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleManagementService.java (line ~72-84 `new RuleEntity(...)` in create(RuleCreateCommand) — a caller of the constructor that MUST keep compiling; injects only RuleRepository today, NOT GmailConnectionRepository)
    - backend/core/src/main/java/com/zeromail/core/rules/usecases/RuleTemplateMaterializationService.java (line ~193-205 `new RuleEntity(...)` in materializeTemplateOnce — the first-login default-rule seeding caller; injects RuleRepository + OnboardingService + RuleTemplateCatalogService, NOT GmailConnectionRepository)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md (§ RuleEntity + 12x-rules-mailbox-ownership, § 12x-triage-audit-mailbox)
  </read_first>
  <action>
    Changeset 125 (`125-triage-audit-mailbox`): add `source_mailbox_id uuid NOT NULL` + `executing_mailbox_id uuid NOT NULL` columns (mirror 086 addColumn shape: add nullable → backfill → addNotNullConstraint), backfill BOTH to the tenant's primary mailbox (same DISTINCT ON ordering), then DROP and recreate `ux_triage_audit_idem`. **HIGH-3 RESOLUTION (from 11-REVIEWS.md): `triage_audit` has NO `gmail_connection_id` column — the idempotency index MUST use the concrete `executing_mailbox_id` column, NOT the undefined `gmail_connection_id`.** Recreate it as `(tenant_id, executing_mailbox_id, gmail_message_id, rule_id, action_type, args_hash) NULLS NOT DISTINCT` (executing mailbox is the deduplication axis — an action is idempotent per the mailbox that actually executed it, AUD-02). This exact column choice (`executing_mailbox_id`) is the single source of truth that Plan 04's `TriageAuditWriter` idempotency-key construction and Plan 01's `OutboundMailboxRoutingTest` MUST match — state it in the changeset comment and the SUMMARY. Explicit rollback restores the pre-125 `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)` index. Changeset 126 (`126-rules-mailbox-ownership`): add `rules.gmail_connection_id uuid` nullable → backfill-to-primary (same ordering) → NOT NULL; DROP `uq_rules_tenant_template_key_present` and recreate as `(tenant_id, gmail_connection_id, template_key) WHERE template_key IS NOT NULL` (A2 — lets a 2nd mailbox materialize the same default template for UX-05). Then edit `RuleEntity`: add `@Column(name = "gmail_connection_id", nullable = false) private UUID gmailConnectionId;` with a getter, keep the tenant-id inheritance from `AbstractTenantOwnedEntity` (mailbox is a SECOND ownership axis, not a replacement). **COMPILE-GREEN REQUIREMENT (HIGH-1 cycle-2 — RuleEntity constructor/projection compile hole): this task MUST leave `:backend:core` `compileJava` AND `compileTestJava` GREEN.** The change touches THREE coupled symbols that must move together:
    (a) `RuleStatusProjection` — add a `UUID gmailConnectionId` record component (place it right after `ruleId`) so `toStatusProjection()` can surface the owning mailbox; this changes the record's canonical-constructor arity, so update `toStatusProjection()` (RuleEntity lines 153-164) to pass `gmailConnectionId` and grep for any OTHER `new RuleStatusProjection(...)` construction site (currently only RuleEntity.toStatusProjection()) and update it. RuleResponse (api) maps from the projection — Plan 05 Task 3 surfaces `gmailConnectionId` on `RuleResponse`, so leave RuleResponse to Plan 05.
    (b) `RuleEntity` constructor — add `gmailConnectionId` as a parameter on the existing public constructor (lines 75-93) and assign it. Because both `RuleManagementService` and `RuleTemplateMaterializationService` construct `new RuleEntity(...)` and neither injects `GmailConnectionRepository`, DO NOT force a cross-domain repo dependency into the rules usecases in THIS foundation plan. Instead, mirror the Task-3 event pattern: add a TRANSITIONAL old-arity secondary constructor on `RuleEntity` (the exact pre-change parameter list, WITHOUT `gmailConnectionId`) that delegates to the new constructor passing `gmailConnectionId = null`, with a Javadoc `@deprecated` note: "transitional null-mailbox seam; Plan 04 (RuleService/RuleManagementService structured gmailConnectionId) + the default-rule seeding path migrate every caller to the mailbox-carrying constructor and remove this overload." This keeps both existing call sites compiling UNCHANGED in Wave 2.
    (c) The two call sites — leave `RuleManagementService.create(...)` and `RuleTemplateMaterializationService.materializeTemplateOnce(...)` on the transitional constructor for now (they compile unchanged via the old-arity overload). Enumerate them in the SUMMARY as the call sites Plan 04 migrates: Plan 04 Task 1 makes `RuleService`/`RuleManagementService` accept the structured `gmailConnectionId` and pass it to the new constructor; the default-rule seeding (`RuleTemplateMaterializationService`, first-login) owns the tenant's PRIMARY mailbox as the structured value. Plan 04 then removes the transitional `RuleEntity` constructor so the null-mailbox seam never reaches a shipped runtime (the column is NOT NULL post-126, so a null insert would fail — the transitional ctor is compile-only and must be removed before any runtime create).
    Include 125 + 126 from master in numeric order.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*Migration12*" --tests "*RuleEntity*"</automated>
  </verify>
  <acceptance_criteria>
    - 125 + 126 exist and are `include`d from db.changelog-master.yaml
    - triage_audit has NOT NULL `source_mailbox_id` + `executing_mailbox_id` (no `gmail_connection_id` column on triage_audit); `ux_triage_audit_idem` is exactly `(tenant_id, executing_mailbox_id, gmail_message_id, rule_id, action_type, args_hash) NULLS NOT DISTINCT` (HIGH-3 — concrete column, executing axis)
    - rules has NOT NULL `gmail_connection_id` backfilled to primary; template-key unique index is `(tenant_id, gmail_connection_id, template_key)`
    - `RuleEntity.java` contains a NOT NULL `gmailConnectionId` `@Column` + getter surfaced through the constructor + `toStatusProjection()`; still extends AbstractTenantOwnedEntity
    - `RuleStatusProjection` declares a `gmailConnectionId` component and every `new RuleStatusProjection(...)` site (RuleEntity.toStatusProjection()) passes it
    - `RuleEntity` carries a transitional OLD-arity secondary constructor (no `gmailConnectionId`, delegates with null) `@deprecated`-noted for Plan 04 removal, so `RuleManagementService` + `RuleTemplateMaterializationService` compile UNCHANGED
    - `./gradlew :backend:core:compileJava` AND `:backend:core:compileTestJava` BOTH exit 0 with ZERO errors (HIGH-1 cycle-2 — RuleEntity/projection compile hole closed)
    - `./gradlew :backend:core:test --tests "*Migration12*"` boots Liquibase clean
    - SUMMARY enumerates `RuleManagementService` + `RuleTemplateMaterializationService` as the `new RuleEntity(...)` call sites Plan 04 migrates to the mailbox-carrying constructor (and that Plan 04 removes the transitional constructor)
  </acceptance_criteria>
  <done>Triage audit records source + executing mailbox with mailbox-aware idempotency; rules are mailbox-owned with a per-mailbox template-key uniqueness; RuleEntity carries the ownership field.</done>
</task>

<task type="auto">
  <name>Task 3: Thread gmail_connection_id onto the in-core domain events</name>
  <files>backend/core/src/main/java/com/zeromail/core/gmail/event/MailMessageObserved.java, backend/core/src/main/java/com/zeromail/core/gmail/event/MailOutboundObserved.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/event/MailMessageObserved.java (read all — record carries tenantId; place new field after tenantId; keep the never-subject/snippet/body/display-name Javadoc)
    - backend/core/src/main/java/com/zeromail/core/gmail/event/MailOutboundObserved.java (same record shape)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md (§ MailMessageObserved + MailOutboundObserved)
    - CLAUDE.md (rule 6 — these are in-core Modulith events; api↔worker handoff carries the id via Postgres processing_job/pubsub_delivery rows, NOT the event)
  </read_first>
  <action>
    Add `UUID gmailConnectionId` to both records, positioned immediately after `tenantId`. Keep the privacy Javadoc that forbids subject/snippet/body/display-name on the event. Do NOT add any cross-process coupling — the worker reads mailbox id from the Postgres rows (rule 6); the event extension is safe only within backend/core. **COMPILE-GREEN REQUIREMENT (HIGH-1 from 11-REVIEWS.md): this task MUST leave `:backend:core` `compileJava` and `compileTestJava` GREEN — compile-broken intermediate waves are forbidden.** Because a Java record's canonical constructor arity changes, add a TRANSITIONAL compact secondary constructor on each record that preserves the OLD arity (without `gmailConnectionId`) and defaults the field to `null`, so every existing caller — `GmailDeliveryProcessingService`, `BackfillNeedsReplyService`, and the three tests `TriageOutboundRuntimeGateTest` / `ClassifyThreadReplyStatusServiceIntegrationTest` / `TriagePrivacySweepTest` — still compiles unchanged. Document the transitional constructor with a Javadoc `@deprecated` note: "transitional null-mailbox seam; Plan 03 migrates all callers to the mailbox-carrying constructor and removes this overload." Plan 03 (which lists both events + `GmailDeliveryProcessingService` + `BackfillNeedsReplyService` in its `files_modified`) populates the real `gmailConnectionId` at every call site and DELETES the transitional constructor, so the null seam never reaches runtime in a shipped wave. Enumerate the exact caller files in the SUMMARY so Plan 03 closes every one.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava</automated>
  </verify>
  <acceptance_criteria>
    - Both event records declare `UUID gmailConnectionId` after `tenantId`
    - Both records carry a transitional OLD-arity secondary constructor defaulting `gmailConnectionId` to null, `@deprecated`-noted for Plan 03 removal
    - `./gradlew :backend:core:compileJava` AND `:backend:core:compileTestJava` BOTH exit 0 with ZERO errors (HIGH-1 — no compile-broken seam)
    - The never-subject/body privacy Javadoc is preserved on both
    - No cross-process (api↔worker) event coupling introduced
    - The SUMMARY enumerates the exact caller files Plan 03 must migrate (`GmailDeliveryProcessingService`, `BackfillNeedsReplyService`, + the 3 tests) and that Plan 03 removes the transitional constructor
  </acceptance_criteria>
  <done>Both in-core domain events carry the mailbox id, ready for the ingestion path (Plan 03) to populate.</done>
</task>

<task type="auto">
  <name>Task 4: Global active-email uniqueness so Pub/Sub email→tenant resolution is unambiguous (changeset 127, HIGH-2)</name>
  <files>backend/core/src/main/resources/db/changelog/changes/127-gmail-conn-global-active-email-unique.yaml, backend/core/src/main/resources/db/changelog/db.changelog-master.yaml</files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/changes/119-gmail-connections-multi-mailbox.yaml (read all 67 lines — it created `uq_gmail_conn_active_email` as a PER-TENANT partial unique index `(tenant_id, lower(google_email)) WHERE status='CONNECTED'`; THIS is the gap: it permits the SAME Gmail address to be CONNECTED in TWO different tenants, which makes the Pub/Sub `WHERE LOWER(google_email)=? AND status='CONNECTED'` lookup route to the wrong tenant. Mirror 119's HALT precondition + sqlCheck + raw `sql: splitStatements:false` + explicit rollback shape exactly.)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-REVIEWS.md (§ Concern HIGH-2 — pick global active-email uniqueness, per-connection routing, OR explicit rejection of duplicate active grants; this task chooses GLOBAL active-email uniqueness = explicit rejection of duplicate active grants across tenants)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md (§ Code Examples Pub/Sub lookup — note it assumed at-most-one CONNECTED row per email; this changeset makes that assumption true GLOBALLY, not just per tenant)
  </read_first>
  <action>
    Create append-only changeset `127-gmail-conn-global-active-email-unique`. Mirror the 119 shape: a `preConditions onFail: HALT` + `sqlCheck expectedResult: 0` that detects ANY lowercased google_email currently CONNECTED in more than one tenant (`SELECT count(*) FROM (SELECT lower(google_email) FROM gmail_connections WHERE status='CONNECTED' GROUP BY lower(google_email) HAVING count(DISTINCT tenant_id) > 1) d`) so an operator manually disconnects the cross-tenant duplicate before the index is created (do NOT auto-delete another tenant's grant). Then create a GLOBAL partial unique index via raw `sql: splitStatements:false`: `CREATE UNIQUE INDEX uq_gmail_conn_active_email_global ON gmail_connections (lower(google_email)) WHERE status='CONNECTED';`. Keep the existing per-tenant `uq_gmail_conn_active_email` from 119 in place (it is a strict subset — harmless, and documents intent). Explicit `rollback: DROP INDEX IF EXISTS uq_gmail_conn_active_email_global;`. Include 127 from `db.changelog-master.yaml` in numeric order after 126. Add a changeset comment: "HIGH-2 — a Gmail account may be CONNECTED in at most one tenant so Pub/Sub email→tenant resolution is unambiguous; reconnect from a second tenant must first disconnect elsewhere." Document in the SUMMARY that this is the locked HIGH-2 resolution mechanism (explicit rejection of duplicate active grants) that Plan 03's `LIMIT 1` lookup now relies on.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*Migration12*"</automated>
  </verify>
  <acceptance_criteria>
    - `127-gmail-conn-global-active-email-unique.yaml` exists and is `include`d from db.changelog-master.yaml after 126
    - It creates a GLOBAL partial unique index on `lower(google_email) WHERE status='CONNECTED'` (no tenant_id in the index key)
    - It carries a HALT precondition + `sqlCheck expectedResult:0` detecting cross-tenant CONNECTED duplicates (operator-resolve, never auto-delete another tenant's grant)
    - Explicit rollback drops the global index
    - Liquibase boots clean against the dev DB (`*Migration12*` runs)
  </acceptance_criteria>
  <done>The same Gmail account can be CONNECTED in only one tenant, so the Pub/Sub email lookup resolves exactly one tenant + mailbox — the HIGH-2 cross-tenant isolation flaw is closed at the schema level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → existing DB rows | backfill must point every old row at a real, deterministic primary mailbox; ambiguity is a silent cross-account hazard |
| projection ciphertext → cipher | PK change must not alter AAD or existing rows become undecryptable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | backfill picks a non-deterministic / wrong mailbox for old rows | mitigate | DISTINCT ON `is_primary DESC, (status='CONNECTED') DESC, connected_at NULLS LAST, id` mirroring 119/shim |
| T-11-02-02 | DoS/Tampering | tenant with zero gmail_connections rows crashes NOT NULL backfill | mitigate | HALT precondition + sqlCheck expectedResult:0 so an operator investigates instead of a mid-migration crash |
| T-11-02-03 | Info disclosure | changing projection AAD to include mailbox breaks decryption of every existing row | mitigate | D-00b — InboxProjectionCipher AAD untouched; only PK/indexes change (orthogonal) |
| T-11-02-04 | Tampering | same gmail_message_id across mailboxes ON CONFLICT DO NOTHING drops a row | mitigate | gmail_connection_id added to observed/projection PK; triage_audit idempotency index keyed on executing_mailbox_id (HIGH-3) |
| T-11-02-05 | Tampering | 2nd mailbox default-template seed deduped away by tenant-only unique index | mitigate | widen uq_rules_tenant_template_key_present to include gmail_connection_id (A2) |
| T-11-02-06 | Spoofing/Info disclosure | same Gmail account CONNECTED in two tenants → Pub/Sub push routes mail to the wrong tenant (cross-tenant leak) | mitigate | changeset 127 global active-email unique index (HIGH-2) — at most one CONNECTED tenant per email; reconnect elsewhere must first disconnect |
| T-11-02-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `./gradlew :backend:core:test --tests "*Migration12*"` boots Liquibase against the dev DB cleanly (SSH tunnel :5555 must be up — MEMORY note)
- `./gradlew :backend:core:compileJava` AND `:backend:core:compileTestJava` succeed with ZERO errors (HIGH-1 — transitional event constructor keeps every caller compiling; Plan 03 removes it)
- Grep confirms 120-127 all appear in db.changelog-master.yaml
- No diff to changeset 119 or earlier, and no diff to InboxProjectionCipher
</verification>

<success_criteria>
- All eight changesets (120-127) append-only, included from master, with HALT preconditions + explicit rollback
- Changeset 127 gives global active-email uniqueness so Pub/Sub email→tenant is unambiguous (HIGH-2)
- Observed/projection PKs + rules template-key index include gmail_connection_id; triage_audit idempotency index is keyed on executing_mailbox_id (no gmail_connection_id column on triage_audit — HIGH-3)
- Backfill is deterministic-to-primary; projection AAD untouched (existing ciphertext decryptable)
- RuleEntity + both events carry the mailbox id
</success_criteria>

<output>
Create `.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-02-SUMMARY.md` when done. Record the final changeset id range, the LOCKED idempotency-column choice for triage_audit (`executing_mailbox_id` — HIGH-3, the column Plan 04's writer + Plan 01's test must match), the global-active-email-uniqueness changeset id (HIGH-2), and the exact caller files Plan 03 must migrate to remove the transitional event constructor (HIGH-1).
</output>
