---
phase: 11-mailbox-scoped-ingestion-automation-ui-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - backend/core/src/main/resources/db/changelog/changes/120-pubsub-delivery-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/121-mail-message-observed-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/122-gmail-inbox-projection-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/123-gmail-inbox-sync-state-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/124-processing-job-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/125-triage-audit-mailbox.yaml
  - backend/core/src/main/resources/db/changelog/changes/126-rules-mailbox-ownership.yaml
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  - backend/core/src/main/java/com/zeromail/core/gmail/event/MailMessageObserved.java
  - backend/core/src/main/java/com/zeromail/core/gmail/event/MailOutboundObserved.java
  - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java
autonomous: true
requirements: [WSP-03, ING-02, ING-03, ING-06, AUD-01, AUTO-01, AUTO-02, VER-01, VER-03]
must_haves:
  truths:
    - "Every downstream mailbox-scoped table carries a NOT NULL gmail_connection_id after nullable→backfill-to-primary→NOT NULL, with no orphaned tenants (HALT precondition)"
    - "mail_message_observed and gmail_inbox_projection PKs include gmail_connection_id so the same Gmail message id in two mailboxes never collides"
    - "triage_audit idempotency index and rules template-key unique index include gmail_connection_id"
    - "Existing tenants' rows backfill to the tenant's primary mailbox deterministically; existing projection ciphertext stays decryptable (AAD untouched)"
  artifacts:
    - path: "backend/core/src/main/resources/db/changelog/changes/121-mail-message-observed-mailbox.yaml"
      provides: "Observed-message PK swap to include gmail_connection_id"
      contains: "gmail_connection_id"
    - path: "backend/core/src/main/resources/db/changelog/changes/126-rules-mailbox-ownership.yaml"
      provides: "Rules gmail_connection_id column + widened template-key unique index"
      contains: "uq_rules_tenant"
    - path: "backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java"
      provides: "RuleEntity.gmailConnectionId ownership field"
      contains: "gmailConnectionId"
  key_links:
    - from: "120-126 changesets"
      to: "db.changelog-master.yaml"
      via: "include file entries"
      pattern: "12[0-6]-.*mailbox"
    - from: "RuleEntity.gmailConnectionId"
      to: "rules.gmail_connection_id column"
      via: "@Column(name = \"gmail_connection_id\")"
      pattern: "gmail_connection_id"
---

<objective>
Land all Phase 11 schema migrations as append-only changesets 120-126 (D-00a deferred these to Phase 11) and thread the new `gmail_connection_id` axis onto the in-core domain events and the `RuleEntity`. Each downstream table gets the nullable → backfill-to-primary → NOT NULL + PK/index-swap shape mirroring changeset 119, with HALT preconditions and explicit rollback (CLAUDE.md rule 10). The inbox-projection AAD is NOT touched (D-00b — AAD stays tenant-based; only the PK/indexes change).

Purpose: Foundation for every Wave 2/3 runtime change. After this plan the schema can represent two mailboxes per tenant without id collisions, rules are mailbox-owned, and audit/idempotency disambiguate by mailbox. Backfill points old rows at the tenant's primary mailbox using the exact deterministic ordering Phase 10 uses.
Output: 7 new Liquibase changesets included from master + 3 threaded Java domain artifacts.
</objective>

<artifacts_this_phase_produces>
NEW symbols (exclude from drift verification):
- Changesets `120-pubsub-delivery-mailbox` through `126-rules-mailbox-ownership` (NEW)
- `gmail_connection_id` columns on `pubsub_delivery`, `mail_message_observed`, `gmail_inbox_projection`, `gmail_inbox_sync_state`, `processing_job`, `triage_audit`, `rules`
- `triage_audit.source_mailbox_id` + `triage_audit.executing_mailbox_id` (AUD-01)
- Widened `uq_rules_tenant_template_key_present` → `(tenant_id, gmail_connection_id, template_key)`
- `RuleEntity.gmailConnectionId` field + constructor/projection wiring
- `MailMessageObserved.gmailConnectionId`, `MailOutboundObserved.gmailConnectionId` event fields
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md
@.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md
@.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gmail_connection_id to ingestion tables (changesets 120-124, backfill-to-primary, PK swaps)</name>
  <files>backend/core/src/main/resources/db/changelog/changes/120-pubsub-delivery-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/121-mail-message-observed-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/122-gmail-inbox-projection-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/123-gmail-inbox-sync-state-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/124-processing-job-mailbox.yaml, backend/core/src/main/resources/db/changelog/db.changelog-master.yaml</files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/changes/119-gmail-connections-multi-mailbox.yaml (read all 67 lines — HALT precondition + sqlCheck expectedResult:0 + raw sql splitStatements:false + DISTINCT ON deterministic primary pick + explicit rollback — the exact shape to mirror)
    - backend/core/src/main/resources/db/changelog/changes/108-gmail-inbox-projection.yaml (projection table PK + its 3 indexes that must be rebuilt to include gmail_connection_id)
    - backend/core/src/main/resources/db/changelog/changes/081-processing-job-tenant-scope.yaml (processing_job shape + the INBOX_PROJECTION_BACKFILL idempotency_key — RESEARCH A4: suffix it with mailbox so a 2nd mailbox backfill is not deduped away)
    - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml (include-order; add 120-124 in numeric order)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-RESEARCH.md (§ Code Examples — the 3-changeset add/backfill/notnull-pk split; § Pitfall 1 HALT-on-tenant-without-connection)
  </read_first>
  <action>
    Create five append-only changeset files. Each uses the 119 pattern: `addColumn gmail_connection_id type:uuid` (nullable), then a backfill changeset with `preConditions onFail: HALT` + `sqlCheck expectedResult: 0` (fails if any target tenant has projection/observed/delivery/job/sync rows but ZERO gmail_connections rows), then `addNotNullConstraint` + PK/index swap. Backfill ordering MUST be `DISTINCT ON (tenant_id) ... ORDER BY tenant_id, is_primary DESC, (status='CONNECTED') DESC, connected_at NULLS LAST, id` (matches `findPrimaryMailboxCandidatesByTenantId` shim — Pitfall 1). Per table: `pubsub_delivery` (add column + extend dedup key), `mail_message_observed` (swap PK to `tenant_id, gmail_connection_id, gmail_message_id`), `gmail_inbox_projection` (swap PK to include `gmail_connection_id` AND rebuild its 3 indexes via raw `sql:` — e.g. the list index becomes `(tenant_id, gmail_connection_id, received_at DESC, gmail_message_id DESC) WHERE inbox_state='INBOX'`; do NOT touch `InboxProjectionCipher` AAD — D-00b), `gmail_inbox_sync_state` (PK was `tenant_id` → becomes `(tenant_id, gmail_connection_id)` so cursor is per-mailbox), `processing_job` (add column + suffix the `INBOX_PROJECTION_BACKFILL` idempotency_key with the mailbox id per A4). Every changeset has an explicit roll-forward-safe `rollback`. Use raw `sql: splitStatements: false` for partial/expression indexes. Include each file from `db.changelog-master.yaml` in numeric order.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*Migration12*"</automated>
  </verify>
  <acceptance_criteria>
    - Files 120-124 exist and each is `include`d from db.changelog-master.yaml
    - Each backfill changeset has `preConditions: onFail: HALT` + `sqlCheck expectedResult: 0` and the DISTINCT ON ordering with `is_primary DESC, (status='CONNECTED') DESC, connected_at NULLS LAST, id`
    - `mail_message_observed` and `gmail_inbox_projection` PKs include `gmail_connection_id`; sync-state PK is `(tenant_id, gmail_connection_id)`
    - processing_job INBOX_PROJECTION_BACKFILL idempotency_key includes the mailbox id (A4)
    - No edit to changeset 119 or any pre-120 changeset; no change to InboxProjectionCipher
    - `Migration12xBackfillTest` (from Plan 01) runs against the applied changesets (Liquibase boot succeeds)
  </acceptance_criteria>
  <done>Ingestion-side tables carry NOT NULL gmail_connection_id backfilled to primary, with PKs/indexes/idempotency keys disambiguating by mailbox; projection ciphertext unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Add triage_audit provenance + rules ownership (changesets 125-126) and thread RuleEntity</name>
  <files>backend/core/src/main/resources/db/changelog/changes/125-triage-audit-mailbox.yaml, backend/core/src/main/resources/db/changelog/changes/126-rules-mailbox-ownership.yaml, backend/core/src/main/resources/db/changelog/db.changelog-master.yaml, backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java</files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/changes/086-triage-audit-source.yaml (addColumn + CHECK + partial index + rollback — the shape for source_mailbox_id/executing_mailbox_id)
    - backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml (lines ~127-129 — the idempotency index `ux_triage_audit_idem (tenant_id, gmail_message_id, rule_id, action_type, args_hash) NULLS NOT DISTINCT` to drop + recreate with gmail_connection_id added; keep NULLS NOT DISTINCT for nullable rule_id)
    - backend/core/src/main/resources/db/changelog/changes/021-rules-engine-schema.yaml (lines ~212-217 — `uq_rules_tenant_template_key_present (tenant_id, template_key) WHERE template_key IS NOT NULL` + GIN indexes; widen to include gmail_connection_id per A2)
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java (read all 226 lines — extends AbstractTenantOwnedEntity; add gmailConnectionId; surface through constructor + toStatusProjection(); replaceDefinition lines ~172-189 reused by copy-rules in Plan 04)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md (§ RuleEntity + 12x-rules-mailbox-ownership, § 12x-triage-audit-mailbox)
  </read_first>
  <action>
    Changeset 125 (`125-triage-audit-mailbox`): add `source_mailbox_id uuid` + `executing_mailbox_id uuid` columns (mirror 086 addColumn shape), backfill both to the tenant's primary mailbox (same DISTINCT ON ordering), then DROP and recreate `ux_triage_audit_idem` to include `gmail_connection_id` as `(tenant_id, gmail_connection_id, gmail_message_id, rule_id, action_type, args_hash) NULLS NOT DISTINCT` — note AUD-02 idempotency is keyed by EXECUTING mailbox; pick the column consistently and document it. Explicit rollback. Changeset 126 (`126-rules-mailbox-ownership`): add `rules.gmail_connection_id uuid` nullable → backfill-to-primary (same ordering) → NOT NULL; DROP `uq_rules_tenant_template_key_present` and recreate as `(tenant_id, gmail_connection_id, template_key) WHERE template_key IS NOT NULL` (A2 — lets a 2nd mailbox materialize the same default template for UX-05). Then edit `RuleEntity`: add `@Column(name = "gmail_connection_id", nullable = false) private UUID gmailConnectionId;`, surface it through the constructor and `toStatusProjection()`, keep the tenant-id inheritance from `AbstractTenantOwnedEntity` (mailbox is a SECOND ownership axis, not a replacement). Include 125 + 126 from master in numeric order.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*Migration12*" --tests "*RuleEntity*"</automated>
  </verify>
  <acceptance_criteria>
    - 125 + 126 exist and are `include`d from db.changelog-master.yaml
    - triage_audit has `source_mailbox_id` + `executing_mailbox_id`; idempotency index includes a mailbox column with documented choice (executing) + retains NULLS NOT DISTINCT
    - rules has NOT NULL `gmail_connection_id` backfilled to primary; template-key unique index is `(tenant_id, gmail_connection_id, template_key)`
    - `RuleEntity.java` contains `gmailConnectionId` surfaced through constructor + toStatusProjection(); still extends AbstractTenantOwnedEntity
    - `./gradlew :backend:core:test --tests "*Migration12*"` boots Liquibase clean
  </acceptance_criteria>
  <done>Triage audit records source + executing mailbox with mailbox-aware idempotency; rules are mailbox-owned with a per-mailbox template-key uniqueness; RuleEntity carries the ownership field.</done>
</task>

<task type="auto">
  <name>Task 3: Thread gmail_connection_id onto the in-core domain events</name>
  <files>backend/core/src/main/java/com/zeromail/core/gmail/event/MailMessageObserved.java, backend/core/src/main/java/com/zeromail/core/gmail/event/MailOutboundObserved.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/event/MailMessageObserved.java (read all — record carries tenantId; place new field after tenantId; keep the never-subject/snippet/body/display-name Javadoc)
    - backend/core/src/main/java/com/zeromail/core/gmail/event/MailOutboundObserved.java (same record shape)
    - .planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-PATTERNS.md (§ MailMessageObserved + MailOutboundObserved)
    - CLAUDE.md (rule 6 — these are in-core Modulith events; api↔worker handoff carries the id via Postgres processing_job/pubsub_delivery rows, NOT the event)
  </read_first>
  <action>
    Add `UUID gmailConnectionId` to both records, positioned immediately after `tenantId`. Keep the privacy Javadoc that forbids subject/snippet/body/display-name on the event. Do NOT add any cross-process coupling — the worker reads mailbox id from the Postgres rows (rule 6); the event extension is safe only within backend/core. This task intentionally leaves all CALLERS compiling-broken if they don't yet pass the id — that is expected; Plan 03 (ingestion) fills the constructor call sites. If keeping the wave green is required, add the new field with a transitional secondary constructor or update the obvious in-core call sites in `GmailDeliveryProcessingService` minimally; prefer letting Plan 03 own the full ingestion edit and document the compile seam in the SUMMARY.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava</automated>
  </verify>
  <acceptance_criteria>
    - Both event records declare `UUID gmailConnectionId` after `tenantId`
    - The never-subject/body privacy Javadoc is preserved on both
    - No cross-process (api↔worker) event coupling introduced
    - Compile seam (if any callers break) is documented in the SUMMARY for Plan 03 to close
  </acceptance_criteria>
  <done>Both in-core domain events carry the mailbox id, ready for the ingestion path (Plan 03) to populate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → existing DB rows | backfill must point every old row at a real, deterministic primary mailbox; ambiguity is a silent cross-account hazard |
| projection ciphertext → cipher | PK change must not alter AAD or existing rows become undecryptable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | backfill picks a non-deterministic / wrong mailbox for old rows | mitigate | DISTINCT ON `is_primary DESC, (status='CONNECTED') DESC, connected_at NULLS LAST, id` mirroring 119/shim |
| T-11-02-02 | DoS/Tampering | tenant with zero gmail_connections rows crashes NOT NULL backfill | mitigate | HALT precondition + sqlCheck expectedResult:0 so an operator investigates instead of a mid-migration crash |
| T-11-02-03 | Info disclosure | changing projection AAD to include mailbox breaks decryption of every existing row | mitigate | D-00b — InboxProjectionCipher AAD untouched; only PK/indexes change (orthogonal) |
| T-11-02-04 | Tampering | same gmail_message_id across mailboxes ON CONFLICT DO NOTHING drops a row | mitigate | gmail_connection_id added to observed/projection PK + triage_audit idempotency index |
| T-11-02-05 | Tampering | 2nd mailbox default-template seed deduped away by tenant-only unique index | mitigate | widen uq_rules_tenant_template_key_present to include gmail_connection_id (A2) |
| T-11-02-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `./gradlew :backend:core:test --tests "*Migration12*"` boots Liquibase against the dev DB cleanly (SSH tunnel :5555 must be up — MEMORY note)
- `./gradlew :backend:core:compileJava` succeeds (or documented event-caller seam for Plan 03)
- Grep confirms 120-126 all appear in db.changelog-master.yaml
- No diff to changeset 119 or earlier, and no diff to InboxProjectionCipher
</verification>

<success_criteria>
- All seven changesets (120-126) append-only, included from master, with HALT preconditions + explicit rollback
- Observed/projection PKs + triage_audit idempotency + rules template-key index all include gmail_connection_id
- Backfill is deterministic-to-primary; projection AAD untouched (existing ciphertext decryptable)
- RuleEntity + both events carry the mailbox id
</success_criteria>

<output>
Create `.planning/phases/11-mailbox-scoped-ingestion-automation-ui-and-verification/11-02-SUMMARY.md` when done. Record the final changeset id range, the idempotency-column choice (source vs executing) for triage_audit, and any event-caller compile seam left for Plan 03.
</output>
