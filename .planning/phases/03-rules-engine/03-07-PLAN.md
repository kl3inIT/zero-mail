---
phase: 03-rules-engine
plan: "07"
type: execute
wave: 4
depends_on: ["03", "05", "06"]
files_modified:
  - backend/api/src/main/java/com/zeromail/api/controllers/rules/RulesController.java
  - backend/api/src/main/java/com/zeromail/api/dto/rules/RuleDtos.java
  - backend/api/src/main/java/com/zeromail/api/dto/rules/package-info.java
  - backend/api/src/main/java/com/zeromail/api/error/ErrorCodes.java
  - backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java
  - backend/api/src/test/java/com/zeromail/api/controllers/rules/RulesControllerIntegrationTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/rules/RulesControllerTenantIsolationTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/rules/RulesControllerPrivacyTest.java
  - apps/web/openapi/openapi.json
  - apps/web/lib/api/schema.d.ts
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03, RULE-04, RULE-05, RULE-06, RULE-07]
must_haves:
  truths:
    - "RulesController is thin and delegates to core services; no repository injection"
    - "GET /api/rules materializes onboarding templates idempotently before returning ordered rules"
    - "API exposes compile, create, update, enable/disable, reorder, delete, saved preview, draft preview, and template catalog/materialization endpoints"
    - "OpenAPI is regenerated and frontend schema includes every rules endpoint"
    - "API responses contain sanitized preview summaries only, never raw Gmail bodies/headers/snippets/prompts/completions"
  artifacts:
    - path: "backend/api/src/main/java/com/zeromail/api/controllers/rules/RulesController.java"
      provides: "HTTP surface for Phase 3 rules"
      contains: "/api/rules"
    - path: "apps/web/lib/api/schema.d.ts"
      provides: "Generated frontend rules path/schema types"
      contains: "/api/rules"
  key_links:
    - from: "RulesController"
      to: "RuleTemplateMaterializationService"
      via: "GET /api/rules"
      pattern: "materialize"
---

<objective>
Expose the rules backend over a typed API and regenerate OpenAPI for frontend consumption.

Purpose: the frontend should consume generated rules types only, while backend integration tests prove tenant isolation, validation errors, insufficient credits, and privacy-safe preview responses.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-SPEC.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@.planning/phases/03-rules-engine/03-03-SUMMARY.md
@.planning/phases/03-rules-engine/03-05-SUMMARY.md
@.planning/phases/03-rules-engine/03-06-SUMMARY.md
@backend/api/src/main/java/com/zeromail/api/controllers/llm/ByokController.java
@backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java
@backend/api/src/main/java/com/zeromail/api/error/ErrorCodes.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTOs, controller, and error mapping</name>
  <read_first>
    - backend/api/src/main/java/com/zeromail/api/controllers/llm/ByokController.java
    - backend/api/src/main/java/com/zeromail/api/dto/llm/ByokSaveRequest.java
    - backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java
  </read_first>
  <action>
    Add rules DTO records and `RulesController`.

    Endpoints:
    - `GET /api/rules` - materialize onboarding templates idempotently, return ordered rules and template summaries.
    - `GET /api/rules/{ruleId}`
    - `POST /api/rules/compile`
    - `POST /api/rules` - create disabled rule from validated compile payload.
    - `PUT /api/rules/{ruleId}` - edit source/compiled payload and reset preview eligibility.
    - `PATCH /api/rules/{ruleId}/enabled` - enable/disable, with enable requiring successful preview of current version.
    - `PUT /api/rules/reorder`
    - `DELETE /api/rules/{ruleId}`
    - `POST /api/rules/{ruleId}/preview`
    - `POST /api/rules/preview`
    - `GET /api/rules/templates`
    - `POST /api/rules/templates/{templateKey}/materialize`

    Add stable error codes under `error.rules.*` for invalid compile output, ambiguity, not found, preview required, invalid sample size, invalid reorder, unsafe action, and Gmail preview unavailable. Map rule exceptions in `GlobalExceptionHandler` without leaking raw input.
  </action>
  <verify>
    <automated>./gradlew :backend:api:test --tests "com.zeromail.api.controllers.rules.RulesControllerIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - Controller injects services only, never repositories.
    - DTOs are records with Jakarta validation.
    - ProblemDetail bodies contain stable codes and safe params only.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: API integration, tenant isolation, privacy, OpenAPI</name>
  <read_first>
    - backend/api/build.gradle.kts
    - apps/web/scripts/generate-api.ts
    - backend/api/src/test/java/com/zeromail/api/controllers/llm/ByokControllerIntegrationTest.java
  </read_first>
  <action>
    Add integration tests:
    - success paths for compile/create/list/update/preview/enable/disable/reorder/delete/templates.
    - validation errors for unknown actions/nodes, invalid sample sizes, invalid reorder ids, preview-required enablement.
    - insufficient credits from `LlmGateway` compile path maps to 402 billing code.
    - Tenant A cannot read/mutate/preview Tenant B rules.
    - Preview response and captured logs do not include sentinel raw header/body/snippet/prompt/completion strings.

    Regenerate OpenAPI:
    - `./gradlew :backend:api:generateOpenApiDocs`
    - `pnpm --filter web generate:api`
  </action>
  <verify>
    <automated>./gradlew :backend:api:test --tests "com.zeromail.api.controllers.rules.*" && ./gradlew :backend:api:generateOpenApiDocs && pnpm --filter web generate:api</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/api/schema.d.ts` includes all `/api/rules` paths and schemas.
    - Tenant isolation tests use two tenants and prove cross-tenant denial.
    - Privacy tests fail if sentinel content appears in response/logs.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :backend:api:test --tests "com.zeromail.api.controllers.rules.*"`
- `./gradlew :backend:api:generateOpenApiDocs`
- `pnpm --filter web generate:api`
</verification>

<success_criteria>
- Rules API is complete, typed, and privacy-safe.
- Generated frontend schema contains all rules contracts.
- Backend API integration tests cover happy path, validation, credits, and tenant isolation.
</success_criteria>

<output>
Create `.planning/phases/03-rules-engine/03-07-SUMMARY.md` documenting endpoint list, error codes, OpenAPI regeneration path, and test evidence.
</output>
