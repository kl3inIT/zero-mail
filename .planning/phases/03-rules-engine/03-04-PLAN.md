---
phase: 03-rules-engine
plan: "04"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/rules/model/RuleEvaluationInput.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RuleEvaluationResult.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/MatcherEvaluationState.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/ActionProposal.java
  - backend/core/src/main/java/com/zeromail/core/rules/service/RuleEvaluator.java
  - backend/core/src/main/java/com/zeromail/core/rules/service/ActionProposalMerger.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/RuleEvaluatorTest.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/ActionProposalMergerTest.java
autonomous: true
requirements: [RULE-03, RULE-04]
must_haves:
  truths:
    - "Evaluator returns tri-state match results and never returns a bare boolean"
    - "SEMANTIC_INTENT always evaluates as DEFERRED and never calls LlmGateway"
    - "Every deterministic matcher in 03-SPEC has unit coverage"
    - "Duplicate action proposals dedupe while preserving contributing rule ids and evidence"
    - "Conflicts are represented as warnings, not hard blocks"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/rules/service/RuleEvaluator.java"
      provides: "Deterministic matcher evaluator"
      contains: "DEFERRED"
    - path: "backend/core/src/main/java/com/zeromail/core/rules/service/ActionProposalMerger.java"
      provides: "Safe action dedupe and conflict summary"
      contains: "ActionProposal"
  key_links:
    - from: "RuleEvaluator"
      to: "MatcherNode"
      via: "sealed matcher dispatch"
      pattern: "instanceof|switch"
---

<objective>
Implement deterministic matcher evaluation and action proposal merging for preview and future Phase 4 reuse.

Purpose: preview must be deterministic, explainable, and LLM-free. This plan proves the matcher vocabulary works on synthetic preview inputs before Gmail preview fetching is added.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-SPEC.md
@.planning/phases/03-rules-engine/03-AI-SPEC.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@.planning/phases/03-rules-engine/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic evaluator</name>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/model/MatcherNode.java
    - backend/core/src/test/java/com/zeromail/core/rules/service/RuleEvaluatorWave0Test.java
  </read_first>
  <action>
    Create evaluation input/result records and `RuleEvaluator`.

    Input must include only preview-safe fields:
    - sanitized sender email/domain.
    - sanitized recipient emails.
    - sanitized subject excerpt.
    - Gmail label ids/categories.
    - internal date, observed date, attachment/list-unsubscribe/newsletter flags.
    - optional body-evidence-present boolean, not raw body text.

    Evaluate:
    - sender email/domain.
    - recipient to/cc.
    - subject contains/equals/regex.
    - Gmail label/category present/absent.
    - has attachment.
    - list-unsubscribe/newsletter indicators.
    - message age/date.
    - all/any/not boolean groups with tri-state semantics.
    - semantic intent as `DEFERRED` with evidence id, not match true/false.

    Regex safety: reject invalid regex at compile validation; evaluator should handle only prevalidated bounded patterns.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.RuleEvaluatorTest"</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests cover every matcher type.
    - A fake `LlmGateway` that throws is not referenced by evaluator tests.
    - Repeated evaluation over same input is stable.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Action dedupe and conflict warnings</name>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/model/ActionIntent.java
    - .planning/phases/03-rules-engine/03-CONTEXT.md decisions D-D1..D-D4
  </read_first>
  <action>
    Implement action proposal records and merger:
    - Evaluate enabled rules in order, plus the currently previewed disabled rule when requested by preview.
    - Exact duplicate safe actions merge into one proposal.
    - Preserve ordered contributing rule ids, rule names, and matched matcher evidence ids.
    - Detect conflicts such as multiple labels with different labels plus archive/save draft combinations as warnings only.
    - Never include raw subject/body/header content in conflict metadata.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.ActionProposalMergerTest"</automated>
  </verify>
  <acceptance_criteria>
    - Duplicate label/archive/save_draft intents dedupe with provenance.
    - Conflict warning does not block proposal creation.
    - Ordering is deterministic by rule order.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.RuleEvaluatorTest" --tests "com.zeromail.core.rules.service.ActionProposalMergerTest"`
</verification>

<success_criteria>
- Deterministic evaluator is complete for Phase 3 matcher vocabulary.
- Semantic nodes are explicitly deferred.
- Action proposals are explainable and provenance-preserving.
</success_criteria>

<output>
Create `.planning/phases/03-rules-engine/03-04-SUMMARY.md` documenting tri-state semantics and action merge/conflict rules.
</output>
