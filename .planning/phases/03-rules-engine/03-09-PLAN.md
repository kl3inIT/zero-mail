---
phase: 03-rules-engine
plan: "09"
type: execute
wave: 6
depends_on: ["01", "02", "03", "04", "05", "06", "07", "08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/03-rules-engine/03-VERIFICATION.md
  - .planning/phases/03-rules-engine/03-UAT.md
  - .planning/phases/03-rules-engine/03-09-SUMMARY.md
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03, RULE-04, RULE-05, RULE-06, RULE-07]
must_haves:
  truths:
    - "Full backend, API, frontend, i18n, OpenAPI, and Playwright checks pass"
    - "Privacy grep/log assertions show no raw Gmail content, prompts, completions, or tool args persisted/logged"
    - "RULE-01 through RULE-07 are demonstrably covered by implementation and tests before requirements are marked complete"
    - "Phase 4 handoff documents the runtime triage boundary and semantic deferral contract"
  artifacts:
    - path: ".planning/phases/03-rules-engine/03-VERIFICATION.md"
      provides: "Goal-backward verification report for Phase 3"
      contains: "RULE-01"
  key_links:
    - from: "03-VERIFICATION.md"
      to: "REQUIREMENTS.md"
      via: "RULE-01..RULE-07 evidence"
      pattern: "RULE-07"
---

<objective>
Run the Phase 3 closure sweep: full verification, privacy audit, generated contract checks, requirements traceability, and Phase 4 handoff notes.

Purpose: do not mark the rules engine phase complete until the implementation proves the user-facing workflow and the safety boundaries that Phase 4 triage will rely on.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-SPEC.md
@.planning/phases/03-rules-engine/03-AI-SPEC.md
@.planning/phases/03-rules-engine/03-UI-SPEC.md
@.planning/phases/03-rules-engine/03-VALIDATION.md
@.planning/phases/03-rules-engine/03-01-SUMMARY.md
@.planning/phases/03-rules-engine/03-02-SUMMARY.md
@.planning/phases/03-rules-engine/03-03-SUMMARY.md
@.planning/phases/03-rules-engine/03-04-SUMMARY.md
@.planning/phases/03-rules-engine/03-05-SUMMARY.md
@.planning/phases/03-rules-engine/03-06-SUMMARY.md
@.planning/phases/03-rules-engine/03-07-SUMMARY.md
@.planning/phases/03-rules-engine/03-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated verification</name>
  <read_first>
    - .planning/phases/03-rules-engine/03-VALIDATION.md
    - package.json
    - apps/web/package.json
  </read_first>
  <action>
    Run the full phase verification battery:
    - `./gradlew clean check`
    - `./gradlew :backend:api:generateOpenApiDocs`
    - `pnpm --filter web generate:api`
    - `pnpm --filter web lint`
    - `pnpm --filter web typecheck`
    - `pnpm --filter web i18n:check`
    - `pnpm --filter web test`
    - `pnpm --filter web test:e2e -- apps/web/e2e/rules.spec.ts`

    If a failure is unrelated to Phase 3, document evidence and keep Phase 3 checks focused. Do not silently skip browser verification for frontend changes.
  </action>
  <verify>
    <automated>./gradlew clean check && pnpm --filter web lint && pnpm --filter web typecheck && pnpm --filter web i18n:check && pnpm --filter web test</automated>
  </verify>
  <acceptance_criteria>
    - All focused Phase 3 tests pass.
    - OpenAPI generated artifacts are current.
    - Browser rules spec has a recorded pass or a clearly documented environment blocker.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Privacy and architecture closure</name>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/arch/LlmRepositoryContentBanTest.java
    - backend/core/src/test/java/com/zeromail/core/arch/LlmGatewayBoundaryTest.java
    - backend/core/src/test/java/com/zeromail/core/arch/RulesBoundaryArchTest.java
  </read_first>
  <action>
    Run and document privacy/architecture checks:
    - ArchUnit gateway and rules boundary tests.
    - Repository/content ban tests.
    - Source grep for banned durable fields/log strings in `core.rules`, `api.controllers.rules`, and `features/rules`.
    - DB/log sentinel tests from preview.

    Confirm:
    - No Spring AI/vendor imports in `core.rules`.
    - No Gmail write API references in preview services.
    - No raw mail headers/snippets/bodies/prompts/completions/tool args are stored or logged.
    - `SEMANTIC_INTENT` remains deferred and visible.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "LlmGatewayBoundaryTest" --tests "RulesBoundaryArchTest" --tests "LlmRepositoryContentBanTest" --tests "com.zeromail.core.rules.privacy.*"</automated>
  </verify>
  <acceptance_criteria>
    - Privacy and architecture checks are green.
    - Any accepted residual risk is documented with owner and follow-up phase.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Requirements traceability and handoff</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - all Phase 3 summaries
  </read_first>
  <action>
    Create `03-VERIFICATION.md` and `03-UAT.md`:
    - Map RULE-01..RULE-07 to code files, tests, and browser evidence.
    - Include Phase 4 handoff: runtime triage owns live rule application, Gmail writes, audit, undo, shadow mode, sender safety net, and semantic LLM evaluation.
    - Include no-stored-content evidence and template materialization evidence.

    Only after evidence is complete, update:
    - `.planning/REQUIREMENTS.md`: mark RULE-01..RULE-07 complete.
    - `.planning/ROADMAP.md`: Phase 3 plans complete and completion status.
    - `.planning/STATE.md`: current focus moves to Phase 4 planning/execution per GSD state conventions.
  </action>
  <verify>
    <automated>gsd-sdk query check.decision-coverage-plan ".planning/phases/03-rules-engine" ".planning/phases/03-rules-engine/03-CONTEXT.md"</automated>
  </verify>
  <acceptance_criteria>
    - Verification report explicitly covers every RULE requirement and every CONTEXT decision D-A through D-D.
    - ROADMAP/STATE/REQUIREMENTS updates happen only after tests and evidence pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew clean check`
- `pnpm --filter web lint`
- `pnpm --filter web typecheck`
- `pnpm --filter web i18n:check`
- `pnpm --filter web test`
- `pnpm --filter web test:e2e -- apps/web/e2e/rules.spec.ts`
- `gsd-sdk query check.decision-coverage-plan ".planning/phases/03-rules-engine" ".planning/phases/03-rules-engine/03-CONTEXT.md"`
</verification>

<success_criteria>
- Phase 3 is fully verified and traceable.
- Requirements and roadmap are updated only after evidence exists.
- Phase 4 receives a clean handoff with runtime triage boundaries preserved.
</success_criteria>

<output>
Create `.planning/phases/03-rules-engine/03-09-SUMMARY.md` plus `03-VERIFICATION.md` and `03-UAT.md`, then commit closure docs if the workflow is configured to commit.
</output>
