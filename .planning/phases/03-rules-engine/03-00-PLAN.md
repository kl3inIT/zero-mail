---
phase: 03-rules-engine
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/core/src/test/java/com/zeromail/core/rules/model/RuleAstContractTest.java
  - backend/core/src/test/java/com/zeromail/core/rules/persistence/RulePersistenceWave0Test.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/RuleCompilerServiceWave0Test.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/RuleEvaluatorWave0Test.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/RulePreviewServiceWave0Test.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/RuleTemplateMaterializationWave0Test.java
  - backend/core/src/test/java/com/zeromail/core/arch/RulesBoundaryArchTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/DomainBoundaryArchTests.java
  - backend/api/src/test/java/com/zeromail/api/controllers/rules/RulesControllerWave0Test.java
  - apps/web/features/rules/components/RulesWorkspace.test.tsx
  - apps/web/__tests__/rules-feature-contract.test.ts
  - apps/web/e2e/rules.spec.ts
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03, RULE-04, RULE-05, RULE-06, RULE-07]
must_haves:
  truths:
    - "Wave 0 tests encode every Phase 3 acceptance criterion before implementation plans land"
    - "RulesBoundaryArchTest fails any org.springframework.ai or vendor SDK import outside core.llm.gateway.springai, with a specific assertion for core.rules"
    - "DomainBoundaryArchTests fails any core.rules import of another domain's persistence repository"
    - "RuleEvaluatorWave0Test models tri-state results: MATCHED, NOT_MATCHED, DEFERRED"
    - "RulePreviewServiceWave0Test contains no Gmail write client dependency and includes a fail-if-called write fake"
    - "Rules page Playwright spec covers create -> compile -> save disabled -> preview -> enable/disable -> reorder -> edit -> delete on desktop and mobile"
  artifacts:
    - path: "backend/core/src/test/java/com/zeromail/core/arch/RulesBoundaryArchTest.java"
      provides: "Boundary pin for core.rules and Spring AI/vendor SDK isolation"
      contains: "core.rules"
    - path: "apps/web/e2e/rules.spec.ts"
      provides: "Real browser golden-path contract for Rules page"
      contains: "No Gmail changes were made"
  key_links:
    - from: "RuleCompilerServiceWave0Test"
      to: "LlmGateway.chat(CallSite.PREVIEW, ...)"
      via: "mock gateway result"
      pattern: "CallSite.PREVIEW"
---

<objective>
Create the Phase 3 Wave 0 validation spine. These tests may be disabled or marked as contract scaffolds where production symbols do not exist yet, but they must compile and clearly document the behavior each downstream plan turns green.

Purpose: prevent scope drift. The executor should be able to run focused tests after each later plan and know exactly which Phase 3 acceptance criterion is still red or still disabled.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-SPEC.md
@.planning/phases/03-rules-engine/03-AI-SPEC.md
@.planning/phases/03-rules-engine/03-UI-SPEC.md
@.planning/phases/03-rules-engine/03-RESEARCH.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@.planning/phases/03-rules-engine/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Wave 0 tests</name>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/arch/LlmGatewayBoundaryTest.java
    - backend/core/src/test/java/com/zeromail/core/arch/DomainBoundaryArchTests.java
    - backend/core/src/test/java/com/zeromail/core/support/PostgresContainerTest.java
    - backend/core/src/test/java/com/zeromail/core/llm/service/LlmGatewayCreditLifecycleTest.java
    - .planning/phases/03-rules-engine/03-VALIDATION.md
  </read_first>
  <action>
    Add backend test scaffolds for:
    1. AST contract: all matcher ids, safe action ids, semantic deferred invariant, unknown-node rejection.
    2. Persistence: Liquibase future table expectations, tenant isolation, JSONB round-trip, enabled default false, template provenance columns.
    3. Compiler: calls `LlmGateway.chat(CallSite.PREVIEW, compilerPayload)`, rejects unknown tool/name/args, returns a single clarification and persists nothing for ambiguity.
    4. Evaluator: every deterministic matcher family, boolean groups, stable repeat results, semantic returns DEFERRED, no LLM invocation.
    5. Preview: sample sizes 10/25/50 only, default 25, max 50, no Gmail writes, no raw content persistence/logging.
    6. Template materialization: selected onboarding keys create disabled rules exactly once and customized rules are preserved.
    7. ArchUnit: `core.rules` must not import `org.springframework.ai..`, `com.openai..`, `com.anthropic..`, or Gmail write/execution packages.
    8. ArchUnit D-D1: extend `DomainBoundaryArchTests` so `core.rules` cannot import repositories from `core.account`, `core.onboarding`, `core.gmail`, `core.tenant`, `core.billing`, or `core.llm`.

    Use `@Disabled("Plan 03-XX lands production symbols")` only where a missing future class would otherwise break the suite. Tests that can already run, such as ArchUnit package scans, should not be disabled.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - All backend Wave 0 test files exist and compile.
    - `RulesBoundaryArchTest` is active, not disabled.
    - `DomainBoundaryArchTests` actively covers `core.rules` cross-domain repository imports.
    - Every disabled test has a message naming the plan expected to enable it.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: API and frontend Wave 0 tests</name>
  <read_first>
    - backend/api/src/test/java/com/zeromail/api/controllers/llm/ByokControllerIntegrationTest.java
    - apps/web/e2e/byok.spec.ts
    - apps/web/scripts/check-i18n.ts
    - apps/web/features/llm/components/ByokForm.tsx
  </read_first>
  <action>
    Add API and web scaffolds:
    1. `RulesControllerWave0Test` asserts planned endpoints, stable error codes, tenant isolation, insufficient-credit mapping, invalid sample-size handling, and no raw preview content in responses.
    2. `RulesWorkspace.test.tsx` covers composer state, clarification state, preview-before-enable gate, template badges, and reorder rollback rendering with mocked hooks.
    3. `rules-feature-contract.test.ts` source-scans for feature folder files, generated API usage, i18n keys, and absence of raw `fetch('/api/rules'...)` outside the feature API module.
    4. `apps/web/e2e/rules.spec.ts` mocks backend routes and covers desktop and mobile golden paths, including no horizontal overflow.
  </action>
  <verify>
    <automated>./gradlew :backend:api:compileTestJava && pnpm --filter web test -- --run features/rules __tests__/rules-feature-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - API and frontend Wave 0 files exist and compile/typecheck.
    - Playwright spec includes desktop and `375x812` mobile coverage.
    - Test strings use i18n-visible copy from UI-SPEC, including `No Gmail changes were made.`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:compileTestJava :backend:api:compileTestJava`
- `./gradlew :backend:core:test --tests "DomainBoundaryArchTests" --tests "RulesBoundaryArchTest"`
- `pnpm --filter web test -- --run features/rules __tests__/rules-feature-contract.test.ts`
</verification>

<success_criteria>
- Wave 0 test spine exists for every Phase 3 requirement.
- Active boundary tests prevent the most dangerous architectural violations before implementation begins.
- Disabled scaffolds are named, narrow, and tied to exact downstream plans.
</success_criteria>

<output>
Create `.planning/phases/03-rules-engine/03-00-SUMMARY.md` documenting which tests are active, which are disabled, and which implementation plan owns each disabled test.
</output>
