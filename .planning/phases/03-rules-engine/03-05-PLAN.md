---
phase: 03-rules-engine
plan: "05"
type: execute
wave: 3
depends_on: ["03", "04"]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/rules/service/RulePreviewDataService.java
  - backend/core/src/main/java/com/zeromail/core/rules/service/RulePreviewService.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RulePreviewCommand.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RulePreviewResult.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/PreviewSampleSize.java
  - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/RulePreviewServiceTest.java
  - backend/core/src/test/java/com/zeromail/core/rules/privacy/RulePreviewPrivacyTest.java
autonomous: true
requirements: [RULE-03, RULE-04, RULE-05]
must_haves:
  truths:
    - "Preview supports sample sizes 10, 25, 50; defaults to 25; rejects or normalizes anything above 50"
    - "Preview service depends only on read-side Gmail access and never injects Gmail write/action services"
    - "Preview includes the current disabled saved rule plus enabled sibling rules; unrelated disabled rules are ignored"
    - "Preview returns safe display summaries and evidence chips, not raw headers/snippets/bodies/prompts/completions"
    - "Successful preview marks the current rule version previewed so enablement can proceed"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/rules/service/RulePreviewService.java"
      provides: "Side-effect-free preview orchestration"
      contains: "PreviewSampleSize"
    - path: "backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java"
      provides: "Read-only transient Gmail preview fetch"
      contains: "messages().get"
  key_links:
    - from: "RulePreviewService"
      to: "RuleEvaluator"
      via: "deterministic evaluation of preview inputs"
      pattern: "RuleEvaluator"
---

<objective>
Implement side-effect-free preview over recent Gmail messages using transient read data and deterministic evaluation.

Purpose: users must see what would match and why before enabling a rule, while the backend proves no Gmail write path or durable raw mail content is involved.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-SPEC.md
@.planning/phases/03-rules-engine/03-AI-SPEC.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@.planning/phases/03-rules-engine/03-03-SUMMARY.md
@.planning/phases/03-rules-engine/03-04-SUMMARY.md
@backend/core/src/main/java/com/zeromail/core/gmail/persistence/MailMessageObservedRepository.java
@backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview data fetch boundary</name>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/MailMessageObservedEntity.java
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java
  </read_first>
  <action>
    Add a read-only preview data service:
    - Select recent observed Gmail message ids for tenant ordered by internal date/observed date.
    - Fetch Gmail metadata for the current request only.
    - Prefer metadata headers and label ids; fetch body-derived evidence only when matcher needs it.
    - Sanitize and truncate display fields before returning preview DTO input. Subject display is an excerpt, never a raw stored field.
    - Do not persist fetched headers, subject, snippet, body, prompt, completion, or model output.
    - Do not log Gmail email addresses, subjects, snippets, body text, token bytes, prompts, or completions.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.RulePreviewDataServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - Data service returns only request-scoped preview input objects.
    - Tests assert no repository accepts raw header/body/snippet/prompt/completion fields.
    - Gmail write APIs are not referenced by preview service.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Preview orchestration and privacy tests</name>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/service/RuleManagementService.java
    - backend/core/src/main/java/com/zeromail/core/rules/service/RuleEvaluator.java
    - backend/core/src/main/java/com/zeromail/core/rules/service/ActionProposalMerger.java
  </read_first>
  <action>
    Implement `RulePreviewService`:
    - Accept saved-rule preview by rule id and draft preview by validated matcher/action payload.
    - Enforce allowed sample sizes through `PreviewSampleSize`.
    - For saved disabled rule preview, evaluate that rule plus enabled siblings ordered by `order_index`.
    - Ignore unrelated disabled rules.
    - Return impact summary: sample size, matched count, proposed action counts, deferred count, conflict count, and explicit no-write notice flag/copy key.
    - Return row summaries with sanitized sender/domain, subject excerpt, date, existing Gmail labels, proposed action chips, matched matcher-clause chips, and deferred semantic chips.
    - Mark saved rule current version as previewed only after preview completes successfully.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.RulePreviewServiceTest" --tests "com.zeromail.core.rules.privacy.RulePreviewPrivacyTest"</automated>
  </verify>
  <acceptance_criteria>
    - Preview does not create labels, archive messages, save drafts, or mutate enabled state.
    - Preview marks `lastPreviewedVersion` for saved current version only.
    - Privacy test with sentinel body/header/prompt/completion strings finds zero durable/log occurrences.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.RulePreviewServiceTest" --tests "com.zeromail.core.rules.privacy.RulePreviewPrivacyTest"`
- `./gradlew :backend:core:test --tests "RulesBoundaryArchTest"`
</verification>

<success_criteria>
- Preview is side-effect-free by construction.
- Sample limits and safe summaries match SPEC/UI-SPEC.
- Successful preview enables the later service/API enable gate for that exact rule version.
</success_criteria>

<output>
Create `.planning/phases/03-rules-engine/03-05-SUMMARY.md` documenting preview data fields, sample-size handling, no-write proof, and privacy test sentinels.
</output>
