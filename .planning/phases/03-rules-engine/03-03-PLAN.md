---
phase: 03-rules-engine
plan: "03"
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/rules/service/RuleCompilerService.java
  - backend/core/src/main/java/com/zeromail/core/rules/service/RuleCompileResultValidator.java
  - backend/core/src/main/java/com/zeromail/core/rules/service/RuleManagementService.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RuleCompileCommand.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RuleCompileResult.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RuleCreateCommand.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RuleUpdateCommand.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RuleReorderCommand.java
  - backend/core/src/main/java/com/zeromail/core/rules/model/RuleValidationException.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/RuleCompilerServiceTest.java
  - backend/core/src/test/java/com/zeromail/core/rules/service/RuleManagementServiceTest.java
autonomous: true
requirements: [RULE-01, RULE-02, RULE-06]
must_haves:
  truths:
    - "RuleCompilerService calls LlmGateway.chat(CallSite.PREVIEW, payload, LlmToolProfile.RULE_COMPILE)"
    - "Ambiguous compile output returns one clarification question and persists no rule"
    - "Unknown matcher/action/schema fields are rejected before persistence"
    - "Create/update saves rules disabled until successful preview for that exact rule version"
    - "Reorder validates all rule IDs belong to current tenant and normalizes contiguous order indexes"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/rules/service/RuleCompilerService.java"
      provides: "NL rule compile use case"
      contains: "CallSite.PREVIEW"
    - path: "backend/core/src/main/java/com/zeromail/core/rules/service/RuleManagementService.java"
      provides: "CRUD, enable/disable, reorder, delete service"
      contains: "@Transactional"
  key_links:
    - from: "RuleManagementService"
      to: "RuleRepository"
      via: "tenant-qualified CRUD and reorder"
      pattern: "tenantId"
---

<objective>
Implement the core rules services for natural-language compile validation and rule CRUD/reorder/enablement state transitions.

Purpose: users can compile a natural-language rule, save a validated disabled rule, manage rule definitions, and reorder them without crossing tenant boundaries. Preview and API layers are separate later plans.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-SPEC.md
@.planning/phases/03-rules-engine/03-AI-SPEC.md
@.planning/phases/03-rules-engine/03-RESEARCH.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@.planning/phases/03-rules-engine/03-01-SUMMARY.md
@.planning/phases/03-rules-engine/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rule compiler and result validator</name>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/llm/service/LlmGateway.java
    - backend/core/src/main/java/com/zeromail/core/llm/model/ToolCallResult.java
    - backend/core/src/main/java/com/zeromail/core/rules/model/MatcherNode.java
    - backend/core/src/main/java/com/zeromail/core/rules/model/ActionIntent.java
  </read_first>
  <action>
    Implement command/result records and `RuleCompilerService`.

    Required behavior:
    - Accept tenant id, source text, optional clarification answer, and optional prior compile context.
    - Build a sanitized compiler payload containing schema version, authored source text, language hint, allowed matcher/action ids, and clarification answer if present.
    - Call `LlmGateway.chat(CallSite.PREVIEW, compilerPayload, LlmToolProfile.RULE_COMPILE)`.
    - Require tool name `rule_compile`.
    - Validate every returned field into typed rules-owned model records.
    - If ambiguous or required slots are missing, return `RuleCompileResult.requiresClarification(question)` with exactly one focused question and do not save.
    - Reject unknown matcher/action ids, unsafe actions, malformed regex, excessive string lengths, empty action list, and semantic nodes where `deferred != true`.

    Logs must be privacy-safe: `event=rule_compile_started tenantId={}` and outcome/reason metadata only. Do not log source text, prompt, completion, tool args, Gmail fields, or labels from user content.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.RuleCompilerServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - Stripe receipt fixture compiles into deterministic matcher(s) plus `archive` and `label`.
    - Unsafe action fixtures (`send`, `forward`, `spam`, webhook, delayed action) are rejected.
    - Ambiguous fixture returns one clarification and creates no row.
    - Mock gateway result with unknown node leaves no partial persistence.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rule management service</name>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleRepository.java
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java
    - backend/core/src/main/java/com/zeromail/core/tenant/TenantContext.java
  </read_first>
  <action>
    Implement `RuleManagementService` with service-owned transactions:
    - list ordered rules for tenant.
    - get one rule by tenant and id, returning not found for cross-tenant ids.
    - create from validated compile result, always `enabled=false`, order index appended to tenant's list.
    - update source/compiled matcher/action state, mark template-derived rules customized, increment version, clear preview eligibility for that version, and save disabled unless explicitly toggled after preview.
    - enable only when `lastPreviewedVersion == version`.
    - disable immediately.
    - delete by tenant/id and normalize remaining order.
    - reorder by full ordered id list or explicit moves; validate all ids belong to tenant and preserve contiguous `order_index`.

    Use explicit parameter/local names. Do not use abbreviations such as `repo`, `svc`, `ctx`, or `ex`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.RuleManagementServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - Cross-tenant get/update/delete/reorder attempts fail as not found or forbidden without mutating rows.
    - New and edited rules cannot be enabled before a successful preview of current version.
    - Reorder is stable, tenant-qualified, and contiguous after delete.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:test --tests "com.zeromail.core.rules.service.RuleCompilerServiceTest" --tests "com.zeromail.core.rules.service.RuleManagementServiceTest"`
- `./gradlew :backend:core:test --tests "RulesBoundaryArchTest"`
</verification>

<success_criteria>
- Natural-language compile is validated and fail-closed.
- Rule CRUD/reorder/enable/delete is tenant-safe and transactional.
- New or edited rules remain disabled until preview succeeds.
</success_criteria>

<output>
Create `.planning/phases/03-rules-engine/03-03-SUMMARY.md` documenting compiler schema validation, ambiguity behavior, and rule version/preview eligibility semantics.
</output>
