---
phase: 03-rules-engine
plan: "02"
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/llm/model/ToolCallResult.java
  - backend/core/src/main/java/com/zeromail/core/llm/model/LlmToolProfile.java
  - backend/core/src/main/java/com/zeromail/core/llm/model/SystemPrompts.java
  - backend/core/src/main/java/com/zeromail/core/llm/service/AllowListedTools.java
  - backend/core/src/main/java/com/zeromail/core/llm/service/ActionValidator.java
  - backend/core/src/main/java/com/zeromail/core/llm/service/LlmGatewayImpl.java
  - backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/SpringAiLlmModelClient.java
  - backend/core/src/test/java/com/zeromail/core/llm/service/RuleCompileToolProfileTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/RulesBoundaryArchTest.java
autonomous: true
requirements: [RULE-02]
must_haves:
  truths:
    - "LlmGateway remains the only LLM dependency used by Phase 3"
    - "Gateway owns a non-arbitrary RULE_COMPILE tool profile; callers never pass raw tool schemas"
    - "ToolCallResult can represent both existing safe action tools and the rule_compile tool without widening Action enum"
    - "Existing safe action drift/gateway behavior still passes"
    - "core.rules has zero Spring AI or vendor SDK imports"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/llm/model/LlmToolProfile.java"
      provides: "Gateway-owned tool profile enum"
      contains: "RULE_COMPILE"
    - path: "backend/core/src/main/java/com/zeromail/core/llm/service/AllowListedTools.java"
      provides: "Profile-specific tool schemas"
      contains: "rule_compile"
  key_links:
    - from: "LlmGatewayImpl"
      to: "AllowListedTools"
      via: "profile-specific tools for CallSite.PREVIEW rule compilation"
      pattern: "RULE_COMPILE"
---

<objective>
Resolve the Phase 3 compile-contract gap inside `core.llm`: add a gateway-owned rule compile tool profile and result shape so `core.rules` can request structured rule compilation without importing Spring AI or passing arbitrary tools.

Purpose: RULE-02 requires structured tool-call output, but the current gateway only exposes action tools. This plan extends the gateway boundary safely before `core.rules` consumes it.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-SPEC.md
@.planning/phases/03-rules-engine/03-AI-SPEC.md
@.planning/phases/03-rules-engine/03-RESEARCH.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@backend/core/src/main/java/com/zeromail/core/llm/service/LlmGateway.java
@backend/core/src/main/java/com/zeromail/core/llm/service/AllowListedTools.java
@backend/core/src/main/java/com/zeromail/core/llm/model/ToolCallResult.java
@backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/SpringAiLlmModelClient.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gateway-owned rule compile tool profile</name>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/llm/model/Action.java
    - backend/core/src/main/java/com/zeromail/core/llm/service/ActionValidator.java
    - backend/core/src/main/java/com/zeromail/core/llm/service/LlmGatewayImpl.java
  </read_first>
  <action>
    Add `LlmToolProfile implements IdentifiedEnum` with at least:
    - `SAFE_ACTIONS("safe-actions")`
    - `RULE_COMPILE("rule-compile")`

    Add an overload to `LlmGateway`:
    - Existing `chat(CallSite callSite, String rawHtml)` remains and delegates to `SAFE_ACTIONS`.
    - New `chat(CallSite callSite, String rawInput, LlmToolProfile toolProfile)` accepts only enum profiles, not caller-provided schemas.

    Update `AllowListedTools` to expose profile-specific lists:
    - `SAFE_ACTIONS` returns existing `label`, `archive`, `save_draft`.
    - `RULE_COMPILE` returns exactly one `rule_compile` tool with JSON schema for `rules.v1`, language, displayName, matcher, actions, ambiguity flag, and optional clarificationQuestion.

    Do not add `RULE_COMPILE` to `Action`. `Action` remains the safe Gmail action enum.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.llm.service.RuleCompileToolProfileTest"</automated>
  </verify>
  <acceptance_criteria>
    - Existing callers of `chat(callSite, rawHtml)` still compile.
    - `AllowListedTools.tools(LlmToolProfile.RULE_COMPILE)` contains exactly `rule_compile`.
    - `Action` enum membership remains only `label`, `archive`, `save_draft`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ToolCallResult and validation update</name>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/llm/service/LlmGatewayPlatformPathTest.java
    - backend/core/src/test/java/com/zeromail/core/llm/service/LlmGatewayCreditLifecycleTest.java
    - backend/worker/src/main/java/com/zeromail/worker/llm/DriftDetectionJob.java
  </read_first>
  <action>
    Refactor `ToolCallResult` so it can safely represent a non-action compile tool:
    - Store `toolName` as the canonical function name plus `args`.
    - Keep a convenience constructor `ToolCallResult(Action action, Map<String,Object> args)` for existing tests/callers.
    - Keep an `action()` helper that returns `Action.fromFunctionName(toolName)` for safe action results and fails loud for `rule_compile`.
    - Add `requireToolName(String expectedToolName)` or equivalent helper for compile validation.

    Update `ActionValidator` or introduce `ToolCallValidator` so:
    - `SAFE_ACTIONS` validates only `Action` function names.
    - `RULE_COMPILE` validates only `rule_compile`.
    - Null/blank/unknown names throw `SafetyViolationException` without carrying raw content.

    Update `LlmGatewayImpl.parseToolCall(...)`, drift tests, and gateway tests accordingly.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.llm.service.*" --tests "com.zeromail.core.llm.model.ToolCallResultTest" && ./gradlew :backend:worker:test --tests "com.zeromail.worker.llm.*"</automated>
  </verify>
  <acceptance_criteria>
    - Existing safe action tests still assert `.action()` for action profiles.
    - Rule compile profile tests assert `toolName` is `rule_compile`.
    - No exception/log includes tool-call arguments.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Compile prompt and adapter preservation</name>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/llm/model/SystemPrompts.java
    - backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/SpringAiLlmModelClient.java
    - Context7 notes in 03-RESEARCH.md
  </read_first>
  <action>
    Add a rule compile system prompt that treats authored rule text as data, requires exactly one `rule_compile` tool call, forbids unsafe actions, and forbids free text. Keep prompt/completion observation disabled.

    Ensure `SpringAiLlmModelClient` continues using `.toolCallbacks(...)`, `internalToolExecutionEnabled(false)`, and `toolChoice("required")` when `request.toolChoiceRequired()` is true. Do not move Spring AI imports out of `core.llm.gateway.springai`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "LlmGatewayBoundaryTest" --tests "RulesBoundaryArchTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "org.springframework.ai" backend/core/src/main/java/com/zeromail/core/rules` returns no matches once core.rules exists.
    - Gateway boundary tests pass with no exemptions.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:test --tests "com.zeromail.core.llm.*" --tests "LlmGatewayBoundaryTest" --tests "RulesBoundaryArchTest"`
- `./gradlew :backend:worker:test --tests "com.zeromail.worker.llm.*"`
</verification>

<success_criteria>
- Rule compilation has a structured gateway-owned tool profile.
- Existing LLM gateway safe-action behavior and drift checks remain intact.
- Phase 3 can consume `LlmGateway` without touching Spring AI or raw provider SDKs.
</success_criteria>

<output>
Create `.planning/phases/03-rules-engine/03-02-SUMMARY.md` documenting the final gateway compile profile, `ToolCallResult` compatibility decisions, and any test updates required for drift detection.
</output>
