---
phase: 03-rules-engine
plan: "08"
type: execute
wave: 5
depends_on: ["07"]
files_modified:
  - apps/web/app/(protected)/rules/page.tsx
  - apps/web/features/rules/api/rules-api.ts
  - apps/web/features/rules/query-keys.ts
  - apps/web/features/rules/hooks/use-rules.ts
  - apps/web/features/rules/components/RulesWorkspace.tsx
  - apps/web/features/rules/components/RuleComposer.tsx
  - apps/web/features/rules/components/RuleList.tsx
  - apps/web/features/rules/components/RulePreviewPanel.tsx
  - apps/web/features/rules/components/RuleTemplateGallery.tsx
  - apps/web/features/rules/messages.ts
  - apps/web/i18n/messages/vi.json
  - apps/web/i18n/messages/en.json
  - apps/web/scripts/check-i18n.ts
  - apps/web/features/rules/components/RulesWorkspace.test.tsx
  - apps/web/__tests__/rules-feature-contract.test.ts
  - apps/web/e2e/rules.spec.ts
autonomous: true
requirements: [RULE-01, RULE-05, RULE-06, RULE-07]
must_haves:
  truths:
    - "Protected `/rules` page follows 03-UI-SPEC layout and uses a client workspace for interactivity"
    - "Feature API uses generated OpenAPI path/schema types; no hand-rolled response types"
    - "Rules Query hooks invalidate correctly and reorder mutation uses optimistic update with rollback"
    - "Composer supports compile, one-question clarification, save disabled, preview-before-enable"
    - "Preview panel shows impact summary, safe rows, evidence chips, deferred semantic chips, conflicts, and no-write copy"
    - "VI/EN i18n parity and real-browser desktop/mobile verification pass"
  artifacts:
    - path: "apps/web/features/rules/api/rules-api.ts"
      provides: "Typed API functions for all rules endpoints"
      contains: "/api/rules"
    - path: "apps/web/app/(protected)/rules/page.tsx"
      provides: "Protected Rules page"
      contains: "RulesWorkspace"
  key_links:
    - from: "use-rules.ts"
      to: "query-keys.ts"
      via: "rulesKeys.list/detail/templates"
      pattern: "invalidateQueries"
---

<objective>
Build the authenticated Rules page and frontend feature module that consumes the generated rules API.

Purpose: deliver the user-facing Phase 3 workflow: write a natural-language rule, compile or clarify, save disabled, preview safely, enable, reorder/edit/delete, and start from templates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-rules-engine/03-CONTEXT.md
@.planning/phases/03-rules-engine/03-SPEC.md
@.planning/phases/03-rules-engine/03-UI-SPEC.md
@.planning/phases/03-rules-engine/03-PATTERNS.md
@.planning/phases/03-rules-engine/03-07-SUMMARY.md
@apps/web/features/llm/api/llm-api.ts
@apps/web/features/llm/hooks/use-byok.ts
@apps/web/features/llm/components/ByokForm.tsx
@apps/web/features/onboarding/components/TemplateCard.tsx
@apps/web/app/(protected)/settings/page.tsx
@apps/web/e2e/byok.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature API, query keys, hooks, and i18n</name>
  <read_first>
    - apps/web/lib/api/schema.d.ts
    - apps/web/features/llm/api/llm-api.ts
    - apps/web/features/llm/hooks/use-byok.ts
    - apps/web/features/llm/messages.ts
    - apps/web/scripts/check-i18n.ts
  </read_first>
  <action>
    Create `features/rules` API and hooks:
    - `rules-api.ts` exports generated-schema request/response aliases and functions for list/get/compile/create/update/enabled/reorder/delete/saved preview/draft preview/templates/materialize.
    - `query-keys.ts` exports `rulesKeys.all`, `list`, `detail(ruleId)`, `templates`.
    - `use-rules.ts` exports query hooks and mutation hooks.
    - Reorder mutation uses TanStack Query optimistic update: cancel list query, snapshot, set ordered list, rollback on error, invalidate on settled.
    - Other mutations invalidate list/detail/templates as appropriate.

    Add `features/rules/messages.ts` source keys and run existing i18n build/check flow. Include `rules.*` and `errors.rules.*` copy from UI-SPEC in VI and EN. Update `EN_SCAN_FILES` for new rules components.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && pnpm --filter web i18n:check && pnpm --filter web test -- --run __tests__/rules-feature-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No raw `fetch('/api/rules'...)` exists outside `features/rules/api/rules-api.ts`.
    - Reorder hook has `onMutate`, rollback `onError`, and `invalidateQueries` on settled.
    - i18n parity passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rules page components</name>
  <read_first>
    - .planning/phases/03-rules-engine/03-UI-SPEC.md
    - apps/web/components/ui/card.tsx
    - apps/web/components/ui/button.tsx
    - apps/web/components/ui/badge.tsx
    - apps/web/components/ui/alert.tsx
    - apps/web/components/ui/toggle-group.tsx
    - apps/web/components/ui/dialog.tsx
    - apps/web/components/ui/tooltip.tsx
  </read_first>
  <action>
    Before writing JSX, apply the frontend design instructions in AGENTS and the 03-UI-SPEC. Use raw shadcn/Base UI primitives and lucide icons. Add official `textarea` via `pnpm dlx shadcn@latest add textarea` only if no local textarea primitive exists and the composer needs it.

    Implement:
    - `app/(protected)/rules/page.tsx`: server page shell with `main mx-auto w-full max-w-6xl p-6` and `RulesWorkspace`.
    - `RulesWorkspace`: client orchestrator for selected rule, composer, list, templates, preview state, and responsive layout.
    - `RuleComposer`: natural-language textarea, compile CTA, one inline clarification prompt, read-only compiled matcher/action review, save disabled rule CTA.
    - `RuleList`: dense ordered list, enabled state, template/customized badges, reorder icon buttons with keyboard-accessible up/down, edit/delete.
    - `RulePreviewPanel`: sample size ToggleGroup 10/25/50 default 25, impact summary, no-write notice, safe row summaries, evidence/action/deferred chips.
    - `RuleTemplateGallery`: compact template rows/cards, materialize/use starter rule flow, disabled/template provenance.

    Do not render JSON AST as primary review UI. Do not render raw HTML, full body, raw headers, or raw snippets.
  </action>
  <verify>
    <automated>pnpm --filter web lint && pnpm --filter web typecheck && pnpm --filter web test -- --run features/rules</automated>
  </verify>
  <acceptance_criteria>
    - UI follows UI-SPEC copy, layout, spacing, accessibility, and responsive contracts.
    - Enable CTA appears only after successful preview of current rule version.
    - Icon-only controls have accessible names and tooltips.
    - No card is nested inside another card.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Browser verification</name>
  <read_first>
    - apps/web/e2e/byok.spec.ts
    - apps/web/playwright.config.ts
  </read_first>
  <action>
    Implement/enable `apps/web/e2e/rules.spec.ts`:
    - Mock `/me`, Gmail status, and all `/api/rules` routes.
    - Desktop flow: no rules empty state -> compile -> clarification -> recompile -> save disabled -> preview -> enable -> disable -> reorder -> edit -> preview again -> delete.
    - Template flow: template gallery materializes a starter rule and shows template provenance.
    - Mobile viewport 375x812: no horizontal overflow; composer, preview, and rule list remain usable.
    - Assert preview text contains `No Gmail changes were made.`
  </action>
  <verify>
    <automated>pnpm --filter web test:e2e -- apps/web/e2e/rules.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Playwright spec passes against a running dev server.
    - Screenshots/snapshot review shows no text overlap or horizontal overflow.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pnpm --filter web lint`
- `pnpm --filter web typecheck`
- `pnpm --filter web i18n:check`
- `pnpm --filter web test -- --run features/rules __tests__/rules-feature-contract.test.ts`
- `pnpm --filter web test:e2e -- apps/web/e2e/rules.spec.ts`
</verification>

<success_criteria>
- `/rules` is a complete protected operational surface for Phase 3.
- Frontend uses generated API types and feature-owned hooks.
- Desktop and mobile browser tests verify the full rules workflow.
</success_criteria>

<output>
Create `.planning/phases/03-rules-engine/03-08-SUMMARY.md` documenting frontend files, i18n key count, Playwright command/result, and screenshots if captured.
</output>
