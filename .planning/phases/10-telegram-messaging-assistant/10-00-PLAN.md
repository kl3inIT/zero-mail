---
phase: 10
plan: 00
name: foundation-bucket4j-requirements-liquibase-archunit-skeletons
type: execute
wave: 0
depends_on: []
requirements: [TG-01, TG-02, TG-03, TG-04, TG-05, TG-06, TG-07, TG-08, TG-09, TG-10, TG-11, TG-12, TG-13, TG-14, TG-15, TG-16, TG-17, TG-18, TG-19]
files_modified:
  - .planning/REQUIREMENTS.md
  - gradle/libs.versions.toml
  - backend/core/build.gradle.kts
  - backend/api/build.gradle.kts
  - backend/worker/build.gradle.kts
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  - backend/core/src/main/resources/db/changelog/changes/099-telegram-account.yaml
  - backend/core/src/main/resources/db/changelog/changes/100-outbound-action-audit.yaml
  - backend/core/src/main/resources/db/changelog/changes/101-telegram-notification-dedup.yaml
  - backend/core/src/main/resources/db/changelog/changes/102-telegram-notification-log.yaml
  - backend/core/src/main/resources/db/changelog/changes/103-app-db-grants-telegram.yaml
  - backend/core/src/main/java/com/zeromail/core/messaging/telegram/package-info.java
  - backend/core/src/main/java/com/zeromail/core/messaging/api/package-info.java
  - backend/core/src/main/java/com/zeromail/core/mailaction/package-info.java
  - backend/core/src/test/java/com/zeromail/core/arch/TelegramPathBodyBanTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/TelegramPrivacySweepTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/TelegramOutboxDrainArchTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/OutboundActionAuditMandatoryArchTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/MailActionServiceArchTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/TelegramChatStreamingOnlyArchTest.java
  - backend/api/src/test/resources/telegram-fixtures/.gitkeep
  - apps/web/e2e/telegram-pairing.spec.ts
autonomous: true
threat_refs: [T-10-04]
must_haves:
  truths:
    - "REQUIREMENTS.md có 19 dòng `**TG-01**: ... **TG-19**: ...` ánh xạ tới Phase 10"
    - "`com.bucket4j:bucket4j_jdk17-core:8.19.0` xuất hiện trong `gradle/libs.versions.toml` và được wired vào `backend/core/build.gradle.kts`"
    - "5 Liquibase changesets 099-103 tồn tại + được tham chiếu từ master changelog"
    - "3 Modulith `package-info.java` mới tồn tại (`core.messaging.telegram`, `core.messaging.api`, `core.mailaction`) với `@ApplicationModule(allowedDependencies=...)` đúng shape"
    - "6 ArchUnit test skeletons tồn tại + compile được (`./gradlew :backend:core:compileTestJava` xanh)"
    - "Bộ Playwright e2e skeleton + WireMock fixtures directory tồn tại"
    - "`OnlyOneGmailSendCallSiteTest` + `ChatPersistenceContentBanTest` vẫn xanh sau Wave 0 (không được làm hỏng existing invariants)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      contains: "TG-19"
    - path: "gradle/libs.versions.toml"
      contains: "bucket4j_jdk17-core"
    - path: "backend/core/src/main/resources/db/changelog/changes/099-telegram-account.yaml"
      provides: "telegram_account table"
    - path: "backend/core/src/main/resources/db/changelog/changes/100-outbound-action-audit.yaml"
      provides: "outbound_action_audit table"
    - path: "backend/core/src/main/resources/db/changelog/changes/101-telegram-notification-dedup.yaml"
      provides: "telegram_notification_dedup table"
    - path: "backend/core/src/main/resources/db/changelog/changes/102-telegram-notification-log.yaml"
      provides: "telegram_notification_log table"
    - path: "backend/core/src/main/resources/db/changelog/changes/103-app-db-grants-telegram.yaml"
      provides: "DB grants (no DELETE on telegram_account)"
    - path: "backend/core/src/main/java/com/zeromail/core/messaging/telegram/package-info.java"
      provides: "@ApplicationModule for messaging.telegram"
    - path: "backend/core/src/main/java/com/zeromail/core/mailaction/package-info.java"
      provides: "@ApplicationModule for mailaction"
    - path: "backend/core/src/test/java/com/zeromail/core/arch/TelegramPathBodyBanTest.java"
      provides: "ArchUnit skeleton (red until impl wires)"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md Phase 10"
      via: "Traceability table"
      pattern: "TG-[0-9]{2}.*Phase 10"
    - from: "backend/core/src/main/resources/db/changelog/db.changelog-master.yaml"
      to: "099-telegram-account.yaml..103-app-db-grants-telegram.yaml"
      via: "include sequence"
      pattern: "099-telegram-account|100-outbound-action-audit|101-telegram-notification-dedup|102-telegram-notification-log|103-app-db-grants-telegram"
---

<objective>
Đặt nền móng Wave 0 cho Phase 10 — Telegram Messaging Assistant:

1. Mint 19 yêu cầu TG-01..TG-19 vào `.planning/REQUIREMENTS.md` (mục mới + Traceability) ánh xạ Phase 10
2. Pin Bucket4j 8.19.0 trong `libs.versions.toml` + wire vào `backend/core/build.gradle.kts` (và `backend/worker/build.gradle.kts` nếu cần truy cập rate limiter)
3. Tạo 5 Liquibase YAML changesets 099-103 (telegram_account, outbound_action_audit, telegram_notification_dedup, telegram_notification_log, DB grants) — chỉ schema, chưa có entity Java
4. Tạo 3 Modulith `package-info.java` mới (`core.messaging.telegram`, `core.messaging.api`, `core.mailaction`) với `@ApplicationModule(allowedDependencies=...)` đúng shape RESEARCH §6
5. Tạo 6 ArchUnit test skeletons (compile được, các assertion thật được fill ở Wave 1/2): `TelegramPathBodyBanTest`, `TelegramPrivacySweepTest`, `TelegramOutboxDrainArchTest`, `OutboundActionAuditMandatoryArchTest`, `MailActionServiceArchTest`, `TelegramChatStreamingOnlyArchTest`
6. Tạo WireMock fixture directory + Playwright e2e skeleton (sẽ fill ở Wave 3)

Purpose: Wave 1+ phải compile + chạy được ngay khi land. Các plans sau chỉ cần thêm production code đáp ứng existing schema + ArchUnit invariants — không cần đụng vào REQUIREMENTS.md / build.gradle.kts / module boundary.

Output: 19 ID mới được mint, 5 changesets schema-only đã apply (DB migration sạch trong môi trường dev), 3 Modulith leaves mới, 6 test skeletons compile được, `./gradlew :backend:core:test --tests "*OnlyOneGmailSendCallSiteTest" --tests "*ChatPersistenceContentBanTest"` vẫn xanh sau commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-telegram-messaging-assistant/10-CONTEXT.md
@.planning/phases/10-telegram-messaging-assistant/10-SPEC.md
@.planning/phases/10-telegram-messaging-assistant/10-RESEARCH.md
@.planning/phases/10-telegram-messaging-assistant/10-PATTERNS.md
@CLAUDE.md
@backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java
@backend/core/src/test/java/com/zeromail/core/arch/ChatPersistenceContentBanTest.java

<interfaces>
<!-- Key existing types/patterns this plan must NOT break. -->

From `backend/core/src/main/java/com/zeromail/core/chat/package-info.java`:
```java
@ApplicationModule(
    displayName = "Chat (assistant streaming + confirmation)",
    allowedDependencies = { ... })
package com.zeromail.core.chat;
```

From `backend/core/src/main/java/com/zeromail/core/outbound/package-info.java`:
```java
@ApplicationModule(
    displayName = "Outbound",
    allowedDependencies = { "gmail", "gmail :: gateway", "tenant", "shared :: persistence", ... })
package com.zeromail.core.outbound;
```

From `backend/core/src/main/resources/db/changelog/changes/086-triage-audit-source.yaml`:
```yaml
databaseChangeLog:
  - changeSet:
      id: 086-triage-audit-source
      author: zeromail
      changes:
        - addColumn: { tableName: triage_audit, columns: [...] }
        - sql: { sql: "ALTER TABLE triage_audit ADD CONSTRAINT ck_triage_audit_source CHECK ..." }
      rollback: [...]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint 19 TG-XX requirements vào REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (đọc trọn file để xác định nơi chèn block TG-XX — sau §Rule Actions and Examples Catalog hoặc trước §Architecture Invariants)
    - .planning/phases/10-telegram-messaging-assistant/10-SPEC.md §Requirements (nguồn cho 19 mô tả TG-01..TG-19)
    - .planning/phases/10-telegram-messaging-assistant/10-CONTEXT.md §spec_lock (mô tả ngắn từng TG-XX)
  </read_first>
  <action>
    Chèn vào `.planning/REQUIREMENTS.md` 2 block:

    (A) Block "Phase 10 — Telegram Messaging Assistant (NEW)" sau §Rule Actions and Examples Catalog với 19 dòng `**TG-XX**:` ngắn gọn (1-2 câu mỗi dòng, lấy từ SPEC.md `Target` field — không paste full Current/Target/Acceptance triple). Mẫu cho mỗi dòng:
      - `- [ ] **TG-01**: Publish new `TriageDecisionRecorded` Spring Modulith event from `TriageOrchestratorService` (header + classification + action metadata only; never body/snippet/prompt/completion/token).`
      - `- [ ] **TG-02**: Introduce `MailActionService` facade in `core.mailaction.usecases` exposing archive/markRead/markSpam/trash/snooze; `TriageGmailWriter` delegates to it.`
      - `- [ ] **TG-03**: Add `OutboundActionSource` enum (7 values incl. `WEB_LEGACY` backfill sentinel) + new `outbound_action_audit` table (D-01) — do NOT extend `triage_audit.source`.`
      - `- [ ] **TG-04**: Adopt Bucket4j 8.19.0 in-memory rate limiter (per-chat 1/s + global 30/s); promote SEED-016 to closed-by-phase-10.`
      - `- [ ] **TG-05**: Single global bot configured via `zero-mail.messaging.telegram.bot-token` env var; missing token → /api/integrations/telegram/status returns 503.`
      - `- [ ] **TG-06**: Webhook double-secret verify (URL path + `X-Telegram-Bot-Api-Secret-Token` header) + IP allowlist `149.154.160.0/20`,`91.108.4.0/22`; either mismatch → 401.`
      - `- [ ] **TG-07**: `setMyCommands` idempotent on app startup; expected list = `/start`, `/help`, `/disconnect`.`
      - `- [ ] **TG-08**: Pairing via compact signed code (HMAC-SHA256 truncated 16 bytes, base64url; ~61 chars fits Telegram 64-char deep-link limit) — NOT JWT.`
      - `- [ ] **TG-09**: `telegram_account` table + lifecycle (CONNECTED/BLOCKED/DISCONNECTED); DELETE forbidden by DB grants.`
      - `- [ ] **TG-10**: DM-only enforcement (`message.chat.type='private'`) on every command + callback + free-text.`
      - `- [ ] **TG-11**: `TelegramNotificationListener` (@ApplicationModuleListener) subscribed to `TriageDecisionRecorded`; payload header-only (no body/snippet/prompt).`
      - `- [ ] **TG-12**: Inline keyboard primary + More submenu + destructive confirm-twice.`
      - `- [ ] **TG-13**: Callback verification — deterministic 16-char token + cross-actor `callback_query.from.id == telegram_account.telegram_user_id`.`
      - `- [ ] **TG-14**: All Gmail writes from Telegram route via `OutboundSendGateway` (single send call site) + `MailActionService` (single non-send mutation surface).`
      - `- [ ] **TG-15**: 3 slash commands only: `/start`, `/help`, `/disconnect`. `/pause`,`/digest`,`/unread` deferred.`
      - `- [ ] **TG-16**: Free-text Telegram chat → `ChatOrchestrator.stream(...)` with `ResponseSurface=TELEGRAM`; streaming via Reactor `sample(800ms)` + Bucket4j throttle.`
      - `- [ ] **TG-17**: Pending email confirm via existing `assistant_pending_action` CAS (per D-02; NOT `chat_message.parts` iteration).`
      - `- [ ] **TG-18**: `/settings/connected-apps` sub-route + `apps/web/features/telegram-integration/` feature module; main Settings page untouched.`
      - `- [ ] **TG-19**: Audit + observability + `TelegramPathBodyBanTest` + `TelegramPrivacySweepTest` (ARCH-02 body-ban extended to Telegram surface).`

    (B) Cập nhật §Traceability table — thêm 19 dòng `| TG-XX | Phase 10 | Pending |` ngay sau dòng `| RACT-12 | Phase 08.1 | Complete |`.

    (C) Cập nhật footer counter `v1.2 requirements: 73 total` → `v1.2 requirements: 92 total` và breakdown thêm dòng "Phase 10 (Telegram Messaging Assistant, planned 2026-05-28): 19 reqs — TG-01..TG-19".

    Không sửa các block khác. Không xóa Phase 9 SET-* placeholders.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$content = Get-Content -Raw -Path '.planning/REQUIREMENTS.md'; if (($content -match '\*\*TG-01\*\*') -and ($content -match '\*\*TG-19\*\*') -and ($content -match '\| TG-01 \| Phase 10 \|') -and ($content -match '\| TG-19 \| Phase 10 \|')) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>19 dòng TG-XX xuất hiện trong section mới + 19 hàng tracability table mới với Phase 10 mapping; footer counter cập nhật 92 total.</done>
</task>

<task type="auto">
  <name>Task 2: Pin Bucket4j 8.19.0 trong libs.versions.toml + wire vào backend/core + backend/worker build</name>
  <files>
    gradle/libs.versions.toml,
    backend/core/build.gradle.kts,
    backend/worker/build.gradle.kts
  </files>
  <read_first>
    - gradle/libs.versions.toml (đọc trọn — xác định vị trí thêm `[versions]` key `bucket4j = "8.19.0"` + `[libraries]` entry `bucket4j-jdk17-core`)
    - backend/core/build.gradle.kts (đọc trọn — xác định nơi thêm `implementation(libs.bucket4j.jdk17.core)`)
    - backend/worker/build.gradle.kts (đọc trọn — worker xài Bucket4j qua dependency lên `core`; chỉ kiểm tra core dep đã có sẵn)
    - .planning/phases/10-telegram-messaging-assistant/10-RESEARCH.md §4 (verify Bucket4j 8.19.0 artifact ID = `com.bucket4j:bucket4j_jdk17-core`)
  </read_first>
  <action>
    (A) Trong `gradle/libs.versions.toml`:
      - Thêm vào `[versions]` (giữ alphabetical order): `bucket4j = "8.19.0"`
      - Thêm vào `[libraries]`: `bucket4j-jdk17-core = { module = "com.bucket4j:bucket4j_jdk17-core", version.ref = "bucket4j" }`

    (B) Trong `backend/core/build.gradle.kts` `dependencies { ... }` block, thêm dòng `implementation(libs.bucket4j.jdk17.core)` cạnh các implementation dep khác (sau Spring AI nếu có, hoặc theo alphabetical).

    (C) `backend/worker/build.gradle.kts` đã có `implementation(project(":backend:core"))` (verify); KHÔNG thêm dep Bucket4j trực tiếp ở worker (nhận transitive qua core). Nếu Gradle compile complains về missing `io.github.bucket4j.*` import ở worker, dùng `implementation(libs.bucket4j.jdk17.core)` cùng cách như core; document trong commit message.

    KHÔNG sửa Spring Boot, Spring AI, hoặc Modulith versions. KHÔNG thêm `bucket4j-spring-boot-starter` (RESEARCH §4: không cần — `LocalBucketBuilder` đủ).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:dependencies --configuration runtimeClasspath -q 2>&1 | Select-String -Pattern 'bucket4j_jdk17-core:8.19.0' -Quiet; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>`./gradlew :backend:core:dependencies` output chứa `bucket4j_jdk17-core:8.19.0`; `./gradlew :backend:core:compileJava` xanh.</done>
</task>

<task type="auto">
  <name>Task 3: Tạo 5 Liquibase changesets 099-103 (schema-only)</name>
  <files>
    backend/core/src/main/resources/db/changelog/changes/099-telegram-account.yaml,
    backend/core/src/main/resources/db/changelog/changes/100-outbound-action-audit.yaml,
    backend/core/src/main/resources/db/changelog/changes/101-telegram-notification-dedup.yaml,
    backend/core/src/main/resources/db/changelog/changes/102-telegram-notification-log.yaml,
    backend/core/src/main/resources/db/changelog/changes/103-app-db-grants-telegram.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  </files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml (xem cú pháp `include` list để thêm 5 entries 099-103 tail)
    - backend/core/src/main/resources/db/changelog/changes/043-assistant-pending-action.yaml (mẫu createTable raw SQL + CHECK)
    - backend/core/src/main/resources/db/changelog/changes/086-triage-audit-source.yaml (mẫu rollback section)
    - backend/core/src/main/resources/db/changelog/changes/078-processing-job-extend.yaml (mẫu grant section)
    - .planning/phases/10-telegram-messaging-assistant/10-RESEARCH.md §9 + §15 (Liquibase changesets summary + 100-outbound-action-audit verbatim YAML)
    - .planning/phases/10-telegram-messaging-assistant/10-CONTEXT.md §decisions D-01 (schema sketch cho outbound_action_audit)
  </read_first>
  <action>
    Tạo 5 file YAML mới + cập nhật master changelog. Mỗi changeset có `author: zeromail`, `comment: >` 2-3 dòng giải thích, `changes:` body và `rollback:` body. ID format: `{padded_phase}-{padded_seq}-{slug}` ví dụ `099-telegram-account`.

    (A) `099-telegram-account.yaml` — `createTable telegram_account` với cột per TG-9:
      - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
      - tenant_id UUID NOT NULL UNIQUE REFERENCES user_account(id) ON DELETE CASCADE
      - telegram_chat_id BIGINT NOT NULL UNIQUE
      - telegram_user_id BIGINT NOT NULL
      - telegram_username VARCHAR(64)
      - language_code VARCHAR(8)
      - status VARCHAR(20) NOT NULL DEFAULT 'CONNECTED'
      - notifications_enabled BOOLEAN NOT NULL DEFAULT TRUE
      - notification_filter JSONB NOT NULL DEFAULT '{}'::jsonb
      - linked_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
      - last_active_at TIMESTAMPTZ
      - blocked_at TIMESTAMPTZ
      - disconnected_at TIMESTAMPTZ
      - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
      - updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
      + CHECK `status IN ('CONNECTED','BLOCKED','DISCONNECTED')`
      + Indexes: `idx_telegram_account_tenant_id`, `idx_telegram_account_chat_id` (unique đã có qua UNIQUE constraint), `idx_telegram_account_status_last_active (status, last_active_at)`.
      Rollback: drop table.

    (B) `100-outbound-action-audit.yaml` — `createTable outbound_action_audit` theo D-01 schema sketch (verbatim từ RESEARCH §9):
      - 11 cột, CHECK action IN (13 values: SEND/REPLY/FORWARD/ARCHIVE/MARK_READ/MARK_UNREAD/MARK_SPAM/TRASH/SNOOZE/STAR/UNSTAR/ADD_TO_DIGEST/SAVE_DRAFT)
      - CHECK source IN (7 values: RULE_AUTO/WEB_CHAT_CONFIRMED/TELEGRAM_INLINE_BUTTON/TELEGRAM_INLINE_BUTTON_DESTRUCTIVE_CONFIRMED/TELEGRAM_CHAT_CONFIRMED/TELEGRAM_DEEPLINK_FROM_NOTIFICATION/WEB_LEGACY)
      - 2 indexes: `(tenant_id, initiated_at DESC)`, `(tenant_id, gmail_message_id) WHERE gmail_message_id IS NOT NULL`
      Rollback: drop table.
      KHÔNG động vào `triage_audit.source` (D-01: triage_audit untouched).

    (C) `101-telegram-notification-dedup.yaml` — `createTable telegram_notification_dedup` per RESEARCH §9 Alt A:
      - tenant_id UUID NOT NULL
      - gmail_message_id VARCHAR(64) NOT NULL
      - sent_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
      - PRIMARY KEY (tenant_id, gmail_message_id)
      - INDEX `idx_telegram_notification_dedup_sent_at (sent_at)` (cho vacuum job)
      Rollback: drop table.
      KHÔNG dùng partial UNIQUE index với `NOW()` (Pitfall 1).

    (D) `102-telegram-notification-log.yaml` — `createTable telegram_notification_log` (privacy-bounded log, NOT a queue):
      - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
      - tenant_id UUID NOT NULL REFERENCES user_account(id) ON DELETE CASCADE
      - telegram_chat_id BIGINT NOT NULL
      - gmail_message_id VARCHAR(64)
      - notification_kind VARCHAR(32) NOT NULL (CHECK IN ('RULE_FIRED','COMMAND_REPLY','EDIT_MESSAGE','CHAT_STREAM'))
      - telegram_message_id BIGINT  -- returned by Telegram sendMessage, null nếu chưa gửi
      - sent_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
      - acted_on_at TIMESTAMPTZ  -- set when user taps an inline button (idempotency)
      - action_taken VARCHAR(32)  -- mirror outbound_action_audit.action enum when applicable
      Constraints: NO `body` / `snippet` / `subject` / `content` column. INDEX `(tenant_id, sent_at DESC)`, `(telegram_chat_id, sent_at DESC)`.
      Rollback: drop table.

    (E) `103-app-db-grants-telegram.yaml` — GRANT/REVOKE for app DB role per TG-09 (DELETE forbidden on telegram_account):
      ```yaml
      changes:
        - sql:
            sql: |
              GRANT SELECT, INSERT, UPDATE ON telegram_account TO zeromail_app;
              GRANT SELECT, INSERT, UPDATE ON outbound_action_audit TO zeromail_app;
              GRANT SELECT, INSERT, DELETE ON telegram_notification_dedup TO zeromail_app;
              GRANT SELECT, INSERT, UPDATE ON telegram_notification_log TO zeromail_app;
              -- DELETE on telegram_account intentionally NOT granted (TG-09)
      rollback:
        - sql:
            sql: |
              REVOKE SELECT, INSERT, UPDATE ON telegram_account FROM zeromail_app;
              ...
      ```
      Verify rolename `zeromail_app` từ existing changesets (e.g., changeset 081); nếu tên khác (e.g., `zero_mail_app`), dùng tên đó.

    (F) Cập nhật `db.changelog-master.yaml` — append 5 `include` entries:
      ```yaml
        - include: { file: changes/099-telegram-account.yaml, relativeToChangelogFile: true }
        - include: { file: changes/100-outbound-action-audit.yaml, relativeToChangelogFile: true }
        - include: { file: changes/101-telegram-notification-dedup.yaml, relativeToChangelogFile: true }
        - include: { file: changes/102-telegram-notification-log.yaml, relativeToChangelogFile: true }
        - include: { file: changes/103-app-db-grants-telegram.yaml, relativeToChangelogFile: true }
      ```

    KHÔNG động vào changeset 086 (triage_audit.source). KHÔNG thêm CHECK vào processing_job.job_type (D-04 + RESEARCH §8 Option B: skip CHECK, rely on enum-at-write-site).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:liquibaseValidate -q 2>&1 | Out-String; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>5 changesets tồn tại + được include trong master changelog; `liquibaseValidate` xanh; `./gradlew :backend:core:test` xanh (existing tests không bị break).</done>
</task>

<task type="auto">
  <name>Task 4: Tạo 3 Spring Modulith package-info.java (core.messaging.telegram, core.messaging.api, core.mailaction)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/messaging/api/package-info.java,
    backend/core/src/main/java/com/zeromail/core/messaging/telegram/package-info.java,
    backend/core/src/main/java/com/zeromail/core/mailaction/package-info.java
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/chat/package-info.java (allowedDependencies shape mẫu — VERBATIM lines 20-51 trong PATTERNS.md)
    - backend/core/src/main/java/com/zeromail/core/outbound/package-info.java (mẫu sibling — verify nó đã expose `outbound :: domain` chưa, hay cần add domain sub-package package-info riêng)
    - .planning/phases/10-telegram-messaging-assistant/10-RESEARCH.md §6 (allowedDependencies cho 3 module)
    - .planning/phases/10-telegram-messaging-assistant/10-PATTERNS.md "messaging/telegram/package-info.java"
  </read_first>
  <action>
    Tạo 3 file `package-info.java`:

    (A) `core/messaging/api/package-info.java` — module leaf rỗng (placeholder cho `MessagingChannel` interface ở Wave 1+ khi Zalo follow-on cần):
      ```java
      @org.springframework.modulith.ApplicationModule(
          displayName = "Messaging - API",
          allowedDependencies = {
              "shared :: lang"
          })
      package com.zeromail.core.messaging.api;
      ```

    (B) `core/messaging/telegram/package-info.java` — module chính, allowedDependencies per RESEARCH §6:
      ```java
      @org.springframework.modulith.ApplicationModule(
          displayName = "Messaging - Telegram",
          allowedDependencies = {
              "messaging :: api",
              "chat",
              "chat :: usecases",
              "chat :: domain",
              "outbound",
              "outbound :: usecases",
              "outbound :: domain",
              "mailaction",
              "mailaction :: usecases",
              "triage :: domain",
              "tenant",
              "tenant :: usecases",
              "rules :: domain",
              "rules :: projection",
              "config",
              "shared :: persistence",
              "shared :: lang",
              "shared :: privacy",
              "shared :: exception",
              "shared :: lock",
              "llm :: domain",
              "llm :: gateway.springai"
          })
      package com.zeromail.core.messaging.telegram;
      ```

    (C) `core/mailaction/package-info.java` — façade module per D-03 + RESEARCH §6:
      ```java
      @org.springframework.modulith.ApplicationModule(
          displayName = "Mail Action",
          allowedDependencies = {
              "gmail",
              "gmail :: gateway",
              "tenant",
              "tenant :: usecases",
              "outbound :: domain",
              "outbound :: usecases",
              "shared :: persistence",
              "shared :: lang",
              "shared :: exception"
          })
      package com.zeromail.core.mailaction;
      ```

    KHÔNG sửa `core/outbound/package-info.java` ở Plan này (Plan 02 sẽ add `domain` sub-package + cập nhật `outbound/package-info.java` allowed list nếu Modulith yêu cầu).
    KHÔNG sửa `core/triage/package-info.java` hay `core/chat/package-info.java` ở Plan này (Plan 01 và Plan 02 sẽ làm khi cần thêm `mailaction :: usecases` dependency cho triage hoặc khi expose TriageDecisionRecorded NamedInterface).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:compileJava -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>`./gradlew :backend:core:compileJava` xanh; `./gradlew :backend:core:test --tests "*ApplicationModules*"` xanh (existing Modulith verification tests vẫn pass — empty modules được phép).</done>
</task>

<task type="auto">
  <name>Task 5: Tạo 6 ArchUnit test skeletons (compile được, assertion thật fill ở Wave 1/2)</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/arch/TelegramPathBodyBanTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/TelegramPrivacySweepTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/TelegramOutboxDrainArchTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/OutboundActionAuditMandatoryArchTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/MailActionServiceArchTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/TelegramChatStreamingOnlyArchTest.java
  </files>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java (mẫu ArchUnit + `importPackages("com.zeromail")` shape — VERBATIM)
    - backend/core/src/test/java/com/zeromail/core/arch/ChatPersistenceContentBanTest.java (mẫu source-file regex sweep + `Files.exists` guard)
    - .planning/phases/10-telegram-messaging-assistant/10-RESEARCH.md §12 + §14 (test surface mapping + skeleton hints)
    - .planning/phases/10-telegram-messaging-assistant/10-PATTERNS.md "Shared Patterns / ArchUnit invariant test"
  </read_first>
  <action>
    Mỗi file chứa class với ≥1 `@Test` method, nhưng assertion ban đầu là `assertThat(true).isTrue()` placeholder + javadoc nêu rõ "Wave X plan YY sẽ replace placeholder với real ArchUnit rule". Mỗi class theo mẫu OnlyOneGmailSendCallSiteTest (cùng package `com.zeromail.core.arch`, junit5 + assertj + ArchUnit imports).

    Mỗi file PHẢI có:
    1. `package com.zeromail.core.arch;`
    2. Javadoc class-level mô tả rõ invariant + plan hứa fill assertion + reference TG-XX.
    3. ít nhất 1 method `@Test void <descriptive_name>()` chứa `assertThat(true).as("Skeleton — Wave 1/2 plan YY fills real assertion").isTrue();`
    4. Pre-fill các constants/regex sẽ dùng (BODY_FIELD_PATTERN cho TelegramPathBodyBanTest mirror ChatPersistenceContentBanTest line 23-25; TELEGRAM_PACKAGE = "com.zeromail.core.messaging.telegram"; v.v.) — như TODO comments hoặc real constants để Wave 1/2 chỉ cần thêm logic.

    Cụ thể từng file:

    (A) `TelegramPathBodyBanTest.java` — ARCH-02 extension (TG-19). Skeleton:
      - Pre-define `BODY_FIELD_PATTERN = Pattern.compile("(?i)\\b(emailBody|messageBody|bodyHtml|bodyText|htmlBody|textBody|body|snippet|messageHtml|content)\\b")`
      - Pre-define `TELEGRAM_SOURCE_ROOT = Path.of("backend/core/src/main/java/com/zeromail/core/messaging/telegram")`
      - Method `telegram_sources_do_not_declare_body_shaped_fields()` — guard `if (!Files.exists(TELEGRAM_SOURCE_ROOT)) return;` + placeholder assertion. (Wave 1 Plan 03 fills the regex sweep.)
      - Method `telegram_notification_log_entity_does_not_persist_body_columns()` — placeholder.

    (B) `TelegramPrivacySweepTest.java` — Logback appender capture sweep (TG-19). Skeleton:
      - Pre-define `BANNED_SUBSTRINGS = List.of("body", "bodyHtml", "snippet", "messageHtml", "content", "prompt", "completion", "token")`
      - Pre-define `EMAIL_PATTERN = Pattern.compile("[a-z0-9._+-]+@[a-z0-9.-]+\\.[a-z]{2,}", Pattern.CASE_INSENSITIVE)`
      - Method `telegram_log_lines_contain_no_body_or_unmasked_email()` — placeholder. (Wave 3 Plan 10 fills Logback test appender wiring.)

    (C) `TelegramOutboxDrainArchTest.java` — D-04 invariant (worker-only drain). Skeleton:
      - Method `no_scheduled_in_backend_api_references_messaging_notification()` — placeholder (Wave 2 Plan 07 fills `JavaClasses` import + `@Scheduled` annotation scan).
      - Method `drain_handler_lives_only_in_worker_or_core_messaging_notification()` — placeholder.

    (D) `OutboundActionAuditMandatoryArchTest.java` — TG-03/D-01 (every Gmail mutation writes audit). Skeleton:
      - Method `every_gmail_mutation_method_writes_outbound_action_audit_in_same_method_body()` — placeholder (Wave 1 Plan 02 fills the call-site scan).

    (E) `MailActionServiceArchTest.java` — TG-02 (single non-send Gmail mutation surface). Skeleton:
      - Method `gmail_messages_modify_only_called_from_core_mailaction_usecases()` — placeholder.
      - Method `gmail_messages_trash_only_called_from_core_mailaction_usecases()` — placeholder. (Wave 1 Plan 02 fills the rule.)

    (F) `TelegramChatStreamingOnlyArchTest.java` — TG-16 (no non-streaming chat in Telegram). Skeleton:
      - Method `telegram_chat_package_does_not_import_non_streaming_chat_model()` — placeholder. (Wave 3 Plan 09 fills the import scan rule.)

    Tất cả 6 file PHẢI compile sạch (`./gradlew :backend:core:compileTestJava` xanh). Placeholder assertions PHẢI pass — không được fail Wave 0 build. Test sẽ chuyển từ trivially-green sang real-assertion-green trong waves sau.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:test --tests 'com.zeromail.core.arch.TelegramPathBodyBanTest' --tests 'com.zeromail.core.arch.TelegramPrivacySweepTest' --tests 'com.zeromail.core.arch.TelegramOutboxDrainArchTest' --tests 'com.zeromail.core.arch.OutboundActionAuditMandatoryArchTest' --tests 'com.zeromail.core.arch.MailActionServiceArchTest' --tests 'com.zeromail.core.arch.TelegramChatStreamingOnlyArchTest' --tests 'com.zeromail.core.arch.OnlyOneGmailSendCallSiteTest' --tests 'com.zeromail.core.arch.ChatPersistenceContentBanTest' -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>6 ArchUnit skeletons compile + tất cả test methods PASS (trivially-green qua placeholder); `OnlyOneGmailSendCallSiteTest` + `ChatPersistenceContentBanTest` vẫn xanh (no regression).</done>
</task>

<task type="auto">
  <name>Task 6: Tạo WireMock fixtures dir + Playwright e2e skeleton</name>
  <files>
    backend/api/src/test/resources/telegram-fixtures/.gitkeep,
    backend/api/src/test/resources/telegram-fixtures/README.md,
    apps/web/e2e/telegram-pairing.spec.ts
  </files>
  <read_first>
    - backend/api/src/test/resources/ (xem dir structure tồn tại; verify path)
    - apps/web/e2e/settings-notifications.spec.ts (mẫu Playwright + helpers `openAuthenticatedRoute`, `createChromeMockState`)
    - .planning/phases/10-telegram-messaging-assistant/10-RESEARCH.md §13 (WireMock fixtures + Playwright e2e snippet)
  </read_first>
  <action>
    (A) Tạo `backend/api/src/test/resources/telegram-fixtures/.gitkeep` (file rỗng) + README.md ngắn liệt kê 8 fixtures mà các waves sau sẽ fill (getMe.200, sendMessage.200, sendMessage.429, editMessageText.200, editMessageText.429, setMyCommands.200, getMyCommands.empty, getMyCommands.match, answerCallbackQuery.200). README format ngắn gọn, format Markdown bullet list, không include actual JSON.

    (B) Tạo `apps/web/e2e/telegram-pairing.spec.ts` — Playwright skeleton với 1 test `test.skip(...)` mô tả happy path. Skeleton:
      ```typescript
      import { test, expect } from '@playwright/test';

      // Wave 3 Plan 10 will replace test.skip with real implementation.
      // Flow per TG-18: visit /settings/connected-apps → click Connect →
      // capture deep-link → simulate webhook /start <code> via test helper →
      // dialog closes within 4s → card shows @username connected →
      // click Disconnect → confirm → returns to disconnected state.
      test.skip('Telegram Connect → notify → callback-confirm-send happy path', async ({ page }) => {
        // TODO Wave 3 Plan 10
      });
      ```

    Test PHẢI compile (TypeScript valid) and được Playwright detect (config glob match `apps/web/e2e/**/*.spec.ts`). `test.skip` keeps suite green.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Test-Path 'backend/api/src/test/resources/telegram-fixtures/.gitkeep') -and (Test-Path 'apps/web/e2e/telegram-pairing.spec.ts')) { pnpm.cmd --filter web exec tsc --noEmit -p apps/web/tsconfig.json 2>&1 | Out-Null; exit $LASTEXITCODE } else { exit 1 }"</automated>
  </verify>
  <done>WireMock dir + .gitkeep + README tồn tại; Playwright skeleton compile sạch qua `tsc --noEmit`.</done>
</task>

<task type="auto">
  <name>Task 7: Verification gate Wave 0 — toàn bộ existing invariants vẫn xanh</name>
  <files></files>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java (verify pattern unchanged)
    - backend/core/src/test/java/com/zeromail/core/arch/ChatPersistenceContentBanTest.java (verify pattern unchanged)
  </read_first>
  <action>
    Verification-only task: chạy full backend test + Modulith test + key invariants. KHÔNG sửa code/file nào. Mục đích — gate phase: Wave 0 LANDED nguyên si các thay đổi schema/dep/module mà KHÔNG break:
    1. `OnlyOneGmailSendCallSiteTest` (gmail send call site count = 1)
    2. `ChatPersistenceContentBanTest` (3 methods, all green)
    3. ApplicationModulesTest (Modulith verification — 3 module mới được phép boot empty)
    4. Liquibase migration applies clean trên fresh dev DB
    5. Bucket4j dep resolves
    6. 6 ArchUnit skeletons trivially-green

    Nếu task này fail, executor PHẢI quay lại task tương ứng để fix, KHÔNG hide failures.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:test :backend:core:compileTestJava :backend:api:compileTestJava -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>Full backend test xanh; 0 regression trên `OnlyOneGmailSendCallSiteTest` / `ChatPersistenceContentBanTest` / `ApplicationModulesTest`; build artifacts có Bucket4j 8.19.0 trên classpath.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → prod migration | Liquibase changesets 099-103 chạy `liquibase update` lần đầu trên prod; rollback paths phải đúng nếu cần revert |
| bot-token env var | `TELEGRAM_BOT_TOKEN` từ env vào `TelegramProperties` — không bao giờ vào DB / logs / Git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | bot-token in build artifacts / libs.versions.toml | mitigate | Bucket4j dep is public OSS; bot-token KHÔNG xuất hiện trong build files; chỉ env-var-based (verify trong Plan 06 TelegramProperties) |
| T-10-SC | Tampering | Bucket4j 8.19.0 supply chain | mitigate | Verified Maven Central `com.bucket4j:bucket4j_jdk17-core:8.19.0` published 2026-05-19; RESEARCH.md §4 confirmed legitimate (per Package Legitimacy Audit pattern); group ID `com.bucket4j` matches official bucket4j.com owner; transitive deps minimal (slf4j-api only) |
</threat_model>

<verification>
- `./gradlew :backend:core:test :backend:core:compileTestJava :backend:api:compileTestJava` exits 0
- `./gradlew :backend:core:dependencies --configuration runtimeClasspath` output chứa `bucket4j_jdk17-core:8.19.0`
- 5 Liquibase YAML files + master include update apply clean (no exceptions on fresh dev DB)
- 3 Modulith package-info.java compile + ApplicationModulesTest passes
- 6 ArchUnit skeletons trivially-green (placeholder assertions pass; real rules wired in Wave 1+)
- WireMock fixtures dir + Playwright e2e skeleton tồn tại + Playwright tsc clean
- REQUIREMENTS.md có 19 dòng TG-01..TG-19 + 19 traceability rows + counter cập nhật
- Không có regression trên `OnlyOneGmailSendCallSiteTest` / `ChatPersistenceContentBanTest` / `ApplicationModulesTest`
</verification>

<success_criteria>
- 19 TG-XX requirement IDs minted trong REQUIREMENTS.md (mục riêng + traceability + counter 92 total)
- Bucket4j 8.19.0 pin trong `libs.versions.toml` + wired vào `backend/core/build.gradle.kts`
- 5 Liquibase changesets 099-103 tồn tại + included trong master changelog + `liquibaseValidate` xanh
- 3 Modulith package-info.java mới (`core.messaging.api`, `core.messaging.telegram`, `core.mailaction`)
- 6 ArchUnit test skeletons compile + trivially-green
- WireMock fixtures dir + Playwright e2e skeleton
- `./gradlew :backend:core:test :backend:core:compileTestJava :backend:api:compileTestJava` xanh
- Zero regression: `OnlyOneGmailSendCallSiteTest`, `ChatPersistenceContentBanTest`, `ApplicationModulesTest` vẫn xanh
</success_criteria>

<output>
Tạo `.planning/phases/10-telegram-messaging-assistant/10-00-SUMMARY.md` khi xong, ghi nhận:
- 5 changesets shape final
- Tên rolename app DB user (verify từ existing changesets — `zeromail_app` hay khác)
- Path Bucket4j artifact ID đúng (`com.bucket4j:bucket4j_jdk17-core`)
- 19 TG-XX titles ngắn gọn để Wave 1+ tham chiếu
- Bất kỳ deviation nào (ví dụ rolename khác từ expectations)
</output>
