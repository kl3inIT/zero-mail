---
phase: 10
plan: 02
name: outbound-action-source-audit-and-mailaction-facade
type: execute
wave: 1
depends_on: ["10-00"]
requirements: [TG-02, TG-03, TG-14]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/outbound/domain/OutboundActionSource.java
  - backend/core/src/main/java/com/zeromail/core/outbound/domain/OutboundAction.java
  - backend/core/src/main/java/com/zeromail/core/outbound/domain/package-info.java
  - backend/core/src/main/java/com/zeromail/core/outbound/persistence/OutboundActionAuditEntity.java
  - backend/core/src/main/java/com/zeromail/core/outbound/persistence/OutboundActionAuditRepository.java
  - backend/core/src/main/java/com/zeromail/core/outbound/persistence/OutboundActionAuditWriter.java
  - backend/core/src/main/java/com/zeromail/core/outbound/persistence/package-info.java
  - backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendCommand.java
  - backend/core/src/main/java/com/zeromail/core/outbound/usecases/GmailOutboundSendGateway.java
  - backend/core/src/main/java/com/zeromail/core/outbound/package-info.java
  - backend/core/src/main/java/com/zeromail/core/mailaction/usecases/MailActionService.java
  - backend/core/src/main/java/com/zeromail/core/mailaction/usecases/DefaultMailActionService.java
  - backend/core/src/main/java/com/zeromail/core/mailaction/domain/MailActionPerformed.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java
  - backend/core/src/main/java/com/zeromail/core/triage/package-info.java
  - backend/core/src/main/java/com/zeromail/core/chat/package-info.java
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java
  - backend/core/src/test/java/com/zeromail/core/arch/MailActionServiceArchTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/OutboundActionAuditMandatoryArchTest.java
  - backend/core/src/test/java/com/zeromail/core/mailaction/usecases/DefaultMailActionServiceTest.java
  - backend/core/src/test/java/com/zeromail/core/outbound/persistence/OutboundActionAuditWriterIntegrationTest.java
autonomous: true
threat_refs: [T-10-11, T-10-12, T-10-13]
must_haves:
  truths:
    - "`OutboundActionSource` enum tồn tại với 7 giá trị (RULE_AUTO, WEB_CHAT_CONFIRMED, TELEGRAM_INLINE_BUTTON, TELEGRAM_INLINE_BUTTON_DESTRUCTIVE_CONFIRMED, TELEGRAM_CHAT_CONFIRMED, TELEGRAM_DEEPLINK_FROM_NOTIFICATION, WEB_LEGACY)"
    - "`OutboundActionAuditEntity` mapped to `outbound_action_audit` table; CRUD via Spring Data JPA repository"
    - "`OutboundActionAuditWriter.record(...)` là entry point duy nhất ghi audit row"
    - "`OutboundSendCommand` gains non-null `OutboundActionSource source` field"
    - "`GmailOutboundSendGateway.send(...)` ghi 1 audit row trong cùng @Transactional với Gmail send call"
    - "`MailActionService` interface + `DefaultMailActionService` impl tồn tại với 5 methods (archive/markRead/markSpam/trash/snooze)"
    - "`TriageGmailWriter` refactored: 5 archive-like methods delegate sang `MailActionService` (source=RULE_AUTO)"
    - "`MailActionServiceArchTest` green — không Gmail mutate ngoài `core.mailaction.usecases`"
    - "`OutboundActionAuditMandatoryArchTest` green — mọi Gmail mutation call site có audit write trong cùng method"
    - "`OnlyOneGmailSendCallSiteTest` vẫn green sau refactor (Gmail send vẫn ở 1 nơi)"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/outbound/domain/OutboundActionSource.java"
      provides: "7-value enum"
    - path: "backend/core/src/main/java/com/zeromail/core/outbound/persistence/OutboundActionAuditEntity.java"
      provides: "JPA entity mapped to outbound_action_audit"
    - path: "backend/core/src/main/java/com/zeromail/core/mailaction/usecases/MailActionService.java"
      provides: "5-method facade interface"
    - path: "backend/core/src/main/java/com/zeromail/core/mailaction/usecases/DefaultMailActionService.java"
      provides: "Default impl gọi Gmail + audit ghi cùng tx"
  key_links:
    - from: "core.mailaction.usecases.DefaultMailActionService"
      to: "core.outbound.persistence.OutboundActionAuditWriter"
      via: "constructor injection + same-tx record call"
      pattern: "outboundActionAuditWriter\\.record"
    - from: "core.triage.usecases.TriageGmailWriter"
      to: "core.mailaction.usecases.MailActionService"
      via: "constructor injection + delegate"
      pattern: "mailActionService\\.(archive|markRead|markSpam|trash|snooze)"
---

<objective>
Mint audit + mail-action backbone cho Phase 10 (TG-02 + TG-03 + TG-14 enforcement):

1. `OutboundActionSource` enum (7 values, IdentifiedEnum, fromId fail-loud) trong `core.outbound.domain` (D-01).
2. `OutboundActionAuditEntity` + `OutboundActionAuditRepository` + `OutboundActionAuditWriter` ánh xạ tới `outbound_action_audit` table (changeset 100).
3. `OutboundSendCommand` thêm `OutboundActionSource source` field — non-null required.
4. `GmailOutboundSendGateway.send(...)` write 1 audit row trong cùng @Transactional với Gmail API call.
5. `MailActionService` interface + `DefaultMailActionService` impl trong NEW `core.mailaction.usecases` — 5 methods (archive/markRead/markSpam/trash/snooze), mỗi method call Gmail + audit write cùng tx + publish `MailActionPerformed` event.
6. Refactor `TriageGmailWriter`: 5 archive-like methods delegate sang `MailActionService.*(..., RULE_AUTO)`. Existing triage tests stay green.
7. ArchUnit gate `MailActionServiceArchTest` (no Gmail `.modify(...)` / `.trash(...)` outside `core.mailaction.usecases`) + `OutboundActionAuditMandatoryArchTest` (every Gmail mutation method writes audit row).
8. Update `chat/confirm/send/AssistantSendExecutor` (Phase 7) để pass `WEB_CHAT_CONFIRMED` qua `OutboundSendCommand.source`.

Purpose: Wave 2 (Telegram callback) call `OutboundSendGateway.send(source=TELEGRAM_CHAT_CONFIRMED)` và `MailActionService.archive(source=TELEGRAM_INLINE_BUTTON)` → audit shape ready, invariants enforced.

Output: Single send call site preserved, single non-send Gmail mutation surface, mandatory audit per mutation, NO regression trong triage flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-telegram-messaging-assistant/10-CONTEXT.md
@.planning/phases/10-telegram-messaging-assistant/10-SPEC.md
@.planning/phases/10-telegram-messaging-assistant/10-RESEARCH.md
@.planning/phases/10-telegram-messaging-assistant/10-PATTERNS.md
@CLAUDE.md
@backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendGateway.java
@backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendCommand.java
@backend/core/src/main/java/com/zeromail/core/outbound/usecases/GmailOutboundSendGateway.java
@backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java
@backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java
@backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java
@backend/core/src/main/java/com/zeromail/core/queue/domain/JobFailureReason.java
@backend/core/src/main/java/com/zeromail/core/shared/lang/IdentifiedEnum.java
@backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint OutboundActionSource + OutboundAction enums + outbound domain package-info</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/outbound/domain/OutboundActionSource.java,
    backend/core/src/main/java/com/zeromail/core/outbound/domain/OutboundAction.java,
    backend/core/src/main/java/com/zeromail/core/outbound/domain/package-info.java,
    backend/core/src/main/java/com/zeromail/core/outbound/package-info.java
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/queue/domain/JobFailureReason.java (mẫu IdentifiedEnum + fromId)
    - backend/core/src/main/java/com/zeromail/core/outbound/package-info.java (xem allowedDependencies current — Plan 02 mở thêm `outbound :: domain` sub-NamedInterface nếu cần)
    - .planning/phases/10-telegram-messaging-assistant/10-CONTEXT.md §D-01 (7 enum values + 13 action values)
  </read_first>
  <action>
    (A) `core/outbound/domain/OutboundActionSource.java` — IdentifiedEnum với 7 values theo D-01:
      ```java
      package com.zeromail.core.outbound.domain;
      // imports
      public enum OutboundActionSource implements IdentifiedEnum {
          RULE_AUTO,
          WEB_CHAT_CONFIRMED,
          TELEGRAM_INLINE_BUTTON,
          TELEGRAM_INLINE_BUTTON_DESTRUCTIVE_CONFIRMED,
          TELEGRAM_CHAT_CONFIRMED,
          TELEGRAM_DEEPLINK_FROM_NOTIFICATION,
          WEB_LEGACY;
          @Override public String id() { return name(); }
          public static OutboundActionSource fromId(String id) { ... }
      }
      ```

    (B) `core/outbound/domain/OutboundAction.java` — IdentifiedEnum với 13 values mirror outbound_action_audit.action CHECK:
      `SEND, REPLY, FORWARD, ARCHIVE, MARK_READ, MARK_UNREAD, MARK_SPAM, TRASH, SNOOZE, STAR, UNSTAR, ADD_TO_DIGEST, SAVE_DRAFT`. Same shape as JobFailureReason.

    (C) `core/outbound/domain/package-info.java` — `@NamedInterface("domain")`:
      ```java
      @org.springframework.modulith.NamedInterface("domain")
      package com.zeromail.core.outbound.domain;
      ```

    (D) `core/outbound/package-info.java` — verify allowed list cho phép modules consume `outbound :: domain` (web chat module + mailaction + messaging.telegram). Nếu existing `outbound/package-info.java` chỉ expose `outbound :: api`, add `domain` as additional NamedInterface — OR keep open module (no nest, just add `@NamedInterface` on the domain sub-package). Use second approach (per PATTERNS.md). KHÔNG sửa allowedDependencies của outbound module itself; chỉ thêm NamedInterface trên sub-package.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:test --tests '*OutboundActionSourceTest*' --tests 'com.zeromail.core.arch.OnlyOneGmailSendCallSiteTest' -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>2 enum compiles + fromId fail-loud; @NamedInterface published; existing OnlyOneGmailSendCallSiteTest still green.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo OutboundActionAuditEntity + Repository + Writer service</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/outbound/persistence/OutboundActionAuditEntity.java,
    backend/core/src/main/java/com/zeromail/core/outbound/persistence/OutboundActionAuditRepository.java,
    backend/core/src/main/java/com/zeromail/core/outbound/persistence/OutboundActionAuditWriter.java,
    backend/core/src/main/java/com/zeromail/core/outbound/persistence/package-info.java,
    backend/core/src/test/java/com/zeromail/core/outbound/persistence/OutboundActionAuditWriterIntegrationTest.java
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java (mẫu @Entity + AbstractTenantOwnedEntity + @JdbcTypeCode(JSON))
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java (mẫu writer service shape + same-tx invariant)
    - backend/core/src/main/java/com/zeromail/core/shared/persistence/AbstractTenantOwnedEntity.java (parent class — `id`, `tenantId`, tenant filter)
    - backend/core/src/main/resources/db/changelog/changes/100-outbound-action-audit.yaml (column types — verify ánh xạ chính xác)
  </read_first>
  <action>
    (A) `OutboundActionAuditEntity.java`:
      - `@Entity @Table(name = "outbound_action_audit") extends AbstractTenantOwnedEntity`
      - Fields ánh xạ tới columns changeset 100:
        - `gmailMessageId String @Column(name="gmail_message_id", length=64)` (nullable)
        - `gmailThreadId String @Column(name="gmail_thread_id", length=64)` (nullable)
        - `action String @Column(name="action", length=32, nullable=false)` — store enum value via `@Enumerated(EnumType.STRING)` HOẶC plain String + helper conversion in writer (chọn enum String per Convention 4)
        - `source String @Column(name="source", length=64, nullable=false)` + `@Enumerated(EnumType.STRING)` cho `OutboundActionSource`
        - `initiatedByUserId UUID @Column(name="initiated_by_user_id")` (nullable)
        - `initiatedAt Instant @Column(name="initiated_at", nullable=false)`
        - `succeeded boolean @Column(name="succeeded", nullable=false)`
        - `failureReason String @Column(name="failure_reason", length=100)` (nullable)
        - `metadataJson String @Column(name="metadata_json", columnDefinition="jsonb", nullable=false) @JdbcTypeCode(SqlTypes.JSON)`
      - Constructor: `protected` no-arg (Hibernate) + public constructor with all fields.
      - Getters explicit; NO Lombok. Equals/hashCode via super (`AbstractTenantOwnedEntity` thường có).

    (B) `OutboundActionAuditRepository.java`:
      ```java
      public interface OutboundActionAuditRepository extends JpaRepository<OutboundActionAuditEntity, UUID> {
          List<OutboundActionAuditEntity> findByTenantIdAndGmailMessageIdOrderByInitiatedAtDesc(
              UUID tenantId, String gmailMessageId);
          // For dedup check by source pattern — used by Plan 07 TelegramNotificationListener
          boolean existsByTenantIdAndGmailMessageIdAndSourceStartingWith(
              UUID tenantId, String gmailMessageId, String sourcePrefix);
      }
      ```

    (C) `OutboundActionAuditWriter.java`:
      ```java
      @Component
      public class OutboundActionAuditWriter {
          private final OutboundActionAuditRepository outboundActionAuditRepository;
          public OutboundActionAuditWriter(OutboundActionAuditRepository outboundActionAuditRepository) { ... }

          @Transactional(propagation = Propagation.MANDATORY)
          public UUID record(UUID tenantId, String gmailMessageId, String gmailThreadId,
                             OutboundAction action, OutboundActionSource source,
                             UUID initiatedByUserId, boolean succeeded, String failureReason,
                             String metadataJson) {
              Objects.requireNonNull(action, "action must not be null");
              Objects.requireNonNull(source, "source must not be null");
              OutboundActionAuditEntity entity = new OutboundActionAuditEntity(
                  UUID.randomUUID(), tenantId, gmailMessageId, gmailThreadId,
                  action.name(), source.name(), initiatedByUserId, Instant.now(),
                  succeeded, failureReason, metadataJson != null ? metadataJson : "{}");
              return outboundActionAuditRepository.save(entity).getId();
          }
      }
      ```
      `Propagation.MANDATORY` đảm bảo caller PHẢI có tx — cement same-tx invariant.

    (D) `OutboundActionAuditWriter.persistence/package-info.java` — nếu chưa có, add `@NamedInterface("persistence")` để chỉ outbound module subpackages access.

    (E) Integration test `OutboundActionAuditWriterIntegrationTest.java` extends existing `PostgresContainerTest`:
      - Test 1: writer.record(...) success → 1 row in outbound_action_audit
      - Test 2: writer.record(...) WITHOUT enclosing @Transactional → IllegalTransactionStateException (Propagation.MANDATORY)
      - Test 3: invalid source / action enum literal → constraint violation (CHECK constraint hits)
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:test --tests 'com.zeromail.core.outbound.persistence.OutboundActionAuditWriterIntegrationTest' -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>Entity + repo + writer compile + integration test xanh; CHECK constraint validates 7 source enum values + 13 action enum values.</done>
</task>

<task type="auto">
  <name>Task 3: Update OutboundSendCommand + GmailOutboundSendGateway to carry+write source</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendCommand.java,
    backend/core/src/main/java/com/zeromail/core/outbound/usecases/GmailOutboundSendGateway.java,
    backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendCommand.java (current shape — record gồm `tenantId, gmailMessage` per RESEARCH §canonical_refs)
    - backend/core/src/main/java/com/zeromail/core/outbound/usecases/GmailOutboundSendGateway.java (verbatim PATTERNS line 274-300)
    - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java (Phase 7 caller — finds where OutboundSendCommand được build)
    - grep all `new OutboundSendCommand(` callers (likely AssistantSendExecutor + Phase 4 rule auto-send call site nếu có)
  </read_first>
  <action>
    (A) `OutboundSendCommand.java` — thêm field `OutboundActionSource source`:
      ```java
      public record OutboundSendCommand(
              UUID tenantId,
              UUID initiatedByUserId,    // nullable: null for RULE_AUTO
              com.google.api.services.gmail.model.Message gmailMessage,
              OutboundAction action,      // SEND | REPLY | FORWARD
              OutboundActionSource source) {
          public OutboundSendCommand {
              Objects.requireNonNull(tenantId, "tenantId");
              Objects.requireNonNull(gmailMessage, "gmailMessage");
              Objects.requireNonNull(action, "action");
              Objects.requireNonNull(source, "source");
          }
      }
      ```
      Note: nếu current record không có `action` (chỉ là SEND inferred), Plan này thêm `action` để generalize sang REPLY/FORWARD; record canonical fail-loud Objects.requireNonNull.

    (B) `GmailOutboundSendGateway.send(...)` — inject `OutboundActionAuditWriter` + write audit cùng tx:
      ```java
      @Component
      @AllowedSendCallSite
      public class GmailOutboundSendGateway implements OutboundSendGateway {
          private final GmailApiClientFactory gmailApiClientFactory;
          private final OutboundActionAuditWriter outboundActionAuditWriter;

          @Override
          @Transactional
          public OutboundSendResult send(OutboundSendCommand command) throws IOException {
              try {
                  Gmail gmail = gmailApiClientFactory.buildClientForTenant(command.tenantId());
                  Message sendResult = gmail.users().messages().send(USER_ID, command.gmailMessage()).execute();
                  String messageId = messageId(sendResult);
                  String threadId = threadId(sendResult);
                  outboundActionAuditWriter.record(
                      command.tenantId(), messageId, threadId,
                      command.action(), command.source(),
                      command.initiatedByUserId(), true, null, "{}");
                  return new OutboundSendResult(messageId, threadId);
              } catch (InvalidGrantException | IllegalStateException sendFailure) {
                  outboundActionAuditWriter.record(
                      command.tenantId(), null, null,
                      command.action(), command.source(),
                      command.initiatedByUserId(), false, mapFailureReason(sendFailure), "{}");
                  throw new OutboundSendException(sendFailure);
              }
          }
      }
      ```
      KHÔNG move `gmail.users().messages().send` call site — chỉ thêm audit. `OnlyOneGmailSendCallSiteTest` stays green.

    (C) Update `AssistantSendExecutor.java` (Phase 7 chat confirm-send):
      - Tìm chỗ build `OutboundSendCommand` — thêm `OutboundAction.SEND` / `.REPLY` / `.FORWARD` (theo logic existing) + `OutboundActionSource.WEB_CHAT_CONFIRMED`.
      - Khi truyền user-action `initiatedByUserId` = current authenticated user UUID (từ TenantContext/SecurityContext).
      - KHÔNG đụng pending action CAS logic — chỉ extend command shape.

    (D) Update bất kỳ Phase 4 rule auto-send caller nào (nếu existing — verify via grep `new OutboundSendCommand`): pass `OutboundActionSource.RULE_AUTO` + `OutboundAction.SEND` + `initiatedByUserId = null`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:test --tests 'com.zeromail.core.arch.OnlyOneGmailSendCallSiteTest' --tests '*OutboundSend*' --tests '*AssistantSend*' -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>`OutboundSendCommand` carries source/action; `GmailOutboundSendGateway` writes audit row in same @Transactional as send; all callers updated; `OnlyOneGmailSendCallSiteTest` GREEN (still 1 match); existing chat confirm-send tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Mint MailActionService interface + DefaultMailActionService impl + MailActionPerformed event</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/mailaction/usecases/MailActionService.java,
    backend/core/src/main/java/com/zeromail/core/mailaction/usecases/DefaultMailActionService.java,
    backend/core/src/main/java/com/zeromail/core/mailaction/usecases/package-info.java,
    backend/core/src/main/java/com/zeromail/core/mailaction/domain/MailActionPerformed.java,
    backend/core/src/main/java/com/zeromail/core/mailaction/domain/package-info.java,
    backend/core/src/test/java/com/zeromail/core/mailaction/usecases/DefaultMailActionServiceTest.java
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java (mẫu Gmail call shape — `executeGmailWrite` lambda pattern + `.modify(...)` / `.trash(...)` API usage)
    - backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java (constructor injection target)
    - backend/core/src/main/java/com/zeromail/core/outbound/usecases/OutboundSendGateway.java (interface mẫu)
    - .planning/phases/10-telegram-messaging-assistant/10-CONTEXT.md §D-03 (interface contract)
  </read_first>
  <action>
    (A) `MailActionService.java` interface (5 methods):
      ```java
      package com.zeromail.core.mailaction.usecases;

      public interface MailActionService {
          void archive(UUID tenantId, String gmailMessageId, OutboundActionSource source) throws IOException;
          void markRead(UUID tenantId, String gmailMessageId, OutboundActionSource source) throws IOException;
          void markSpam(UUID tenantId, String gmailMessageId, OutboundActionSource source) throws IOException;
          void trash(UUID tenantId, String gmailMessageId, OutboundActionSource source) throws IOException;
          void snooze(UUID tenantId, String gmailMessageId, Instant snoozeUntil, OutboundActionSource source) throws IOException;
      }
      ```

    (B) `DefaultMailActionService.java` — `@Component @Transactional` implementation:
      - Constructor inject: `GmailApiClientFactory gmailApiClientFactory`, `OutboundActionAuditWriter outboundActionAuditWriter`, `ApplicationEventPublisher applicationEventPublisher`.
      - Mỗi method:
        1. Call `gmail.users().messages().modify(USER_ID, gmailMessageId, modifyRequest).execute()` (hoặc `.trash(USER_ID, id).execute()` cho `trash`).
        2. Write audit: `outboundActionAuditWriter.record(tenantId, gmailMessageId, null, OutboundAction.ARCHIVE, source, null, true, null, "{}")`. (snooze: action = SNOOZE + metadataJson includes snoozeUntil).
        3. Publish `applicationEventPublisher.publishEvent(new MailActionPerformed(tenantId, gmailMessageId, OutboundAction.X, source, Instant.now()))`.
        4. On Gmail IOException: write audit `succeeded=false, failureReason=mapFailureReason(io)` rồi throw lên.
      - Label semantics (mirror TriageGmailWriter constants):
        - `archive` → remove `INBOX` label
        - `markRead` → remove `UNREAD` label
        - `markSpam` → add `SPAM` + remove `INBOX`
        - `trash` → call `.trash(USER_ID, id)` (different from modify)
        - `snooze` → custom label `[Zero Mail] Snoozed` + scheduled un-snooze (or label-only if scheduler deferred — verify TriageGmailWriter existing snooze pattern; if no existing snooze, simplify to: add `SNOOZED` label + store `snoozeUntil` in `metadataJson`. Real un-snooze worker deferred to follow-on phase.)

    (C) `MailActionPerformed.java` event record:
      ```java
      public record MailActionPerformed(
          UUID tenantId, String gmailMessageId, OutboundAction action,
          OutboundActionSource source, Instant performedAt) {}
      ```
      No body fields. Reserved for future listeners (Phase 10 doesn't register one).

    (D) `mailaction/usecases/package-info.java` + `mailaction/domain/package-info.java` — `@NamedInterface("usecases")` / `@NamedInterface("domain")` để `core.messaging.telegram` consume được.

    (E) `DefaultMailActionServiceTest.java`:
      - Mockito-mock `GmailApiClientFactory` → returns a stubbed `Gmail` client với chain-mock `users().messages().modify(...).execute()`
      - For each of 5 methods: assert Gmail mock called once, `outboundActionAuditWriter.record(...)` called once with correct action/source, event published.
      - Negative test: Gmail throws IOException → audit row written with succeeded=false, exception propagates.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:test --tests 'com.zeromail.core.mailaction.usecases.DefaultMailActionServiceTest' -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>5 methods compile + each writes audit row via OutboundActionAuditWriter + publishes MailActionPerformed; tests xanh.</done>
</task>

<task type="auto">
  <name>Task 5: Refactor TriageGmailWriter to delegate sang MailActionService + cập nhật Modulith deps</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java,
    backend/core/src/main/java/com/zeromail/core/triage/package-info.java,
    backend/core/src/main/java/com/zeromail/core/chat/package-info.java
  </files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java (đọc trọn — xác định 5 methods archive/markRead/markSpam/trash/markUnread/star tương ứng; verify methodName chính xác trong codebase)
    - backend/core/src/main/java/com/zeromail/core/triage/package-info.java (allowedDependencies current)
    - backend/core/src/main/java/com/zeromail/core/chat/package-info.java (allowedDependencies — chat module likely cần mailaction usecase nếu nó cũng dùng — verify)
  </read_first>
  <action>
    (A) `TriageGmailWriter.java`:
      - Inject `MailActionService mailActionService` qua constructor.
      - Refactor 5 methods (`archiveSkipInbox`, `markRead`, `markSpam`, `trash`, `snooze`) — delete `executeGmailWrite(...)` body, replace với `mailActionService.<method>(tenantId, gmailMessageId, OutboundActionSource.RULE_AUTO)`.
      - KEEP: `executeGmailWrite(...)` private helper nếu vẫn dùng cho non-mail-action operations (label resolution, draft create) — chỉ refactor 5 hard-coded methods kể trên.
      - KEEP: existing logging format (Convention 5) — `event=triage_gmail_archive_recorded tenantId={}` etc.
      - Existing tests should still pass — TriageGmailWriter contract unchanged from caller POV; only implementation delegates.

    (B) `core/triage/package-info.java` allowedDependencies — add `mailaction :: usecases` + `outbound :: domain` (cho OutboundActionSource enum reference).

    (C) `core/chat/package-info.java` allowedDependencies — add `mailaction :: usecases` nếu Phase 7 chat đã (hoặc sẽ) dùng MailActionService. Inspect — nếu chỉ web SSE confirm-send dùng OutboundSendGateway (đã có outbound :: api), không cần thêm mailaction. Plan này chỉ thêm mailaction nếu compile force.

    Modulith verification: `ApplicationModulesTest` must stay green.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:test --tests '*Triage*' --tests '*ApplicationModules*' -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>TriageGmailWriter delegates sang MailActionService; existing triage tests xanh (zero behavior regression); Modulith verification xanh.</done>
</task>

<task type="auto">
  <name>Task 6: Fill ArchUnit MailActionServiceArchTest + OutboundActionAuditMandatoryArchTest</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/arch/MailActionServiceArchTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/OutboundActionAuditMandatoryArchTest.java
  </files>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java (mẫu method-call scan)
    - .planning/phases/10-telegram-messaging-assistant/10-PATTERNS.md ("ArchUnit invariant test" shared pattern)
  </read_first>
  <action>
    Replace placeholders với real ArchUnit assertions:

    (A) `MailActionServiceArchTest.java` — single non-send Gmail mutation surface:
      ```java
      @Test
      void gmail_modify_only_called_from_core_mailaction_usecases() {
          JavaClasses importedClasses = importProductionClasses();
          long offendingCount = importedClasses.stream()
              .flatMap(javaClass -> javaClass.getMethodCallsFromSelf().stream())
              .filter(MailActionServiceArchTest::isGmailModifyOrTrashCall)
              .filter(call -> !call.getOriginOwner().getPackageName().startsWith("com.zeromail.core.mailaction.usecases"))
              .count();
          assertThat(offendingCount)
              .as("Gmail .modify(...) / .trash(...) call sites must live in core.mailaction.usecases (TG-02)")
              .isZero();
      }

      private static boolean isGmailModifyOrTrashCall(JavaMethodCall call) {
          String owner = call.getTargetOwner().getName().replace('$', '.');
          String name = call.getName();
          return owner.endsWith("Gmail.Users.Messages") && (name.equals("modify") || name.equals("trash") || name.equals("untrash"));
      }
      ```

    (B) `OutboundActionAuditMandatoryArchTest.java` — every Gmail mutation method writes audit:
      ```java
      @Test
      void every_gmail_send_method_writes_outbound_action_audit_in_same_method_body() {
          JavaClasses importedClasses = importProductionClasses();
          // Find methods that call gmail.users().messages().send(...) and assert same method body also calls OutboundActionAuditWriter.record(...)
          importedClasses.stream()
              .flatMap(javaClass -> javaClass.getMethods().stream())
              .filter(OutboundActionAuditMandatoryArchTest::callsGmailSend)
              .forEach(method -> assertThat(callsAuditWriter(method))
                  .as("Method %s calls Gmail send but does NOT write OutboundActionAudit (TG-03/D-01)", method.getFullName())
                  .isTrue());
      }

      @Test
      void mailaction_default_service_methods_each_write_audit() {
          // assert DefaultMailActionService 5 methods each have a method-body call to OutboundActionAuditWriter.record
          JavaClasses importedClasses = importProductionClasses();
          // ... iterate the class DefaultMailActionService, assert each public method calls outboundActionAuditWriter.record
      }
      ```
      Implementation hint: use ArchUnit `JavaMethod.getCallsFromSelf()` to find calls inside method body; cross-reference with method origin to ensure same method writes audit.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "./gradlew.bat :backend:core:test --tests 'com.zeromail.core.arch.MailActionServiceArchTest' --tests 'com.zeromail.core.arch.OutboundActionAuditMandatoryArchTest' --tests 'com.zeromail.core.arch.OnlyOneGmailSendCallSiteTest' -q; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>2 ArchUnit tests pass with real assertions; OnlyOneGmailSendCallSiteTest still green (1 match).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail mutate boundary | All Gmail `.modify(...)` / `.trash(...)` flows through MailActionService; ArchUnit prevents bypass |
| Audit-per-mutation invariant | Audit write happens in same @Transactional as Gmail call; rollback together on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | Future code path performs Gmail mutate without going through MailActionService | mitigate | `MailActionServiceArchTest` scans all Gmail `.modify(...)` / `.trash(...)` calls and fails if any reside outside `core.mailaction.usecases` |
| T-10-12 | Repudiation | Mutation happens but audit row missing (race / forgotten write) | mitigate | `OutboundActionAuditMandatoryArchTest` scans every method that calls Gmail send/modify, asserts same method body calls `OutboundActionAuditWriter.record`; `Propagation.MANDATORY` on writer forces tx context |
| T-10-13 | Tampering | Audit DB bypass via direct repository call (not via writer) | mitigate | Future plan can add `OutboundActionAuditAccessArchTest` if needed; for v1.3 the writer remains the single entry point; Repository is package-private candidate (verify) |
</threat_model>

<verification>
- `./gradlew :backend:core:test --tests 'com.zeromail.core.outbound.persistence.*' --tests 'com.zeromail.core.mailaction.usecases.*' --tests 'com.zeromail.core.arch.MailActionServiceArchTest' --tests 'com.zeromail.core.arch.OutboundActionAuditMandatoryArchTest' --tests 'com.zeromail.core.arch.OnlyOneGmailSendCallSiteTest' --tests '*Triage*'` xanh
- ApplicationModulesTest xanh
- Manual: trigger a triage rule auto-archive on dev DB → 1 row in `outbound_action_audit` with `source='RULE_AUTO', action='ARCHIVE'`
</verification>

<success_criteria>
- 7-value OutboundActionSource enum + 13-value OutboundAction enum
- OutboundActionAuditEntity + Repository + Writer (Propagation.MANDATORY)
- OutboundSendCommand carries source + action
- GmailOutboundSendGateway writes audit cùng tx với send
- MailActionService interface + DefaultMailActionService impl (5 methods, audit + event publish)
- TriageGmailWriter delegates 5 archive-like methods sang MailActionService
- AssistantSendExecutor passes WEB_CHAT_CONFIRMED
- MailActionServiceArchTest + OutboundActionAuditMandatoryArchTest real assertions green
- OnlyOneGmailSendCallSiteTest still 1 match (zero regression)
</success_criteria>

<output>
Tạo `.planning/phases/10-telegram-messaging-assistant/10-02-SUMMARY.md`:
- Final shape of OutboundActionAuditEntity column mapping
- Snooze semantics decision (label + metadataJson vs. scheduled un-snooze)
- TriageGmailWriter method-name mapping (existing → mailaction delegate)
- ArchUnit pattern used for `callsAuditWriter` detection
- Any Modulith dep adjustments needed (chat/triage adding mailaction :: usecases)
</output>
