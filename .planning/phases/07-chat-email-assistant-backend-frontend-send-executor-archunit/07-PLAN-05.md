---
phase: 07-chat-email-assistant-backend-frontend-send-executor-archunit
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/ConfirmationLeaseService.java
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/ConfirmationStateMachine.java
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AllowedSendCallSite.java
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/GmailMessageBuilder.java
  - backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantWriteExecutor.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/WriteReversibleToolHandlers.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/ConfirmRequiredToolHandlers.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/ConfirmedSendToolHandlers.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/AssistantMemoryService.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/AssistantKnowledgeService.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/AssistantPersonalInstructionsService.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/ConfirmActionService.java
  - backend/api/src/main/java/com/zeromail/api/controllers/chat/ConfirmController.java
  - backend/api/src/test/java/com/zeromail/api/controllers/chat/ConfirmControllerShellIT.java
  - backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java
  - .github/workflows/gates.yml
  - backend/core/src/test/java/com/zeromail/core/chat/confirm/ConfirmationRaceIT.java
  - backend/core/src/test/java/com/zeromail/core/chat/confirm/AuditAtomicityIT.java
  - backend/core/src/test/java/com/zeromail/core/chat/confirm/AssistantSendExecutorVipIT.java
  - backend/core/src/test/java/com/zeromail/core/chat/confirm/ConfirmationLeaseServiceIT.java
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
  - ARCH-04
  - CHAT-04
  - CHAT-05
  - SET-SAFE-05
nyquist_compliant: true
must_haves:
  truths:
    - "Exactly ONE Gmail send call site exists in production: AssistantSendExecutor with @AllowedSendCallSite annotation"
    - "OnlyOneGmailSendCallSiteTest assertion flips from isEqualTo(0L) to isEqualTo(1L) in the SAME commit as AssistantSendExecutor and updated NoGmailSendAllowedTest and CI grep gate flip"
    - "NoGmailSendAllowedTest flips allowEmptyShould(true) -> allowEmptyShould(false) with @AllowedSendCallSite early-return"
    - "CI grep gate in .github/workflows/gates.yml flips EXECUTOR_COUNT from 0 to exactly 1"
    - "ConfirmControllerShellIT (Wave 3 paired-flip partner) is DELETED in the same atomic commit because controller now wires the executor"
    - "Confirmation state machine handles double-click race: exactly 1 Gmail send + 1 audit row even on concurrent confirms"
    - "Stale toolCallId (after Edit changed the id) -> 404 response"
    - "Confirm-during-stream -> blocked until stream completes or canceled"
    - "Same @Transactional boundary writes assistant_action_audit row + flips chat_message confirmation state (ARCH-04)"
    - "100 concurrent confirms produce exactly 100 audit rows + 100 confirmed states"
    - "AssistantSendExecutor re-checks sender_safety_net server-side: VIP recipient without acknowledgment flag -> 409 (SET-SAFE-05 server-side reject)"
    - "Gmail Message-ID is pre-generated <tenantId>.<chatId>.<toolCallId>@zero-mail.invalid (Pitfall 9 retry idempotency)"
    - "Redis lease commits BEFORE Gmail send call; Gmail send call is OUTSIDE any @Transactional"
    - "HIGH-5 pre-send audit pattern: INSERT assistant_action_audit row with state='SEND_IN_FLIGHT' + pre-generated Message-ID BEFORE Gmail send; UPDATE same row to state='COMMITTED' + sent_at AFTER Gmail success in same @Transactional as pending_action state flip"
    - "Exactly 24 tools wired in ChatToolCatalog: 8 read (Wave 2) + 7 write-reversible + 6 confirm-required + 3 confirmed-send (per the 24-Tool Authoritative List in Plan 03) — HIGH-1 Path A restored + HIGH-2 createRule upgrade"
    - "createRule wired as confirm-required (HIGH-2) with a preview card body slot (rule preview + destructive-action language); NOT write-reversible"
    - "saveMemory + addToKnowledgeBase + updatePersonalInstructions wired per HIGH-1 Path A — backing services AssistantMemoryService / AssistantKnowledgeService / AssistantPersonalInstructionsService own the writes; audit rows use tool_category='write-reversible' or 'confirm-required' accordingly"
    - "Both AssistantSendExecutor and AssistantWriteExecutor persist audit rows into the SINGLE assistant_action_audit table (per Plan 02 HIGH-2 revision); tool_category discriminator distinguishes them; state column tracks SEND_IN_FLIGHT/COMMITTED/FAILED for HIGH-5"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java"
      provides: "ARCH-01 — sole Gmail send call site (@AllowedSendCallSite)"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AllowedSendCallSite.java"
      provides: "Marker annotation referenced by ArchUnit rules"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/confirm/ConfirmationLeaseService.java"
      provides: "Redis SETNX 5-min lease + LockBackendUnavailableException fail-loud"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/confirm/ConfirmationStateMachine.java"
      provides: "ARCH-03 — CAS on assistant_pending_action.parts_updated_at + 3-race protection (HIGH-4)"
    - path: "backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java"
      provides: "Count flips 0->1 in this commit [ATOMIC-GROUP: arch01-flip]"
  key_links:
    - from: "AssistantSendExecutor"
      to: "ConfirmationLeaseService"
      via: "lease acquired BEFORE Gmail.users().messages().send()"
      pattern: "lease.*before.*send"
    - from: "AssistantSendExecutor"
      to: "assistant_action_audit insert"
      via: "same @Transactional as state flip (ARCH-04); tool_category='confirmed-send'"
      pattern: "@Transactional.*assistantActionAuditRepository"
    - from: "AssistantWriteExecutor"
      to: "assistant_action_audit insert"
      via: "same @Transactional as state flip; tool_category='write-reversible' or 'confirm-required'"
      pattern: "@Transactional.*assistantActionAuditRepository"
    - from: "@AllowedSendCallSite"
      to: "ArchUnit early-return in NoGmailSendAllowedTest + OnlyOneGmailSendCallSiteTest"
      via: "annotation FQN string referenced in ArchUnit rule"
      pattern: "@AllowedSendCallSite"
---

<plan_preamble>
**Wave 4 — Confirmation state machine + AssistantSendExecutor + ArchUnit 0->1 flip.**

This is **the most dangerous moment in Phase 7** per `07-RESEARCH.md` §"Key Findings" and Pitfall 1. The ArchUnit count flip 0->1 MUST land as a SINGLE atomic commit including all six files of [ATOMIC-GROUP: arch01-flip]:

1. `backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AllowedSendCallSite.java` — NEW annotation
2. `backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java` — NEW class, sole Gmail send call site
3. `backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java` — FLIP from `isEqualTo(0L)` to `isEqualTo(1L)`
4. `backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java` — FLIP from `allowEmptyShould(true)` to `allowEmptyShould(false)` with `@AllowedSendCallSite` early-return
5. `.github/workflows/gates.yml` — FLIP `EXECUTOR -ne 0` check to `EXECUTOR -ne 1` check
6. `backend/api/src/test/java/com/zeromail/api/controllers/chat/ConfirmControllerShellIT.java` — DELETED (Wave 3 paired-flip partner; the 501 shell is now wired to executor)

If ANY of those 6 files lands without the others in the same commit, the build fails (count drift OR shell-state mismatch) — which is the desired safety behavior. Task 5.4 packages all 6 as a single commit. Do NOT split.

**Audit table — unified per Plan 02 HIGH-2 revision.** `AssistantSendExecutor` and `AssistantWriteExecutor` both insert into `assistant_action_audit` (NOT separate `assistant_send_audit` + `assistant_write_audit` tables; NOT a new 047 changelog). The `tool_category` column discriminates. Gmail-specific columns (`gmail_message_id`, `recipient_hash`, `subject_hash`) are nullable and populated only for `confirmed-send` rows.

**24-tool catalog reference (cycle-1 revision).** The authoritative tool list lives at the top of Plan 03 (`## 24-Tool Authoritative List`). This wave wires the remaining 16 = 7 write-reversible + 6 confirm-required + 3 confirmed-send. Per cycle-1 review HIGH-1 Path A, the 4 personalization tools (`saveMemory`, `addToKnowledgeBase`, `updatePersonalInstructions`) are NOW INCLUDED (`searchMemories` ships in Wave 2 read tools per Plan 03 revision). Per HIGH-2, `createRule` upgraded from write-reversible → confirm-required. Still EXCLUDED: `unarchiveThread`, `createOrGetLabel`, `getInboxStats`, `readEmail` (deferred or earlier aliases per Plan 03).
</plan_preamble>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Confirm HTTP request -> AssistantSendExecutor | Lease must be held; CAS must succeed; cross-tenant chatId rejected |
| @Transactional boundary -> Gmail.users().messages().send() | Gmail send MUST be OUTSIDE tx (Pitfall 2 connection-pool starvation) |
| sender_safety_entry -> preview card render | Server-side re-check at confirmation; frontend hardening alone is bypassable (Pitfall 13) |
| ArchUnit invariant + CI grep gate + AssistantSendExecutor | ARCH-01 must flip atomically; any weakening = silent regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-23 | Repudiation | Second Gmail send call site appears post-Wave-4 | mitigate | `OnlyOneGmailSendCallSiteTest` asserts count == 1; CI grep gate fails if non-`AssistantSendExecutor` send call appears |
| T-07-24 | Tampering | Double-click race produces 2 sends | mitigate | Redis SETNX lease + UNIQUE (chat_id, tool_call_id) on assistant_action_audit + CAS on assistant_pending_action.parts_updated_at (HIGH-4) |
| T-07-25 | Repudiation | Audit row written without state flip (or vice versa) | mitigate | Same `@Transactional` boundary; ARCH-04 acceptance test confirms 100/100 atomicity |
| T-07-26 | Tampering | VIP recipient send fires without acknowledgment | mitigate | Server-side intersect with `sender_safety_entry` at confirm time; 409 reject if `vipAcknowledged=false` |
| T-07-27 | Tampering | Gmail send retry double-delivers because Message-ID not pre-generated | mitigate | `GmailMessageBuilder` pre-generates Message-ID `<tenantId>.<chatId>.<toolCallId>@zero-mail.invalid` (Pitfall 9) |
| T-07-28 | DoS | @Transactional wraps Gmail send -> connection-pool starvation | mitigate | Gmail send OUTSIDE tx; lease commit BEFORE send; same-tx audit + state flip AFTER send |
| T-07-29 | Spoofing | Confirm endpoint accepts cross-tenant toolCallId | mitigate | Verify `assistant_pending_action.tenantId == TenantContext.currentTenantUuid()`; 404 (not 403) on mismatch |
| T-07-30 | Tampering | Stale toolCallId after Edit -> confirm fires on old plan | mitigate | CAS on `assistant_pending_action.parts_updated_at` fails -> return 409 with `stale_tool_call` code (HIGH-4) |
</threat_model>

<objective>
Land the confirmation state machine, the sole Gmail send call site `AssistantSendExecutor`, the remaining 16 tool wirings (7 write-reversible + 6 confirm-required + 3 confirmed-send — exactly per Plan 03's 24-Tool Authoritative List), wire the `ConfirmController` to call the new state machine + executor, and **atomically flip ArchUnit count 0->1 + CI grep gate 0->1 + NoGmailSendAllowedTest scope + delete Wave-3 ConfirmControllerShellIT** in a single commit.

Purpose: Wave 4 is the load-bearing safety boundary. Every invariant from ARCH-01/03/04 + SET-SAFE-05 + Pitfall 1/2/9/13 lives here.

Output: Confirmation state machine + executor + lease service + Gmail message builder + 16 tool wirings + ConfirmController body + ArchUnit + CI gate atomic flip + 4 critical tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-CONTEXT.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-SPEC.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-RESEARCH.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-PATTERNS.md
@CLAUDE.md
@CONVENTIONS.md

# 24-Tool Authoritative List (locked in Plan 03; 8 read + 7 write-reversible + 6 confirm-required + 3 confirmed-send)
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-PLAN-03.md

# Codebase analogs (read once)
@backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java
@backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java
@backend/core/src/main/java/com/zeromail/core/shared/lock/RedisDistributedLock.java
@backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java
@backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: ConfirmationLeaseService + ConfirmationStateMachine + GmailMessageBuilder + ConfirmationLeaseServiceIT</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/chat/confirm/ConfirmationLeaseService.java,
    backend/core/src/main/java/com/zeromail/core/chat/confirm/ConfirmationStateMachine.java,
    backend/core/src/main/java/com/zeromail/core/chat/confirm/send/GmailMessageBuilder.java,
    backend/core/src/test/java/com/zeromail/core/chat/confirm/ConfirmationLeaseServiceIT.java
  </files>
  <action>
    **`ConfirmationLeaseService`** — mirror `RedisDistributedLock.java` pattern (per `07-PATTERNS.md` §2.12):
    - Inject `StringRedisTemplate` via `ObjectProvider<StringRedisTemplate>` + supplier pattern (lines 21-38 of `RedisDistributedLock.java`).
    - Method `boolean tryAcquire(UUID chatId, String toolCallId, String processInstanceId)` -> `redisTemplate.opsForValue().setIfAbsent("chat-confirm:" + chatId + ":" + toolCallId, processInstanceId, Duration.ofMinutes(5))`. Returns `true` if lease acquired, `false` if already held.
    - Method `void release(UUID chatId, String toolCallId)` -> DEL. Best-effort; TTL expires regardless.
    - Throw `LockBackendUnavailableException` when Redis is down (per `RedisDistributedLock.java` lines 44-46).
    - Log: `event=chat_confirmation_lease_acquired tenantId={} chatId={} ttlSeconds=300` / `event=chat_confirmation_lease_released ...`. Never log the full key (embeds chatId — but chatId is per-tenant scoped so logging chatId is acceptable; just don't log the value/process instance).

    **`ConfirmationStateMachine`** — `@Service`. Per `07-RESEARCH.md` §"Pattern 2" verbatim with name expansion.
    - Method `ConfirmationStateMachine.Reservation reserve(UUID chatId, UUID tenantId, String toolCallId, Instant previouslyObservedPartsUpdatedAt)`:
      Optimistic CAS lives on `assistant_pending_action` (HIGH-4 — CAS moved off `chat_message`; `chat_message.parts` is append-only projection). The CAS flips `assistant_pending_action.state` from `PENDING` to `PROCESSING` only when `parts_updated_at` still matches the previously-observed value. The `bump_pending_action_parts_updated_at` trigger (Plan 02 changelog 042) auto-bumps `parts_updated_at` on every state transition so retries / stale Edits fail loudly.
      ```java
      int updatedRowCount = jdbcTemplate.update(
          "UPDATE assistant_pending_action SET state = 'PROCESSING' " +
          "WHERE tool_call_id = ? AND chat_id = ? AND tenant_id = ? AND parts_updated_at = ? AND state = 'PENDING'",
          toolCallId, chatId, tenantId, Timestamp.from(previouslyObservedPartsUpdatedAt));
      if (updatedRowCount != 1) {
          throw new StaleToolCallException(toolCallId);   // 409 or 404 — Wave 4 ConfirmController maps to 404 per ARCH-03 acceptance
      }
      return new Reservation(...);
      ```
    - Method `UUID recordSendInFlight(SendInFlightCommand command)` (HIGH-5 NEW) — INSERT pre-send audit row `state='SEND_IN_FLIGHT'`, `in_flight_at=now()`, `sent_at=NULL`, `gmail_message_id` populated with pre-generated Message-ID. Called by `AssistantSendExecutor` BEFORE Gmail call. Returns the audit row UUID for the post-send flip.
    - Method `void commitSendCompleted(UUID auditId, SendCommitCommand command)` (HIGH-5 renamed from `commitConfirmed`) — same-tx UPDATE of the SEND_IN_FLIGHT row to `state='COMMITTED'`, `sent_at=now()` + flip pending_action state to `CONFIRMED`. Called by `AssistantSendExecutor` AFTER Gmail returns successfully.
    - Method `void commitSendFailed(UUID auditId, String toolCallId, String reasonCode)` (HIGH-5 NEW) — UPDATE the SEND_IN_FLIGHT row to `state='FAILED'` + flip pending_action state to `FAILED`. Called by `AssistantSendExecutor` catch-block on Gmail IOException AND by reconciliation cron Sweep B when Gmail confirms no message.
    - Method `void commitWriteReversible(WriteCommitCommand commitCommand)` — same-tx audit + state flip for write-reversible / confirm-required tools (called by `AssistantWriteExecutor`). Insert into `assistant_action_audit` with `tool_category='write-reversible'` or `'confirm-required'`, `tool_name` from `ChatToolName.id()`, `state='COMMITTED'` directly (no SEND_IN_FLIGHT intermediate — there's no Gmail call to fail), `gmail_message_id`/`recipient_hash`/`subject_hash` left NULL, `result_summary_json` populated with affected ids.
    - Method `void commitCanceled(UUID chatId, String toolCallId)` — sets pending action state to `CANCELED`; emits cancel audit row (`state='COMMITTED'`, tool_category from the original action). Reachable from `ConfirmController` cancel path (Task 5.4).
    - Method `void commitFailedFromReconciler(UUID chatId, String toolCallId, String reasonCode)` — sets state to `FAILED` for the lease-expired-no-audit case (Sweep A); reconciliation cron calls this.

    **`GmailMessageBuilder`** — `@Component`. Pre-generates Gmail Message-ID for retry idempotency + HIGH-5 reconciliation lookup:
    ```java
    public String generateMessageId(String tenantId, UUID chatId, String toolCallId) {
        return "<%s.%s.%s@zero-mail.invalid>".formatted(tenantId, chatId, toolCallId);
    }
    public com.google.api.services.gmail.model.Message build(AssistantSendCommand command, String preGeneratedMessageId) { ... }
    ```
    The pre-generated Message-ID is also stored on the `assistant_action_audit.gmail_message_id` column at SEND_IN_FLIGHT INSERT time — Plan 04's reconciler queries Gmail by this exact Message-ID to recover from crashes (HIGH-5 Sweep B).
    Builds the MIME message (subject, from, to, cc, bcc, body, In-Reply-To/References headers for `replyEmail`/`forwardEmail`, plus the `Message-ID:` header set to the pre-generated value). Returns a `com.google.api.services.gmail.model.Message` ready for `gmail.users().messages().send(...)`. **This builder does NOT call send — `AssistantSendExecutor` is the sole caller.**

    **`ConfirmationLeaseServiceIT`** (extends `PostgresContainerTest` with Redis Testcontainer or use existing v1.0 Redis test fixture — verify via Grep `Redis` in `core/shared/lock` tests):
    1. Acquire lease for `(chatA, toolA)` -> ✅ true.
    2. Re-acquire same key -> false (lease held).
    3. Wait > 5 min OR explicit `release(chatA, toolA)` -> re-acquire -> true.
    4. Acquire `(chatA, toolA)` then simulate Redis down (close container) -> `tryAcquire` throws `LockBackendUnavailableException` fail-loud.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.chat.confirm.ConfirmationLeaseServiceIT"</automated>
  </verify>
  <done>Lease service round-trips on Redis; fail-loud on outage. State machine compiles with both `commitConfirmed` and `commitWriteReversible` paths against the unified audit table. `mcp__jetbrains__get_file_problems` clean.</done>
  <deviation_handling>If v1.0 doesn't have a Redis Testcontainer base test class, extend `PostgresContainerTest` and add `@Container static RedisContainer ...` per Testcontainers Redis module docs. Verify the Spring Session Redis test runs against a Testcontainer in v1.0 — if so, reuse that fixture base.</deviation_handling>
  <threat_ref>T-07-24, T-07-28, T-07-30</threat_ref>
  <post-task>`mcp__jetbrains__get_file_problems` on every Java file.</post-task>
</task>

<task type="auto">
  <name>Task 5.2: Wire the 16 remaining tool handlers (7 write-reversible + 6 confirm-required + 3 confirmed-send) + AssistantWriteExecutor + AssistantMemory/Knowledge/PersonalInstructions services — using the unified assistant_action_audit table</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantWriteExecutor.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/WriteReversibleToolHandlers.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/ConfirmRequiredToolHandlers.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/ConfirmedSendToolHandlers.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/AssistantMemoryService.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/AssistantKnowledgeService.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/AssistantPersonalInstructionsService.java
  </files>
  <action>
    Wire the remaining 16 tools per the **24-Tool Authoritative List** in Plan 03 (`## 24-Tool Authoritative List`). Group by class to avoid 16 separate files (each class is a `@Service` exposing `Map<ChatToolName, ChatWriteToolHandler>` via `@PostConstruct` self-registration into `ChatToolCatalog`).

    **NO new changelog 047.** Both `AssistantWriteExecutor` and `AssistantSendExecutor` persist audit rows into the **single** `assistant_action_audit` table introduced by Plan 02 Task 2.1 (changelog 044). The `tool_category` column discriminates `'confirmed-send'` vs `'write-reversible'` vs `'confirm-required'`. The `state` column (HIGH-5) tracks `SEND_IN_FLIGHT`/`COMMITTED`/`FAILED`. Gmail-specific columns are nullable. Phase 7 ships exactly 6 Liquibase changelogs (041–046).

    **`AssistantWriteExecutor`** (NOT a send call site — ArchUnit treats it separately per AI-SPEC §4):
    - Executes write-reversible + confirm-required tools after user confirm.
    - Method `AssistantWriteResult execute(AssistantWriteCommand command)`:
      1. Verify `TenantContext` matches command tenantId.
      2. Acquire/verify Redis lease (typically already held by `ConfirmActionService` caller).
      3. Dispatch to the appropriate v1.0 backend service based on `command.toolName()`:
         - `applyLabel` / `removeLabel` -> v1.0 `core.gmail` label modify.
         - `archiveThread` -> v1.0 Gmail archive (= REMOVE_LABEL `INBOX`).
         - `createRule` / `updateRule` / `disableRule` -> v1.0 `core.rules`.
         - `saveDraft` -> v1.0 Gmail drafts().create(). NOT a send — `gmail.users().drafts().create(...)` does not invoke `messages().send(...)`, so `NoGmailSendAllowedTest` does not flag it.
         - `deleteRule` -> v1.0 `core.rules` delete.
         - `removeSenderFromSafetyNet` -> v1.0 `core.triage` sender_safety_entry delete.
         - `bulkArchive` -> v1.0 Gmail batch archive (= REMOVE_LABEL `INBOX` for each threadId).
      4. Capture result summary (affected ids).
      5. Call `confirmationStateMachine.commitWriteReversible(...)` — same-tx audit insert into `assistant_action_audit` (with `tool_category` = "write-reversible" or "confirm-required") + state flip.
      6. Release lease.

    **`WriteReversibleToolHandlers`** — wires exactly 7 tools per the 24-Tool Authoritative List:
    - `applyLabel(messageId, labelId)` -> v1.0 `core.gmail` label modify.
    - `removeLabel(messageId, labelId)` -> same.
    - `archiveThread(threadId)` -> v1.0 Gmail archive (= REMOVE_LABEL `INBOX`).
    - `updateRule(ruleId, ...)` -> v1.0 `core.rules` update.
    - `disableRule(ruleId)` -> v1.0 `core.rules` disable.
    - `saveDraft(toolCallArgs)` -> v1.0 Gmail `drafts().create(...)` (verify path; this is `gmail.users().drafts().create(...)` — NOT a send call site).
    - **`addToKnowledgeBase(title, content)`** -> calls `AssistantKnowledgeService.append(...)` (HIGH-1 restored). Inserts into `assistant_knowledge_snippet` (schema lives at changelog 046 per Plan 02). Reversible — admin can delete from Phase 8 Settings UI.

    Note: `createRule` is **NOT** in this group — moved to confirm-required per HIGH-2 (a domain-wide auto-archive rule is arguably more destructive than a single archive).

    Do NOT wire `unarchiveThread`, `createOrGetLabel`, or `getInboxStats` — they are explicitly EXCLUDED from the 24-Tool Authoritative List.

    **`ConfirmRequiredToolHandlers`** — wires exactly 6 tools per the 24-Tool Authoritative List:
    - **`createRule(displayName, when, then)`** — HIGH-2 upgrade. Calls v1.0 `core.rules` create AFTER user reviews the preview card. Preview body slot shows the proposed rule's `when` matcher + `then` actions in human-readable form (Plan 06 `create-rule-body.tsx`).
    - `deleteRule(ruleId)` — destructive (rule loss).
    - `removeSenderFromSafetyNet(senderEmailHash)` — destructive (VIP protection loss).
    - `bulkArchive(threadIds)` — destructive (scale of effect).
    - **`saveMemory(content)`** — HIGH-1 restored. Calls `AssistantMemoryService.save(...)`. Inserts into `assistant_memory`. Preview body slot shows the proposed memory text so the user can edit before persisting.
    - **`updatePersonalInstructions(newInstructions)`** — HIGH-1 restored. Calls `AssistantPersonalInstructionsService.overwrite(...)`. Updates `assistant_settings.personal_instructions`. Preview body slot shows a before/after diff (Plan 06 `update-personal-instructions-body.tsx`).

    Same execution path as write-reversible but with destructive-action / explicit-confirm language in the preview card (handled in Plan 06 Task 6.5 — now 6 confirm-required body slots, not 3). Audit row uses `tool_category='confirm-required'`.

    **Backing services**:
    - `AssistantMemoryService.save(tenantId, content)` — `@Transactional`. Validates content length ≤ 4000 chars; persists via `AssistantMemoryJpaRepository`. Returns the saved row id for the audit `result_summary_json`. NOT logged (per Privacy invariant — even though chat config is DB-persistable, the content stays out of application logs).
    - `AssistantKnowledgeService.append(tenantId, title, content)` — `@Transactional`. Validates content length ≤ 8000 chars; persists via `AssistantKnowledgeSnippetJpaRepository`.
    - `AssistantPersonalInstructionsService.overwrite(tenantId, newInstructions)` — `@Transactional`. Loads current value (for the audit diff), validates length ≤ 2000 chars (matches `PersonalizationSanitizer` cap — Plan 02), updates `assistant_settings.personal_instructions`. Records `result_summary_json = {before_length, after_length}` (lengths only, not content — privacy-safe).

    Each service is a `@Service` in `core.chat.usecases`. No cross-module wiring needed — `assistant_*` tables live in `core.chat`'s own persistence layer.

    **`ConfirmedSendToolHandlers`** — wires exactly 3 tools per the 24-Tool Authoritative List: `sendEmail`, `replyEmail`, `forwardEmail`. **These handlers do NOT execute Gmail send themselves** — they (a) validate the arg record (`SendEmailToolArgs` etc. from Plan 03 Task 3.1), (b) compute the `outside_source_thread` flag for `replyEmail`/`forwardEmail` (req #17 — recipient ∉ source thread participants — fetched via `getThread` tool output or repeated lookup), (c) compute the VIP intersection flag against `sender_safety_entry` (SET-SAFE-05), (d) emit `tool-input-available` envelope to the sink with those two flags so Plan 06 frontend renders the badge + banner. The actual `gmail.users().messages().send(...)` call is exclusively in `AssistantSendExecutor` (Task 5.3).

    **Tool count audit (PRE-COMMIT — fail-loud if mismatch) — cycle-1 revision: 24 tools, 8/7/6/3:**
    - Read tools (Plan 03 Task 3.3): 8 -> `searchInbox, getMessage, listLabels, getThread, listRules, getRule, getSenderSafetyEntry, searchMemories`.
    - Write-reversible (this task): 7 -> `applyLabel, removeLabel, archiveThread, updateRule, disableRule, saveDraft, addToKnowledgeBase`.
    - Confirm-required (this task): 6 -> `createRule, deleteRule, removeSenderFromSafetyNet, bulkArchive, saveMemory, updatePersonalInstructions`.
    - Confirmed-send (this task): 3 -> `sendEmail, replyEmail, forwardEmail`.
    - **Total: 8 + 7 + 6 + 3 = 24. Locked per HIGH-1 Path A + HIGH-2 revision. No deviation.** If implementation finds a 25th tool needed, STOP and escalate to a planner revision pass — do not silently add or drop.

    Run `ChatToolCatalog`'s startup validation (`size() == 24` + partition `8/7/6/3` + every name has a registered handler) — this lives in `SpringAiStreamingChatModelClient @PostConstruct` per LOW-5; fails boot on drift.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.chat.usecases.tools.*"</automated>
  </verify>
  <done>All 16 write-side tools wired into `ChatToolCatalog` (7 write-reversible + 6 confirm-required + 3 confirmed-send); total tool count = exactly 24 (8+7+6+3). `mcp__jetbrains__get_file_problems` clean. No new Gmail send call site introduced (sanity Grep before commit). No new Liquibase changelogs introduced (still 041–046).</done>
  <deviation_handling>If a v1.0 service path for a write-reversible tool differs from the planner's guess, use the actual v1.0 service via direct call per D-01 allowed deps. Do NOT add new gateway interfaces. Do NOT alter the 24-Tool Authoritative List without a planner revision pass.</deviation_handling>
  <threat_ref>T-07-15, T-07-16</threat_ref>
  <post-task>Run Grep `messages\(\)\.send\(` across `backend/core/src/main` — must still return 0 hits (no executor yet; Task 5.3 introduces it). Run `ChatToolCatalog`'s startup self-check (boot the API context briefly OR a unit test that constructs `ChatToolCatalog` and asserts `.size() == 24`).</post-task>
</task>

<task type="auto">
  <name>Task 5.3: AllowedSendCallSite annotation + AssistantSendExecutor + AuditAtomicityIT + AssistantSendExecutorVipIT</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AllowedSendCallSite.java,
    backend/core/src/main/java/com/zeromail/core/chat/confirm/send/AssistantSendExecutor.java,
    backend/core/src/test/java/com/zeromail/core/chat/confirm/AuditAtomicityIT.java,
    backend/core/src/test/java/com/zeromail/core/chat/confirm/AssistantSendExecutorVipIT.java
  </files>
  <action>
    **`AllowedSendCallSite`** — marker annotation:
    ```java
    @Target({ElementType.TYPE, ElementType.METHOD})
    @Retention(RetentionPolicy.RUNTIME)
    public @interface AllowedSendCallSite {
        String reason() default "ARCH-01 carve-out: AssistantSendExecutor is the sole Gmail send call site.";
    }
    ```
    Lives in `core.chat.confirm.send` exactly because the Wave 0 ArchUnit rule references its FQN `com.zeromail.core.chat.confirm.send.AllowedSendCallSite`.

    **`AssistantSendExecutor`** — `@Service`, class-annotated `@AllowedSendCallSite`. Single Gmail send call site in the entire codebase.

    **HIGH-5 send execution pattern — pre-send audit row + Message-ID reconciliation:**

    ```java
    public AssistantSendResult execute(AssistantSendCommand command) {
        // 1. Verify TenantContext + reservation already done by ConfirmActionService caller.

        // 2. Re-check VIP server-side (SET-SAFE-05). T-07-26.
        if (senderSafetyEntryQueryService.isSafetyNetRecipient(tenantId, recipientHash)
            && !command.vipAcknowledged()) {
            throw new VipAcknowledgmentMissingException(...);
        }

        // 3. Re-check outside-source-thread (req #17) — defense-in-depth.

        // 4. Acquire Redis lease BEFORE send (already held by caller usually; belt-and-braces).

        // 5. Build Gmail Message (pre-generated Message-ID per Pitfall 9 + HIGH-5).
        String preGeneratedMessageId = gmailMessageBuilder.generateMessageId(tenantId, chatId, toolCallId);
        com.google.api.services.gmail.model.Message gmailMessage =
                gmailMessageBuilder.build(command, preGeneratedMessageId);

        // 6. HIGH-5 Step A — INSERT pre-send audit row state='SEND_IN_FLIGHT' BEFORE Gmail call.
        //    Separate @Transactional via TransactionTemplate; commits before Gmail send begins.
        UUID auditId = transactionTemplate.execute(status ->
            confirmationStateMachine.recordSendInFlight(SendInFlightCommand.from(
                command, preGeneratedMessageId)));
        //  Row state: SEND_IN_FLIGHT, gmail_message_id populated, in_flight_at = now(), sent_at NULL.

        // 7. CALL GMAIL SEND — OUTSIDE any @Transactional (Pitfall 2). If we crash here, Plan 04's
        //    reconciliation cron Sweep B will query Gmail by gmail_message_id and reconcile.
        com.google.api.services.gmail.model.Message sendResult;
        try {
            sendResult = gmail.users().messages().send("me", gmailMessage).execute();
        } catch (IOException gmailFailure) {
            // 7a. Mark audit row FAILED + flip pending_action FAILED (separate @Transactional).
            transactionTemplate.execute(status ->
                confirmationStateMachine.commitSendFailed(auditId, toolCallId, gmailFailure.getMessage()));
            throw new GmailSendFailedException(gmailFailure);
        }

        // 8. HIGH-5 Step B — UPDATE the same audit row to state='COMMITTED' + sent_at + flip
        //    pending_action state, all in ONE @Transactional (ARCH-04 atomicity intact).
        transactionTemplate.execute(status ->
            confirmationStateMachine.commitSendCompleted(
                auditId, SendCommitCommand.from(command, sendResult)));

        // 9. Publish AssistantSendCompleted event (after-commit listener — event class from Plan 03).
        applicationEventPublisher.publishEvent(new AssistantSendCompleted(...));

        // 10. Release lease (best-effort; TTL handles crash).
        confirmationLeaseService.release(chatId, toolCallId);

        return AssistantSendResult.success(sendResult.getId());
    }
    ```

    **Crash window analysis (HIGH-5)**:
    - Crash between step 6 commit and step 7 Gmail call → reconciler Sweep B finds SEND_IN_FLIGHT row, queries Gmail, sees no message → marks FAILED. User retries with new toolCallId → new send attempt → no duplicate (the original Message-ID was never sent).
    - Crash AFTER step 7 Gmail success but BEFORE step 8 commit → reconciler Sweep B finds SEND_IN_FLIGHT row, queries Gmail BY MESSAGE-ID, sees the message → marks COMMITTED. User sees "Sent ✓" on next reload. No duplicate, no false-failed message.
    - Residual gap (MEDIUM-9 doc note in reconciler): Gmail accepts the message but the server died BEFORE step 6 committed (i.e., before the SEND_IN_FLIGHT row exists). This is rare (the pre-send INSERT is a small, fast transaction) but real — no DB row means no recovery path, and the user sees "failed" while the email did go out. Documented in `AssistantSendExecutor` javadoc as inherent to two-system delivery without 2PC.

    `ConfirmationStateMachine` additions (extends Plan 05 Task 5.1 definition):
    - `UUID recordSendInFlight(SendInFlightCommand command)` — INSERT pre-send row, return audit id.
    - `void commitSendCompleted(UUID auditId, SendCommitCommand command)` — UPDATE audit row + flip pending_action in ONE tx.
    - `void commitSendFailed(UUID auditId, String toolCallId, String reasonCode)` — UPDATE audit row to FAILED + flip pending_action.

    **Critical invariants in the body**:
    - The literal `gmail.users().messages().send(` appears EXACTLY ONCE in production code — here. CI grep gate checks this. Search for it via Grep before commit to confirm 1 hit.
    - The Gmail send call is OUTSIDE any `@Transactional`. The method itself MUST NOT carry `@Transactional`. `commitConfirmed(...)` carries `@Transactional` and is the boundary that wraps audit + state flip atomic.
    - Use full domain names: `gmailMessage`, `sendResult`, `assistantActionAuditRow`, `senderSafetyEntryQueryService`. NO `e`/`ex`/`req`/`res`/`svc`.
    - Log privacy-format: `event=chat_send_committed tenantId={} chatId={} auditId={} gmailMessageId={}`. Never log subject/body/recipient plaintext.

    **`AuditAtomicityIT`** (ARCH-04 + HIGH-5 acceptance):
    1. Seed a `chat` + 100 `chat_message` rows each with a pending `assistant_pending_action` for `sendEmail` to distinct recipients (or same; doesn't matter — distinct toolCallIds).
    2. Mock `gmail.users().messages().send(...)` to return a fake `Message` with a unique id per call (use Spring `@MockitoBean Gmail`).
    3. Run 100 confirm calls via `executor.execute(...)` concurrently using `CompletableFuture.allOf` on virtual threads (per CLAUDE.md virtual threads enabled).
    4. Assert: exactly 100 `assistant_action_audit` rows with `tool_category='confirmed-send' AND state='COMMITTED'` + 100 `assistant_pending_action` rows in `CONFIRMED` state + zero `SEND_IN_FLIGHT` audit rows remaining + zero `PROCESSING`/`PENDING` pending-action rows.
    5. **HIGH-5 mid-stream crash recovery**: invoke 100 confirms but kill 30 of them by mocking Gmail to succeed (returns a Message id) AND then throwing `InterruptedException` inside the mock BEFORE step 8 (`commitSendCompleted`). Outcome on first sweep: 30 audit rows stuck in `SEND_IN_FLIGHT` + 30 pending_actions in `PROCESSING`. After reconciliation cron Sweep B runs once → reconciler queries Gmail by Message-ID → finds the 30 sent messages → flips audit + pending to `COMMITTED` + `CONFIRMED`. Final state: 100/100 COMMITTED audit rows, zero residual SEND_IN_FLIGHT.
    6. **HIGH-5 genuine-failure recovery**: invoke 10 sends where Gmail mock throws `IOException` (delivery failure). Each `execute()` catches and calls `commitSendFailed`. Final state: 10 audit rows `state='FAILED'`, sent_at NULL; pending_actions `state='FAILED'`. No SEND_IN_FLIGHT leakage.

    **`AssistantSendExecutorVipIT`** (SET-SAFE-05 server-side reject):
    1. Seed tenant with `ceo@acme.com` in `sender_safety_entry`.
    2. Invoke `executor.execute(...)` for `sendEmail` to `ceo@acme.com` WITH `vipAcknowledged=false` -> expect `VipAcknowledgmentMissingException` -> ConfirmController (Task 5.4) maps to 409. Zero Gmail send call observed. Zero audit row in `assistant_action_audit`.
    3. Same invocation with `vipAcknowledged=true` -> ✅ 1 Gmail send + 1 audit row in `assistant_action_audit` (`tool_category='confirmed-send'`).
    4. Non-VIP recipient -> no acknowledgment required; normal flow.

    Note: `AssistantSendExecutor` is the FIRST production Gmail send call site. As soon as this file compiles, the Wave 0 ArchUnit `OnlyOneGmailSendCallSiteTest` MUST be flipped (Task 5.4) — they go together. Do NOT ship 5.3 without 5.4 in the same commit.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.chat.confirm.AuditAtomicityIT" --tests "com.zeromail.core.chat.confirm.AssistantSendExecutorVipIT"</automated>
  </verify>
  <done>`AssistantSendExecutor` compiles, both tests green. WITHOUT Task 5.4 in the same commit, the build WILL fail because count == 0 expectation in `OnlyOneGmailSendCallSiteTest` no longer holds. **This is the intended safety behavior.** Task 5.4 is the matching atomic commit.</done>
  <deviation_handling>If the v1.0 `Gmail` client bean is named differently or requires per-tenant credential resolution that's not yet wired for chat use, reuse the existing `core.gmail.gateway.*` GmailClientFactory pattern. Do NOT introduce a new credential path.</deviation_handling>
  <threat_ref>T-07-23, T-07-25, T-07-26, T-07-27, T-07-28</threat_ref>
  <post-task>`mcp__jetbrains__get_file_problems`. Then Grep `gmail.users().messages().send` across `backend/` (excluding tests) — must return EXACTLY 1 hit pointing to `AssistantSendExecutor.java`.</post-task>
</task>

<task type="auto">
  <name>Task 5.4: [ATOMIC-GROUP: arch01-flip] Flip OnlyOneGmailSendCallSiteTest 0->1 + NoGmailSendAllowedTest scope + CI gate + ConfirmController + ConfirmActionService + delete ConfirmControllerShellIT + ConfirmationRaceIT</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java,
    .github/workflows/gates.yml,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/ConfirmActionService.java,
    backend/api/src/main/java/com/zeromail/api/controllers/chat/ConfirmController.java,
    backend/api/src/test/java/com/zeromail/api/controllers/chat/ConfirmControllerShellIT.java,
    backend/core/src/test/java/com/zeromail/core/chat/confirm/ConfirmationRaceIT.java
  </files>
  <action>
    **THIS TASK MUST COMMIT AS A SINGLE ATOMIC COMMIT [ATOMIC-GROUP: arch01-flip].** Commit message tag: `feat(07): [ATOMIC-GROUP: arch01-flip] flip Gmail send call site count 0->1`. The 6 files (annotation + executor from Task 5.3 + the 4 flip files here + the shell-IT deletion + CI gate) are part of the same logical change. If executor is committed first and the flip after, CI is RED for one commit — which is acceptable as a transitional state IF AND ONLY IF the two commits are in the SAME PR. **Cleanest: combine into one commit.**

    **`OnlyOneGmailSendCallSiteTest`** (modify in place from Wave 0):
    - Change the positive count assertion: `assertThat(callSiteCount).isEqualTo(1L)` (was `0L`).
    - Inline comment: "Count flipped 0->1 in Wave 4 [ATOMIC-GROUP: arch01-flip] with `AssistantSendExecutor`."
    - Keep the `..chat.confirm.send..` package carve-out as-is (was correctly placed at Wave 0).

    **`NoGmailSendAllowedTest`** (modify in place — per `07-PATTERNS.md` §3 "Existing Invariant Tests to Update in Same PR"):
    - Add early-return: classes annotated `@com.zeromail.core.chat.confirm.send.AllowedSendCallSite` are excluded from the violation walk.
    - Flip `.allowEmptyShould(true)` -> `.allowEmptyShould(false)`.
    - Inline comment: "ARCH-01 carve-out — `@AllowedSendCallSite` excluded; paired with `OnlyOneGmailSendCallSiteTest.exactly_one_gmail_send_call_site_exists` which asserts count == 1."

    **`.github/workflows/gates.yml`** — flip the grep gate step (Wave 0 set this to expect 0; flip to expect 1):
    ```yaml
    if [ "$EXECUTOR" -ne 1 ]; then
      echo "::error::AssistantSendExecutor send call site count drifted from exactly 1 — ARCH-01"
      exit 1
    fi
    ```
    Plus add the same-commit cross-check (if not already added in Wave 0): the workflow step that fails when `NoGmailSendAllowedTest.java` is in `git diff` without `OnlyOneGmailSendCallSiteTest.java` in `git diff` (Pitfall 1 mitigation — research §"Pitfall 1").

    **`ConfirmActionService`** (`core.chat.usecases`) — coordinates the confirm flow:
    1. Resolve `TenantContext.currentTenantUuid()`; verify against `assistant_pending_action.tenantId` (404 on mismatch — T-07-29).
    2. Load `assistant_pending_action` by `(chat_id, tool_call_id)`. If missing -> 404. If state != `PENDING` -> 409 (`already_confirmed` / `already_canceled` / `processing`).
    3. Acquire Redis lease via `ConfirmationLeaseService.tryAcquire(...)` -> false -> 409 `lease_held`.
    4. CAS reservation via `ConfirmationStateMachine.reserve(...)` -> 404 or 409 `stale_tool_call` (T-07-30).
    5. Branch on tool category (look up `ChatToolName` -> category):
       - **Confirmed-send** (`sendEmail`, `replyEmail`, `forwardEmail`) -> call `AssistantSendExecutor.execute(...)`.
       - **Write-reversible + confirm-required** -> call `AssistantWriteExecutor.execute(...)`.
       - **Read tools** -> 400 (read tools don't go through confirm endpoint).
    6. Map exceptions to HTTP codes; return `ConfirmActionResponseDto`.

    **`ConfirmController`** (modify from Wave 3 shell):
    - Remove the 501 `NOT_IMPLEMENTED` body.
    - `@PostMapping("/{chatId}/confirm")` -> call `confirmActionService.confirm(chatId, requestDto, tenantId)`. Catch domain exceptions, map to HTTP. Also accept `@PostMapping("/{chatId}/cancel")` for the Cancel button path (calls `confirmationStateMachine.commitCanceled(...)`).

    **`ConfirmControllerShellIT`** — **DELETE THIS FILE** in the same atomic commit. The Wave 3 paired-flip partner has fulfilled its purpose (proving the shell returned 501 across all of Wave 3 + the start of Wave 4). With the controller now wired, the test would become a false alarm. The deletion lands inside [ATOMIC-GROUP: arch01-flip]. Use `git rm backend/api/src/test/java/com/zeromail/api/controllers/chat/ConfirmControllerShellIT.java` before staging.

    **`ConfirmationRaceIT`** (ARCH-03 acceptance — 3 race scenarios per `CompletableFuture.allOf`):
    1. **Double-click race**: 2 simultaneous confirm requests for same `(chatId, toolCallId)`. Mock Gmail send. Expect: exactly 1 row in `assistant_action_audit`, exactly 1 Gmail send call observed, second request returns 409.
    2. **Stale toolCallId race**: User Edits the draft (changes toolCallId from `T1` to `T2`), then both `T1` and `T2` confirm requests fire. Expect: `T1` returns 404 (CAS fails because `assistant_pending_action.parts_updated_at` advanced via the bump trigger when T2 was created); `T2` returns 200 + 1 audit row.
    3. **Confirm-during-stream race**: Trigger a stream that has emitted `tool-input-available` for `T1` but the stream is still in-flight. Fire confirm for `T1` before stream completes. Expect: blocked (lease conflict or pending state check) until stream completes; OR the orchestrator releases the lease at stream-end; OR returns 409 `stream_in_flight`. Match SPEC ARCH-03 acceptance: "confirm-during-stream -> blocked until stream completes or canceled".

    All three scenarios use real Postgres + Redis Testcontainers + mocked Gmail.

    **Pre-commit grep sanity** (run before staging):
    ```bash
    grep -rE 'messages\(\)\.send\(' backend/ --include='*.java' | grep -v Test
    # MUST return exactly 1 line pointing to AssistantSendExecutor.java
    ```

    **Pre-commit deletion sanity**:
    ```bash
    test ! -f backend/api/src/test/java/com/zeromail/api/controllers/chat/ConfirmControllerShellIT.java
    # MUST exit 0 (file gone)
    ```
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.arch.OnlyOneGmailSendCallSiteTest" --tests "com.zeromail.core.arch.NoGmailSendAllowedTest" --tests "com.zeromail.core.chat.confirm.ConfirmationRaceIT" :backend:api:test --tests "com.zeromail.api.controllers.chat.*"</automated>
  </verify>
  <done>All four tests green. `OnlyOneGmailSendCallSiteTest.exactly_one_gmail_send_call_site_exists` passes at count == 1. `NoGmailSendAllowedTest` passes with `@AllowedSendCallSite` carve-out. CI grep gate (run locally via `act` or via `gh workflow run gates.yml --ref <branch>`) passes. `ConfirmControllerShellIT.java` no longer exists in the tree. All 6 flip files in ONE commit per [ATOMIC-GROUP: arch01-flip].</done>
  <deviation_handling>If the grep gate sanity check returns 2+ hits or 0 hits, STOP. Do not commit. Investigate: either an accidental second call site was introduced (Task 5.2/5.3 deviation) or the executor file wasn't saved. Re-run after fix.</deviation_handling>
  <threat_ref>T-07-23, T-07-24, T-07-25, T-07-29, T-07-30</threat_ref>
  <post-task>`mcp__jetbrains__get_file_problems` on every Java file. Grep sanity: exactly 1 production Gmail send call site. File-existence sanity: `ConfirmControllerShellIT.java` deleted.</post-task>
</task>

</tasks>

<threats>
T-07-23, T-07-24, T-07-25, T-07-26, T-07-27, T-07-28, T-07-29, T-07-30 — see threat_model block.
</threats>

<verification>
- ArchUnit `OnlyOneGmailSendCallSiteTest.exactly_one_gmail_send_call_site_exists` passes at count == 1.
- ArchUnit `NoGmailSendAllowedTest` flipped + carve-out for `@AllowedSendCallSite` — green.
- CI grep gate `EXECUTOR_COUNT == 1` green via `scripts/ci/count-gmail-send-call-sites.sh`; same-commit cross-check (no flip without test-flip) green.
- `ConfirmControllerShellIT.java` deleted; no orphan test asserting 501 against the now-wired controller.
- `ConfirmationRaceIT` proves 3-scenario race safety (ARCH-03 acceptance) — CAS lives on `assistant_pending_action.parts_updated_at` (HIGH-4) not `chat_message.updated_at`.
- `AuditAtomicityIT` proves 100/100 atomicity into the unified `assistant_action_audit` table (ARCH-04) + HIGH-5 mid-stream crash recovery (30/30 SEND_IN_FLIGHT rows reconciled via Sweep B) + HIGH-5 genuine-failure recovery (10/10 FAILED).
- `AssistantSendExecutorVipIT` proves server-side VIP reject (SET-SAFE-05 acceptance) + pre-send SEND_IN_FLIGHT row is rolled back when VIP rejection fires (no orphan in-flight audit).
- `ConfirmationLeaseServiceIT` proves lease + fail-loud.
- All Wave 0/1/2/3 ArchUnit + tests still green.
- `ChatToolCatalog` reports `.size() == 24` at boot (8+7+6+3 per the 24-Tool Authoritative List) — `SpringAiStreamingChatModelClient @PostConstruct` enforces (LOW-5).
- `createRule` categorized as CONFIRM_REQUIRED (HIGH-2) with a preview body slot.
- `saveMemory` / `addToKnowledgeBase` / `updatePersonalInstructions` wired with backing services (HIGH-1 Path A).
- Phase 7 ships 6 Liquibase changelogs only (041–046). No 047.
- `mcp__jetbrains__get_file_problems` clean on every new file.
- Grep `messages().send(` in `backend/` excluding tests -> EXACTLY 1 hit pointing to `AssistantSendExecutor`.
</verification>

<success_criteria>
- [ ] `AssistantSendExecutor` is the SOLE Gmail send call site (grep verified)
- [ ] `OnlyOneGmailSendCallSiteTest` asserts count == 1
- [ ] `NoGmailSendAllowedTest` carves out `@AllowedSendCallSite`
- [ ] CI grep gate flipped to `EXECUTOR_COUNT == 1`
- [ ] `ConfirmControllerShellIT.java` deleted in the same atomic commit
- [ ] All 6 flip files committed as [ATOMIC-GROUP: arch01-flip]
- [ ] `ConfirmationRaceIT` 3 scenarios green
- [ ] `AuditAtomicityIT` 100/100 green (against unified `assistant_action_audit`)
- [ ] `AssistantSendExecutorVipIT` VIP reject + non-VIP pass green
- [ ] `ConfirmationLeaseServiceIT` lease + Redis-down fail-loud green
- [ ] Exactly 24 tools wired in `ChatToolCatalog` (8+7+6+3; matches Plan 03 24-Tool Authoritative List per HIGH-1 + HIGH-2)
- [ ] `createRule` categorized as CONFIRM_REQUIRED with preview body slot (HIGH-2)
- [ ] `saveMemory` / `addToKnowledgeBase` / `updatePersonalInstructions` handlers wired with backing services (HIGH-1 Path A)
- [ ] HIGH-5 pre-send audit pattern implemented: SEND_IN_FLIGHT INSERT → Gmail send → COMMITTED UPDATE; Message-ID stored at INSERT for reconciliation
- [ ] `commitSendCompleted` / `commitSendFailed` methods on `ConfirmationStateMachine` cover the SEND_IN_FLIGHT flip paths
- [ ] Both executors write to the single `assistant_action_audit` table; no `assistant_write_audit`, no 047 changelog
- [ ] ConfirmController accepts confirm + cancel; maps domain exceptions to HTTP codes
- [ ] Reconciliation cron heals after kill-mid-confirm
- [ ] `mcp__jetbrains__get_file_problems` clean on every new file
- [ ] All Wave 0–3 ArchUnit + tests still green
</success_criteria>

<output>
Create `.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-05-SUMMARY.md`. Record actual grep gate output + git log showing [ATOMIC-GROUP: arch01-flip] single-commit hash. Note the 24-tool catalog size assertion at boot and the unified `assistant_action_audit` table reuse (no new changelog).
</output>
