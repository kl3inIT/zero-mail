---
phase: 07-chat-email-assistant-backend-frontend-send-executor-archunit
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/chat/package-info.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/Chat.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/ChatId.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/ChatMessage.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/ChatMessageId.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/ToolCallId.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/ChatRole.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/ConfirmationState.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/ChatToolName.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/sendaction/SendEmailToolArgs.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/sendaction/ReplyEmailToolArgs.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/sendaction/ForwardEmailToolArgs.java
  - backend/core/src/main/java/com/zeromail/core/chat/domain/event/AssistantSendCompleted.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatLlmGateway.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatStreamRequest.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatStreamCommand.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatStreamSink.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/RawToolCall.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatToolCatalog.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/SanitizingSink.java
  - backend/core/src/main/java/com/zeromail/core/chat/llm/TenantAwareReactorScheduler.java
  - backend/core/src/main/java/com/zeromail/core/chat/llm/VercelProtocolEmitter.java
  - backend/core/src/main/java/com/zeromail/core/chat/llm/ChatToolCallRegistry.java
  - backend/core/src/main/java/com/zeromail/core/chat/llm/springai/ZeroMailChatMemory.java
  - backend/core/src/main/java/com/zeromail/core/chat/llm/springai/SpringAiStreamingChatModelClient.java
  - backend/core/src/main/java/com/zeromail/core/chat/llm/springai/SpringAiChatModelFactory.java
  - backend/core/src/main/java/com/zeromail/core/chat/llm/springai/ToolCallbackTranslator.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/SearchInboxToolHandler.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/GetMessageToolHandler.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/ListLabelsToolHandler.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/GetThreadToolHandler.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/ListRulesToolHandler.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/GetRuleToolHandler.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/GetSenderSafetyEntryToolHandler.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/SearchMemoriesToolHandler.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/LlmTokenizer.java
  - backend/core/src/test/java/com/zeromail/core/chat/llm/ChatToolCallRegistryIT.java
  - backend/core/src/test/java/com/zeromail/core/chat/llm/springai/ZeroMailChatMemoryIT.java
  - backend/core/src/test/java/com/zeromail/core/chat/llm/VercelProtocolEmitterTest.java
  - backend/core/src/test/java/com/zeromail/core/chat/llm/MultiTenantChatLeakIT.java
  - backend/core/src/test/java/com/zeromail/core/chat/usecases/tools/ReadToolsIT.java
autonomous: true
requirements:
  - ARCH-05
  - ARCH-07
  - CHAT-02
  - CHAT-03
nyquist_compliant: true
must_haves:
  truths:
    - "core.chat package-info.java declares @ApplicationModule with exactly the D-01 allowedDependencies set"
    - "ApplicationModulesTest passes with core.chat module now populated"
    - "ALL org.springframework.ai.* imports are confined to core.chat.llm.springai.* (ChatLlmAdapterBoundaryTest green)"
    - "TenantAwareReactorScheduler rebinds TenantContext ScopedValue on every scheduled task"
    - "MultiTenantChatLeakIT runs 10 tenants × 5 streams = 50 streams; every stream's tool output returns ONLY that tenant's data"
    - "ChatToolCallRegistry populates tool-call records from raw chunk deltas when Spring AI aggregator returns empty"
    - "ZeroMailChatMemory reconstructs conversation history from chat_message.parts JSONB directly; ZeroMailChatMemory.get(tokenBudget) accepts a token-budget parameter and truncates the oldest turns when budget exceeded (MEDIUM-7 fix)"
    - "VercelProtocolEmitter enforces ordering text-start → text-delta → text-end"
    - "8 read tool handlers wired into ChatToolCatalog with JSON-schema generated from Java records (HIGH-1 Path A restored searchMemories)"
    - "ChatLlmGateway.streamChat signature is Spring-AI-free (no org.springframework.ai.* types in interface)"
    - "ChatToolName enum contains exactly 24 entries matching the 24-Tool Authoritative List verbatim (HIGH-1 Path A; HIGH-2 upgrade)"
    - "SpringAiStreamingChatModelClient @PostConstruct calls chatToolCatalog.validate() — fails boot if catalog size != 24 (LOW-5 owner specified)"
    - "LlmTokenizer counts tokens for history truncation; default heuristic = 4 chars / token (chosen tokenizer documented in javadoc)"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/chat/package-info.java"
      provides: "Modulith boundary declaration per D-01"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/llm/TenantAwareReactorScheduler.java"
      provides: "ARCH-05 — tenant context propagation across Reactor"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/llm/ChatToolCallRegistry.java"
      provides: "ARCH-07 — Spring AI #3366/#5167 workaround (raw chunk capture)"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/llm/springai/ZeroMailChatMemory.java"
      provides: "ARCH-07 — replay history from chat_message.parts (not InMemoryChatMemory); lives in springai subpackage because it imports org.springframework.ai.chat.memory.ChatMemory"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/llm/springai/SpringAiStreamingChatModelClient.java"
      provides: "Spring AI adapter — all Spring AI imports confined here"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatToolCatalog.java"
      provides: "24-tool catalog (8 read tools wired in this wave; remaining 16 in Wave 4) — validate() asserts size == 24"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/domain/ChatToolName.java"
      provides: "Locked enumeration of the 24 tool names (8 read + 7 write-reversible + 6 confirm-required + 3 confirmed-send) per HIGH-1 Path A + HIGH-2 createRule upgrade"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/usecases/LlmTokenizer.java"
      provides: "MEDIUM-7 token budget — counts tokens for ZeroMailChatMemory.get(tokenBudget) truncation"
  key_links:
    - from: "SpringAiStreamingChatModelClient"
      to: "ChatToolCallRegistry"
      via: "per-chunk callback in Flux<ChatResponse> subscriber"
      pattern: "ChatToolCallRegistry.appendDelta"
    - from: "SpringAiStreamingChatModelClient"
      to: "TenantAwareReactorScheduler"
      via: ".subscribeOn(tenantAwareReactorScheduler.scheduler())"
      pattern: "tenantAwareReactorScheduler.scheduler"
    - from: "ChatLlmGateway interface"
      to: "ChatStreamSink emit-only events"
      via: "adapter translates Spring AI types behind the seam"
      pattern: "ChatLlmGateway implements"
---

## 24-Tool Authoritative List (REVISED per cycle-1 review)

This list is **canonical for Phase 7** and supersedes the earlier "20-tool" version. The revision restores the 4 memory/knowledge tools (HIGH-1 Path A — user-chosen) and upgrades `createRule` to confirm-required (HIGH-2). Final composition: **8 read + 7 write-reversible + 6 confirm-required + 3 confirmed-send = 24**. SPEC.md "20 tools" wording is updated to "24 tools" downstream.

Plans 05 and 06 reference this list verbatim. Do not add or rename entries without revising all three plans together.

| # | Category | Tool name (id) | Notes |
|---|----------|----------------|-------|
| 1 | Read | `searchInbox` | Gmail search wrapper |
| 2 | Read | `getMessage` | Single-message fetch (returns body to LLM in-memory only; sanitized before persist) |
| 3 | Read | `listLabels` | Gmail label list |
| 4 | Read | `getThread` | Thread fetch with participant list |
| 5 | Read | `getRule` | v1.0 `core.rules` single-rule fetch |
| 6 | Read | `listRules` | v1.0 `core.rules` list |
| 7 | Read | `getSenderSafetyEntry` | v1.0 `core.triage` sender_safety_entry lookup |
| 8 | Read | `searchMemories` | (HIGH-1 restored) v1.1 `core.chat` `assistant_memory` lookup |
| 9 | Write-reversible | `applyLabel` | Adds label to message/thread |
| 10 | Write-reversible | `removeLabel` | Removes label (including INBOX = archive) |
| 11 | Write-reversible | `archiveThread` | Convenience: removeLabel(INBOX) on a thread |
| 12 | Write-reversible | `updateRule` | v1.0 `core.rules` update |
| 13 | Write-reversible | `disableRule` | v1.0 `core.rules` disable |
| 14 | Write-reversible | `saveDraft` | Gmail drafts().create() — NOT a send |
| 15 | Write-reversible | `addToKnowledgeBase` | (HIGH-1 restored) appends to `assistant_knowledge_snippet`; reversible (admin can delete from settings later) |
| 16 | Confirm-required | `createRule` | (HIGH-2 upgrade — was write-reversible) destructive at scale (a domain-wide auto-archive rule needs explicit preview) |
| 17 | Confirm-required | `deleteRule` | Destructive — extra friction in preview card |
| 18 | Confirm-required | `removeSenderFromSafetyNet` | Destructive — extra friction |
| 19 | Confirm-required | `bulkArchive` | Destructive (scale of effect) — extra friction |
| 20 | Confirm-required | `saveMemory` | (HIGH-1 restored) writes to `assistant_memory`; preview card lets user review what gets persisted |
| 21 | Confirm-required | `updatePersonalInstructions` | (HIGH-1 restored) overwrites `assistant_settings.personal_instructions`; preview card shows before/after diff |
| 22 | Confirmed-send | `sendEmail` | Routes through `AssistantSendExecutor` |
| 23 | Confirmed-send | `replyEmail` | Routes through `AssistantSendExecutor`; outside-source-thread badge applies |
| 24 | Confirmed-send | `forwardEmail` | Routes through `AssistantSendExecutor`; outside-source-thread badge applies |

**Explicitly EXCLUDED** from the locked 24 (do not introduce in Phase 7):
- `unarchiveThread` (redundant with `applyLabel(INBOX)`)
- `readEmail` (use `getMessage` — same semantics, AI-SPEC name wins)
- `getInboxStats` (deferred; not in AI-SPEC §4)
- `createOrGetLabel` (deferred; not in AI-SPEC §4)

The 4 previously-excluded personalization tools (`saveMemory`, `searchMemories`, `addToKnowledgeBase`, `updatePersonalInstructions`) are now **included** per HIGH-1 Path A.

Preview card body slots (used by Plan 06 Task 6.5) map 1:1 to the **9 user-confirmable tools** = confirm-required (6) + confirmed-send (3) = #16..#24 above. **9 body slots, NOT 6** (HIGH-1 + HIGH-2 revision). Generic shell stays 1; body slots are 9.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spring AI SDK → core.chat.usecases | Vendor SDK confinement — leak past this boundary breaks D-02 |
| Reactor Scheduler thread → TenantContext ScopedValue | Pitfall 5 — wrong scheduler drops tenant binding mid-stream |
| Streaming chunk → tool-call reconstruction | ARCH-07 — Spring AI aggregator returns empty toolCalls |
| Read tool handler → v1.0 backend (rules engine, Gmail reader, sender_safety_entry) | Cross-module call must filter by tenantId at every join |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Information Disclosure | Reactor scheduler drops TenantContext → cross-tenant data | mitigate | `TenantAwareReactorScheduler` wraps every task in `ScopedValue.where(TENANT, capturedTenantId)`; `ChatNoReactorSchedulerTest` ArchUnit rule (Wave 0) bans direct `Schedulers.*` usage |
| T-07-12 | Tampering | Spring AI aggregator returns empty toolCalls → confirmation never fires → silent failure | mitigate | `ChatToolCallRegistry` populates from raw chunks; `ZeroMailChatMemory` replays from DB not from in-memory aggregator |
| T-07-13 | Information Disclosure | Spring AI imports leak outside adapter package | mitigate | `ChatLlmAdapterBoundaryTest` (Wave 0) enforces; `ChatLlmGateway` interface is Spring-AI-free; `ZeroMailChatMemory` lives in `core.chat.llm.springai` (not `core.chat.llm`) because it imports `org.springframework.ai.chat.memory.ChatMemory` |
| T-07-14 | Tampering | `VercelProtocolEmitter` emits events out of order → `useChat` parses partial JSON → frontend crash | mitigate | `VercelProtocolEmitter` ordering enforcement + unit test sweeps invariants |
| T-07-15 | Spoofing | Read tool returns data from wrong tenant | mitigate | Each handler resolves `TenantContext.currentTenantUuid()` AT call time + filters every query/repo call by tenantId |
| T-07-16 | Information Disclosure | Tool output leaks email body into `chat_message.parts` despite read-only tool | mitigate | All tool outputs route through `SanitizingSink` (wraps `ChatStreamSink` + applies `ToolOutputSanitizer` before persist) |
</threat_model>

<objective>
Land the `core.chat` Modulith module declaration, domain records + enums, usecases-layer interfaces (`ChatLlmGateway`, `ChatStreamSink`, `ChatToolCatalog`), Spring AI adapter (all vendor SDK imports confined), tenant-aware Reactor scheduler, Vercel UI Message Stream Protocol emitter, the two ARCH-07 workaround classes (`ChatToolCallRegistry` + `ZeroMailChatMemory`), and the 7 read-only tool handlers.

Purpose: Wave 2 establishes the LLM streaming + tool-invocation infrastructure WITHOUT user-facing endpoints (those land in Wave 3). Read tools land here because they don't need confirmation state machine (Wave 4) — they execute inline.

Output: ~30 new Java files + 5 ITs/unit tests. ArchUnit rules from Wave 0 now have something to enforce against and stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-CONTEXT.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-SPEC.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-RESEARCH.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-PATTERNS.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-AI-SPEC.md
@CLAUDE.md
@CONVENTIONS.md

# Codebase analogs (MUST read; do not re-read after the first pass)
@backend/core/src/main/java/com/zeromail/core/llm/package-info.java
@backend/core/src/main/java/com/zeromail/core/triage/package-info.java
@backend/core/src/main/java/com/zeromail/core/llm/usecases/LlmGateway.java
@backend/core/src/main/java/com/zeromail/core/llm/usecases/LlmGatewayImpl.java
@backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/SpringAiLlmModelClient.java
@backend/core/src/main/java/com/zeromail/core/tenant/concurrency/TenantAwareTaskScope.java
@backend/core/src/test/java/com/zeromail/core/support/PostgresContainerTest.java
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Modulith package-info + domain records + enums + event class</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/chat/package-info.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/Chat.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/ChatId.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/ChatMessage.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/ChatMessageId.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/ToolCallId.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/ChatRole.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/ConfirmationState.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/ChatToolName.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/sendaction/SendEmailToolArgs.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/sendaction/ReplyEmailToolArgs.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/sendaction/ForwardEmailToolArgs.java,
    backend/core/src/main/java/com/zeromail/core/chat/domain/event/AssistantSendCompleted.java
  </files>
  <action>
    **`package-info.java`** — copy `core/triage/package-info.java` shape per D-01. Declare:
    ```java
    @org.springframework.modulith.ApplicationModule(
        displayName = "Chat (assistant streaming + confirmation)",
        allowedDependencies = {
            "llm", "rules", "gmail", "triage",
            "tenant", "shared.persistence", "shared.lang", "shared.privacy"
        }
    )
    package com.zeromail.core.chat;
    ```
    Add Javadoc-style block comment mirroring v1.0 `core/llm/package-info.java` lines 18-22 with the privacy invariant: "MUST NOT log raw email-content prompts/completions; MAY persist user-typed chat config + structured tool I/O per Privacy scope carve-out."

    **Domain records (pure-Java, framework-free per CONVENTIONS #3)**:
    - `ChatId(UUID value)`, `ChatMessageId(UUID value)`, `ToolCallId(String value)` — value-object wrappers; compact constructor validates non-null + non-blank.
    - `Chat(ChatId id, String tenantId, String title, Instant createdAt, Instant updatedAt, Instant softDeletedAt)` — record.
    - `ChatMessage(ChatMessageId id, ChatId chatId, String tenantId, ChatRole role, ChatMessageParts parts, Instant createdAt)` — record; carries the `ChatMessageParts` domain wrapper from Wave 1.

    **Enums (per CONVENTIONS #4)**:
    - `ChatRole implements IdentifiedEnum<String>` — `USER("user")`, `ASSISTANT("assistant")`, `SYSTEM("system")`, `TOOL("tool")`. Static `fromId(String)` throws `NoSuchElementException` on unknown.
    - `ConfirmationState implements OrderedEnum<ConfirmationState>` — `PENDING`, `PROCESSING`, `CONFIRMED`, `CANCELED`, `FAILED`. Storage as string identifier; explicit `fromId(String)` fail-loud.
    - `ChatToolName implements IdentifiedEnum<String>` — exactly **24** entries per the **24-Tool Authoritative List** at the top of this plan. Define enum constants in the same category order:
      - Read (8): `SEARCH_INBOX("searchInbox")`, `GET_MESSAGE("getMessage")`, `LIST_LABELS("listLabels")`, `GET_THREAD("getThread")`, `GET_RULE("getRule")`, `LIST_RULES("listRules")`, `GET_SENDER_SAFETY_ENTRY("getSenderSafetyEntry")`, `SEARCH_MEMORIES("searchMemories")`.
      - Write-reversible (7): `APPLY_LABEL("applyLabel")`, `REMOVE_LABEL("removeLabel")`, `ARCHIVE_THREAD("archiveThread")`, `UPDATE_RULE("updateRule")`, `DISABLE_RULE("disableRule")`, `SAVE_DRAFT("saveDraft")`, `ADD_TO_KNOWLEDGE_BASE("addToKnowledgeBase")`. *(Wiring is Wave 4's job; this enum lists them here. Note `createRule` is NOT here — moved to confirm-required per HIGH-2.)*
      - Confirm-required (6): `CREATE_RULE("createRule")`, `DELETE_RULE("deleteRule")`, `REMOVE_SENDER_FROM_SAFETY_NET("removeSenderFromSafetyNet")`, `BULK_ARCHIVE("bulkArchive")`, `SAVE_MEMORY("saveMemory")`, `UPDATE_PERSONAL_INSTRUCTIONS("updatePersonalInstructions")`.
      - Confirmed-send (3): `SEND_EMAIL("sendEmail")`, `REPLY_EMAIL("replyEmail")`, `FORWARD_EMAIL("forwardEmail")`.

      Total = 24. Do NOT add `readEmail` / `unarchiveThread` / `getInboxStats` / `createOrGetLabel` (still EXCLUDED — they're earlier planning aliases / deferred). The 4 personalization tools that were previously excluded are now INCLUDED per HIGH-1 Path A.

      Add a unit test `ChatToolNameEnumTest` (in this task or co-located with `ChatToolCallRegistryIT`) asserting `ChatToolName.values().length == 24` and that the enum constants partition cleanly into the 4 categories with the documented sizes (8/7/6/3). Add a per-tool assertion that `createRule.category() == CONFIRM_REQUIRED` (HIGH-2). Plan 06 Task 6.4 ships the OpenAPI-export-driven contract test that double-checks this from the frontend side.

    **Sendaction args records** — `SendEmailToolArgs(String to, String subject, String body, String replyToMessageId)`, `ReplyEmailToolArgs(String sourceMessageId, String to, String body)`, `ForwardEmailToolArgs(String sourceMessageId, String to, String additionalBody)`. Compact constructors apply `@NotBlank` semantics via explicit `requireText(...)` helpers (per `TriageAuditEntity.java` pattern — see CONVENTIONS #3 + `TriageAuditEntity` private helpers).

    **`AssistantSendCompleted`** — record event with `String tenantId, UUID chatId, String toolCallId, UUID auditId, Instant sentAt`. Lives in `core.chat.domain.event` per CONVENTIONS #6 ("Domain events shared by API/worker/future modules belong in `backend/core`").
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.arch.*" :backend:api:test --tests "com.zeromail.api.ZeroMailApiApplicationModulesTest"</automated>
  </verify>
  <done>`ApplicationModulesTest` green with new `core.chat` module declaration. Wave 0 ArchUnit rules still green (Spring AI confinement, no scheduler, body ban, count == 0 send sites). Exactly 24 entries in `ChatToolName` enum (8 read + 7 write-reversible + 6 confirm-required + 3 confirmed-send per the 24-Tool Authoritative List) — verified by `ChatToolName.values().length == 24` assertion.</done>
  <deviation_handling>If `IdentifiedEnum`/`OrderedEnum` base interfaces don't exist in v1.0 (verify via Grep first), use existing v1.0 enum convention — likely `core.shared.lang.IdentifiedEnum` or equivalent; do NOT introduce new base contracts in Phase 7.</deviation_handling>
  <threat_ref>T-07-13, T-07-15</threat_ref>
  <post-task>`mcp__jetbrains__get_file_problems` on every new Java file.</post-task>
</task>

<task type="auto">
  <name>Task 3.2: Usecases interfaces + Spring AI adapter + Reactor scheduler + Vercel emitter + ARCH-07 workaround classes</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatLlmGateway.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatStreamRequest.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatStreamCommand.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatStreamSink.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/RawToolCall.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/ChatToolCatalog.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/SanitizingSink.java,
    backend/core/src/main/java/com/zeromail/core/chat/llm/TenantAwareReactorScheduler.java,
    backend/core/src/main/java/com/zeromail/core/chat/llm/VercelProtocolEmitter.java,
    backend/core/src/main/java/com/zeromail/core/chat/llm/ChatToolCallRegistry.java,
    backend/core/src/main/java/com/zeromail/core/chat/llm/springai/ZeroMailChatMemory.java,
    backend/core/src/main/java/com/zeromail/core/chat/llm/springai/SpringAiStreamingChatModelClient.java,
    backend/core/src/main/java/com/zeromail/core/chat/llm/springai/SpringAiChatModelFactory.java,
    backend/core/src/main/java/com/zeromail/core/chat/llm/springai/ToolCallbackTranslator.java,
    backend/core/src/test/java/com/zeromail/core/chat/llm/ChatToolCallRegistryIT.java,
    backend/core/src/test/java/com/zeromail/core/chat/llm/springai/ZeroMailChatMemoryIT.java,
    backend/core/src/test/java/com/zeromail/core/chat/llm/VercelProtocolEmitterTest.java,
    backend/core/src/test/java/com/zeromail/core/chat/llm/MultiTenantChatLeakIT.java
  </files>
  <action>
    **Usecases (Spring-AI-free seam per D-02):**

    - `ChatLlmGateway` — interface. Single method: `reactor.core.Disposable streamChat(ChatStreamRequest streamRequest, ChatStreamSink streamSink)`. Javadoc mirrors `LlmGateway.java` privacy invariant, then adds: "MAY persist user-typed chat config + structured tool I/O per Privacy scope carve-out; MUST NOT log raw email-body prompts/completions; the `internalToolExecutionEnabled(false)` flag is load-bearing — every implementation MUST set it." No Spring AI imports.

    - `ChatStreamRequest` (record): `String tenantId, UUID chatId, String modelId, String systemPrompt, ChatToolCatalog toolCatalog, List<ChatMessage> conversationHistory`. Pure Java.

    - `ChatStreamCommand` (record): inbound from `ChatController` — `String tenantId, UUID chatId, String userText, String modelOverride`. `toRequest(...)` factory inside `ChatOrchestrator` (Wave 3) translates.

    - `ChatStreamSink` — interface with methods: `void emitTextStart(String partId)`, `void emitTextDelta(String partId, String tokenText)`, `void emitTextEnd(String partId)`, `void emitToolInputStart(String toolCallId, String toolName)`, `void emitToolInputAvailable(String toolCallId, String toolName, String inputJson)`, `void emitToolOutputAvailable(String toolCallId, String outputJson)`, `void emitDataPersistence(UUID chatMessageId, String state)`, `void emitFinish(String reason)`, `void emitError(String code, String userFacingMessage)`, `void emitHeartbeat()`. Ordering rules enforced by impls.

    - `RawToolCall` (record): `String toolCallId, String toolName, StringBuilder argsDeltaAccumulator, boolean finalized` — populated by `ChatToolCallRegistry` from raw stream chunks (ARCH-07 workaround). Field name `argsDeltaAccumulator` is full domain; no `acc`.

    - `ChatToolCatalog` — immutable startup-built bean. Holds `List<ToolDefinition>` records (`ChatToolName name, String description, Class<?> argsRecordClass, ToolCategory category`). At construction, `validate()` asserts **exactly 24 distinct tool names** + descriptions + arg classes + category partition (8/7/6/3) against the 24-Tool Authoritative List — fail-loud if count or partition differs (HIGH-1 + HIGH-2 revision). Exposes `Optional<ToolDefinition> byName(ChatToolName name)` and `List<ToolDefinition> byCategory(ToolCategory category)`. Wave 2 wires 8 read tools; Wave 4 expands to all 24.

    - `SanitizingSink` — `ChatStreamSink` decorator wrapping a downstream sink and applying `ToolOutputSanitizer` to every `emitToolOutputAvailable(...)` payload BEFORE forwarding. Logs `event=chat_sink_sanitized tenantId={} chatId={} toolName={}`.

    - **`LlmTokenizer` (MEDIUM-7)** — `@Component`. Counts tokens for `ZeroMailChatMemory` truncation. Default impl: simple heuristic `Math.ceilDiv(stringLength, 4)` (≈ 4 chars per token across English+Vietnamese; documented in javadoc as conservative — slight overcounting is safe because it triggers earlier truncation, not later overflow). Method `int count(Message message)` walks the message text + any embedded tool args. If a real tokenizer is needed later (e.g., `jtokkit` for GPT models), extension point lives here. Per-model accuracy upgrades are Phase 8 territory; Phase 7 ships the heuristic + config knob `zeromail.chat.tokenizer.chars-per-token` (default 4).

    **LLM adapter package (`core.chat.llm`)** — pure-Java helpers, NO Spring AI imports:

    - `TenantAwareReactorScheduler` — `@Component`. Holds a reactor `Scheduler` built from a virtual-thread `Executor` (`Executors.newVirtualThreadPerTaskExecutor()` per CLAUDE.md "virtual threads enabled") wrapped via `Schedulers.fromExecutorService(...)`. Public `Scheduler scheduler()`. Internal task decorator wraps each Runnable in `ScopedValue.where(TenantContext.TENANT, capturedTenantId).run(...)` per `TenantAwareTaskScope.java` lines 18-26 pattern. Capture `currentTenantId` at construction-per-stream (i.e., return a NEW scheduler per stream via `forCurrentTenant()` factory, OR capture-on-each-task — pick one and document; the per-stream factory matches the existing `TenantAwareTaskScope.openInherit()` idiom and is recommended).

    - `VercelProtocolEmitter` — implements `ChatStreamSink`. Wraps a `SseEmitter`. Enforces ordering: `emitTextDelta` requires prior `emitTextStart` for the same `partId`; `emitTextEnd` requires prior `emitTextStart`; `emitToolInputAvailable` requires prior `emitToolInputStart`. Violation throws `IllegalStateException` (fail-loud). Heartbeat emits `: keepalive\n\n` (SSE comment frame). Each event serialized as Vercel UI Message Stream Protocol v1 JSON per `https://ai-sdk.dev/docs/ai-sdk-ui/stream-protocol` (event types: `text-start`, `text-delta`, `text-end`, `tool-input-start`, `tool-input-available`, `tool-output-available`, `data-<name>`, `finish`, `error`).

    - `ChatToolCallRegistry` — per-stream registry (NEW instance per stream — not @Component singleton). Method `void appendDelta(String toolCallId, String toolName, String argsChunkDelta)` appends to the registry's internal `Map<String, RawToolCall>`. Method `List<RawToolCall> finalize()` marks all entries finalized + returns. This is the ARCH-07 workaround: built from raw stream events, not from `AssistantMessage.toolCalls` (which is empty per #3366/#5167).

    **Spring AI adapter package (`core.chat.llm.springai`)** — ALL `org.springframework.ai.*` imports live ONLY here:

    - `ZeroMailChatMemory` — implements `org.springframework.ai.chat.memory.ChatMemory`. Lives under `core.chat.llm.springai` (NOT `core.chat.llm`) because importing a Spring AI type would otherwise breach `ChatLlmAdapterBoundaryTest`. Loads history from `ChatMessageJdbcRepository` (Wave 1) ordered by `created_at` ASC. `add(...)` is a no-op (we persist via `ChatOrchestrator` directly).
      **MEDIUM-7 token-budget signature**: in addition to the Spring-AI-required `List<Message> get(String conversationId)`, expose an overload `List<Message> get(String conversationId, int tokenBudget)` that the orchestrator (Plan 04) calls with the per-tenant computed budget. Algorithm: load all messages ordered ASC → iterate from newest backwards, accumulate token count via `LlmTokenizer.count(message)`, stop when the next message would exceed budget → reverse → return. The newest user turn is always included even if it alone exceeds budget (with a `truncated_history: true` flag in the assistant's system prompt). Budget rule per AI-SPEC §4b: 60% of model context window for history + leave 40% for tool outputs + final response.
      Hibernate reconstructs `AssistantMessage` with `.toolCalls(...)` populated directly from each `chat_message.parts` row's `ToolCallPart` envelopes (not from any Spring AI internal aggregator). Reconstructed `AssistantMessage` for HIGH-7's persisted assistant text uses `AssistantTextPart` (Plan 02 schema).

    - `SpringAiChatModelFactory` — `@Component`. Method `StreamingChatModel forTenant(String tenantId)`. Reads `assistant_settings.provider_id` for the tenant (Wave 1 repo). Default → platform OpenRouter via OpenAI adapter with `base-url: https://openrouter.ai/api/v1`. BYOK → resolve per-tenant provider via existing v1.0 `core.llm.gateway.springai.SpringAiByokChatSupport` (cross-module direct call — D-01 declares `llm` as allowed dep). Cache `StreamingChatModel` per tenant in a `Caffeine` cache keyed by `(tenantId, providerId, modelId)` (avoid rebuilding per turn; v1.0 has Caffeine on classpath).

    - `SpringAiStreamingChatModelClient` — `@Component` implementing `ChatLlmGateway`. **LOW-5 owner**: this bean carries `@PostConstruct void verifyCatalogContract() { chatToolCatalog.validate(); }` — fails Spring boot if the injected catalog's size is not 24 or partition doesn't match 8/7/6/3. Per-request flow:
      1. Build `Prompt` from `streamRequest.systemPrompt + conversationHistory + new user turn`.
      2. Build `OpenAiChatOptions` (or provider-specific options via factory) with `.temperature(0.2)`, `.maxTokens(2048)`, `.internalToolExecutionEnabled(false)`, `.toolCallbacks(toolCallbackTranslator.translate(streamRequest.toolCatalog()))`.
      3. `streamingChatModel.stream(prompt)` → `Flux<ChatResponse>` → `.subscribeOn(tenantAwareReactorScheduler.scheduler())` → per chunk: extract text-delta → `sink.emitTextDelta(...)`; extract tool-call delta (raw, not aggregated) → `chatToolCallRegistry.appendDelta(...)`.
      4. On terminal completion: `chatToolCallRegistry.finalize()` → for each finalized tool call → persist via `chatOrchestrator` callback (Wave 3) → `sink.emitToolInputAvailable(...)`.
      5. On error / cancel: do NOT emit `finish`; dispose subscription.
      Return the `Disposable` so `ChatController` (Wave 3) can wire it to `SseEmitter.onCompletion/onTimeout/onError`.

    - `ToolCallbackTranslator` — translates `ChatToolCatalog` to `List<org.springframework.ai.tool.function.FunctionToolCallback>`. Schema generated via `JsonSchemaGenerator.generateForType(toolDef.argsRecordClass())` (Spring AI util). Function body is `(input) -> Map.of()` (never invoked because `internalToolExecutionEnabled=false` — per AI-SPEC §4 Tool Use).

    **Tests**:

    - `ChatToolCallRegistryIT` (in `core.chat.llm` test package): feed 5 synthetic raw chunk events (mixing text deltas and tool-call argument deltas interleaved across the same `toolCallId`) → `finalize()` returns coherent `RawToolCall` with full argsJson string assembled in order.

    - `ZeroMailChatMemoryIT` (in `core.chat.llm.springai` test package, matching production package): extends `PostgresContainerTest`. Insert 4 `chat_message` rows for chat_id X (text-only, tool-call, tool-output, text-only) → `zeroMailChatMemory.get(chatX)` returns 4 messages with toolCalls populated FROM `chat_message.parts` (NOT from any Spring AI internal aggregator). Assert the `AssistantMessage` for the tool-call row has `.getToolCalls().size() == 1` even though no real Spring AI stream ran.

    - `VercelProtocolEmitterTest`: ordering invariants — `emitTextDelta` without prior `emitTextStart` → `IllegalStateException`; correct sequence emits JSON lines matching the Vercel UI Message Stream Protocol v1 grammar; heartbeat emits SSE comment frame `: keepalive\n\n` exactly.

    - `MultiTenantChatLeakIT` (port FND-05 pattern per `07-RESEARCH.md`): launch 10 tenants × 5 concurrent virtual-thread tasks each, each starting a fake stream through `SpringAiStreamingChatModelClient` (mock `StreamingChatModel.stream(...)` returning a `Flux<ChatResponse>` that emits 3 chunks then completes). Each task's chunk handler queries the DB (using a `ChatJpaRepository.findByTenantIdAndSoftDeletedAtIsNullOrderByUpdatedAtDesc(...)` call). Assert: every task observes EXACTLY its own tenant's data; ZERO cross-tenant rows leaked. Use `CompletableFuture.allOf(...)` to wait for all 50 streams.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.chat.llm.*"</automated>
  </verify>
  <done>All 4 tests green. `ChatLlmAdapterBoundaryTest` (Wave 0) green — Spring AI imports stayed confined to `core.chat.llm.springai.*` (including `ZeroMailChatMemory`). `ChatNoReactorSchedulerTest` (Wave 0) green — no bare `Schedulers.*` outside the scheduler abstraction. `MultiTenantChatLeakIT` reports zero leaks. `mcp__jetbrains__get_file_problems` clean.</done>
  <deviation_handling>If Spring AI 2.0.0-M6's `OpenAiChatOptions.Builder` lacks `.internalToolExecutionEnabled(boolean)` (verify via Context7 `mcp__context7__query-docs` `/websites/spring_io_spring-ai_reference_2_0-snapshot` for "ToolCallingChatOptions internalToolExecutionEnabled"), use `ToolCallingChatOptions.builder()` instead. Cross-check against v1.0 `SpringAiLlmModelClient.java` line 66 — that file already exercises the flag and is the canonical reference.</deviation_handling>
  <threat_ref>T-07-11, T-07-12, T-07-13, T-07-14</threat_ref>
  <post-task>`mcp__jetbrains__get_file_problems` on every Java file. Re-run all Wave 0 ArchUnit tests as a sanity sweep — especially `ChatLlmAdapterBoundaryTest` (the relocation of `ZeroMailChatMemory` into `springai` must keep this rule green).</post-task>
</task>

<task type="auto">
  <name>Task 3.3: 8 read-only tool handlers + ReadToolsIT (HIGH-1 restored searchMemories)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/SearchInboxToolHandler.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/GetMessageToolHandler.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/ListLabelsToolHandler.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/GetThreadToolHandler.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/ListRulesToolHandler.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/GetRuleToolHandler.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/GetSenderSafetyEntryToolHandler.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/tools/SearchMemoriesToolHandler.java,
    backend/core/src/test/java/com/zeromail/core/chat/usecases/tools/ReadToolsIT.java
  </files>
  <action>
    Each read-tool handler is a `@Service` implementing a common interface `ChatReadToolHandler` (define inside this task — `ChatToolName name()` + `String executeJson(String inputJson, String tenantId)` returning sanitized JSON output). Execution path: orchestrator (Wave 3) invokes the handler synchronously inline when LLM emits a read-tool call; output flows through `SanitizingSink` → `ToolOutputSanitizer` before persist → emitted to frontend.

    Each handler:
    1. Resolves `TenantContext.currentTenantUuid()` at method entry (defensive — should match `tenantId` param; fail-loud if not).
    2. Calls the v1.0 backend service via direct service call (D-01 — `rules`, `gmail`, `triage`, `tenant` are allowed deps). NO new gateway interfaces per CONTEXT decisions.
    3. Maps result to a tool-output record (no body fields per ARCH-02). Serializes via injected Jackson 3 `ObjectMapper` (`tools.jackson.databind.ObjectMapper`).
    4. Logs privacy-format: `event=chat_read_tool_executed tenantId={} toolName={} resultCount={}` — NEVER log args containing body or recipient.

    **Per-handler scope (call v1.0 services; do NOT invent new ones). Handler names follow the 24-Tool Authoritative List — `getMessage`, NOT `readEmail`; `getThread`, not `getInboxStats`. The 8th read tool `searchMemories` calls the v1.1 `assistant_memory` repository:**

    - `SearchInboxToolHandler` (`searchInbox`) — calls v1.0 Gmail read service (likely `core.gmail.usecases.GmailMessageReader` or similar; verify via Grep `core.gmail`). Returns list of (messageId, subject [hashed for log], snippet capped at 80 chars [the SNIPPET is sanitized by Gmail anyway and is fine for triage purposes — see CONVENTIONS Privacy carve-out], from, to, date, hasAttachment). NO `bodyHtml`/`bodyText`/`body`.
    - `GetMessageToolHandler` (`getMessage`) — calls v1.0 Gmail single-message fetch. Returns `(messageId, threadId, subject, from, to, cc, date, headers map, decodedTextBody)`. **The decoded text body is returned to the LLM IN-MEMORY ONLY** (acceptance: "returns body to LLM in-memory") but `SanitizingSink` strips body-shaped keys before the JSON envelope persists into `chat_message.parts`. Approach: handler returns the full body in `executeJson`; sanitizer at the sink strips for persist (single source of strip; handler need not double-strip). Wave 4 confirms via end-to-end ReadToolsIT scenario below.
    - `ListLabelsToolHandler` (`listLabels`) — calls v1.0 Gmail label list. Returns `[{id, name, type}]`.
    - `GetThreadToolHandler` (`getThread`) — calls v1.0 Gmail thread fetch. Returns `(threadId, participantList[], messageIds[], lastActivityAt)`. Used by reply/forward flows to compute the outside-source-thread badge in Plan 06.
    - `ListRulesToolHandler` (`listRules`) — calls v1.0 `core.rules` service. Returns canonical rule records.
    - `GetRuleToolHandler` (`getRule`) — calls v1.0 `core.rules` by id; same shape.
    - `GetSenderSafetyEntryToolHandler` (`getSenderSafetyEntry`) — calls v1.0 `core.triage` sender safety net lookup; returns `{recipientEmailHash, mode, addedAt}`.
    - **`SearchMemoriesToolHandler` (`searchMemories`)** — HIGH-1 restored. Calls `AssistantMemoryJpaRepository.findByTenantIdAndContentContainingIgnoreCase(tenantId, query, Pageable.ofSize(20))`. Returns `[{id, content (capped at 200 chars), createdAt}]`. Per Plan 02, the underlying `assistant_memory` table exists from changelog 046; entity + repository are already declared. The handler is purely a Wave 2 wiring task — no schema change needed.

    **`ReadToolsIT`** — `@SpringBootTest` slice covering all 8 handlers:
    1. Seed tenant + minimal Gmail/rules state via repos.
    2. Invoke each handler with synthetic input JSON; assert output JSON shape + no body fields after sanitizer pass.
    3. For `GetMessageToolHandler`: assert the in-memory output handed to the LLM includes the body, but the JSON envelope persisted via a synthetic `SanitizingSink` ends up body-stripped (chain `handler.execute → SanitizingSink.emitToolOutputAvailable → persist → query DB → grep parts JSON for body fields → ZERO matches`).
    4. For `SearchMemoriesToolHandler`: seed 3 `assistant_memory` rows for tenant A + 2 for tenant B; invoke handler with `query="Acme"` for tenant A → returns only tenant-A rows matching "Acme"; for tenant B → returns only tenant-B rows. Cross-tenant leak assertion.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.chat.usecases.tools.ReadToolsIT"</automated>
  </verify>
  <done>All 8 handlers compile, ReadToolsIT green (incl. SearchMemoriesToolHandler tenant isolation), `chat_message.parts` JSONB after the `getMessage` integration scenario contains zero body fields (3-layer ban end-to-end proof). Wave 0 ArchUnit rules still green.</done>
  <deviation_handling>If a v1.0 read service doesn't exist for one of the listed capabilities (e.g., `getThread` may not have a direct service — could need composition over `core.gmail`), then create the handler but use direct repository access via D-01 allowed deps. Do NOT invent new gateway interfaces; CONTEXT D-01 explicitly rejected them.</deviation_handling>
  <threat_ref>T-07-15, T-07-16</threat_ref>
  <post-task>`mcp__jetbrains__get_file_problems` on every Java file. Re-run `./gradlew :backend:core:test --tests "com.zeromail.core.arch.ChatPersistenceContentBanTest"` as sanity sweep.</post-task>
</task>

</tasks>

<threats>
T-07-11, T-07-12, T-07-13, T-07-14, T-07-15, T-07-16 — see threat_model block.
</threats>

<verification>
- `ApplicationModulesTest` green with `core.chat` populated.
- All 4 Wave 0 ArchUnit rules still green (source-aware body ban per HIGH-3, Spring AI confinement, no scheduler, count == 0 send sites).
- `MultiTenantChatLeakIT` reports zero cross-tenant data leaks across 10 × 5 = 50 streams.
- `ChatToolCallRegistry` populates from raw chunks; `ZeroMailChatMemory` reads from `chat_message.parts` (and lives in `core.chat.llm.springai`). `ZeroMailChatMemory.get(conversationId, tokenBudget)` honors budget (MEDIUM-7).
- `VercelProtocolEmitter` ordering enforcement verified.
- All 8 read tool handlers callable (incl. searchMemories per HIGH-1); `GetMessageToolHandler` does NOT persist body content (3-layer ban end-to-end proof).
- `ChatToolName.values().length == 24` asserted; partition 8/7/6/3.
- `createRule.category() == CONFIRM_REQUIRED` (HIGH-2).
- `SpringAiStreamingChatModelClient @PostConstruct` fails boot if catalog drifts (LOW-5).
- `mcp__jetbrains__get_file_problems` clean on every new file.
</verification>

<success_criteria>
- [ ] `core.chat/package-info.java` declares `@ApplicationModule` with D-01 allowedDependencies exactly
- [ ] Exactly 24 entries in `ChatToolName` enum matching the 24-Tool Authoritative List (8/7/6/3 partition)
- [ ] `createRule` categorized as CONFIRM_REQUIRED (HIGH-2)
- [ ] `searchMemories` wired as the 8th read tool (HIGH-1 Path A restored)
- [ ] `ChatToolCatalog.validate()` asserts size == 24 + partition (called from `SpringAiStreamingChatModelClient @PostConstruct` per LOW-5)
- [ ] `ZeroMailChatMemory.get(conversationId, tokenBudget)` overload honors budget (MEDIUM-7)
- [ ] `LlmTokenizer` bean exists with `chars-per-token` config knob
- [ ] `ChatLlmGateway` interface is Spring-AI-free (Grep `org.springframework.ai` in `core.chat.usecases.*` = 0 hits)
- [ ] `ZeroMailChatMemory` lives in `core.chat.llm.springai` (not `core.chat.llm`)
- [ ] All 4 Wave 0 ArchUnit rules STILL green (source-aware body ban per HIGH-3)
- [ ] `MultiTenantChatLeakIT` zero leaks across 10 × 5 streams
- [ ] All 8 read tool handlers + `ReadToolsIT` green (incl. SearchMemoriesToolHandler tenant isolation)
- [ ] `ApplicationModulesTest` green
- [ ] JetBrains diagnostics clean on every new file
</success_criteria>

<output>
Create `.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-03-SUMMARY.md`. Note the 24-Tool Authoritative List is now locked here; downstream plans (05, 06) reference it. Note HIGH-1 Path A (4 personalization tools restored) and HIGH-2 (createRule confirm-required upgrade).
</output>
