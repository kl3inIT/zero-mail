---
phase: 07-chat-email-assistant-backend-frontend-send-executor-archunit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/ChatPersistenceContentBanTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/ChatNoReactorSchedulerTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/ChatLlmAdapterBoundaryTest.java
  - backend/api/src/test/java/com/zeromail/api/ZeroMailApiApplicationModulesTest.java
  - backend/core/src/test/resources/chat-message-fixtures/v1/text-only.json
  - backend/core/src/test/resources/chat-message-fixtures/v1/single-tool-call.json
  - backend/core/src/test/resources/chat-message-fixtures/v1/multi-tool-call-confirmed-send.json
  - backend/core/src/test/resources/chat-message-fixtures/v1/send-email-with-draft-body.json
  - scripts/ci/count-gmail-send-call-sites.sh
  - .github/workflows/gates.yml
autonomous: false
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-05
  - ARCH-07
  - CHAT-07
nyquist_compliant: true
must_haves:
  truths:
    - "Pre-Wave verifications (Gmail scope, sender_safety_entry.mode, Liquibase numbering, Spring AI bug status, npm package legitimacy) all signed off before any code lands"
    - "ArchUnit framework armed at count == 0 for Gmail send call sites (flips to 1 in Wave 4)"
    - "ArchUnit ChatPersistenceContentBan rule green against empty core.chat package — rule is source-aware (Layer 2): body-shaped fields banned in email-read tool outputs (tool-getMessage / tool-searchInbox / tool-getThread / tool-listLabels) but ALLOWED in send/draft tool args (tool-sendEmail / tool-replyEmail / tool-forwardEmail / tool-saveDraft) per HIGH-3 draft-body carve-out"
    - "ArchUnit Scheduler ban green against empty core.chat package"
    - "ArchUnit Spring AI confinement rule green (only core.chat.llm.springai may import org.springframework.ai.*)"
    - "CI grep gate driven by checked-in scripts/ci/count-gmail-send-call-sites.sh — returns stable counts on zero matches (MEDIUM-2 fix; replaces inline grep | wc -l which exit 1 under pipefail when count == 0)"
    - "ApplicationModulesTest still green (no core.chat package yet — adding empty package-info is Wave 2's job; this wave only extends the test harness if needed)"
    - "v1 fixture set committed (text-only, single-tool-call, multi-tool-call-confirmed-send, send-email-with-draft-body — proves draft body carve-out shape is part of the locked v1 schema)"
  artifacts:
    - path: "backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java"
      provides: "Paired negative+positive ArchUnit count rule (count == 0 initially)"
    - path: "backend/core/src/test/java/com/zeromail/core/arch/ChatPersistenceContentBanTest.java"
      provides: "Body-content ban ArchUnit for chat_message.parts (ARCH-02 Layer 2) — source-aware: ToolOutputSanitizer.sanitize(...) strips body fields from email-read tool outputs but PASSES THROUGH body in send/draft tool args (HIGH-3 carve-out)"
    - path: "backend/core/src/test/java/com/zeromail/core/arch/ChatNoReactorSchedulerTest.java"
      provides: "Scheduler ban inside ..core.chat.. (ARCH-05)"
    - path: "scripts/ci/count-gmail-send-call-sites.sh"
      provides: "Checked-in counter script — stable under pipefail with zero matches (MEDIUM-2 fix)"
    - path: ".github/workflows/gates.yml"
      provides: "CI grep gate step (calls scripts/ci/count-gmail-send-call-sites.sh; expects count == 0 in Wave 0; flipped to count == 1 in Wave 4)"
  key_links:
    - from: ".github/workflows/gates.yml"
      to: "backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java"
      via: "Atomic group [ATOMIC-GROUP: arch01-flip] — both files change together in Wave 4"
      pattern: "ATOMIC-GROUP: arch01-flip"
---

<plan_preamble>
**Wave 0 — Test scaffolding & ArchUnit foundation.**

Per `07-RESEARCH.md` §Wave Ordering, Wave 0 lands BEFORE any production code so the invariants are armed when Wave 1+ start writing. This plan also owns the **5 critical pre-Wave verifications** flagged in `07-RESEARCH.md` §"Critical Pre-Wave-0 Verifications". Verifications run first as `checkpoint:human-verify` tasks; if any fails (e.g., Gmail scope missing), Wave 0 stops and the user decides remediation BEFORE Liquibase changelog 041 is written.

Wave 0 is split-free — Wave 0 is small enough (no production code, only tests + CI gate + fixtures) that it stays its own plan. Wave 5 is split into 05a (infra/route/feature scaffold) and 05b (preview cards + sidebar + i18n) inside `07-06-PLAN.md` for context-budget reasons.

[ATOMIC-GROUP: arch01-flip-scaffold] — the four files (`OnlyOneGmailSendCallSiteTest`, body-ban test, scheduler ban test, gates.yml) land in ONE commit here so the test framework is fully armed before Wave 1.
</plan_preamble>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production Java code → external Gmail send API | Any non-`@AllowedSendCallSite` caller is a Tampering/Repudiation event (Pitfall 1) |
| Runtime tool output → `chat_message.parts` JSONB | Any unsanitized path persists email body (Pitfall 4, ARCH-02) |
| Reactor scheduler boundary → ScopedValue propagation | Wrong scheduler loses TenantContext (Pitfall 5, ARCH-05) |
| CI pipeline → repo invariants | Missing grep gate lets ARCH-01 regression slip into main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Repudiation | ArchUnit `NoGmailSendAllowedTest` regression | mitigate | `OnlyOneGmailSendCallSiteTest` paired negative+positive lands in Wave 0 at count == 0; flips to == 1 in Wave 4 [ATOMIC-GROUP: arch01-flip] |
| T-07-02 | Information Disclosure | `chat_message.parts` JSONB carries email body | mitigate | `ChatPersistenceContentBanTest` ArchUnit rule lands at Wave 0; trigger + sanitizer follow in Wave 1 |
| T-07-03 | Information Disclosure | Reactor `.subscribeOn(boundedElastic)` drops tenant binding | mitigate | `ChatNoReactorSchedulerTest` ArchUnit ban lands at Wave 0 |
| T-07-04 | Tampering | Spring AI imports leak outside the adapter package | mitigate | `ChatLlmAdapterBoundaryTest` confines `org.springframework.ai.*` to `core.chat.llm.springai..` |
| T-07-05 | Repudiation | CI pipeline lacks grep gate; ArchUnit flip lands without coordinated CI update | mitigate | Add grep gate step to `.github/workflows/gates.yml` in this wave (count == 0 wording; flip to == 1 happens in Wave 4 in same atomic group) |
| T-07-SC | Tampering | npm package legitimacy (planner intake) | mitigate | Pre-Wave checkpoint runs `npm view ... scripts.postinstall` and Context7 verification — see Pre-Wave 1 task |
</threat_model>

<objective>
Land Wave 0 test scaffolding, ArchUnit invariants armed at count == 0, fixture set v1, and CI grep gate skeleton — BEFORE any production code is written. Also run the 5 critical pre-Wave verifications.

Purpose: Make every Phase 7 invariant fail-loud before the code that would violate it exists. Pitfall 1 (ArchUnit weakening) and Pitfall 4 (body persisted) only get caught early if the rules are live when the executor first compiles.

Output: 4 new ArchUnit test files, 1 extended Modulith test, 3 v1 fixtures, 1 updated CI workflow, 5 verified pre-Wave sign-offs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-CONTEXT.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-SPEC.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-RESEARCH.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-PATTERNS.md
@.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-VALIDATION.md
@CLAUDE.md
@CONVENTIONS.md
@TESTING.md

# Codebase analogs — read before touching files
@backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java
@backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java
@backend/core/src/test/java/com/zeromail/core/arch/LlmRepositoryContentBanTest.java
@backend/core/src/test/java/com/zeromail/core/arch/TenantIsolationArchTests.java
@backend/api/src/test/java/com/zeromail/api/ZeroMailApiApplicationModulesTest.java
@.github/workflows/gates.yml
@backend/api/src/main/java/com/zeromail/api/security/GoogleOAuthSuccessHandler.java
@backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1.1: Pre-Wave verification — Gmail gmail.send OAuth scope (A3)</name>
  <what-built>Read `backend/api/src/main/java/com/zeromail/api/security/GoogleOAuthSuccessHandler.java` and the OAuth client registration YAML (likely `backend/api/src/main/resources/application.yml` or a `google-oauth.yml`). Verify whether `https://www.googleapis.com/auth/gmail.send` is bundled into the v1.0 OAuth scope set.</what-built>
  <how-to-verify>
    1. `mcp__jetbrains__search_in_files_by_text` for `gmail.send` and `auth/gmail` across `backend/api/src/main/resources/**` and `GoogleOAuthSuccessHandler.java`.
    2. Confirm one of:
       (a) `gmail.send` IS in the scope set → ✅ proceed.
       (b) `gmail.send` is MISSING → STOP. User must decide: add the scope (forced re-consent on active tenants — acceptable per `feedback_bundled_oauth_scopes` memory: bundled flow) OR ship Phase 7.1 incremental re-grant.
    3. Record the decision in `07-CONTEXT.md` as an addendum (or new `07-PREFLIGHT.md`) so Wave 4 `AssistantSendExecutor` doesn't surprise users with 403s.
  </how-to-verify>
  <threat_ref>A3 (Assumptions Log)</threat_ref>
  <resume-signal>Type "scope-confirmed" (with the scope list) or "scope-missing — adding to bundle" / "scope-missing — shipping 7.1 re-grant"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1.2: Pre-Wave verification — sender_safety_entry.mode column (A4)</name>
  <what-built>Verify TRG-08 shipped the `sender_safety_entry.mode VARCHAR(16)` column. If missing, an extra Liquibase changelog must precede 041.</what-built>
  <how-to-verify>
    1. `mcp__postgres__get_object_details` for table `sender_safety_entry`; check column list.
    2. Or `mcp__jetbrains__search_in_files_by_text` for `mode VARCHAR` and `sender_safety_entry` in `backend/core/src/main/resources/db/changelog/changes/*.yaml`.
    3. If present → ✅. If missing → STOP and prepend a Liquibase changelog (e.g., `040.5-sender-safety-entry-mode.yaml`) BEFORE 041. Use the established `025-triage-audit.yaml` rationale-comment block style.
  </how-to-verify>
  <threat_ref>A4</threat_ref>
  <resume-signal>Type "mode-column-present" or "added-040.5-changelog" with path</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1.3: Pre-Wave verification — Liquibase 041–046 numbering uncontested (A9)</name>
  <what-built>Confirm `db.changelog-master.yaml` head is still `040-triage-audit-message-ref.yaml` and no parallel agent has claimed 041–046 since research was authored. Honor the `project_parallel_agent_main_worktree` memory.</what-built>
  <how-to-verify>
    1. `git pull --rebase origin main` (or check current head explicitly — note user policy: never `--no-verify` / never destructive ops without prompt).
    2. Inspect last lines of `backend/core/src/main/resources/db/changelog/db.changelog-master.yaml`.
    3. `Grep -rE "041-|042-|043-|044-|045-|046-"` under `backend/core/src/main/resources/db/changelog/changes/`.
    4. If all six numbers free → ✅. If a parallel agent landed something in that range → renumber Phase 7's changelogs upward in `07-CONTEXT.md` addendum and update SPEC's six numbers downstream.
  </how-to-verify>
  <threat_ref>A9</threat_ref>
  <resume-signal>Type "041-046-free" or "renumbered to NNN-NNN"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1.4: Pre-Wave verification — Spring AI #3366 / #5167 status on 2.0.0-M6 (A2)</name>
  <what-built>Re-check Spring AI bug status via Context7 immediately before Wave 0 code lands. If either bug is fixed in M6.x or M7 has dropped, the `ChatToolCallRegistry` + `ZeroMailChatMemory` workaround stays correct (no harm) but record the status for downstream cleanup.</what-built>
  <how-to-verify>
    1. Context7: `mcp__context7__resolve-library-id` for "spring-ai" → use `/websites/spring_io_spring-ai_reference_2_0-snapshot`.
    2. `mcp__context7__query-docs` with `topic: "MessageAggregator toolCalls streaming"` and `topic: "internalToolExecutionEnabled streaming"`.
    3. Cross-check upstream issues `spring-projects/spring-ai#3366` and `#5167`. If still open → ✅ workaround proceeds. If fixed → note in `07-CONTEXT.md` addendum + add a TODO in `libs.versions.toml` near the Spring AI pin to revisit the workaround when bumping past M6.
  </how-to-verify>
  <threat_ref>A2</threat_ref>
  <resume-signal>Type "#3366-open #5167-open" (status pair) or include fix info</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1.5: Pre-Wave verification — npm package legitimacy ([ASSUMED] tags)</name>
  <what-built>Re-run the Package Legitimacy Audit for the 4 frontend packages (`ai`, `@ai-sdk/react`, `streamdown`, `ai-elements` CLI). All currently tagged [ASSUMED] per `07-RESEARCH.md`. This is a SoftBlocking checkpoint — never auto-approve (per planner constraint).</what-built>
  <how-to-verify>
    1. Run `npm view ai version`, `npm view ai scripts.postinstall` (expected: `undefined`).
    2. Run same pair for `@ai-sdk/react`, `streamdown`, `ai-elements`.
    3. Open each package's npmjs.com page; confirm publisher org is `vercel` and source repo links to `github.com/vercel/*`.
    4. Cross-check Inbox Zero's `package.json` (local clone at `../inbox-zero`) is using the same packages in production.
    5. Record actual versions resolved (may differ from spec floor `^6.0.184` / `^3.0.186` / `^2.5.0`).
  </how-to-verify>
  <threat_ref>A1, T-07-SC</threat_ref>
  <resume-signal>Type "packages-verified" with `{ai: X, ai-sdk-react: Y, streamdown: Z, ai-elements: W}` versions, or "blocked: <which package> failed because <reason>"</resume-signal>
</task>

<task type="auto">
  <name>Task 1.6: Create OnlyOneGmailSendCallSiteTest (count == 0 at this wave)</name>
  <files>backend/core/src/test/java/com/zeromail/core/arch/OnlyOneGmailSendCallSiteTest.java</files>
  <action>
    Create paired negative + positive ArchUnit test per `07-RESEARCH.md` §"ArchUnit 0→1 carve-out (Wave 0 — lands BEFORE executor)" code example. Use enterprise-readability names (no `e`/`ex`/`req` abbreviations) — `importedClasses`, `javaClass`, `methodCall`, `callSiteCount`, etc.

    Set the count assertion to **`isEqualTo(0L)`** in this wave (Wave 4 [ATOMIC-GROUP: arch01-flip] flips it to `isEqualTo(1L)` together with `AssistantSendExecutor`, updated `NoGmailSendAllowedTest`, and `.github/workflows/gates.yml`). Inline comment: "Count flips 0→1 in Wave 4 atomic group [ATOMIC-GROUP: arch01-flip]; do not edit this number in isolation."

    Pattern source: copy `NoGmailSendAllowedTest.java` constants (`GMAIL_MESSAGES_OWNER`, `GMAIL_DRAFTS_OWNER`) and `TriageGmailWriteBoundaryTest.java` early-return idiom for the `..chat.confirm.send..` carve-out (which is empty in Wave 0; that is correct — the rule is armed for Wave 4).

    Annotation `@AllowedSendCallSite` does not yet exist (Wave 4 creates it in `core.chat.confirm.send.AllowedSendCallSite`). The ArchUnit rule references the FQN as a string `com.zeromail.core.chat.confirm.send.AllowedSendCallSite`; this is acceptable in ArchUnit and prevents a compile dep cycle.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.arch.OnlyOneGmailSendCallSiteTest"</automated>
  </verify>
  <done>Test compiles and passes against current main (count == 0 because no Gmail send call site exists yet).</done>
  <deviation_handling>If Spring AI or another dep accidentally introduces a `messages().send()` reference in production code outside `..chat.confirm.send..`, the test fails — investigate immediately, do not weaken the rule.</deviation_handling>
  <threat_ref>T-07-01</threat_ref>
  <post-task>Run `mcp__jetbrains__get_file_problems` on the new file before marking done.</post-task>
</task>

<task type="auto">
  <name>Task 1.7: Create ChatPersistenceContentBanTest + ChatNoReactorSchedulerTest + ChatLlmAdapterBoundaryTest</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/arch/ChatPersistenceContentBanTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/ChatNoReactorSchedulerTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/ChatLlmAdapterBoundaryTest.java
  </files>
  <action>
    Three sibling ArchUnit tests, all armed against an empty `core.chat` package (Wave 2+ populate it; rules pass vacuously until then).

    **`ChatPersistenceContentBanTest`** (ARCH-02 Layer 2) — **source-aware per HIGH-3 draft-body carve-out**. Mirror `LlmRepositoryContentBanTest.java` regex shape but route through `ToolOutputSanitizer.sanitize(...)`'s tool-name dispatch:
    - Forbid any method inside `..core.chat.persistence..` from writing a field whose name matches `(?i)(emailBody|messageBody|bodyHtml|bodyText|htmlBody|textBody|body)` into a JSONB column UNLESS the enclosing part envelope's `type` is one of `tool-sendEmail` / `tool-replyEmail` / `tool-forwardEmail` / `tool-saveDraft` (user-authored draft args — the user reviews and edits this body in the preview card, then explicitly clicks Send).
    - Forbid body fields unconditionally in parts whose `type` is one of `tool-getMessage` / `tool-searchInbox` / `tool-getThread` / `tool-listLabels` (email content extracted from Gmail — the privacy-locked path).
    - Also forbid `ToolOutputSanitizer` bypass: any persistence method calling `chatMessageJdbcRepository.save*` that did NOT pass through a `ToolOutputSanitizer.sanitize(...)` call upstream must fail (`onlyDependOnClassesThat()` chain).
    - Phrase the message clearly: "chat_message.parts must route through ToolOutputSanitizer (source-aware: strips body from email-read tool outputs, preserves body in send/draft tool args per HIGH-3); see ARCH-02 + CLAUDE.md Privacy Draft-body carve-out".

    The Layer 1 enforcement (runtime sanitizer dispatching by tool name) lands in Plan 02 Task 2.2; this ArchUnit rule (Layer 2) is the static double-check; the Postgres trigger (Layer 3, Plan 02 Task 2.1 changelog 042) is the runtime DB-side backstop and uses the same source-aware predicate.

    **`ChatNoReactorSchedulerTest`** (ARCH-05): copy `TenantIsolationArchTests.java` lines 27-38 shape. Ban `reactor.core.scheduler.Schedulers.boundedElastic`, `Schedulers.parallel`, `Schedulers.single`, and any `.subscribeOn(Scheduler)` whose argument isn't `TenantAwareReactorScheduler` inside `..core.chat..`. Allow these calls under `..core.chat.llm.springai..` ONLY if wrapping is delegated to `TenantAwareReactorScheduler` (express as: the call site class name must equal `SpringAiStreamingChatModelClient` AND the called method receives a `TenantAwareReactorScheduler` instance — express via ArchUnit predicate on `MethodCallTarget`).

    **`ChatLlmAdapterBoundaryTest`** (Spring AI confinement): forbid `org.springframework.ai..` imports from any class under `..core.chat..` except those under `..core.chat.llm.springai..`. Mirror existing `LlmGatewayBoundaryTest` pattern; if that test already enforces the v1.0 boundary, EXTEND its allow-list to add `..core.chat.llm.springai..` instead of duplicating — verify by reading the existing test first.

    All three tests use `@AnalyzeClasses(packages = "com.zeromail", importOptions = ImportOption.DoNotIncludeTests.class)`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.arch.ChatPersistenceContentBanTest" --tests "com.zeromail.core.arch.ChatNoReactorSchedulerTest" --tests "com.zeromail.core.arch.ChatLlmAdapterBoundaryTest"</automated>
  </verify>
  <done>All three tests compile and pass vacuously (no classes in `..core.chat..` yet).</done>
  <threat_ref>T-07-02, T-07-03, T-07-04</threat_ref>
  <post-task>`mcp__jetbrains__get_file_problems` on each new test file.</post-task>
</task>

<task type="auto">
  <name>Task 1.8: Commit v1 fixture set + extend ApplicationModulesTest harness</name>
  <files>
    backend/core/src/test/resources/chat-message-fixtures/v1/text-only.json,
    backend/core/src/test/resources/chat-message-fixtures/v1/single-tool-call.json,
    backend/core/src/test/resources/chat-message-fixtures/v1/multi-tool-call-confirmed-send.json,
    backend/core/src/test/resources/chat-message-fixtures/v1/send-email-with-draft-body.json,
    backend/api/src/test/java/com/zeromail/api/ZeroMailApiApplicationModulesTest.java
  </files>
  <action>
    Create four JSON fixtures matching the `chat_message.parts` envelope shape from `07-AI-SPEC.md` §"State Management" and `07-RESEARCH.md` §"Pattern 1". Every fixture envelope MUST include `"schemaVersion": 1` at the envelope root (D-08). Email-read tool output fixtures contain NO body content (privacy invariant ARCH-02). The send/draft fixture intentionally DOES carry the user-authored draft body — exercising HIGH-3's carve-out so downstream tests can validate the source-aware sanitizer + Postgres trigger correctly allow it.

    - `text-only.json`: one `TextPart` only.
    - `single-tool-call.json`: one `TextPart` + one `ToolCallPart` for `listLabels` (read-only, output is short list of label IDs only — strings, no body fields).
    - `multi-tool-call-confirmed-send.json`: text + `searchInbox` tool-call/output (output is metadata only — messageId, subject hash, snippet capped at 80 chars; **NO `body`/`htmlBody`/`textBody`** — the email-read path is banned) + `sendEmail` tool-call envelope (`type: "tool-sendEmail"`) carrying user-authored `body` field per HIGH-3 carve-out + confirmation state `confirmed` + `assistant_action_audit` reference.
    - `send-email-with-draft-body.json` (NEW per HIGH-3): single `ToolCallPart` of `type: "tool-sendEmail"` whose `input` carries `{to, subject, body: "Xin chào anh, em xin gửi báo cáo..."}`. This fixture is the proof shape — `ChatMessageBodyBanTriggerIT` (Plan 02) uses it as the **positive** (must-insert) case, while a sibling fixture-equivalent with `type: "tool-getMessage"` carrying the same body shape is the **negative** (must-reject) case. The source-aware trigger distinguishes them.

    Then extend `ZeroMailApiApplicationModulesTest.java` only if the existing test needs an explicit allow-list for `core.chat` (read first — likely the test calls `ApplicationModules.of(...).verify()` which auto-discovers `package-info.java`; in that case NO change is needed in Wave 0 because `core.chat` package doesn't exist yet, and Wave 2 will create `package-info.java`). If a code change IS needed, it's a one-liner; otherwise leave the file untouched and remove it from `files_modified` in the executor's SUMMARY.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.arch.*" :backend:api:test --tests "com.zeromail.api.ZeroMailApiApplicationModulesTest"</automated>
  </verify>
  <done>Fixtures parse as valid JSON (validate via `jq . <file>` locally — not part of automated gate but a one-liner sanity check). ApplicationModulesTest still green.</done>
  <threat_ref>T-07-02 (fixtures encode the GOOD shape and never the banned shape)</threat_ref>
</task>

<task type="auto">
  <name>Task 1.9: Add CI grep gate via checked-in script (count == 0 at this wave) [ATOMIC-GROUP: arch01-flip-scaffold]</name>
  <files>scripts/ci/count-gmail-send-call-sites.sh, .github/workflows/gates.yml</files>
  <action>
    **MEDIUM-2 fix — script-driven, not inline pipeline.** The earlier inline `grep | wc -l` chain exits 1 under GitHub Actions `pipefail` when zero matches are present (the desired Wave 0 baseline). Replace with a checked-in shell script that returns stable counts regardless of match presence.

    **`scripts/ci/count-gmail-send-call-sites.sh`** (NEW, executable per `chmod +x`):
    ```bash
    #!/usr/bin/env bash
    # ARCH-01 — Count Gmail send call sites partitioned by executor vs non-executor.
    # Output: two lines: "non_executor=<N>" and "executor=<N>". Always exits 0.
    # The calling CI step parses + asserts the expected counts.
    set -u   # NOT -e and NOT -o pipefail — we want zero-match to return 0, not 1.

    # Match production code only (exclude test sources).
    MATCHES=$(grep -rE 'messages\(\)\.send\(' backend/ --include='*.java' \
                  --exclude-dir='*Test*' --exclude-dir='test' 2>/dev/null || true)

    if [ -z "$MATCHES" ]; then
      NON_EXECUTOR=0
      EXECUTOR=0
    else
      NON_EXECUTOR=$(printf '%s\n' "$MATCHES" | grep -vc 'AssistantSendExecutor' || true)
      EXECUTOR=$(printf '%s\n' "$MATCHES" | grep -c 'AssistantSendExecutor' || true)
    fi

    # grep -c returns 1 on zero matches; the `|| true` floors to 0. Coerce to integer for safety.
    NON_EXECUTOR=${NON_EXECUTOR:-0}
    EXECUTOR=${EXECUTOR:-0}

    echo "non_executor=${NON_EXECUTOR}"
    echo "executor=${EXECUTOR}"
    exit 0
    ```

    Add a unit-style test fixture under `scripts/ci/tests/` only if the team has a shell-test harness; otherwise the CI step itself is the test (called in two states: zero call sites + a synthetic file with one call site).

    **`.github/workflows/gates.yml`** — add a new step to the `backend` (or equivalent) job per `07-PATTERNS.md` §2.23, AFTER the existing `Run backend checks` step. At this wave the assertion is `non_executor == 0 && executor == 0`. Wave 4 [ATOMIC-GROUP: arch01-flip] flips `executor` expectation 0 → 1 in the same commit as `AssistantSendExecutor` lands.

    Step body:
    ```yaml
    - name: Enforce zero non-AssistantSendExecutor Gmail send call sites (ARCH-01)
      run: |
        chmod +x scripts/ci/count-gmail-send-call-sites.sh
        COUNTS=$(scripts/ci/count-gmail-send-call-sites.sh)
        echo "$COUNTS"
        NON_EXECUTOR=$(echo "$COUNTS" | awk -F= '/^non_executor=/{print $2}')
        EXECUTOR=$(echo "$COUNTS" | awk -F= '/^executor=/{print $2}')
        if [ "$NON_EXECUTOR" != "0" ]; then
          echo "::error::Non-AssistantSendExecutor Gmail send call site detected — ARCH-01 regression"
          exit 1
        fi
        # Wave 0: EXECUTOR == 0 expected (AssistantSendExecutor lands in Wave 4).
        # Wave 4 [ATOMIC-GROUP: arch01-flip]: flip the comparison below to '"$EXECUTOR" != "1"'.
        if [ "$EXECUTOR" != "0" ]; then
          echo "::error::AssistantSendExecutor unexpectedly present before Wave 4"
          exit 1
        fi
    ```

    Also add a follow-up CI step (or inline check) that fails when `NoGmailSendAllowedTest.java` is modified in the same diff WITHOUT `OnlyOneGmailSendCallSiteTest.java` being modified — uses `git diff --name-only origin/main...HEAD` and `grep`. Pitfall 1 mitigation per `07-RESEARCH.md` §"Pitfall 1".
  </action>
  <verify>
    <automated>chmod +x scripts/ci/count-gmail-send-call-sites.sh && scripts/ci/count-gmail-send-call-sites.sh | tee /tmp/counts.txt && grep -q '^non_executor=0$' /tmp/counts.txt && grep -q '^executor=0$' /tmp/counts.txt; act -j backend --workflows .github/workflows/gates.yml 2>/dev/null || echo "act unavailable; run 'gh workflow run gates.yml --ref <branch>' to verify after push"</automated>
  </verify>
  <done>Script returns `non_executor=0` + `executor=0` against current main and exits 0 even when grep finds nothing (MEDIUM-2 fix). Step appears in `gates.yml`; YAML lints clean.</done>
  <deviation_handling>If `act` (local GitHub Actions runner) is not installed in the dev environment, fall back to manual workflow dispatch verification on a feature branch. If the script's `set -u` causes a parser error on the CI runner's older bash, drop the `-u` flag and rely on the explicit `${VAR:-0}` defaults.</deviation_handling>
  <threat_ref>T-07-05</threat_ref>
</task>

</tasks>

<threats>
T-07-01, T-07-02, T-07-03, T-07-04, T-07-05, T-07-SC — see threat_model block.
</threats>

<verification>
- All four ArchUnit tests pass (1.6 + 1.7 + the `LlmGatewayBoundaryTest` extension if applied). `ChatPersistenceContentBanTest` is source-aware per HIGH-3 carve-out.
- 4 v1 fixtures parse as valid JSON; `schemaVersion: 1` present on every envelope. `send-email-with-draft-body.json` proves carve-out shape.
- `scripts/ci/count-gmail-send-call-sites.sh` returns stable `non_executor=0` + `executor=0` on current main and exits 0 (MEDIUM-2 fix).
- CI grep gate step calls the script, lints, and runs against current main (counts 0 + 0).
- All 5 pre-Wave checkpoints signed off with explicit values.
- `mcp__jetbrains__get_file_problems` green on every new Java file.
- Wave 0 commit message tags include `[ATOMIC-GROUP: arch01-flip-scaffold]` so reviewers can correlate with the Wave 4 flip commit.
</verification>

<success_criteria>
- [ ] `OnlyOneGmailSendCallSiteTest` exists and passes at count == 0
- [ ] `ChatPersistenceContentBanTest` is source-aware (allows body in send/draft tool args, bans body in email-read tool outputs), `ChatNoReactorSchedulerTest`, `ChatLlmAdapterBoundaryTest` exist and pass vacuously
- [ ] `scripts/ci/count-gmail-send-call-sites.sh` checked in, executable, returns `non_executor=0` + `executor=0` against current main (MEDIUM-2 fix)
- [ ] CI grep gate step in `gates.yml` calls the script and passes against current main
- [ ] 4 v1 fixtures committed (text-only, single-tool-call, multi-tool-call-confirmed-send, send-email-with-draft-body); `schemaVersion: 1` present on every envelope
- [ ] Gmail `gmail.send` scope verified present (or remediation path locked)
- [ ] `sender_safety_entry.mode` column verified present (or 040.5 added)
- [ ] Liquibase 041–046 numbering uncontested (or renumbered)
- [ ] Spring AI #3366 / #5167 bug status recorded
- [ ] All 4 npm packages legitimacy-verified with version pins recorded
- [ ] `mcp__jetbrains__get_file_problems` clean on every new Java file
- [ ] `ApplicationModulesTest` still green
</success_criteria>

<output>
Create `.planning/phases/07-chat-email-assistant-backend-frontend-send-executor-archunit/07-01-SUMMARY.md` when done. Record actual pre-Wave verification answers verbatim.
</output>
