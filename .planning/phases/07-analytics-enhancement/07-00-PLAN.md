---
phase: 07-analytics-enhancement
plan: "00"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/core/src/test/java/com/zeromail/core/analytics/projection/TrendPointQueryTest.java
  - backend/core/src/test/java/com/zeromail/core/analytics/projection/CreditSpendQueryTest.java
  - backend/core/src/test/java/com/zeromail/core/analytics/projection/AnalyticsDeltaComputationTest.java
  - backend/core/src/test/java/com/zeromail/core/analytics/AnalyticsPrivacySweepTest.java
  - apps/web/features/analytics/components/__tests__/TrendPanel.test.tsx
  - apps/web/features/analytics/components/__tests__/ActionBreakdownPanel.test.tsx
  - apps/web/features/analytics/utils/analyticsUtils.test.ts
autonomous: true
requirements:
  - ANL-04
  - ANL-05
  - ANL-06
  - ANL-09

must_haves:
  truths:
    - "TrendPointQueryTest has failing test asserting generate_series zero-fills all days in window"
    - "CreditSpendQueryTest has failing test asserting JOIN aggregation per call_site"
    - "AnalyticsDeltaComputationTest has failing test asserting null when prior window is zero"
    - "AnalyticsPrivacySweepTest has placeholder test methods for the 3 new query methods"
    - "TrendPanel.test.tsx has failing test for empty-state render"
    - "ActionBreakdownPanel.test.tsx has failing test for total=0 empty-state render"
  artifacts:
    - path: "backend/core/src/test/java/com/zeromail/core/analytics/projection/TrendPointQueryTest.java"
      provides: "Failing test scaffold for ANL-04 trend query"
    - path: "backend/core/src/test/java/com/zeromail/core/analytics/projection/CreditSpendQueryTest.java"
      provides: "Failing test scaffold for ANL-09 credit spend query"
    - path: "backend/core/src/test/java/com/zeromail/core/analytics/projection/AnalyticsDeltaComputationTest.java"
      provides: "Failing test scaffold for ANL-05 delta computation"
    - path: "apps/web/features/analytics/components/__tests__/TrendPanel.test.tsx"
      provides: "Failing Vitest test for TrendPanel"
    - path: "apps/web/features/analytics/components/__tests__/ActionBreakdownPanel.test.tsx"
      provides: "Failing Vitest test for ActionBreakdownPanel"
  key_links:
    - from: "TrendPointQueryTest"
      to: "AnalyticsSummaryQueryService.queryTrendPoints"
      via: "Method call that does not exist yet — test must fail RED"
      pattern: "queryTrendPoints"
    - from: "CreditSpendQueryTest"
      to: "AnalyticsSummaryQueryService.queryCreditSpend"
      via: "Method call that does not exist yet — test must fail RED"
      pattern: "queryCreditSpend"
---

<objective>
Create failing test scaffolds (RED phase) for all new backend query logic and frontend panel components
introduced in Phase 7.

Purpose: Ensure nothing ships without a test gate. Tests are written first, will fail until the
implementation plans (07-01, 07-02, 07-03) complete, then turn GREEN.

Output: 7 test files with clearly failing assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-analytics-enhancement/07-CONTEXT.md
@.planning/phases/07-analytics-enhancement/07-RESEARCH.md
@.planning/phases/07-analytics-enhancement/07-PATTERNS.md
@TESTING.md
@CONVENTIONS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Backend test scaffolds — trend query, credit spend, delta computation</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/analytics/projection/TrendPointQueryTest.java
    backend/core/src/test/java/com/zeromail/core/analytics/projection/CreditSpendQueryTest.java
    backend/core/src/test/java/com/zeromail/core/analytics/projection/AnalyticsDeltaComputationTest.java
    backend/core/src/test/java/com/zeromail/core/analytics/AnalyticsPrivacySweepTest.java
  </files>
  <behavior>
    TrendPointQueryTest:
    - Test: queryTrendPoints for a 7-day window returns exactly 7 TrendPointProjection entries (zero-fill for days with no data).
    - Test: every returned TrendPointProjection has observed >= 0 and applied >= 0 (never negative).
    - Test: the list is ordered by date ascending.
    These tests call a method that does not exist yet on AnalyticsSummaryQueryService — they MUST fail (RED).

    CreditSpendQueryTest:
    - Test: queryCreditSpend groups correctly — TRIAGE credits and DRAFT credits are returned as separate entries.
    - Test: queryCreditSpend returns empty projection (all zeros) when no SETTLE ledger entries exist in window.
    These tests call a method that does not exist yet — they MUST fail (RED).

    AnalyticsDeltaComputationTest:
    - Test: deltaPct(100, 50) returns 100L (100% increase).
    - Test: deltaPct(50, 100) returns -50L (50% decrease).
    - Test: deltaPct(anything, 0) returns null (prior window zero data → null, not NaN).
    - Test: deltaPct(0, 0) returns null (both zero → null).
    These tests call a private-visible static method deltaPct that does not exist yet — use package-private
    helper class or @VisibleForTesting approach. Tests MUST fail (RED).

    AnalyticsPrivacySweepTest extensions:
    - Add @Test placeholder method verifyTrendQueryDoesNotLogSenderEmail — asserts that the
      log output captured during a queryTrendPoints call does NOT contain any "@" character
      (email address indicator). Use @ExtendWith(OutputCaptureExtension.class).
    - Add @Test placeholder method verifyDomainQueryDoesNotLogDomainString — asserts domain
      query log output does not contain the literal domain value.
    - Add @Test placeholder method verifyCreditQueryDoesNotLogCallSiteValue — asserts credit
      spend log output contains event= prefix and tenantId= but not individual callSite values.
    These extend the existing AnalyticsPrivacySweepTest class — append to existing file.
  </behavior>
  <action>
    Create TrendPointQueryTest.java in package com.zeromail.core.analytics.projection.

    The test class is a pure unit test (no Spring context needed for logic tests; use @SpringBootTest
    with TestContainers Postgres slice only if the query methods need a live DB to fail properly).
    For RED gate purposes, use @SpringBootTest and inject AnalyticsSummaryQueryService — the missing
    method causes compilation failure which counts as RED.

    Use the package-private test pattern: same package as the class under test so you can access
    package-visible helpers. Use JUnit 5 (@Test, @DisplayName). Follow the existing test class style
    in backend/core/src/test/java/com/zeromail/core/analytics/.

    For AnalyticsDeltaComputationTest: Since deltaPct will be a private static method on
    AnalyticsSummaryQueryService, extract it to a package-private static utility class named
    AnalyticsDeltaComputation in the same projection package so it is directly testable. Plan 07-01
    will create this class; the test file here declares the call and fails at compile time.

    Privacy sweep test additions go inside the existing AnalyticsPrivacySweepTest.java file.
    Read the existing file first to determine the correct append location. Use
    @Disabled("RED: implementation pending 07-01") on each new placeholder test so the suite
    compiles but the placeholder tests are clearly marked as pending.

    All backend test files: follow the naming convention event=<name> tenantId={} in any log
    assertions (per D-19 privacy logging format).
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*TrendPoint*" --tests "*CreditSpend*" --tests "*DeltaComputation*" 2>&1 | tail -20</automated>
  </verify>
  <done>
    All three new backend test files compile. TrendPointQueryTest and CreditSpendQueryTest fail at
    runtime with NoSuchMethodError or ClassNotFoundException (method not yet implemented). 
    AnalyticsDeltaComputationTest fails with NoClassDefFoundError (class not yet created).
    AnalyticsPrivacySweepTest compiles with new @Disabled methods appended — existing tests still pass.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Frontend test scaffolds — TrendPanel, ActionBreakdownPanel, analyticsUtils</name>
  <files>
    apps/web/features/analytics/components/__tests__/TrendPanel.test.tsx
    apps/web/features/analytics/components/__tests__/ActionBreakdownPanel.test.tsx
    apps/web/features/analytics/utils/analyticsUtils.test.ts
  </files>
  <behavior>
    TrendPanel.test.tsx:
    - Test: renders data-testid="analytics-trend-panel" when imported.
    - Test: renders the empty-state text key analytics.trend.empty when trendPoints prop is empty array.
    - Test: renders ChartContainer when trendPoints has at least one entry.
    These tests import TrendPanel from a path that does not exist yet — they MUST fail (RED).

    ActionBreakdownPanel.test.tsx:
    - Test: renders data-testid="analytics-action-breakdown-panel".
    - Test: renders empty-state text when actionBreakdown is undefined.
    - Test: renders ChartContainer when actionBreakdown has non-zero total.
    These tests import ActionBreakdownPanel from a path that does not exist yet — they MUST fail (RED).

    analyticsUtils.test.ts:
    - Test: safeCount(undefined) returns 0.
    - Test: safeCount(NaN) returns 0.
    - Test: safeCount(-5) returns 0.
    - Test: safeCount(3.9) returns 3 (trunc, not round).
    - Test: formatDelta(null) returns "—".
    - Test: formatDelta(10) returns "+10%".
    - Test: formatDelta(-5) returns "-5%".
    These test safeCount and formatDelta exports from analyticsUtils.ts which does not exist yet —
    they MUST fail (RED).
  </behavior>
  <action>
    Create __tests__ directory under apps/web/features/analytics/components/ if it does not exist.

    TrendPanel.test.tsx and ActionBreakdownPanel.test.tsx: use Vitest + React Testing Library
    (@testing-library/react). Import the component from the relative path. Wrap with any required
    providers (next-intl IntlProvider with a minimal messages object containing all analytics.trend.*
    and analytics.actionBreakdown.* keys). Since chart.tsx is not installed yet, mock
    @/components/ui/chart by adding a vi.mock call at the top of each test file:
      vi.mock('@/components/ui/chart', () => ({
        ChartContainer: ({ children }: { children: React.ReactNode }) => children,
        ChartTooltip: () => null,
        ChartTooltipContent: () => null,
        ChartLegend: () => null,
        ChartLegendContent: () => null,
      }))
    This mock lets tests run without the real chart primitive.

    analyticsUtils.test.ts: pure Vitest test, no React needed. Import safeCount and formatDelta
    from apps/web/features/analytics/utils/analyticsUtils.ts (does not exist yet → test fails RED).

    Follow the project pattern: describe block per component, it() blocks per behavior. File lives
    beside the feature code (apps/web/features/analytics/components/__tests__/).
  </action>
  <verify>
    <automated>pnpm --filter web test --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|Cannot find module)" | head -20</automated>
  </verify>
  <done>
    Three frontend test files created. Running pnpm --filter web test shows the new tests FAIL with
    "Cannot find module" or import errors (RED). Existing passing tests are unaffected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test ↔ Production code | Tests may only call public/package-visible API surfaces — no reflection to bypass access modifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Information Disclosure | Privacy sweep test log capture | mitigate | Use OutputCaptureExtension; assert no "@" in trend query logs |
| T-07-00-SC | Tampering | npm/pnpm test dependencies | accept | No new npm packages added in this plan — only test files |
</threat_model>

<verification>
Backend: ./gradlew :backend:core:test --tests "*Analytics*" 2>&1 | tail -30
Frontend: pnpm --filter web test 2>&1 | tail -20

Expected state after this plan: new test files compiled (backend) or fail with module-not-found (frontend).
The GREEN phase arrives when implementation plans 07-01 through 07-03 ship.
</verification>

<success_criteria>
- All 4 backend test files exist under backend/core/src/test/
- All 3 frontend test files exist under apps/web/features/analytics/
- Backend suite compiles without error (new tests annotated @Disabled where needed)
- Existing passing tests remain green
- New tests fail RED (method/module not found) — confirming test-first discipline
</success_criteria>

<output>
Create .planning/phases/07-analytics-enhancement/07-00-SUMMARY.md when done.
</output>
