---
phase: 07-analytics-enhancement
plan: "05"
type: execute
wave: 6
depends_on:
  - "07-04"
files_modified:
  - backend/core/src/test/java/com/zeromail/core/analytics/AnalyticsPrivacySweepTest.java
  - apps/web/e2e/analytics.spec.ts
autonomous: false
requirements:
  - ANL-04
  - ANL-05
  - ANL-06
  - ANL-07
  - ANL-08
  - ANL-09

must_haves:
  truths:
    - "AnalyticsPrivacySweepTest covers all 3 new query methods with log capture assertions"
    - "pnpm --filter web tsc --noEmit exits 0"
    - "pnpm --filter web lint exits 0"
    - "pnpm --filter web test exits 0 (Vitest including analyticsUtils.test.ts + panel tests)"
    - "./gradlew :backend:core:test :backend:api:test exits 0"
    - "pnpm --filter web run i18n:check exits 0"
    - "Playwright e2e: TrendPanel visible at /analytics?window=7d"
    - "Playwright e2e: changing window chip rerenders TrendPanel"
    - "Playwright e2e: CreditsPanel renders without JavaScript errors"
  artifacts:
    - path: "backend/core/src/test/java/com/zeromail/core/analytics/AnalyticsPrivacySweepTest.java"
      provides: "Extended privacy sweep covering new query methods"
      contains: "verifyTrendQueryDoesNotLogSenderEmail|verifyDomainQueryDoesNotLogDomainString|verifyCreditQueryDoesNotLogCallSiteValue"
    - path: "apps/web/e2e/analytics.spec.ts"
      provides: "Playwright e2e tests for Phase 7 additions"
  key_links:
    - from: "AnalyticsPrivacySweepTest"
      to: "AnalyticsSummaryQueryService"
      via: "OutputCaptureExtension log capture during summarize() call with test data"
      pattern: "outputCapture.*tenantId"
    - from: "Playwright e2e"
      to: "/analytics?window=7d"
      via: "browser_navigate + browser_snapshot"
      pattern: "analytics-trend-panel"
---

<objective>
Run all quality gates across backend and frontend, activate the @Disabled privacy sweep tests from
07-00 with real assertions, and perform a Playwright end-to-end golden path verification at /analytics.

Purpose: Confirm Phase 7 is shippable — all tests green, no TypeScript errors, no lint issues,
no i18n gaps, privacy invariant provably holds for new query methods.

Output: Full test suite passing, Playwright screenshot showing new panels rendered correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-analytics-enhancement/07-CONTEXT.md
@.planning/phases/07-analytics-enhancement/07-PATTERNS.md
@TESTING.md
@CONVENTIONS.md

<interfaces>
From TESTING.md:
  Privacy sweep tests use @ExtendWith(OutputCaptureExtension.class)
  Capture log during service call, assert output does NOT contain prohibited strings
  Pattern: Analytics*PrivacySweepTest in backend/core/src/test/

From AnalyticsPrivacySweepTest.java (extended in 07-00 with @Disabled placeholders):
  Existing tests assert existing query methods do not log email/body content
  New @Disabled tests: verifyTrendQueryDoesNotLogSenderEmail,
    verifyDomainQueryDoesNotLogDomainString, verifyCreditQueryDoesNotLogCallSiteValue
  These must be activated (remove @Disabled) and given real assertions

From CLAUDE.md privacy logging format:
  event=<name> tenantId={} — allowed in logs
  sender_email, gmail_message_id values, message subjects, domain strings — NOT allowed in logs
  call_site values ("TRIAGE", "DRAFT", "PREVIEW" strings) — acceptable to log as event metadata
  but individual credit amounts / reservation IDs should not appear

Playwright: the dev stack at http://localhost:3000 (Next.js) + http://localhost:8080 (Spring Boot)
  must be running before the e2e checkpoint. Use Playwright MCP tools to drive the browser.
  The e2e spec file goes in apps/web/e2e/analytics.spec.ts (existing e2e directory).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate privacy sweep tests + run full backend test suite</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/analytics/AnalyticsPrivacySweepTest.java
  </files>
  <action>
    Read AnalyticsPrivacySweepTest.java fully. Find the 3 @Disabled placeholder methods added in 07-00.

    For each placeholder method:
    1. Remove the @Disabled annotation.
    2. Write a real assertion body using OutputCaptureExtension.

    METHOD 1: verifyTrendQueryDoesNotLogSenderEmail
    Set up: inject AnalyticsSummaryQueryService. Create a test tenantId (UUID.randomUUID()). Create a
    TimeWindow for the last 7 days. Call summarize(testTenantId, window) — this will run against an
    in-memory or Testcontainers DB (depending on test slice used in existing test class). Capture
    the OutputCapture output string. Assert that output does NOT contain any "@" character in lines
    that contain "trend" (using a stream filter on output lines). Also assert every log line from
    the summarize call that is not the "event=analytics_summary_computed" line does not contain the
    test tenantId's associated email string. Simpler approach: assert outputCapture.toString()
    does not contain "sender_email" literal string (would only appear if accidentally logged).

    METHOD 2: verifyDomainQueryDoesNotLogDomainString
    Similar setup. After summarize(), assert outputCapture.toString() does not contain any line
    matching the pattern "domain=.*\\.com" (domain string accidentally in log). The tenant has no
    mail data in test context so domain query returns empty — assert the empty-result log line
    (if any) follows event=analytics_domain_senders_queried tenantId={} format. Use assertThat
    from AssertJ (the project's test assertion library).

    METHOD 3: verifyCreditQueryDoesNotLogCallSiteValue
    After summarize(), assert the captured output contains at least one line with "event=" prefix
    (confirms privacy-formatted logging is happening). Assert it does NOT contain "amount_credits"
    or "ref_id" strings (raw credit ledger values). TRIAGE/DRAFT/PREVIEW strings are acceptable
    in event descriptions but assert they do not appear as standalone column values in a log line
    that also contains credit amounts.

    After implementing, run the backend suite:
    ./gradlew :backend:core:test :backend:api:test

    Fix any failures found. Common issues to check:
    - AnalyticsSummaryProjection constructor argument count mismatch (if a field was added in
      07-01 but the test's mock builder was not updated).
    - NPE in queryCreditSpend when test DB has no credit_reservation rows — ensure the method
      handles empty result set (returns CreditSpendProjection(0, 0, 0, 0)).
    - generate_series parameter type issue — if test DB is H2 (no generate_series), the test
      may need @DisabledIfSystemProperty or a separate Testcontainers profile. Check what the
      existing tests use; if Testcontainers Postgres, the trend query works as-is.

    If the existing test class uses H2 (embedded DB): the TREND_SQL with generate_series will fail
    on H2. In that case, mark verifyTrendQueryDoesNotLogSenderEmail with:
    @Tag("requires-postgres")
    @DisabledIfSystemProperty(named = "spring.datasource.url", matches = ".*h2.*")
    and document this in the test file. The test still exists but is correctly gated.

    Log format compliance check — run a grep against new service code:
    ./gradlew :backend:core:compileJava
    Then check: grep -r "log\." backend/core/src/main/java/com/zeromail/core/analytics/projection/ 2>/dev/null
    Every log statement must follow event=<name> tenantId={} format.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test :backend:api:test 2>&1 | tail -30</automated>
  </verify>
  <done>
    ./gradlew :backend:core:test :backend:api:test exits 0.
    AnalyticsPrivacySweepTest shows 3 new test methods passing (or correctly gated with @Tag
    if H2 limitation applies).
    No @Disabled methods remaining in AnalyticsPrivacySweepTest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend quality gates — TypeScript, ESLint, Vitest, i18n</name>
  <files>
    apps/web/e2e/analytics.spec.ts
  </files>
  <action>
    Run quality gates in order, fixing each failure before proceeding:

    GATE 1: TypeScript
    pnpm --filter web tsc --noEmit 2>&1
    Expected: 0 errors. Common Phase 7 issues:
    - AnalyticsSummaryResponse type from schema not updated → manual interface additions in analytics-api.ts
      need to match backend exactly (field names, nullability).
    - TrendPointResponse.date is string in TypeScript (LocalDate serializes to ISO string) — ensure
      no code assumes it is a Date object.
    - CreditSpendResponse.projectedMonthlyCredits is number (long in Java → number in TypeScript).
    Fix all type errors before proceeding.

    GATE 2: ESLint
    pnpm --filter web lint 2>&1
    Expected: 0 errors, 0 warnings. Common issues:
    - Unused imports from chart install (recharts tree-shaking).
    - Missing 'use client' directive (TrendPanel uses recharts, which requires client context).
    - React hooks rules (useState in TopSendersPanel — verify it is inside a function component).
    Fix all lint errors.

    GATE 3: Vitest
    pnpm --filter web test 2>&1
    Expected: all test files pass. Tests from 07-00 that should now be GREEN:
    - analyticsUtils.test.ts (safeCount, formatDelta, etc.)
    - TrendPanel.test.tsx
    - ActionBreakdownPanel.test.tsx
    If any test is still RED, fix the implementation (not the test — tests encode requirements).

    GATE 4: i18n
    pnpm --filter web run i18n:check 2>&1
    Expected: 0 missing keys. All new analytics.* keys must be present in both vi.json and en.json.
    Missing keys to add if found:
    - analytics.trend.* (6 keys)
    - analytics.actionBreakdown.* (6 keys)
    - analytics.noiseReduction.* (4 keys)
    - analytics.credits.* (7 keys)
    - analytics.topSenders.bySender, analytics.topSenders.byDomain, analytics.topSenders.emptyDomain
    - analytics.ruleHits.column.precision
    Fix any missing keys in both locale files lock-step.

    CREATE apps/web/e2e/analytics.spec.ts:
    Playwright e2e test covering the Phase 7 golden path. Uses Playwright Test syntax.
    Read existing e2e files in apps/web/e2e/ to match the project's Playwright setup pattern
    (baseURL, test fixtures, authentication helper if any).

    Test cases:
    test('trend panel visible on analytics page', async ({ page }) => {
      await page.goto('/analytics?window=7d')
      await page.waitForSelector('[data-testid="analytics-trend-panel"]', { timeout: 10000 })
      // Verify panel is at top of grid (visual position check via bounding rect)
      const trendPanel = page.locator('[data-testid="analytics-trend-panel"]')
      await expect(trendPanel).toBeVisible()
    })

    test('window chip change rerenders analytics panels', async ({ page }) => {
      await page.goto('/analytics?window=7d')
      await page.waitForSelector('[data-testid="analytics-trend-panel"]')
      // Click 30d chip
      await page.click('[data-testid="window-chip-30d"]')  // adjust selector to match existing WindowChips
      await page.waitForSelector('[data-testid="analytics-trend-panel"]')
      await expect(page.locator('[data-testid="analytics-trend-panel"]')).toBeVisible()
    })

    test('credits panel renders without console errors', async ({ page }) => {
      const consoleErrors: string[] = []
      page.on('console', msg => { if (msg.type() === 'error') consoleErrors.push(msg.text()) })
      await page.goto('/analytics?window=7d')
      await page.waitForSelector('[data-testid="analytics-credits-panel"]', { timeout: 10000 })
      // No JS errors during credits panel render
      expect(consoleErrors.filter(error => !error.includes('favicon'))).toHaveLength(0)
    })

    test('top senders domain toggle switches view', async ({ page }) => {
      await page.goto('/analytics?window=30d')
      await page.waitForSelector('[data-testid="analytics-top-senders-panel"]', { timeout: 10000 })
      // Find and click the By Domain tab trigger
      await page.getByRole('tab', { name: /by domain/i }).click()
      // Domain view should render (no crash, no error boundary)
      await expect(page.locator('[data-testid="analytics-top-senders-panel"]')).toBeVisible()
    })

    Note: If the e2e suite requires authentication (the /analytics route is behind auth), add the
    appropriate Playwright auth setup (read existing e2e helpers in the project — do not hardcode
    credentials). If no auth helper exists yet, mark the test with test.skip for now with a TODO
    comment and leave the structure in place for when auth e2e infrastructure is established.
    The checkpoint task below drives manual visual verification as a fallback.
  </action>
  <verify>
    <automated>pnpm --filter web tsc --noEmit && pnpm --filter web lint && pnpm --filter web test 2>&1 | tail -15</automated>
  </verify>
  <done>
    All 4 quality gates pass:
    - pnpm --filter web tsc --noEmit exits 0
    - pnpm --filter web lint exits 0
    - pnpm --filter web test exits 0 (including new panel tests GREEN)
    - pnpm --filter web run i18n:check exits 0
    apps/web/e2e/analytics.spec.ts created with 4 test cases.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 7 analytics enhancements:
    - TrendPanel (line chart, full-width at top, per-day observed/applied)
    - Delta % badges on VolumePanel and TimeSavedPanel
    - ActionBreakdownPanel (donut chart, 3 segments)
    - NoiseReductionPanel (computed % metric)
    - CreditsPanel (per-call_site table + monthly projection)
    - RuleHitsPanel extended with Precision Rate column + Trust Score badge
    - TopSendersPanel with By Sender / By Domain chip toggle (top-10)
    - All i18n in Vietnamese and English
    - Privacy sweep tests for new query methods
    - Full test suite (backend + Vitest + i18n:check) passing
  </what-built>
  <how-to-verify>
    1. Start the development stack: `./gradlew :backend:api:bootRun` and `pnpm --filter web dev`
    2. Navigate to http://localhost:3000/analytics?window=7d
    3. Verify layout top-to-bottom:
       - TrendPanel visible at the very top, full width, with a line chart (or empty state if no data)
       - VolumePanel and TimeSavedPanel show delta badges ("+N%" or "—") below their big numbers
       - NoiseReductionPanel shows a percentage (or "—")
       - ActionBreakdownPanel shows a donut chart (or empty state)
       - TopSendersPanel has "By Sender" / "By Domain" tabs at top; click "By Domain" and confirm domain strings appear
       - CreditsPanel shows per-call_site rows (AI Triage, AI Draft Reply, Rule Preview) with a projection note
       - RuleHitsPanel has a "Precision" column as the 4th column; rows with decisions > 0 show colored badges
    4. Change window chip to 30d, then 90d — confirm TrendPanel rerenders for each window
    5. Open browser DevTools console — confirm zero JavaScript errors during navigation
    6. Check the Vietnamese locale: if language toggle available, switch to Vietnamese and verify all new panel labels are in Vietnamese
  </how-to-verify>
  <resume-signal>Type "approved" if all panels render correctly, or describe any visual issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test log capture → production log assertions | OutputCaptureExtension captures SLF4J logs — asserts privacy invariant holds |
| Playwright → dev stack | e2e tests run against local dev build — not production secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Information Disclosure | Privacy sweep log capture | mitigate | Three new @Test methods assert no email/domain/credit-amount strings in logs for new query methods |
| T-07-05-02 | Information Disclosure | Playwright e2e screenshots | accept | e2e tests run in local dev environment with test data — no production credentials in test files |
| T-07-05-03 | Repudiation | Missing test coverage for new query paths | mitigate | AnalyticsDeltaComputationTest (unit), TrendPointQueryTest, CreditSpendQueryTest (integration) all green before this plan runs |
| T-07-05-SC | Tampering | No new package installs | accept | This plan adds only test/e2e files and activates existing @Disabled tests |
</threat_model>

<verification>
./gradlew :backend:core:test :backend:api:test 2>&1 | grep -E "(PASSED|FAILED|Tests run)" | tail -10
pnpm --filter web tsc --noEmit
pnpm --filter web lint
pnpm --filter web test
pnpm --filter web run i18n:check
</verification>

<success_criteria>
- AnalyticsPrivacySweepTest has 3 active (non-@Disabled) new test methods covering new query methods
- ./gradlew :backend:core:test :backend:api:test exits 0
- pnpm --filter web tsc --noEmit exits 0
- pnpm --filter web lint exits 0
- pnpm --filter web test exits 0 (all Vitest tests green including 07-00 RED tests now GREEN)
- pnpm --filter web run i18n:check exits 0
- apps/web/e2e/analytics.spec.ts created with 4 Playwright test cases
- Human checkpoint approved: all 7 new UI features visible and functional in browser
</success_criteria>

<output>
Create .planning/phases/07-analytics-enhancement/07-05-SUMMARY.md when done.
</output>
