---
phase: 07-analytics-enhancement
plan: "01"
type: execute
wave: 2
depends_on:
  - "07-00"
files_modified:
  - backend/core/src/main/java/com/zeromail/core/analytics/projection/TrendPointProjection.java
  - backend/core/src/main/java/com/zeromail/core/analytics/projection/ActionBreakdownProjection.java
  - backend/core/src/main/java/com/zeromail/core/analytics/projection/DeltasProjection.java
  - backend/core/src/main/java/com/zeromail/core/analytics/projection/CreditSpendProjection.java
  - backend/core/src/main/java/com/zeromail/core/analytics/projection/TopSenderDomainProjection.java
  - backend/core/src/main/java/com/zeromail/core/analytics/projection/AnalyticsDeltaComputation.java
  - backend/core/src/main/java/com/zeromail/core/analytics/projection/AnalyticsSummaryProjection.java
  - backend/core/src/main/java/com/zeromail/core/analytics/projection/AnalyticsSummaryQueryService.java
  - backend/core/src/main/resources/db/changelog/changes/023-analytics-trend-index.yaml
  - backend/api/src/main/java/com/zeromail/api/dto/analytics/AnalyticsSummaryResponse.java
autonomous: true
requirements:
  - ANL-04
  - ANL-05
  - ANL-06
  - ANL-07
  - ANL-08
  - ANL-09

must_haves:
  truths:
    - "GET /api/analytics/summary?window=7d returns trendPoints array with 7 entries"
    - "GET /api/analytics/summary returns actionBreakdown with labelCount, archiveCount, saveDraftCount"
    - "GET /api/analytics/summary returns deltas with nullable Long fields"
    - "GET /api/analytics/summary returns topSenderDomains list"
    - "GET /api/analytics/summary returns creditSpend with triageCredits, draftCredits, previewCredits, projectedMonthlyCredits"
    - "All new log lines follow event=<name> tenantId={} format"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/analytics/projection/TrendPointProjection.java"
      provides: "Projection record for per-day trend data"
    - path: "backend/core/src/main/java/com/zeromail/core/analytics/projection/CreditSpendProjection.java"
      provides: "Projection record for aggregated credit spend"
    - path: "backend/core/src/main/java/com/zeromail/core/analytics/projection/AnalyticsDeltaComputation.java"
      provides: "Package-private static utility for delta % computation (testable)"
    - path: "backend/core/src/main/java/com/zeromail/core/analytics/projection/AnalyticsSummaryQueryService.java"
      provides: "Extended service with 4 new query methods"
      exports: ["summarize"]
    - path: "backend/api/src/main/java/com/zeromail/api/dto/analytics/AnalyticsSummaryResponse.java"
      provides: "Extended response DTO with nested records for new fields"
      exports: ["from"]
    - path: "backend/core/src/main/resources/db/changelog/changes/023-analytics-trend-index.yaml"
      provides: "Optional composite index for trend query JOIN performance"
  key_links:
    - from: "AnalyticsSummaryQueryService.summarize()"
      to: "AnalyticsSummaryProjection"
      via: "Constructor call with 10 fields"
      pattern: "new AnalyticsSummaryProjection"
    - from: "AnalyticsSummaryResponse.from()"
      to: "AnalyticsSummaryProjection"
      via: "Projection field accessors in from() factory"
      pattern: "projection\\.trendPoints\\|creditSpend\\|deltas"
    - from: "AnalyticsSummaryQueryService"
      to: "credit_ledger_entry / credit_reservation tables"
      via: "Raw JdbcTemplate CREDIT_SPEND_SQL — no billing domain import"
      pattern: "CREDIT_SPEND_SQL"
---

<objective>
Implement all backend data layer changes: 5 new projection records, 4 new SQL query methods in
AnalyticsSummaryQueryService, a Liquibase changeset for performance indexes, and the extended
AnalyticsSummaryResponse DTO with nested records.

Purpose: The single GET /api/analytics/summary endpoint now returns all new data dimensions
(ANL-04 through ANL-09 plus domain grouping). Frontend plans (07-02, 07-03, 07-04) read from
this extended response.

Output: Extended backend data pipeline from DB to JSON, turning the RED tests from 07-00 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-analytics-enhancement/07-CONTEXT.md
@.planning/phases/07-analytics-enhancement/07-RESEARCH.md
@.planning/phases/07-analytics-enhancement/07-PATTERNS.md
@CONVENTIONS.md
@CLAUDE.md

<interfaces>
From existing backend/core/.../analytics/projection/AnalyticsSummaryProjection.java (current fields):
  public record AnalyticsSummaryProjection(
          long volumeObserved,
          long volumeApplied,
          long timeSavedSeconds,
          List<TopSenderProjection> topSenders,
          List<RuleHitProjection> ruleHits) { ... }

From existing backend/core/.../analytics/projection/AnalyticsSummaryQueryService.java:
  @Service
  @Transactional(readOnly = true)
  public class AnalyticsSummaryQueryService {
      private final JdbcTemplate jdbcTemplate;
      public AnalyticsSummaryProjection summarize(UUID tenantId, TimeWindow requestedWindow) { ... }
      private long queryCount(String sql, UUID tenantId, Timestamp start, Timestamp end) { ... }
  }

From existing backend/api/.../dto/analytics/AnalyticsSummaryResponse.java (current fields):
  public record AnalyticsSummaryResponse(
          String window,
          long volumeObserved,
          long volumeApplied,
          long timeSavedSeconds,
          List<TopSenderResponse> topSenders,
          List<RuleHitResponse> ruleHits) { ... }
  public static AnalyticsSummaryResponse from(AnalyticsSummaryProjection projection, AnalyticsWindow window) { ... }

From existing backend/api/.../dto/analytics/AnalyticsSummaryResponse.java nested records:
  public record TopSenderResponse(String email, long count) { ... }
  public record RuleHitResponse(String ruleName, long decisions, long applied) { ... }

From backend/api/.../analytics/AnalyticsWindow.java:
  public enum AnalyticsWindow implements IdentifiedEnum {
      SEVEN_DAYS("7d"), THIRTY_DAYS("30d"), NINETY_DAYS("90d");
      public String id() { ... }
      // If days() method does not exist, add it: SEVEN_DAYS → 7, THIRTY_DAYS → 30, NINETY_DAYS → 90
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New projection records + AnalyticsDeltaComputation utility</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/analytics/projection/TrendPointProjection.java
    backend/core/src/main/java/com/zeromail/core/analytics/projection/ActionBreakdownProjection.java
    backend/core/src/main/java/com/zeromail/core/analytics/projection/DeltasProjection.java
    backend/core/src/main/java/com/zeromail/core/analytics/projection/CreditSpendProjection.java
    backend/core/src/main/java/com/zeromail/core/analytics/projection/TopSenderDomainProjection.java
    backend/core/src/main/java/com/zeromail/core/analytics/projection/AnalyticsDeltaComputation.java
  </files>
  <behavior>
    - TrendPointProjection(LocalDate date, long observed, long applied) — no compact constructor needed (no List fields).
    - ActionBreakdownProjection(long labelCount, long archiveCount, long saveDraftCount) — no compact constructor needed.
    - DeltasProjection(Long volumeObservedDeltaPct, Long volumeAppliedDeltaPct, Long timeSavedDeltaPct) — nullable Longs; null means prior window had zero data.
    - CreditSpendProjection(long triageCredits, long draftCredits, long previewCredits, long projectedMonthlyCredits) — all non-negative; projectedMonthlyCredits = (total / windowDays * 30).
    - TopSenderDomainProjection(String domain, long count) — domain is non-null (SQL WHERE filters nulls).
    - AnalyticsDeltaComputation: package-private class with one package-private static method:
        static Long deltaPct(long current, long prior) — returns null when prior == 0, else Math.round((current - prior) * 100.0 / prior) cast to Long.
  </behavior>
  <action>
    Create all 5 projection record files and 1 utility class in package
    com.zeromail.core.analytics.projection. Each file follows the TopSenderProjection.java pattern:
    minimal record with package declaration, no imports beyond java.time.LocalDate for TrendPointProjection.

    TrendPointProjection.java:
      package com.zeromail.core.analytics.projection;
      import java.time.LocalDate;
      public record TrendPointProjection(LocalDate date, long observed, long applied) {}

    ActionBreakdownProjection.java:
      package com.zeromail.core.analytics.projection;
      public record ActionBreakdownProjection(long labelCount, long archiveCount, long saveDraftCount) {}

    DeltasProjection.java:
      package com.zeromail.core.analytics.projection;
      public record DeltasProjection(Long volumeObservedDeltaPct, Long volumeAppliedDeltaPct, Long timeSavedDeltaPct) {}

    CreditSpendProjection.java:
      package com.zeromail.core.analytics.projection;
      public record CreditSpendProjection(long triageCredits, long draftCredits, long previewCredits, long projectedMonthlyCredits) {}

    TopSenderDomainProjection.java:
      package com.zeromail.core.analytics.projection;
      public record TopSenderDomainProjection(String domain, long count) {}

    AnalyticsDeltaComputation.java:
      package com.zeromail.core.analytics.projection;
      final class AnalyticsDeltaComputation {
          private AnalyticsDeltaComputation() {}
          static Long deltaPct(long current, long prior) {
              if (prior == 0L) return null;
              return Math.round((current - prior) * 100.0 / prior);
          }
      }

    No Lombok, no Spring annotations — these are pure data records and a utility class.
    Domain-revealing names: no abbreviations for field names.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -10</automated>
  </verify>
  <done>
    ./gradlew :backend:core:compileJava exits 0. All 6 new files compile.
    AnalyticsDeltaComputationTest from 07-00 now compiles and the deltaPct tests turn GREEN:
    ./gradlew :backend:core:test --tests "*DeltaComputation*" exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend AnalyticsSummaryProjection + AnalyticsSummaryQueryService with new queries</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/analytics/projection/AnalyticsSummaryProjection.java
    backend/core/src/main/java/com/zeromail/core/analytics/projection/AnalyticsSummaryQueryService.java
    backend/core/src/main/resources/db/changelog/changes/023-analytics-trend-index.yaml
  </files>
  <behavior>
    AnalyticsSummaryProjection must gain 5 new fields:
    - List<TrendPointProjection> trendPoints
    - ActionBreakdownProjection actionBreakdown
    - DeltasProjection deltas
    - List<TopSenderDomainProjection> topSenderDomains
    - CreditSpendProjection creditSpend
    The compact constructor must include List.copyOf for trendPoints and topSenderDomains.

    AnalyticsSummaryQueryService.summarize() must:
    1. Call queryTrendPoints(tenantId, windowStartTs, windowEndTs) → trendPoints.
    2. Call toActionBreakdown(appliedByActionType) → actionBreakdown (per D-07, reuse existing map).
    3. Call queryDeltas(tenantId, requestedWindow, currentVolumeObserved, currentVolumeApplied, currentTimeSavedSeconds) → deltas.
    4. Call queryTopSenderDomains(tenantId, windowStartTs, windowEndTs) → topSenderDomains (LIMIT 10 per D-12).
    5. Call queryCreditSpend(tenantId, windowStartTs, windowEndTs, requestedWindow) → creditSpend.
    6. Change existing topSenders query LIMIT from 3 to 10 (per D-11).

    SQL constants (per RESEARCH.md verified patterns — do NOT deviate):
    TREND_SQL: generate_series from windowStart to windowEndExclusive using interval '1 day', LEFT JOIN
      mail_message_observed on tenant_id + date_trunc UTC, LEFT JOIN triage_audit on same, GROUP BY day.
    TOP_SENDER_DOMAINS_SQL: SUBSTRING(sender_email FROM '@(.+)$') GROUP BY domain, ORDER BY count DESC LIMIT 10.
    CREDIT_SPEND_SQL: JOIN credit_ledger_entry cle to credit_reservation cr ON cle.ref_id = cr.id::varchar
      AND ref_type='RESERVATION' AND kind='SETTLE', filter cr.call_site IN ('TRIAGE','DRAFT','PREVIEW').

    Privacy logging: the new log line at end of summarize() or at start of each helper must be
    event=analytics_<name>_queried tenantId={}. NEVER log domain strings, sender emails, or call_site values.
  </behavior>
  <action>
    Read the current AnalyticsSummaryProjection.java and AnalyticsSummaryQueryService.java fully before
    editing (to understand exact field order and existing SQL constant style).

    STEP 1 — Update AnalyticsSummaryProjection.java:
    Replace the record declaration to add 5 new fields at the end (after existing fields). Add
    List.copyOf(trendPoints) and List.copyOf(topSenderDomains) to the compact constructor.
    Keep existing List.copyOf calls for topSenders and ruleHits.

    STEP 2 — Update AnalyticsSummaryQueryService.java:
    a. Add 4 new private static final String SQL constants: TREND_SQL, TOP_SENDER_DOMAINS_SQL,
       CREDIT_SPEND_SQL. Change TOP_SENDERS_SQL LIMIT from 3 to 10.

    b. Add private method queryTrendPoints(UUID tenantId, Timestamp windowStartInclusive, Timestamp windowEndExclusive).
       Uses jdbcTemplate.query(TREND_SQL, rowMapper, windowStartInclusive, windowEndExclusive, tenantId, tenantId).
       Row mapper: resultSet.getDate("day_date").toLocalDate(), getLong("observed"), getLong("applied").
       Column alias in TREND_SQL must be "day_date" (not "day" — avoids reserved word conflicts).

    c. Add private static method toActionBreakdown(Map<String, Long> appliedByActionType):
       Returns new ActionBreakdownProjection(
           appliedByActionType.getOrDefault("LABEL", 0L),
           appliedByActionType.getOrDefault("ARCHIVE", 0L),
           appliedByActionType.getOrDefault("SAVE_DRAFT", 0L)).
       Use UPPERCASE keys matching the CallSite enum values stored in DB.

    d. Add private method queryDeltas(UUID tenantId, TimeWindow currentWindow, long currentObserved,
       long currentApplied, long currentTimeSaved).
       Compute priorWindowStart = currentWindow.startInclusive().minus(Duration.between(
           currentWindow.startInclusive(), currentWindow.endExclusive())).
       priorWindowEnd = currentWindow.startInclusive().
       Query priorObserved and priorApplied using existing queryCount with OBSERVED_VOLUME_SQL and
       APPLIED_VOLUME_SQL. For timeSaved prior, call queryAppliedByActionType for prior window and
       compute time saved using the same TIME_SAVED_PER_ACTION_SECONDS constant used by the current window.
       Return new DeltasProjection(
           AnalyticsDeltaComputation.deltaPct(currentObserved, priorObserved),
           AnalyticsDeltaComputation.deltaPct(currentApplied, priorApplied),
           AnalyticsDeltaComputation.deltaPct(currentTimeSaved, priorTimeSaved)).

    e. Add private method queryTopSenderDomains(UUID tenantId, Timestamp windowStartInclusive, Timestamp windowEndExclusive).
       Uses jdbcTemplate.query with TOP_SENDER_DOMAINS_SQL. Row mapper: getString("domain"), getLong("domain_count").
       Returns List<TopSenderDomainProjection>. Log event=analytics_domain_senders_queried tenantId={} (do NOT log domain values).

    f. Add private method queryCreditSpend(UUID tenantId, Timestamp windowStartInclusive, Timestamp windowEndExclusive, TimeWindow window).
       Uses jdbcTemplate.query with CREDIT_SPEND_SQL. Iterate results into a Map<String, Long> with merge (fail-loud on duplicate per anti-pattern from PATTERNS.md).
       Build CreditSpendProjection:
           triageCredits = spendByCallSite.getOrDefault("TRIAGE", 0L)
           draftCredits = spendByCallSite.getOrDefault("DRAFT", 0L)
           previewCredits = spendByCallSite.getOrDefault("PREVIEW", 0L)
           totalConsumed = triageCredits + draftCredits + previewCredits
           windowDays = Duration.between(window.startInclusive(), window.endExclusive()).toDays()
           projectedMonthlyCredits = windowDays > 0 ? Math.round((double) totalConsumed / windowDays * 30) : 0L
       Returns new CreditSpendProjection(triageCredits, draftCredits, previewCredits, projectedMonthlyCredits).
       Do NOT import any class from com.zeromail.core.billing — use raw String keys ("TRIAGE", "DRAFT", "PREVIEW") per D-10 discretion (raw JdbcTemplate approach).

    g. Update summarize() to call all 5 new private methods and pass results to AnalyticsSummaryProjection constructor.
       The constructor call order must match the record field order (existing fields first, then new fields).

    STEP 3 — Liquibase changeset 023-analytics-trend-index.yaml:
    Create file at backend/core/src/main/resources/db/changelog/changes/023-analytics-trend-index.yaml.
    Add a composite index on mail_message_observed(tenant_id, observed_at) if it does not already exist
    (use preconditions: onFail: MARK_RAN). Also add index on triage_audit(tenant_id, applied_at) with same guard.
    These indexes support the generate_series LEFT JOIN date_trunc lookups.
    Changeset id: 023-analytics-trend-index, author: zero-mail.
    Reference this changeset in db/changelog/db.changelog-master.yaml (append include).

    All variable names must be domain-revealing: windowStartInclusive, windowEndExclusive, priorWindowStart,
    priorWindowEnd, spendByCallSite, triageCredits, draftCredits, previewCredits, totalConsumed, windowDays.
    No abbreviations: no ts, no start, no end, no dur.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*TrendPoint*" --tests "*CreditSpend*" --tests "*Analytics*" 2>&1 | tail -30</automated>
  </verify>
  <done>
    ./gradlew :backend:core:test exits 0 for all Analytics* tests including the RED tests from 07-00
    that now turn GREEN. No compilation errors. New SQL constants are present in the service class.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend AnalyticsSummaryResponse DTO with nested records and updated from() factory</name>
  <files>
    backend/api/src/main/java/com/zeromail/api/dto/analytics/AnalyticsSummaryResponse.java
  </files>
  <behavior>
    AnalyticsSummaryResponse record gains 5 new fields:
    - List<TrendPointResponse> trendPoints
    - ActionBreakdownResponse actionBreakdown
    - DeltasResponse deltas (wraps DeltasProjection nullable Long fields)
    - List<TopSenderDomainResponse> topSenderDomains
    - CreditSpendResponse creditSpend

    5 new nested static records with package-private from() factories:
    - TrendPointResponse(String date, long observed, long applied) — date is LocalDate.toString() ISO-8601
    - ActionBreakdownResponse(long labelCount, long archiveCount, long saveDraftCount)
    - DeltasResponse(Long volumeObservedDeltaPct, Long volumeAppliedDeltaPct, Long timeSavedDeltaPct)
    - TopSenderDomainResponse(String domain, long count)
    - CreditSpendResponse(long triageCredits, long draftCredits, long previewCredits, long projectedMonthlyCredits)

    from() factory maps projection to response correctly. Compact constructor includes
    List.copyOf for trendPoints and topSenderDomains.

    JSON serialization: null Long fields in DeltasResponse serialize as JSON null (Spring Boot 4
    Jackson 3 default — do not configure WRITE_NULL_AS_OMISSION unless explicitly required).
  </behavior>
  <action>
    Read AnalyticsSummaryResponse.java completely before editing to understand current field order
    and nested record placement.

    Add 5 new fields at the end of the outer record component list (after ruleHits).
    Update the compact constructor to add List.copyOf(trendPoints) and List.copyOf(topSenderDomains).
    Keep existing List.copyOf(topSenders) and List.copyOf(ruleHits).

    Update from(AnalyticsSummaryProjection projection, AnalyticsWindow window) factory:
    Pass new fields to the constructor. The 5 new arguments come after the existing 4 arguments:
      - projection.trendPoints().stream().map(TrendPointResponse::from).toList()
      - ActionBreakdownResponse.from(projection.actionBreakdown())
      - DeltasResponse.from(projection.deltas())
      - projection.topSenderDomains().stream().map(TopSenderDomainResponse::from).toList()
      - CreditSpendResponse.from(projection.creditSpend())

    Add 5 new nested static record definitions inside the outer record (after existing RuleHitResponse):

    public record TrendPointResponse(String date, long observed, long applied) {
        static TrendPointResponse from(TrendPointProjection projection) {
            return new TrendPointResponse(projection.date().toString(), projection.observed(), projection.applied());
        }
    }

    public record ActionBreakdownResponse(long labelCount, long archiveCount, long saveDraftCount) {
        static ActionBreakdownResponse from(ActionBreakdownProjection projection) {
            return new ActionBreakdownResponse(projection.labelCount(), projection.archiveCount(), projection.saveDraftCount());
        }
    }

    public record DeltasResponse(Long volumeObservedDeltaPct, Long volumeAppliedDeltaPct, Long timeSavedDeltaPct) {
        static DeltasResponse from(DeltasProjection projection) {
            return new DeltasResponse(projection.volumeObservedDeltaPct(), projection.volumeAppliedDeltaPct(), projection.timeSavedDeltaPct());
        }
    }

    public record TopSenderDomainResponse(String domain, long count) {
        static TopSenderDomainResponse from(TopSenderDomainProjection projection) {
            return new TopSenderDomainResponse(projection.domain(), projection.count());
        }
    }

    public record CreditSpendResponse(long triageCredits, long draftCredits, long previewCredits, long projectedMonthlyCredits) {
        static CreditSpendResponse from(CreditSpendProjection projection) {
            return new CreditSpendResponse(projection.triageCredits(), projection.draftCredits(), projection.previewCredits(), projection.projectedMonthlyCredits());
        }
    }

    Add required imports for TrendPointProjection, ActionBreakdownProjection, DeltasProjection,
    TopSenderDomainProjection, CreditSpendProjection from com.zeromail.core.analytics.projection.

    Verify AnalyticsWindow has a days() method or equivalent. If not, add a private helper in the
    from() factory: long windowDays = switch (window) { case SEVEN_DAYS -> 7L; case THIRTY_DAYS -> 30L; case NINETY_DAYS -> 90L; }.
    The projectedMonthlyCredits is already computed in CreditSpendProjection by the service — the DTO
    just passes it through; do NOT recompute in the DTO.
  </action>
  <verify>
    <automated>./gradlew :backend:api:compileJava :backend:api:test 2>&1 | tail -20</automated>
  </verify>
  <done>
    ./gradlew :backend:api:compileJava exits 0. ./gradlew :backend:api:test exits 0.
    Running the application and calling GET /api/analytics/summary?window=7d returns a JSON response
    that includes trendPoints, actionBreakdown, deltas, topSenderDomains, and creditSpend fields.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → AnalyticsController | window parameter validated by existing controller (no change needed) |
| DB → JdbcTemplate | All tenant_id values come from authenticated session context, never from user input |
| credit_ledger_entry JOIN credit_reservation | Raw table access without billing domain import — correct isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | TREND_SQL log output | mitigate | Log only event=analytics_trend_queried tenantId={}; never log date values or message counts in log body |
| T-07-01-02 | Information Disclosure | TOP_SENDER_DOMAINS_SQL | mitigate | Log only event=analytics_domain_senders_queried tenantId={}; domain strings stay out of logs |
| T-07-01-03 | Information Disclosure | CREDIT_SPEND_SQL | mitigate | Log only event=analytics_credit_spend_queried tenantId={}; callSite grouping keys not logged individually |
| T-07-01-04 | Elevation of Privilege | credit_ledger_entry cross-tenant access | mitigate | WHERE cle.tenant_id = ? ensures all credit queries are tenant-scoped; tenantId comes from session not request |
| T-07-01-05 | Tampering | generate_series parameter injection | accept | Parameters are java.sql.Timestamp objects, not strings — no SQL injection risk via JdbcTemplate |
| T-07-01-SC | Tampering | No new npm/pip packages | accept | This plan is pure Java — no package manager installs |
</threat_model>

<verification>
./gradlew :backend:core:test :backend:api:test 2>&1 | tail -40

Expected: All existing tests pass + new tests from 07-00 that target new methods now turn GREEN.
Check JSON output: curl -s "http://localhost:8080/api/analytics/summary?window=7d" | python3 -m json.tool | grep -E "trendPoints|actionBreakdown|deltas|topSenderDomains|creditSpend"
</verification>

<success_criteria>
- ./gradlew :backend:core:test exits 0 with all Analytics* tests green
- ./gradlew :backend:api:test exits 0
- AnalyticsSummaryProjection has 10 fields (5 existing + 5 new)
- AnalyticsSummaryResponse has 11 fields (6 existing + 5 new)
- AnalyticsSummaryResponse.java contains 5 new nested record types
- 023-analytics-trend-index.yaml exists and is referenced in db.changelog-master.yaml
- No com.zeromail.core.billing imports in AnalyticsSummaryQueryService.java
- All new log statements follow event=<name> tenantId={} format
</success_criteria>

<output>
Create .planning/phases/07-analytics-enhancement/07-01-SUMMARY.md when done.
</output>
