---
phase: 01.6-brand-identity-design-tokens-and-landing-page
plan: 04
subsystem: ui
tags: [nextjs, react, landing, i18n, shadcn, tailwind]

requires:
  - phase: 01.6-00
    provides: Wave 0 landing tests and i18n scan coverage
  - phase: 01.6-01
    provides: Teal paper-warm design tokens
  - phase: 01.6-02
    provides: typography stack and Vietnamese-safe font split
  - phase: 01.6-03
    provides: public layout chrome and theme cookie support
provides:
  - Demo-ready landing section composition at /
  - Auth-aware Hero CTA using getCurrentUserCached
  - Landing how/feature/trust i18n namespaces in EN and VI
affects: [phase-01.6, landing, i18n, public-ui]

tech-stack:
  added: []
  patterns:
    - Static RSC landing sections with token-only styling
    - Direct shadcn primitive composition for repeated landing items
    - Locale-conditional hero heading font to avoid Instrument Serif for Vietnamese

key-files:
  created:
    - apps/web/features/landing/components/Hero.tsx
    - apps/web/features/landing/components/HowItWorks.tsx
    - apps/web/features/landing/components/Features.tsx
    - apps/web/features/landing/components/TrustPillars.tsx
  modified:
    - apps/web/app/(public)/page.tsx
    - apps/web/i18n/messages/en.json
    - apps/web/i18n/messages/vi.json

key-decisions:
  - "Hero owns the auth-aware CTA and calls getCurrentUserCached with the primitive cookie header string."
  - "Vietnamese hero headings stay on font-sans; Instrument Serif is only used for English hero display."
  - "pnpm --filter verification paths are package-relative because zeromail-web commands run from apps/web."

patterns-established:
  - "Landing sections render as async Server Components with next-intl server translations."
  - "Landing section eyebrows use Badge with bg-accent-soft/text-accent token classes."
  - "Repeated landing content uses Card directly; no section wrapper primitive was introduced."

requirements-completed: [1.6-3, 1.6-9]

duration: 8min
completed: 2026-04-28
---

# Phase 01.6 Plan 04: Landing Sections Summary

**Demo-ready public landing sections with auth-aware CTA, token-only shadcn composition, and locked EN/VI copy for workflow, capabilities, and trust.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-28T09:51:21Z
- **Completed:** 2026-04-28T09:59:27Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- Added `Hero`, `HowItWorks`, `Features`, and `TrustPillars` as static async RSC landing sections.
- Moved the public landing page from a skeleton `<main>` into a thin four-section composition.
- Added EN/VI `how.*`, `feat.*`, `trust.*`, and `landing.eyebrow` copy in lock-step.
- Preserved auth-aware CTA behavior in Hero using `getCurrentUserCached(cookieHeader)` with silent fallback to `/login`.

## Task Commits

1. **Task 4.1: Extend i18n VI/EN with landing/how/feat/trust namespaces** - `dd67a26` (feat)
2. **Task 4.2: Create Hero + HowItWorks sections** - `c35477b` (feat)
3. **Task 4.3: Create Features + TrustPillars sections + compose page** - `0e971fa` (feat)
4. **Deviation fix: Replace placeholder hero tagline** - `2a9df29` (fix)

## Files Created/Modified

- `apps/web/features/landing/components/Hero.tsx` - Auth-aware hero with locale-safe heading font and CTA.
- `apps/web/features/landing/components/HowItWorks.tsx` - Three-step workflow section using direct Card composition.
- `apps/web/features/landing/components/Features.tsx` - Six-card capability grid using token-aware styling.
- `apps/web/features/landing/components/TrustPillars.tsx` - Four trust cards aligned to the privacy/no-auto-send contract.
- `apps/web/app/(public)/page.tsx` - Thin fragment that renders the four landing sections in order.
- `apps/web/i18n/messages/en.json` - Added landing/how/feat/trust copy and replaced placeholder hero tagline.
- `apps/web/i18n/messages/vi.json` - Mirrored EN key structure with Vietnamese copy and technical terms preserved.

## Decisions Made

- Hero remains the only section that reads auth state; the page itself is composition-only.
- Badge was used for section eyebrows so the new sections still compose shadcn primitives directly.
- Feature and trust item cards get only token-driven hover shadow polish, keeping the interaction thesis static and RSC-first.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Functionality] Replaced visible placeholder hero tagline**
- **Found during:** Summary stub scan after Task 4.3
- **Issue:** The preserved `landing.tagline` values still said the page was being finalized, which made the landing non-demo-ready.
- **Fix:** Replaced the placeholder EN/VI tagline with trust-aligned Gmail triage copy.
- **Files modified:** `apps/web/i18n/messages/en.json`, `apps/web/i18n/messages/vi.json`
- **Verification:** `pnpm --filter zeromail-web i18n:check`; `pnpm --filter zeromail-web exec tsc --noEmit`; `pnpm --filter zeromail-web test -- test/landing-page.test.tsx`
- **Committed in:** `2a9df29`

**2. [Rule 3 - Blocking Verification] Adjusted package-scoped verification commands**
- **Found during:** Task 4.1 and Task 4.3 verification
- **Issue:** `pnpm --filter zeromail-web tsc --noEmit` failed because `tsc` is not a package script, and `apps/web/test/...` paths failed because filtered scripts execute from `apps/web`.
- **Fix:** Used equivalent package-scoped commands: `pnpm --filter zeromail-web exec tsc --noEmit` and package-relative test paths under `test/...`.
- **Files modified:** None
- **Verification:** Corrected commands passed.
- **Committed in:** Not applicable - verification-only adjustment.

---

**Total deviations:** 2 auto-fixed (1 missing critical content, 1 blocking verification adjustment)
**Impact on plan:** No scope creep; both deviations were necessary to satisfy the landing quality gate and verify the work in this workspace.

## Known Stubs

- `apps/web/i18n/messages/en.json:194` - Pre-existing Settings copy says "Coming soon: you can bring your own API key (BYOK)"; unrelated to this landing plan and not rendered by the new sections.
- `apps/web/i18n/messages/en.json:228` - Pre-existing key name `confirmInputPlaceholder`; false-positive from stub scan, value is the required delete confirmation phrase.
- `apps/web/i18n/messages/vi.json:228` - Pre-existing key name `confirmInputPlaceholder`; false-positive from stub scan, value is the required delete confirmation phrase.

## Issues Encountered

- Context7 resolved Next.js but did not return snippets; local bundled Next.js 16 docs under `node_modules/next/dist/docs/` were used for `headers()`, `cookies()`, Server Components, and `Link`.
- The only untracked file throughout execution was root `REVIEW.md`; it was outside scope and left untouched.

## Verification

- `pnpm --filter zeromail-web test -- test/landing-page.test.tsx test/public-layout.test.tsx` - PASS (2 files, 2 tests)
- `pnpm --filter zeromail-web i18n:check` - PASS (151 leaf keys)
- `pnpm --filter zeromail-web exec tsc --noEmit` - PASS
- New section `<Button` scan - PASS (0 matches)
- Landing raw color scan for `text-stone-|bg-amber-|border-blue-|text-gray-` - PASS (0 matches)
- `git diff --stat 4d99013..HEAD -- backend/` - PASS (no backend changes)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 05 can build sign-in TrustPanel/legal footer copy on the shared `trust.p1..p3` namespace added here. The public landing page now has the section structure and copy needed for the later visual sweep and Lighthouse pass.

## Self-Check: PASSED

- Created/modified files exist: PASS
- Recorded commits exist: PASS (`dd67a26`, `c35477b`, `0e971fa`, `2a9df29`)
- Unexpected tracked deletions: none

---
*Phase: 01.6-brand-identity-design-tokens-and-landing-page*
*Completed: 2026-04-28*
