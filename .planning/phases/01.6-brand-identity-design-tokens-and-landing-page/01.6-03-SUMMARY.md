---
phase: 01.6-brand-identity-design-tokens-and-landing-page
plan: 03
subsystem: ui
tags: [nextjs, react, i18n, theme-cookie, public-layout]

requires:
  - phase: 01.6-01
    provides: "Teal + paper-warm design tokens and chrome helper variables"
  - phase: 01.6-02
    provides: "Root layout font stack with Geist, Be Vietnam Pro, and Instrument Serif"
provides:
  - "Public layout shell with TopBar and Footer"
  - "CurrentColor Zero Mail logo mark"
  - "Cookie-backed theme Server Action and client toggle"
  - "Root layout first-paint dark class from zm-theme cookie"
  - "Mirrored nav/footer i18n namespaces for EN and VI"
affects: [landing-page, auth-topbar, theme-persistence, i18n]

tech-stack:
  added: []
  patterns:
    - "Server Action form action writes non-sensitive zm-theme cookie"
    - "Public chrome uses deep feature imports without barrels"

key-files:
  created:
    - apps/web/features/landing/components/ZMLogoMark.tsx
    - apps/web/features/landing/components/Footer.tsx
    - apps/web/features/landing/components/ThemeToggle.tsx
    - apps/web/features/landing/components/TopBar.tsx
    - apps/web/features/landing/lib/setTheme.ts
  modified:
    - apps/web/app/(public)/layout.tsx
    - apps/web/app/layout.tsx
    - apps/web/test/theme-cookie.test.tsx
    - apps/web/i18n/messages/en.json
    - apps/web/i18n/messages/vi.json

key-decisions:
  - "Theme persistence uses the locked Server Action + cookie contract; no localStorage or client-side cookie write was added."
  - "LanguageSwitcher variant='compact' was supported and reused directly; no fork or prop fallback was needed."
  - "TopBar wraps ZMLogoMark in a token-colored span instead of passing className to avoid the temporary Wave 0 ambient type stub."

patterns-established:
  - "Public TopBar can read zm-theme through async cookies() and pass a closed light/dark prop to ThemeToggle."
  - "Root layout applies dark class from zm-theme during RSC render while preserving reassertServerLocale."

requirements-completed: [1.6-4, 1.6-5, 1.6-7, 1.6-9]

duration: 6min
completed: 2026-04-28
---

# Phase 01.6 Plan 03: Public Layout Shell Summary

**Public chrome with Zero Mail logo, Footer, cookie-backed theme toggle, and first-paint dark-mode class**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-28T09:38:48Z
- **Completed:** 2026-04-28T09:45:46Z
- **Tasks:** 3
- **Files modified:** 11 code/message/test files + 2 planning artifacts

## Accomplishments

- Added `ZMLogoMark`, `TopBar`, `Footer`, `ThemeToggle`, and `setTheme`.
- Wired `(public)/layout.tsx` to render TopBar, children, and Footer.
- Extended root `app/layout.tsx` to read `zm-theme` and emit `dark` on `<html>` for first paint.
- Added mirrored `nav.*` and `footer.*` i18n keys in English and Vietnamese.

## Task Commits

1. **Task 3.1: Create ZMLogoMark + Footer + i18n keys** - `fed16ce`
2. **Task 3.2: Create setTheme Server Action + ThemeToggle** - `3ddecc7`
3. **Task 3.3: Create TopBar + wire layouts/theme read** - `91fc35b`
4. **Acceptance repair: mock next/font in theme-cookie spec** - `422e986`

## Files Created/Modified

- `apps/web/features/landing/components/ZMLogoMark.tsx` - token-inheriting currentColor logo SVG.
- `apps/web/features/landing/components/Footer.tsx` - public legal footer links.
- `apps/web/features/landing/components/ThemeToggle.tsx` - client form action toggle with inline SVG icons.
- `apps/web/features/landing/components/TopBar.tsx` - auth-aware public chrome with logo, theme, language, and CTA controls.
- `apps/web/features/landing/lib/setTheme.ts` - Server Action writing the locked `zm-theme` cookie.
- `apps/web/app/(public)/layout.tsx` - public shell now wraps children with TopBar and Footer.
- `apps/web/app/layout.tsx` - reads `zm-theme` and applies conditional `dark` class.
- `apps/web/test/theme-cookie.test.tsx` - adds the same spec-local `next/font/google` mock pattern used by the root layout font spec.
- `apps/web/i18n/messages/en.json` / `vi.json` - added mirrored `nav.*` and `footer.*` keys.

## Verification

- PASS: `pnpm --filter zeromail-web test -- --run test/zm-logo-mark.test.tsx` - 3 tests passed.
- PASS: `pnpm --filter zeromail-web test -- --run test/public-layout.test.tsx test/zm-logo-mark.test.tsx test/root-layout-fonts.test.tsx` - 5 tests passed.
- PASS: `pnpm --filter zeromail-web test -- --run test/theme-cookie.test.tsx` - 2 tests passed.
- PASS: `pnpm --filter zeromail-web i18n:check` - 109 leaf keys, EN/VI parity OK.
- PASS: `pnpm --filter zeromail-web exec tsc --noEmit`.
- PASS: static acceptance greps for ZMLogoMark, setTheme cookie attributes, ThemeToggle constraints, TopBar imports, public layout imports, root `zm-theme`, and no `localStorage` theme usage.
- PASS: `git diff --stat -- backend/` returned no output.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Used package-relative Vitest paths**
- **Found during:** Task 3.1 verification
- **Issue:** The plan's `apps/web/test/...` paths are invalid once `pnpm --filter zeromail-web` runs inside `apps/web`, causing "No test files found".
- **Fix:** Ran equivalent package-relative paths such as `test/zm-logo-mark.test.tsx`.
- **Files modified:** None
- **Verification:** Package-relative Vitest commands passed.
- **Committed in:** N/A

**2. [Rule 3 - Blocking] Used existing TypeScript invocation**
- **Found during:** Task 3.1 verification
- **Issue:** `zeromail-web` has no `tsc` script, so `pnpm --filter zeromail-web tsc --noEmit` cannot run.
- **Fix:** Used `pnpm --filter zeromail-web exec tsc --noEmit`.
- **Files modified:** None
- **Verification:** TypeScript passed.
- **Committed in:** N/A

**3. [Rule 3 - Blocking] Avoided temporary ZMLogoMark ambient type stub**
- **Found during:** Task 3.3 TypeScript verification
- **Issue:** `apps/web/test/types.d.ts` temporarily declares ZMLogoMark without `className`, so passing `className` from TopBar fails `tsc`.
- **Fix:** Kept ZMLogoMark's real `className` prop, but applied `text-accent` to a wrapper span in TopBar.
- **Files modified:** `apps/web/features/landing/components/TopBar.tsx`
- **Verification:** `pnpm --filter zeromail-web exec tsc --noEmit` passed.
- **Committed in:** `91fc35b`

**4. [Rule 3 - Blocking] Added Vitest next/font/google mock to theme-cookie spec**
- **Found during:** Plan 03 acceptance verification
- **Issue:** `theme-cookie.test.tsx` imported `app/layout.tsx`, but bare Vitest cannot execute compiler-handled `next/font/google` exports, causing `TypeError: Geist is not a function` before assertions.
- **Fix:** Added the same spec-local font mock used by `root-layout-fonts.test.tsx`.
- **Files modified:** `apps/web/test/theme-cookie.test.tsx`
- **Verification:** `pnpm --filter zeromail-web test -- --run test/theme-cookie.test.tsx` passed.
- **Committed in:** `422e986`

**Total deviations:** 4 auto-fixed (4 blocking/tooling)
**Impact on plan:** Source behavior remains aligned with the plan. Verification command adjustments reflect existing workspace scripts and cwd behavior.

## Deferred Issues

None.

## Known Stubs

None.

## Auth Gates

None.

## Threat Flags

None - the new Server Action and theme cookie surfaces were already covered by the plan threat model.

## User Setup Required

None.

## Next Phase Readiness

Plan 04 can consume the public layout shell and rely on the root layout's theme class. AuthTopBar work in later plans can reuse `ZMLogoMark` and the theme toggle pattern.

## Self-Check: PASSED

- Verified all created/modified plan files and planning artifacts exist.
- Verified task commits `fed16ce`, `3ddecc7`, `91fc35b`, and acceptance repair commit `422e986` exist in git history.

---
*Phase: 01.6-brand-identity-design-tokens-and-landing-page*
*Completed: 2026-04-28*
