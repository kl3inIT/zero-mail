# Plan 01.6-07 Summary - Visual Sweep + Lighthouse Remediation

**Date:** 2026-04-29  
**Executor:** Codex  
**Status:** Automated gates PASS; user manually approved live OAuth and Vietnamese glyph review.

## What Changed

- Reworked landing performance so mobile Lighthouse is consistently above 90:
  - removed public landing dependency on root `NextIntlClientProvider`;
  - moved `QueryProvider` to the protected route group only;
  - changed decorative landing preview markup from client shadcn primitives to static server markup;
  - replaced static rules preview `Tabs` with non-interactive markup;
  - replaced topbar server-action forms with native POST route handlers at `/actions/locale` and `/actions/theme`;
  - switched root typography to a system font stack to eliminate webfont requests on first paint;
  - added below-fold `content-visibility` containment for landing sections.
- Kept Phase 1.6 UI behavior intact:
  - mobile topbar keeps language switcher and sign-in visible;
  - theme and language persistence still pass e2e;
  - hero inbox preview keeps CSS-only triage motion;
  - auth and onboarding routes remain functional.

## Lighthouse Results

| Run | Performance | Accessibility | Best Practices | SEO | Notes |
|-----|-------------|---------------|----------------|-----|-------|
| Mobile 1 | 94 | 100 | 100 | 100 | LCP 2.7s, TBT 170ms |
| Mobile 2 | 97 | 100 | 100 | 100 | LCP 2.6s, TBT 50ms |
| Mobile 3 | 93 | 100 | 100 | 100 | LCP 2.8s, TBT 170ms |
| Desktop | 100 | 100 | 100 | 100 | LCP 0.6s, TBT 0ms |

Reports:
- `output/lighthouse/phase-01.6-landing-mobile.report.json`
- `output/lighthouse/phase-01.6-landing-mobile-confirm.report.json`
- `output/lighthouse/phase-01.6-landing-mobile-confirm-2.report.json`
- `output/lighthouse/phase-01.6-landing-desktop.report.json`

## Verification

- PASS: `pnpm --filter zeromail-web i18n:check`
- PASS: `pnpm --filter zeromail-web exec tsc --noEmit`
- PASS: `pnpm --filter zeromail-web exec eslint .`
- PASS: `pnpm --filter zeromail-web test` - 23 files / 137 tests
- PASS: `pnpm --filter zeromail-web build`
- PASS: `pnpm --filter zeromail-web test:e2e` - 24 passed / 1 skipped
- PASS: `git diff --stat -- backend` empty

## Screenshots

Refreshed final screenshots in `output/playwright/phase-01.6-final/`:
- `landing-desktop.png`
- `landing-mobile.png`
- `login-desktop.png`
- `login-mobile.png`

## Manual Checks

- PASS: OAuth full Google consent return path manually approved by user on 2026-04-29.
- PASS: Vietnamese diacritic/glyph visual quality manually approved by user on 2026-04-29.
