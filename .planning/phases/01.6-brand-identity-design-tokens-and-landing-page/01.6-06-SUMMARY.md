---
phase: 01.6-brand-identity-design-tokens-and-landing-page
plan: 06
subsystem: ui
tags: [nextjs, onboarding, auth, i18n, route-groups]

requires:
  - phase: 01.6-03
    provides: Public/auth chrome and theme cookie infrastructure
  - phase: 01.6-05
    provides: AuthTopBar children slot used by onboarding progress chrome
provides:
  - onboarding.steps.* EN/VI i18n namespace
  - RSC StepIndicator with desktop 3-pill row and mobile collapsed progress pill
  - Named onboarding routes for gmail-connect, template-select, and complete
  - Legacy /onboarding redirector for existing CTAs
affects: [onboarding, auth-chrome, i18n, frontend-tests]

tech-stack:
  added: []
  patterns:
    - Route page wrappers stay server components when they import AuthTopBar and StepIndicator
    - Per-step interactive logic lives in colocated client components

key-files:
  created:
    - apps/web/app/(protected)/onboarding/gmail-connect/page.tsx
    - apps/web/app/(protected)/onboarding/gmail-connect/GmailConnectClient.tsx
    - apps/web/app/(protected)/onboarding/template-select/page.tsx
    - apps/web/app/(protected)/onboarding/template-select/TemplateSelectClient.tsx
    - apps/web/app/(protected)/onboarding/complete/page.tsx
    - apps/web/app/(protected)/onboarding/complete/CompleteClient.tsx
  modified:
    - apps/web/features/auth/components/StepIndicator.tsx
    - apps/web/app/(protected)/onboarding/page.tsx
    - apps/web/i18n/messages/en.json
    - apps/web/i18n/messages/vi.json
    - apps/web/__tests__/pages/login-error-rendering.test.tsx
    - apps/web/__tests__/pages/onboarding-complete-redirect.test.tsx

key-decisions:
  - "Keep route pages as server wrappers because AuthTopBar and StepIndicator both rely on server-side next-intl APIs."
  - "Keep the legacy /onboarding route as a redirector to the next actionable state, preserving existing Continue Setup CTAs."
  - "Repair stale page tests in the same route-split task because they asserted the old single-page composition."

patterns-established:
  - "Server route wrappers compose AuthTopBar and StepIndicator; colocated *Client.tsx files own hooks and router.replace guards."
  - "StepIndicator completed pills are links; active and future pills remain spans."

requirements-completed: [1.6-6, 1.6-9]

duration: 35min
completed: 2026-04-28
---

# Phase 01.6 Plan 06: Onboarding Route Split Summary

Named onboarding routes now reuse the branded auth chrome and server-rendered step progress while preserving the Phase 1.5 onboarding state machine hooks and DTO boundaries.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 6.1 | Add onboarding.steps.* EN/VI i18n keys | `f205916` | 2 modified |
| 6.2 | Add StepIndicator | `6af10ab` | 1 created |
| 6.3 | Split onboarding into named routes | `c5b8e65` | 6 created, 3 modified, 2 test repairs |

## Accomplishments

- Added mirrored `onboarding.steps.*` labels and progress copy to EN and VI bundles.
- Added `StepIndicator` with desktop active/completed/future pills, mobile single active pill, backward links for completed steps, and token-only styling.
- Split `/onboarding` into `/onboarding/gmail-connect`, `/onboarding/template-select`, and `/onboarding/complete`.
- Replaced the old single onboarding page with a thin redirector that keeps existing `/onboarding` CTAs valid.
- Updated stale page tests for the new auth chrome and split completion route.

## Phase 1.5 Invariants Preserved

- `useCurrentUser` hook implementation unchanged.
- `useSelectTemplate` hook implementation unchanged.
- `useCompleteOnboarding` hook implementation unchanged.
- `TemplateCard` implementation unchanged and continues to own selected-state styling.
- OpenAPI DTOs and backend code unchanged.
- Onboarding state remains server-authoritative through `me.data.onboardingStep`.
- Completion still calls `useCompleteOnboarding().mutate(..., { onSuccess: router.replace('/settings') })`.

## Verification

- `pnpm --filter zeromail-web test -- --run test/onboarding-step-indicator.test.tsx` - PASS.
- `pnpm --filter zeromail-web test -- --run __tests__/pages/login-error-rendering.test.tsx` - PASS, 4 tests.
- `pnpm --filter zeromail-web test -- --run __tests__/pages/onboarding-complete-redirect.test.tsx` - PASS, 2 tests.
- `pnpm --filter zeromail-web test` - PASS, 23 files / 137 tests.
- `pnpm --filter zeromail-web exec tsc --noEmit` - PASS.
- `pnpm --filter zeromail-web i18n:check` - PASS, 161 leaf keys.
- `pnpm --filter zeromail-web build` - PASS; routes include `/onboarding`, `/onboarding/gmail-connect`, `/onboarding/template-select`, and `/onboarding/complete`.
- `git diff --stat -- apps/web/features/onboarding/components/TemplateCard.tsx` returned no output.
- `git diff --stat -- apps/web/features/onboarding/hooks` returned no output.
- `git diff --stat -- backend` returned no output.
- Raw-color/lucide scan over `StepIndicator.tsx` returned no matches.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking Build] Split route pages into server wrappers plus client components**
- **Found during:** Task 6.3 verification
- **Issue:** The plan's first draft made the named route pages client components while importing server-only `AuthTopBar` and async `StepIndicator`.
- **Fix:** Kept each route `page.tsx` as an RSC wrapper and moved hook/router logic into colocated `GmailConnectClient`, `TemplateSelectClient`, and `CompleteClient`.
- **Files modified:** `apps/web/app/(protected)/onboarding/*/page.tsx`, `apps/web/app/(protected)/onboarding/*/*Client.tsx`
- **Verification:** `pnpm --filter zeromail-web exec tsc --noEmit` and `pnpm --filter zeromail-web build` passed.
- **Committed in:** `c5b8e65`

**2. [Rule 3 - Blocking Verification] Updated stale tests after route split**
- **Found during:** Task 6.3 full test run
- **Issue:** `login-error-rendering.test.tsx` did not mock the new auth chrome components, and `onboarding-complete-redirect.test.tsx` still expected the completion CTA on the old single `/onboarding` page.
- **Fix:** Mocked `AuthTopBar`, `TrustPanel`, and `LegalFooter` in the login spec; moved completion CTA coverage to `CompleteClient` while keeping `/onboarding` complete-state redirect coverage.
- **Files modified:** `apps/web/__tests__/pages/login-error-rendering.test.tsx`, `apps/web/__tests__/pages/onboarding-complete-redirect.test.tsx`
- **Verification:** Targeted specs and full `pnpm --filter zeromail-web test` passed.
- **Committed in:** `c5b8e65`

**Total deviations:** 2 auto-fixed blocking verification issues.
**Impact:** No backend or API scope change; fixes were necessary to make the planned Next.js composition buildable and keep existing behavioral coverage accurate.

## Issues Encountered

None beyond the auto-fixed build/test issues above.

## User Setup Required

None.

## Next Phase Readiness

Plan 07 can visually sweep the public, auth, onboarding, settings, and legal surfaces with the named onboarding routes in place. The automated checks are green; remaining work is the planned human/visual verification artifact and Lighthouse pass.

## Self-Check: PASSED

- Summary file exists.
- Task commits exist in git history: `f205916`, `6af10ab`, `c5b8e65`.
- Created route files exist under all three named onboarding directories.
- Phase 1.5 invariants checklist complete.
- No tracked backend changes.
