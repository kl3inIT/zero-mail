---
phase: 08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat
plan: 08.1-02
type: execute
wave: 1
depends_on:
  - 08.1-01
files_modified:
  - backend/core/src/main/resources/db/changelog/changes/
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  - backend/core/src/main/java/com/zeromail/core/rules/catalog/
  - backend/api/src/main/java/com/zeromail/api/controllers/rules/RuleCatalogController.java
  - backend/api/src/main/java/com/zeromail/api/controllers/admin/AdminRuleCatalogController.java
  - backend/api/src/main/java/com/zeromail/api/dto/rules/catalog/
  - backend/api/src/main/java/com/zeromail/api/dto/admin/rulecatalog/
  - backend/core/src/test/java/com/zeromail/core/rules/catalog/
  - backend/api/src/test/java/com/zeromail/api/controllers/rules/
  - backend/api/src/test/java/com/zeromail/api/controllers/admin/
  - apps/web/lib/api/schema.d.ts
  - apps/admin/src/lib/api/admin-schema.d.ts
autonomous: true
requirements:
  - RACT-02
  - RACT-03
  - RACT-04
  - RACT-05
  - RACT-12
must_haves:
  truths:
    - "D-04 persona keys are seeded exactly: Founder, Influencer, Realtor, Investor, Assistant, Developer, Designer, Sales, Marketer, Support, Recruiter, Student, Outreach, Other."
    - "D-05/D-22 are authoritative: examples/personas/action descriptors are DB-backed, not hard-coded frontend data."
    - "D-06 is authoritative: English seed is copied from Inbox Zero and Vietnamese translations are seeded now."
    - "D-07 is authoritative: admin can edit key/name/icon/order/enabled and prompt EN/VI/order/enabled metadata."
    - "D-08 is authoritative: user API serves prompt by locale with `prompt_vi` fallback to `prompt_en`."
    - "D-09 is authoritative: user settings schema includes one global `Auto-send rules` setting default ON."
  artifacts:
    - path: "backend/core/src/main/resources/db/changelog/changes/08x-rule-action-catalog.yaml"
      provides: "Tables for personas, examples, action descriptors, and global auto-send setting default."
    - path: "backend/api/src/main/java/com/zeromail/api/controllers/rules/RuleCatalogController.java"
      provides: "Tenant/user read API for enabled personas/examples/action descriptors."
    - path: "backend/api/src/main/java/com/zeromail/api/controllers/admin/AdminRuleCatalogController.java"
      provides: "Admin CRUD API for personas/examples/action descriptors."
  key_links:
    - from: "inbox-zero-examples.ts"
      to: "Liquibase seed"
      via: "stable persona key + display order + prompt_en + prompt_vi"
      pattern: "Founder|Influencer|Realtor|Investor|Assistant|Developer|Designer|Sales|Marketer|Support|Recruiter|Student|Outreach|Other"
---

<objective>
Create the persistent catalog foundation for Phase 08.1: bilingual examples/personas, action descriptors, and the single global auto-send setting. Expose typed user read APIs and admin CRUD APIs so UI work can consume generated OpenAPI types instead of hard-coded mirrors.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONVENTIONS.md
@TESTING.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-CONTEXT.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-RESEARCH.md
@.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/inbox-zero-examples.ts
@apps/admin/src/features/catalog/catalog-api.ts
@backend/core/src/main/java/com/zeromail/core/admin/audit/usecases/AdminAuditWriter.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 08.1-02-01: Liquibase schema and bilingual seed data</name>
  <files>
    backend/core/src/main/resources/db/changelog/changes/08x-rule-personas-examples-actions.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  </files>
  <behavior>
    - Add `rule_example_persona` with stable key, display name EN/VI, optional icon, display_order, enabled, created/updated timestamps.
    - Add `rule_example_prompt` with persona FK, prompt_en, prompt_vi, display_order, enabled, source_ref, timestamps.
    - Add `rule_action_descriptor` with action key, label EN/VI, description EN/VI, risk level, availability status, display_order, enabled.
    - Add tenant/user setting column or table row for global `auto_send_rules_enabled` default TRUE.
    - Seed all required personas and examples from `inbox-zero-examples.ts`; English copied verbatim, Vietnamese translated in seed.
  </behavior>
  <action>
    Add a new numbered Liquibase changelog in the next available Phase 08.1 range and include it from `db.changelog-master.yaml`. Transcribe the Inbox Zero English prompts from the copied seed artifact into data inserts and add Vietnamese translations in the same rows. Add constraints/unique keys for persona key, example ordering, and action key so repeated migrations stay deterministic.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*Rule*Catalog*" && ./gradlew :backend:core:liquibaseUpdate -Pdb=local</automated>
  </verify>
  <done>
    Local DB receives personas, examples, descriptors, and default auto-send setting without manual SQL.
  </done>
  <acceptance_criteria>
    - All 14 persona keys exist and are enabled by default.
    - Every seeded example has nonblank `prompt_en` and `prompt_vi`.
    - Disabled examples/personas are representable without delete.
    - Action descriptors include `label`, `archive`, `save_draft`, `mark_read`, `star`, `add_to_digest`, `mark_spam`, `send_reply`, `forward_email`, `send_email`.
    - Global auto-send rules default is TRUE for new tenants/users.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-02-02: Core catalog services and admin audit integration</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/rules/catalog/domain/,
    backend/core/src/main/java/com/zeromail/core/rules/catalog/projection/,
    backend/core/src/main/java/com/zeromail/core/rules/catalog/persistence/,
    backend/core/src/main/java/com/zeromail/core/rules/catalog/usecases/
  </files>
  <behavior>
    - Implement read service for enabled user catalog by locale.
    - Implement admin service for create/edit/disable/reorder/localize personas/examples/action descriptors.
    - Admin mutations write `admin_audit_event` through existing audit writer.
    - Service returns DTO/projection data only; no tenant email content involved.
  </behavior>
  <action>
    Implement a `rules.catalog` core package following existing domain/usecases/projection/persistence conventions. Keep entity/repository types internal, expose use cases through the named API surface from plan 01, and route every admin mutation through `AdminAuditWriter` in the same transaction as the catalog change.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "com.zeromail.core.rules.catalog.*"</automated>
  </verify>
  <done>
    Core catalog services enforce enabled/order/locale rules and audit admin mutations.
  </done>
  <acceptance_criteria>
    - User read service excludes disabled personas and disabled examples.
    - Locale `vi` returns `prompt_vi`; missing `prompt_vi` falls back to `prompt_en`.
    - Reorder operation persists deterministic display order.
    - Admin create/edit/disable operations append audit rows.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 08.1-02-03: User and admin REST APIs with OpenAPI codegen</name>
  <files>
    backend/api/src/main/java/com/zeromail/api/controllers/rules/RuleCatalogController.java,
    backend/api/src/main/java/com/zeromail/api/controllers/admin/AdminRuleCatalogController.java,
    backend/api/src/main/java/com/zeromail/api/dto/rules/catalog/,
    backend/api/src/main/java/com/zeromail/api/dto/admin/rulecatalog/,
    apps/web/lib/api/schema.d.ts,
    apps/admin/src/lib/api/admin-schema.d.ts
  </files>
  <behavior>
    - Add `GET /api/rules/catalog/examples?locale=vi|en`.
    - Add `GET /api/rules/catalog/actions?locale=vi|en`.
    - Add admin endpoints under `/api/admin/rule-catalog/**` for persona/example/action descriptor CRUD and reorder.
    - DTO records publish accurate OpenAPI schema with required properties.
    - Regenerate public and admin TypeScript schemas.
  </behavior>
  <action>
    Add thin API controllers and record DTOs that delegate to the core catalog services. Put user DTOs under `dto/rules/catalog` and admin DTOs under `dto/admin/rulecatalog`, then regenerate both OpenAPI clients so frontend plans consume typed endpoints. Preserve the Phase 8 public/admin OpenAPI split.
  </action>
  <verify>
    <automated>./gradlew :backend:api:test --tests "*RuleCatalog*" && pnpm --filter @zeromail/web generate:api && pnpm --filter @zeromail/admin generate:api</automated>
  </verify>
  <done>
    Frontend plans can import generated types for catalog APIs.
  </done>
  <acceptance_criteria>
    - User endpoint returns only enabled rows ordered by persona/action order.
    - Admin endpoints require admin auth and write audit entries for mutations.
    - OpenAPI generated schemas include the new paths and DTOs.
    - Public schema does not include admin paths; admin schema includes only admin paths per Phase 8 split.
  </acceptance_criteria>
</task>

</tasks>

<verification>

```powershell
./gradlew :backend:core:test --tests "*Rule*Catalog*"
./gradlew :backend:api:test --tests "*RuleCatalog*"
./gradlew :backend:core:liquibaseUpdate -Pdb=local
pnpm --filter @zeromail/web generate:api
pnpm --filter @zeromail/admin generate:api
```

</verification>

<success_criteria>
- [ ] DB schema supports personas/examples/action descriptors with EN/VI/enabled/order fields.
- [ ] Liquibase seed includes Inbox Zero English and Vietnamese translations.
- [ ] User read API filters disabled catalog rows.
- [ ] Admin CRUD API can localize, disable, and reorder.
- [ ] Admin mutations are audited.
- [ ] Generated web/admin OpenAPI schemas include the new endpoints.
</success_criteria>

<output>
Create `.planning/phases/08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat/08.1-02-SUMMARY.md` when done.
</output>
