# Phase 08.1: inbox-zero-style-rule-actions-and-admin-managed-examples-cat - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-23
**Phase:** 08.1-inbox-zero-style-rule-actions-and-admin-managed-examples-cat
**Areas discussed:** Rule creation UX flow, Admin-managed examples catalog, Outbound opt-in UX, Gate failure behavior, Send boundary enforcement

---

## Folded Todos

| Option | Description | Selected |
|--------|-------------|----------|
| `@NamedInterface` backend/core API surfaces | Boundary/API surface todo relevant to outbound gateway and module boundaries | ✓ |
| Draft privacy sweep fail | Privacy leak warning around sanitized subject/audit data | |
| SecurityConfig test slice | OAuth/security test-profile cleanup | |
| Rules UX structured builder | Existing structured builder UX todo | |
| DraftSafetyEvalTest compile fail | Test maintenance issue | |
| TriageAuditControllerContractTest mismatch | Test maintenance issue | |
| Landing SSR 500 | Unrelated frontend bug | |
| Optional real Gmail VPS smoke | Optional smoke gate | |

**User's choice:** "chỉ 1 thôi"
**Notes:** Only the `@NamedInterface`/API surface todo is folded. Other matches are reviewed but not scoped into Phase 08.1.

---

## Rule Creation UX Flow

| Option | Description | Selected |
|--------|-------------|----------|
| Keep current composer + add Inbox Zero-style examples | Existing prompt box remains; choose persona, list examples below prompt box | ✓ |
| Upgrade current inline suggestions only | Keep current suggestions area without persona flow | |
| New launcher/wizard/page | Larger replacement flow | |

**User's choice:** Keep current prompt box; "làm giống inbox zero ấy chọn personal xong liệt kê dưới cái box ấy".
**Notes:** User corrected that Zero Mail already has the prompt box. The missing piece is `Choose from examples` and persona-filtered example listing under the box.

| Option | Description | Selected |
|--------|-------------|----------|
| Replace all current text | Selected example replaces textarea content | ✓ |
| Append to current text | Selected example appends to existing textarea content | |
| Ask replace/append when text exists | Adds a confirmation branch | |

**User's choice:** "1 đi ok chốt"
**Notes:** Click example replaces the current prompt box content.

---

## Admin-managed Examples Catalog

| Option | Description | Selected |
|--------|-------------|----------|
| Full admin UI | Complete catalog management UI in `apps/admin` | |
| Seed DB + admin API + minimal admin UI | Real DB/admin management without heavy CMS scope | ✓ |
| Static/DB seed only | Fast but does not satisfy admin-managed requirement | |

**User's choice:** `2`
**Notes:** Minimal admin UI is enough. Avoid over-building CMS features.

| Option | Description | Selected |
|--------|-------------|----------|
| English only | Copy Inbox Zero examples verbatim only | |
| English seed + optional VI field | Admin translates gradually | |
| English + translated VI seed | Seed both languages; admin edits both | ✓ |

**User's choice:** "dịch luôn cho tôi vào seed vào db nhé lock đi"
**Notes:** English stays verbatim from Inbox Zero. Vietnamese translation is seeded immediately. Examples come from DB, not frontend hard-code.

---

## Outbound Opt-in UX

| Option | Description | Selected |
|--------|-------------|----------|
| Account + per-action toggles default OFF | More gates, more friction | |
| Single global `Auto-send rules` toggle default ON | User-first automation posture | ✓ |
| No setting | Maximum automation, lowest control | |

**User's choice:** User rejected making them go to settings or use per-action toggles. They want one global auto-send toggle and default ON.
**Notes:** This overrides earlier RACT-05 wording that said default OFF and per-action toggles.

| Option | Description | Selected |
|--------|-------------|----------|
| Rule-level checkbox | User confirms each outbound rule | |
| Confirm modal | User confirms on save | |
| No per-rule acknowledgement | Global setting and clear UI copy are enough | ✓ |

**User's choice:** "đứng về phía user đi làm phiền user thế ?"
**Notes:** Per-rule acknowledgement is rejected as too much friction. UI may show clear copy/badges but must not block the flow.

---

## Gate Failure Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Save draft | Produce useful Gmail draft instead of sending | ✓ |
| Needs review in Zero Mail | Requires new review surface | |
| Skip and audit only | Safe but appears to do nothing | |

**User's choice:** `1`
**Notes:** If global auto-send is OFF or a runtime safety gate fails, fallback to Gmail draft.

---

## Send Boundary Enforcement

| Option | Description | Selected |
|--------|-------------|----------|
| Refactor to shared `OutboundSendGateway` | Domain-neutral gateway for chat and rules | ✓ |
| Keep `AssistantSendExecutor`, rules call adapter | Less rename, worse domain naming | |
| Create separate `RuleSendExecutor` | Faster locally, weaker boundary | |

**User's choice:** "vụ này bạn decide sao cho code tốt nhất là được"
**Notes:** Agent locked shared domain-neutral outbound gateway as the best code shape.

---

## Agent's Discretion

- Exact package/class names for outbound gateway, as long as they are domain-neutral and enforceable by ArchUnit.
- Minimal DB schema details for examples/personas/action descriptors.

## Deferred Ideas

- Full admin CMS for examples.
- Zero Mail `needs_review` inbox.
- Arbitrary webhook actions.
- Permanent delete automation.
