---
phase: 04-triage-convergence-hero
plan: 08
type: execute
wave: 7
depends_on: [00, 01, 02, 03, 04, 05, 06, 07]
files_modified:
  - backend/core/src/test/java/com/zeromail/core/triage/TriagePrivacySweepTest.java
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-triage-convergence-hero/04-VALIDATION.md
  - .planning/phases/04-triage-convergence-hero/04-UAT.md
autonomous: true
requirements: [TRG-01, TRG-02, TRG-03, TRG-04, TRG-05, TRG-06, TRG-07, TRG-08]

must_haves:
  truths:
    - "./gradlew clean check is GREEN across backend/{core,api,worker} — all new ArchUnit rules + integration tests + ApplicationModulesTest pass; FND-05 still passes"
    - "A triage privacy-sweep test (analogous to FND-03) confirms no raw email body / snippet / sender display name / prompt / completion appears in triage_audit, application logs, or Micrometer tags"
    - "All Wave-0 contract tests are GREEN (no remaining @Disabled placeholders that should now be enabled)"
    - "REQUIREMENTS.md TRG-01..TRG-08 are flipped to Complete with a Phase 4 traceability note"
    - "04-VALIDATION.md is filled in (Per-Task Verification Map, Wave-0 status, sign-off) with nyquist_compliant: true"
  artifacts:
    - path: "backend/core/src/test/java/com/zeromail/core/triage/TriagePrivacySweepTest.java"
      provides: "FND-03-analogous sweep: no body/snippet/display-name/prompt/completion in audit rows, logs, or metric tags"
      contains: "TriagePrivacySweepTest"
    - path: ".planning/phases/04-triage-convergence-hero/04-UAT.md"
      provides: "UAT scenarios for the 13 SPEC acceptance criteria + the 8 TRG requirements"
      contains: "UAT"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "TRG-01..TRG-08 = Complete (Phase 4)"
      via: "traceability table update"
      pattern: "TRG-0[1-8].*Complete"
    - from: ".planning/phases/04-triage-convergence-hero/04-VALIDATION.md"
      to: "nyquist_compliant: true"
      via: "frontmatter flip after the full suite is green"
      pattern: "nyquist_compliant: true"
---

<objective>
[Wave 7 — closure] Final verification + audit + traceability: the FND-03-analogous triage privacy-sweep test, a full `./gradlew clean check` green run, confirmation that every Wave-0 contract test is GREEN (no orphaned `@Disabled`), the REQUIREMENTS.md `TRG-01..TRG-08` flip to Complete, the `04-VALIDATION.md` fill-in (Per-Task Verification Map / Wave-0 status / sign-off / `nyquist_compliant: true`), and the `04-UAT.md` scenarios for the 13 SPEC acceptance criteria.

Purpose: SPEC §"Acceptance Criteria" final gate ("`./gradlew clean check` is GREEN; new ArchUnit rules + integration tests pass"; "No raw email body / snippet / display name / prompt / completion appears in `triage_audit`, application logs, or Micrometer tags (verified by sweep test analogous to FND-03)"; "Phase 1 FND-05 multi-tenant leak test still passes"); Nyquist Dimension 8 validation sign-off.
Output: 1 privacy-sweep test; REQUIREMENTS.md + 04-VALIDATION.md + 04-UAT.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-AI-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TriagePrivacySweepTest — FND-03-analogous content-bleed sweep</name>
  <files>backend/core/src/test/java/com/zeromail/core/triage/TriagePrivacySweepTest.java</files>
  <read_first>
    - the Phase 1 FND-03 log-scrub synthetic-traffic test (search `backend/` for the FND-03 sweep test — it runs synthetic traffic and greps the captured log output for body/prompt/completion content; mirror its structure: a Logback `ListAppender` (or the project's equivalent capture mechanism) attached during a synthetic triage run, then assertions that no captured line contains forbidden tokens)
    - backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java + ChatResponseToStringSafetyTest.java (the existing privacy/safety assertion idioms)
    - backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java (from 04-05 — every log line is `event=triage_* tenantId={} gmailMessageId={} ruleId={} actionType={} ...`; the sweep verifies none carries content) + TriageGmailWriter.java + SenderSafetyNetService.java + TriageUndoService.java (the other triage log sites)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java (the `reason` field carries matcher node id + structured evidence ids only; the sweep verifies a written audit row's `reason` / `action_args_json` / `gmail_change_token` carry no email-content-shaped strings)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §"Constraints" (privacy invariant) + §"Acceptance Criteria" ("No raw email body / snippet / display name / prompt / completion appears in `triage_audit`, application logs, or Micrometer tags (verified by sweep test analogous to FND-03)") + §req 8 (sender email is logged hashed/id-only) ; CLAUDE.md §Conventions #5 (privacy logging format)
    - .planning/phases/04-triage-convergence-hero/04-AI-SPEC.md §7 (Micrometer tag set — `tenant_id` hashed, `model_id`, `call_site`, etc.; the sweep should assert the triage metric tags carry no raw content / raw sender / raw tenant id if the convention is hashed)
  </read_first>
  <action>
    Create `com.zeromail.core.triage.TriagePrivacySweepTest` (a `@SpringBootTest` Testcontainers integration test, mirroring the FND-03 synthetic-traffic sweep): attach a Logback `ListAppender` to capture all triage-related log output; run a synthetic triage scenario through the orchestrator (a tenant with 1-2 rules, a seeded `MailMessageObserved` event, a fake/stubbed Gmail metadata response containing a recognizable sentinel string in the subject/snippet/sender-display-name AND a sentinel in the rule's draft instruction); after the orchestration completes, assert: (1) NO captured log line contains the subject/snippet/sender-display-name sentinel, the draft-body sentinel, or any prompt/completion fragment; (2) the written `triage_audit` row(s)' `reason` / `action_args_json` / `gmail_change_token` columns contain no sentinel-shaped content (the `action_args_json` may legitimately contain the `labelId` / `draftId` / the rule's static `instruction` — assert it does NOT contain the LLM-output sentinel or the email-body sentinel; if the rule's static `instruction` is itself the draft-body sentinel, that's allowed since it's the rule author's text, not email content — pick sentinels that distinguish "email content" from "rule-authored static instruction"); (3) the `triage.duration` / `triage.semantic_eval.*` Micrometer tags carry no raw email content, no raw sender address, and (if the project hashes tenant ids in metric tags) no raw tenant id. Also run a small assertion that `SenderSafetyNetService`'s log line for the opt-in / check carries a hashed-or-id-only sender, never the raw address. The test should fail loud (descriptive assertion message naming which sentinel leaked where) if any check fails.
  </action>
  <verify>
    <automated>test -f backend/core/src/test/java/com/zeromail/core/triage/TriagePrivacySweepTest.java && ./gradlew :backend:core:test --tests "*TriagePrivacySweepTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriagePrivacySweepTest.java` exists; it runs a synthetic triage scenario with recognizable sentinels in the email subject/snippet/sender-display-name and asserts no captured triage log line, no `triage_audit` `reason`/`action_args_json`/`gmail_change_token` value, and no `triage.*` Micrometer tag contains those sentinels (the rule's static draft `instruction` is exempted from the "no content" check since it's rule-authored, not email content).
    - It also asserts the sender-safety-net log line is hashed/id-only (no raw sender address).
    - `./gradlew :backend:core:test --tests "*TriagePrivacySweepTest"` PASSES.
  </acceptance_criteria>
  <done>The triage privacy-sweep test exists and is GREEN — no content bleed into audit rows, logs, or metric tags.</done>
</task>

<task type="auto">
  <name>Task 2: Full clean check + Wave-0 GREEN confirmation + REQUIREMENTS / VALIDATION / UAT updates</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/04-triage-convergence-hero/04-VALIDATION.md, .planning/phases/04-triage-convergence-hero/04-UAT.md</files>
  <read_first>
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §"Acceptance Criteria" (the 13 pass/fail checks — these become the UAT scenarios; the final gate item is "`./gradlew clean check` is GREEN; new ArchUnit rules + integration tests pass")
    - .planning/phases/04-triage-convergence-hero/04-RESEARCH.md §"Validation Architecture" → "Phase Requirements → Test Map" (the TRG-01..TRG-08 → test mapping — copy into 04-VALIDATION.md's Per-Task Verification Map; flip the "File Exists" column from ❌ Wave 0 to ✅ as the tests were created in 04-00 and enabled across 04-01..04-07)
    - .planning/phases/04-triage-convergence-hero/04-VALIDATION.md (the skeleton to fill: Test Infrastructure, Sampling Rate, Per-Task Verification Map, Wave 0 Requirements, Manual-Only Verifications, Validation Sign-Off, the `nyquist_compliant` + `wave_0_complete` frontmatter flags)
    - .planning/REQUIREMENTS.md (the `TRG-01..TRG-08` rows in the v1 Requirements list — currently `[ ]` / Pending — and the Traceability table rows — flip to `[x]` / Complete with a `Phase 4` note; mirror how prior phases' requirements were flipped, e.g. the BILL-01..BILL-07 flip in `02B-06`)
    - prior phases' `*-UAT.md` files (e.g. `.planning/phases/03-rules-engine/03-...-UAT.md` if one exists, or `.planning/phases/03-rules-engine/03-09-...`) for the UAT scenario format — if none exists, use a simple "Scenario / Steps / Expected / Status" table per SPEC acceptance criterion
    - prior phases' closure plans (e.g. `02B-06-PLAN.md`, `03-09-PLAN.md`) for the closure-plan shape (full `clean check` run, requirement-flip, validation sign-off)
    - the two manual-verification deferral precedents in STATE.md (Playwright e2e / live `generate:api` — note any Phase 4 manual gates: there are none since Phase 4 is backend+REST only and the contract tests cover the REST surface; if the `springdoc-openapi` regen / `schema.d.ts` from 04-06 was deferred, record it as a manual gate with a replay command)
  </read_first>
  <action>
    Run `./gradlew clean check` across `backend/{core,api,worker}` and confirm BUILD SUCCESSFUL — all new ArchUnit rules (`NoGmailSendAllowedTest`, `TriageGmailWriteBoundaryTest`, `TriageAuditRepositoryBoundaryArchTest`, `CallSiteEnumMembershipArchTest` at 5 members), all triage integration/unit/contract tests (orchestrator, idempotency, shadow-mode, credit-accounting, audit-persistence, undo-service, undo-controller, tenant-controller, sender-net-controller, purge-job, privacy-sweep, `MailMessageObserved`), `ApplicationModulesTest` (the new `core.triage` module verifies), and FND-05 `MultiTenantLeakIntegrationTest` all PASS. If anything is RED, FIX it within this plan's scope (or, if the fix belongs in an earlier plan's domain, surface it precisely in the SUMMARY — do not paper over a real failure). Confirm there are no orphaned `@Disabled("Wave 0 ...")` annotations that should now be enabled (grep the `com.zeromail.core.triage` / `com.zeromail.worker.triage` / `com.zeromail.api.controllers.triage` test trees; any still-`@Disabled` test must have an explicit reason that is NOT "Wave 0").
    Update `.planning/REQUIREMENTS.md`: flip `TRG-01`..`TRG-08` from `[ ]` to `[x]` in the v1 Requirements list and from `Pending` to `Complete` in the Traceability table; bump the coverage/last-updated note.
    Fill in `.planning/phases/04-triage-convergence-hero/04-VALIDATION.md`: Test Infrastructure (JUnit 5 + Spring Boot Test + Testcontainers `postgresql` + ArchUnit + Spring Modulith test slice; per-module quick command `./gradlew :backend:<module>:test`; full suite `./gradlew clean check`); Sampling Rate (after every task commit: per-module quick run; after every plan wave: full suite; before verify-work: full suite green); the Per-Task Verification Map populated from RESEARCH §"Validation Architecture" → "Phase Requirements → Test Map" (TRG-01..TRG-08 rows with their test types + automated commands + the now-✅ File-Exists status + the now-green Status); Wave 0 Requirements (all the 04-00 scaffolds — mark complete); Manual-Only Verifications (none, unless the `schema.d.ts` regen was deferred — then list it with the replay command); Validation Sign-Off checklist all ticked; flip `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter; set `status: approved` with today's date.
    Create `.planning/phases/04-triage-convergence-hero/04-UAT.md`: a "Scenario / Steps / Expected / Status" table with one row per SPEC §"Acceptance Criteria" item (13 rows) — each scenario maps to the automated test(s) that verify it (cite the test name + command); mark each Status as ✅ (covered by the green automated suite). Add a short header noting Phase 4 is backend+REST only and all 13 acceptance criteria have automated coverage (no manual UAT required for Phase 4).
  </action>
  <verify>
    <automated>./gradlew clean check 2>&1 | tail -5 && grep -q "TRG-01.*Complete\|TRG-01.*\\[x\\]" .planning/REQUIREMENTS.md && grep -q "nyquist_compliant: true" .planning/phases/04-triage-convergence-hero/04-VALIDATION.md && test -f .planning/phases/04-triage-convergence-hero/04-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew clean check` is BUILD SUCCESSFUL across all backend modules; the new ArchUnit rules, all triage integration/unit/contract tests, `ApplicationModulesTest`, and FND-05 all PASS.
    - No orphaned `@Disabled("Wave 0 ...")` test remains in the triage test trees (any still-disabled test has an explicit non-Wave-0 reason).
    - `.planning/REQUIREMENTS.md` shows `TRG-01..TRG-08` as `[x]` / Complete with a Phase 4 traceability note; the coverage note is bumped.
    - `.planning/phases/04-triage-convergence-hero/04-VALIDATION.md` has the Per-Task Verification Map populated (TRG-01..TRG-08, test types, automated commands, ✅ statuses), all sign-off checkboxes ticked, `nyquist_compliant: true` + `wave_0_complete: true` + `status: approved` in the frontmatter.
    - `.planning/phases/04-triage-convergence-hero/04-UAT.md` exists with 13 scenario rows (one per SPEC acceptance criterion), each mapped to its automated test and marked ✅.
  </acceptance_criteria>
  <done>The full backend suite is green; TRG-01..TRG-08 are Complete; the validation strategy is signed off; the UAT scenarios are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit rows / logs / metrics → privacy invariant | The closure phase's job is to PROVE the boundary held — no email content / prompt / completion / raw sender leaked anywhere a reviewer can grep. |
| green build → ship gate | `./gradlew clean check` GREEN (incl. all new ArchUnit guards + FND-05) is the SPEC's final acceptance gate; nothing ships RED. |
| Wave-0 contract → execution truth | Every RED-by-design scaffold from 04-00 must end GREEN; an orphaned `@Disabled` would hide an unmet contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Information Disclosure | content bleed into audit/logs/metrics | mitigate | `TriagePrivacySweepTest` (FND-03-analogous) runs synthetic triage with sentinels in email subject/snippet/sender-name/draft-body and asserts none appears in any triage log line, `triage_audit` `reason`/`action_args_json`/`gmail_change_token`, or `triage.*` Micrometer tag; the sender-net log is asserted hashed/id-only. |
| T-04-08-02 | Repudiation | unmet Wave-0 contract hidden by @Disabled | mitigate | The closure task greps the triage test trees for orphaned `@Disabled("Wave 0 ...")` and fails the plan if any remain without an explicit non-Wave-0 reason; the full `clean check` must execute every contract test green. |
| T-04-08-03 | Tampering | tenant leak regression | mitigate | FND-05 `MultiTenantLeakIntegrationTest` is re-run as part of `clean check`; the closure plan fails if it's RED — the Phase 4 additions (native `triage_audit` queries, the `@Async` listener) must not have introduced a leak. |
| T-04-08-04 | Elevation of Privilege | auto-send / Gmail-write boundary regression | mitigate | `NoGmailSendAllowedTest` (no `send` call site anywhere; `RuleActionType.SEND` absent) + `TriageGmailWriteBoundaryTest` (only `TriageGmailWriter` calls Gmail write APIs) are re-run as part of `clean check` — the closure plan fails if either is RED. |
</threat_model>

<verification>
- `./gradlew clean check` BUILD SUCCESSFUL across `backend/{core,api,worker}`.
- `./gradlew :backend:core:test --tests "*TriagePrivacySweepTest" --tests "*MultiTenantLeakIntegrationTest" --tests "*NoGmailSendAllowedTest" --tests "*TriageGmailWriteBoundaryTest" --tests "*ApplicationModulesTest"` all PASS.
- `.planning/REQUIREMENTS.md` TRG-01..TRG-08 = Complete; `04-VALIDATION.md` `nyquist_compliant: true`; `04-UAT.md` exists with 13 scenarios.
</verification>

<success_criteria>
- The triage privacy-sweep test exists and is GREEN; no content bleed.
- `./gradlew clean check` is GREEN; all new ArchUnit rules + integration tests + `ApplicationModulesTest` + FND-05 pass; no orphaned `@Disabled` Wave-0 tests.
- `TRG-01..TRG-08` flipped to Complete in REQUIREMENTS.md; `04-VALIDATION.md` filled in + signed off (`nyquist_compliant: true`); `04-UAT.md` records the 13 acceptance scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-08-SUMMARY.md`
</output>
