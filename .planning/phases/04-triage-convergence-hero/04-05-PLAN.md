---
phase: 04-triage-convergence-hero
plan: 05
type: execute
wave: 5
depends_on: [00, 01, 02, 03, 04]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java
  - backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java
  - backend/core/src/main/java/com/zeromail/core/triage/application/TriageRuleEvaluationInputFactory.java
  - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageOrchestratorAdapter.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/package-info.java
  - backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageOrchestratorIntegrationContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageIdempotencyContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageShadowModeContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageCreditAccountingContractTest.java
autonomous: true
requirements: [TRG-01, TRG-02, TRG-03, TRG-04, TRG-05, TRG-08]

must_haves:
  truths:
    - "On a MailMessageObserved event, the orchestrator rebinds TenantContext, returns early if triage_paused, builds a metadata-only RuleEvaluationInput, loads enabled rules in display_order, evaluates matchers (resolving SEMANTIC_INTENT inline via LlmGateway), merges proposals, gates each through TriageSafetyPolicy, checks the sender safety net, and either applies via TriageGmailWriter or writes a SHADOW_LOGGED / REJECTED_* audit row"
    - "Per applied proposal the audit saga uses THREE separate transaction phases: (1) a committed REQUIRES_NEW tx that reserves+leases the PENDING row (insertAuditPendingIfAbsent, or reclaimStalePending on an existing stale PENDING row); (2) the TriageGmailWriter call OUTSIDE any DB transaction; (3) a separate committed REQUIRES_NEW tx that markApplied(+gmailChangeToken)/markFailed — so a crash after the Gmail call but before commit cannot duplicate a draft on retry, and a crash mid-write leaves a reclaimable PENDING row, not a skip-forever one"
    - "Replaying the same (tenantId, gmailMessageId) produces exactly one audit row per action and at most one Gmail write per action; a crashed-mid-write PENDING row is reclaimed and the write completed on the next delivery, not skipped"
    - "When the tenant is in shadow mode OR the sender is protected OR the safety policy rejects a proposal, the audit row is inserted DIRECTLY in its terminal state (SHADOW_LOGGED / REJECTED_BY_SAFETY_NET / REJECTED_BY_SAFETY_POLICY) via TriageAuditWriter.insertTerminal with gmail_change_token=null — no wasted PENDING insert + UPDATE; a protected sender suppresses ALL actions (label/archive/save-draft), per SPEC req 8 point b"
    - "LLM failure marks all semantic nodes DEFERRED-(error) (treated as NOT_MATCHED); deterministic-only actions still fire; over-budget triggers per-rule fanout; credit accounting is 1 reserve per LLM call + 1 per pure-deterministic message"
    - "The string passed to LlmGateway.evaluateSemanticIntents is the v1 'semanticEvalContent' — sanitizedSubjectExcerpt + a deterministic content-free flag summary (no body/snippet/raw-headers); the metadata-only fetch carries no body, so none is sent; the v1-limitation note appears on the field declaration, the builder method, AND the class Javadoc, plus a 'do NOT add body/snippet/raw-header fetch here' code comment"
    - "Exactly ONE class carries @ApplicationModuleListener for MailMessageObserved (TriageOrchestratorService, unless the worker AOT path forces TriageOrchestratorAdapter — then ONLY the adapter has it and the service does not); no double-handling"
    - "Listener exceptions are captured by Modulith into FailedEventPublications, not swallowed"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java"
      provides: "@ApplicationModuleListener consumer of MailMessageObserved; the hero orchestration (rules -> LLM -> safety -> sender net -> delegates each proposal to TriageAuditSaga)"
      contains: "ApplicationModuleListener"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java"
      provides: "The 3-phase audit mini-saga: reserve/lease PENDING (committed tx) -> Gmail call (no tx) -> markApplied/markFailed (committed tx); plus the direct-terminal insert for shadow/rejected"
      contains: "REQUIRES_NEW"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageRuleEvaluationInputFactory.java"
      provides: "Metadata-only Gmail fetch -> RuleEvaluationInput facade for triage"
      contains: "RuleEvaluationInput"
  key_links:
    - from: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java"
      to: "MailMessageObserved (Spring Modulith JDBC registry)"
      via: "@ApplicationModuleListener (single location) + TenantContext.runWith rebind"
      pattern: "ApplicationModuleListener"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java"
      to: "TriageAuditWriter.insertPending/insertTerminal + TriageAuditRepository.reclaimStalePending/markApplied/markFailed"
      via: "phase 1 = committed REQUIRES_NEW reserve+lease; phase 2 = Gmail call (no DB tx); phase 3 = committed REQUIRES_NEW finalize; shadow/rejected = insertTerminal"
      pattern: "REQUIRES_NEW"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java"
      to: "LlmGateway.evaluateSemanticIntents"
      via: "resolve DEFERRED semantic nodes; passes the v1 semanticEvalContent string (subject excerpt + content-free flag summary); on TokenBudgetExceededException fan out per-rule; on failure mark DEFERRED-(error)"
      pattern: "evaluateSemanticIntents"
---

<objective>
[Wave 5] Land the hero: `TriageOrchestratorService` — the `@ApplicationModuleListener` consumer of `MailMessageObserved` (in `core.triage.application`, consumed by `backend/worker`) that runs the full triage flow per the SPEC §req 1 + RESEARCH §"System Architecture Diagram": tenant rebind → `triage_paused` early-return → metadata-only `RuleEvaluationInput` → enabled rules in `display_order` → tri-state `RuleEvaluator` → inline `SEMANTIC_INTENT` resolution via `LlmGateway.evaluateSemanticIntents` (per-message batched default, per-rule fanout on `TokenBudgetExceededException`, `DEFERRED-(error)` → `NOT_MATCHED` on failure) → `ActionProposalMerger` → per-proposal `TriageSafetyPolicy` gate → `SenderSafetyNetService` check → delegate each proposal to the `TriageAuditSaga` (the 3-phase mini-saga: committed REQUIRES_NEW reserve+lease PENDING → Gmail call OUTSIDE any DB tx → committed REQUIRES_NEW `markApplied(+gmailChangeToken)`/`markFailed`; OR a direct terminal insert via `TriageAuditWriter.insertTerminal` for shadow-mode / sender-protected / safety-policy-rejected proposals) → credit accounting (1 reserve per LLM call / 1 per pure-deterministic message). Plus the `TriageAuditSaga` itself, the `TriageRuleEvaluationInputFactory` (metadata-only Gmail fetch facade in `core.gmail`/`core.triage.application`), the optional `worker.triage` adapter (if AOT needs it), the `worker.triage` package-info, and verification that the orchestrator integration / idempotency / shadow-mode / credit-accounting Wave-0 tests go GREEN.

Purpose: per SPEC §req 1/2/3/4/5/8 (incl. req 8 point b "skip Gmail writes" — a protected sender gates ALL actions), D-A4/D-C3/D-C5/D-D1..D-D6, RESEARCH Pitfalls 1/3/4/6/7/8, CONTEXT Open Question 2 (package placement), and the consensus review findings #1 (orchestrator needs the SHADOW_LOGGED / REJECTED_* transitions + `gmail_change_token` post-write set — landed via `TriageAuditWriter`/the extended repo from 04-02 and the direct-terminal-insert path here), #2 (two-phase transaction boundaries + lease/reclaim — the `TriageAuditSaga`), Codex MEDIUM "possible duplicate listeners" (exactly one `@ApplicationModuleListener` location), and OpenCode suggestion C (the `semanticEvalContent` v1-limitation Javadoc lock-in in three places + the do-not-add-body code comment).
Output: 1 orchestrator service + 1 audit saga + 1 input factory + 1 modified `GmailPreviewReadService` (expose the triage facade) + 1 optional worker adapter + 1 worker package-info; 4 Wave-0 worker tests verified GREEN; `NoGmailSendAllowedTest` re-verified GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/phases/04-triage-convergence-hero/04-AI-SPEC.md
@.planning/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TriageRuleEvaluationInputFactory — metadata-only Gmail fetch → RuleEvaluationInput facade</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/application/TriageRuleEvaluationInputFactory.java, backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java (the existing metadata-only fetch: `messageGetRequest` `format=metadata` + `fields=id,threadId,labelIds,internalDate,payload/headers`, `toPreviewMessage` header parsing → sanitized sender/domain/recipients/subject-excerpt/labels/categories/`hasAttachment`/`listUnsubscribePresent`/`newsletterIndicatorPresent`; `Clock` injection; `GoogleJsonResponseException` handling — expose a triage-friendly facade `fetchTriageInput(UUID tenantId, String gmailMessageId)` returning the same shape the rules `RuleEvaluator` consumes)
    - backend/core/src/main/java/com/zeromail/core/rules/service/RuleEvaluator.java + the `RuleEvaluationInput` type (what fields `RuleEvaluator.evaluate(...)` reads — the factory must produce exactly that; note `RuleEvaluationInput` carries `sanitizedSubjectExcerpt` + derived booleans/flags but NO email body — that is by design, SPEC §req 1). CHECK whether `RuleEvaluationInput` already exposes the sanitized FULL sender email address (not just `sanitizedSenderDomain`) — the orchestrator (04-05 Task 3) needs the full sanitized sender address for the `SenderSafetyNetService.isProtected(tenantId, senderEmail)` check; `GmailPreviewReadService.toPreviewMessage` already parses the sanitized sender, so if `RuleEvaluationInput` does not carry it, return it alongside (e.g. on a small `TriageRuleEvaluationInput` wrapper record `(RuleEvaluationInput evaluationInput, String sanitizedSenderEmail)`) — do NOT widen `RuleEvaluationInput` itself (that's a Phase-3 type; keep its contract).
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java (the `buildClientForTenant(...)` facade from 04-04 — the input factory uses it; no cipher reference from `core.triage`)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 1 (the metadata-only fetch shape `format=metadata, fields=id,threadId,labelIds,internalDate,payload/headers` — never bodies/snippets)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md "Reusable Assets" — `GmailPreviewReadService` "lift the fetch helper or expose a triage-friendly facade"; CLAUDE.md §Conventions #5 (privacy logging)
  </read_first>
  <action>
    Decide the cleanest placement: either (a) add `public TriageRuleEvaluationInput fetchTriageInput(UUID tenantId, String gmailMessageId)` directly to `GmailPreviewReadService` (it already owns the metadata-fetch + header-parse logic — recommended; minimal new surface) OR (b) create `com.zeromail.core.triage.application.TriageRuleEvaluationInputFactory` that injects `GmailPreviewReadService` + `GmailApiClientFactory` and composes the fetch. Either way: the fetch is `format=metadata` with `fields=id,threadId,labelIds,internalDate,payload/headers` ONLY — no bodies, no snippets (privacy invariant). The returned value is a small `TriageRuleEvaluationInput` wrapper record `(RuleEvaluationInput evaluationInput, String sanitizedSenderEmail, String gmailThreadId)` — `evaluationInput` is the `RuleEvaluator.evaluate(...)`-consumable type reused verbatim from `core.rules.domain` (do NOT widen it); `sanitizedSenderEmail` is the parsed sanitized sender address the orchestrator passes to `SenderSafetyNetService.isProtected(...)`; `gmailThreadId` is the thread id the audit rows + `SaveDraft` need. (If `RuleEvaluationInput` already carries the sanitized full sender address and the thread id, skip the wrapper and return `RuleEvaluationInput` directly — note which in the SUMMARY.) Translate `GoogleJsonResponseException` (mirror `GmailPreviewReadService`) — a 404 on the message means the message vanished before triage; log `event=triage_input_fetch_message_gone tenantId={} gmailMessageId={}` and return an empty/sentinel the orchestrator can skip on (no audit row). Log `event=triage_input_fetched tenantId={} gmailMessageId={}` — ids only. Whichever placement is chosen, prefer the one that adds the least new public surface and keeps the Gmail-client construction inside `core.gmail`; `core.triage.application.TriageRuleEvaluationInputFactory` is acceptable if it only orchestrates calls into `core.gmail`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && (grep -q "fetchTriageInput" backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java || test -f backend/core/src/main/java/com/zeromail/core/triage/application/TriageRuleEvaluationInputFactory.java)</automated>
  </verify>
  <acceptance_criteria>
    - A triage-facing facade exists (either `GmailPreviewReadService.fetchTriageInput(UUID, String)` or `TriageRuleEvaluationInputFactory.fetch(...)`) that builds the `RuleEvaluator`-consumable input from a `format=metadata, fields=id,threadId,labelIds,internalDate,payload/headers` Gmail fetch only.
    - No body/snippet fetch; `GoogleJsonResponseException`/404 handled (logs `event=triage_input_fetch_message_gone`, returns a sentinel/empty the orchestrator can skip on).
    - Gmail-client construction stays inside `core.gmail` (uses `GmailApiClientFactory.buildClientForTenant(...)` or `GmailPreviewReadService`'s existing client path).
    - `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>The metadata-only triage input facade exists; no email content is read.</done>
</task>

<task type="auto">
  <name>Task 2: TriageAuditSaga — the 3-phase reserve/lease -> Gmail-write -> finalize mini-saga (+ direct terminal insert for shadow/rejected)</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java</files>
  <read_first>
    - .planning/phases/04-triage-convergence-hero/04-REVIEWS.md "Agreed Concerns" #2 (the transaction-boundary design: reserve PENDING must be DURABLY COMMITTED before the external Gmail call; the call runs OUTSIDE a DB transaction; APPLIED/FAILED is a SEPARATE transaction; define lease/attempt semantics so a crashed-mid-write PENDING can be reclaimed) + Codex "Suggestions" "Add a dedicated audit saga transaction design task" — this task IS that.
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-A4 (`@ApplicationModuleListener` macro = `@Async + @Transactional(REQUIRES_NEW) + @TransactionalEventListener(AFTER_COMMIT)` — note the LISTENER already runs in a REQUIRES_NEW tx; the saga must NOT do its external Gmail call inside that ambient tx, so the saga's reserve/finalize methods are `@Transactional(propagation = REQUIRES_NEW)` on a SEPARATE `@Component` (proxy-boundary correctness — RESEARCH Pitfall 6) and the Gmail-write step is a plain method with NO `@Transactional`), D-C3 (the PENDING→APPLIED write order), D-C5 (the stuck-PENDING reaper is the backstop; here the saga must produce reclaimable PENDING rows — set `attempt_count`/`last_attempt_at` via `reclaimStalePending`), D-B4 (`gmail_change_token` set post-write)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java + TriageAuditRepository.java (from 04-02 — `TriageAuditWriter.insertPending(...)` returning `Optional<UUID>`, `TriageAuditWriter.insertTerminal(...)` for shadow/rejected, `TriageAuditRepository.reclaimStalePending(auditId, tenantId)` returning `int`, `markApplied(auditId, tenantId, externalRef, gmailChangeTokenJson)`, `markFailed(auditId, tenantId, failureReason)`, `findByAuditIdAndTenantId(auditId, tenantId)` — the saga calls these; it does NOT call the native repo insert directly, it goes through `TriageAuditWriter` so validation cannot be bypassed)
    - backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java (from 04-04 — `applyLabel(tenantId, gmailMessageId, labelId)`, `archiveSkipInbox(tenantId, gmailMessageId)`, `saveDraft(tenantId, saveDraftSpec, gmailThreadId)` returning the `draftId`; the saga calls these in phase 2)
    - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java + TriageActionArgsCanonicalizer.java + TriageDecision.java (from 04-02 — the pre-write `TriageActionResult` whose `canonicalHash` is computed BEFORE the Gmail call; `draftId == null` on the pre-write `SaveDraft`)
    - backend/worker/src/main/java/com/zeromail/worker/billing/CreditReserveWatchdog.java + CreditReserveWatchdogBatch.java (the scheduler/batch SPLIT pattern — the structural model for "a method that must run in its own committed transaction lives on a SEPARATE @Component the caller injects", RESEARCH Pitfall 6)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/PubSubDeliveryRepository.java (the explicit-status WHERE update idiom — reference for how `reclaimStalePending` / `markApplied` behave under concurrent retries)
  </read_first>
  <action>
    Create `com.zeromail.core.triage.application.TriageAuditSaga` as a `@Component` (a SEPARATE bean from `TriageOrchestratorService` so its `@Transactional(propagation = REQUIRES_NEW)` methods get a real proxy boundary — Spring self-invocation would silently break the new-transaction semantics, RESEARCH Pitfall 6). Inject `TriageAuditWriter`, `TriageAuditRepository`, `TriageGmailWriter`, `TriageActionArgsCanonicalizer`. It exposes:
    1. `record SagaContext(UUID tenantId, String gmailMessageId, String gmailThreadId, UUID ruleId, String ruleNameSnapshot, RuleActionType actionType, TriageActionResult preWriteIntent, String reasonEvidence)` — the immutable input the orchestrator passes per proposal.
    2. `void applyProposal(SagaContext context)` — the 3-phase happy path, public, NOT `@Transactional` itself (it orchestrates three calls, two of which are `@Transactional(REQUIRES_NEW)` on this same bean — but since `applyProposal` calls them on `this`, that would NOT cross a proxy; so EITHER make the two phase-methods package-private and have the ORCHESTRATOR call them in sequence, OR — cleaner — inject a self-reference (`@Lazy TriageAuditSaga self`) and call `self.reservePhase(...)` / `self.finalizePhase(...)` so the proxy fires; pick the self-reference approach and document it). The flow:
       - **Phase 1 (committed REQUIRES_NEW): `Optional<ReservationOutcome> reservePhase(SagaContext)`** — `@Transactional(propagation = REQUIRES_NEW)`. Compute `argsHash = canonicalizer.canonicalHash(context.preWriteIntent())` (pre-write — `draftId == null`). Call `Optional<UUID> reserved = triageAuditWriter.insertPending(tenantId, gmailMessageId, gmailThreadId, ruleId, ruleNameSnapshot, actionType, preWriteIntent, reasonEvidence)`. If `reserved.isPresent()` → it's a fresh PENDING row; also call `triageAuditRepository.reclaimStalePending(reserved.get(), tenantId)` to stamp `attempt_count=1`/`last_attempt_at=NOW()` (the first lease) — return `ReservationOutcome(reserved.get(), shouldAttemptGmail=true)`. If `reserved.isEmpty()` → a row already exists for this idempotency key; load it via `findByAuditIdAndTenantId`-equivalent (or a dedicated `findByIdemKey(...)` if the writer/repo exposes one — add a narrow SELECT in 04-02 if not). If that row's `decision` is NOT `PENDING` (already APPLIED/FAILED/REVERTED/terminal) → return `ReservationOutcome(existingId, shouldAttemptGmail=false)` (idempotent: nothing more to do; the orchestrator logs `event=triage_audit_already_present`). If it IS still `PENDING` → call `int claimed = triageAuditRepository.reclaimStalePending(existingId, tenantId)` — `claimed == 1` means the prior attempt's lease is stale (older than the 2-min window) so THIS delivery should retry the Gmail call → `ReservationOutcome(existingId, shouldAttemptGmail=true)`; `claimed == 0` means another worker holds a fresh lease → `ReservationOutcome(existingId, shouldAttemptGmail=false)` (skip; the reaper or the other worker will finish it). The transaction COMMITS here — the PENDING reservation + lease are durable before any external call.
       - **Phase 2 (NO DB transaction): `GmailWriteOutcome gmailWritePhase(SagaContext, UUID auditId)`** — a plain method (no `@Transactional`). Switch on `context.actionType()` / `context.preWriteIntent()`: `APPLY_LABEL` → `triageGmailWriter.applyLabel(tenantId, gmailMessageId, label.labelId())`, change token `{"addedLabelId":"<labelId>"}`, external_ref = null; `ARCHIVE_SKIP_INBOX` → `triageGmailWriter.archiveSkipInbox(tenantId, gmailMessageId)`, change token `{"removedLabelIds":["INBOX"]}`, external_ref = null; `SAVE_DRAFT` → `String draftId = triageGmailWriter.saveDraft(tenantId, saveDraftSpec, gmailThreadId)`, change token = null (D-B4), external_ref = draftId. On a Gmail exception → return `GmailWriteOutcome.failed(exception.getClass().getSimpleName())`; on success → `GmailWriteOutcome.applied(gmailChangeTokenJsonOrNull, externalRefOrNull)`. NO DB write happens in this phase — a crash here leaves the durable PENDING row from phase 1, reclaimable.
       - **Phase 3 (committed REQUIRES_NEW): `void finalizePhase(UUID auditId, UUID tenantId, GmailWriteOutcome outcome)`** — `@Transactional(propagation = REQUIRES_NEW)`. If `outcome.isApplied()` → `triageAuditRepository.markApplied(auditId, tenantId, outcome.externalRef(), outcome.gmailChangeTokenJson())` (the `WHERE decision='PENDING'` guard makes this safe under retry — a row already APPLIED by another worker is untouched). If `outcome.isFailed()` → `triageAuditRepository.markFailed(auditId, tenantId, outcome.failureReason())`. The transaction commits.
    3. `void recordTerminal(SagaContext context, TriageDecision terminalDecision)` — `@Transactional(propagation = REQUIRES_NEW)`, the direct-terminal-insert path the orchestrator calls for shadow-mode / sender-protected / safety-policy-rejected proposals: `triageAuditWriter.insertTerminal(tenantId, gmailMessageId, gmailThreadId, ruleId, ruleNameSnapshot, actionType, preWriteIntent, reasonEvidence, terminalDecision)` (validates+hashes, inserts directly with `decision = terminalDecision`, `decided_at = NOW()`, `gmail_change_token = NULL`, `attempt_count = 0`; `ON CONFLICT DO NOTHING` — idempotent). No Gmail call, no PENDING row.
    4. Content-free logging only: `event=triage_audit_reserved auditId={} tenantId={} gmailMessageId={} ruleId={} actionType={} attempt={}` in phase 1; `event=triage_audit_lease_held auditId={} ...` when `shouldAttemptGmail=false` on an existing PENDING; `event=triage_gmail_write_outcome auditId={} ... outcome=applied|failed errorClass={}` in phase 2/3; `event=triage_audit_terminal auditId={} ... decision={}` in `recordTerminal`. NO labels/subjects/snippets/sender names/draft bodies.
    Javadoc on the class: this is the audit mini-saga — three small transactions around one external side effect; the `reclaimStalePending` lease is what makes a worker crash between phase 2 and phase 3 recoverable instead of skip-forever (Codex HIGH); `markApplied`/`markFailed` are `WHERE decision='PENDING'` so double-finalization is a no-op; `SAVE_DRAFT` is the genuinely non-idempotent Gmail call (`drafts.create`) — the lease + the unique idempotency key + the `WHERE decision='PENDING'` finalize together bound it to "at most one draft per (tenant, message, rule, action_type, args_hash) under normal operation; a stale-lease retry after a phase-2 crash that actually succeeded would re-create the draft, which the reaper's optional metadata-verify path in 04-07 mitigates — document this residual as a known v1 trade-off in the Javadoc".
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && grep -q "REQUIRES_NEW" backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java && grep -q "reclaimStalePending" backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java && grep -q "insertTerminal\|recordTerminal" backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java && grep -q "gmailWritePhase\|gmailWriteOutcome\|GmailWriteOutcome" backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java</automated>
  </verify>
  <acceptance_criteria>
    - `TriageAuditSaga` is a `@Component` separate from `TriageOrchestratorService`; its `reservePhase` / `finalizePhase` / `recordTerminal` methods are `@Transactional(propagation = REQUIRES_NEW)`; the Gmail-write phase has NO `@Transactional`; the saga uses a self-reference (`@Lazy`) so the `REQUIRES_NEW` proxy actually fires when `applyProposal` chains the phases.
    - `reservePhase` returns a `(auditId, shouldAttemptGmail)` outcome: fresh insert → attempt=true with `attempt_count` stamped via `reclaimStalePending`; existing terminal row → attempt=false; existing PENDING with a stale lease → `reclaimStalePending`=1 → attempt=true; existing PENDING with a fresh lease → attempt=false.
    - `finalizePhase` calls `markApplied(auditId, tenantId, externalRef, gmailChangeTokenJson)` (with the post-write change token: `{"addedLabelId":...}` / `{"removedLabelIds":["INBOX"]}` / null) on success, `markFailed` on a Gmail exception; both rely on the `WHERE decision='PENDING'` guard for idempotency.
    - `recordTerminal(context, decision)` does the direct `TriageAuditWriter.insertTerminal` for SHADOW_LOGGED / REJECTED_BY_SAFETY_NET / REJECTED_BY_SAFETY_POLICY with `gmail_change_token=null` and no Gmail call.
    - `args_hash` is computed in phase 1 from the pre-write intent (`SaveDraft.draftId == null`) so a retry re-derives the same key.
    - No log line carries email content / subject / snippet / sender display name / drafted body.
    - `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>The audit mini-saga exists with three small transactions around the single external Gmail write, a lease-and-reclaim path for crash recovery, idempotent `WHERE decision='PENDING'` finalization, and the direct-terminal-insert path for shadow/rejected rows.</done>
</task>

<task type="auto">
  <name>Task 3: TriageOrchestratorService — the @ApplicationModuleListener hero orchestration (delegates each proposal to TriageAuditSaga)</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java, backend/worker/src/main/java/com/zeromail/worker/triage/TriageOrchestratorAdapter.java, backend/worker/src/main/java/com/zeromail/worker/triage/package-info.java, backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java</files>
  <read_first>
    - .planning/phases/04-triage-convergence-hero/04-RESEARCH.md §"System Architecture Diagram" (the full step list 1–N: ScopedValue rebind → `triage_paused` early-return → metadata fetch → load rules `display_order` → tri-state eval collecting DEFERRED → budget check → batched vs per-rule LLM → merge → per-proposal safety gate → sender-net check → audit PENDING → Gmail write OR shadow → markApplied/markFailed) + §"Code Examples" "Worker loop rebinding TenantContext" + "Tri-state evaluation + proposal merge" + Pitfalls 1 (drafts.create non-idempotent → the saga), 3 (native SQL tenant scoping — already enforced in the repo from 04-02), 4 (ScopedValue rebind in the listener — use `TenantContext.runWith` from 04-01), 6 (the `@Transactional` proxy-boundary lesson — why the saga is a separate bean), 7 (shadow vs paused), 8 (LLM failure → DEFERRED-(error) → NOT_MATCHED, never default-false)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-A4 (`@ApplicationModuleListener` macro = `@Async + @Transactional(REQUIRES_NEW) + @TransactionalEventListener(AFTER_COMMIT)`; runs on a virtual thread; exceptions captured into `FailedEventPublications`; rebind tenant at listener entry — note this means the listener already opens a tx, so the per-proposal audit work MUST be delegated to the saga's `REQUIRES_NEW` methods so the Gmail call escapes the ambient tx), D-C3 (the write order, now realized by `TriageAuditSaga`), D-C5 (the reaper is the backstop), D-D1 (per-message all-rules batched default), D-D2 (per-rule fanout on token budget overrun, `CompletableFuture.allOf` on virtual threads), D-D3 (1 reserve per LLM call; N for fanout; 1 `TRIAGE_DETERMINISTIC` for pure-deterministic messages; BYOK bypasses), D-D5 (LLM failure → all nodes `DEFERRED-(error)`, treated as `NOT_MATCHED`; deterministic actions still fire; NO new top-level `decision=FAILED` from semantic failure alone), D-D6 (7s batched / 6s per-rule timeout via HTTP read-timeout, not `future.get`)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 8 point b ("Protected senders cause the orchestrator to: ... (b) skip Gmail writes" — NO per-tier carve-out; a protected sender suppresses APPLY_LABEL too, not just ARCHIVE/SAVE_DRAFT — this overrides the earlier draft of step 9b which only gated the destructive tiers)
    - .planning/phases/04-triage-convergence-hero/04-PATTERNS.md the `TriageOrchestratorService` pattern-assignment section (the exact analog lines: `GmailHistoryProcessor.tick()` lines 28-35 for the ScopedValue rebind; `ActionProposalMerger.evaluateAndMerge` lines 40-69 for evaluate+merge; `RuleEvaluator.evaluate` lines 18-101 + `RuleEvaluationResult.deferred(nodeId, "semantic_intent_deferred")` line 97-99; `RuleRepository.findOrderedByTenantId` for the rule load; `GmailDeliveryProcessingService` log lines 73/97/103/111 for the content-free log idiom)
    - backend/core/src/main/java/com/zeromail/core/triage/application/TriageAuditSaga.java (from Task 2 — `applyProposal(SagaContext)` / `recordTerminal(SagaContext, TriageDecision)`; the orchestrator builds the `SagaContext` per proposal and calls one of these — it does NOT touch `TriageAuditRepository` directly)
    - backend/core/src/main/java/com/zeromail/core/rules/service/RuleEvaluator.java + ActionProposalMerger.java + RuleRepository.java (the Phase 3 contracts the orchestrator calls — `findOrderedByTenantId`, `RuleEntity.getMatcherAst()` / `getActionIntents()`, `evaluate(...)` returning `MATCHED/NOT_MATCHED/DEFERRED`, `merge(...)` returning the ordered `List<ActionProposal>`; do NOT change Phase 3 behavior) + `RuleEvaluationInput.java` (the fields available for building `semanticEvalContent` — `sanitizedSubjectExcerpt`, `sanitizedSenderDomain`, `gmailCategories`, `bodyDerivedFlags`, `hasAttachment`, `listUnsubscribePresent`, `newsletterIndicatorPresent`; there is NO body field; it should also carry or let the orchestrator derive the sanitized SENDER address for the sender-net check — verify the field and use it; the sender-net service canonicalizes it)
    - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java + TriageActionArgsCanonicalizer.java + TriageDecision.java (from 04-02 — the `TriageActionResult` the orchestrator builds from a proposal's `ActionIntent` to pass into the saga; for `SaveDraft` the `instruction` is the rule's static instruction, `draftId == null` pre-write)
    - backend/core/src/main/java/com/zeromail/core/triage/service/TriageSafetyPolicy.java + TriageGmailWriter.java + SenderSafetyNetService.java (from 04-04 — `gate(proposal)`, the writer methods (the orchestrator never calls these directly — only the saga does), `isProtected(tenantId, senderEmail)`)
    - backend/core/src/main/java/com/zeromail/core/llm/service/LlmGateway.java#evaluateSemanticIntents + SemanticIntentRequest.java + TokenBudgetExceededException.java + LlmEvaluationFailedException.java (from 04-03 — the gateway contract: `Map<String,Boolean> evaluateSemanticIntents(CallSite, String rawMessageContent, List<SemanticIntentRequest>)`; the gateway sanitizes `rawMessageContent` internally; the two exceptions to catch)
    - backend/core/src/main/java/com/zeromail/core/billing/service/CreditLedger.java + CallSite.java (from 04-02 — `reserve(tenantId, callSite) → settle/release`; `TRIAGE_PLATFORM_LLM` / `TRIAGE_DETERMINISTIC`; BYOK bypass — mirror how `LlmGatewayImpl` detects BYOK, or reserve unconditionally and let the ledger no-op for BYOK tenants per the Phase 2C convention)
    - backend/core/src/main/java/com/zeromail/core/tenant/TenantContext.java#runWith + TenantService.java#isTriageShadowMode (from 04-01 / 04-02) + the existing `isTriagePaused` accessor; backend/worker/src/main/java/com/zeromail/worker/ZeroMailWorkerApplication.java (verify it scans `com.zeromail.core` so the listener bean is component-scanned — CONTEXT Open Question 2)
    - backend/worker/src/main/java/com/zeromail/worker/GmailHistoryProcessor.java (the worker-side ScopedValue rebind precedent)
    - backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java (re-verify GREEN after the orchestrator lands — the orchestrator never calls Gmail directly; the saga delegates to `TriageGmailWriter`)
    - Micrometer Timer idiom in the codebase (search for an existing `Timer` usage in `core.*` or `worker.*`) — wrap the orchestration in a `triage.duration` timer tagged per AI-SPEC §7; also add a `triage.semantic_eval.fanout.per_rule` counter (OpenCode suggestion F — cheap, helps the v2 adaptive-token-budget decision)
    - Spring Modulith Events reference (re-fetch via Context7 for `org.springframework.modulith` at the pinned `springModulith` version BEFORE authoring — Codex suggested execution-time doc checks here, not just in 04-07): confirm `@org.springframework.modulith.events.ApplicationModuleListener` expands to `@Async + @Transactional(propagation = REQUIRES_NEW) + @TransactionalEventListener(phase = AFTER_COMMIT)` (D-A4 — if the macro's composition differs in the pinned version, the orchestrator must achieve the same effect explicitly, and the saga's `REQUIRES_NEW` reasoning must be re-checked against it), that a thrown listener exception is captured into the failed-publications view (so the 04-07 retry job can resubmit it), and that the JDBC event registry needs no extra `@EnableScheduling`-style opt-in beyond `spring-modulith-starter-jdbc` (added in 04-00); note any deviation in the SUMMARY.
  </read_first>
  <action>
    Create `com.zeromail.core.triage.application.TriageOrchestratorService` (placement per CONTEXT Open Question 2 — `application/` per CONVENTIONS #2; the worker already scans `com.zeromail.core` so the listener bean is component-scanned). Inject `TriageRuleEvaluationInputFactory` (or `GmailPreviewReadService` facade), `RuleRepository`, `RuleEvaluator`, `ActionProposalMerger`, `LlmGateway`, `CreditLedger`, `TenantService`, `TriageSafetyPolicy`, `SenderSafetyNetService`, `TriageAuditSaga`, `TriageActionArgsCanonicalizer` (only if needed for logging — the saga owns the hash), a virtual-thread executor, and `MeterRegistry`. It exposes a `@org.springframework.modulith.events.ApplicationModuleListener void onMailMessageObserved(MailMessageObserved event)` — **this is the ONLY class that carries `@ApplicationModuleListener` for `MailMessageObserved`** (Codex MEDIUM "possible duplicate listeners" — if the worker AOT path later forces an adapter, the adapter takes the annotation and THIS method drops it; the two never both have it). The macro = `@Async + @Transactional(REQUIRES_NEW) + @TransactionalEventListener(AFTER_COMMIT)`; runs on a virtual thread; exceptions propagate to `FailedEventPublications` — do NOT catch-all-and-swallow the listener body. Inside the listener, first line: `TenantContext.runWith(event.tenantId(), () -> orchestrate(event))` (the ScopedValue rebind across the `@Async` boundary — RESEARCH Pitfall 4). The private `orchestrate(MailMessageObserved event)`:
    1. Wrap the whole body in a `triage.duration` Micrometer timer (tags per AI-SPEC §7 — hash the tenant id if the project convention is hashed).
    2. If `tenantService.isTriagePaused(tenantId)` → log `event=triage_skipped_paused tenantId={} gmailMessageId={}` and return (no audit row — D-A3/Pitfall 7).
    3. Fetch the metadata-only input via the Task-1 facade (`TriageRuleEvaluationInputFactory` / `GmailPreviewReadService.fetchTriageInput`); if the message is gone (404) → log `event=triage_input_fetch_message_gone tenantId={} gmailMessageId={}` and return (no audit row).
    4. `List<RuleEntity> rules = ruleRepository.findOrderedByTenantId(tenantId)` (enabled, `display_order`); if empty → log `event=triage_no_rules tenantId={}` and return.
    5. Parse each rule's `getMatcherAst()` / `getActionIntents()`; run `RuleEvaluator.evaluate(...)` per rule collecting `DEFERRED` results (the `SemanticIntentMatcher` nodes — `{nodeId, intent}`).
    5b. **Build the exact string passed to `LlmGateway.evaluateSemanticIntents` — name it `semanticEvalContent`.** Per SPEC §req 1 the metadata-only fetch carries NO email body — only `sanitizedSubjectExcerpt` (Phase-3 sanitized + truncated) plus the derived booleans/flags (`bodyDerivedFlags`, `hasAttachment`, `listUnsubscribePresent`, `newsletterIndicatorPresent`, `sanitizedSenderDomain`, `gmailCategories`). The v1 classifier input is therefore the **sanitized subject excerpt plus a deterministic content-free flag summary** — NOT the email body (there is none in the metadata-only fetch; sending one would require a richer Gmail fetch the SPEC forbids for triage). Construct `semanticEvalContent` as a small fixed-shape, stable-ordered string built only from the already-sanitized/derived fields above (sort the flag/category tokens); never raw headers, never a body, never the sender display name, never the raw sender email beyond the domain. Pass it as the `rawMessageContent` arg to `evaluateSemanticIntents` — the gateway re-runs the Phase 2C sanitization pipeline over it (harmless for an already-sanitized excerpt). **Documentation lock-in (OpenCode suggestion C):** place the v1-limitation Javadoc note ON the `semanticEvalContent`-building method, ON the field/local that holds it (a Javadoc-style comment), AND in the CLASS-level Javadoc — all three say "v1 limitation — semantic-intent matchers see only the sanitized subject excerpt + derived flags (the metadata-only triage fetch carries no body, per SPEC §req 1); a richer sanitized-content fetch is a future enhancement that would still transit the LLM only, never be persisted." Add a code comment at the build site: `// NOTE: do NOT add body/snippet/raw-header fetch here — that would require a richer Gmail API call and re-review under privacy+latency constraints.` Do NOT add a body/snippet fetch in this plan. (Build `semanticEvalContent` only when step 6 actually fires.)
    6. If there is ≥1 DEFERRED semantic node across all matched rules: build `semanticEvalContent` (5b); compute the token budget; attempt the per-message batched call `Map<String,Boolean> matches = llmGateway.evaluateSemanticIntents(CallSite.TRIAGE_PLATFORM_LLM, semanticEvalContent, allIntentRequests)`; on `TokenBudgetExceededException` → fan out per-rule on the virtual-thread executor (`CompletableFuture.supplyAsync(...)`, join all — N reserves total, D-D3; increment the `triage.semantic_eval.fanout.per_rule` counter per fanned-out rule); if a single rule's intents alone exceed the cap, mark that rule's nodes `DEFERRED-(error)` and continue; on `LlmEvaluationFailedException`/`SafetyViolationException` for the batched call → mark ALL semantic nodes `DEFERRED-(error)` with the opaque error class (D-D5); per-rule fanout failures mark only that rule's nodes `DEFERRED-(error)`. Action-gating treats `DEFERRED-(error)` as `NOT_MATCHED`; deterministic-only actions still fire; NO new top-level `decision=FAILED` from semantic failure alone (D-D5). The orchestrator passes `semanticEvalContent` verbatim — never a body/snippet/raw headers; the gateway sanitizes internally.
    7. If there are zero semantic nodes anywhere (pure-deterministic message): `creditLedger.reserve(tenantId, CallSite.TRIAGE_DETERMINISTIC)` once (cost default 0 — D-D3); settle/release per the `CreditLedger` lifecycle JavaDoc. BYOK tenants bypass per Phase 2C convention.
    8. `List<ActionProposal> proposals = actionProposalMerger.merge(...)` (resolving DEFERRED nodes with the LLM results, treating `DEFERRED-(error)` as `NOT_MATCHED`) — the ordered candidate actions.
    9. Compute `boolean shadowMode = tenantService.isTriageShadowMode(tenantId)` and `boolean senderProtected = senderSafetyNetService.isProtected(tenantId, sanitizedSenderEmailFromInput)` ONCE per message (not per proposal — the sender is the same for the whole message). Then for each proposal, in order, build `TriageActionResult preWriteIntent` from the proposal's `ActionIntent` (`Label(labelId, labelName)` / `Archive()` / `SaveDraft(staticInstruction, null, threadId)`) and a `TriageAuditSaga.SagaContext` (tenantId, gmailMessageId, gmailThreadId, ruleId, ruleNameSnapshot, actionType, preWriteIntent, reasonNodeIdEvidence), then:
       (a) `RuleActionType gated;` — `try { gated = triageSafetyPolicy.gate(proposal); } catch (TriageSafetyViolationException violation) { triageAuditSaga.recordTerminal(sagaContext, TriageDecision.REJECTED_BY_SAFETY_POLICY); log "event=triage_action_rejected_policy ..."; continue; }` (do NOT abort the whole message).
       (b) If `senderProtected` → `triageAuditSaga.recordTerminal(sagaContext, TriageDecision.REJECTED_BY_SAFETY_NET); log "event=triage_action_rejected_sender_net ..."; continue;` — this gates EVERY action type (label/archive/save-draft) per SPEC §req 8 point b "skip Gmail writes" — NOT just the destructive tiers.
       (c) If `shadowMode` → `triageAuditSaga.recordTerminal(sagaContext, TriageDecision.SHADOW_LOGGED); log "event=triage_action_shadow_logged ..."; continue;` — no Gmail call (D-B4/Pitfall 7).
       (d) Else (real apply) → `triageAuditSaga.applyProposal(sagaContext)` — the saga runs its 3-phase reserve→Gmail→finalize; the orchestrator just logs the bracketing `event=triage_action_handed_to_saga ...` (the saga emits its own `triage_audit_reserved` / `triage_gmail_write_outcome` lines). Catch nothing here that the saga doesn't already handle — a saga-internal Gmail exception is recorded as `markFailed` by the saga itself; an unexpected non-Gmail exception escapes the listener so Modulith captures it.
    10. NO email content / subject / snippet / sender display name / drafted body / prompt / completion in ANY log line or audit field (privacy invariant — `reason` carries matcher node id + structured evidence ids only).
    If Spring AOT / `@ApplicationModuleListener` detection misbehaves in the worker for a bean defined in `core` (CONTEXT Open Question 2), add a thin `com.zeromail.worker.triage.TriageOrchestratorAdapter` `@Component` in `backend/worker` that carries the `@ApplicationModuleListener` and delegates to `TriageOrchestratorService` — AND remove the annotation from `TriageOrchestratorService.onMailMessageObserved` so there is exactly ONE listener (verify via the integration test which boots the worker and dispatches an event, asserting the event is handled exactly once). Create `com.zeromail.worker.triage.package-info.java` (one-line Javadoc: "Worker-side triage scheduled jobs and event-listener adapters."). Re-verify `NoGmailSendAllowedTest` is GREEN (the orchestrator and the saga delegate all Gmail writes to `TriageGmailWriter`; neither calls a Gmail write API directly).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava :backend:worker:compileJava 2>&1 | tail -3 && grep -q "ApplicationModuleListener" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && grep -q "triageAuditSaga\|TriageAuditSaga" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && grep -q "evaluateSemanticIntents" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && grep -q "semanticEvalContent" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && grep -q "do NOT add body" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && ! grep -q "TriageGmailWriter\|insertAuditPendingIfAbsent\|markApplied" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && ./gradlew :backend:core:test --tests "*ApplicationModulesTest" --tests "*NoGmailSendAllowedTest" 2>&1 | tail -3 && ./gradlew :backend:worker:test --tests "*TriageOrchestratorIntegrationContractTest" --tests "*TriageIdempotencyContractTest" --tests "*TriageShadowModeContractTest" --tests "*TriageCreditAccountingContractTest" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `TriageOrchestratorService` has exactly ONE `@ApplicationModuleListener` method consuming `MailMessageObserved` (or, if the worker AOT path forces it, the annotation lives ONLY on `TriageOrchestratorAdapter` and NOT on the service — never both); its first action is `TenantContext.runWith(event.tenantId(), ...)`; the listener body does not catch-and-swallow unexpected exceptions.
    - The orchestrator does NOT reference `TriageGmailWriter`, `TriageAuditRepository`, `insertAuditPendingIfAbsent`, or `markApplied` directly — all per-proposal audit + Gmail work is delegated to `TriageAuditSaga` (`applyProposal` / `recordTerminal`).
    - The string passed to `LlmGateway.evaluateSemanticIntents` is the explicitly-named `semanticEvalContent` — built from `sanitizedSubjectExcerpt()` + a deterministic content-free flag/category/sender-domain summary, with NO email body / snippet / raw headers / sender display name / raw sender email; the v1-limitation Javadoc note appears on the builder method, the field/local, AND the class Javadoc; a `// NOTE: do NOT add body/snippet/raw-header fetch here` code comment is present at the build site.
    - The orchestration: returns early on `isTriagePaused`; builds a metadata-only input; loads `findOrderedByTenantId`; tri-state evaluates; resolves DEFERRED semantic nodes via `evaluateSemanticIntents` with per-message batched default + per-rule fanout on `TokenBudgetExceededException` (incrementing `triage.semantic_eval.fanout.per_rule`) + `DEFERRED-(error)` on `LlmEvaluationFailedException`/`SafetyViolationException` (treated as `NOT_MATCHED`); reserves `CallSite.TRIAGE_PLATFORM_LLM` once per LLM call (N for fanout) or `CallSite.TRIAGE_DETERMINISTIC` once for pure-deterministic messages; merges proposals; computes `shadowMode` + `senderProtected` once per message; per proposal: `TriageSafetyPolicy.gate` (→ `recordTerminal(REJECTED_BY_SAFETY_POLICY)` on violation), then if `senderProtected` → `recordTerminal(REJECTED_BY_SAFETY_NET)` for ANY action type (label included — SPEC §req 8 point b), then if `shadowMode` → `recordTerminal(SHADOW_LOGGED)`, else `triageAuditSaga.applyProposal(sagaContext)`.
    - No log line or audit field carries email content / subject / snippet / sender display name / drafted body / prompt / completion; `reason` is matcher node id + evidence ids only.
    - `com.zeromail.worker.triage.package-info.java` exists; the worker's component scan covers `com.zeromail.core` (verified by the integration test booting and processing an event exactly once); if needed, `TriageOrchestratorAdapter` is the sole worker-side `@ApplicationModuleListener` delegate.
    - `./gradlew :backend:core:compileJava :backend:worker:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*ApplicationModulesTest" --tests "*NoGmailSendAllowedTest"` PASS; `./gradlew :backend:worker:test --tests "*TriageOrchestratorIntegrationContractTest" --tests "*TriageIdempotencyContractTest" --tests "*TriageShadowModeContractTest" --tests "*TriageCreditAccountingContractTest"` all PASS (event wiring + 2-rule deterministic control run + 1 audit row per applied action; replay → 1 audit row per action / ≤1 Gmail write per action AND a crashed-mid-write PENDING row reclaimed-and-completed on the next delivery; shadow → SHADOW_LOGGED + zero Gmail calls; 1 reserve per LLM call / 1 per pure-deterministic message).
    - `./gradlew :backend:core:test --tests "*MultiTenantLeakIntegrationTest"` still PASSES (FND-05).
  </acceptance_criteria>
  <done>The hero orchestrator is wired end-to-end (event → rules → LLM → safety → sender-net → delegate-to-saga for apply / direct-terminal for shadow+rejected) with a single listener location, fail-loud LLM semantics, the named `semanticEvalContent` LLM input + triple-placed v1-limitation Javadoc + the do-not-add-body comment, all-action sender-net gating, and content-free logging; the orchestrator + idempotency (incl. crash-reclaim) + shadow + credit-accounting Wave-0 contracts are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pub/Sub-redelivered event → triage actions | A redelivered `MailMessageObserved` must not double-act; the `TriageAuditSaga`'s 3-phase loop (committed-PENDING-reserve → Gmail call → committed-finalize) + the `NULLS NOT DISTINCT` unique index + the `WHERE decision='PENDING'` finalize make re-triage of `(tenantId, gmailMessageId)` idempotent; a crash between the Gmail call and the finalize commit leaves a reclaimable PENDING row (lease via `reclaimStalePending`), not a skip-forever one (review concern #2). |
| `@Async` listener → tenant isolation | The listener runs on a fresh virtual thread with no inherited ScopedValue; `TenantContext.runWith(event.tenantId(), ...)` rebinds before any tenant-scoped repository / LLM call. |
| ambient listener tx → external Gmail call | The `@ApplicationModuleListener` already opens a `REQUIRES_NEW` tx; the per-proposal audit work is delegated to `TriageAuditSaga`, whose reserve/finalize methods are themselves `@Transactional(REQUIRES_NEW)` on a SEPARATE bean (proxy-boundary correctness, RESEARCH Pitfall 6) and whose Gmail-write phase has NO `@Transactional` — so the external side effect never runs inside an open DB transaction. |
| sender content → LLM call | Untrusted content reaches the model via `evaluateSemanticIntents`; in v1 the only sender-derived text sent is the already-sanitized subject excerpt + content-free derived flags (no body) — sanitization (Phase 2C) re-runs on `semanticEvalContent`, the gateway's set-equality validator + the orchestrator's `DEFERRED-(error)`→`NOT_MATCHED` semantics contain the blast radius; a single-message batched call's cross-rule reach is the accepted v1 trade-off (D-D1); the v1-limitation is Javadoc-locked in three places + a do-not-extend code comment. |
| rule proposals → Gmail writes | Every proposal passes `TriageSafetyPolicy` (allow-list); if the sender is protected, the orchestrator records `REJECTED_BY_SAFETY_NET` for EVERY action type (label included — SPEC §req 8 point b "skip Gmail writes") and never calls `TriageGmailWriter`; only an allow-listed, non-protected, non-shadow proposal reaches the saga's Gmail-write phase. |
| listener failure → durability | Exceptions escape the `@ApplicationModuleListener` body and are captured into `FailedEventPublications`; the Wave-6 retry job (04-07) resubmits incomplete AND failed publications — a worker crash mid-orchestration never silently loses an action. |
| shadow mode | When `isTriageShadowMode`, the orchestrator calls `triageAuditSaga.recordTerminal(..., SHADOW_LOGGED)` (a direct terminal insert, `gmail_change_token = null`, no PENDING, no Gmail call) — no Gmail state change occurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering (idempotency) | redelivered event → double-act / lost action | mitigate | `TriageAuditSaga` 3-phase loop: phase-1 committed `insertAuditPendingIfAbsent` (DB `NULLS NOT DISTINCT` unique index on `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)` — `args_hash` over pre-write intent only so a retry re-derives the same key) → phase-2 Gmail call (no DB tx) → phase-3 committed `markApplied`/`markFailed` (`WHERE decision='PENDING'`); a crash mid-write leaves a PENDING row that `reclaimStalePending` (2-min lease) lets the next delivery reclaim — replay → 1 audit row per action, ≤1 Gmail write per action; the stuck-PENDING reaper (04-07) is the backstop (Wave-0 `TriageIdempotencyContractTest` incl. the crash-reclaim case). The residual: a stale-lease retry after a phase-2 crash that *actually* succeeded re-creates a `drafts.create` draft — documented v1 trade-off, mitigated by the reaper's optional metadata-verify path. |
| T-04-05-02 | Information Disclosure | tenant leak via `@Async` listener | mitigate | `TenantContext.runWith(event.tenantId(), ...)` rebinds the ScopedValue at listener entry before any tenant-scoped access; the saga's `REQUIRES_NEW` methods run inside that rebound scope; FND-05 `MultiTenantLeakIntegrationTest` re-verified GREEN; native `triage_audit` queries (from 04-02) carry the explicit tenant term. |
| T-04-05-03 | Elevation of Privilege | non-allow-listed / SEND action | mitigate | `TriageSafetyPolicy.gate(proposal)` rejects anything outside `{LABEL, ARCHIVE, SAVE_DRAFT}` → `recordTerminal(REJECTED_BY_SAFETY_POLICY)` + no Gmail call; `RuleActionType.SEND` does not exist (`NoGmailSendAllowedTest`); neither the orchestrator nor the saga calls a Gmail write API directly — only `TriageGmailWriter` (`TriageGmailWriteBoundaryTest`); the orchestrator references no `TriageGmailWriter`/`TriageAuditRepository` at all (delegation to the saga). |
| T-04-05-04 | Repudiation / mis-calibrated trust | silent LLM failure | mitigate | `LlmEvaluationFailedException` / `SafetyViolationException` from the gateway → all affected nodes `DEFERRED-(error)` with the opaque error class; semantic-gated actions do not fire; deterministic-only actions still fire; booleans are NEVER defaulted to `false`; AI-SPEC eval Dim 3 is the build-breaking check. |
| T-04-05-05 | Information Disclosure | content in logs / audit / metrics / LLM input | mitigate | Every triage log line (orchestrator AND saga) is `event=triage_* tenantId={} gmailMessageId={} ruleId={} actionType={} ...` (ids + op names + opaque error classes only); `triage_audit.reason` = matcher node id + structured evidence ids; the LLM input (`semanticEvalContent`) is restricted to the already-sanitized subject excerpt + content-free derived flags (no body, no raw headers, no sender display name) — Javadoc-locked in three places + a do-not-extend code comment; `triage.duration` / `triage.semantic_eval.fanout.per_rule` metric tags follow the project's hashed-tenant convention — no raw content anywhere; cross-cutting privacy-sweep test in plan 04-08. |
| T-04-05-06 | Tampering / over-eager action | auto-action on a frequent sender | mitigate | `senderSafetyNetService.isProtected(tenantId, sanitizedSenderEmail)` is computed once per message; if protected, EVERY proposal (label / archive / save-draft) is `recordTerminal(REJECTED_BY_SAFETY_NET)` with no Gmail write (SPEC §req 8 point b "skip Gmail writes" — no per-tier carve-out); the service fails SAFE on a Gmail outage (treats senders as protected); the `tenant_sender_opt_in` row is the user's explicit override; the `tenant_protected_sender_observation` row makes the flagged set visible via the GET endpoint (point c). |
| T-04-05-07 | Denial of Service / SLO breach | unbounded orchestration latency | mitigate | LLM call timeouts 7s batched / 6s per-rule (HTTP read-timeout); `maxTokens(512)` output cap; the `triage.duration` timer surfaces p95/p99 against the 10s SLO; per-rule fanout runs in parallel on virtual threads so total latency stays bounded; `triage_paused` is the global kill-switch; the saga's per-proposal transactions are small (reserve / finalize) so no long-held locks. |
</threat_model>

<verification>
- `./gradlew :backend:core:compileJava :backend:worker:compileJava` BUILD SUCCESSFUL.
- `./gradlew :backend:core:test --tests "*ApplicationModulesTest" --tests "*NoGmailSendAllowedTest" --tests "*MultiTenantLeakIntegrationTest"` all PASS.
- `./gradlew :backend:worker:test --tests "*TriageOrchestratorIntegrationContractTest" --tests "*TriageIdempotencyContractTest" --tests "*TriageShadowModeContractTest" --tests "*TriageCreditAccountingContractTest"` all PASS (incl. the crash-then-reclaim idempotency case and the all-action sender-net gate).
</verification>

<success_criteria>
- `TriageAuditSaga` (3-phase reserve→Gmail→finalize, `REQUIRES_NEW` on a separate bean, lease/reclaim crash recovery, `WHERE decision='PENDING'` idempotent finalize, direct-terminal insert for shadow/rejected) exists.
- `TriageOrchestratorService` (single `@ApplicationModuleListener`, tenant rebind, batched-vs-fanout LLM, the named `semanticEvalContent` LLM input + triple-placed v1-limitation Javadoc + the do-not-add-body comment, fail-loud `DEFERRED-(error)`, per-proposal safety-policy gating + once-per-message sender-net gate over ALL action types + shadow-mode terminal insert + delegate-to-saga for apply, content-free logging, no direct `TriageGmailWriter`/`TriageAuditRepository` reference) exists and is wired in the worker.
- The metadata-only triage input facade exists (returns the `RuleEvaluator` input + the sanitized sender email + thread id); no email content is read.
- The orchestrator + idempotency (incl. crash-reclaim) + shadow + credit-accounting Wave-0 contracts are GREEN; `NoGmailSendAllowedTest` + `ApplicationModulesTest` + FND-05 still GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-05-SUMMARY.md`
</output>
