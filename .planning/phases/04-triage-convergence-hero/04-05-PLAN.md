---
phase: 04-triage-convergence-hero
plan: 05
type: execute
wave: 5
depends_on: [00, 01, 02, 03, 04]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java
  - backend/core/src/main/java/com/zeromail/core/triage/application/TriageRuleEvaluationInputFactory.java
  - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageOrchestratorAdapter.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/package-info.java
  - backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageOrchestratorIntegrationContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageIdempotencyContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageShadowModeContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageCreditAccountingContractTest.java
autonomous: true
requirements: [TRG-01, TRG-02, TRG-03, TRG-04, TRG-05, TRG-08]

must_haves:
  truths:
    - "On a MailMessageObserved event, the orchestrator rebinds TenantContext, returns early if triage_paused, builds a metadata-only RuleEvaluationInput, loads enabled rules in display_order, evaluates matchers (resolving SEMANTIC_INTENT inline via LlmGateway), merges proposals, gates each through TriageSafetyPolicy, checks the sender safety net, and either applies via TriageGmailWriter or writes a SHADOW_LOGGED audit row"
    - "The orchestrator uses a two-phase PENDING -> APPLIED audit write loop: insertAuditPendingIfAbsent (skip Gmail if RETURNING is empty) -> TriageGmailWriter call -> markApplied (or markFailed on Gmail exception)"
    - "Replaying the same (tenantId, gmailMessageId) produces exactly one audit row per action and at most one Gmail write per action"
    - "LLM failure marks all semantic nodes DEFERRED-(error) (treated as NOT_MATCHED); deterministic-only actions still fire; over-budget triggers per-rule fanout; credit accounting is 1 reserve per LLM call + 1 per pure-deterministic message"
    - "Listener exceptions are captured by Modulith into FailedEventPublications, not swallowed"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java"
      provides: "@ApplicationModuleListener consumer of MailMessageObserved; the hero orchestration"
      contains: "ApplicationModuleListener"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageRuleEvaluationInputFactory.java"
      provides: "Metadata-only Gmail fetch -> RuleEvaluationInput facade for triage"
      contains: "RuleEvaluationInput"
  key_links:
    - from: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java"
      to: "MailMessageObserved (Spring Modulith JDBC registry)"
      via: "@ApplicationModuleListener + TenantContext.runWith rebind"
      pattern: "ApplicationModuleListener"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java"
      to: "TriageAuditRepository.insertAuditPendingIfAbsent / markApplied / markFailed"
      via: "two-phase PENDING -> APPLIED write loop (skip Gmail when RETURNING empty)"
      pattern: "insertAuditPendingIfAbsent"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java"
      to: "LlmGateway.evaluateSemanticIntents"
      via: "resolve DEFERRED semantic nodes; on TokenBudgetExceededException fan out per-rule; on failure mark DEFERRED-(error)"
      pattern: "evaluateSemanticIntents"
---

<objective>
[Wave 5] Land the hero: `TriageOrchestratorService` — the `@ApplicationModuleListener` consumer of `MailMessageObserved` (in `core.triage.application`, consumed by `backend/worker`) that runs the full triage flow per the SPEC §req 1 + RESEARCH §"System Architecture Diagram": tenant rebind → `triage_paused` early-return → metadata-only `RuleEvaluationInput` → enabled rules in `display_order` → tri-state `RuleEvaluator` → inline `SEMANTIC_INTENT` resolution via `LlmGateway.evaluateSemanticIntents` (per-message batched default, per-rule fanout on `TokenBudgetExceededException`, `DEFERRED-(error)` → `NOT_MATCHED` on failure) → `ActionProposalMerger` → per-proposal `TriageSafetyPolicy` gate → `SenderSafetyNetService` check → two-phase `PENDING → APPLIED` audit write loop → `TriageGmailWriter` apply OR `SHADOW_LOGGED` audit row → credit accounting (1 reserve per LLM call / 1 per pure-deterministic message). Plus the `TriageRuleEvaluationInputFactory` (metadata-only Gmail fetch facade in `core.gmail`/`core.triage.application`), the optional `worker.triage` adapter (if AOT needs it), the `worker.triage` package-info, and verification that the orchestrator integration / idempotency / shadow-mode / credit-accounting Wave-0 tests go GREEN.

Purpose: per SPEC §req 1/2/3/4/5/8, D-A4/D-C3/D-C5/D-D1..D-D6, RESEARCH Pitfalls 1/3/4/7/8, CONTEXT Open Question 2 (package placement).
Output: 1 orchestrator service + 1 input factory + 1 modified `GmailPreviewReadService` (expose the triage facade) + 1 optional worker adapter + 1 worker package-info; 4 Wave-0 worker tests verified GREEN; `NoGmailSendAllowedTest` re-verified GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/phases/04-triage-convergence-hero/04-AI-SPEC.md
@.planning/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TriageRuleEvaluationInputFactory — metadata-only Gmail fetch → RuleEvaluationInput facade</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/application/TriageRuleEvaluationInputFactory.java, backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java (the existing metadata-only fetch: `messageGetRequest` `format=metadata` + `fields=id,threadId,labelIds,internalDate,payload/headers`, `toPreviewMessage` header parsing → sanitized sender/domain/recipients/subject-excerpt/labels/categories/`hasAttachment`/`listUnsubscribePresent`/`newsletterIndicatorPresent`; `Clock` injection; `GoogleJsonResponseException` handling — expose a triage-friendly facade `fetchTriageInput(UUID tenantId, String gmailMessageId)` returning the same shape the rules `RuleEvaluator` consumes)
    - backend/core/src/main/java/com/zeromail/core/rules/service/RuleEvaluator.java + the `RuleEvaluationInput` type (what fields `RuleEvaluator.evaluate(...)` reads — the factory must produce exactly that)
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java (the `buildClientForTenant(...)` facade from 04-04 — the input factory uses it; no cipher reference from `core.triage`)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 1 (the metadata-only fetch shape `format=metadata, fields=id,threadId,labelIds,internalDate,payload/headers` — never bodies/snippets)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md "Reusable Assets" — `GmailPreviewReadService` "lift the fetch helper or expose a triage-friendly facade"; CLAUDE.md §Conventions #5 (privacy logging)
  </read_first>
  <action>
    Decide the cleanest placement: either (a) add `public TriageRuleEvaluationInput fetchTriageInput(UUID tenantId, String gmailMessageId)` directly to `GmailPreviewReadService` (it already owns the metadata-fetch + header-parse logic — recommended; minimal new surface) OR (b) create `com.zeromail.core.triage.application.TriageRuleEvaluationInputFactory` that injects `GmailPreviewReadService` + `GmailApiClientFactory` and composes the fetch. Either way: the fetch is `format=metadata` with `fields=id,threadId,labelIds,internalDate,payload/headers` ONLY — no bodies, no snippets (privacy invariant); the result is whatever `RuleEvaluator.evaluate(...)` consumes (`RuleEvaluationInput` — reuse the rules-domain type; the orchestrator passes it to `RuleEvaluator`). If `RuleEvaluationInput` is in `core.rules.domain`, the orchestrator (which has a `rules` Modulith edge) can construct/use it directly — the factory just supplies the Gmail-derived data. Translate `GoogleJsonResponseException` (mirror `GmailPreviewReadService`) — a 404 on the message means the message vanished before triage; log `event=triage_input_fetch_message_gone tenantId={} gmailMessageId={}` and let the orchestrator skip this message (no audit row). Log `event=triage_input_fetched tenantId={} gmailMessageId={}` — ids only. Whichever placement is chosen, prefer the one that adds the least new public surface and keeps the Gmail-client construction inside `core.gmail`; `core.triage.application.TriageRuleEvaluationInputFactory` is acceptable if it only orchestrates calls into `core.gmail`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && (grep -q "fetchTriageInput" backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java || test -f backend/core/src/main/java/com/zeromail/core/triage/application/TriageRuleEvaluationInputFactory.java)</automated>
  </verify>
  <acceptance_criteria>
    - A triage-facing facade exists (either `GmailPreviewReadService.fetchTriageInput(UUID, String)` or `TriageRuleEvaluationInputFactory.fetch(...)`) that builds the `RuleEvaluator`-consumable input from a `format=metadata, fields=id,threadId,labelIds,internalDate,payload/headers` Gmail fetch only.
    - No body/snippet fetch; `GoogleJsonResponseException`/404 handled (logs `event=triage_input_fetch_message_gone`, returns a sentinel/empty the orchestrator can skip on).
    - Gmail-client construction stays inside `core.gmail` (uses `GmailApiClientFactory.buildClientForTenant(...)` or `GmailPreviewReadService`'s existing client path).
    - `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>The metadata-only triage input facade exists; no email content is read.</done>
</task>

<task type="auto">
  <name>Task 2: TriageOrchestratorService — the @ApplicationModuleListener hero orchestration</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java, backend/worker/src/main/java/com/zeromail/worker/triage/TriageOrchestratorAdapter.java, backend/worker/src/main/java/com/zeromail/worker/triage/package-info.java, backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java</files>
  <read_first>
    - .planning/phases/04-triage-convergence-hero/04-RESEARCH.md §"System Architecture Diagram" (the full step list 1–N: ScopedValue rebind → `triage_paused` early-return → metadata fetch → load rules `display_order` → tri-state eval collecting DEFERRED → budget check → batched vs per-rule LLM → merge → per-proposal safety gate → sender-net check → audit PENDING → Gmail write OR shadow → markApplied/markFailed) + §"Code Examples" "Worker loop rebinding TenantContext" + "Tri-state evaluation + proposal merge" + Pitfalls 1 (drafts.create non-idempotent → two-phase loop), 3 (native SQL tenant scoping — already enforced in the repo from 04-02), 4 (ScopedValue rebind in the listener — use `TenantContext.runWith` from 04-01), 7 (shadow vs paused), 8 (LLM failure → DEFERRED-(error) → NOT_MATCHED, never default-false)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-A4 (`@ApplicationModuleListener` macro = `@Async + @Transactional(REQUIRES_NEW) + @TransactionalEventListener(AFTER_COMMIT)`; runs on a virtual thread; exceptions captured into `FailedEventPublications`; rebind tenant at listener entry), D-C3 (two-phase PENDING→APPLIED write order, step by step), D-C5 (stuck-PENDING reaper — at least a minimal version; this orchestrator just produces PENDING rows correctly, the reaper is plan 04-07), D-D1 (per-message all-rules batched default), D-D2 (per-rule fanout on token budget overrun, `CompletableFuture.allOf` on virtual threads), D-D3 (1 reserve per LLM call; N for fanout; 1 `TRIAGE_DETERMINISTIC` for pure-deterministic messages; BYOK bypasses), D-D5 (LLM failure → all nodes `DEFERRED-(error)`, treated as `NOT_MATCHED`; deterministic actions still fire; NO new top-level `decision=FAILED` from semantic failure alone), D-D6 (7s batched / 6s per-rule timeout via HTTP read-timeout, not `future.get`)
    - .planning/phases/04-triage-convergence-hero/04-PATTERNS.md the `TriageOrchestratorService` pattern-assignment section (the exact analog lines: `GmailHistoryProcessor.tick()` lines 28-35 for the ScopedValue rebind; `ActionProposalMerger.evaluateAndMerge` lines 40-69 for evaluate+merge; `RuleEvaluator.evaluate` lines 18-101 + `RuleEvaluationResult.deferred(nodeId, "semantic_intent_deferred")` line 97-99; `RuleRepository.findOrderedByTenantId` for the rule load; `GmailDeliveryProcessingService` log lines 73/97/103/111 for the content-free log idiom)
    - backend/core/src/main/java/com/zeromail/core/rules/service/RuleEvaluator.java + ActionProposalMerger.java + RuleRepository.java (the Phase 3 contracts the orchestrator calls — `findOrderedByTenantId`, `RuleEntity.getMatcherAst()` / `getActionIntents()`, `evaluate(...)` returning `MATCHED/NOT_MATCHED/DEFERRED`, `merge(...)` returning the ordered `List<ActionProposal>`; do NOT change Phase 3 behavior)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java + TriageAuditEntity.java + TriageActionResult.java + TriageActionArgsCanonicalizer.java + TriageDecision.java (from 04-02 — `insertAuditPendingIfAbsent` returning `Optional<UUID>`, `markApplied`/`markFailed`, the `args_hash` via canonicalizer, the `TriageActionResult` to persist as `action_args_json`, the `gmail_change_token` per D-B4)
    - backend/core/src/main/java/com/zeromail/core/triage/service/TriageSafetyPolicy.java + TriageGmailWriter.java + SenderSafetyNetService.java (from 04-04 — `gate(proposal)`, `applyLabel`/`archiveSkipInbox`/`saveDraft`, `isProtected(tenantId, senderEmail)`)
    - backend/core/src/main/java/com/zeromail/core/llm/service/LlmGateway.java#evaluateSemanticIntents + SemanticIntentRequest.java + TokenBudgetExceededException.java + LlmEvaluationFailedException.java (from 04-03 — the gateway contract + the two exceptions to catch)
    - backend/core/src/main/java/com/zeromail/core/billing/service/CreditLedger.java + CallSite.java (from 04-02 — `reserve(tenantId, callSite) → settle/release`; `TRIAGE_PLATFORM_LLM` / `TRIAGE_DETERMINISTIC`; BYOK bypass — mirror how `LlmGatewayImpl` detects BYOK, or reserve unconditionally and let the ledger no-op for BYOK tenants per the Phase 2C convention)
    - backend/core/src/main/java/com/zeromail/core/tenant/TenantContext.java#runWith + TenantService.java#isTriageShadowMode (from 04-01 / 04-02) + the existing `isTriagePaused` accessor; backend/worker/src/main/java/com/zeromail/worker/ZeroMailWorkerApplication.java (verify it scans `com.zeromail.core` so the listener bean is component-scanned — CONTEXT Open Question 2)
    - backend/worker/src/main/java/com/zeromail/worker/GmailHistoryProcessor.java (the worker-side ScopedValue rebind precedent) + worker/billing/CreditReserveWatchdogBatch.java (the scheduler/batch split — not needed here but read for the `@Transactional` proxy-boundary lesson, RESEARCH Pitfall 6)
    - backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java (re-verify GREEN after the orchestrator lands — the orchestrator never calls Gmail directly; it delegates to `TriageGmailWriter`)
    - Micrometer Timer idiom in the codebase (search for an existing `Timer` usage in `core.*` or `worker.*`) — wrap the orchestration in a `triage.duration` timer tagged per AI-SPEC §7
  </read_first>
  <action>
    Create `com.zeromail.core.triage.application.TriageOrchestratorService` (placement per CONTEXT Open Question 2 — `application/` per CONVENTIONS #2; the worker already scans `com.zeromail.core` so the listener bean is component-scanned). It exposes a `@org.springframework.modulith.events.ApplicationModuleListener void onMailMessageObserved(MailMessageObserved event)` (the macro = `@Async + @Transactional(REQUIRES_NEW) + @TransactionalEventListener(AFTER_COMMIT)`; runs on a virtual thread; exceptions propagate to be captured into `FailedEventPublications` — do NOT catch-all-and-swallow the listener body; let unexpected exceptions escape). Inside the listener, first line: `TenantContext.runWith(event.tenantId(), () -> orchestrate(event))` (the ScopedValue rebind across the `@Async` boundary — RESEARCH Pitfall 4). The private `orchestrate(MailMessageObserved event)`:
    1. Wrap the whole body in a `triage.duration` Micrometer timer (tags `tenant`-hashed where AI-SPEC §7 calls for it; do not tag with raw tenant id if the convention is hashed — follow the existing project metric-tagging convention).
    2. If `tenantService.isTriagePaused(tenantId)` → log `event=triage_skipped_paused tenantId={} gmailMessageId={}` and return (no audit row — D-A3/Pitfall 7).
    3. Fetch the metadata-only input via the task-1 facade; if the message is gone (404) → log + return (no audit row).
    4. `List<RuleEntity> rules = ruleRepository.findOrderedByTenantId(tenantId)` (enabled, `display_order`); if empty → log `event=triage_no_rules tenantId={}` and return.
    5. Parse each rule's `getMatcherAst()` / `getActionIntents()`; run `RuleEvaluator.evaluate(...)` per rule collecting `DEFERRED` results (the `SemanticIntentMatcher` nodes — `{nodeId, intent}`).
    6. If there is ≥1 DEFERRED semantic node across all matched rules: compute the token budget (`sanitized email tokens + Σ intent tokens + tool-schema overhead` — call into the gateway's pre-check, which raises `TokenBudgetExceededException`); attempt the per-message batched call `Map<String,Boolean> matches = llmGateway.evaluateSemanticIntents(CallSite.TRIAGE_PLATFORM_LLM, rawSanitizedContentSource, allIntentRequests)`; on `TokenBudgetExceededException` → fan out per-rule: for each rule with semantic intents, `CompletableFuture.supplyAsync(() -> llmGateway.evaluateSemanticIntents(CallSite.TRIAGE_PLATFORM_LLM, rawSanitizedContentSource, ruleIntents), virtualThreadExecutor)`, join all (N reserves total — D-D3); if a single rule's intents alone exceed the cap, mark that rule's nodes `DEFERRED-(error)` and continue with the rest; on `LlmEvaluationFailedException` or `SafetyViolationException` (for the batched call) → mark ALL semantic nodes `DEFERRED-(error)` with the opaque error class (D-D5); per-rule fanout failures mark only that rule's nodes `DEFERRED-(error)`. Action-gating treats `DEFERRED-(error)` as `NOT_MATCHED` — semantic-gated actions do NOT fire; deterministic-only actions still do. NO new top-level `decision=FAILED` from semantic failure alone (D-D5). NOTE: the gateway's `evaluateSemanticIntents` does the sanitization internally — the orchestrator passes the raw fetched content (or, if the fetched metadata input doesn't carry a content string suitable for the LLM, pass whatever Phase 3 `RuleEvaluationInput` exposes as the sanitizable text — coordinate with what 04-03's `LlmGatewayImpl.evaluateSemanticIntents` expects as `rawMessageContent`).
    7. If there are zero semantic nodes anywhere (pure-deterministic message): `creditLedger.reserve(tenantId, CallSite.TRIAGE_DETERMINISTIC)` once (cost defaults to 0 — D-D3); settle/release around the rest of the orchestration as appropriate (or settle immediately since there's no LLM cost — follow the `CreditLedger` lifecycle JavaDoc). BYOK tenants bypass per Phase 2C convention.
    8. `List<ActionProposal> proposals = actionProposalMerger.merge(...)` (resolving the DEFERRED nodes with the LLM results, treating `DEFERRED-(error)` as `NOT_MATCHED`) — the ordered candidate actions for this message.
    9. For each proposal, in order: (a) `RuleActionType gated = triageSafetyPolicy.gate(proposal)` — if it throws `TriageSafetyViolationException`, catch it, write an audit row with `decision=REJECTED_BY_SAFETY_POLICY` (gmail_change_token = null) and continue to the next proposal (do NOT abort the whole message); (b) for `ARCHIVE_SKIP_INBOX` and `SAVE_DRAFT` proposals (the destructive tiers), check `senderSafetyNetService.isProtected(tenantId, senderEmail)` — if protected, write an audit row with `decision=REJECTED_BY_SAFETY_NET` (gmail_change_token = null), skip the Gmail write, and continue; `APPLY_LABEL` is not gated by the sender net (lowest tier); (c) compute `byte[] argsHash = triageActionArgsCanonicalizer.canonicalHash(triageActionResult)` where `triageActionResult` is the `TriageActionResult` built from the proposal's `ActionIntent` (`Label`/`Archive`/`SaveDraft` — for `SaveDraft` the `instruction` comes from the rule's static draft instruction, NOT an LLM call; `draftId`/`threadId` are filled after the Gmail call); (d) two-phase write loop (D-C3): `Optional<UUID> reservedAuditId = triageAuditRepository.insertAuditPendingIfAbsent(tenantId, gmailMessageId, gmailThreadId, ruleId, ruleNameSnapshot, actionType.id(), argsHash, actionArgsJson, gmailChangeTokenJsonOrNull, reasonNodeIdEvidence, ...)`; if `reservedAuditId.isEmpty()` → row already exists from a prior delivery → log `event=triage_audit_already_present tenantId={} gmailMessageId={} ruleId={} actionType={}` and SKIP the Gmail call entirely (idempotent retry path); else log `event=triage_audit_reserved auditId={} tenantId={} gmailMessageId={} ruleId={} actionType={}`; (e) if `tenantService.isTriageShadowMode(tenantId)` → `triageAuditRepository.markShadowLogged(reservedAuditId.get(), tenantId)` (or write the row directly as `SHADOW_LOGGED` instead of `PENDING` — pick one; if the two-phase loop already inserted PENDING, transition it to SHADOW_LOGGED via a narrow `mark*` method; gmail_change_token stays null — D-B4/Pitfall 7), log `event=triage_action_shadow_logged auditId={} ...`, no Gmail call; (f) else (real apply): call the matching `TriageGmailWriter` method (`applyLabel` / `archiveSkipInbox` / `saveDraft` returning the `draftId`); on success → `triageAuditRepository.markApplied(reservedAuditId.get(), tenantId, gmailDraftIdOrNull)` (and update `gmail_change_token` to `{"addedLabelId": "..."}` / `{"removedLabelIds": ["INBOX"]}` / `null` for SAVE_DRAFT per D-B4 — if `markApplied` doesn't carry the change token, add a narrow update or set it on the PENDING insert and only flip `decision`/`applied_at` here), log `event=triage_action_applied auditId={} tenantId={} gmailMessageId={} ruleId={} actionType={}`; on Gmail exception → `triageAuditRepository.markFailed(reservedAuditId.get(), tenantId, exception.getClass().getSimpleName())`, log `event=triage_action_failed auditId={} ... errorClass={}` (opaque class name only), and continue to the next proposal.
    10. NO email content / subject / snippet / sender display name / drafted body / prompt / completion in ANY log line or audit field (privacy invariant — `reason` carries matcher node id + structured evidence ids only).
    If Spring AOT / `@ApplicationModuleListener` detection misbehaves in the worker for a bean defined in `core` (CONTEXT Open Question 2), add a thin `com.zeromail.worker.triage.TriageOrchestratorAdapter` `@Component` in `backend/worker` that itself carries the `@ApplicationModuleListener` and delegates to `TriageOrchestratorService` — verify via the integration test which boots the worker and dispatches an event. Create `com.zeromail.worker.triage.package-info.java` (one-line Javadoc: "Worker-side triage scheduled jobs and event-listener adapters."). Re-verify `NoGmailSendAllowedTest` is GREEN (the orchestrator delegates all Gmail writes to `TriageGmailWriter`; it never calls a Gmail write API directly).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava :backend:worker:compileJava 2>&1 | tail -3 && grep -q "ApplicationModuleListener" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && grep -q "insertAuditPendingIfAbsent" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && grep -q "evaluateSemanticIntents" backend/core/src/main/java/com/zeromail/core/triage/application/TriageOrchestratorService.java && ./gradlew :backend:core:test --tests "*ApplicationModulesTest" --tests "*NoGmailSendAllowedTest" 2>&1 | tail -3 && ./gradlew :backend:worker:test --tests "*TriageOrchestratorIntegrationContractTest" --tests "*TriageIdempotencyContractTest" --tests "*TriageShadowModeContractTest" --tests "*TriageCreditAccountingContractTest" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `TriageOrchestratorService` has an `@ApplicationModuleListener` method consuming `MailMessageObserved`; its first action is `TenantContext.runWith(event.tenantId(), ...)`; the listener body does not catch-and-swallow unexpected exceptions (so Modulith captures them into `FailedEventPublications`).
    - The orchestration: returns early on `isTriagePaused`; builds a metadata-only input; loads `findOrderedByTenantId`; tri-state evaluates; resolves DEFERRED semantic nodes via `LlmGateway.evaluateSemanticIntents` with per-message batched default + per-rule fanout on `TokenBudgetExceededException` + `DEFERRED-(error)` on `LlmEvaluationFailedException`/`SafetyViolationException` (treated as `NOT_MATCHED`); reserves `CallSite.TRIAGE_PLATFORM_LLM` once per LLM call (N for fanout) or `CallSite.TRIAGE_DETERMINISTIC` once for pure-deterministic messages; merges proposals; per proposal gates via `TriageSafetyPolicy` (REJECTED_BY_SAFETY_POLICY audit on violation), checks the sender net for ARCHIVE/SAVE_DRAFT (REJECTED_BY_SAFETY_NET audit when protected), runs the two-phase PENDING→APPLIED loop (skips the Gmail call when `insertAuditPendingIfAbsent` returns empty), writes SHADOW_LOGGED when `isTriageShadowMode`, else calls `TriageGmailWriter` and `markApplied`/`markFailed`.
    - No log line or audit field carries email content / subject / snippet / sender display name / drafted body / prompt / completion; `reason` is matcher node id + evidence ids only.
    - `com.zeromail.worker.triage.package-info.java` exists; the worker's component scan covers `com.zeromail.core` (verified by the integration test booting and processing an event); if needed, `TriageOrchestratorAdapter` is the worker-side `@ApplicationModuleListener` delegate.
    - `./gradlew :backend:core:compileJava :backend:worker:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*ApplicationModulesTest" --tests "*NoGmailSendAllowedTest"` PASS; `./gradlew :backend:worker:test --tests "*TriageOrchestratorIntegrationContractTest" --tests "*TriageIdempotencyContractTest" --tests "*TriageShadowModeContractTest" --tests "*TriageCreditAccountingContractTest"` all PASS (the Wave-0 contracts: event wiring + 2-rule deterministic control run + 1 audit row per applied action; replay → 1 audit row per action / ≤1 Gmail write per action; shadow → SHADOW_LOGGED + zero Gmail calls; 1 reserve per LLM call / 1 per pure-deterministic message).
    - `./gradlew :backend:core:test --tests "*MultiTenantLeakIntegrationTest"` still PASSES (FND-05).
  </acceptance_criteria>
  <done>The hero orchestrator is wired end-to-end (event → rules → LLM → safety → audit → Gmail/shadow) with the two-phase idempotent write loop, fail-loud LLM semantics, and content-free logging; the orchestrator + idempotency + shadow + credit-accounting Wave-0 contracts are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pub/Sub-redelivered event → triage actions | A redelivered `MailMessageObserved` must not double-act; the two-phase PENDING→APPLIED loop + the `triage_audit` unique index make re-triage of `(tenantId, gmailMessageId)` idempotent. |
| `@Async` listener → tenant isolation | The listener runs on a fresh virtual thread with no inherited ScopedValue; `TenantContext.runWith(event.tenantId(), ...)` rebinds before any tenant-scoped repository / LLM call. |
| sender content → LLM call | Untrusted content reaches the model via `evaluateSemanticIntents`; sanitization (Phase 2C) + the gateway's set-equality validator + the orchestrator's `DEFERRED-(error)`→`NOT_MATCHED` semantics contain the blast radius; a single-message batched call's cross-rule reach is the accepted v1 trade-off (D-D1). |
| rule proposals → Gmail writes | Every proposal passes `TriageSafetyPolicy` (allow-list) AND, for destructive tiers, `SenderSafetyNetService`; only then does the orchestrator call `TriageGmailWriter`. |
| listener failure → durability | Exceptions escape the `@ApplicationModuleListener` body and are captured into `FailedEventPublications`; the Wave-4 retry job resubmits incomplete publications — a worker crash mid-orchestration never silently loses an action. |
| shadow mode | When `isTriageShadowMode`, the orchestrator skips `TriageGmailWriter` entirely and records `SHADOW_LOGGED` with `gmail_change_token = null` — no Gmail state change occurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering (idempotency) | redelivered event → double-act | mitigate | Two-phase loop: `insertAuditPendingIfAbsent` (DB unique index `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)`) — empty `RETURNING` → skip the Gmail call; replay → 1 audit row per action, ≤1 Gmail write per action (Wave-0 `TriageIdempotencyContractTest`). |
| T-04-05-02 | Information Disclosure | tenant leak via `@Async` listener | mitigate | `TenantContext.runWith(event.tenantId(), ...)` rebinds the ScopedValue at listener entry before any tenant-scoped access; FND-05 `MultiTenantLeakIntegrationTest` re-verified GREEN; native `triage_audit` queries (from 04-02) carry the explicit tenant term. |
| T-04-05-03 | Elevation of Privilege | non-allow-listed / SEND action | mitigate | `TriageSafetyPolicy.gate(proposal)` rejects anything outside `{LABEL, ARCHIVE, SAVE_DRAFT}` → `decision=REJECTED_BY_SAFETY_POLICY` audit + no Gmail call; `RuleActionType.SEND` does not exist (`NoGmailSendAllowedTest`); the orchestrator never calls a Gmail write API directly — only via `TriageGmailWriter` (`TriageGmailWriteBoundaryTest`). |
| T-04-05-04 | Repudiation / mis-calibrated trust | silent LLM failure | mitigate | `LlmEvaluationFailedException` / `SafetyViolationException` from the gateway → all affected nodes `DEFERRED-(error)` with the opaque error class in the audit row; semantic-gated actions do not fire; deterministic-only actions still fire; booleans are NEVER defaulted to `false`; AI-SPEC eval Dim 3 is the build-breaking check. |
| T-04-05-05 | Information Disclosure | content in logs / audit / metrics | mitigate | Every triage log line is `event=triage_* tenantId={} gmailMessageId={} ruleId={} actionType={} ...` (ids + op names + opaque error classes only); `triage_audit.reason` = matcher node id + structured evidence ids; `triage.duration` metric tags follow the project's hashed-tenant convention — no raw content anywhere; cross-cutting privacy-sweep test in plan 04-08. |
| T-04-05-06 | Tampering / over-eager action | destructive action on a frequent sender | mitigate | `ARCHIVE_SKIP_INBOX` and `SAVE_DRAFT` proposals are gated by `SenderSafetyNetService.isProtected` (≥3 sent in 90d → protected) which fails SAFE on Gmail outage; protected → `decision=REJECTED_BY_SAFETY_NET` + no Gmail write; the `tenant_sender_opt_in` row is the user's explicit override. |
| T-04-05-07 | Denial of Service / SLO breach | unbounded orchestration latency | mitigate | LLM call timeouts 7s batched / 6s per-rule (HTTP read-timeout); `maxTokens(512)` output cap; the `triage.duration` timer surfaces p95/p99 against the 10s SLO; per-rule fanout runs in parallel on virtual threads so total latency stays bounded; `triage_paused` is the global kill-switch. |
</threat_model>

<verification>
- `./gradlew :backend:core:compileJava :backend:worker:compileJava` BUILD SUCCESSFUL.
- `./gradlew :backend:core:test --tests "*ApplicationModulesTest" --tests "*NoGmailSendAllowedTest" --tests "*MultiTenantLeakIntegrationTest"` all PASS.
- `./gradlew :backend:worker:test --tests "*TriageOrchestratorIntegrationContractTest" --tests "*TriageIdempotencyContractTest" --tests "*TriageShadowModeContractTest" --tests "*TriageCreditAccountingContractTest"` all PASS.
</verification>

<success_criteria>
- `TriageOrchestratorService` (`@ApplicationModuleListener`, tenant rebind, two-phase PENDING→APPLIED loop, batched-vs-fanout LLM, fail-loud `DEFERRED-(error)`, per-proposal safety-policy + sender-net gating, shadow-mode branch, content-free logging) exists and is wired in the worker.
- The metadata-only triage input facade exists; no email content is read.
- The orchestrator + idempotency + shadow + credit-accounting Wave-0 contracts are GREEN; `NoGmailSendAllowedTest` + `ApplicationModulesTest` + FND-05 still GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-05-SUMMARY.md`
</output>
