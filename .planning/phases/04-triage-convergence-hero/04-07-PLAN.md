---
phase: 04-triage-convergence-hero
plan: 07
type: execute
wave: 6
depends_on: [00, 01, 02, 04, 05]
files_modified:
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditPurgeJobContractTest.java
autonomous: true
requirements: [TRG-04, TRG-06]

must_haves:
  truths:
    - "TriageEventRetryJob resubmits BOTH incomplete AND failed Modulith event publications older than 5 minutes every 2 minutes, ShedLock-coordinated (a listener exception that lands in FailedEventPublications is not lost)"
    - "TriageEventCleanupJob deletes completed Modulith event publications older than 7 days daily, ShedLock-coordinated; logs a count of still-incomplete/failed publications it skipped so a stuck event is observable"
    - "TriageAuditPurgeJob deletes triage_audit rows whose decided_at is older than 30 days with purge-set decisions daily (decided_at, NOT applied_at — SHADOW_LOGGED / REJECTED_* rows have null applied_at), bounded LIMIT 1000 repeat-until-zero, ShedLock-coordinated, via a separate @Transactional batch collaborator"
    - "TriagePendingReaperJob flips PENDING rows whose last_attempt_at (or created_at if never leased) is older than the ABANDONED threshold of 30 MINUTES — NOT the saga's 2-minute retry-lease window — to FAILED (or APPLIED after an optional metadata verification), every 5 minutes, ShedLock-coordinated; the much-longer 30-min cutoff means a slow-but-live in-flight Gmail attempt (which still holds a fresh ≤2-min lease and is mid-retry) is never flipped to FAILED by the reaper — only genuinely-abandoned rows no delivery will ever revisit are reaped; PENDING rows never live forever; it uses only the whitelisted markFailed/markApplied transitions"
    - "All four jobs piggyback on the existing ShedLockConfig LockProvider; the worker component scan covers com.zeromail.worker.triage"
  artifacts:
    - path: "backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java"
      provides: "@Scheduled + @SchedulerLock job calling IncompleteEventPublications.resubmitIncompletePublicationsOlderThan"
      contains: "resubmitIncompletePublicationsOlderThan"
    - path: "backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java"
      provides: "@Transactional bounded-delete collaborator for the 30-day retention purge"
      contains: "Transactional"
    - path: "backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java"
      provides: "@Scheduled + @SchedulerLock job flipping ABANDONED (>30 min, not the 2-min retry lease) stuck PENDING audit rows"
      contains: "SchedulerLock"
  key_links:
    - from: "backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java"
      to: "Spring Modulith IncompleteEventPublications (+ the failed-publications view if distinct in this Modulith version)"
      via: "@Scheduled(fixedDelay=PT2M) + @SchedulerLock(name=triageEventRetry) -> resubmitIncompletePublicationsOlderThan(Duration.ofMinutes(5)) AND a resubmit of failed publications older than 5m"
      pattern: "IncompleteEventPublications"
    - from: "backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java"
      to: "TriageAuditPurgeBatch (separate @Component for the @Transactional proxy boundary)"
      via: "scheduler delegates to a batch collaborator, repeat-until-zero"
      pattern: "TriageAuditPurgeBatch"
    - from: "backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java"
      to: "TriageAuditRepository.findStuckPendingForReaping(cutoff, Pageable) + markFailed/markApplied"
      via: "rows decision=PENDING with last_attempt_at (or created_at) older than the 30-min ABANDONED cutoff (NOT the 2-min retry lease) -> markFailed (or markApplied after metadata verify)"
      pattern: "findStuckPendingForReaping"
---

<objective>
[Wave 6] Land the four `worker.triage` scheduled jobs (all ShedLock-coordinated, mirroring `CreditReserveWatchdog`): `TriageEventRetryJob` (`@Scheduled(fixedDelay="PT2M")` → resubmit BOTH incomplete AND failed Modulith publications older than 5 minutes), `TriageEventCleanupJob` (`@Scheduled(cron="0 0 3 * * *")` → `CompletedEventPublications.deletePublicationsOlderThan(Duration.ofDays(7))` + a `skippedOutstandingCount` observability log), `TriageAuditPurgeJob` + `TriageAuditPurgeBatch` (`@Scheduled(cron="0 0 4 * * *")`, bounded `LIMIT 1000` repeat-until-zero `JdbcTemplate` delete of `triage_audit` rows whose `decided_at` is older than 30 days with purge-set decisions, via a separate `@Transactional` collaborator per the `CreditReserveWatchdog`/`CreditReserveWatchdogBatch` split), and `TriagePendingReaperJob` + `TriagePendingReaperBatch` (`@Scheduled(fixedDelay="PT5M")`, flip `decision='PENDING'` rows whose `last_attempt_at`/`created_at` is older than the **ABANDONED threshold of 30 minutes** — deliberately MUCH longer than the saga's 2-minute retry-lease window so a slow-but-live in-flight Gmail attempt is never reaped — to `FAILED`, or `APPLIED` after an optional metadata verification on `gmailMessageId`, so `PENDING` rows never live forever; review concern #2). Uses the bounded `findStuckPendingForReaping(cutoff, Pageable)` SELECT 04-02 declares on `TriageAuditRepository` (the bounded purge delete lives on the worker batch via `JdbcTemplate`, not on the repo).

Purpose: per D-A5 (retry/cleanup via ShedLock), D-C5 (stuck-PENDING reaper — at minimum the flip-to-FAILED variant; the metadata-verify-to-APPLIED is the optional enhancement), D-E2 (audit retention purge job), D-E4 (ShedLock lock-key convention), RESEARCH Pitfall 6 (the scheduler/batch split for `@Transactional` proxy correctness), and the review findings: the cycle-2 HIGH #2 (the reaper's "abandoned" threshold (30 min) MUST be decoupled from — much longer than — the saga's 2-min retry lease so a slow-but-live in-flight Gmail attempt is never flipped to FAILED, plus the `lease_owner` token from 04-02 lets the reaper see who currently owns a row); Codex MEDIUM "failed Modulith publications are not retried" (the retry job resubmits failed publications too); Codex MEDIUM "purge timestamp is wrong for shadow/rejected rows" (purge on `decided_at`, not `applied_at`); OpenCode LOW "event retry window vs daily cleanup" (the `skippedOutstandingCount` log).
Output: 4 jobs + 2 batch collaborators; `TriageAuditRepository` extended ONLY with a stuck-PENDING SELECT (no `delete*`); the purge-job Wave-0 test verified GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TriageEventRetryJob + TriageEventCleanupJob (Modulith event-registry retry/cleanup, ShedLock-coordinated)</name>
  <files>backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java, backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java</files>
  <read_first>
    - backend/worker/src/main/java/com/zeromail/worker/billing/CreditReserveWatchdog.java (the `@Scheduled` + `@SchedulerLock(name=..., lockAtLeastFor=..., lockAtMostFor=...)` + Micrometer counter shape — the verbatim model; note the tighter `lockAtLeastFor` than the schedule interval guards against immediate re-entry)
    - backend/worker/src/main/java/com/zeromail/worker/billing/BillingIntentExpirySweeper.java (a cron-scheduled variant)
    - backend/worker/src/main/java/com/zeromail/worker/billing/ShedLockConfig.java (`@EnableSchedulerLock` + the `LockProvider` bean — already wired; the new jobs just register additional lock names)
    - backend/worker/src/main/java/com/zeromail/worker/triage/package-info.java (created in 04-05; the worker component scan `scanBasePackages = {"com.zeromail.worker", "com.zeromail.core"}` covers this package)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-A5 (retry: `@Scheduled(fixedDelay="PT2M")` → `IncompleteEventPublications.resubmitIncompletePublicationsOlderThan(Duration.ofMinutes(5))`, ShedLock name `triageEventRetry`, `lockAtLeastFor="PT30S"`, `lockAtMostFor="PT5M"`; cleanup: `@Scheduled(cron="0 0 3 * * *")` → `CompletedEventPublications.deletePublicationsOlderThan(Duration.ofDays(7))`, ShedLock name `triageEventCleanup`) + D-E4 (lock-key convention `<domain><Purpose>` camelCase) + the RESEARCH §"Standard Stack" note that `IncompleteEventPublications` / `CompletedEventPublications` come transitively from `spring-modulith-starter-jdbc` (added in 04-00)
    - .planning/phases/04-triage-convergence-hero/04-REVIEWS.md Codex MEDIUM "failed Modulith publications are not retried" (the retry job must also resubmit `FailedEventPublications`, not only `IncompleteEventPublications`) and Codex MEDIUM "purge timestamp is wrong for shadow/rejected rows" (use `decided_at` / `COALESCE(applied_at, created_at)`, not `applied_at` — `decided_at` is the column added in 04-02 Task 1 for exactly this) and OpenCode LOW "event retry window vs daily cleanup" (add a `triage_event_cleanup_skipped_incomplete_count` log so a stuck event is observable)
    - Spring Modulith Events reference (re-fetch via Context7 at execute time — `https://docs.spring.io/spring-modulith/reference/events.html`): the exact bean types and method signatures for the INCOMPLETE and FAILED publication views (in Spring Modulith 2.0.x the `EventPublicationRegistry` / the `IncompleteEventPublications` bean exposes incomplete publications; whether `FailedEventPublications` is a distinct bean OR failed publications surface through the same incomplete view depends on the version — CONFIRM at execute time and resubmit whichever view(s) actually hold listener-failure rows; if the version has only one view, that's fine — the requirement is "failed listener invocations get resubmitted", not "two beans must be injected"); confirm `spring.modulith.events.republish-outstanding-events-on-restart` stays OFF (D-A5 uses the explicit scheduled resubmit, not the restart republish); confirm `deletePublicationsOlderThan(Duration)` / a count of outstanding publications is queryable for the cleanup log line
  </read_first>
  <action>
    Create `com.zeromail.worker.triage.TriageEventRetryJob` `@Component`: inject the Modulith publication view(s) that hold not-yet-completed publications (transitive from the JDBC starter — `org.springframework.modulith.events.IncompleteEventPublications`, plus a `FailedEventPublications`-equivalent IF the project's Spring Modulith version exposes failed publications as a distinct view; CONFIRM at execute time per the read_first — if it's one view, inject one); `@Scheduled(fixedDelay = 120_000L)` (or `fixedDelayString="PT2M"` if the project uses that style) `@SchedulerLock(name = "triageEventRetry", lockAtLeastFor = "PT30S", lockAtMostFor = "PT5M") public void resubmitOutstandingPublications()` → call `incompleteEventPublications.resubmitIncompletePublicationsOlderThan(Duration.ofMinutes(5))` AND, if a separate failed-publications view exists, resubmit failed publications older than 5 minutes the same way (so a listener exception captured into `FailedEventPublications` — which is exactly the path `TriageOrchestratorService` exceptions take — is not lost; Codex MEDIUM); log `event=triage_event_retry_run` (and a count if the API returns one — ids only, no payloads); increment a `triage.event_retry.run` Micrometer counter (mirror `CreditReserveWatchdog`'s counter wiring).
    Create `com.zeromail.worker.triage.TriageEventCleanupJob` `@Component`: inject `org.springframework.modulith.events.CompletedEventPublications` (+ the incomplete/failed view for the observability count below); `@Scheduled(cron = "0 0 3 * * *")` `@SchedulerLock(name = "triageEventCleanup", lockAtLeastFor = "PT1M", lockAtMostFor = "PT30M") public void deleteCompletedPublications()` → call `completedEventPublications.deletePublicationsOlderThan(Duration.ofDays(7))`; before/after, count the still-outstanding (incomplete/failed) publications and log `event=triage_event_cleanup_run skippedOutstandingCount={}` (count only — no payloads — so a publication stuck in the retry-loop past the 3 AM run is observable; OpenCode LOW); increment a `triage.event_cleanup.run` counter and a `triage.event_cleanup.skipped_outstanding` gauge/counter by the count.
    Both jobs are thin schedulers — no `@Transactional` scan-and-mutate logic in them (the Modulith APIs handle their own transactions). Lock names follow the `<domain><Purpose>` camelCase convention (D-E4).
  </action>
  <verify>
    <automated>./gradlew :backend:worker:compileJava 2>&1 | tail -2 && grep -q "resubmitIncompletePublicationsOlderThan" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java && grep -q "deletePublicationsOlderThan" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java && grep -q "triageEventRetry" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java && grep -q "triageEventCleanup" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java && grep -q "skippedOutstanding\|skipped_outstanding\|Failed" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java</automated>
  </verify>
  <acceptance_criteria>
    - `TriageEventRetryJob` is a `@Component` with `@Scheduled(fixedDelay ~PT2M)` + `@SchedulerLock(name="triageEventRetry", lockAtLeastFor="PT30S", lockAtMostFor="PT5M")` that resubmits BOTH incomplete AND (where the Modulith version exposes them separately) failed publications older than 5 minutes; logs `event=triage_event_retry_run`.
    - `TriageEventCleanupJob` is a `@Component` with `@Scheduled(cron="0 0 3 * * *")` + `@SchedulerLock(name="triageEventCleanup", ...)` calling `CompletedEventPublications.deletePublicationsOlderThan(Duration.ofDays(7))` and logging `event=triage_event_cleanup_run skippedOutstandingCount={}` (count only).
    - Neither job carries `@Transactional` scan-and-mutate logic; both use the existing `ShedLockConfig` `LockProvider`.
    - `./gradlew :backend:worker:compileJava` BUILD SUCCESSFUL; the worker boots with the new jobs registered (verified by the orchestrator integration test from 04-05 or a smoke boot — no new scan config needed).
  </acceptance_criteria>
  <done>The Modulith event-registry retry (incomplete + failed) + cleanup (with the stuck-event observability count) jobs exist, ShedLock-coordinated, mirroring the billing watchdog pattern.</done>
</task>

<task type="auto">
  <name>Task 2: TriageAuditPurgeJob + TriageAuditPurgeBatch (30-day retention purge, bounded LIMIT 1000 repeat-until-zero)</name>
  <files>backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java, backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java, backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditPurgeJobContractTest.java</files>
  <read_first>
    - backend/worker/src/main/java/com/zeromail/worker/billing/CreditReserveWatchdog.java + CreditReserveWatchdogBatch.java (the scheduler/batch SPLIT — copy verbatim: the `@Component` scheduler with `@Scheduled` + `@SchedulerLock` delegates to a SEPARATE `@Component` collaborator whose method is `@Transactional`, so Spring's proxy fires and the bounded delete loop holds locks correctly; the `CreditReserveWatchdogBatch` JavaDoc explains exactly why — RESEARCH Pitfall 6)
    - backend/worker/src/main/java/com/zeromail/worker/billing/ShedLockConfig.java (the `LockProvider` — reused)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java + TriageAuditRepositoryBoundaryArchTest.java (from 04-02 — the repo's mutating-name method whitelist is `{insertAuditPendingIfAbsent, insertAuditTerminalIfAbsent, reclaimStalePending, markApplied, markFailed, markReverted, markShadowLogged}` and does NOT include any `delete*`/`purge*` method; so the bounded purge delete MUST NOT be a repo method — put the bounded-delete native SQL directly on `TriageAuditPurgeBatch` via a `JdbcTemplate` / `EntityManager.createNativeQuery(...)`, keeping `TriageAuditRepository` clean and the ArchUnit guard untouched. The purge predicate uses `decided_at` (the column added in 04-02 Task 1) NOT `applied_at` — `SHADOW_LOGGED` / `REJECTED_BY_SAFETY_NET` / `REJECTED_BY_SAFETY_POLICY` rows have null `applied_at` but a real `decided_at`, so `applied_at`-based purge would never reap them (Codex MEDIUM); `COALESCE(applied_at, decided_at, created_at)` is also acceptable but `decided_at` is the dedicated column.)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-E2 (the exact purge: `@Scheduled(cron="0 0 4 * * *")` `com.zeromail.worker.triage.TriageAuditPurgeJob`; deletes rows where the decision timestamp is older than 30 days AND `decision IN ('APPLIED','REVERTED','SHADOW_LOGGED','REJECTED_BY_SAFETY_NET','REJECTED_BY_SAFETY_POLICY','FAILED')` — D-E2's draft said `applied_at` but that misses the null-`applied_at` terminal rows, so use `decided_at`; bounded `LIMIT 1000` per tick + repeat-until-zero to avoid long-tx vacuum stalls; ShedLock name `triageAuditPurge`, `lockAtLeastFor="PT1M"`, `lockAtMostFor="PT30M"`) + D-E4 (lock-key convention)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 6 (a daily worker purges audit rows older than 30 days with purge-set decisions to honor the retention bound) + §"Acceptance Criteria" ("Daily worker purges `triage_audit` rows older than 30 days; verified by Testcontainers test with mocked clock")
    - backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditPurgeJobContractTest.java (Wave-0 — asserts rows older than 30d with purge-set decisions are deleted, mocked clock; this task makes it GREEN; the batch/job should accept an injected `Clock` so the cutoff is testable, or the test seeds rows with `applied_at` in the past)
  </read_first>
  <action>
    Create `com.zeromail.worker.triage.TriageAuditPurgeBatch` `@Component`: inject a `JdbcTemplate` (or `EntityManager`) for the native bounded delete — NOT `TriageAuditRepository` (its mutating-name whitelist from 04-02 does not include a `delete*`/`purge*` method, and adding one would force a coordinated ArchUnit-whitelist change; keeping the delete on the worker batch via `JdbcTemplate` keeps `TriageAuditRepository` clean and `TriageAuditRepositoryBoundaryArchTest` untouched) — plus an injected `Clock`. Method `int purgeExpiredOnce(int batchLimit)` `@Transactional`: compute `cutoff = clock.instant().minus(Duration.ofDays(30))`; execute the bounded delete `DELETE FROM triage_audit WHERE audit_id IN (SELECT audit_id FROM triage_audit WHERE decided_at < :cutoff AND decision IN ('APPLIED','REVERTED','SHADOW_LOGGED','REJECTED_BY_SAFETY_NET','REJECTED_BY_SAFETY_POLICY','FAILED') ORDER BY decided_at LIMIT :batchLimit)` (uses `decided_at` so the null-`applied_at` SHADOW_LOGGED / REJECTED_* rows are reaped too — Codex MEDIUM); return the rows-deleted count.
    Create `com.zeromail.worker.triage.TriageAuditPurgeJob` `@Component`: inject `TriageAuditPurgeBatch` + a Micrometer counter; `@Scheduled(cron = "0 0 4 * * *")` `@SchedulerLock(name = "triageAuditPurge", lockAtLeastFor = "PT1M", lockAtMostFor = "PT30M") public void purge()` → loop: `int deleted = batch.purgeExpiredOnce(1000); total += deleted;` repeat while `deleted == 1000` (repeat-until-zero/partial); log `event=triage_audit_purged totalDeleted={}` (count only — no row content); increment a `triage.audit_purge.rows_deleted` counter by `total`.
    No `TriageAuditRepositoryBoundaryArchTest` change is needed here — the delete lives on the batch via `JdbcTemplate`, not on the repo. Verify the Wave-0 `TriageAuditPurgeJobContractTest` now resolves and is GREEN.
  </action>
  <verify>
    <automated>./gradlew :backend:worker:compileJava :backend:core:compileJava 2>&1 | tail -3 && grep -q "triageAuditPurge" backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java && grep -q "Transactional" backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java && grep -q "decided_at" backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java && grep -q "1000" backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java && ./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest" 2>&1 | tail -2 && ./gradlew :backend:worker:test --tests "*TriageAuditPurgeJobContractTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriageAuditPurgeJob` is a `@Component` with `@Scheduled(cron="0 0 4 * * *")` + `@SchedulerLock(name="triageAuditPurge", lockAtLeastFor="PT1M", lockAtMostFor="PT30M")` that loops over `TriageAuditPurgeBatch.purgeExpiredOnce(1000)` repeat-until-zero/partial; logs `event=triage_audit_purged totalDeleted={}` (count only).
    - `TriageAuditPurgeBatch.purgeExpiredOnce(int)` is `@Transactional` on a SEPARATE `@Component` (proxy-boundary correctness), uses an injected `Clock` for the 30-day cutoff, and the bounded delete targets `decided_at < cutoff AND decision IN (the 6 purge-set values)` with `LIMIT 1000` — uses `decided_at`, NOT `applied_at`.
    - The bounded delete is a `JdbcTemplate`/native query localized to the batch (no `delete*`/`purge*` method added to `TriageAuditRepository`) — `TriageAuditRepositoryBoundaryArchTest` stays GREEN unchanged.
    - `./gradlew :backend:worker:compileJava :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest"` PASSES; `./gradlew :backend:worker:test --tests "*TriageAuditPurgeJobContractTest"` PASSES (Testcontainers + mocked clock: APPLIED rows >30d AND a SHADOW_LOGGED row with aged `decided_at` + null `applied_at` are both deleted; rows newer than 30d retained).
  </acceptance_criteria>
  <done>The 30-day audit retention purge job exists with the scheduler/batch split, the `decided_at`-based bounded delete (so null-`applied_at` terminal rows are reaped), ShedLock coordination, and the boundary ArchUnit guard still GREEN unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: TriagePendingReaperJob + TriagePendingReaperBatch (stuck-PENDING reaper — PENDING never lives forever)</name>
  <files>backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java, backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java</files>
  <read_first>
    - backend/worker/src/main/java/com/zeromail/worker/billing/CreditReserveWatchdog.java + CreditReserveWatchdogBatch.java (the scheduler/batch split — the model; `CreditReserveWatchdog` reaps stale credit reservations the same way this reaps stale PENDING audit rows)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java (from 04-02 — `markFailed(auditId, tenantId, failureReason)` already exists (WHERE `decision='PENDING'`), and 04-02 declares the bounded `findStuckPendingForReaping(@Param("cutoff") Instant cutoff, Pageable batchLimit)` SELECT for exactly this reaper (predicate `decision='PENDING' AND (last_attempt_at < :cutoff OR (last_attempt_at IS NULL AND created_at < :cutoff))`); 04-02 also added the `lease_owner` column the reaper can read/log; if a more precise bounded selection is needed, add another SELECT (not a delete/update — fine under the boundary ArchUnit) — but do NOT add a new `delete*`/`update*`-named method; the reaper uses `markFailed` (and optionally `markApplied`) for the flip — both are already on the 04-02 whitelist)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-C5 (the reaper: `@Scheduled(fixedDelay="PT5M")` `com.zeromail.worker.triage`; rows with `decision='PENDING'` whose `last_attempt_at` (or `created_at` if never leased) is older than the **ABANDONED threshold** are inspected; **the abandoned threshold is 30 MINUTES — NOT the saga's 2-minute retry-lease window** (review concern #2: a row whose lease is merely "stale" by the saga's standard, ≤30 min old, may still be in active retry by a worker that's slow on the Gmail call; only a row that no delivery has touched for a full 30 min is genuinely abandoned and safe to terminate — flipping it sooner risks marking a live Gmail attempt FAILED while it later succeeds, leaving Gmail changed but the audit row non-undoable); if the corresponding Gmail call is verifiable as "succeeded" via a metadata fetch on `gmailMessageId`, flip to `APPLIED`; otherwise flip to `FAILED`; ShedLock name `triagePendingReaper`; "Acceptable to defer the metadata-verify variant to a follow-up plan; but PENDING rows must NEVER live forever") + D-C4 (the ArchUnit "no UPDATE/DELETE outside revert" rule must allow the `PENDING → APPLIED/FAILED` transition — already handled in 04-02 by whitelisting `markApplied`/`markFailed`). NOTE the layering: the `TriageAuditSaga` (04-05) uses `reclaimStalePending` with a 2-min lease so a *redelivered* event reclaims and finishes a crashed-mid-write row promptly; this reaper is the LAST-RESORT backstop for rows that NO delivery will ever revisit (e.g. the message was permanently deleted from Gmail, or Pub/Sub gave up) — its 30-min cutoff is intentionally far longer than the saga's lease so the two never race.
    - .planning/phases/04-triage-convergence-hero/04-RESEARCH.md Open Question 3 ("include at least a minimal version (flip PENDING older than N minutes to FAILED without the Gmail re-verification, leaving the re-verification as the follow-up) so the invariant holds from day one") — here N = 30 minutes (the abandoned threshold), NOT the saga's 2-minute retry lease
    - .planning/phases/04-triage-convergence-hero/04-REVIEWS.md "Agreed Concerns" #2 ("2-minute reaper/lease can mark a live Gmail attempt FAILED" — the fix is exactly this: the reaper's "abandoned" cutoff (30 min) is decoupled from / much longer than the saga's retry lease (2 min), and the reaper logs/considers the `lease_owner` token; the metadata-verify-to-APPLIED enhancement additionally closes the saga's "stale-lease retry re-created a label/archive change" residual for label/archive, though not for SAVE_DRAFT)
    - backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java + GmailPreviewReadService.java (if implementing the optional metadata-verify-to-APPLIED variant: a `format=metadata` fetch on `gmailMessageId` to check whether the intended label change is present — but the minimal scope is just flip-to-FAILED; the metadata-verify path is OPTIONAL — pick the cut and document it)
  </read_first>
  <action>
    Reuse the bounded `findStuckPendingForReaping(@Param("cutoff") Instant cutoff, Pageable batchLimit)` SELECT 04-02 declares on `TriageAuditRepository` (predicate `decision = 'PENDING' AND (last_attempt_at < :cutoff OR (last_attempt_at IS NULL AND created_at < :cutoff))` — SELECT only, allowed under the boundary ArchUnit); if a more precise bounded selection is later needed, add another SELECT — never a new `delete*`/`update*`-named method (04-02's whitelist already covers `markFailed`/`markApplied`; do NOT add a `markRejectedBy*`).
    Create `com.zeromail.worker.triage.TriagePendingReaperBatch` `@Component`: inject `TriageAuditRepository`, an injected `Clock`, the abandoned-threshold as a configurable `@Value("${zeromail.triage.pending-abandoned-threshold:PT30M}") Duration abandonedThreshold` (default **30 minutes** — review concern #2: this MUST be much longer than the `TriageAuditSaga`'s 2-minute retry lease from 04-05 so a slow-but-live in-flight Gmail attempt is never selected and flipped to FAILED while it later succeeds), and (for the optional verify variant) `TriageGmailWriter` or `GmailPreviewReadService`. Method `int reapStuckPendingOnce(int batchLimit)` `@Transactional`: `cutoff = clock.instant().minus(abandonedThreshold)` (i.e. `now() - 30 min` by default — explicitly NOT the saga's 2-min lease window: a row whose lease is merely "stale" by the saga's 2-min standard is still a candidate for an in-flight retry on the next delivery; only a row untouched for the full 30-min abandoned window is genuinely orphaned); load up to `batchLimit` rows via `findStuckPendingForReaping(cutoff, PageRequest.of(0, batchLimit))`; for each — MINIMAL scope: `triageAuditRepository.markFailed(row.getAuditId(), row.getTenantId(), "STUCK_PENDING_REAPED_ABANDONED")` (the Gmail call may or may not have succeeded long ago; marking FAILED is the conservative choice and the user can re-trigger; document this — and note this is the LAST-RESORT backstop for rows no delivery will ever revisit; an in-flight retry from a redelivered event would have already claimed the row via `reclaimStalePending` within the 2-min lease, long before this 30-min cutoff); log `event=triage_pending_reaped_one auditId={} tenantId={} lastLeaseOwner={} lastAttemptAt={}` (the `lease_owner` token + timestamp — ids only, no email content); OPTIONAL enhancement (if the planner/executor judges it worthwhile and within budget): for `APPLY_LABEL`/`ARCHIVE_SKIP_INBOX` rows, do a `format=metadata` fetch on `gmailMessageId` and if the `gmail_change_token`'s label state is already present, `markApplied(row.getAuditId(), row.getTenantId(), null, changeTokenJson)` instead — this also closes the residual "stale-lease retry re-creates a label/archive change" gap (it cannot for `SAVE_DRAFT` — a duplicate draft is not detectable from metadata; document that residual); but the minimal flip-to-FAILED is sufficient to hold the invariant; return the count processed.
    Create `com.zeromail.worker.triage.TriagePendingReaperJob` `@Component`: inject `TriagePendingReaperBatch` + a counter; `@Scheduled(fixedDelay = 300_000L)` (`PT5M`) `@SchedulerLock(name = "triagePendingReaper", lockAtLeastFor = "PT1M", lockAtMostFor = "PT5M") public void reap()` → loop `int processed = batch.reapStuckPendingOnce(500); total += processed;` while `processed == 500` (bounded); log `event=triage_pending_reaped totalProcessed={}` (count only); increment a `triage.pending_reaper.rows_processed` counter by `total` (OpenCode suggestion — a counter, not just the log line, so a spike in reaped rows is alertable).
    Lock name follows the `<domain><Purpose>` convention (D-E4). Document in the SUMMARY: (a) the abandoned threshold value used (30 min default) and that it is decoupled from the saga's 2-min lease, (b) whether the optional metadata-verify-to-APPLIED variant was included or deferred to a follow-up.
  </action>
  <verify>
    <automated>./gradlew :backend:worker:compileJava :backend:core:compileJava 2>&1 | tail -3 && grep -q "triagePendingReaper" backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java && grep -q "Transactional" backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java && grep -q "abandonedThreshold\|pending-abandoned-threshold\|PT30M\|ofMinutes(30)" backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java && ! grep -q "ofMinutes(2)\b" backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java && grep -q "findStuckPendingForReaping" backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java && grep -q "findStuckPendingForReaping\|last_attempt_at\|lastAttemptAt" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java && ./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*ApplicationModulesTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriagePendingReaperJob` is a `@Component` with `@Scheduled(fixedDelay ~PT5M)` + `@SchedulerLock(name="triagePendingReaper", ...)` that loops over `TriagePendingReaperBatch.reapStuckPendingOnce(...)`; logs `event=triage_pending_reaped totalProcessed={}` (count only) and increments a `triage.pending_reaper.rows_processed` counter.
    - `TriagePendingReaperBatch.reapStuckPendingOnce(int)` is `@Transactional` on a SEPARATE `@Component`, uses an injected `Clock` plus a configurable `abandonedThreshold` (default **30 minutes** — explicitly NOT the saga's 2-minute retry lease; the cutoff is `now() - abandonedThreshold`), selects `decision='PENDING' AND (last_attempt_at < cutoff OR (last_attempt_at IS NULL AND created_at < cutoff))` rows via `findStuckPendingForReaping` (bounded), and flips each to `FAILED` (minimal scope) logging the row's `lease_owner` token — the metadata-verify-to-APPLIED variant is optional and documented either way.
    - The reaper's abandoned threshold (30 min) is strictly greater than the `TriageAuditSaga` retry-lease window (2 min) — a slow-but-live in-flight Gmail attempt that still holds a fresh lease is never selected by `findStuckPendingForReaping`, so it cannot be flipped to FAILED while it later succeeds (review concern #2).
    - `TriageAuditRepository` is used as-is — only the existing SELECT for stuck-PENDING rows (no new `delete*`/`update*`/`markRejectedBy*` method added by this plan); `TriageAuditRepositoryBoundaryArchTest` stays GREEN.
    - `./gradlew :backend:worker:compileJava :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*ApplicationModulesTest"` PASS.
  </acceptance_criteria>
  <done>The stuck-PENDING reaper exists (at minimum flip-to-FAILED, abandoned-threshold-aware via `last_attempt_at`/`created_at` with a 30-min default that is decoupled from the saga's 2-min retry lease), ShedLock-coordinated, so PENDING audit rows never live forever AND a slow-but-live in-flight Gmail attempt is never flipped to FAILED; the boundary + Modulith ArchUnit guards stay GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker crash mid-orchestration → durability | The Modulith event registry leaves an `INCOMPLETE` publication on crash; `TriageEventRetryJob` resubmits it so no action is silently lost. |
| event-registry growth → DB bloat | `TriageEventCleanupJob` removes completed publications after 7 days; bounded so it doesn't stall vacuum. |
| audit table growth / retention bound | `TriageAuditPurgeJob` enforces the 30-day retention with a bounded `LIMIT 1000` repeat-until-zero delete on a separate `@Transactional` collaborator (proxy-boundary correctness). |
| stuck PENDING audit rows | `TriagePendingReaperJob` flips PENDING rows older than the 30-minute ABANDONED threshold (not the saga's 2-min retry lease) to FAILED — a JVM crash between the Gmail call and the final `UPDATE` never leaves a permanently-PENDING row, and the long threshold guarantees a slow-but-live in-flight attempt is never reaped while it later succeeds. |
| multi-node scheduling | All four jobs are ShedLock-coordinated (single fire across nodes) on the existing `LockProvider`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Repudiation / lost action | worker crash mid-orchestration | mitigate | `TriageEventRetryJob` (`@Scheduled PT2M`, ShedLock `triageEventRetry`) resubmits BOTH incomplete AND failed Modulith publications older than 5m — a `TriageOrchestratorService` listener exception that lands in `FailedEventPublications` is retried, not lost (Codex MEDIUM); the durable registry + retry job is the "trust the product" safety net D-A1 demands. |
| T-04-07-02 | Denial of Service | event-registry / audit-table bloat | mitigate | `TriageEventCleanupJob` (`cron 0 0 3`) deletes completed publications > 7d and logs `skippedOutstandingCount` so a stuck event is observable (OpenCode LOW); `TriageAuditPurgeJob` (`cron 0 0 4`) deletes audit rows whose `decided_at` is > 30d with purge-set decisions in bounded `LIMIT 1000` `JdbcTemplate` batches repeat-until-zero — long-tx vacuum stalls avoided. |
| T-04-07-03 | Tampering / data lifecycle | undo after expiry | mitigate | The 30-day purge (keyed on `decided_at` so SHADOW_LOGGED / REJECTED_* rows with null `applied_at` are also reaped — Codex MEDIUM) removes rows past the window, so the retention bound is enforced by data lifecycle in addition to the `TriageUndoService` clock check — an audit row past 30d simply no longer exists to undo. |
| T-04-07-04 | Repudiation | permanently-PENDING audit row / live attempt flipped FAILED | mitigate | `TriagePendingReaperJob` (`@Scheduled PT5M`, ShedLock `triagePendingReaper`) flips `decision='PENDING'` rows whose `last_attempt_at` (or `created_at` if never leased) is older than the **30-minute ABANDONED threshold** — deliberately MUCH longer than the `TriageAuditSaga`'s 2-minute retry lease (review concern #2) — to `FAILED` (conservative), logging the row's `lease_owner`; the invariant "PENDING never lives forever" (D-C5) holds from day one, AND because the cutoff is far past the saga's lease window, a slow-but-live in-flight Gmail attempt (still holding a fresh ≤2-min lease, mid-retry on a redelivered event) is never selected — so the reaper can never mark a row FAILED while the original write later succeeds (no "Gmail changed but audit non-undoable" state). The optional metadata-verify-to-APPLIED is an enhancement that also closes the saga's "stale-lease retry re-creates a label/archive change" residual (it cannot for `SAVE_DRAFT` — documented). |
| T-04-07-05 | Tampering | scheduler/batch `@Transactional` proxy | mitigate | The purge and reaper put their `@Transactional` scan-and-mutate logic on SEPARATE `@Component` collaborators (`*Batch`), so Spring's proxy fires and the bounded loops hold locks correctly (RESEARCH Pitfall 6) — self-invocation would silently break the transaction. |
| T-04-07-06 | Information Disclosure | job log lines | mitigate | Every job log line is `event=triage_*_run/purged/reaped totalX={}` / `skippedOutstandingCount={}` — counts only, no row content, no email content, no payloads. |
| T-04-07-07 | Tampering | audit immutability vs purge/reaper | mitigate | The reaper uses only the whitelisted `markFailed`/`markApplied` transitions (WHERE `decision='PENDING'`) plus a SELECT; the purge's bounded delete is a `JdbcTemplate` on the batch (no `delete*`/`purge*` method on `TriageAuditRepository`) — `TriageAuditRepositoryBoundaryArchTest` (whitelist owned by 04-02) stays GREEN unchanged; no ad-hoc UPDATE/DELETE on `triage_audit`. |
</threat_model>

<verification>
- `./gradlew :backend:worker:compileJava :backend:core:compileJava` BUILD SUCCESSFUL.
- `./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*ApplicationModulesTest"` PASS.
- `./gradlew :backend:worker:test --tests "*TriageAuditPurgeJobContractTest"` PASSES (incl. a SHADOW_LOGGED row with aged `decided_at` + null `applied_at` being purged); the worker boots with all four jobs registered.
</verification>

<success_criteria>
- Four `worker.triage` jobs exist (event retry [incomplete + failed] + event cleanup [+ outstanding-count log] + audit purge [on `decided_at`] + pending reaper [abandoned-threshold-aware: 30-min default cutoff on `last_attempt_at`/`created_at`, decoupled from the saga's 2-min retry lease, logs `lease_owner`, increments a `triage.pending_reaper.rows_processed` counter]), all ShedLock-coordinated on the existing `LockProvider`, with the purge + reaper using the scheduler/batch `@Transactional` split.
- `TriageAuditRepository` gains only a bounded SELECT for stuck-PENDING rows (no `delete*`/`purge*` method — the purge delete is a `JdbcTemplate` on the batch); the boundary ArchUnit guard (owned by 04-02) stays GREEN unchanged.
- The purge-job Wave-0 contract is GREEN; `ApplicationModulesTest` still GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-07-SUMMARY.md`
</output>
