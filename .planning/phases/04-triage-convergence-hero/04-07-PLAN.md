---
phase: 04-triage-convergence-hero
plan: 07
type: execute
wave: 6
depends_on: [00, 01, 02, 04, 05]
files_modified:
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java
  - backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditPurgeJobContractTest.java
autonomous: true
requirements: [TRG-04, TRG-06]

must_haves:
  truths:
    - "TriageEventRetryJob resubmits incomplete Modulith event publications older than 5 minutes every 2 minutes, ShedLock-coordinated"
    - "TriageEventCleanupJob deletes completed Modulith event publications older than 7 days daily, ShedLock-coordinated"
    - "TriageAuditPurgeJob deletes triage_audit rows older than 30 days with purge-set decisions daily, bounded LIMIT 1000 repeat-until-zero, ShedLock-coordinated, via a separate @Transactional batch collaborator"
    - "TriagePendingReaperJob flips PENDING rows older than 2 minutes to FAILED (or APPLIED after a metadata verification), every 5 minutes, ShedLock-coordinated — PENDING rows never live forever"
    - "All four jobs piggyback on the existing ShedLockConfig LockProvider; the worker component scan covers com.zeromail.worker.triage"
  artifacts:
    - path: "backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java"
      provides: "@Scheduled + @SchedulerLock job calling IncompleteEventPublications.resubmitIncompletePublicationsOlderThan"
      contains: "resubmitIncompletePublicationsOlderThan"
    - path: "backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java"
      provides: "@Transactional bounded-delete collaborator for the 30-day retention purge"
      contains: "Transactional"
    - path: "backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java"
      provides: "@Scheduled + @SchedulerLock job flipping stuck PENDING audit rows"
      contains: "SchedulerLock"
  key_links:
    - from: "backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java"
      to: "Spring Modulith IncompleteEventPublications"
      via: "@Scheduled(fixedDelay=PT2M) + @SchedulerLock(name=triageEventRetry) -> resubmitIncompletePublicationsOlderThan(Duration.ofMinutes(5))"
      pattern: "IncompleteEventPublications"
    - from: "backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java"
      to: "TriageAuditPurgeBatch (separate @Component for the @Transactional proxy boundary)"
      via: "scheduler delegates to a batch collaborator, repeat-until-zero"
      pattern: "TriageAuditPurgeBatch"
    - from: "backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java"
      to: "TriageAuditRepository (PENDING-row selection + markFailed)"
      via: "rows decision=PENDING older than 2 min -> markFailed (or markApplied after metadata verify)"
      pattern: "PENDING"
---

<objective>
[Wave 6] Land the four `worker.triage` scheduled jobs (all ShedLock-coordinated, mirroring `CreditReserveWatchdog`): `TriageEventRetryJob` (`@Scheduled(fixedDelay="PT2M")` → `IncompleteEventPublications.resubmitIncompletePublicationsOlderThan(Duration.ofMinutes(5))`), `TriageEventCleanupJob` (`@Scheduled(cron="0 0 3 * * *")` → `CompletedEventPublications.deletePublicationsOlderThan(Duration.ofDays(7))`), `TriageAuditPurgeJob` + `TriageAuditPurgeBatch` (`@Scheduled(cron="0 0 4 * * *")`, bounded `LIMIT 1000` repeat-until-zero delete of `triage_audit` rows older than 30 days with purge-set decisions, via a separate `@Transactional` collaborator per the `CreditReserveWatchdog`/`CreditReserveWatchdogBatch` split), and `TriagePendingReaperJob` + `TriagePendingReaperBatch` (`@Scheduled(fixedDelay="PT5M")`, flip `decision='PENDING'` rows older than 2 minutes to `FAILED` — or `APPLIED` after a metadata verification on `gmailMessageId` — so `PENDING` rows never live forever). Adds the few `TriageAuditRepository` methods these jobs need (bounded-delete query + stuck-PENDING selection).

Purpose: per D-A5 (retry/cleanup via ShedLock), D-C5 (stuck-PENDING reaper — at minimum the flip-to-FAILED variant; the metadata-verify-to-APPLIED is the optional enhancement), D-E2 (audit retention purge job), D-E4 (ShedLock lock-key convention), RESEARCH Pitfall 6 (the scheduler/batch split for `@Transactional` proxy correctness).
Output: 4 jobs + 2 batch collaborators; `TriageAuditRepository` extended with the bounded-delete + stuck-PENDING queries; the purge-job Wave-0 test verified GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TriageEventRetryJob + TriageEventCleanupJob (Modulith event-registry retry/cleanup, ShedLock-coordinated)</name>
  <files>backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java, backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java</files>
  <read_first>
    - backend/worker/src/main/java/com/zeromail/worker/billing/CreditReserveWatchdog.java (the `@Scheduled` + `@SchedulerLock(name=..., lockAtLeastFor=..., lockAtMostFor=...)` + Micrometer counter shape — the verbatim model; note the tighter `lockAtLeastFor` than the schedule interval guards against immediate re-entry)
    - backend/worker/src/main/java/com/zeromail/worker/billing/BillingIntentExpirySweeper.java (a cron-scheduled variant)
    - backend/worker/src/main/java/com/zeromail/worker/billing/ShedLockConfig.java (`@EnableSchedulerLock` + the `LockProvider` bean — already wired; the new jobs just register additional lock names)
    - backend/worker/src/main/java/com/zeromail/worker/triage/package-info.java (created in 04-05; the worker component scan `scanBasePackages = {"com.zeromail.worker", "com.zeromail.core"}` covers this package)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-A5 (retry: `@Scheduled(fixedDelay="PT2M")` → `IncompleteEventPublications.resubmitIncompletePublicationsOlderThan(Duration.ofMinutes(5))`, ShedLock name `triageEventRetry`, `lockAtLeastFor="PT30S"`, `lockAtMostFor="PT5M"`; cleanup: `@Scheduled(cron="0 0 3 * * *")` → `CompletedEventPublications.deletePublicationsOlderThan(Duration.ofDays(7))`, ShedLock name `triageEventCleanup`) + D-E4 (lock-key convention `<domain><Purpose>` camelCase) + the RESEARCH §"Standard Stack" note that `IncompleteEventPublications` / `CompletedEventPublications` come transitively from `spring-modulith-starter-jdbc` (added in 04-00)
    - Spring Modulith Events reference (re-fetch via Context7 at execute time — `https://docs.spring.io/spring-modulith/reference/events.html`): the exact bean types (`IncompleteEventPublications`, `CompletedEventPublications`) and method signatures (`resubmitIncompletePublicationsOlderThan(Duration)`, `deletePublicationsOlderThan(Duration)`); confirm `spring.modulith.events.republish-outstanding-events-on-restart` stays OFF (D-A5 uses the explicit scheduled resubmit, not the restart republish)
  </read_first>
  <action>
    Create `com.zeromail.worker.triage.TriageEventRetryJob` `@Component`: inject `org.springframework.modulith.events.IncompleteEventPublications` (transitive from the JDBC starter); `@Scheduled(fixedDelay = 120_000L)` (or the duration-string form `fixedDelayString="PT2M"` if the project uses that style) `@SchedulerLock(name = "triageEventRetry", lockAtLeastFor = "PT30S", lockAtMostFor = "PT5M") public void resubmitIncompletePublications()` → call `incompleteEventPublications.resubmitIncompletePublicationsOlderThan(Duration.ofMinutes(5))`; log `event=triage_event_retry_run` (and a count if the API returns one — ids only, no payloads); increment a `triage.event_retry.run` Micrometer counter (mirror `CreditReserveWatchdog`'s counter wiring).
    Create `com.zeromail.worker.triage.TriageEventCleanupJob` `@Component`: inject `org.springframework.modulith.events.CompletedEventPublications`; `@Scheduled(cron = "0 0 3 * * *")` `@SchedulerLock(name = "triageEventCleanup", lockAtLeastFor = "PT1M", lockAtMostFor = "PT30M") public void deleteCompletedPublications()` → call `completedEventPublications.deletePublicationsOlderThan(Duration.ofDays(7))`; log `event=triage_event_cleanup_run`; increment a `triage.event_cleanup.run` counter.
    Both jobs are thin schedulers — no `@Transactional` scan-and-mutate logic in them (the Modulith APIs handle their own transactions). Lock names follow the `<domain><Purpose>` camelCase convention (D-E4).
  </action>
  <verify>
    <automated>./gradlew :backend:worker:compileJava 2>&1 | tail -2 && grep -q "resubmitIncompletePublicationsOlderThan" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java && grep -q "deletePublicationsOlderThan" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java && grep -q "triageEventRetry" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventRetryJob.java && grep -q "triageEventCleanup" backend/worker/src/main/java/com/zeromail/worker/triage/TriageEventCleanupJob.java</automated>
  </verify>
  <acceptance_criteria>
    - `TriageEventRetryJob` is a `@Component` with `@Scheduled(fixedDelay ~PT2M)` + `@SchedulerLock(name="triageEventRetry", lockAtLeastFor="PT30S", lockAtMostFor="PT5M")` calling `IncompleteEventPublications.resubmitIncompletePublicationsOlderThan(Duration.ofMinutes(5))`; logs `event=triage_event_retry_run`.
    - `TriageEventCleanupJob` is a `@Component` with `@Scheduled(cron="0 0 3 * * *")` + `@SchedulerLock(name="triageEventCleanup", ...)` calling `CompletedEventPublications.deletePublicationsOlderThan(Duration.ofDays(7))`; logs `event=triage_event_cleanup_run`.
    - Neither job carries `@Transactional` scan-and-mutate logic; both use the existing `ShedLockConfig` `LockProvider`.
    - `./gradlew :backend:worker:compileJava` BUILD SUCCESSFUL; the worker boots with the new jobs registered (verified by the orchestrator integration test from 04-05 or a smoke boot — no new scan config needed).
  </acceptance_criteria>
  <done>The Modulith event-registry retry + cleanup jobs exist, ShedLock-coordinated, mirroring the billing watchdog pattern.</done>
</task>

<task type="auto">
  <name>Task 2: TriageAuditPurgeJob + TriageAuditPurgeBatch (30-day retention purge, bounded LIMIT 1000 repeat-until-zero)</name>
  <files>backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java, backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java, backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditPurgeJobContractTest.java</files>
  <read_first>
    - backend/worker/src/main/java/com/zeromail/worker/billing/CreditReserveWatchdog.java + CreditReserveWatchdogBatch.java (the scheduler/batch SPLIT — copy verbatim: the `@Component` scheduler with `@Scheduled` + `@SchedulerLock` delegates to a SEPARATE `@Component` collaborator whose method is `@Transactional`, so Spring's proxy fires and the bounded delete loop holds locks correctly; the `CreditReserveWatchdogBatch` JavaDoc explains exactly why — RESEARCH Pitfall 6)
    - backend/worker/src/main/java/com/zeromail/worker/billing/ShedLockConfig.java (the `LockProvider` — reused)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java (from 04-02 — add a native bounded-delete: `@Modifying @Query` `DELETE FROM triage_audit WHERE audit_id IN (SELECT audit_id FROM triage_audit WHERE applied_at < :cutoff AND decision IN ('APPLIED','REVERTED','SHADOW_LOGGED','REJECTED_BY_SAFETY_NET','REJECTED_BY_SAFETY_POLICY','FAILED') ORDER BY applied_at LIMIT :batchLimit)` returning `int` (rows deleted) — NOTE this is a `DELETE` repo method, which the `TriageAuditRepositoryBoundaryArchTest` from 04-00 bans EXCEPT for the explicit narrow operations; either name it something the ArchUnit rule whitelists, or — better per the SPEC's "no UPDATE/DELETE outside revert" framing — keep the delete OUT of `TriageAuditRepository` and put the bounded-delete native query directly on the `TriageAuditPurgeBatch` via a `JdbcTemplate` / `EntityManager.createNativeQuery(...)`; CHECK the `TriageAuditRepositoryBoundaryArchTest` wording — if it bans ALL `delete*` repo methods, use the `JdbcTemplate`-on-batch approach; if it allows a `purgeExpiredBatch` exception, add it to both the repo and the ArchUnit whitelist in the same plan)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-E2 (the exact purge: `@Scheduled(cron="0 0 4 * * *")` `com.zeromail.worker.triage.TriageAuditPurgeJob`; deletes rows where `applied_at < now() - 30 days` AND `decision IN ('APPLIED','REVERTED','SHADOW_LOGGED','REJECTED_BY_SAFETY_NET','REJECTED_BY_SAFETY_POLICY','FAILED')`; bounded `LIMIT 1000` per tick + repeat-until-zero to avoid long-tx vacuum stalls; ShedLock name `triageAuditPurge`, `lockAtLeastFor="PT1M"`, `lockAtMostFor="PT30M"`) + D-E4 (lock-key convention)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 6 (a daily worker purges audit rows older than 30 days with purge-set decisions to honor the retention bound) + §"Acceptance Criteria" ("Daily worker purges `triage_audit` rows older than 30 days; verified by Testcontainers test with mocked clock")
    - backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditPurgeJobContractTest.java (Wave-0 — asserts rows older than 30d with purge-set decisions are deleted, mocked clock; this task makes it GREEN; the batch/job should accept an injected `Clock` so the cutoff is testable, or the test seeds rows with `applied_at` in the past)
  </read_first>
  <action>
    Create `com.zeromail.worker.triage.TriageAuditPurgeBatch` `@Component`: inject either `TriageAuditRepository` (if the boundary ArchUnit allows a whitelisted `purgeExpiredBatch` delete method — update the whitelist in the same plan if so) OR a `JdbcTemplate`/`EntityManager` for the native bounded delete (preferred if the ArchUnit rule bans all `delete*` repo methods — this keeps `TriageAuditRepository` clean and the delete localized to the worker batch), plus an injected `Clock`. Method `int purgeExpiredOnce(int batchLimit)` `@Transactional`: compute `cutoff = clock.instant().minus(Duration.ofDays(30))`; execute the bounded delete (`DELETE FROM triage_audit WHERE audit_id IN (SELECT audit_id FROM triage_audit WHERE applied_at IS NOT NULL AND applied_at < :cutoff AND decision IN ('APPLIED','REVERTED','SHADOW_LOGGED','REJECTED_BY_SAFETY_NET','REJECTED_BY_SAFETY_POLICY','FAILED') ORDER BY applied_at LIMIT :batchLimit)`); return the rows-deleted count.
    Create `com.zeromail.worker.triage.TriageAuditPurgeJob` `@Component`: inject `TriageAuditPurgeBatch` + a Micrometer counter; `@Scheduled(cron = "0 0 4 * * *")` `@SchedulerLock(name = "triageAuditPurge", lockAtLeastFor = "PT1M", lockAtMostFor = "PT30M") public void purge()` → loop: `int deleted = batch.purgeExpiredOnce(1000); total += deleted;` repeat while `deleted == 1000` (repeat-until-zero/partial); log `event=triage_audit_purged totalDeleted={}` (count only — no row content); increment a `triage.audit_purge.rows_deleted` counter by `total`.
    If the bounded-delete native query was added to `TriageAuditRepository`, update `TriageAuditRepositoryBoundaryArchTest` (from 04-00) to whitelist that one method name alongside `markApplied`/`markFailed`/`markReverted` — keep the test otherwise strict (no other `delete*`/`update*`). If the delete lives on the batch via `JdbcTemplate`, no ArchUnit change is needed.
    Verify the Wave-0 `TriageAuditPurgeJobContractTest` now resolves and is GREEN.
  </action>
  <verify>
    <automated>./gradlew :backend:worker:compileJava :backend:core:compileJava 2>&1 | tail -3 && grep -q "triageAuditPurge" backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java && grep -q "Transactional" backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java && grep -q "1000" backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java && ./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest" 2>&1 | tail -2 && ./gradlew :backend:worker:test --tests "*TriageAuditPurgeJobContractTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriageAuditPurgeJob` is a `@Component` with `@Scheduled(cron="0 0 4 * * *")` + `@SchedulerLock(name="triageAuditPurge", lockAtLeastFor="PT1M", lockAtMostFor="PT30M")` that loops over `TriageAuditPurgeBatch.purgeExpiredOnce(1000)` repeat-until-zero/partial; logs `event=triage_audit_purged totalDeleted={}` (count only).
    - `TriageAuditPurgeBatch.purgeExpiredOnce(int)` is `@Transactional` on a SEPARATE `@Component` (proxy-boundary correctness), uses an injected `Clock` for the 30-day cutoff, and the bounded delete targets `applied_at < cutoff AND decision IN (the 6 purge-set values)` with `LIMIT 1000`.
    - The bounded delete is either a whitelisted `TriageAuditRepository.purgeExpiredBatch(...)` method (with the ArchUnit whitelist updated in this plan) OR a `JdbcTemplate`/native query localized to the batch (no `delete*` repo method added) — `TriageAuditRepositoryBoundaryArchTest` stays GREEN either way.
    - `./gradlew :backend:worker:compileJava :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest"` PASSES; `./gradlew :backend:worker:test --tests "*TriageAuditPurgeJobContractTest"` PASSES (Testcontainers + mocked clock: rows >30d with purge-set decisions are deleted, others retained).
  </acceptance_criteria>
  <done>The 30-day audit retention purge job exists with the scheduler/batch split, bounded delete, ShedLock coordination, and the boundary ArchUnit guard still GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: TriagePendingReaperJob + TriagePendingReaperBatch (stuck-PENDING reaper — PENDING never lives forever)</name>
  <files>backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java, backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java</files>
  <read_first>
    - backend/worker/src/main/java/com/zeromail/worker/billing/CreditReserveWatchdog.java + CreditReserveWatchdogBatch.java (the scheduler/batch split — the model; `CreditReserveWatchdog` reaps stale credit reservations the same way this reaps stale PENDING audit rows)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java (from 04-02 — `markFailed(auditId, tenantId, failureReason)` already exists (WHERE `decision='PENDING'`); add a selection query for stuck PENDING rows: `@Query("SELECT a FROM TriageAuditEntity a WHERE a.decision = :pending AND a.createdAt < :cutoff")` or a native variant returning a bounded batch — this is a SELECT, not a delete/update, so it's fine under the boundary ArchUnit; the reaper uses `markFailed` for the flip)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-C5 (the reaper: `@Scheduled(fixedDelay="PT5M")` `com.zeromail.worker.triage`; rows with `decision='PENDING'` older than 2 minutes are inspected — if the corresponding Gmail call is verifiable as "succeeded" via a metadata fetch on `gmailMessageId`, flip to `APPLIED`; otherwise flip to `FAILED`; ShedLock name `triagePendingReaper`; "Acceptable to defer ... to a follow-up plan if the planner judges the failure window narrow enough; but PENDING rows must NEVER live forever") + D-C4 (the ArchUnit "no UPDATE/DELETE outside revert" rule must allow the `PENDING → APPLIED/FAILED` transition — already handled in 04-02 by whitelisting `markApplied`/`markFailed`)
    - .planning/phases/04-triage-convergence-hero/04-RESEARCH.md Open Question 3 ("include at least a minimal version (flip PENDING older than N minutes to FAILED without the Gmail re-verification, leaving the re-verification as the follow-up) so the invariant holds from day one")
    - backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java + GmailPreviewReadService.java (if implementing the optional metadata-verify-to-APPLIED variant: a `format=metadata` fetch on `gmailMessageId` to check whether the intended label change is present — but the minimal scope is just flip-to-FAILED; the metadata-verify path is OPTIONAL — pick the cut and document it)
  </read_first>
  <action>
    Add to `TriageAuditRepository` (from 04-02) a SELECT for stuck PENDING rows: `List<TriageAuditEntity> findStuckPendingOlderThan(@Param("cutoff") Instant cutoff, Pageable batchLimit)` (or a native bounded variant) — SELECT only, allowed under the boundary ArchUnit; do NOT add any new `delete*`/`update*`-named method.
    Create `com.zeromail.worker.triage.TriagePendingReaperBatch` `@Component`: inject `TriageAuditRepository`, an injected `Clock`, and (for the optional verify variant) `TriageGmailWriter` or `GmailPreviewReadService`. Method `int reapStuckPendingOnce(int batchLimit)` `@Transactional`: `cutoff = clock.instant().minus(Duration.ofMinutes(2))`; load up to `batchLimit` rows where `decision='PENDING' AND created_at < cutoff`; for each — MINIMAL scope: `triageAuditRepository.markFailed(row.getAuditId(), row.getTenantId(), "STUCK_PENDING_REAPED")` (the Gmail call may or may not have succeeded; marking FAILED is the conservative choice and the user can re-trigger; document this); OPTIONAL enhancement (if the planner/executor judges it worthwhile and within budget): for `APPLY_LABEL`/`ARCHIVE_SKIP_INBOX` rows, do a `format=metadata` fetch on `gmailMessageId` and if the change token's label state is already present, `markApplied(...)` instead — but this is OPT-IN; the minimal flip-to-FAILED is sufficient to hold the invariant; return the count processed.
    Create `com.zeromail.worker.triage.TriagePendingReaperJob` `@Component`: inject `TriagePendingReaperBatch` + a counter; `@Scheduled(fixedDelay = 300_000L)` (`PT5M`) `@SchedulerLock(name = "triagePendingReaper", lockAtLeastFor = "PT1M", lockAtMostFor = "PT5M") public void reap()` → loop `int processed = batch.reapStuckPendingOnce(500); total += processed;` while `processed == 500` (bounded); log `event=triage_pending_reaped totalProcessed={}` (count only); increment a `triage.pending_reaper.rows_processed` counter.
    Lock name follows the `<domain><Purpose>` convention (D-E4). Document in the SUMMARY whether the optional metadata-verify-to-APPLIED variant was included or deferred to a follow-up.
  </action>
  <verify>
    <automated>./gradlew :backend:worker:compileJava :backend:core:compileJava 2>&1 | tail -3 && grep -q "triagePendingReaper" backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperJob.java && grep -q "Transactional" backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java && grep -q "PENDING\|pending" backend/worker/src/main/java/com/zeromail/worker/triage/TriagePendingReaperBatch.java && ./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*ApplicationModulesTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriagePendingReaperJob` is a `@Component` with `@Scheduled(fixedDelay ~PT5M)` + `@SchedulerLock(name="triagePendingReaper", ...)` that loops over `TriagePendingReaperBatch.reapStuckPendingOnce(...)`; logs `event=triage_pending_reaped totalProcessed={}` (count only).
    - `TriagePendingReaperBatch.reapStuckPendingOnce(int)` is `@Transactional` on a SEPARATE `@Component`, uses an injected `Clock` for the 2-minute cutoff, selects `decision='PENDING' AND created_at < cutoff` rows (bounded), and flips each to `FAILED` (minimal scope) — the metadata-verify-to-APPLIED variant is optional and documented either way.
    - `TriageAuditRepository` gains only a SELECT for stuck-PENDING rows (no new `delete*`/`update*` method); `TriageAuditRepositoryBoundaryArchTest` stays GREEN.
    - `./gradlew :backend:worker:compileJava :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*ApplicationModulesTest"` PASS.
  </acceptance_criteria>
  <done>The stuck-PENDING reaper exists (at minimum flip-to-FAILED), ShedLock-coordinated, so PENDING audit rows never live forever; the boundary + Modulith ArchUnit guards stay GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker crash mid-orchestration → durability | The Modulith event registry leaves an `INCOMPLETE` publication on crash; `TriageEventRetryJob` resubmits it so no action is silently lost. |
| event-registry growth → DB bloat | `TriageEventCleanupJob` removes completed publications after 7 days; bounded so it doesn't stall vacuum. |
| audit table growth / retention bound | `TriageAuditPurgeJob` enforces the 30-day retention with a bounded `LIMIT 1000` repeat-until-zero delete on a separate `@Transactional` collaborator (proxy-boundary correctness). |
| stuck PENDING audit rows | `TriagePendingReaperJob` flips PENDING rows older than 2 minutes to FAILED — a JVM crash between the Gmail call and the final `UPDATE` never leaves a permanently-PENDING row. |
| multi-node scheduling | All four jobs are ShedLock-coordinated (single fire across nodes) on the existing `LockProvider`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Repudiation / lost action | worker crash mid-orchestration | mitigate | `TriageEventRetryJob` (`@Scheduled PT2M`, ShedLock `triageEventRetry`) calls `IncompleteEventPublications.resubmitIncompletePublicationsOlderThan(5m)` — the durable registry + retry job is the "trust the product" safety net D-A1 demands. |
| T-04-07-02 | Denial of Service | event-registry / audit-table bloat | mitigate | `TriageEventCleanupJob` (`cron 0 0 3`) deletes completed publications > 7d; `TriageAuditPurgeJob` (`cron 0 0 4`) deletes audit rows > 30d with purge-set decisions in bounded `LIMIT 1000` batches repeat-until-zero — long-tx vacuum stalls avoided. |
| T-04-07-03 | Tampering / data lifecycle | undo after expiry | mitigate | The 30-day purge removes `APPLIED`/`REVERTED`/... rows past the window, so the retention bound is enforced by data lifecycle in addition to the `TriageUndoService` clock check — an audit row past 30d simply no longer exists to undo. |
| T-04-07-04 | Repudiation | permanently-PENDING audit row | mitigate | `TriagePendingReaperJob` (`@Scheduled PT5M`, ShedLock `triagePendingReaper`) flips `decision='PENDING'` rows older than 2 minutes to `FAILED` (conservative) — the invariant "PENDING never lives forever" (D-C5) holds from day one; the optional metadata-verify-to-APPLIED is an enhancement, not a prerequisite. |
| T-04-07-05 | Tampering | scheduler/batch `@Transactional` proxy | mitigate | The purge and reaper put their `@Transactional` scan-and-mutate logic on SEPARATE `@Component` collaborators (`*Batch`), so Spring's proxy fires and the bounded loops hold locks correctly (RESEARCH Pitfall 6) — self-invocation would silently break the transaction. |
| T-04-07-06 | Information Disclosure | job log lines | mitigate | Every job log line is `event=triage_*_run/purged/reaped totalX={}` — counts only, no row content, no email content, no payloads. |
| T-04-07-07 | Tampering | audit immutability vs purge/reaper | mitigate | The reaper uses only the whitelisted `markFailed`/`markApplied` transitions (WHERE `decision='PENDING'`); the purge's bounded delete is either a whitelisted repo method or a `JdbcTemplate` on the batch — `TriageAuditRepositoryBoundaryArchTest` stays GREEN; no ad-hoc UPDATE/DELETE on `triage_audit`. |
</threat_model>

<verification>
- `./gradlew :backend:worker:compileJava :backend:core:compileJava` BUILD SUCCESSFUL.
- `./gradlew :backend:core:test --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*ApplicationModulesTest"` PASS.
- `./gradlew :backend:worker:test --tests "*TriageAuditPurgeJobContractTest"` PASSES; the worker boots with all four jobs registered.
</verification>

<success_criteria>
- Four `worker.triage` jobs exist (event retry + event cleanup + audit purge + pending reaper), all ShedLock-coordinated on the existing `LockProvider`, with the purge + reaper using the scheduler/batch `@Transactional` split.
- `TriageAuditRepository` gains only a bounded-SELECT for stuck-PENDING rows (and, if chosen, a whitelisted `purgeExpiredBatch` delete); the boundary ArchUnit guard stays GREEN.
- The purge-job Wave-0 contract is GREEN; `ApplicationModulesTest` still GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-07-SUMMARY.md`
</output>
