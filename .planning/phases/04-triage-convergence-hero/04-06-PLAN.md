---
phase: 04-triage-convergence-hero
plan: 06
type: execute
wave: 5
depends_on: [00, 02, 04]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/triage/application/TriageUndoService.java
  - backend/core/src/main/java/com/zeromail/core/triage/application/UndoAuditCommand.java
  - backend/core/src/main/java/com/zeromail/core/triage/application/UndoAuditResult.java
  - backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageAuditController.java
  - backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageTenantController.java
  - backend/api/src/main/java/com/zeromail/api/controllers/triage/SenderSafetyNetController.java
  - backend/api/src/main/java/com/zeromail/api/dto/triage/package-info.java
  - backend/api/src/main/java/com/zeromail/api/dto/triage/UndoAuditResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/triage/TriageShadowModeRequest.java
  - backend/api/src/main/java/com/zeromail/api/dto/triage/TriageShadowModeResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/triage/ProtectedSendersResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/triage/SenderOptInResponse.java
  - backend/api/src/main/java/com/zeromail/api/error/ErrorCodes.java
  - backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java
  - apps/web/i18n/messages/vi.json
  - apps/web/i18n/messages/en.json
  - apps/web/lib/api/schema.d.ts
  - backend/api/src/test/java/com/zeromail/api/controllers/triage/TriageUndoControllerContractTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/triage/TriageTenantControllerContractTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/triage/SenderSafetyNetControllerContractTest.java
  - backend/core/src/test/java/com/zeromail/core/triage/TriageUndoServiceContractTest.java
autonomous: true
requirements: [TRG-06, TRG-07, TRG-08]

must_haves:
  truths:
    - "POST /api/triage/audit/{auditId}/undo reverts an APPLIED action within 30 days (inverse Gmail call computed from gmail_change_token + decision=REVERTED + reverted_at), returns 409 TRIAGE_UNDO_ALREADY_DONE on a second call, 409 TRIAGE_UNDO_EXPIRED at 30d+1s, 409 TRIAGE_UNDO_UNSUPPORTED_ACTION on an unknown action type, 404 on a cross-tenant id, and only acts on the requesting tenant's own audit rows"
    - "PATCH /api/tenant/triage/shadow-mode {enabled: bool} flips tenants.triage_shadow_mode for the current tenant"
    - "GET /api/triage/sender-safety-net lists the senders triage actually flagged (sourced from tenant_protected_sender_observation via SenderSafetyNetService.listProtectedSenders), each marked optedIn or not — SPEC req 8 point c; POST /api/triage/sender-safety-net/{senderEmail}/opt-in persists an override that makes that sender no longer protected"
    - "TriageUndoService computes the inverse via an exhaustive switch on TriageActionResult (compile-time exhaustiveness preserves 'SEND forbidden'), reading the gmail_change_token written post-apply by markApplied for the label/archive inverses (and the draftId from action_args_json for SAVE_DRAFT); a new RuleActionType not present in TriageActionResult throws TriageAuditException.unsupportedActionType -> HTTP 409"
    - "New ErrorCodes (TRIAGE_UNDO_EXPIRED / TRIAGE_UNDO_ALREADY_DONE / TRIAGE_UNDO_UNSUPPORTED_ACTION / TRIAGE_SAFETY_VIOLATION) have vi + en i18n keys; pnpm i18n:check passes"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageUndoService.java"
      provides: "Compute-inverse + flip-decision undo service (tenant ownership, APPLIED-state, 30d window, exhaustive switch)"
      contains: "switch"
    - path: "backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageAuditController.java"
      provides: "POST /api/triage/audit/{auditId}/undo"
      contains: "undo"
    - path: "backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java"
      provides: "409 mappings for the 3 undo exceptions + 500 for TriageSafetyViolationException"
      contains: "TRIAGE_UNDO"
  key_links:
    - from: "backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageAuditController.java"
      to: "TriageUndoService.undo"
      via: "thin controller resolves tenantId from TenantContext, delegates to the core service, maps result to DTO"
      pattern: "triageUndoService\\.undo"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/application/TriageUndoService.java"
      to: "TriageGmailWriter inverse calls + TriageAuditRepository.markReverted"
      via: "exhaustive switch on TriageActionResult -> removeLabel / restoreToInbox / deleteDraft -> markReverted"
      pattern: "markReverted"
    - from: "backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java"
      to: "ErrorCodes.TRIAGE_UNDO_EXPIRED / TRIAGE_UNDO_ALREADY_DONE / TRIAGE_UNDO_UNSUPPORTED_ACTION / TRIAGE_SAFETY_VIOLATION"
      via: "@ExceptionHandler -> ProblemDetail with the dotted error code"
      pattern: "TRIAGE_SAFETY_VIOLATION"
---

<objective>
[Wave 5] Land the user-facing REST surface: `TriageUndoService` (compute-inverse + flip-decision, tenant-ownership + APPLIED-state + 30-day-window checks, exhaustive `switch` on `TriageActionResult` preserving "SEND forbidden", fail-loud `TRIAGE_UNDO_UNSUPPORTED_ACTION` on unknown action types), the three thin controllers under `backend/api/controllers/triage/` (`POST /api/triage/audit/{auditId}/undo`, `PATCH /api/tenant/triage/shadow-mode`, `GET /api/triage/sender-safety-net` + `POST /api/triage/sender-safety-net/{senderEmail}/opt-in`), the new DTO records, the four new `ErrorCodes` + `GlobalExceptionHandler` mappings (3× 409 undo + 1× 500 safety violation), the vi+en i18n keys, and the regenerated `apps/web/lib/api/schema.d.ts`.

Purpose: per SPEC §req 6/7/8, D-B3 (exhaustive switch as layer-2 validation), D-B6 (fail-loud on unknown action type → 409), D-E3 (sender opt-in endpoint), "Claude's Discretion" (DTO field names + i18n key spelling). Phase 4 ships backend + REST only — all triage UI is Phase 5.
Output: 1 undo service + 3 command/result records; 3 controllers + 1 dto package-info + 6 DTOs; ErrorCodes + GlobalExceptionHandler; vi+en i18n; schema.d.ts regen; the undo-controller / tenant-controller / sender-net-controller / undo-service Wave-0 tests verified GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TriageUndoService — compute-inverse + flip-decision (tenant ownership, APPLIED state, 30d window, exhaustive switch)</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/application/TriageUndoService.java, backend/core/src/main/java/com/zeromail/core/triage/application/UndoAuditCommand.java, backend/core/src/main/java/com/zeromail/core/triage/application/UndoAuditResult.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/service/RuleManagementService.java (the tenant-scoped service-owned `@Transactional` mutation pattern with ownership + version checks — the model for `TriageUndoService`)
    - backend/core/src/main/java/com/zeromail/core/rules/application/RuleUpdateCommand.java + RuleCompileCommand.java (the command-record idiom — the model for `UndoAuditCommand` / `UndoAuditResult`)
    - backend/core/src/main/java/com/zeromail/core/rules/domain/RuleEvaluator.java + ActionIntent.java#fromAction (the sealed-`switch` style — exhaustive `switch` over `TriageActionResult` so a future `RuleActionType` value not present in `TriageActionResult` is a compile-time gap, then a runtime fail-loud)
    - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java + TriageActionResultJsonValidator.java + TriageDecision.java (from 04-02 — parse `action_args_json` via the validator; the `TriageActionResult` sealed type to switch on; `TriageDecision.APPLIED` / `REVERTED`)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java + TriageAuditEntity.java (from 04-02 — `findByAuditIdAndTenantId(UUID, UUID)`, `markReverted(auditId, tenantId, revertedAt)` WHERE `decision='APPLIED'`; the `gmail_change_token` JSONB written post-apply by `markApplied(auditId, tenantId, externalRef, gmailChangeTokenJson)` is the source of truth for the inverse — `{"addedLabelId":...}` → `removeLabel`; `{"removedLabelIds":["INBOX"]}` → `restoreToInbox`; null for SAVE_DRAFT, where the `draftId` is `external_ref` and/or in `action_args_json`)
    - backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java (from 04-04 — the inverse calls `removeLabel` / `restoreToInbox` / `deleteDraft`)
    - backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java + backend/core/src/main/java/com/zeromail/core/triage/application/ProtectedSenderListItem.java (from 04-04 — `optInSender(tenantId, senderEmail)` + `listProtectedSenders(tenantId)` returning `List<ProtectedSenderListItem>` (`senderEmail`, `optedIn`) sourced from `tenant_protected_sender_observation` joined with `tenant_sender_opt_in`; `TriageUndoService` is the undo/audit service and does NOT own the sender-net list — the `SenderSafetyNetController` in Task 2 calls `SenderSafetyNetService.listProtectedSenders` directly and maps `ProtectedSenderListItem` (already declared by 04-04) to the GET-endpoint response)
    - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoExpiredException.java + TriageUndoAlreadyDoneException.java + TriageUndoUnsupportedActionException.java + TriageAuditException.java (from 04-02 — thrown by the undo service; `TriageAuditException.unsupportedActionType()` for the unknown-action-type case)
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java (the package-private `Clock` ctor pattern — `TriageUndoService` injects a `Clock` so the 30d-window check is mocked-clock-testable; mirror it)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 6 (the exact undo logic: tenant ownership → `APPLIED` state else 409 `TRIAGE_UNDO_ALREADY_DONE` → `applied_at >= now - 30d` else 409 `TRIAGE_UNDO_EXPIRED` → compute inverse → execute → flip `decision=REVERTED` + `reverted_at`; reverted entries remain in the table forever) + D-B3 layer 2 + D-B6 (unknown `RuleActionType` → `TriageAuditException.unsupportedActionType()` → 409 `TRIAGE_UNDO_UNSUPPORTED_ACTION`, fail-loud refusal, not silent no-op)
  </read_first>
  <action>
    Create `com.zeromail.core.triage.application.UndoAuditCommand` = `record UndoAuditCommand(UUID auditId, UUID tenantId) {}` and `UndoAuditResult` = `record UndoAuditResult(UUID auditId, TriageDecision decision, Instant revertedAt) {}` (or a richer result if the controller needs more — keep to what the DTO maps; project record-DTO convention). NOTE: `com.zeromail.core.triage.application.ProtectedSenderListItem` is CREATED IN 04-04 (the producer — `SenderSafetyNetService.listProtectedSenders` returns it); this plan only IMPORTS it for the `SenderSafetyNetController` → `ProtectedSendersResponse.from(...)` mapping in Task 2 — do NOT re-declare it here.
    Create `com.zeromail.core.triage.application.TriageUndoService` as a `@Component` injecting `TriageAuditRepository`, `TriageGmailWriter`, a `Clock` (package-private ctor for the mocked-clock test, mirror `GmailPreviewReadService`), and a Micrometer counter for `triage.audit.undo` (tags `action_type`, `days_after_apply_bucket` per AI-SPEC §7). Method `UndoAuditResult undo(UndoAuditCommand command)` in a service-owned `@Transactional`:
    1. `TriageAuditEntity row = triageAuditRepository.findByAuditIdAndTenantId(command.auditId(), command.tenantId()).orElseThrow(...)` — if absent (wrong tenant or non-existent id) throw a not-found-style exception (the controller maps to 404; do NOT leak whether the id exists for another tenant — a not-found is correct for both cases).
    2. If `row.getDecision()` (parsed via `TriageDecision.fromId`) is not `APPLIED` → throw `TriageUndoAlreadyDoneException` (covers `REVERTED`, `SHADOW_LOGGED`, `REJECTED_*`, `FAILED`, `PENDING` — none are undoable; the controller maps to 409 `TRIAGE_UNDO_ALREADY_DONE`).
    3. If `row.getAppliedAt()` is null or `row.getAppliedAt().isBefore(clock.instant().minus(Duration.ofDays(30)))` → throw `TriageUndoExpiredException` (controller maps to 409 `TRIAGE_UNDO_EXPIRED`).
    4. Parse the `action_type` to a `RuleActionType`; parse `action_args_json` to a `TriageActionResult` via `TriageActionResultJsonValidator`; parse `gmail_change_token` (if non-null) to a small change-token shape (`{"addedLabelId":"..."}` / `{"removedLabelIds":["INBOX"]}` — the JSONB written post-apply by `TriageAuditRepository.markApplied(...)`). Compute the inverse with an exhaustive `switch` on `TriageActionResult`: `case Label label -> triageGmailWriter.removeLabel(tenantId, gmailMessageId, addedLabelIdFromChangeToken)` — PREFER the `addedLabelId` value from `gmail_change_token` since that is literally "what we changed" per D-B4 (it survives a label rename better than the snapshot `label.labelId()`); fall back to `label.labelId()` only if `gmail_change_token` is null/missing the field (older row); `case Archive ignored -> triageGmailWriter.restoreToInbox(tenantId, gmailMessageId)` (the `{"removedLabelIds":["INBOX"]}` token confirms what to restore — no extra data needed beyond the message id); `case SaveDraft draft -> triageGmailWriter.deleteDraft(tenantId, draftIdFromExternalRefOrArgsJson)` (`draftId` is `external_ref` and/or `draft.draftId()` — they should agree; prefer `external_ref` since that is the Gmail-returned value written post-create). If the `RuleActionType` does not map to a `TriageActionResult` variant (a future v2 value persisted by a newer build) the parse will already fail loud via the validator; ALSO add an explicit guard so an unrecognized action type → `throw TriageAuditException.unsupportedActionType()` (controller maps to 409 `TRIAGE_UNDO_UNSUPPORTED_ACTION` — fail-loud refusal, NOT a silent no-op; D-B6). If `decision == APPLIED` but `gmail_change_token` is unexpectedly null for a `Label`/`Archive` row (a data invariant violation — `markApplied` should have set it) → also throw `TriageAuditException.unsupportedActionType()` rather than guessing; surface it loud.
    5. On a successful inverse Gmail call: `triageAuditRepository.markReverted(command.auditId(), command.tenantId(), clock.instant())` (the narrow `decision=APPLIED → REVERTED` transition from 04-02). Increment the undo counter. Log `event=triage_action_reverted auditId={} tenantId={} actionType={} daysAfterApply={}` — ids + bucketed days only, no content. Return `new UndoAuditResult(auditId, TriageDecision.REVERTED, revertedAt)`.
    6. On an inverse Gmail exception: do NOT flip the decision (the apply still stands); rethrow / wrap so the controller returns an appropriate error (a 502/500 — the action couldn't be undone right now; the user can retry); log `event=triage_action_revert_failed auditId={} errorClass={}` (opaque class only).
    NO email content / subject / snippet / sender display name / drafted body in any log line.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && grep -q "switch" backend/core/src/main/java/com/zeromail/core/triage/application/TriageUndoService.java && grep -q "markReverted" backend/core/src/main/java/com/zeromail/core/triage/application/TriageUndoService.java && grep -q "Duration.ofDays(30)\|ofDays( *30 *)" backend/core/src/main/java/com/zeromail/core/triage/application/TriageUndoService.java && ./gradlew :backend:core:test --tests "*TriageUndoServiceContractTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriageUndoService.undo(UndoAuditCommand)` is `@Transactional`, checks tenant ownership (not-found for wrong tenant), `decision == APPLIED` (else `TriageUndoAlreadyDoneException`), `applied_at >= clock.instant() - 30d` (else `TriageUndoExpiredException`), computes the inverse via an exhaustive `switch` on `TriageActionResult` (`Label → removeLabel`, `Archive → restoreToInbox`, `SaveDraft → deleteDraft`), throws `TriageAuditException.unsupportedActionType()` on an unrecognized action type, and on success calls `markReverted` + returns `UndoAuditResult(auditId, REVERTED, revertedAt)`.
    - The `Clock` is constructor-injected (package-private ctor) so the 30d-boundary is mocked-clock-testable.
    - `UndoAuditCommand` / `UndoAuditResult` records exist (`ProtectedSenderListItem` is declared by 04-04, referenced here).
    - No log line carries email content / subject / snippet / sender display name / drafted body.
    - `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageUndoServiceContractTest"` PASSES (within-window undo + 409 already-done + 409 expired at 30d+1s + 409 unsupported-action).
  </acceptance_criteria>
  <done>The undo service exists with tenant-ownership / state / 30d-window checks, exhaustive-switch inverse computation, and fail-loud unsupported-action behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Three thin triage controllers + DTOs + ErrorCodes + GlobalExceptionHandler mappings</name>
  <files>backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageAuditController.java, backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageTenantController.java, backend/api/src/main/java/com/zeromail/api/controllers/triage/SenderSafetyNetController.java, backend/api/src/main/java/com/zeromail/api/dto/triage/package-info.java, backend/api/src/main/java/com/zeromail/api/dto/triage/UndoAuditResponse.java, backend/api/src/main/java/com/zeromail/api/dto/triage/TriageShadowModeRequest.java, backend/api/src/main/java/com/zeromail/api/dto/triage/TriageShadowModeResponse.java, backend/api/src/main/java/com/zeromail/api/dto/triage/ProtectedSendersResponse.java, backend/api/src/main/java/com/zeromail/api/dto/triage/SenderOptInResponse.java, backend/api/src/main/java/com/zeromail/api/error/ErrorCodes.java, backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java</files>
  <read_first>
    - backend/api/src/main/java/com/zeromail/api/controllers/tenant/TriagePauseController.java (the verbatim thin-controller shape: `@RestController @Tag(name=...)`, inject the core service, `UUID tenantId = UUID.fromString(TenantContext.currentOrThrow())`, call the service, `log.info("event=...")`, return a DTO `from(...)` — the model for `TriageTenantController` PATCH shadow-mode and the structure of the others)
    - backend/api/src/main/java/com/zeromail/api/controllers/rules/RulesController.java (PathVariable + `@RequestBody @Valid` + GET-list + POST-mutation patterns — the model for `TriageAuditController` (PathVariable `auditId`, POST) and `SenderSafetyNetController` (GET list, POST with PathVariable `senderEmail`); controllers never inject repositories — Convention #1/#2; grouped under `controllers/triage/`, DTOs under `dto/triage/`)
    - backend/api/src/main/java/com/zeromail/api/dto/tenant/TriagePauseRequest.java + TriagePauseResponse.java (record + `static from(...)` — the model for the new DTO records); backend/api/src/main/java/com/zeromail/api/dto/package-info.java + dto/tenant/package-info.java (the `dto/<domain>/package-info.java` + `@NamedInterface` re-exposure pattern if the api module needs it)
    - backend/api/src/main/java/com/zeromail/api/error/ErrorCodes.java (the existing dotted-key constants `rules.*`, `llm.*` — add `TRIAGE_UNDO_EXPIRED`, `TRIAGE_UNDO_ALREADY_DONE`, `TRIAGE_UNDO_UNSUPPORTED_ACTION`, `TRIAGE_SAFETY_VIOLATION` as dotted keys following the same naming style — e.g. `triage.undo.expired` etc.; "Claude's Discretion" on exact spelling)
    - backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java (the existing `onRuleApiException` reason-switch → `ProblemDetail` via the `problem(...)` helper, `onSafetyViolation` → 500 — add `@ExceptionHandler` methods: `TriageUndoExpiredException` → 409 `triage.undo.expired`; `TriageUndoAlreadyDoneException` → 409 `triage.undo.already_done`; `TriageUndoUnsupportedActionException` (or `TriageAuditException` with reason `UNSUPPORTED_ACTION_TYPE`) → 409 `triage.undo.unsupported_action`; `TriageSafetyViolationException` → 500 `triage.safety_violation` — never leak exception detail in the ProblemDetail)
    - backend/core/src/main/java/com/zeromail/core/triage/application/TriageUndoService.java (from task 1 — `undo(UndoAuditCommand)`); backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java#setTriageShadowMode/isTriageShadowMode (from 04-02); backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java#optInSender/listProtectedSenders (from 04-04); backend/core/src/main/java/com/zeromail/core/triage/domain/SenderEmailCanonicalizer.java (from 04-02 — `SenderSafetyNetController` injects it to hash the opt-in log line and canonicalize the echoed response value)
    - apps/web/i18n/messages/vi.json + en.json (the `errors.*` namespace — add a vi+en key for each new dotted error code; must pass `pnpm i18n:check` / `I18nArchUnitTest`); the existing `errors.rules.*` / `errors.llm.*` entries for tone/wording
    - the `springdoc-openapi` Gradle task that emits the spec (the existing `generate:api` / hermetic spec-emit task — after the new controllers land, regenerate `apps/web/lib/api/schema.d.ts`); STATE.md decision "springdoc-openapi-gradle-plugin 1.9.0 hermetic spec emit"
  </read_first>
  <action>
    Create `backend/api/src/main/java/com/zeromail/api/dto/triage/package-info.java` (mirror `dto/tenant/package-info.java` — include the `@NamedInterface` re-exposure if the api module's Modulith config requires it). Create the DTO records (project record-DTO convention, `static from(...)` where a mapping is needed): `UndoAuditResponse(UUID auditId, String decision, Instant revertedAt)` with `static from(UndoAuditResult)`; `TriageShadowModeRequest(boolean enabled)` (with `@NotNull` on `enabled` if the Boolean is boxed — match the `TriagePauseRequest` validation idiom); `TriageShadowModeResponse(boolean enabled)` with `static from(boolean)`; `ProtectedSendersResponse(List<...> senders)` (each entry carrying `senderEmail` + `optedIn`) with `static from(List<ProtectedSenderListItem>)`; `SenderOptInResponse(String senderEmail, boolean optedIn)` with `static from(...)`. Exact field names are Claude's discretion within the convention.
    Create `backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageAuditController.java` `@RestController @Tag(name = "triage")`: `@PostMapping("/triage/audit/{auditId}/undo") public UndoAuditResponse undo(@PathVariable UUID auditId)` → `UUID tenantId = UUID.fromString(TenantContext.currentOrThrow()); UndoAuditResult result = triageUndoService.undo(new UndoAuditCommand(auditId, tenantId)); log.info("event=triage_undo_requested tenantId={} auditId={}", tenantId, auditId); return UndoAuditResponse.from(result);` (thin — no repository injection).
    Create `backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageTenantController.java` `@RestController @Tag(name = "tenant")`: `@PatchMapping("/tenant/triage/shadow-mode") public TriageShadowModeResponse setShadowMode(@RequestBody @Valid TriageShadowModeRequest request)` → resolve `tenantId`, `tenantService.setTriageShadowMode(tenantId, request.enabled())`, `log.info("event=triage_shadow_mode_toggled tenantId={} enabled={}", tenantId, request.enabled())`, `return TriageShadowModeResponse.from(request.enabled())` (verbatim twin of `TriagePauseController`).
    Create `backend/api/src/main/java/com/zeromail/api/controllers/triage/SenderSafetyNetController.java` `@RestController @Tag(name = "triage")`: `@GetMapping("/triage/sender-safety-net") public ProtectedSendersResponse listProtectedSenders()` → resolve `tenantId`, `return ProtectedSendersResponse.from(senderSafetyNetService.listProtectedSenders(tenantId))` — the data source is `tenant_protected_sender_observation` (the senders triage actually flagged) joined with `tenant_sender_opt_in`, returned as `List<ProtectedSenderListItem(senderEmail, optedIn)>` (SPEC §req 8 point c — the endpoint lists triage-flagged senders, not just opt-ins). `@PostMapping("/triage/sender-safety-net/{senderEmail}/opt-in") public SenderOptInResponse optIn(@PathVariable String senderEmail)` → resolve `tenantId`, `senderSafetyNetService.optInSender(tenantId, senderEmail)` (the service canonicalizes the address; if it is malformed the service throws `IllegalArgumentException` → map to a 400 in the handler), `log.info("event=triage_sender_opt_in tenantId={} senderEmailHash={}", tenantId, senderEmailCanonicalizer.redisCacheKeyComponent(senderEmailCanonicalizer.canonicalize(senderEmail)))` (NEVER log the raw sender address — privacy invariant; use the same hashing the service uses), `return SenderOptInResponse.from(senderEmailCanonicalizer.canonicalize(senderEmail), true)` (the response echoes the canonicalized address the caller supplied; the LOG stays hashed). NOTE: prefer the path-variable form `{senderEmail}` as the SPEC specifies it; if a `+`/`%`-encoding concern arises with email-in-path, the controller can additionally accept the address as a `?senderEmail=` query param — but the SPEC's path form is the contract, so keep it and just `@PathVariable` it (Spring URL-decodes it). (Codex suggested "prefer request body/query param over a path variable" — but the SPEC line 60 fixes the path form `POST /api/triage/sender-safety-net/{senderEmail}/opt-in`, so keep it; the hardening is in `SenderEmailCanonicalizer.canonicalize` rejecting malformed input, not in moving the parameter.)
    Modify `backend/api/src/main/java/com/zeromail/api/error/ErrorCodes.java`: add `TRIAGE_UNDO_EXPIRED` (= `"triage.undo.expired"`), `TRIAGE_UNDO_ALREADY_DONE` (= `"triage.undo.already_done"`), `TRIAGE_UNDO_UNSUPPORTED_ACTION` (= `"triage.undo.unsupported_action"`), `TRIAGE_SAFETY_VIOLATION` (= `"triage.safety_violation"`) as dotted-key constants (style-matched to the existing ones).
    Modify `backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java`: add `@ExceptionHandler` methods returning `ProblemDetail` via the existing `problem(...)` helper — `TriageUndoExpiredException` → 409 + `ErrorCodes.TRIAGE_UNDO_EXPIRED`; `TriageUndoAlreadyDoneException` → 409 + `ErrorCodes.TRIAGE_UNDO_ALREADY_DONE`; `TriageAuditException` (or `TriageUndoUnsupportedActionException`) when the reason is `UNSUPPORTED_ACTION_TYPE` → 409 + `ErrorCodes.TRIAGE_UNDO_UNSUPPORTED_ACTION`; `TriageSafetyViolationException` → 500 + `ErrorCodes.TRIAGE_SAFETY_VIOLATION` (mirror the existing `onSafetyViolation` — generic detail, no exception text). Also map the not-found-style exception from `TriageUndoService` to 404 if it isn't already covered by a generic handler, and `IllegalArgumentException` from `SenderSafetyNetService.optInSender` (malformed sender address from `SenderEmailCanonicalizer.canonicalize`) to a 400 with a generic detail (a validation-style error code — reuse the existing validation error code/handler if there is one rather than inventing a new triage-specific one) — never echo the rejected address in the ProblemDetail.
    Add vi+en i18n keys for each new error code under `apps/web/i18n/messages/vi.json` and `en.json` (the `errors.*` namespace — e.g. `errors.triage.undo.expired`, `errors.triage.undo.alreadyDone`, `errors.triage.undo.unsupportedAction`, `errors.triage.safetyViolation`; exact key spelling + copy is Claude's discretion, Vietnamese-first; lock-step parity so `pnpm i18n:check` passes — update `EN_SCAN_FILES` in `scripts/check-i18n.ts` only if a new source file needs scanning, which it likely does not since these are error-map keys).
    Run the `springdoc-openapi` hermetic spec-emit task and regenerate `apps/web/lib/api/schema.d.ts` so the new triage endpoints are in the typed client (Phase 5 consumes it). If the codegen requires a running backend / hermetic task fails in the sandbox, commit the regenerated `schema.d.ts` from the hermetic task output if available, else surface in the SUMMARY with the replay command (mirror prior phases' deferral pattern).
  </action>
  <verify>
    <automated>./gradlew :backend:api:compileJava 2>&1 | tail -2 && grep -q "triage/audit/{auditId}/undo\|/triage/audit/" backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageAuditController.java && grep -q "triage/shadow-mode\|/tenant/triage/shadow-mode" backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageTenantController.java && grep -q "TRIAGE_SAFETY_VIOLATION\|TRIAGE_UNDO_EXPIRED" backend/api/src/main/java/com/zeromail/api/error/ErrorCodes.java && grep -q "TRIAGE_UNDO" backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java && cd apps/web && pnpm i18n:check 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - Three controllers exist under `backend/api/src/main/java/com/zeromail/api/controllers/triage/` — `TriageAuditController` (`POST /api/triage/audit/{auditId}/undo`), `TriageTenantController` (`PATCH /api/tenant/triage/shadow-mode`), `SenderSafetyNetController` (`GET /api/triage/sender-safety-net` + `POST /api/triage/sender-safety-net/{senderEmail}/opt-in`); all thin (resolve `tenantId` from `TenantContext`, delegate to a core service, no repository injection); the sender-opt-in log line carries a hashed/id-only sender, never the raw address.
    - 5 DTO records + `dto/triage/package-info.java` exist; DTOs use `static from(...)` mappers; request DTOs use `@Valid`-friendly validation.
    - `ErrorCodes` has the 4 new dotted constants; `GlobalExceptionHandler` maps the 3 undo exceptions → 409 (with the matching code) and `TriageSafetyViolationException` → 500 (`triage.safety_violation`), with generic ProblemDetail detail (no exception text).
    - vi+en i18n bundles have a key for each new error code; `pnpm i18n:check` PASSES.
    - `apps/web/lib/api/schema.d.ts` is regenerated to include the new triage endpoints (or the deferral is documented with a replay command).
    - `./gradlew :backend:api:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:api:test --tests "*TriageUndoControllerContractTest" --tests "*TriageTenantControllerContractTest" --tests "*SenderSafetyNetControllerContractTest"` all PASS (200 within window / 409 already-done / 409 expired / 409 unsupported-action / 404 cross-tenant; PATCH flips the flag; GET lists the senders triage flagged from `tenant_protected_sender_observation` with the `optedIn` flag; POST persists the override + a malformed sender → 400 with no echoed address).
  </acceptance_criteria>
  <done>The triage REST surface (undo + shadow-mode + sender-safety-net) exists with thin controllers, DTOs, error codes, exception handlers, vi+en i18n, and a regenerated typed client; the api-side Wave-0 contracts are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → undo endpoint | A user may only undo their own tenant's audit rows; a cross-tenant `auditId` must look identical to a non-existent one (no existence leak). |
| undo retention window | Undo is permitted only within 30 days and only for `APPLIED` rows; outside that → 409, not a partial / silent action. |
| unknown action type → undo | A future v2 `RuleActionType` persisted by a newer build must fail loud (`409 TRIAGE_UNDO_UNSUPPORTED_ACTION`), never silent no-op — the exhaustive `switch` makes the gap a compile-time signal too. |
| error responses → client | `ProblemDetail` carries a machine-readable dotted code only; no exception text, stack trace, or PII; the frontend localizes via i18n. |
| sender email in logs vs response | The opt-in endpoint echoes the CANONICALIZED caller-supplied sender email in the response body but MUST NOT log the raw address — the log line carries `SenderEmailCanonicalizer.redisCacheKeyComponent(canonical)` (a hash), the same form the sender-net service uses (privacy invariant); a malformed address is rejected by `canonicalize` → 400 with no echoed value. |
| sender-safety-net list source | `GET /api/triage/sender-safety-net` reads `tenant_protected_sender_observation` (the senders triage actually flagged), joined with `tenant_sender_opt_in` for the `optedIn` flag — SPEC §req 8 point c; not just the opt-in table. |
| controllers → repositories | Thin controllers delegate to core services; no controller injects a repository (CONVENTIONS #1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Elevation of Privilege / Information Disclosure | `POST /triage/audit/{auditId}/undo` | mitigate | `TriageUndoService` loads via `findByAuditIdAndTenantId(auditId, tenantId)` (tenant resolved from the session-bound `TenantContext`); a wrong-tenant id throws a not-found-style exception → 404, identical to a non-existent id — no cross-tenant existence leak; `@TenantId` filters the lookup at the Hibernate layer. |
| T-04-06-02 | Tampering / replay | second undo on the same row | mitigate | `markReverted` is `WHERE decision='APPLIED'`; after the first undo the row is `REVERTED`, so the state check throws `TriageUndoAlreadyDoneException` → 409 `triage.undo.already_done` (Wave-0 contract verifies the second call). |
| T-04-06-03 | Repudiation | undo outside the 30-day window | mitigate | `applied_at < clock.instant() - 30d` → `TriageUndoExpiredException` → 409 `triage.undo.expired`; `Clock` is injected so the boundary (30d+1s) is mocked-clock-tested; the retention purge job (plan 04-07) removes expired rows so the window is also enforced by data lifecycle. |
| T-04-06-04 | Repudiation / silent failure | unknown action type on undo | mitigate | Exhaustive `switch` on `TriageActionResult` (compile-time gap on a new variant) + an explicit runtime `TriageAuditException.unsupportedActionType()` → 409 `triage.undo.unsupported_action` (fail-loud refusal, D-B6) — never a silent no-op. |
| T-04-06-05 | Information Disclosure | error responses / logs | mitigate | `GlobalExceptionHandler` returns `ProblemDetail` with a dotted code and generic detail (mirrors `onSafetyViolation`); the controllers log ids only; the sender-opt-in log line hashes/omits the raw sender address; vi+en i18n keys carry user-facing copy, not server-constructed strings. |
| T-04-06-06 | Tampering | controller → core boundary | mitigate | Thin controllers resolve `tenantId` from `TenantContext.currentOrThrow()` and delegate to core services; no repository injection (CONVENTIONS #1/#2); request bodies are `@Valid`-checked. |
</threat_model>

<verification>
- `./gradlew :backend:core:compileJava :backend:api:compileJava` BUILD SUCCESSFUL.
- `./gradlew :backend:core:test --tests "*TriageUndoServiceContractTest"` PASSES; `./gradlew :backend:api:test --tests "*TriageUndoControllerContractTest" --tests "*TriageTenantControllerContractTest" --tests "*SenderSafetyNetControllerContractTest"` all PASS.
- `cd apps/web && pnpm i18n:check` PASSES; `apps/web/lib/api/schema.d.ts` includes the new triage endpoints.
</verification>

<success_criteria>
- `TriageUndoService` exists with tenant-ownership / APPLIED-state / 30d-window checks, exhaustive-switch inverse computation reading the post-apply `gmail_change_token` for the label/archive inverses, and fail-loud unsupported-action behavior (incl. a loud failure on a missing change token for an APPLIED label/archive row).
- Three thin triage controllers + 5 DTOs + dto package-info exist; the `GET /api/triage/sender-safety-net` controller reads `SenderSafetyNetService.listProtectedSenders` (sourced from `tenant_protected_sender_observation`); the opt-in controller canonicalizes via `SenderEmailCanonicalizer`, hashes the log line, and a malformed address → 400; `ErrorCodes` + `GlobalExceptionHandler` cover the 4 new codes + the malformed-sender 400; vi+en i18n parity holds; `schema.d.ts` regenerated.
- The undo-service + 3 controller Wave-0 contracts are GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-06-SUMMARY.md`
</output>
