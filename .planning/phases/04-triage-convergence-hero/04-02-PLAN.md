---
phase: 04-triage-convergence-hero
plan: 02
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml
  - backend/core/src/main/resources/db/changelog/changes/026-tenants-triage-shadow-mode.yaml
  - backend/core/src/main/resources/db/changelog/changes/027-tenant-sender-opt-in.yaml
  - backend/core/src/main/resources/db/changelog/changes/028-tenant-protected-sender-observation.yaml
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResultJsonValidator.java
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionArgsCanonicalizer.java
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageDecision.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInEntity.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInRepository.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationEntity.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationRepository.java
  - backend/core/src/main/java/com/zeromail/core/triage/domain/SenderEmailCanonicalizer.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageSafetyViolationException.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageAuditException.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoExpiredException.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoAlreadyDoneException.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoUnsupportedActionException.java
  - backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java
  - backend/core/src/main/java/com/zeromail/core/config/ZeroMailCoreProperties.java
  - backend/core/src/main/java/com/zeromail/core/tenant/persistence/TenantEntity.java
  - backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java
  - backend/core/src/test/java/com/zeromail/core/arch/TriageAuditRepositoryBoundaryArchTest.java
autonomous: true
requirements: [TRG-04, TRG-05, TRG-07, TRG-08]

must_haves:
  truths:
    - "triage_audit table exists with a NULLS-NOT-DISTINCT unique index on (tenant_id, gmail_message_id, rule_id, action_type, args_hash), JSONB columns action_args_json + gmail_change_token, a decided_at TIMESTAMPTZ column (the dedicated terminal-decision timestamp the 30-day purge keys on, since SHADOW_LOGGED/REJECTED_* rows have null applied_at), and an attempt_count + last_attempt_at + lease_owner triple so a crashed-mid-write PENDING row can be reclaimed by a later attempt and the stuck-PENDING reaper (04-07) can distinguish the row's current lease owner — the reaper's 'abandoned' threshold is a separate, much longer window (30 min) than the saga's retry lease (2 min) so a slow-but-live attempt is never reaped"
    - "tenants.triage_shadow_mode BOOLEAN NOT NULL DEFAULT FALSE exists; TenantService can read/flip it"
    - "tenant_sender_opt_in table exists with a unique (tenant_id, sender_email) constraint on the canonicalized lower-cased address; tenant_protected_sender_observation table exists so GET /api/triage/sender-safety-net can list senders triage actually flagged (per SPEC req 8 point c)"
    - "TriageActionResult is a sealed interface permitting Label / Archive / SaveDraft, validated by a manual JSON discriminator validator, and round-trips Gmail response state (draftId on SaveDraft is set ONLY post-write, never part of args_hash)"
    - "CallSite has 5 members including TRIAGE_PLATFORM_LLM and TRIAGE_DETERMINISTIC; the membership ArchUnit test is GREEN"
    - "TriageDecision enum has PENDING/APPLIED/SHADOW_LOGGED/REJECTED_BY_SAFETY_NET/REJECTED_BY_SAFETY_POLICY/FAILED/REVERTED via IdentifiedEnum + fail-loud fromId"
    - "TriageAuditWriter is the validation/canonicalization seam that runs TriageActionResultJsonValidator + TriageActionArgsCanonicalizer BEFORE any native triage_audit insert, so the native upsert can never bypass entity-level validation"
    - "TriageAuditRepository declares exactly these write methods — insertAuditPendingIfAbsent + insertAuditTerminalIfAbsent + reclaimStalePending(auditId, tenantId, leaseOwner) + markApplied(+gmailChangeToken) + markFailed + markReverted + markShadowLogged — plus the read/list/select queries the orchestrator/undo/purge/reaper need (incl. a bounded findStuckPendingForReaping SELECT), and NO other delete*/update*-named method; the SHADOW_LOGGED / REJECTED_BY_SAFETY_NET / REJECTED_BY_SAFETY_POLICY rows are created via insertAuditTerminalIfAbsent (a direct terminal insert), NOT via dedicated markRejectedBy* transitions; TriageAuditRepositoryBoundaryArchTest whitelists exactly that write-method set"
  artifacts:
    - path: "backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml"
      provides: "triage_audit table + args_hash BYTEA + NULLS-NOT-DISTINCT unique index + JSONB columns + decision CHECK + decided_at TIMESTAMPTZ + attempt_count/last_attempt_at/lease_owner lease columns + FK to tenants"
      contains: "triage_audit"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java"
      provides: "Sealed TriageActionResult: Label / Archive / SaveDraft"
      contains: "sealed interface TriageActionResult"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java"
      provides: "Tenant-owned JPA audit entity with @JdbcTypeCode(SqlTypes.JSON) jsonb columns + manual validator on getter + decided_at + attempt_count/last_attempt_at/lease_owner"
      contains: "extends AbstractTenantOwnedEntity"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java"
      provides: "insertAuditPendingIfAbsent + insertAuditTerminalIfAbsent native upserts + reclaimStalePending(auditId, tenantId, leaseOwner) + markApplied/markFailed/markReverted/markShadowLogged narrow transitions + read/list/select queries (incl. findStuckPendingForReaping)"
      contains: "insertAuditPendingIfAbsent"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java"
      provides: "Validation/canonicalization seam: validates+canonicalizes TriageActionResult, computes args_hash, then calls the native insertAuditPendingIfAbsent"
      contains: "TriageActionResultJsonValidator"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationEntity.java"
      provides: "Tenant-owned record of a sender triage flagged as protected (for the GET list endpoint)"
      contains: "extends AbstractTenantOwnedEntity"
  key_links:
    - from: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java"
      to: "triage_audit (native ON CONFLICT DO NOTHING RETURNING audit_id)"
      via: "@Modifying @Query mirroring MailMessageObservedRepository.insertObservedIfAbsent + explicit tenant_id"
      pattern: "ON CONFLICT.*DO NOTHING.*RETURNING"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java"
      to: "TriageActionResultJsonValidator + TriageActionArgsCanonicalizer"
      via: "validate + canonicalize + hash BEFORE the native insert so the native path cannot bypass validation"
      pattern: "TriageActionResultJsonValidator"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java"
      to: "TriageActionResultJsonValidator"
      via: "validator invoked from getActionArgsJson()/@PrePersist"
      pattern: "TriageActionResultJsonValidator"
    - from: "backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java"
      to: "TenantEntity.triageShadowMode"
      via: "setTriageShadowMode / isTriageShadowMode mirroring setTriagePaused / isTriagePaused"
      pattern: "TriageShadowMode"
---

<objective>
[Wave 2] Land the triage persistence + domain layer: Liquibase `025-028` (triage_audit with the lease columns / tenants.triage_shadow_mode / tenant_sender_opt_in / tenant_protected_sender_observation), the `TriageActionResult` sealed value type + manual validator + canonical-JSON hasher + `SenderEmailCanonicalizer`, the `TriageDecision` enum, the `TriageAuditEntity` + `TriageAuditRepository` (full transition surface) + `TriageAuditWriter` validation seam + `TenantSenderOptInEntity`/`TenantSenderOptInRepository` + `TenantProtectedSenderObservationEntity`/`TenantProtectedSenderObservationRepository`, the five `core.triage.exception` classes, the additive `CallSite` extension + `ZeroMailCoreProperties` triage-deterministic cost, the `TenantEntity.triageShadowMode` + `TenantService` accessors, and the matching update to the Wave-0 `TriageAuditRepositoryBoundaryArchTest` whitelist.

Purpose: per D-B1..D-B6 (audit JSON schema + undo state capture; `args_hash` over PRE-WRITE intent only, `draftId` set post-write), D-C1..D-C4 (idempotency unique-index + canonical hash + two-phase decision states), D-E1 (CallSite additive), D-E5 (changelog allocation), the TRG-04/05/07/08 acceptance criteria, SPEC §req 8 point c (the GET endpoint lists senders triage flagged — needs a persistence source), and the consensus review findings #1 (audit repository surface incomplete: missing SHADOW_LOGGED / REJECTED_* transitions + `gmail_change_token` post-write set), #3 (native insert must not bypass validation — `TriageAuditWriter` seam), #6 (sender-email canonicalize-once-reuse-everywhere), plus the Codex HIGH "PENDING conflict handling can lose actions" (the `attempt_count`/`last_attempt_at` lease + `reclaimStalePending`) and "nullable `rule_id` breaks the unique index" (`NULLS NOT DISTINCT`).
Output: 4 Liquibase changelogs + master include; 5 domain types; 7 persistence types (entities + repos + the `TriageAuditWriter` seam); 5 exceptions; CallSite + properties; TenantEntity + TenantService; the ArchUnit whitelist update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liquibase 025/026/027/028 — triage_audit (with lease columns + NULLS-NOT-DISTINCT idem index), tenants.triage_shadow_mode, tenant_sender_opt_in, tenant_protected_sender_observation</name>
  <files>backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml, backend/core/src/main/resources/db/changelog/changes/026-tenants-triage-shadow-mode.yaml, backend/core/src/main/resources/db/changelog/changes/027-tenant-sender-opt-in.yaml, backend/core/src/main/resources/db/changelog/changes/028-tenant-protected-sender-observation.yaml, backend/core/src/main/resources/db/changelog/db.changelog-master.yaml</files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/changes/021-rules-engine-schema.yaml (createTable + jsonb columns + FK deleteCascade + CHECK constraints + indexes — the closest structural analog)
    - backend/core/src/main/resources/db/changelog/changes/015-credit-reservation.yaml (status CHECK + partial index idiom)
    - backend/core/src/main/resources/db/changelog/changes/013-tenants-triage-paused.yaml (addColumn boolean defaultValueBoolean + rollback dropColumn — the verbatim shape for `026`)
    - backend/core/src/main/resources/db/changelog/changes/018-tenant-byok-credentials.yaml (small tenant-owned table with FK + unique constraint — the analog for `027` and `028`)
    - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml (numbered include list — append `025`, `026`, `027`, `028` after `024`)
    - backend/core/src/main/java/com/zeromail/core/shared/persistence/AbstractTenantOwnedEntity.java (the audit columns `created_at`/`updated_at`/`version` that every tenant-owned table needs; `tenant_id` discriminator)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-B1..D-B6, D-C1..D-C4, D-E2 (the purge predicate uses `applied_at` — but SHADOW_LOGGED / REJECTED_* rows have null `applied_at`, so the purge column must be `decided_at` or `COALESCE(applied_at, created_at)`; resolve this here), D-E5; 04-PATTERNS.md the `025-triage-audit.yaml` row (the full column list)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 8 point c ("include sender in a per-tenant 'protected senders' list surfaced via GET /api/triage/sender-safety-net") — the GET list must reflect senders triage actually flagged, so `028` adds `tenant_protected_sender_observation`
    - PostgreSQL docs (re-fetch via Context7 if needed): `CREATE UNIQUE INDEX ... NULLS NOT DISTINCT` (PG 15+) — required because `rule_id` is nullable and a plain unique index treats NULL `rule_id` rows as never-conflicting, which silently disables idempotency for safety-net-rejection rows
  </read_first>
  <action>
    Create `025-triage-audit.yaml`: `createTable name: triage_audit` with columns — `audit_id uuid PK` (default `gen_random_uuid()` or set by the native insert), `tenant_id uuid NOT NULL`, `gmail_message_id varchar NOT NULL`, `gmail_thread_id varchar`, `rule_id uuid` (nullable — REJECTED_BY_SAFETY_POLICY rows still record the rule, but a safety-net rejection may not have one; follow the SPEC column list — `rule_id` + `rule_name_snapshot`), `rule_name_snapshot varchar`, `action_type varchar NOT NULL` (stores `RuleActionType.id()`), `args_hash BYTEA NOT NULL` (32 raw SHA-256 bytes per D-C2 — computed over the PRE-WRITE action intent ONLY, NEVER including the Gmail-returned `draftId`/`labelId`), `action_args_json jsonb NOT NULL`, `gmail_change_token jsonb` (nullable — set post-write; null for SHADOW_LOGGED / REJECTED_* / SAVE_DRAFT per D-B4), `reason varchar` (matcher node id + structured evidence ids only — never raw text), `decision varchar NOT NULL` with a CHECK constraint restricting to `PENDING/APPLIED/SHADOW_LOGGED/REJECTED_BY_SAFETY_NET/REJECTED_BY_SAFETY_POLICY/FAILED/REVERTED` (D-C4), `external_ref varchar` (the returned Gmail `draftId` for SAVE_DRAFT — written post-create alongside `markApplied`), `failure_reason varchar` (opaque exception class name), `decided_at timestamptz NOT NULL DEFAULT now()` (the timestamp every terminal decision sets — APPLIED/SHADOW_LOGGED/REJECTED_*/FAILED — used by the purge job so SHADOW_LOGGED/REJECTED_* rows with null `applied_at` are still bounded), `applied_at timestamptz` (set only on APPLIED), `reverted_at timestamptz`, `attempt_count integer NOT NULL DEFAULT 0` (incremented each time a PENDING row is (re)claimed for a Gmail-write attempt — the lease counter), `last_attempt_at timestamptz` (when the most recent Gmail-write attempt was claimed — a PENDING row whose `last_attempt_at` is older than the *retry-lease* window (2 min) is reclaimable by a fresh delivery), `lease_owner varchar` (nullable — the worker/instance id that holds the current lease on a PENDING row, stamped by `reclaimStalePending`; lets the stuck-PENDING reaper distinguish "still owned by a worker that might be slow-but-alive" from "abandoned" and is set NULL on every terminal insert/transition), `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()`, `version bigint NOT NULL DEFAULT 0`; a `createIndex` for the idempotency unique index `ux_triage_audit_idem` on `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)` with `unique: true` AND — because `rule_id` is nullable — emit it as a raw `sql`/`createIndex` changeset using `NULLS NOT DISTINCT` so NULL `rule_id` rows still conflict (PG 15+; the project's PostgreSQL 17.6 supports it); a FK on `tenant_id → tenants(id)` `ON DELETE CASCADE`; supporting non-unique indexes on `(tenant_id, gmail_message_id)`, `(tenant_id, decided_at)` (purge), and a partial index `WHERE decision = 'PENDING'` on `(last_attempt_at)` (the reaper / reclaim path); a `rollback` `dropTable`. Add a changelog comment documenting: `args_hash` is over pre-write intent only; the idem index is `NULLS NOT DISTINCT`; the lease triple (`attempt_count`/`last_attempt_at`/`lease_owner`) backs PENDING reclaim — the saga's retry-lease window (2 min) is intentionally SHORTER than the stuck-PENDING reaper's "abandoned" threshold (30 min, 04-07) so a slow-but-live in-flight Gmail attempt is never reaped to FAILED.
    Create `026-tenants-triage-shadow-mode.yaml`: `addColumn` to `tenants` — `triage_shadow_mode boolean defaultValueBoolean: false constraints: { nullable: false }` (verbatim shape of `013-tenants-triage-paused.yaml`); `rollback` `dropColumn`.
    Create `027-tenant-sender-opt-in.yaml`: `createTable name: tenant_sender_opt_in` — `id uuid PK`, `tenant_id uuid NOT NULL`, `sender_email varchar NOT NULL` (always the canonicalized lower-cased address — `SenderEmailCanonicalizer` is the single normalizer; the column comment says so), `created_at`/`updated_at`/`version` (the tenant-owned base columns); unique constraint on `(tenant_id, sender_email)`; FK `tenant_id → tenants(id)` `ON DELETE CASCADE`; `rollback` `dropTable`.
    Create `028-tenant-protected-sender-observation.yaml`: `createTable name: tenant_protected_sender_observation` — `id uuid PK`, `tenant_id uuid NOT NULL`, `sender_email varchar NOT NULL` (canonicalized lower-cased), `first_observed_at timestamptz NOT NULL DEFAULT now()`, `last_observed_at timestamptz NOT NULL DEFAULT now()`, `observation_count integer NOT NULL DEFAULT 1`, `created_at`/`updated_at`/`version`; unique constraint on `(tenant_id, sender_email)` (so an upsert can bump `last_observed_at`/`observation_count`); FK `tenant_id → tenants(id)` `ON DELETE CASCADE`; `rollback` `dropTable`. (Privacy note in the comment: this stores only the canonicalized sender address + counters — never any message content; it exists so the GET endpoint can return the senders triage flagged.)
    Append four `- include:` entries to `db.changelog-master.yaml` in numbered order (`025`, `026`, `027`, `028`, after `024`). All changesets additive only — no destructive ops (CLAUDE.md schema policy). NOTE: a `rule_id` FK to a `rules` table would couple triage_audit to the rules domain's table — do NOT add a `rule_id → rules(id)` FK; the SPEC treats `rule_id`/`rule_name_snapshot` as a denormalized provenance pair so an audit row survives rule deletion. Document that choice in the changelog comment.
  </action>
  <verify>
    <automated>test -f backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml && grep -q "ux_triage_audit_idem\|args_hash" backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml && grep -qi "nulls not distinct" backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml && grep -q "attempt_count\|last_attempt_at" backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml && grep -q "lease_owner" backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml && grep -q "decided_at" backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml && grep -q "triage_shadow_mode" backend/core/src/main/resources/db/changelog/changes/026-tenants-triage-shadow-mode.yaml && grep -q "tenant_sender_opt_in" backend/core/src/main/resources/db/changelog/changes/027-tenant-sender-opt-in.yaml && grep -q "tenant_protected_sender_observation" backend/core/src/main/resources/db/changelog/changes/028-tenant-protected-sender-observation.yaml && grep -c "02[5678]-" backend/core/src/main/resources/db/changelog/db.changelog-master.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `025-triage-audit.yaml` createTable `triage_audit` has all SPEC columns including `args_hash` (BYTEA, NOT NULL), `action_args_json` (jsonb, NOT NULL), `gmail_change_token` (jsonb, nullable), `decided_at` (NOT NULL, used by the purge job), `attempt_count` (integer NOT NULL DEFAULT 0) + `last_attempt_at` (timestamptz nullable) + `lease_owner` (varchar nullable) lease columns, a `decision` CHECK with the 7 D-C4 values, a unique index on `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)` declared `NULLS NOT DISTINCT`, a partial PENDING index for the reclaim path, and a FK to `tenants(id)` ON DELETE CASCADE; no FK to a `rules` table. The changelog comment notes the retry-lease window (2 min) is shorter than the reaper's abandoned threshold (30 min).
    - `026-tenants-triage-shadow-mode.yaml` adds `tenants.triage_shadow_mode boolean NOT NULL DEFAULT false` with a dropColumn rollback.
    - `027-tenant-sender-opt-in.yaml` creates `tenant_sender_opt_in` with a unique `(tenant_id, sender_email)` constraint (canonicalized lower-cased email) and a cascading FK to `tenants(id)`.
    - `028-tenant-protected-sender-observation.yaml` creates `tenant_protected_sender_observation` with `(tenant_id, sender_email)` unique + `first_observed_at`/`last_observed_at`/`observation_count` + a cascading FK to `tenants(id)`; the comment states it stores only the canonicalized address + counters, no message content.
    - `db.changelog-master.yaml` includes `025`, `026`, `027`, `028` in numbered order after `024`.
    - A Testcontainers schema-push test applies `025`–`028` cleanly with no checksum/validation errors and the `NULLS NOT DISTINCT` index is present (queryable via `pg_indexes` or by inserting two NULL-`rule_id` rows with the same other-columns and asserting the second is rejected).
  </acceptance_criteria>
  <done>The four triage tables/columns exist via additive Liquibase changelogs with correct constraints, the NULLS-NOT-DISTINCT idempotency index, the PENDING-lease columns (`attempt_count`/`last_attempt_at`/`lease_owner`), the decided_at purge column, and the protected-sender-observation table.</done>
</task>

<task type="auto">
  <name>Task 2: TriageActionResult sealed type + JSON validator + canonicalizer (pre-write intent only) + SenderEmailCanonicalizer + TriageDecision enum + triage exceptions</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java, backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResultJsonValidator.java, backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionArgsCanonicalizer.java, backend/core/src/main/java/com/zeromail/core/triage/domain/SenderEmailCanonicalizer.java, backend/core/src/main/java/com/zeromail/core/triage/domain/TriageDecision.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageSafetyViolationException.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageAuditException.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoExpiredException.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoAlreadyDoneException.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoUnsupportedActionException.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/domain/ActionIntent.java (the sealed-interface shape: `sealed interface ActionIntent permits Label, Archive, SaveDraft` + `record` impls + `"type"` discriminator handling — the model for `TriageActionResult`, but Phase 4 builds a SEPARATE type per D-B1, NOT a reuse)
    - backend/core/src/main/java/com/zeromail/core/rules/domain/ActionIntentJsonValidator.java (the manual validator: Jackson 3 `tools.jackson.databind.json.JsonMapper.builder().build()`, `readTree`, `path("type")`, `RuleActionType.fromId` → `NoSuchElementException` on unknown, reject-unknown-fields per type — mirror exactly)
    - backend/core/src/main/java/com/zeromail/core/rules/domain/RuleActionType.java (the `@JsonValue`/`@JsonCreator` on the type-id enum stay in `com.fasterxml.jackson.annotation.*` — CLAUDE.md Boot 4 / Jackson 3 split; Phase 4 adds NO new RuleActionType values)
    - backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java + CreditReservationStatus.java (the `IdentifiedEnum` + static `fromId` fail-loud pattern — the model for `TriageDecision`)
    - backend/core/src/main/java/com/zeromail/core/shared/lang/IdentifiedEnum.java (the interface `TriageDecision` implements)
    - backend/core/src/main/java/com/zeromail/core/llm/exception/SafetyViolationException.java (no-arg, no-payload — the model for `TriageSafetyViolationException`; do NOT add a message-carrying constructor to the existing `llm` one — RESEARCH Pitfall 5)
    - backend/core/src/main/java/com/zeromail/core/rules/exception/RuleValidationException.java (private ctor + `Reason` enum + static factories — the model for `TriageAuditException`)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-B1..D-B6 (the exact record shapes: `Label(String labelId, String labelName)`, `Archive()`, `SaveDraft(String instruction, String draftId, String threadId)`; note `draftId` is filled ONLY post-write), D-C2 (canonicalizer: sort keys lexicographically, normalize whitespace, force UTF-8, output 32 raw SHA-256 bytes — NOT hex)
    - .planning/phases/04-triage-convergence-hero/04-REVIEWS.md "Agreed Concerns" #1 (`args_hash` cannot include `draftId` — it is computed before Gmail creates the draft) and #6 / "canonicalize-once-reuse-everywhere" — the rationale for splitting the canonical hash from the persisted JSON and for `SenderEmailCanonicalizer`
  </read_first>
  <action>
    Create `com.zeromail.core.triage.domain.TriageActionResult` as `public sealed interface TriageActionResult permits Label, Archive, SaveDraft` with nested records `record Label(String labelId, String labelName) implements TriageActionResult`, `record Archive() implements TriageActionResult`, `record SaveDraft(String instruction, String draftId, String threadId) implements TriageActionResult`. NO Jackson `@JsonTypeInfo` (D-B1) — the JSON shape is `{"type": "label"|"archive"|"save_draft", ...payload}`; `instruction` on `SaveDraft` is the rule's static draft instruction (NOT LLM output — privacy invariant: no drafted body text is ever stored). The `draftId` component is `null` on the pre-write `SaveDraft` value and is only populated post-`drafts.create`; the persisted `action_args_json` carries the post-write value, but `args_hash` is computed from the pre-write value (no `draftId`). Javadoc on the interface: this round-trips Gmail response state (`draftId` for `drafts.delete` on undo; resolved `labelId` because users rename labels) — that is why it is NOT a reuse of `ActionIntent`; AND a Javadoc note that `args_hash` MUST be computed over the pre-write intent (`draftId == null` for `SaveDraft`) so a retry that re-derives the hash before calling Gmail still matches the existing PENDING row.
    Create `TriageActionResultJsonValidator` mirroring `ActionIntentJsonValidator`: a static `ObjectMapper OBJECT_MAPPER = JsonMapper.builder().build()` (import `tools.jackson.databind.json.JsonMapper`), `readTree(json)`, switch on the resolved `path("type")` value, `RuleActionType.fromId` (or a local discriminator map) → throw `NoSuchElementException` on unknown discriminator, and `rejectUnknownFields(node, Set.of(<type-specific field names>))` on write. Strict-write / lenient-read (Jackson 3 `FAIL_ON_UNKNOWN_PROPERTIES` defaults to `false` — old audit rows with v2 fields still read fine, D-B6). Also expose a `String toCanonicalJson(TriageActionResult)` / `String toJson(TriageActionResult)` helper if `ActionIntentJsonValidator` doesn't already give one — `TriageAuditWriter` (Task 3 below) uses it to produce the persisted JSON.
    Create `TriageActionArgsCanonicalizer` (no in-repo analog — new): `byte[] canonicalHash(TriageActionResult preWriteIntent)` — REQUIRES that the argument is the PRE-WRITE intent (for `SaveDraft`, `draftId` must be null; the canonicalizer should defensively normalize a non-null `draftId` to absent before hashing, or assert it is null, so a caller mistake fails loud rather than producing a hash mismatch); serializes to a canonical JSON form (object keys sorted lexicographically, whitespace normalized, UTF-8 bytes), then `MessageDigest.getInstance("SHA-256").digest(...)` returning the 32 raw bytes (NOT hex-encoded — index space). Reuse the Jackson 3 `JsonMapper` import path; expose this so the orchestrator, the `TriageAuditWriter`, and any future test fixture share one canonicalization. Javadoc: the hash is over pre-write action intent only — never the Gmail-returned `draftId`/`labelId`.
    Create `com.zeromail.core.triage.domain.SenderEmailCanonicalizer` (new — the single normalizer for sender addresses): `String canonicalize(String rawSenderEmail)` that trims, lower-cases (`Locale.ROOT`), strips display-name wrapping if a `"Name <addr>"` form leaks in (extract the `<...>` part), and rejects (`IllegalArgumentException`) anything that is not a syntactically plausible address (a conservative regex / `jakarta.validation` `@Email`-style check is enough — this is defense-in-depth, not RFC-5321 perfection); plus `String redisCacheKeyComponent(String canonicalizedEmail)` returning a stable HEX SHA-256 of the canonicalized address (so the Redis key the sender-net service builds in 04-04 carries a hash, not the raw lower-cased email — review concern #6 "Redis key leaks raw sender email"); plus `String gmailSearchToken(String canonicalizedEmail)` returning the address wrapped/escaped for safe use in a Gmail `to:` query (quote it, escape embedded quotes — the input is untrusted; review concern "Gmail search query needs input hardening"). Everything that touches a sender address (the sender-net service, the opt-in repo writes, the protected-sender-observation writes, the controller log lines, the GET-list mapping) routes through this class.
    Create `com.zeromail.core.triage.domain.TriageDecision` implementing `IdentifiedEnum` with members `PENDING`, `APPLIED`, `SHADOW_LOGGED`, `REJECTED_BY_SAFETY_NET`, `REJECTED_BY_SAFETY_POLICY`, `FAILED`, `REVERTED`; stable string `id()` == `name()` per the project invariant; static `fromId(String)` that throws `NoSuchElementException` on unknown (mirror `CallSite.fromId`).
    Create the five exceptions in `com.zeromail.core.triage.exception`: `TriageSafetyViolationException` (no-arg, extends `RuntimeException`, no message payload — privacy-safe, mirror `llm.exception.SafetyViolationException`); `TriageAuditException` (private ctor + `Reason` enum incl. `UNSUPPORTED_ACTION_TYPE` + static factory `unsupportedActionType()` — mirror `RuleValidationException`); `TriageUndoExpiredException`, `TriageUndoAlreadyDoneException`, `TriageUndoUnsupportedActionException` (plain `RuntimeException` subclasses, no content payload — the `GlobalExceptionHandler` in plan 04-06 maps each to a 409 + an `ErrorCodes` constant).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && grep -q "sealed interface TriageActionResult" backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java && grep -q "SHA-256" backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionArgsCanonicalizer.java && grep -q "redisCacheKeyComponent\|gmailSearchToken" backend/core/src/main/java/com/zeromail/core/triage/domain/SenderEmailCanonicalizer.java && ./gradlew :backend:core:test --tests "*TriageActionResultJsonValidatorContractTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriageActionResult` is `sealed` permitting exactly `Label`, `Archive`, `SaveDraft` with the D-B5 record components; no `@JsonTypeInfo`; Javadoc states `args_hash` is over the pre-write intent (`draftId == null`).
    - `TriageActionResultJsonValidator` rejects unknown discriminators with `NoSuchElementException` and unknown per-type fields on write; imports `tools.jackson.databind.json.JsonMapper`; any annotation imports stay in `com.fasterxml.jackson.annotation.*`; exposes a JSON-serialize helper for the persisted column.
    - `TriageActionArgsCanonicalizer.canonicalHash(...)` returns a `byte[]` of length 32 (raw SHA-256), and either asserts or normalizes-away a non-null `SaveDraft.draftId` so the hash is stable across the pre-write→post-write transition (unit-asserted: `canonicalHash(SaveDraft(instr, null, thr)) == canonicalHash(SaveDraft(instr, "draft-123", thr))`).
    - `SenderEmailCanonicalizer.canonicalize(...)` lower-cases + trims + extracts `<addr>` + rejects malformed input; `redisCacheKeyComponent(...)` returns a hex SHA-256 (not the raw address); `gmailSearchToken(...)` returns a quoted/escaped form safe to interpolate into a Gmail `to:` query.
    - `TriageDecision` implements `IdentifiedEnum`, has the 7 members, and `fromId("NOPE")` throws `NoSuchElementException`.
    - `TriageSafetyViolationException` has only a no-arg constructor; `llm.exception.SafetyViolationException` is unchanged.
    - `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageActionResultJsonValidatorContractTest"` PASSES.
  </acceptance_criteria>
  <done>The triage domain value types, manual validator, canonical hasher (pre-write-intent only), sender-email canonicalizer, decision enum, and exception classes exist and compile.</done>
</task>

<task type="auto">
  <name>Task 3: TriageAuditEntity + full TriageAuditRepository transition surface + TriageAuditWriter validation seam + TenantSenderOptIn + TenantProtectedSenderObservation entities/repos + CallSite extension + ZeroMailCoreProperties cost + TenantEntity.triageShadowMode + TenantService accessors + ArchUnit whitelist update</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInEntity.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInRepository.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationEntity.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationRepository.java, backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java, backend/core/src/main/java/com/zeromail/core/config/ZeroMailCoreProperties.java, backend/core/src/main/java/com/zeromail/core/tenant/persistence/TenantEntity.java, backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java, backend/core/src/test/java/com/zeromail/core/arch/TriageAuditRepositoryBoundaryArchTest.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java (the `extends AbstractTenantOwnedEntity` + `@JdbcTypeCode(SqlTypes.JSON) @Column(columnDefinition="jsonb") private String ...` + getter-runs-validator pattern — the verbatim model for `TriageAuditEntity`)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/MailMessageObservedRepository.java (the native `INSERT ... ON CONFLICT DO NOTHING` template — `insertObservedIfAbsent`; for triage add `RETURNING audit_id` and a `tenant_id = :tenantId` term per RESEARCH Pitfall 3)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/PubSubDeliveryRepository.java (the `@Modifying @Query` `updateStatus` / `releaseForRetry` shape with explicit-status WHERE — the model for the `mark*` transitions AND the `reclaimStalePending` lease-renewal update)
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleRepository.java (`findByIdAndTenantId`, derived queries — the model for `TenantSenderOptInRepository` and `TenantProtectedSenderObservationRepository`)
    - backend/core/src/main/java/com/zeromail/core/shared/persistence/AbstractTenantOwnedEntity.java (what the base hoists: `@TenantId @Column("tenant_id") private UUID tenantId` + audit columns + `@Version`)
    - backend/core/src/main/java/com/zeromail/core/rules/service/RuleManagementService.java (a `@Component` in `core` that owns a `@Transactional` mutation + calls a validator before persisting — the structural model for `TriageAuditWriter`, the validation seam in front of the native insert)
    - backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java (existing members `TRIAGE(1)`, `DRAFT(2)`, `PREVIEW(1)` + `cost()` + `fromId` — Phase 4 ADDS `TRIAGE_PLATFORM_LLM` and `TRIAGE_DETERMINISTIC` per D-E1; do NOT remove or renumber existing members)
    - backend/core/src/main/java/com/zeromail/core/config/ZeroMailCoreProperties.java (the nested-record billing-cost properties shape — add `triage-deterministic` cost default 0 per D-D3 / "Claude's Discretion")
    - backend/core/src/main/java/com/zeromail/core/tenant/persistence/TenantEntity.java (the `triagePaused` field + `isTriagePaused()` / `setTriagePaused(...)` — mirror exactly for `triageShadowMode`)
    - backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java (`setTriagePaused` / `isTriagePaused` — mirror for `setTriageShadowMode` / `isTriageShadowMode`; tenant-scoped, service-owned `@Transactional`)
    - backend/core/src/test/java/com/zeromail/core/billing/CallSiteEnumMembershipArchTest.java (Wave-0 expects 5 members — this task makes it GREEN)
    - backend/core/src/test/java/com/zeromail/core/arch/TriageAuditRepositoryBoundaryArchTest.java (Wave-0 — currently whitelists only `markApplied`/`markFailed`/`markReverted`; this task updates the whitelist to exactly `{insertAuditPendingIfAbsent, insertAuditTerminalIfAbsent, reclaimStalePending, markApplied, markFailed, markReverted, markShadowLogged}` — note there is NO `markRejectedBySafetyNet`/`markRejectedBySafetyPolicy` method: the REJECTED_* rows are created via `insertAuditTerminalIfAbsent`, not a transition — while still banning every OTHER `delete*`/`update*`-named method)
    - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResultJsonValidator.java + TriageActionArgsCanonicalizer.java + SenderEmailCanonicalizer.java (from Task 2 — used by `TriageAuditWriter` and the sender-net entities)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-B2 (persistence shape), D-B4 (`gmail_change_token` top-level column, set post-write), D-C3 (two-phase PENDING→APPLIED write order), D-C4 (PENDING→APPLIED/FAILED + APPLIED→REVERTED whitelisted; the SHADOW_LOGGED / REJECTED_* rows are inserted directly in a terminal state, not transitioned from PENDING — see Task-3 action)
    - .planning/phases/04-triage-convergence-hero/04-REVIEWS.md "Agreed Concerns" #1 (audit repository surface incomplete — SHADOW_LOGGED / REJECTED_* transitions + `gmail_change_token` post-write set), #3 (native insert bypasses entity validation), Codex HIGH "existing PENDING conflict handling can lose actions" (lease/reclaim), Codex HIGH "nullable rule_id breaks the unique index" (handled in Task 1's `NULLS NOT DISTINCT` — the repo just relies on it)
  </read_first>
  <action>
    Create `TriageAuditEntity` `@Entity @Table(name = "triage_audit") extends AbstractTenantOwnedEntity`: `@Id @Column("audit_id") private UUID auditId`; `@JdbcTypeCode(SqlTypes.JSON) @Column(name="action_args_json", columnDefinition="jsonb", nullable=false) private String actionArgsJson` whose getter runs a static `TriageActionResultJsonValidator` before returning (mirror `RuleEntity.getMatcherAst()` style); `@JdbcTypeCode(SqlTypes.JSON) @Column(name="gmail_change_token", columnDefinition="jsonb") private String gmailChangeToken` (nullable — Javadoc: stores "what we changed" not a full label snapshot, set post-write); `@Column(name="args_hash", nullable=false) private byte[] argsHash` (32 raw SHA-256 bytes — over pre-write intent only); plus `gmailMessageId`, `gmailThreadId`, `ruleId` (nullable), `ruleNameSnapshot`, `actionType` (stored as `RuleActionType.id()` String), `decision` (stored as `TriageDecision.id()` String), `reason`, `externalRef`, `failureReason`, `decidedAt` (Instant), `appliedAt` (Instant), `revertedAt` (Instant), `attemptCount` (int), `lastAttemptAt` (Instant), `leaseOwner` (String, nullable). No-arg ctor for Hibernate + a builder/factory for inserts where appropriate (Lombok-free).
    Create `com.zeromail.core.triage.persistence.TriageAuditWriter` as a `@Component` (the validation/canonicalization SEAM in front of the native insert — review concern #3): inject `TriageAuditRepository`, `TriageActionResultJsonValidator`, `TriageActionArgsCanonicalizer`. It exposes the only sanctioned way to create a triage_audit row: e.g. `Optional<UUID> insertPending(UUID tenantId, String gmailMessageId, String gmailThreadId, UUID ruleId, String ruleNameSnapshot, RuleActionType actionType, TriageActionResult preWriteIntent, String reasonEvidence)` which (1) validates `preWriteIntent` and serializes it to canonical-or-strict JSON via the validator, (2) computes `argsHash = canonicalizer.canonicalHash(preWriteIntent)`, (3) calls `triageAuditRepository.insertAuditPendingIfAbsent(...)` with `decision='PENDING'`, `attempt_count=0`, `gmail_change_token=null`, and returns the `Optional<UUID>`; AND a `void insertTerminal(UUID tenantId, ..., TriageDecision terminalDecision, ...)` for the SHADOW_LOGGED / REJECTED_BY_SAFETY_NET / REJECTED_BY_SAFETY_POLICY rows that are known terminal at insert time — same validate+hash, but inserts directly with the terminal decision + `decided_at=NOW()` + `gmail_change_token=null` (this avoids a wasted PENDING insert + UPDATE for shadow/rejected rows — OpenCode review suggestion D, and it removes the need for `markShadowLogged`/`markRejectedBy*` on the hot path; still keep `markShadowLogged` on the repo as a fallback transition for the rare case the orchestrator already inserted PENDING and only THEN discovered shadow mode). Javadoc: native SQL bypasses `@PrePersist`/getter validation, so EVERY native insert MUST go through this writer; ArchUnit (`TriageAuditRepositoryBoundaryArchTest`) is loosened just enough to whitelist the writer's calls.
    Create `TriageAuditRepository extends JpaRepository<TriageAuditEntity, UUID>` with the FULL transition surface:
      - native `@Modifying @Query` `insertAuditPendingIfAbsent(...)` mirroring `MailMessageObservedRepository.insertObservedIfAbsent` — `INSERT INTO triage_audit (audit_id, tenant_id, gmail_message_id, gmail_thread_id, rule_id, rule_name_snapshot, action_type, args_hash, action_args_json, gmail_change_token, reason, decision, attempt_count, last_attempt_at, lease_owner, decided_at, created_at, updated_at, version) VALUES (gen_random_uuid(), :tenantId, ..., 'PENDING', 0, NULL, NULL, NOW(), NOW(), NOW(), 0) ON CONFLICT (tenant_id, gmail_message_id, rule_id, action_type, args_hash) DO NOTHING RETURNING audit_id` returning `Optional<UUID>` (empty = a row already exists for this idempotency key — caller then either skips or reclaims, see `reclaimStalePending`). NOTE the `decided_at` for PENDING is the insert time; it is overwritten by each terminal transition; `last_attempt_at`/`lease_owner` start NULL and are stamped by the first `reclaimStalePending`.
      - native `@Modifying @Query` `insertAuditTerminalIfAbsent(...)` — same shape but inserts directly with a caller-supplied terminal `:decision` (one of `SHADOW_LOGGED`/`REJECTED_BY_SAFETY_NET`/`REJECTED_BY_SAFETY_POLICY`) + `decided_at = NOW()` + `gmail_change_token = NULL` + `attempt_count = 0` + `last_attempt_at = NULL` + `lease_owner = NULL`, `ON CONFLICT ... DO NOTHING RETURNING audit_id` (idempotent). (The `TriageAuditWriter.insertTerminal` calls this.)
      - native `@Modifying @Query` `reclaimStalePending(@Param("auditId") UUID, @Param("tenantId") UUID, @Param("leaseOwner") String)` = `UPDATE triage_audit SET attempt_count = attempt_count + 1, last_attempt_at = NOW(), lease_owner = :leaseOwner, updated_at = NOW() WHERE audit_id = :auditId AND tenant_id = :tenantId AND decision = 'PENDING' AND (last_attempt_at IS NULL OR last_attempt_at < NOW() - INTERVAL '2 minutes')` returning `int` (1 = lease claimed by `:leaseOwner`, this attempt may call Gmail; 0 = another worker holds a fresh lease, skip). The `leaseOwner` param is the saga's per-worker/per-JVM id — it lets the reaper distinguish a row currently owned by a (possibly slow-but-alive) worker from a genuinely abandoned one. The orchestrator's loop calls `insertAuditPendingIfAbsent`; if it returns empty (row exists), it calls `findByAuditIdAndTenantId` to get the existing row, and if that row is still `PENDING` it calls `reclaimStalePending` — 1 means "the prior attempt's lease is stale (>2 min), retry the Gmail call"; 0 means "skip". This is the fix for Codex HIGH "PENDING conflict handling can lose actions" — a crash after PENDING-insert but before Gmail no longer means skip-forever.
      - native `@Modifying @Query` `markApplied(@Param("auditId") UUID, @Param("tenantId") UUID, @Param("externalRef") String, @Param("gmailChangeToken") String)` = `UPDATE triage_audit SET applied_at = NOW(), decided_at = NOW(), decision = 'APPLIED', external_ref = :externalRef, gmail_change_token = CAST(:gmailChangeToken AS jsonb), lease_owner = NULL, updated_at = NOW() WHERE audit_id = :auditId AND tenant_id = :tenantId AND decision = 'PENDING'` (the `gmailChangeToken` param closes review concern #1 — for `APPLY_LABEL` it is `{"addedLabelId":"..."}`, for `ARCHIVE_SKIP_INBOX` `{"removedLabelIds":["INBOX"]}`, for `SAVE_DRAFT` `NULL`; `external_ref` carries the `draftId` for SAVE_DRAFT, NULL otherwise — keeping `args_hash` free of the draftId; `lease_owner` cleared on the terminal transition).
      - native `@Modifying @Query` `markFailed(@Param("auditId") UUID, @Param("tenantId") UUID, @Param("failureReason") String)` = `... SET decision = 'FAILED', decided_at = NOW(), failure_reason = :failureReason, lease_owner = NULL, updated_at = NOW() WHERE audit_id = :auditId AND tenant_id = :tenantId AND decision = 'PENDING'`.
      - native `@Modifying @Query` `markReverted(@Param("auditId") UUID, @Param("tenantId") UUID, @Param("revertedAt") java.time.Instant)` = `... SET decision = 'REVERTED', reverted_at = :revertedAt, updated_at = NOW() WHERE audit_id = :auditId AND tenant_id = :tenantId AND decision = 'APPLIED'`.
      - native `@Modifying @Query` `markShadowLogged(@Param("auditId") UUID, @Param("tenantId") UUID)` = `... SET decision = 'SHADOW_LOGGED', decided_at = NOW(), lease_owner = NULL, updated_at = NOW() WHERE audit_id = :auditId AND tenant_id = :tenantId AND decision = 'PENDING'` (the rare PENDING→SHADOW_LOGGED fallback when the orchestrator already inserted PENDING; the fast path inserts terminal SHADOW_LOGGED directly).
      - read/select/list queries (HQL/derived — inherit `@TenantId`): `Optional<TriageAuditEntity> findByAuditIdAndTenantId(UUID, UUID)`; **the bounded stuck-PENDING SELECT for the reaper (used by 04-07): `List<TriageAuditEntity> findStuckPendingForReaping(@Param("cutoff") java.time.Instant cutoff, org.springframework.data.domain.Pageable batchLimit)` whose predicate is `decision = 'PENDING' AND (last_attempt_at < :cutoff OR (last_attempt_at IS NULL AND created_at < :cutoff))`** — SELECT only (allowed under the boundary ArchUnit), so 04-07's reaper only references this; the reaper passes a cutoff of `now() - 30 minutes` (its "abandoned" threshold), which is intentionally MUCH longer than the saga's 2-min retry-lease window so a slow-but-live in-flight attempt is never selected for FAILED-flipping. (If a more precise bounded selection is later needed, ADD another SELECT here — never a `delete*`/`update*`-named method.) A list-by-tenant query if anything other than the protected-sender table needs it (note: the GET sender-safety-net endpoint reads `tenant_protected_sender_observation`, NOT `triage_audit`).
      - EVERY native `@Modifying @Query` MUST carry `tenant_id = :tenantId` (RESEARCH Pitfall 3 — `@TenantId` does not apply to native SQL; keep `MultiTenantLeakIntegrationTest` green).
      - Do NOT declare any `delete*` repo method here (the purge's bounded delete lives on the worker batch in 04-07, or is a single explicitly-whitelisted method — that plan decides). Do NOT add `update*`-named methods. The ONLY mutating-name methods allowed are `insertAuditPendingIfAbsent`, `insertAuditTerminalIfAbsent`, `reclaimStalePending`, `markApplied`, `markFailed`, `markReverted`, `markShadowLogged` — `TriageAuditRepositoryBoundaryArchTest` is updated in this same task to whitelist exactly that set.
    Create `TenantSenderOptInEntity` `@Entity @Table(name = "tenant_sender_opt_in") extends AbstractTenantOwnedEntity` (minimal shape: `@Id private UUID id`, `private String senderEmail` — always the canonicalized lower-cased address) and `TenantSenderOptInRepository extends JpaRepository<..., UUID>` with `boolean existsByTenantIdAndSenderEmail(UUID, String)` (HQL — `@TenantId` filtered) plus `List<TenantSenderOptInEntity> findByTenantId(UUID)`; a derived insert via `save(...)`. (HQL/derived queries inherit `@TenantId`; if any native query is added it must carry the explicit tenant term.)
    Create `TenantProtectedSenderObservationEntity` `@Entity @Table(name = "tenant_protected_sender_observation") extends AbstractTenantOwnedEntity` (`@Id private UUID id`, `private String senderEmail` canonicalized, `private Instant firstObservedAt`, `private Instant lastObservedAt`, `private int observationCount`) and `TenantProtectedSenderObservationRepository extends JpaRepository<..., UUID>` with `Optional<TenantProtectedSenderObservationEntity> findByTenantIdAndSenderEmail(UUID, String)` and `List<TenantProtectedSenderObservationEntity> findByTenantId(UUID)` (HQL/derived — `@TenantId` filtered); the sender-net service (04-04) upserts a row each time it flags a sender protected, so the GET endpoint (04-06) can list them. (No native query; if one is added it carries the explicit tenant term.)
    Modify `CallSite`: add members `TRIAGE_PLATFORM_LLM` and `TRIAGE_DETERMINISTIC` following the existing `IdentifiedEnum` pattern (give each a stable `id()`; `cost()` resolves from `ZeroMailCoreProperties` like the others — `TRIAGE_DETERMINISTIC` defaults to 0 credits). Do NOT remove/renumber existing members.
    Modify `ZeroMailCoreProperties`: add a `triage-deterministic` entry under the billing-cost nested record with default `0` (D-D3 / Claude's Discretion — recommend 0, configurable).
    Modify `TenantEntity`: add `@Column("triage_shadow_mode") private boolean triageShadowMode` + `isTriageShadowMode()` / `setTriageShadowMode(boolean)` (verbatim mirror of `triagePaused`).
    Modify `TenantService`: add `setTriageShadowMode(UUID tenantId, boolean enabled)` and `isTriageShadowMode(UUID tenantId)` (verbatim mirror of `setTriagePaused` / `isTriagePaused`; service-owned `@Transactional`, tenant-scoped lookup).
    Update `TriageAuditRepositoryBoundaryArchTest` (Wave-0 from 04-00): change the whitelist of allowed mutating-name methods on `TriageAuditRepository` from `{markApplied, markFailed, markReverted}` to `{insertAuditPendingIfAbsent, insertAuditTerminalIfAbsent, reclaimStalePending, markApplied, markFailed, markReverted, markShadowLogged}`; keep the rule otherwise strict (any other `delete*`/`update*`-named method on the repo fails the build). The test stays GREEN after this task.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && grep -q "insertAuditPendingIfAbsent" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java && grep -q "reclaimStalePending" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java && grep -q "leaseOwner" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java && grep -q "findStuckPendingForReaping" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java && grep -q "gmailChangeToken\|gmail_change_token" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java && grep -q "TriageActionResultJsonValidator" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java && grep -q "TenantProtectedSenderObservation" backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationRepository.java && grep -q "TRIAGE_PLATFORM_LLM" backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java && grep -q "triageShadowMode\|TriageShadowMode" backend/core/src/main/java/com/zeromail/core/tenant/persistence/TenantEntity.java && grep -q "reclaimStalePending\|markShadowLogged" backend/core/src/test/java/com/zeromail/core/arch/TriageAuditRepositoryBoundaryArchTest.java && ! grep -q "markRejectedBy" backend/core/src/test/java/com/zeromail/core/arch/TriageAuditRepositoryBoundaryArchTest.java && ./gradlew :backend:core:test --tests "*CallSiteEnumMembershipArchTest" --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*TriageAuditPersistenceContractTest" 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `TriageAuditEntity` extends `AbstractTenantOwnedEntity`, has `@JdbcTypeCode(SqlTypes.JSON)` on `actionArgsJson` (nullable=false) and `gmailChangeToken` (nullable), a `byte[] argsHash`, `decidedAt` + `attemptCount` + `lastAttemptAt` + `leaseOwner` fields, and its `getActionArgsJson()` invokes `TriageActionResultJsonValidator`.
    - `TriageAuditWriter` is a `@Component` that validates + canonicalizes + hashes a pre-write `TriageActionResult` BEFORE calling `insertAuditPendingIfAbsent`, exposes `insertTerminal` for the SHADOW_LOGGED/REJECTED_* fast path (direct terminal insert), and is the only sanctioned creator of triage_audit rows.
    - `TriageAuditRepository` declares `insertAuditPendingIfAbsent` + `insertAuditTerminalIfAbsent` + `reclaimStalePending(auditId, tenantId, leaseOwner)` (lease renewal, stamps `lease_owner = :leaseOwner`, `WHERE decision='PENDING' AND (last_attempt_at IS NULL OR last_attempt_at < NOW() - INTERVAL '2 minutes')`) + `markApplied(auditId, tenantId, externalRef, gmailChangeToken)` (also clears `lease_owner`) + `markFailed` (also clears `lease_owner`) + `markReverted` + `markShadowLogged` (also clears `lease_owner`) + the read/select queries (incl. the bounded `findStuckPendingForReaping(cutoff, Pageable)` SELECT the reaper uses); every native query carries `tenant_id = :tenantId`; no `delete*` method, no `update*`-named method, NO `markRejectedBy*` method.
    - `TriageAuditRepositoryBoundaryArchTest` whitelists exactly `{insertAuditPendingIfAbsent, insertAuditTerminalIfAbsent, reclaimStalePending, markApplied, markFailed, markReverted, markShadowLogged}` (no `markRejectedBy*`) and stays GREEN; any other `delete*`/`update*` repo method still fails it.
    - `TenantSenderOptInEntity`/`Repository` + `TenantProtectedSenderObservationEntity`/`Repository` exist; the observation repo supports `findByTenantIdAndSenderEmail` (upsert) + `findByTenantId` (GET-list source).
    - `CallSite` has exactly 5 members `{TRIAGE, DRAFT, PREVIEW, TRIAGE_PLATFORM_LLM, TRIAGE_DETERMINISTIC}`; `ZeroMailCoreProperties` has a `triage-deterministic` cost default of 0.
    - `TenantEntity.triageShadowMode` + `isTriageShadowMode()`/`setTriageShadowMode()` exist; `TenantService.setTriageShadowMode(UUID,boolean)` + `isTriageShadowMode(UUID)` exist.
    - `./gradlew :backend:core:test --tests "*CallSiteEnumMembershipArchTest"` PASSES (now GREEN); `--tests "*TriageAuditRepositoryBoundaryArchTest"` PASSES; `--tests "*TriageAuditPersistenceContractTest"` PASSES (Testcontainers round-trip + `@TenantId` scoping + JSON validity + a reclaim-stale-PENDING assertion: an aged-`last_attempt_at` PENDING row → `reclaimStalePending` returns 1; a fresh-lease one → 0).
    - `./gradlew :backend:core:test --tests "*MultiTenantLeakIntegrationTest"` still PASSES (FND-05 regression).
  </acceptance_criteria>
  <done>Triage persistence (entity with lease columns + full repo transition surface), the `TriageAuditWriter` validation seam, the opt-in + protected-sender-observation tables, CallSite, and tenant shadow-mode accessors are landed; the membership and (updated-whitelist) repo-boundary ArchUnit guards are GREEN; FND-05 still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native SQL → tenant isolation | `@TenantId` is a Hibernate HQL/Criteria filter; native queries on `triage_audit` / `tenant_sender_opt_in` bypass it unless the tenant term is explicit. |
| audit row → user-visible "what Zero Mail did" | `triage_audit` is the Phase 5 source of truth; column/JSON shape must round-trip without lossy interpretation and must never carry email content. |
| `decision` lifecycle | Only the narrow `PENDING→APPLIED/FAILED`, `PENDING→SHADOW_LOGGED` (fallback), `APPLIED→REVERTED`, and the lease-renewal `reclaimStalePending` (still `WHERE decision='PENDING'`) transitions are permitted; SHADOW_LOGGED / REJECTED_* rows are inserted directly in a terminal state; any other UPDATE/DELETE on `triage_audit` is forbidden (ArchUnit-enforced, whitelist updated). |
| native insert vs entity validation | The native `insertAuditPendingIfAbsent` / `insertAuditTerminalIfAbsent` bypass `@PrePersist`/getter validation; `TriageAuditWriter` is the mandatory seam that validates + canonicalizes + hashes the `TriageActionResult` before any native insert (review concern #3). |
| PENDING durability vs lost actions | A crash after PENDING-insert but before the Gmail call no longer means skip-forever: the `attempt_count`/`last_attempt_at`/`lease_owner` lease + `reclaimStalePending(auditId, tenantId, leaseOwner)` lets a later delivery reclaim a stale (>2 min) PENDING row (Codex HIGH). The retry-lease window (2 min) is deliberately SHORTER than the stuck-PENDING reaper's "abandoned" threshold (30 min, 04-07) so a slow-but-live in-flight Gmail attempt is never flipped to FAILED by the reaper (review concern #2). |
| `CallSite` membership | The credit ledger keys reservations off `CallSite`; additive-only extension keeps the Phase 2B ledger contract intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure / Tampering | native `triage_audit` queries | mitigate | Every `@Modifying @Query` carries `tenant_id = :tenantId` in WHERE/VALUES (RESEARCH Pitfall 3); `MultiTenantLeakIntegrationTest` re-verified GREEN; HQL/derived queries inherit `@TenantId`. |
| T-04-02-02 | Information Disclosure | `triage_audit.reason` / `action_args_json` / sender tables | mitigate | `reason` stores matcher node id + structured evidence ids only (never raw text); `SaveDraft.instruction` is the rule's static instruction, never LLM-drafted body; `TriageAuditWriter` runs the manual validator which rejects unknown fields on write so no content-bearing field can be smuggled in; `tenant_sender_opt_in` / `tenant_protected_sender_observation` store only the canonicalized sender address + counters (no message content). |
| T-04-02-03 | Tampering (audit immutability) | `TriageAuditRepository` method surface | mitigate | No `delete*` method, no `markRejectedBy*` method (REJECTED_* rows are `insertAuditTerminalIfAbsent` inserts, not transitions); only the whitelisted `insertAuditPendingIfAbsent`/`insertAuditTerminalIfAbsent`/`reclaimStalePending`/`markApplied`/`markFailed`/`markReverted`/`markShadowLogged` with explicit-status WHERE clauses (the `mark*` transitions clear `lease_owner`); `TriageAuditRepositoryBoundaryArchTest` (whitelist updated) fails the build on any other `delete*`/`update*` method. |
| T-04-02-04 | Tampering (idempotency) | unique index `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)` | mitigate | DB-enforced uniqueness — declared `NULLS NOT DISTINCT` so NULL-`rule_id` safety-net-rejection rows still conflict (Codex HIGH) — backs the two-phase PENDING→APPLIED write loop; `args_hash` (canonical-JSON SHA-256 over PRE-WRITE intent only, no `draftId`) disambiguates legitimate multi-action-same-rule cases (D-C1) without collapsing them and stays stable across the pre-write→post-write transition (review concern #1). |
| T-04-02-05 | Elevation of Privilege | `CallSite` extension | mitigate | Additive only (`TRIAGE_PLATFORM_LLM`, `TRIAGE_DETERMINISTIC`); existing members unchanged; the membership ArchUnit guard pins the exact set so a future stray addition fails the build. |
| T-04-02-06 | Repudiation / lost action | PENDING row stuck after a worker crash | mitigate | `attempt_count`/`last_attempt_at`/`lease_owner` lease columns + `reclaimStalePending` (retry-lease window 2 min) let a later delivery reclaim and retry; the stuck-PENDING reaper (04-07) is a SEPARATE last-resort backstop on a much longer 30-min "abandoned" threshold (decoupled from the retry lease so a slow-but-live attempt is never flipped to FAILED); PENDING never lives forever. |
</threat_model>

<verification>
- `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL.
- `./gradlew :backend:core:test --tests "*CallSiteEnumMembershipArchTest" --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*TriageActionResultJsonValidatorContractTest" --tests "*TriageAuditPersistenceContractTest" --tests "*MultiTenantLeakIntegrationTest"` all PASS.
- Testcontainers schema-push applies `025`–`028` cleanly; the `NULLS NOT DISTINCT` idem index is present; `reclaimStalePending` returns 1 for an aged PENDING row and 0 for a fresh-lease one.
</verification>

<success_criteria>
- triage_audit (with the `decided_at` purge column, the `attempt_count`/`last_attempt_at`/`lease_owner` lease columns, and the `NULLS NOT DISTINCT` idempotency index) / tenants.triage_shadow_mode / tenant_sender_opt_in / tenant_protected_sender_observation exist via additive Liquibase changelogs.
- TriageActionResult sealed type + manual validator + canonical SHA-256 hasher (pre-write-intent only) + SenderEmailCanonicalizer + TriageDecision enum + 5 triage exceptions exist.
- TriageAuditEntity + the full TriageAuditRepository transition surface (insertPending/insertTerminal/reclaimStalePending(auditId,tenantId,leaseOwner)/markApplied[+gmailChangeToken]/markFailed/markReverted/markShadowLogged + reads incl. findStuckPendingForReaping; NO markRejectedBy*) + the TriageAuditWriter validation seam + TenantSenderOptIn + TenantProtectedSenderObservation entities/repos exist.
- CallSite has 5 members; ZeroMailCoreProperties has the triage-deterministic cost; TenantEntity/TenantService expose shadow-mode accessors.
- Wave-0 membership + (updated-whitelist) repo-boundary ArchUnit guards are GREEN; FND-05 still green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-02-SUMMARY.md`
</output>
