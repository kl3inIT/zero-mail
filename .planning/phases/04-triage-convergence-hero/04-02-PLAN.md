---
phase: 04-triage-convergence-hero
plan: 02
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml
  - backend/core/src/main/resources/db/changelog/changes/026-tenants-triage-shadow-mode.yaml
  - backend/core/src/main/resources/db/changelog/changes/027-tenant-sender-opt-in.yaml
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResultJsonValidator.java
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionArgsCanonicalizer.java
  - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageDecision.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInEntity.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInRepository.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageSafetyViolationException.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageAuditException.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoExpiredException.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoAlreadyDoneException.java
  - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoUnsupportedActionException.java
  - backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java
  - backend/core/src/main/java/com/zeromail/core/config/ZeroMailCoreProperties.java
  - backend/core/src/main/java/com/zeromail/core/tenant/persistence/TenantEntity.java
  - backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java
autonomous: true
requirements: [TRG-04, TRG-05, TRG-07, TRG-08]

must_haves:
  truths:
    - "triage_audit table exists with a unique index on (tenant_id, gmail_message_id, rule_id, action_type, args_hash) and JSONB columns action_args_json + gmail_change_token"
    - "tenants.triage_shadow_mode BOOLEAN NOT NULL DEFAULT FALSE exists; TenantService can read/flip it"
    - "tenant_sender_opt_in table exists with a unique (tenant_id, sender_email) constraint"
    - "TriageActionResult is a sealed interface permitting Label / Archive / SaveDraft, validated by a manual JSON discriminator validator, and round-trips Gmail response state"
    - "CallSite has 5 members including TRIAGE_PLATFORM_LLM and TRIAGE_DETERMINISTIC; the membership ArchUnit test is GREEN"
    - "TriageDecision enum has PENDING/APPLIED/SHADOW_LOGGED/REJECTED_BY_SAFETY_NET/REJECTED_BY_SAFETY_POLICY/FAILED/REVERTED via IdentifiedEnum + fail-loud fromId"
  artifacts:
    - path: "backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml"
      provides: "triage_audit table + args_hash BYTEA + unique index + JSONB columns + decision CHECK + FK to tenants"
      contains: "triage_audit"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java"
      provides: "Sealed TriageActionResult: Label / Archive / SaveDraft"
      contains: "sealed interface TriageActionResult"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java"
      provides: "Tenant-owned JPA audit entity with @JdbcTypeCode(SqlTypes.JSON) jsonb columns + manual validator on getter"
      contains: "extends AbstractTenantOwnedEntity"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java"
      provides: "insertAuditPendingIfAbsent native upsert + markApplied/markFailed/markReverted narrow transitions"
      contains: "insertAuditPendingIfAbsent"
  key_links:
    - from: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java"
      to: "triage_audit (native ON CONFLICT DO NOTHING RETURNING audit_id)"
      via: "@Modifying @Query mirroring MailMessageObservedRepository.insertObservedIfAbsent + explicit tenant_id"
      pattern: "ON CONFLICT.*DO NOTHING.*RETURNING"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java"
      to: "TriageActionResultJsonValidator"
      via: "validator invoked from getActionArgsJson()/@PrePersist"
      pattern: "TriageActionResultJsonValidator"
    - from: "backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java"
      to: "TenantEntity.triageShadowMode"
      via: "setTriageShadowMode / isTriageShadowMode mirroring setTriagePaused / isTriagePaused"
      pattern: "TriageShadowMode"
---

<objective>
[Wave 2] Land the triage persistence + domain layer: Liquibase `025-027` (triage_audit / tenants.triage_shadow_mode / tenant_sender_opt_in), the `TriageActionResult` sealed value type + manual validator + canonical-JSON hasher, the `TriageDecision` enum, the `TriageAuditEntity`/`TriageAuditRepository` + `TenantSenderOptInEntity`/`TenantSenderOptInRepository`, the five `core.triage.exception` classes, the additive `CallSite` extension + `ZeroMailCoreProperties` triage-deterministic cost, and the `TenantEntity.triageShadowMode` + `TenantService` accessors.

Purpose: per D-B1..D-B6 (audit JSON schema + undo state capture), D-C1..D-C4 (idempotency unique-index + canonical hash + two-phase decision states), D-E1 (CallSite additive), D-E5 (changelog allocation), and the TRG-04/05/07/08 acceptance criteria.
Output: 3 Liquibase changelogs + master include; 4 domain types; 4 persistence types; 5 exceptions; CallSite + properties; TenantEntity + TenantService.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liquibase 025/026/027 — triage_audit, tenants.triage_shadow_mode, tenant_sender_opt_in</name>
  <files>backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml, backend/core/src/main/resources/db/changelog/changes/026-tenants-triage-shadow-mode.yaml, backend/core/src/main/resources/db/changelog/changes/027-tenant-sender-opt-in.yaml, backend/core/src/main/resources/db/changelog/db.changelog-master.yaml</files>
  <read_first>
    - backend/core/src/main/resources/db/changelog/changes/021-rules-engine-schema.yaml (createTable + jsonb columns + FK deleteCascade + CHECK constraints + indexes — the closest structural analog)
    - backend/core/src/main/resources/db/changelog/changes/015-credit-reservation.yaml (status CHECK + partial index idiom)
    - backend/core/src/main/resources/db/changelog/changes/013-tenants-triage-paused.yaml (addColumn boolean defaultValueBoolean + rollback dropColumn — the verbatim shape for `026`)
    - backend/core/src/main/resources/db/changelog/changes/018-tenant-byok-credentials.yaml (small tenant-owned table with FK + unique constraint — the analog for `027`)
    - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml (numbered include list — append `025`, `026`, `027` after `024`)
    - backend/core/src/main/java/com/zeromail/core/shared/persistence/AbstractTenantOwnedEntity.java (the audit columns `created_at`/`updated_at`/`version` that every tenant-owned table needs; `tenant_id` discriminator)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-B1..D-B6, D-C1..D-C4, D-E5; 04-PATTERNS.md the `025-triage-audit.yaml` row (the full column list)
  </read_first>
  <action>
    Create `025-triage-audit.yaml`: `createTable name: triage_audit` with columns — `audit_id uuid PK` (default `gen_random_uuid()` or set by the native insert), `tenant_id uuid NOT NULL`, `gmail_message_id varchar NOT NULL`, `gmail_thread_id varchar`, `rule_id uuid` (nullable — REJECTED_BY_SAFETY_POLICY rows still record the rule, but a safety-net rejection may not have a rule; follow the SPEC column list — `rule_id` + `rule_name_snapshot`), `rule_name_snapshot varchar`, `action_type varchar NOT NULL` (stores `RuleActionType.id()`), `args_hash BYTEA NOT NULL` (32 raw SHA-256 bytes per D-C2), `action_args_json jsonb NOT NULL`, `gmail_change_token jsonb` (nullable — null for SHADOW_LOGGED / SAVE_DRAFT per D-B4), `reason varchar` (matcher node id + structured evidence ids only — never raw text), `decision varchar NOT NULL` with a CHECK constraint restricting to `PENDING/APPLIED/SHADOW_LOGGED/REJECTED_BY_SAFETY_NET/REJECTED_BY_SAFETY_POLICY/FAILED/REVERTED` (D-C4), `external_ref varchar` (the returned Gmail `draftId` for SAVE_DRAFT), `failure_reason varchar` (opaque exception class name), `applied_at timestamptz`, `reverted_at timestamptz`, `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()`, `version bigint NOT NULL DEFAULT 0`; a unique index `ux_triage_audit_idem` on `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)` (D-C1); a FK on `tenant_id → tenants(id)` `ON DELETE CASCADE`; supporting non-unique indexes on `(tenant_id, gmail_message_id)` and `(tenant_id, applied_at)` for the purge job; a `rollback` `dropTable`.
    Create `026-tenants-triage-shadow-mode.yaml`: `addColumn` to `tenants` — `triage_shadow_mode boolean defaultValueBoolean: false constraints: { nullable: false }` (verbatim shape of `013-tenants-triage-paused.yaml`); `rollback` `dropColumn`.
    Create `027-tenant-sender-opt-in.yaml`: `createTable name: tenant_sender_opt_in` — `id uuid PK`, `tenant_id uuid NOT NULL`, `sender_email varchar NOT NULL`, `created_at`/`updated_at`/`version` (the tenant-owned base columns); unique constraint on `(tenant_id, sender_email)`; FK `tenant_id → tenants(id)` `ON DELETE CASCADE`; `rollback` `dropTable`.
    Append three `- include:` entries to `db.changelog-master.yaml` in numbered order (`025`, then `026`, then `027`, after `024`). All changesets additive only — no destructive ops (CLAUDE.md schema policy). NOTE: a `rule_id` FK to a `rules` table would couple triage_audit to the rules domain's table — do NOT add a `rule_id → rules(id)` FK; the SPEC treats `rule_id`/`rule_name_snapshot` as a denormalized provenance pair so an audit row survives rule deletion. Document that choice in the changelog comment.
  </action>
  <verify>
    <automated>test -f backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml && grep -q "ux_triage_audit_idem\|args_hash" backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml && grep -q "triage_shadow_mode" backend/core/src/main/resources/db/changelog/changes/026-tenants-triage-shadow-mode.yaml && grep -q "tenant_sender_opt_in" backend/core/src/main/resources/db/changelog/changes/027-tenant-sender-opt-in.yaml && grep -c "02[567]-" backend/core/src/main/resources/db/changelog/db.changelog-master.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `025-triage-audit.yaml` createTable `triage_audit` has all SPEC columns including `args_hash` (BYTEA, NOT NULL), `action_args_json` (jsonb, NOT NULL), `gmail_change_token` (jsonb, nullable), a `decision` CHECK with the 7 D-C4 values, a unique index on `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)`, and a FK to `tenants(id)` ON DELETE CASCADE; no FK to a `rules` table.
    - `026-tenants-triage-shadow-mode.yaml` adds `tenants.triage_shadow_mode boolean NOT NULL DEFAULT false` with a dropColumn rollback.
    - `027-tenant-sender-opt-in.yaml` creates `tenant_sender_opt_in` with a unique `(tenant_id, sender_email)` constraint and a cascading FK to `tenants(id)`.
    - `db.changelog-master.yaml` includes `025`, `026`, `027` in numbered order after `024`.
    - A Testcontainers schema-push test applies `025`–`027` cleanly with no checksum/validation errors.
  </acceptance_criteria>
  <done>The three triage tables/columns exist via additive Liquibase changelogs with correct constraints and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: TriageActionResult sealed type + JSON validator + canonicalizer + TriageDecision enum + triage exceptions</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java, backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResultJsonValidator.java, backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionArgsCanonicalizer.java, backend/core/src/main/java/com/zeromail/core/triage/domain/TriageDecision.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageSafetyViolationException.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageAuditException.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoExpiredException.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoAlreadyDoneException.java, backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoUnsupportedActionException.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/domain/ActionIntent.java (the sealed-interface shape: `sealed interface ActionIntent permits Label, Archive, SaveDraft` + `record` impls + `"type"` discriminator handling — the model for `TriageActionResult`, but Phase 4 builds a SEPARATE type per D-B1, NOT a reuse)
    - backend/core/src/main/java/com/zeromail/core/rules/domain/ActionIntentJsonValidator.java (the manual validator: Jackson 3 `tools.jackson.databind.json.JsonMapper.builder().build()`, `readTree`, `path("type")`, `RuleActionType.fromId` → `NoSuchElementException` on unknown, reject-unknown-fields per type — mirror exactly)
    - backend/core/src/main/java/com/zeromail/core/rules/domain/RuleActionType.java (the `@JsonValue`/`@JsonCreator` on the type-id enum stay in `com.fasterxml.jackson.annotation.*` — CLAUDE.md Boot 4 / Jackson 3 split; Phase 4 adds NO new RuleActionType values)
    - backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java + CreditReservationStatus.java (the `IdentifiedEnum` + static `fromId` fail-loud pattern — the model for `TriageDecision`)
    - backend/core/src/main/java/com/zeromail/core/shared/lang/IdentifiedEnum.java (the interface `TriageDecision` implements)
    - backend/core/src/main/java/com/zeromail/core/llm/exception/SafetyViolationException.java (no-arg, no-payload — the model for `TriageSafetyViolationException`; do NOT add a message-carrying constructor to the existing `llm` one — RESEARCH Pitfall 5)
    - backend/core/src/main/java/com/zeromail/core/rules/exception/RuleValidationException.java (private ctor + `Reason` enum + static factories — the model for `TriageAuditException`)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-B1..D-B6 (the exact record shapes: `Label(String labelId, String labelName)`, `Archive()`, `SaveDraft(String instruction, String draftId, String threadId)`), D-C2 (canonicalizer: sort keys lexicographically, normalize whitespace, force UTF-8, output 32 raw SHA-256 bytes — NOT hex)
  </read_first>
  <action>
    Create `com.zeromail.core.triage.domain.TriageActionResult` as `public sealed interface TriageActionResult permits Label, Archive, SaveDraft` with nested records `record Label(String labelId, String labelName) implements TriageActionResult`, `record Archive() implements TriageActionResult`, `record SaveDraft(String instruction, String draftId, String threadId) implements TriageActionResult`. NO Jackson `@JsonTypeInfo` (D-B1) — the JSON shape is `{"type": "label"|"archive"|"save_draft", ...payload}`; `instruction` on `SaveDraft` is the rule's static draft instruction (NOT LLM output — privacy invariant: no drafted body text is ever stored). Javadoc on the interface: this round-trips Gmail response state (`draftId` for `drafts.delete` on undo; resolved `labelId` because users rename labels) — that is why it is NOT a reuse of `ActionIntent`.
    Create `TriageActionResultJsonValidator` mirroring `ActionIntentJsonValidator`: a static `ObjectMapper OBJECT_MAPPER = JsonMapper.builder().build()` (import `tools.jackson.databind.json.JsonMapper`), `readTree(json)`, switch on the resolved `path("type")` value, `RuleActionType.fromId` (or a local discriminator map) → throw `NoSuchElementException` on unknown discriminator, and `rejectUnknownFields(node, Set.of(<type-specific field names>))` on write. Strict-write / lenient-read (Jackson 3 `FAIL_ON_UNKNOWN_PROPERTIES` defaults to `false` — old audit rows with v2 fields still read fine, D-B6).
    Create `TriageActionArgsCanonicalizer` (no in-repo analog — new): `byte[] canonicalHash(TriageActionResult result)` that serializes the result to a canonical JSON form (object keys sorted lexicographically, whitespace normalized, UTF-8 bytes), then `MessageDigest.getInstance("SHA-256").digest(...)` returning the 32 raw bytes (NOT hex-encoded — index space). Reuse the Jackson 3 `JsonMapper` import path; expose this so the orchestrator and any future test fixture share one canonicalization.
    Create `com.zeromail.core.triage.domain.TriageDecision` implementing `IdentifiedEnum` with members `PENDING`, `APPLIED`, `SHADOW_LOGGED`, `REJECTED_BY_SAFETY_NET`, `REJECTED_BY_SAFETY_POLICY`, `FAILED`, `REVERTED`; stable string `id()` == `name()` per the project invariant; static `fromId(String)` that throws `NoSuchElementException` on unknown (mirror `CallSite.fromId`).
    Create the five exceptions in `com.zeromail.core.triage.exception`: `TriageSafetyViolationException` (no-arg, extends `RuntimeException`, no message payload — privacy-safe, mirror `llm.exception.SafetyViolationException`); `TriageAuditException` (private ctor + `Reason` enum incl. `UNSUPPORTED_ACTION_TYPE` + static factory `unsupportedActionType()` — mirror `RuleValidationException`); `TriageUndoExpiredException`, `TriageUndoAlreadyDoneException`, `TriageUndoUnsupportedActionException` (plain `RuntimeException` subclasses, no content payload — the `GlobalExceptionHandler` in plan 04-06 maps each to a 409 + an `ErrorCodes` constant).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && grep -q "sealed interface TriageActionResult" backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java && grep -q "SHA-256" backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionArgsCanonicalizer.java && ./gradlew :backend:core:test --tests "*TriageActionResultJsonValidatorContractTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriageActionResult` is `sealed` permitting exactly `Label`, `Archive`, `SaveDraft` with the D-B5 record components; no `@JsonTypeInfo`.
    - `TriageActionResultJsonValidator` rejects unknown discriminators with `NoSuchElementException` and unknown per-type fields on write; imports `tools.jackson.databind.json.JsonMapper`; any annotation imports stay in `com.fasterxml.jackson.annotation.*`.
    - `TriageActionArgsCanonicalizer.canonicalHash(...)` returns a `byte[]` of length 32 (raw SHA-256) for any `TriageActionResult` (unit-asserted in the validator contract test or a small new test).
    - `TriageDecision` implements `IdentifiedEnum`, has the 7 members, and `fromId("NOPE")` throws `NoSuchElementException`.
    - `TriageSafetyViolationException` has only a no-arg constructor; `llm.exception.SafetyViolationException` is unchanged.
    - `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageActionResultJsonValidatorContractTest"` PASSES.
  </acceptance_criteria>
  <done>The triage domain value types, manual validator, canonical hasher, decision enum, and exception classes exist and compile.</done>
</task>

<task type="auto">
  <name>Task 3: TriageAuditEntity + TriageAuditRepository + TenantSenderOptInEntity + TenantSenderOptInRepository + CallSite extension + ZeroMailCoreProperties cost + TenantEntity.triageShadowMode + TenantService accessors</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInEntity.java, backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInRepository.java, backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java, backend/core/src/main/java/com/zeromail/core/config/ZeroMailCoreProperties.java, backend/core/src/main/java/com/zeromail/core/tenant/persistence/TenantEntity.java, backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleEntity.java (the `extends AbstractTenantOwnedEntity` + `@JdbcTypeCode(SqlTypes.JSON) @Column(columnDefinition="jsonb") private String ...` + getter-runs-validator pattern — the verbatim model for `TriageAuditEntity`)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/MailMessageObservedRepository.java (the native `INSERT ... ON CONFLICT DO NOTHING` template — `insertObservedIfAbsent`; for triage add `RETURNING audit_id` and a `tenant_id = :tenantId` term per RESEARCH Pitfall 3)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/PubSubDeliveryRepository.java (the `@Modifying @Query` `updateStatus` / `releaseForRetry` shape with explicit-status WHERE — the model for `markApplied` / `markFailed` / `markReverted`)
    - backend/core/src/main/java/com/zeromail/core/rules/persistence/RuleRepository.java (`findByIdAndTenantId`, derived queries — the model for `TenantSenderOptInRepository`)
    - backend/core/src/main/java/com/zeromail/core/shared/persistence/AbstractTenantOwnedEntity.java (what the base hoists: `@TenantId @Column("tenant_id") private UUID tenantId` + audit columns + `@Version`)
    - backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java (existing members `TRIAGE(1)`, `DRAFT(2)`, `PREVIEW(1)` + `cost()` + `fromId` — Phase 4 ADDS `TRIAGE_PLATFORM_LLM` and `TRIAGE_DETERMINISTIC` per D-E1; do NOT remove or renumber existing members)
    - backend/core/src/main/java/com/zeromail/core/config/ZeroMailCoreProperties.java (the nested-record billing-cost properties shape — add `triage-deterministic` cost default 0 per D-D3 / "Claude's Discretion")
    - backend/core/src/main/java/com/zeromail/core/tenant/persistence/TenantEntity.java (the `triagePaused` field + `isTriagePaused()` / `setTriagePaused(...)` — mirror exactly for `triageShadowMode`)
    - backend/core/src/main/java/com/zeromail/core/tenant/service/TenantService.java (`setTriagePaused` / `isTriagePaused` — mirror for `setTriageShadowMode` / `isTriageShadowMode`; tenant-scoped, service-owned `@Transactional`)
    - backend/core/src/test/java/com/zeromail/core/billing/CallSiteEnumMembershipArchTest.java (Wave-0 expects 5 members — this task makes it GREEN)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-B2 (persistence shape), D-B4 (`gmail_change_token` top-level column), D-C3 (two-phase PENDING→APPLIED write order), D-C4 (PENDING/FAILED transitions whitelisted)
  </read_first>
  <action>
    Create `TriageAuditEntity` `@Entity @Table(name = "triage_audit") extends AbstractTenantOwnedEntity`: `@Id @Column("audit_id") private UUID auditId`; `@JdbcTypeCode(SqlTypes.JSON) @Column(name="action_args_json", columnDefinition="jsonb", nullable=false) private String actionArgsJson` whose getter runs a static `TriageActionResultJsonValidator` before returning (mirror `RuleEntity.getMatcherAst()` style); `@JdbcTypeCode(SqlTypes.JSON) @Column(name="gmail_change_token", columnDefinition="jsonb") private String gmailChangeToken` (nullable — Javadoc: stores "what we changed" not a full label snapshot, per CONTEXT §specifics); `@Column(name="args_hash", nullable=false) private byte[] argsHash` (32 raw SHA-256 bytes); plus `gmailMessageId`, `gmailThreadId`, `ruleId` (nullable), `ruleNameSnapshot`, `actionType` (stored as `RuleActionType.id()` String), `decision` (stored as `TriageDecision.id()` String), `reason`, `externalRef`, `failureReason`, `appliedAt` (Instant), `revertedAt` (Instant). No-arg ctor for Hibernate + a builder/factory for inserts where appropriate (Lombok-free).
    Create `TriageAuditRepository extends JpaRepository<TriageAuditEntity, UUID>`: a native `@Modifying @Query` `insertAuditPendingIfAbsent(...)` mirroring `MailMessageObservedRepository.insertObservedIfAbsent` — `INSERT INTO triage_audit (audit_id, tenant_id, gmail_message_id, gmail_thread_id, rule_id, rule_name_snapshot, action_type, args_hash, action_args_json, gmail_change_token, reason, decision, created_at, updated_at, version) VALUES (gen_random_uuid(), :tenantId, ..., 'PENDING', NOW(), NOW(), 0) ON CONFLICT (tenant_id, gmail_message_id, rule_id, action_type, args_hash) DO NOTHING RETURNING audit_id` returning `Optional<UUID>` (empty = already applied → orchestrator skips the Gmail call); native `@Modifying @Query` `markApplied(@Param("auditId") UUID, @Param("tenantId") UUID, @Param("externalRef") String)` = `UPDATE triage_audit SET applied_at = NOW(), decision = 'APPLIED', external_ref = :externalRef, updated_at = NOW() WHERE audit_id = :auditId AND tenant_id = :tenantId AND decision = 'PENDING'`; `markFailed(...)` = `... SET decision = 'FAILED', failure_reason = :failureReason, updated_at = NOW() WHERE audit_id = :auditId AND tenant_id = :tenantId AND decision = 'PENDING'`; `markReverted(...)` = `... SET decision = 'REVERTED', reverted_at = :revertedAt, updated_at = NOW() WHERE audit_id = :auditId AND tenant_id = :tenantId AND decision = 'APPLIED'`; plus derived queries the orchestrator/undo/purge need (e.g., `Optional<TriageAuditEntity> findByAuditIdAndTenantId(UUID, UUID)`, list-by-tenant for sender-safety-net display). EVERY native query MUST carry `tenant_id = :tenantId` (RESEARCH Pitfall 3 — `@TenantId` does not apply to native SQL; keep `MultiTenantLeakIntegrationTest` green). Do NOT declare any `delete*` repo method, and do NOT add `update*`-named methods other than the three `mark*` transitions (the Wave-0 `TriageAuditRepositoryBoundaryArchTest` enforces this).
    Create `TenantSenderOptInEntity` `@Entity @Table(name = "tenant_sender_opt_in") extends AbstractTenantOwnedEntity` (minimal shape: `@Id private UUID id`, `private String senderEmail`) and `TenantSenderOptInRepository extends JpaRepository<..., UUID>` with `boolean existsByTenantIdAndSenderEmail(UUID, String)` (HQL — `@TenantId` filtered) plus `List<TenantSenderOptInEntity> findByTenantId(UUID)`; a derived insert via `save(...)`. (HQL/derived queries inherit `@TenantId`; if any native query is added it must carry the explicit tenant term.)
    Modify `CallSite`: add members `TRIAGE_PLATFORM_LLM` and `TRIAGE_DETERMINISTIC` following the existing `IdentifiedEnum` pattern (give each a stable `id()`; `cost()` resolves from `ZeroMailCoreProperties` like the others — `TRIAGE_DETERMINISTIC` defaults to 0 credits). Do NOT remove/renumber existing members.
    Modify `ZeroMailCoreProperties`: add a `triage-deterministic` entry under the billing-cost nested record with default `0` (D-D3 / Claude's Discretion — recommend 0, configurable).
    Modify `TenantEntity`: add `@Column("triage_shadow_mode") private boolean triageShadowMode` + `isTriageShadowMode()` / `setTriageShadowMode(boolean)` (verbatim mirror of `triagePaused`).
    Modify `TenantService`: add `setTriageShadowMode(UUID tenantId, boolean enabled)` and `isTriageShadowMode(UUID tenantId)` (verbatim mirror of `setTriagePaused` / `isTriagePaused`; service-owned `@Transactional`, tenant-scoped lookup).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && grep -q "insertAuditPendingIfAbsent" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java && grep -q "TRIAGE_PLATFORM_LLM" backend/core/src/main/java/com/zeromail/core/billing/domain/CallSite.java && grep -q "triageShadowMode\|TriageShadowMode" backend/core/src/main/java/com/zeromail/core/tenant/persistence/TenantEntity.java && ./gradlew :backend:core:test --tests "*CallSiteEnumMembershipArchTest" --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*TriageAuditPersistenceContractTest" 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `TriageAuditEntity` extends `AbstractTenantOwnedEntity`, has `@JdbcTypeCode(SqlTypes.JSON)` on `actionArgsJson` (nullable=false) and `gmailChangeToken` (nullable), a `byte[] argsHash`, and its `getActionArgsJson()` invokes `TriageActionResultJsonValidator`.
    - `TriageAuditRepository.insertAuditPendingIfAbsent` is a native `INSERT ... ON CONFLICT DO NOTHING RETURNING audit_id` returning `Optional<UUID>`, carries `:tenantId`, and inserts `decision = 'PENDING'`; `markApplied` / `markFailed` (WHERE `decision='PENDING'`) and `markReverted` (WHERE `decision='APPLIED'`) exist; no `delete*` method and no `update*`-named method other than the three `mark*`.
    - `CallSite` has exactly 5 members `{TRIAGE, DRAFT, PREVIEW, TRIAGE_PLATFORM_LLM, TRIAGE_DETERMINISTIC}`; `ZeroMailCoreProperties` has a `triage-deterministic` cost default of 0.
    - `TenantEntity.triageShadowMode` + `isTriageShadowMode()`/`setTriageShadowMode()` exist; `TenantService.setTriageShadowMode(UUID,boolean)` + `isTriageShadowMode(UUID)` exist.
    - `./gradlew :backend:core:test --tests "*CallSiteEnumMembershipArchTest"` PASSES (now GREEN); `--tests "*TriageAuditRepositoryBoundaryArchTest"` PASSES; `--tests "*TriageAuditPersistenceContractTest"` PASSES (Testcontainers round-trip + `@TenantId` scoping + JSON validity).
    - `./gradlew :backend:core:test --tests "*MultiTenantLeakIntegrationTest"` still PASSES (FND-05 regression).
  </acceptance_criteria>
  <done>Triage persistence + CallSite + tenant shadow-mode accessors are landed; the membership and repo-boundary ArchUnit guards are GREEN; FND-05 still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native SQL → tenant isolation | `@TenantId` is a Hibernate HQL/Criteria filter; native queries on `triage_audit` / `tenant_sender_opt_in` bypass it unless the tenant term is explicit. |
| audit row → user-visible "what Zero Mail did" | `triage_audit` is the Phase 5 source of truth; column/JSON shape must round-trip without lossy interpretation and must never carry email content. |
| `decision` lifecycle | Only the narrow `PENDING→APPLIED/FAILED` and `APPLIED→REVERTED` transitions are permitted; any other UPDATE/DELETE on `triage_audit` is forbidden (ArchUnit-enforced). |
| `CallSite` membership | The credit ledger keys reservations off `CallSite`; additive-only extension keeps the Phase 2B ledger contract intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure / Tampering | native `triage_audit` queries | mitigate | Every `@Modifying @Query` carries `tenant_id = :tenantId` in WHERE/VALUES (RESEARCH Pitfall 3); `MultiTenantLeakIntegrationTest` re-verified GREEN; HQL/derived queries inherit `@TenantId`. |
| T-04-02-02 | Information Disclosure | `triage_audit.reason` / `action_args_json` | mitigate | `reason` stores matcher node id + structured evidence ids only (never raw text); `SaveDraft.instruction` is the rule's static instruction, never LLM-drafted body; the manual validator rejects unknown fields on write so no content-bearing field can be smuggled in. |
| T-04-02-03 | Tampering (audit immutability) | `TriageAuditRepository` method surface | mitigate | No `delete*` method; only `markApplied`/`markFailed`/`markReverted` with explicit-status WHERE clauses; `TriageAuditRepositoryBoundaryArchTest` fails the build on any other `delete*`/`update*` method. |
| T-04-02-04 | Tampering (idempotency) | unique index `(tenant_id, gmail_message_id, rule_id, action_type, args_hash)` | mitigate | DB-enforced uniqueness backs the two-phase PENDING→APPLIED write loop; `args_hash` (canonical-JSON SHA-256) disambiguates legitimate multi-action-same-rule cases (D-C1) without collapsing them. |
| T-04-02-05 | Elevation of Privilege | `CallSite` extension | mitigate | Additive only (`TRIAGE_PLATFORM_LLM`, `TRIAGE_DETERMINISTIC`); existing members unchanged; the membership ArchUnit guard pins the exact set so a future stray addition fails the build. |
</threat_model>

<verification>
- `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL.
- `./gradlew :backend:core:test --tests "*CallSiteEnumMembershipArchTest" --tests "*TriageAuditRepositoryBoundaryArchTest" --tests "*TriageActionResultJsonValidatorContractTest" --tests "*TriageAuditPersistenceContractTest" --tests "*MultiTenantLeakIntegrationTest"` all PASS.
- Testcontainers schema-push applies `025`–`027` cleanly.
</verification>

<success_criteria>
- triage_audit / tenants.triage_shadow_mode / tenant_sender_opt_in exist via additive Liquibase changelogs with the correct constraints and idempotency index.
- TriageActionResult sealed type + manual validator + canonical SHA-256 hasher + TriageDecision enum + 5 triage exceptions exist.
- TriageAuditEntity/Repository (two-phase PENDING write loop, narrow transitions, tenant-scoped native SQL) + TenantSenderOptInEntity/Repository exist.
- CallSite has 5 members; ZeroMailCoreProperties has the triage-deterministic cost; TenantEntity/TenantService expose shadow-mode accessors.
- Wave-0 membership + repo-boundary ArchUnit guards are GREEN; FND-05 still green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-02-SUMMARY.md`
</output>
