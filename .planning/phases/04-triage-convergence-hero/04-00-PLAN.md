---
phase: 04-triage-convergence-hero
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - backend/core/build.gradle.kts
  - backend/worker/build.gradle.kts
  - backend/core/src/test/java/com/zeromail/core/triage/TriageOrchestratorContractTest.java
  - backend/core/src/test/java/com/zeromail/core/triage/TriageSafetyPolicyContractTest.java
  - backend/core/src/test/java/com/zeromail/core/triage/TriageActionResultJsonValidatorContractTest.java
  - backend/core/src/test/java/com/zeromail/core/triage/TriageAuditPersistenceContractTest.java
  - backend/core/src/test/java/com/zeromail/core/triage/TriageUndoServiceContractTest.java
  - backend/core/src/test/java/com/zeromail/core/triage/SenderSafetyNetServiceContractTest.java
  - backend/core/src/test/java/com/zeromail/core/gmail/event/MailMessageObservedContractTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/TriageAuditRepositoryBoundaryArchTest.java
  - backend/core/src/test/java/com/zeromail/core/billing/CallSiteEnumMembershipArchTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageOrchestratorIntegrationContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageIdempotencyContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageShadowModeContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageCreditAccountingContractTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditPurgeJobContractTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/triage/TriageUndoControllerContractTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/triage/TriageTenantControllerContractTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/triage/SenderSafetyNetControllerContractTest.java
  - backend/core/src/test/resources/semantic-intent-eval/README.md
autonomous: true
requirements: [TRG-01, TRG-02, TRG-03, TRG-04, TRG-05, TRG-06, TRG-07, TRG-08]

must_haves:
  truths:
    - "spring-modulith-starter-jdbc resolves on both backend/core and backend/worker classpaths"
    - "A failing-by-design test spine references every Phase 4 production class that does not yet exist"
    - "CallSiteEnumMembershipArchTest now asserts 5 CallSite members (3 existing + TRIAGE_PLATFORM_LLM + TRIAGE_DETERMINISTIC) and is RED until 04-02"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "spring-modulith-starter-jdbc library alias"
      contains: "spring-modulith-starter-jdbc"
    - path: "backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java"
      provides: "ArchUnit guard banning Gmail send call sites"
      contains: "NoGmailSendAllowedTest"
    - path: "backend/core/src/test/resources/semantic-intent-eval/README.md"
      provides: "Eval-harness directory marker (fixtures/cassettes owned by eval-auditor)"
      contains: "semantic-intent-eval"
  key_links:
    - from: "backend/core/build.gradle.kts"
      to: "org.springframework.modulith:spring-modulith-starter-jdbc"
      via: "implementation dependency from the spring-modulith BOM"
      pattern: "spring-modulith-starter-jdbc"
    - from: "backend/core/src/test/java/com/zeromail/core/billing/CallSiteEnumMembershipArchTest.java"
      to: "CallSite.TRIAGE_PLATFORM_LLM / CallSite.TRIAGE_DETERMINISTIC"
      via: "membership assertion expecting 5 enum constants"
      pattern: "TRIAGE_PLATFORM_LLM"
---

<objective>
[BLOCKING — Wave 0] Add the one new dependency (`spring-modulith-starter-jdbc`) and stand up the RED-by-design verification spine for all of Phase 4: backend/core + backend/worker + backend/api test scaffolds that reference future Phase 4 production classes, the four new ArchUnit guards, the `CallSiteEnumMembershipArchTest` bump from 3 → 5 members, and the empty `semantic-intent-eval` harness directory the eval-auditor will populate.

Purpose: Establish the executable acceptance contract Waves 1–4 must satisfy. Per RESEARCH §"Validation Architecture" and STATE.md Wave-0 protocol, these tests will not compile / will fail until later plans land their production classes — that compile/fail surface IS the contract.
Output: `libs.versions.toml` + 2 build files updated; ~20 test files (mix of `@Disabled` placeholders, compile-RED references, and behavior-RED ArchUnit rules); empty eval-harness README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/phases/04-triage-convergence-hero/04-AI-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spring-modulith-starter-jdbc dependency and verify resolution</name>
  <files>gradle/libs.versions.toml, backend/core/build.gradle.kts, backend/worker/build.gradle.kts</files>
  <read_first>
    - gradle/libs.versions.toml (current [libraries] block + version pins)
    - buildSrc/src/main/kotlin/zeromail.modulith-conventions.gradle.kts (the spring-modulith BOM import — the new starter's version comes from there, NOT a separate pin)
    - backend/core/build.gradle.kts (current dependency block + applied convention plugins)
    - backend/worker/build.gradle.kts (current dependency block)
    - .planning/phases/04-triage-convergence-hero/04-RESEARCH.md §"Standard Stack" → §"Version verification (DO before authoring 024)" and Assumption A1
  </read_first>
  <action>
    Add a `[libraries]` alias in `gradle/libs.versions.toml`: `spring-modulith-starter-jdbc = { module = "org.springframework.modulith:spring-modulith-starter-jdbc" }` (no `version.ref` — the version is supplied by the `spring-modulith-bom` already imported by the `zeromail.modulith-conventions` build plugin; mirror how the existing modulith deps are declared). Add `implementation(libs.spring.modulith.starter.jdbc)` to BOTH `backend/core/build.gradle.kts` (the event publisher needs the registry on the classpath) AND `backend/worker/build.gradle.kts` (the consumer plus the `IncompleteEventPublications` / `CompletedEventPublications` / `FailedEventPublications` beans the Wave-4 retry/cleanup jobs inject). Then run `./gradlew :backend:core:dependencies --configuration runtimeClasspath` and confirm `spring-modulith-starter-jdbc` and `spring-modulith-events-jdbc` resolve at the project's pinned `springModulith` line. If the pinned snapshot line does NOT publish the JDBC starter, surface that in the plan SUMMARY (do not improvise a re-pin — that is a discuss-phase decision per A1) and stop.
  </action>
  <verify>
    <automated>./gradlew :backend:core:dependencies --configuration runtimeClasspath 2>&1 | grep -q "spring-modulith-starter-jdbc" && ./gradlew :backend:worker:dependencies --configuration runtimeClasspath 2>&1 | grep -q "spring-modulith-starter-jdbc"</automated>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` contains a `spring-modulith-starter-jdbc` library alias with no hard version pin (version inherited from the modulith BOM).
    - `backend/core/build.gradle.kts` and `backend/worker/build.gradle.kts` each declare `implementation(libs.spring.modulith.starter.jdbc)`.
    - `./gradlew :backend:core:dependencies --configuration runtimeClasspath` lists `spring-modulith-starter-jdbc` AND `spring-modulith-events-jdbc`.
    - `./gradlew :backend:core:compileJava :backend:worker:compileJava` still compiles (no production source changed in this task).
  </acceptance_criteria>
  <done>The new dependency is on both module classpaths and resolves; no production source changed.</done>
</task>

<task type="auto">
  <name>Task 2: RED test spine — backend/core + backend/worker + backend/api scaffolds referencing future Phase 4 classes</name>
  <files>backend/core/src/test/java/com/zeromail/core/triage/TriageOrchestratorContractTest.java, backend/core/src/test/java/com/zeromail/core/triage/TriageSafetyPolicyContractTest.java, backend/core/src/test/java/com/zeromail/core/triage/TriageActionResultJsonValidatorContractTest.java, backend/core/src/test/java/com/zeromail/core/triage/TriageAuditPersistenceContractTest.java, backend/core/src/test/java/com/zeromail/core/triage/TriageUndoServiceContractTest.java, backend/core/src/test/java/com/zeromail/core/triage/SenderSafetyNetServiceContractTest.java, backend/core/src/test/java/com/zeromail/core/gmail/event/MailMessageObservedContractTest.java, backend/worker/src/test/java/com/zeromail/worker/triage/TriageOrchestratorIntegrationContractTest.java, backend/worker/src/test/java/com/zeromail/worker/triage/TriageIdempotencyContractTest.java, backend/worker/src/test/java/com/zeromail/worker/triage/TriageShadowModeContractTest.java, backend/worker/src/test/java/com/zeromail/worker/triage/TriageCreditAccountingContractTest.java, backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditPurgeJobContractTest.java, backend/api/src/test/java/com/zeromail/api/controllers/triage/TriageUndoControllerContractTest.java, backend/api/src/test/java/com/zeromail/api/controllers/triage/TriageTenantControllerContractTest.java, backend/api/src/test/java/com/zeromail/api/controllers/triage/SenderSafetyNetControllerContractTest.java, backend/core/src/test/resources/semantic-intent-eval/README.md</files>
  <read_first>
    - .planning/phases/04-triage-convergence-hero/04-RESEARCH.md §"Validation Architecture" → "Phase Requirements → Test Map" + "Wave 0 Gaps" (the exact test class names + which production symbols each references)
    - .planning/phases/04-triage-convergence-hero/04-AI-SPEC.md §5 (eval-harness directory layout: `semantic-intent-eval/fixtures/*.json` + `cassettes/*.json` + `@Tag("semantic-intent-eval")` — eval-auditor owns content; Wave 0 only needs the directory marker)
    - backend/api/src/security/MultiTenantLeakIntegrationTest.java (the `RestClient + @LocalServerPort` test pattern — reuse for the api-side contract tests)
    - backend/core/src/test/java/com/zeromail/core/billing/CallSiteEnumMembershipArchTest.java (the existing 3-member assertion shape to mirror in the contract tests)
    - backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java + RulesBoundaryArchTest.java (ArchUnit idiom — needed in task 3 but read now for consistency)
    - STATE.md Decisions: "Wave 0 RED-by-design test scaffolds locked" + "02A-00: Worker RED tests use a package-local SpringBootTest scaffold because backend/core test sources are not on the worker test classpath" (apply the same package-local scaffold trick to the worker.triage contract tests)
  </read_first>
  <action>
    Create the listed test files as Wave-0 RED scaffolds following the established project pattern (mirror `02A-00` / `02C-01` / `03-00`). Rules:
    1. Each test references the future Phase 4 production classes by their planned FQN — `com.zeromail.core.triage.application.TriageOrchestratorService`, `...application.TriageUndoService`, `...application.SemanticIntentRequest` (NOTE per PATTERNS this lives in `com.zeromail.core.llm.application` — use that path), `...domain.TriageActionResult` / `TriageActionResultJsonValidator` / `TriageActionArgsCanonicalizer` / `TriageDecision`, `...persistence.TriageAuditEntity` / `TriageAuditRepository` / `TenantSenderOptInEntity` / `TenantSenderOptInRepository`, `...service.TriageSafetyPolicy` / `TriageGmailWriter` / `SenderSafetyNetService`, `...exception.TriageSafetyViolationException` / `TriageUndoExpiredException` / `TriageUndoAlreadyDoneException` / `TriageUndoUnsupportedActionException` / `TriageAuditException`, `com.zeromail.core.gmail.event.MailMessageObserved`, `com.zeromail.core.llm.service.LlmGateway#evaluateSemanticIntents`, the new worker jobs `com.zeromail.worker.triage.TriageEventRetryJob` / `TriageEventCleanupJob` / `TriageAuditPurgeJob` / `TriagePendingReaperJob`, the new api controllers `com.zeromail.api.controllers.triage.TriageAuditController` / `TriageTenantController` / `SenderSafetyNetController`, and the new error codes `ErrorCodes.TRIAGE_UNDO_EXPIRED` / `TRIAGE_UNDO_ALREADY_DONE` / `TRIAGE_UNDO_UNSUPPORTED_ACTION` / `TRIAGE_SAFETY_VIOLATION`.
    2. The assertions encode the SPEC §"Acceptance Criteria" behaviors: `MailMessageObservedContractTest` asserts the record has exactly `tenantId/gmailMessageId/gmailThreadId/observedAt` components and NO subject/snippet/body/sender-name field; `TriageActionResultJsonValidatorContractTest` asserts unknown discriminator → `NoSuchElementException` and unknown field per type → rejection; `TriageSafetyPolicyContractTest` asserts a non-allow-listed `RuleActionType` → `TriageSafetyViolationException` and zero Gmail calls; `TriageOrchestratorIntegrationContractTest` (worker) asserts event-wiring + 2-rule deterministic proposal control run + exactly 1 audit row per applied action; `TriageIdempotencyContractTest` asserts replaying `(tenantId, gmailMessageId)` twice → 1 audit row per action / ≤1 Gmail write per action; `TriageShadowModeContractTest` asserts `triage_shadow_mode=true` → `decision=SHADOW_LOGGED` + zero Gmail calls; `TriageCreditAccountingContractTest` asserts 1 `CreditLedger.reserve` per LLM call / 1 per pure-deterministic message; `TriageAuditPurgeJobContractTest` asserts rows older than 30 days with purge-set decisions are deleted (mocked clock); `TriageUndoControllerContractTest` asserts 200 within window / 409 `TRIAGE_UNDO_ALREADY_DONE` on second call / 409 `TRIAGE_UNDO_EXPIRED` at 30d+1s; `TriageTenantControllerContractTest` asserts `PATCH /api/tenant/triage/shadow-mode` flips the flag; `SenderSafetyNetControllerContractTest` asserts `GET /api/triage/sender-safety-net` lists protected senders and `POST .../{senderEmail}/opt-in` persists an override.
    3. For tests whose production fixtures (Testcontainers context, seeded rules, Gmail mock) are not yet wired, use `@org.junit.jupiter.api.Disabled("Wave 0 contract — enabled by 04-NN")` on the test METHODS but keep the class compiling against the future FQNs where those FQNs are landed by an earlier wave; where the FQN itself does not exist yet, the class is allowed to be compile-RED (that is the contract — same as `02A-00`). Worker contract tests use a package-local `@SpringBootTest` scaffold per the `02A-00` decision (backend/core test sources are not on the worker test classpath).
    4. Create `backend/core/src/test/resources/semantic-intent-eval/README.md` describing the directory contract: `fixtures/*.json` and `cassettes/*.json` are owned by the eval-auditor per AI-SPEC §5 (35 fixtures); the `semanticIntentEval` Gradle task registration is deferred to plan 04-03.
  </action>
  <verify>
    <automated>test -f backend/core/src/test/java/com/zeromail/core/gmail/event/MailMessageObservedContractTest.java && test -f backend/worker/src/test/java/com/zeromail/worker/triage/TriageOrchestratorIntegrationContractTest.java && test -f backend/api/src/test/java/com/zeromail/api/controllers/triage/TriageUndoControllerContractTest.java && test -f backend/core/src/test/resources/semantic-intent-eval/README.md</automated>
  </verify>
  <acceptance_criteria>
    - All 15 listed test files plus the eval README exist at the listed paths.
    - Every test file references at least one not-yet-existing Phase 4 production FQN (grep for `com.zeromail.core.triage.` or `com.zeromail.worker.triage.` or `com.zeromail.api.controllers.triage.` in each file returns ≥1 match).
    - `MailMessageObservedContractTest` contains an assertion that the event record has exactly 4 record components and no field name matching `(?i)subject|snippet|body|sender.?name`.
    - `backend/core/src/test/resources/semantic-intent-eval/README.md` names `fixtures/`, `cassettes/`, `@Tag("semantic-intent-eval")`, and AI-SPEC §5 as the owner.
    - `./gradlew :backend:core:test :backend:worker:test :backend:api:test` produces compile errors OR test failures attributable to missing Phase 4 production classes (NOT to malformed test source) — confirm by inspecting the failure log for `cannot find symbol` / `package com.zeromail.core.triage does not exist` style messages, which is the expected Wave-0 state.
  </acceptance_criteria>
  <done>The Phase 4 test spine exists, is RED-by-design, and the eval-harness directory marker is in place.</done>
</task>

<task type="auto">
  <name>Task 3: Four new ArchUnit guards + CallSite membership bump (3 → 5)</name>
  <files>backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java, backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java, backend/core/src/test/java/com/zeromail/core/arch/TriageAuditRepositoryBoundaryArchTest.java, backend/core/src/test/java/com/zeromail/core/billing/CallSiteEnumMembershipArchTest.java</files>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/arch/RulesBoundaryArchTest.java (the `noClasses().that().resideInAPackage(...).should().dependOnClassesThat()...` + `allowEmptyShould(true)` + `ImportOption.DoNotIncludeTests` idiom)
    - backend/core/src/test/java/com/zeromail/core/arch/SafetyContractArchTests.java (custom `ArchCondition` over `getMethodCallsFromSelf()` — the model for asserting "no class calls Gmail.Users.Messages.send / Gmail.Users.Drafts.send")
    - backend/core/src/test/java/com/zeromail/core/arch/LlmGatewayBoundaryTest.java (single-class write-boundary pattern — model for `TriageGmailWriteBoundaryTest`)
    - backend/core/src/test/java/com/zeromail/core/arch/LlmRepositoryContentBanTest.java (repo-method introspection — model for the `triage_audit` repo-method ban)
    - backend/core/src/test/java/com/zeromail/core/billing/CallSiteEnumMembershipArchTest.java (current assertion: exactly 3 members `{TRIAGE, DRAFT, PREVIEW}` — bump to 5)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §"Acceptance Criteria" (the exact wording of `NoGmailSendAllowedTest` and `TriageGmailWriteBoundaryTest` contracts)
  </read_first>
  <action>
    Create `NoGmailSendAllowedTest`: a custom `ArchCondition` (mirror `SafetyContractArchTests`) that fails the build if ANY class under `com.zeromail` calls `com.google.api.services.gmail.Gmail$Users$Messages#send` or `Gmail$Users$Drafts#send` (any method whose owner ends with `Gmail.Users.Messages` / `Gmail.Users.Drafts` and whose name is `send`). Also assert `com.zeromail.core.rules.domain.RuleActionType` has no constant named `SEND` (reflective `RuleActionType.valueOf("SEND")` → `IllegalArgumentException`). Use `ImportOption.DoNotIncludeTests` and `allowEmptyShould(true)` so the rule is GREEN today (no Gmail write call sites exist yet) and stays GREEN as `TriageGmailWriter` lands (its `send`-free methods are allowed).
    Create `TriageGmailWriteBoundaryTest`: fails the build if any class OTHER than `com.zeromail.core.triage.service.TriageGmailWriter` invokes a Gmail write API (`Gmail.Users.Messages#modify`, `Gmail.Users.Drafts#create`, `Gmail.Users.Drafts#delete`) from within the `com.zeromail.core.triage..` package tree. `allowEmptyShould(true)`.
    Create `TriageAuditRepositoryBoundaryArchTest`: fails the build if `com.zeromail.core.triage.persistence.TriageAuditRepository` declares any method whose name starts with `delete` or `update` EXCEPT the explicit narrow-transition methods `markApplied`, `markFailed`, `markReverted` (mirror `LlmRepositoryContentBanTest`'s method-introspection style). `allowEmptyShould(true)`.
    Modify `CallSiteEnumMembershipArchTest`: change the expected member set from exactly `{TRIAGE, DRAFT, PREVIEW}` (3) to exactly `{TRIAGE, DRAFT, PREVIEW, TRIAGE_PLATFORM_LLM, TRIAGE_DETERMINISTIC}` (5). This test goes RED now and stays RED until plan 04-02 extends `CallSite` — that RED state IS the Wave-0 contract.
  </action>
  <verify>
    <automated>test -f backend/core/src/test/java/com/zeromail/core/arch/NoGmailSendAllowedTest.java && test -f backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java && test -f backend/core/src/test/java/com/zeromail/core/arch/TriageAuditRepositoryBoundaryArchTest.java && grep -q "TRIAGE_PLATFORM_LLM" backend/core/src/test/java/com/zeromail/core/billing/CallSiteEnumMembershipArchTest.java</automated>
  </verify>
  <acceptance_criteria>
    - `NoGmailSendAllowedTest.java`, `TriageGmailWriteBoundaryTest.java`, `TriageAuditRepositoryBoundaryArchTest.java` exist under `backend/core/src/test/java/com/zeromail/core/arch/`.
    - `NoGmailSendAllowedTest` references both `Gmail.Users.Messages` / `Gmail.Users.Drafts` `send` and asserts `RuleActionType` has no `SEND` constant.
    - `TriageGmailWriteBoundaryTest` names `com.zeromail.core.triage.service.TriageGmailWriter` as the single permitted Gmail-write call site.
    - `CallSiteEnumMembershipArchTest` asserts a 5-element member set including `TRIAGE_PLATFORM_LLM` and `TRIAGE_DETERMINISTIC`.
    - `./gradlew :backend:core:test --tests "*NoGmailSendAllowedTest"` PASSES today (no Gmail write call sites exist); `./gradlew :backend:core:test --tests "*CallSiteEnumMembershipArchTest"` FAILS today (only 3 members exist) — both are the expected Wave-0 states.
  </acceptance_criteria>
  <done>Four ArchUnit guards exist; `NoGmailSendAllowedTest` is GREEN, `CallSiteEnumMembershipArchTest` is RED-by-design pending 04-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build classpath → runtime | A new third-party dependency (`spring-modulith-starter-jdbc`) enters the supply chain; version must come from the already-trusted modulith BOM, not an ad-hoc pin. |
| test source → CI gate | RED-by-design tests must fail for the *intended* reason (missing Phase 4 classes), not mask real regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | new Gradle dependency | mitigate | Version inherited from the existing `spring-modulith-bom` (already imported by `zeromail.modulith-conventions`); no standalone pin; resolution verified via `./gradlew :backend:core:dependencies`. |
| T-04-00-02 | Repudiation | RED test spine | mitigate | Each test file is required to reference a concrete future Phase 4 FQN; the acceptance criteria grep ensures the spine cannot silently degrade into empty `@Disabled` shells with no contract. |
| T-04-00-03 | Elevation of Privilege | `NoGmailSendAllowedTest` / `TriageGmailWriteBoundaryTest` | mitigate | These guards are *added in Wave 0* so the "auto-send forbidden" and "single Gmail-write call site" invariants are enforced from the first commit that touches triage code, not retrofitted. |
</threat_model>

<verification>
- `./gradlew :backend:core:dependencies --configuration runtimeClasspath` lists `spring-modulith-starter-jdbc`.
- All Phase 4 test scaffolds + 4 ArchUnit guards + eval README exist at the documented paths.
- `./gradlew :backend:core:test --tests "*NoGmailSendAllowedTest"` PASSES; `./gradlew :backend:core:test --tests "*CallSiteEnumMembershipArchTest"` FAILS (RED-by-design); the broader `:backend:core:test :backend:worker:test :backend:api:test` fails only on missing-Phase-4-class compile/test errors.
</verification>

<success_criteria>
- Single new dependency added and resolving on both backend modules.
- ~20 RED-by-design test files + eval README in place, each carrying a real Phase 4 contract.
- `CallSiteEnumMembershipArchTest` bumped 3 → 5; `NoGmailSendAllowedTest` GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-00-SUMMARY.md`
</output>
