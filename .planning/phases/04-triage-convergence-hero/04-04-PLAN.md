---
phase: 04-triage-convergence-hero
plan: 04
type: execute
wave: 4
depends_on: [00, 01, 02, 03]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/triage/service/TriageSafetyPolicy.java
  - backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java
  - backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java
  - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java
  - backend/core/src/main/java/com/zeromail/core/triage/config/TriageRedisConfig.java
  - backend/core/build.gradle.kts
  - backend/worker/build.gradle.kts
  - backend/api/build.gradle.kts
  - backend/worker/src/main/resources/application.yml
  - backend/api/src/main/resources/application.yml
  - backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java
autonomous: true
requirements: [TRG-02, TRG-03, TRG-04, TRG-08]

must_haves:
  truths:
    - "TriageSafetyPolicy rejects any ActionProposal whose RuleActionType is outside {LABEL, ARCHIVE, SAVE_DRAFT} with TriageSafetyViolationException and zero Gmail calls"
    - "TriageGmailWriter is the ONLY class in core.triage that calls Gmail write APIs; it exposes exactly applyLabel / archiveSkipInbox / saveDraft plus the inverse calls removeLabel / restoreToInbox / deleteDraft and NO send method"
    - "SenderSafetyNetService.isProtected returns true when Gmail SENT history has >= 3 messages to the sender in the trailing 90 days, reading metadata only, cached in Redis 24h, with the tenant_sender_opt_in row overriding protection"
    - "Triage code obtains authenticated Gmail clients via a facade on core.gmail (no direct core.triage -> gmail.persistence.crypto edge)"
    - "The sender-net Redis cache degrades gracefully (re-queries Gmail) if Redis is unavailable"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/triage/service/TriageSafetyPolicy.java"
      provides: "Allow-list gate over RuleActionType with fail-closed exception"
      contains: "EnumSet"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java"
      provides: "Single Gmail-write call site: applyLabel / archiveSkipInbox / saveDraft + inverse calls"
      contains: "modify"
    - path: "backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java"
      provides: "Sent-history heuristic + Redis 24h cache + opt-in override"
      contains: "newer_than:90d"
  key_links:
    - from: "backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java"
      to: "GmailApiClientFactory facade (decrypt + refresh + buildGmailClient internally)"
      via: "buildClientForTenant(tenantId) — cipher stays in core.gmail"
      pattern: "buildClientForTenant|buildGmailClientForTenant"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java"
      to: "tenant_sender_opt_in (override) + Redis (cache) + Gmail SENT list (read)"
      via: "opt-in row short-circuits to protected=false; else Redis hit/miss; else Gmail metadata list"
      pattern: "existsByTenantIdAndSenderEmail"
    - from: "backend/core/src/main/java/com/zeromail/core/triage/service/TriageSafetyPolicy.java"
      to: "TriageSafetyViolationException"
      via: "throw on RuleActionType not in the allow-list EnumSet"
      pattern: "TriageSafetyViolationException"
---

<objective>
[Wave 4] Land the three triage domain services: `TriageSafetyPolicy` (allow-list gate — the runtime backstop for the architectural "auto-send forbidden" guarantee), `TriageGmailWriter` (the ONLY class permitted to call Gmail write APIs from triage code — `applyLabel` / `archiveSkipInbox` / `saveDraft` plus the inverse `removeLabel` / `restoreToInbox` / `deleteDraft` for undo, all `send`-free), and `SenderSafetyNetService` (sent-history `>=3 in 90d` heuristic, Gmail SENT metadata-only, Redis 24h cache with graceful degradation, `tenant_sender_opt_in` override). Includes the `core.gmail` facade that builds authenticated Gmail clients (so the AES-GCM cipher stays inside `core.gmail` — no direct `core.triage → gmail.persistence.crypto` edge per CONTEXT Open Question 1) and the Redis bean wiring (Assumption A6).

Purpose: per SPEC §req 2/3/4/8, D-E3 (sender-cache invalidation on opt-in), CONTEXT Open Questions 1 (Gmail facade) and Assumption A6 (Redis wiring).
Output: 3 triage services; 1 modified gmail factory (new facade method); 1 Redis config + build/yml wiring; verify `TriageGmailWriteBoundaryTest` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-convergence-hero/04-SPEC.md
@.planning/phases/04-triage-convergence-hero/04-CONTEXT.md
@.planning/phases/04-triage-convergence-hero/04-RESEARCH.md
@.planning/phases/04-triage-convergence-hero/04-PATTERNS.md
@.planning/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TriageSafetyPolicy allow-list gate + core.gmail Gmail-client facade</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/service/TriageSafetyPolicy.java, backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/llm/service/ActionValidator.java (the `EnumSet ALLOW_LIST` + fail-closed `SafetyViolationException` pattern — the verbatim model for `TriageSafetyPolicy`; note the structure: null/blank → throw, unknown → throw, not-in-set → throw)
    - backend/core/src/main/java/com/zeromail/core/rules/domain/RuleActionType.java (the enum membership `{LABEL, ARCHIVE, SAVE_DRAFT}` — Phase 4 adds NO new values; the allow-list EnumSet uses exactly these)
    - backend/core/src/main/java/com/zeromail/core/rules/service/ActionProposalMerger.java + the `ActionProposal` type (what an `ActionProposal` carries — `RuleActionType` + the `ActionIntent` payload + the originating rule id; `TriageSafetyPolicy.gate(...)` inspects the `RuleActionType`)
    - backend/core/src/main/java/com/zeromail/core/triage/exception/TriageSafetyViolationException.java (created in 04-02 — no-arg, no payload)
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java (existing `refreshAccessToken(decryptedRefreshToken)` + `buildGmailClient(accessToken)` — add a `buildClientForTenant(UUID tenantId)` facade)
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailDeliveryProcessingService.java lines 59-67 (the decrypt-refresh-build dance to lift into the facade)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCipher.java (the cipher — it must NOT be referenced from `core.triage`)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md "Claude's Discretion" — "Recommend NO direct edge from core.triage to core.gmail.persistence.crypto"; CONTEXT Open Question 1
    - backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java (Wave-0 — the facade is in `core.gmail`, not `core.triage`, so it's fine)
  </read_first>
  <action>
    Create `com.zeromail.core.triage.service.TriageSafetyPolicy` as a `@Component` mirroring `ActionValidator`: `private static final EnumSet<RuleActionType> ALLOW_LIST = EnumSet.of(RuleActionType.LABEL, RuleActionType.ARCHIVE, RuleActionType.SAVE_DRAFT)`; a method `RuleActionType gate(ActionProposal proposal)` (the orchestrator passes the proposal so the policy can read its `RuleActionType` and the originating rule id for logging) that: throws `TriageSafetyViolationException` if the `RuleActionType` is null or `!ALLOW_LIST.contains(actionType)`; on rejection logs `log.warn("event=triage_safety_violation tenantId={} ruleId={} actionType={}", tenantId, ruleId, actionType)` (ids only — no content) BEFORE throwing; returns the `RuleActionType` unchanged on success. Javadoc: this is the runtime backstop for the architectural "auto-send forbidden" guarantee; `NoGmailSendAllowedTest` is the compile-time twin.
    Modify `GmailApiClientFactory` (in `core.gmail`): add `public Gmail buildClientForTenant(UUID tenantId)` that internally resolves the tenant's `GmailConnectionEntity` (via the connection repository — inject it, or expose a thin overload `buildClientForConnection(GmailConnectionEntity, UUID tenantId)` and a `buildClientForTenant(UUID)` convenience), `refreshTokenCipher.decrypt(connection.getRefreshTokenEncrypted(), tenantId.toString())` → `refreshAccessToken(decryptedRefreshToken)` → `buildGmailClient(tokenResult.accessToken().value())`, zeroing the decrypted bytes after use (mirror the existing zeroing pattern). This is the single place triage code obtains an authenticated Gmail client; `RefreshTokenCipher` never leaves `core.gmail`. Do NOT remove or change the existing factory methods.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && grep -q "EnumSet" backend/core/src/main/java/com/zeromail/core/triage/service/TriageSafetyPolicy.java && grep -q "TriageSafetyViolationException" backend/core/src/main/java/com/zeromail/core/triage/service/TriageSafetyPolicy.java && grep -q "buildClientForTenant" backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java && ./gradlew :backend:core:test --tests "*TriageSafetyPolicyContractTest" 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `TriageSafetyPolicy` has an `EnumSet<RuleActionType>` allow-list of exactly `{LABEL, ARCHIVE, SAVE_DRAFT}`; `gate(...)` throws `TriageSafetyViolationException` for null and for any `RuleActionType` not in the set, logs `event=triage_safety_violation` with ids only before throwing, and returns the type on success.
    - `GmailApiClientFactory.buildClientForTenant(UUID)` builds an authenticated `Gmail` client from the tenant's encrypted refresh token internally; `RefreshTokenCipher` is not referenced from `core.triage`.
    - `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageSafetyPolicyContractTest"` PASSES (a non-allow-listed `RuleActionType` → exception, zero Gmail calls).
  </acceptance_criteria>
  <done>The allow-list gate exists with fail-closed semantics; the Gmail-client facade keeps the cipher inside `core.gmail`.</done>
</task>

<task type="auto">
  <name>Task 2: TriageGmailWriter — the single Gmail-write call site (forward + inverse, send-free)</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java, backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java (the Gmail API request shape, `GoogleJsonResponseException` handling, `GmailApiClientFactory` reuse — the model for the write adapter's request/error idiom)
    - backend/core/src/main/java/com/zeromail/core/triage/domain/TriageActionResult.java (created in 04-02 — `Label(labelId, labelName)` / `Archive()` / `SaveDraft(instruction, draftId, threadId)`; the writer's methods take/return the data these records carry)
    - .planning/phases/04-triage-convergence-hero/04-PATTERNS.md the `TriageGmailWriter` row (exact Gmail calls: `applyLabel` → `gmail.users().messages().modify("me", id, new ModifyMessageRequest().setAddLabelIds(List.of(labelId)))`; `archiveSkipInbox` → `...modify(...).setRemoveLabelIds(List.of("INBOX"))`; `saveDraft` → `gmail.users().drafts().create("me", new Draft().setMessage(...))` returning the `draftId`; inverse: `removeLabel` → `...modify(...).setRemoveLabelIds(List.of(labelId))`; `restoreToInbox` → `...modify(...).setAddLabelIds(List.of("INBOX"))`; `deleteDraft` → `gmail.users().drafts().delete("me", draftId)`)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 3/4 + §"Acceptance Criteria" (no `sendDraft`/`sendMessage` anywhere in triage code; `users.messages.modify` idempotent, `users.drafts.create` NOT — the orchestrator's PENDING→APPLIED loop handles it, RESEARCH Pitfall 1; grep for `users.messages().send` / `users.drafts().send` across `backend/` must return zero matches)
    - CLAUDE.md §"Hard do not use" — auto-send forbidden; CLAUDE.md §Conventions #5 — content-free logging
    - backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java (Wave-0 — names `TriageGmailWriter` as the single permitted Gmail-write call site; this task makes it GREEN against a real class)
  </read_first>
  <action>
    Create `com.zeromail.core.triage.service.TriageGmailWriter` as a `@Component` injecting `GmailApiClientFactory` (using `buildClientForTenant(...)` from task 1 — NOT `RefreshTokenCipher`). Public methods (and ONLY these write-shaped methods — no `sendDraft`/`sendMessage`):
    - `void applyLabel(UUID tenantId, String gmailMessageId, String labelId)` → `messages().modify("me", gmailMessageId, new ModifyMessageRequest().setAddLabelIds(List.of(labelId)))`.
    - `void archiveSkipInbox(UUID tenantId, String gmailMessageId)` → `messages().modify("me", gmailMessageId, new ModifyMessageRequest().setRemoveLabelIds(List.of("INBOX")))`.
    - `String saveDraft(UUID tenantId, TriageActionResult.SaveDraft draftSpec, String gmailThreadId)` → builds a MIME `Message` from the rule's static `instruction` (SPEC §req 4 — NO LLM call here; Phase 5 owns AI-drafted content) inside a `Draft` with `setThreadId(gmailThreadId)`, `drafts().create("me", draft)`, returns the created `draftId`.
    - Inverse methods for undo: `void removeLabel(UUID tenantId, String gmailMessageId, String labelId)` → `setRemoveLabelIds(List.of(labelId))`; `void restoreToInbox(UUID tenantId, String gmailMessageId)` → `setAddLabelIds(List.of("INBOX"))`; `void deleteDraft(UUID tenantId, String draftId)` → `drafts().delete("me", draftId)`.
    Each method: build the client via the facade, perform the single Gmail call, translate `GoogleJsonResponseException` (mirror `GmailPreviewReadService`) — rethrow (or wrap in a triage-side exception) for the orchestrator's two-phase loop to mark `FAILED`. Log `log.info("event=triage_gmail_write tenantId={} gmailMessageId={} op={}", ...)` — ids + op name only, no labels/subjects/body. Javadoc: this is the ONLY class allowed to call Gmail write APIs from triage code (`TriageGmailWriteBoundaryTest` + `NoGmailSendAllowedTest` enforce this); `messages.modify` idempotency is relied upon by the orchestrator's PENDING→APPLIED retry path, `drafts.create` non-idempotency is the reason that path exists.
    Verify the Wave-0 `TriageGmailWriteBoundaryTest` now resolves against the real `com.zeromail.core.triage.service.TriageGmailWriter` and is GREEN; update the test's import/reference if needed but keep its assertion intact.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&1 | tail -2 && grep -q "modify" backend/core/src/main/java/com/zeromail/core/triage/service/TriageGmailWriter.java && ! grep -rq "messages().send\|drafts().send" backend/ && ./gradlew :backend:core:test --tests "*TriageGmailWriteBoundaryTest" --tests "*NoGmailSendAllowedTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TriageGmailWriter` exposes exactly `applyLabel`, `archiveSkipInbox`, `saveDraft` (forward) + `removeLabel`, `restoreToInbox`, `deleteDraft` (inverse); no `sendDraft` / `sendMessage` / any `send`-named method.
    - It obtains the Gmail client via `GmailApiClientFactory.buildClientForTenant(...)`; no `RefreshTokenCipher` reference in `core.triage`.
    - A repo-wide grep for `messages().send` / `drafts().send` returns zero matches.
    - `./gradlew :backend:core:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*TriageGmailWriteBoundaryTest" --tests "*NoGmailSendAllowedTest"` both PASS.
  </acceptance_criteria>
  <done>The single Gmail-write call site exists with forward+inverse, send-free methods; the boundary and no-send ArchUnit guards are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: SenderSafetyNetService (sent-history heuristic + Redis 24h cache + opt-in override) + Redis bean wiring</name>
  <files>backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java, backend/core/src/main/java/com/zeromail/core/triage/config/TriageRedisConfig.java, backend/core/build.gradle.kts, backend/worker/build.gradle.kts, backend/api/build.gradle.kts, backend/worker/src/main/resources/application.yml, backend/api/src/main/resources/application.yml</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailPreviewReadService.java (Gmail `users().messages()` request shape, metadata fetch, `Clock` injection, `GoogleJsonResponseException` handling — the model for the SENT-history lookup)
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailApiClientFactory.java (the `buildClientForTenant(...)` facade from task 1 — `SenderSafetyNetService` uses it; no cipher reference)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInEntity.java + TenantSenderOptInRepository.java (created in 04-02 — `existsByTenantIdAndSenderEmail(UUID, String)` overrides protection; `findByTenantId(UUID)` lists protected/opted-in senders for the REST endpoint in 04-06)
    - .planning/phases/04-triage-convergence-hero/04-SPEC.md §req 8 (the exact heuristic: protected when ≥ 3 sent emails to the sender's address in the trailing 90 days; Gmail SENT label metadata only, query `in:sent to:<senderEmail> newer_than:90d`, `setMaxResults(3L)` early-exit, never bodies/snippets; cache key `triage:sender-protect:{tenantId}:{lower(senderEmail)}`, 24h TTL; opt-in row overrides → no longer protected) + §"Acceptance Criteria" (seeded SENT fixture of 3 messages → `decision=REJECTED_BY_SAFETY_NET`; after opt-in → `decision=APPLIED`; cache hit on second call within 24h verified via Redis key inspection)
    - .planning/phases/04-triage-convergence-hero/04-CONTEXT.md D-E3 (Redis key shape; cache stores ONLY the boolean `protected` flag, not the sent count — minimizes footprint + avoids leaking sent-volume metadata via cache inspection; cache invalidation on opt-in via `TransactionSynchronization.afterCommit` in the SAME service method that inserts the `tenant_sender_opt_in` row, AFTER DB commit)
    - .planning/phases/04-triage-convergence-hero/04-RESEARCH.md Assumption A6 + Environment Availability "Redis" row (no `RedisTemplate`/`StringRedisTemplate` bean exists today — verify with a grep; add `spring-boot-starter-data-redis` + connection config + a `StringRedisTemplate` bean if absent; the cache is best-effort — degrade to a Gmail re-query if Redis is down)
    - backend/worker/src/main/resources/application.yml + backend/api/src/main/resources/application.yml + the docker-compose Redis service (the `spring.data.redis.host`/`port` config to add; worker-only / api-only props belong in the respective module's yml per CLAUDE.md Convention #9 — `core.triage` ships the shared `StringRedisTemplate` bean but each runnable module declares its own connection values)
    - CLAUDE.md §Conventions #5 (privacy logging); CLAUDE.md §Technology Stack ("Redis ... for rate limiting, idempotency, session store, per-tenant ChatModel cache — NOT a queue")
  </read_first>
  <action>
    First, grep `backend/` for `StringRedisTemplate` / `RedisTemplate` / `LettuceConnectionFactory` / `spring.data.redis`. If a `StringRedisTemplate` bean / Spring Data Redis auto-config is NOT present: add `implementation("org.springframework.boot:spring-boot-starter-data-redis")` to `backend/core/build.gradle.kts` (the service lives in core) and ensure it's on the `backend/worker` + `backend/api` runtime classpaths (add to those build files if not transitively present); add `spring.data.redis.host` / `spring.data.redis.port` (pointing at the docker-compose Redis service / `${REDIS_HOST:localhost}` / `${REDIS_PORT:6379}`) to `backend/worker/src/main/resources/application.yml` and `backend/api/src/main/resources/application.yml` (worker needs it for the orchestrator's `isProtected` check; api needs it for the opt-in cache invalidation in 04-06). Create `com.zeromail.core.triage.config.TriageRedisConfig` exposing a `@Bean StringRedisTemplate triageSenderSafetyNetRedisTemplate(RedisConnectionFactory)` (or reuse the auto-configured `StringRedisTemplate` if Spring Boot already provides one — prefer the auto-config; only add the config class if a named/qualified template is needed). If a `StringRedisTemplate` already exists, skip the dependency/config additions and note it in the SUMMARY.
    Create `com.zeromail.core.triage.service.SenderSafetyNetService` as a `@Component` injecting `GmailApiClientFactory` (the facade), `TenantSenderOptInRepository`, an `ObjectProvider<StringRedisTemplate>` (so Redis is optional — the cache is best-effort), a `Clock` (package-private ctor for the mocked-clock-friendly tests, mirror `GmailPreviewReadService`), and a counter (Micrometer) for `triage.sender_safety_net.cache_hit` / `..._miss`. Method `boolean isProtected(UUID tenantId, String senderEmail)`:
    1. If `tenantSenderOptInRepository.existsByTenantIdAndSenderEmail(tenantId, senderEmail.toLowerCase(Locale.ROOT))` → return `false` (opt-in overrides — short-circuit, no Gmail call, no cache write).
    2. Else, compute the Redis key `"triage:sender-protect:" + tenantId + ":" + senderEmail.toLowerCase(Locale.ROOT)`; if Redis is available and the key holds a value, parse the boolean and return it (cache hit; increment cache_hit counter).
    3. Else (cache miss or Redis down): build the Gmail client via the facade, `gmail.users().messages().list("me").setQ("in:sent to:" + senderEmail + " newer_than:90d").setMaxResults(3L).execute()`, compute `protectedFlag = (count of returned message ids >= 3)` (or `getResultSizeEstimate() >= 3` — prefer counting the returned `messages` list for accuracy, capped at 3 by `maxResults`); never call `.get(...)` on any message — list returns ids only. If Redis is available, `redisTemplate.opsForValue().set(key, String.valueOf(protectedFlag), Duration.ofHours(24))` (store ONLY the boolean — never the count). Return `protectedFlag`.
    Method `void optInSender(UUID tenantId, String senderEmail)` (called by the REST controller in 04-06 but defined here): inside a service-owned `@Transactional`, insert a `TenantSenderOptInEntity` (lower-cased email) if not already present, then register a `TransactionSynchronization.afterCommit` callback that does `redisTemplate.delete(key)` for that sender (best-effort; only if Redis is available) — so the next `isProtected` re-evaluates and now short-circuits to `false` via the opt-in check (D-E3). Method `List<String> listProtectedOptInSenders(UUID tenantId)` returns the opted-in senders (for the `GET` endpoint). Log `log.info("event=triage_sender_safety_net_checked tenantId={} senderEmailHash={} protected={} source={}", tenantId, hashOf(senderEmail), protectedFlag, "optin|cache|gmail")` — hash the sender email or log only a stable id; NEVER log the raw sender address or any subject/snippet (privacy invariant). On Gmail failure: log `event=triage_sender_safety_net_lookup_failed` and FAIL-SAFE — treat the sender as PROTECTED (return `true`) so a Gmail outage never causes an over-eager auto-action; document this fail-safe choice in Javadoc.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava :backend:worker:compileJava :backend:api:compileJava 2>&1 | tail -3 && grep -q "newer_than:90d" backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java && grep -q "triage:sender-protect:" backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java && grep -q "afterCommit" backend/core/src/main/java/com/zeromail/core/triage/service/SenderSafetyNetService.java && ./gradlew :backend:core:test --tests "*SenderSafetyNetServiceContractTest" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `SenderSafetyNetService.isProtected(UUID, String)` short-circuits to `false` when an opt-in row exists; otherwise reads Redis (24h TTL key `triage:sender-protect:{tenantId}:{lower(senderEmail)}` storing only the boolean), and on miss queries Gmail SENT with `in:sent to:<senderEmail> newer_than:90d` `setMaxResults(3L)` returning `protected = count >= 3`; never calls `.get(...)` on a message.
    - On Gmail lookup failure the service fails SAFE (returns `true` / protected) and logs `event=triage_sender_safety_net_lookup_failed` with no raw sender address.
    - `optInSender(...)` inserts the `tenant_sender_opt_in` row in a service `@Transactional` and registers a `TransactionSynchronization.afterCommit` Redis-delete for that sender (best-effort).
    - Redis is optional: the service uses an `ObjectProvider<StringRedisTemplate>` (or null-safe access) and re-queries Gmail if Redis is unavailable.
    - If Spring Data Redis was not already wired, `spring-boot-starter-data-redis` + `spring.data.redis.*` config is added to the relevant build files / `application.yml` files (worker + api).
    - `./gradlew :backend:core:compileJava :backend:worker:compileJava :backend:api:compileJava` BUILD SUCCESSFUL; `./gradlew :backend:core:test --tests "*SenderSafetyNetServiceContractTest"` PASSES.
  </acceptance_criteria>
  <done>The sent-history sender safety net exists with the 90d/≥3 heuristic, optional Redis 24h cache, opt-in override + afterCommit invalidation, and fail-safe Gmail-outage behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule-engine proposals → Gmail writes | `TriageSafetyPolicy` is the runtime allow-list gate; nothing reaches `TriageGmailWriter` without passing it. |
| `core.triage` → Gmail credentials | Triage never touches `RefreshTokenCipher`; it gets an authenticated client via a `core.gmail` facade — the cipher's blast radius stays in `core.gmail`. |
| Gmail write API surface | Exactly one class (`TriageGmailWriter`) may call Gmail write APIs from triage code; `send` is absent everywhere (ArchUnit-enforced). |
| sender identity → automated action | The sent-history safety net suppresses destructive actions on correspondents the user actively engages; a Gmail outage fails safe (treats senders as protected). |
| Redis cache contents | Stores only a boolean `protected` flag — never sent counts, never email content — so cache inspection leaks nothing about sent volume. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Elevation of Privilege | `TriageSafetyPolicy` | mitigate | Fail-closed `EnumSet` allow-list `{LABEL, ARCHIVE, SAVE_DRAFT}`; null / unknown / not-in-set → `TriageSafetyViolationException`; orchestrator records `decision=REJECTED_BY_SAFETY_POLICY`; the runtime backstop for "auto-send forbidden". |
| T-04-04-02 | Elevation of Privilege / Tampering | Gmail write surface | mitigate | `TriageGmailWriter` is the single Gmail-write call site (ArchUnit `TriageGmailWriteBoundaryTest`); no `send`-named method anywhere (`NoGmailSendAllowedTest` + repo-wide grep); inverse calls (`removeLabel`/`restoreToInbox`/`deleteDraft`) are also funneled here. |
| T-04-04-03 | Information Disclosure | Gmail credentials in `core.triage` | mitigate | Triage obtains clients via `GmailApiClientFactory.buildClientForTenant(...)`; `RefreshTokenCipher` is referenced only from `core.gmail`; the decrypted bytes are zeroed inside the facade; no `core.triage → gmail.persistence.crypto` Modulith edge. |
| T-04-04-04 | Information Disclosure | sender-net Gmail reads / cache / logs | mitigate | SENT lookup is metadata-only (`format`-free `messages.list` returns ids; `maxResults=3` early-exit; no `.get(...)`); Redis stores only the boolean flag; logs carry a hashed/id-only sender, never the raw address or any subject/snippet. |
| T-04-04-05 | Denial of Service | Gmail outage → over-eager action | mitigate | `SenderSafetyNetService` fails SAFE on Gmail error (returns `protected=true`), so an outage suppresses auto-actions rather than letting a destructive action fire unchecked; the cache is best-effort (Redis down → re-query, never throw). |
| T-04-04-06 | Tampering (consistency) | opt-in row vs cache | mitigate | The cache delete is registered via `TransactionSynchronization.afterCommit` in the SAME `@Transactional` that inserts the `tenant_sender_opt_in` row — the cache is invalidated only after the override is durably committed; the opt-in check also short-circuits ahead of the cache so a stale cache entry can't keep a sender protected after opt-in. |
</threat_model>

<verification>
- `./gradlew :backend:core:compileJava :backend:worker:compileJava :backend:api:compileJava` BUILD SUCCESSFUL.
- `./gradlew :backend:core:test --tests "*TriageSafetyPolicyContractTest" --tests "*TriageGmailWriteBoundaryTest" --tests "*NoGmailSendAllowedTest" --tests "*SenderSafetyNetServiceContractTest"` all PASS.
- Repo-wide grep for `messages().send` / `drafts().send` returns zero matches.
</verification>

<success_criteria>
- `TriageSafetyPolicy` allow-list gate exists with fail-closed semantics; `GmailApiClientFactory.buildClientForTenant(...)` facade keeps the cipher in `core.gmail`.
- `TriageGmailWriter` is the single Gmail-write call site with forward+inverse, send-free methods; `TriageGmailWriteBoundaryTest` + `NoGmailSendAllowedTest` GREEN.
- `SenderSafetyNetService` implements the 90d/≥3 sent-history heuristic, optional Redis 24h cache, opt-in override + afterCommit invalidation, fail-safe Gmail-outage behavior; Redis bean wired.
</success_criteria>

<output>
After completion, create `.planning/phases/04-triage-convergence-hero/04-04-SUMMARY.md`
</output>
