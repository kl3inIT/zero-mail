---
phase: 01.4-gmail-identity-semantics-permission-ux-and-ui-consistency
verified: 2026-04-26T22:25:55Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Live Google OAuth account-chooser UX with login_hint"
    expected: "User signed-in as Account A clicking 'Connect Gmail' lands on Google's chooser pre-selecting Account A; switching to Account B and granting redirects to /onboarding?error=gmail_identity_mismatch with no GmailConnectionEntity row written and the just-issued Google token revoked."
    why_human: "Requires live Google OAuth + two real Workspace test accounts; the chooser UI cannot be stubbed in JUnit/Vitest. Subject-mismatch path is integration-tested with a fixture, but the UX (chooser pre-selection + revoke side-effect on Google's side) is observable only against real Google."
  - test: "Native-Vietnamese copy review"
    expected: "All 13 new VI keys (errors.boundary.*, errors.notFound.*, gmail.identity.mismatch.*, gmail.consent.denied.*, settings.gmailConnection.singleAccountNote) read naturally; tone matches confident-not-apologetic stance set in CONTEXT D-D5."
    why_human: "i18n:check enforces vi/en parity but cannot judge fluency, register, or tone. CLAUDE memory flags the user as Vietnamese-fluent — likely the right reviewer."
  - test: "Visual sweep across 12 surfaces"
    expected: "Token discipline produces visually consistent surfaces: spacing rhythm, border/background tokens, typography hierarchy match UI-SPEC §Spacing/Typography across /login, /onboarding (4 step states), /settings, /, /docs, /docs/[slug], plus shared components LanguageSwitcher / ConnectionHealthBadge / ReconnectPrompt / DeleteAccountDialog / TemplateCard. Light theme only (dark-mode toggle deferred to Phase 5)."
    why_human: "No visual regression baseline yet (Storybook/Chromatic deferred per CONTEXT). Subjective consistency check requires `pnpm dev` walkthrough with a designer eye."
  - test: "Reconnect-after-disconnect flow"
    expected: "User who disconnected Gmail clicks ReconnectPrompt → /tenant/connect-gmail → Google chooser pre-selects matching email via login_hint → grant succeeds → connection row updates in-place (no duplicate row), status flips back to CONNECTED, disconnectedAt cleared."
    why_human: "Requires live OAuth + DB inspection; D-D6 specifies 'no special-case logic' so we are validating that the same code paths truly work on the reconnect leg."
  - test: "Onboarding mismatch alert dismissal"
    expected: "Visiting /onboarding?error=gmail_identity_mismatch renders StatusAlert variant=error; clicking 'Try again' clears the query string (router.replace) and navigates to /tenant/connect-gmail; closing without retry does not flash the alert again on the next /onboarding visit."
    why_human: "Vitest covers the pure-render closed-enum mapping; URL-hygiene + full-page navigation behavior depends on Next.js runtime + jsdom limitation noted in 01.4-06-SUMMARY decisions. Worth one human spot-check."
---

# Phase 01.4: Gmail Identity Semantics, Permission UX, and UI Consistency — Verification Report

**Phase Goal:** Align v1 Gmail identity semantics so the Google login account is the first managed Gmail account, convert the incremental `gmail.modify` OAuth grant into the durable Gmail connection record, and run a bounded frontend UI consistency sweep across the current app without finalizing the Phase 5 brand identity.

**Verified:** 2026-04-26T22:25:55Z
**Status:** human_needed (5/5 automated must-haves verified; 5 items deferred to live OAuth + visual review)
**Re-verification:** No — initial verification

---

## Verdict: PASS-WITH-NOTES (pending human verification)

All 5 ROADMAP success criteria are verified by the codebase + green automated gates. No BLOCKERs. One sanctioned deviation (CONTEXT D-D3 `reset()` → executor used `unstable_retry()`) is documented in 01.4-RESEARCH.md §Finding 2 + §Pitfall 3 and is an upgrade, not a regression. Five items remain that genuinely require human verification (live OAuth, native-speaker copy review, visual sweep) — these are not gaps but unavoidable manual gates per VALIDATION.md §Manual-Only Verifications.

---

## Goal Achievement

### Observable Truths (5 ROADMAP Success Criteria)

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|--------------------|--------|----------|
| 1 | Second OAuth leg for `google-gmail` rejects mismatched OIDC subjects; no Gmail connection persisted on mismatch | VERIFIED | `GmailOAuthSuccessHandler.java:120-128` throws `GmailIdentityMismatchException` BEFORE any persistence call; `GmailOAuthFailureHandler.java:65-80` removes AuthorizedClient + revokes token + redirects with `?error=gmail_identity_mismatch`; integration test `GmailOAuthSuccessHandlerIntegrationTest.java` + `GmailOAuthFailureHandlerIntegrationTest.java` exercise both paths and pass under `./gradlew :backend:api:test` (BUILD SUCCESSFUL after `--rerun-tasks`). |
| 2 | Matching grant idempotently upserts single GmailConnectionEntity, encrypts refresh token (AES-GCM envelope), stores scopes, status=CONNECTED, advances onboarding SIGNED_IN → GMAIL_CONNECTED | VERIFIED | `GmailConnectionService.upsert(...)` lines 87-98: findByTenantId → orElseGet new entity → set status=CONNECTED + refreshTokenEncrypted + scopesGranted + connectedAt + clear disconnectedAt → save. `GmailOAuthSuccessHandler.java:150-159` binds `TenantContext.TENANT` ScopedValue BEFORE the JPA tx (Pitfall 6), encrypts via existing `RefreshTokenCipher`, advances OnboardingStep via `users.advanceTo(GMAIL_CONNECTED)`. Test: `GmailConnectionServiceUpsertTest.java` covers idempotent re-grant. Schema unchanged (latest changeset still `007-add-audit-columns.yaml`). |
| 3 | login_hint, distinguishes mismatch vs consent denied, redirects with machine-readable codes, vi/en localized; never exposes subjects/tokens/stack traces | VERIFIED | `GmailScopeRequestResolver.java:51-57` injects `login_hint=<oidcEmail>` with graceful-degrade. `GmailOAuthFailureHandler.java:65-87` distinguishes `GmailIdentityMismatchException` vs `OAuth2AuthenticationException(access_denied)` with closed redirect targets `?error=gmail_identity_mismatch` + `?error=gmail_consent_denied`. `ErrorCodes.java:25-26` declares both constants. i18n keys `gmail.identity.mismatch.{title,body,cta}` + `gmail.consent.denied.{title,body,cta}` present in vi.json + en.json (13/13 parity check below). Privacy: grep for `log\..*subject|email|token` under `backend/api/.../security/` returns 0 matches. |
| 4 | Tailwind 4 token layer + 5 reusable primitives applied across login, onboarding, settings, landing, docs, shared components | VERIFIED | `apps/web/app/globals.css:7-49` defines `@theme inline` token block with light (`:root`) + dark (`.dark`) values. 5 primitives present: `apps/web/components/ui/{PageShell,SectionCard,StatusAlert,EmptyState,LoadingState}.tsx` (442 LOC added vs main; only those 5 added — no extras). 12 surfaces consume primitives or tokens (login + onboarding + settings + public/page + docs/page + docs/[slug]/page + LanguageSwitcher + ConnectionHealthBadge + ReconnectPrompt + DeleteAccountDialog + TemplateCard + 4 layout files); raw color literal grep across `apps/web/{app,features,components}` returns 0 matches inside JSX className strings (2 hits inside JSDoc comments documenting the migration, intentional). |
| 5 | Next.js error fallbacks for root + public + auth + protected + 404; use new primitives where provider context allows; never render raw exception details; pass i18n/type/lint | VERIFIED | 5 boundary files exist: `app/global-error.tsx`, `app/not-found.tsx`, `app/(public)/error.tsx`, `app/(auth)/error.tsx`, `app/(protected)/error.tsx`. Privacy contract: grep for `error.message\|error.stack\|error.digest` returns only 3 matches, all inside JSDoc comments warning AGAINST rendering them — actual JSX never references those fields. `global-error.tsx` is English-only inline (next-intl unavailable per Next.js contract); other 4 use next-intl `errors.boundary.*` / `errors.notFound.*` keys. Sentry/Datadog/OTel grep across `apps/web` returns 0 matches in source (1 reference inside `global-error.tsx` JSDoc explicitly forbidding SDKs). All gates green: tsc --noEmit (exit 0), pnpm lint (exit 0), pnpm i18n:check (exit 0, "OK - 102 leaf keys"), pnpm exec vitest run (`19 passed (19) / 125 passed (125)`). |

**Score:** 5/5 ROADMAP success criteria verified

---

## Test Gate Matrix

| Gate | Command | Result | Evidence |
|------|---------|--------|----------|
| Backend full suite (cached) | `./gradlew :backend:api:test :backend:core:test` | GREEN | "BUILD SUCCESSFUL in 16s, 19 actionable tasks: 19 up-to-date" |
| Backend full suite (forced re-run) | `./gradlew :backend:api:test :backend:core:test --rerun-tasks` | GREEN | "BUILD SUCCESSFUL in 2m 6s, 19 actionable tasks: 19 executed" — confirms no stale cache |
| Frontend Vitest | `pnpm exec vitest run` (in `apps/web`) | GREEN | "Test Files 19 passed (19) / Tests 125 passed (125)" |
| TypeScript typecheck | `pnpm exec tsc --noEmit` | GREEN | exit 0, no diagnostics |
| Frontend lint | `pnpm lint` | GREEN | exit 0, eslint produced no output |
| i18n parity | `pnpm i18n:check` | GREEN | "OK - vi/en parity, 102 leaf keys, backend ErrorCodes coverage, locked errors.validation.generic, no English-prose literals in 19 Phase 1 files." |
| 13 new i18n keys parity | manual node script across vi.json + en.json | 13/13 | All of `errors.boundary.{title,body,reset}`, `errors.notFound.{title,body,backHome}`, `gmail.identity.mismatch.{title,body,cta}`, `gmail.consent.denied.{title,body,cta}`, `settings.gmailConnection.singleAccountNote` present in BOTH locale bundles. |

---

## Privacy / Contract Gate Matrix

| Gate | Command | Result | Evidence |
|------|---------|--------|----------|
| Boundary files do not render `error.message/stack/digest` | `grep -rE "error\.(message\|stack\|digest)" apps/web/app/{global-error,not-found,(public)/error,(auth)/error,(protected)/error}.tsx` | PASS | 3 matches total, all inside JSDoc comments warning AGAINST rendering them. Zero JSX-text occurrences. |
| No observability SDK leaked into frontend source | `grep -rnE "Sentry\|@datadog\|opentelemetry" apps/web/{app,features,components}` | PASS | 1 match inside `global-error.tsx` JSDoc explicitly forbidding SDK use. Zero imports/calls. |
| Backend handler files do not log subject/email/token | `grep -rnE "log\..*(subject\|email\|token)" backend/api/.../security/Gmail*.java` | PASS | 0 matches. Only structured event logs: `log.info("event=gmail_connection_upserted tenantId={}", tenantId)`, `log.info("event=gmail_identity_mismatch outcome=rejected")`, `log.info("event=gmail_consent_denied outcome=user_declined")`. |
| Token discipline (no raw color literals in JSX) | `grep -rnE "text-(stone\|gray\|slate\|zinc\|neutral\|red\|amber\|emerald\|sky\|blue)-[0-9]\|bg-(white\|stone\|gray\|amber\|emerald\|red)-[0-9]\|border-(red\|amber\|emerald)-[0-9]" apps/web/{app,features,components}` | PASS | 2 matches total, both inside JSDoc comments documenting the *previous* color literal that was migrated (`ReconnectPrompt.tsx:9` "the previous border-amber-500 / bg-amber-50 raw color"; `TemplateCard.tsx:17` "text-stone-600 -> text-muted-foreground"). Zero active className literals. |

---

## Decision Conformance Matrix

| Decision | Required behavior | Status | Evidence |
|----------|------------------|--------|----------|
| D-A1 | GmailOAuthSuccessHandler bound to `google-gmail` only via dispatcher; google still uses GoogleOAuthSuccessHandler | VERIFIED | `OAuth2LoginDispatchingSuccessHandler.java:46-50` switch routes by `getAuthorizedClientRegistrationId()`: `"google" → googleHandler`, `"google-gmail" → gmailHandler`, default fallthrough. SecurityConfig wires the single dispatcher into `oauth2Login().successHandler(...)`. |
| D-A2 | `login_hint` injected from session principal email; graceful-degrade when anonymous | VERIFIED | `GmailScopeRequestResolver.java:51-57` checks `SecurityContextHolder` Authentication is `OAuth2AuthenticationToken` with `OidcUser` principal and non-blank email; otherwise omits the hint silently. |
| D-A3 | GmailOAuthFailureHandler distinguishes mismatch (revoke + redirect) vs consent denied (redirect only) | VERIFIED | `GmailOAuthFailureHandler.java:65-87` matches `GmailIdentityMismatchException` first (removes AuthorizedClient + best-effort revoke + log event + redirect), then `OAuth2AuthenticationException("access_denied")` (just log + redirect, no token to revoke). |
| D-A4 | GmailConnectionService.upsert preserves 1-row-per-tenant invariant | VERIFIED | `GmailConnectionService.java:88-98`: findByTenantId → orElseGet new entity (single row maintained). googleEmail set only on INSERT branch (defensive - subject check guarantees equality on UPDATE). disconnectedAt cleared on every call. |
| D-B4 | NO "add another Gmail account" affordance in /settings | VERIFIED | grep for "add another\|add account\|connect another\|add new.*gmail" across `apps/web/app/` + `apps/web/features/` returns 1 hit — `settings/page.tsx:33` JSDoc which says "the single-Gmail expectation without seeing a disabled 'add another' stub". This is documentation explicitly forbidding the affordance, not the affordance itself. |
| D-C2 | Tokens defined for both light and dark; NO toggle UI | VERIFIED | `globals.css:51-84` defines light values under `:root`; `:86-118` defines dark values under `.dark`. No `useTheme` hook, no toggle component, no dark-mode persistence. Token names are token-aware (`text-foreground`, `bg-background`, `text-muted-foreground`, etc.). |
| D-D3 | NO error reporting SDK; reset button calls Next's reset prop | DEVIATION (sanctioned) | NO error reporting SDK present (Sentry/Datadog/OTel grep returns only JSDoc warning matches). Reset button calls `unstable_retry()` instead of `reset()` — this deviation is documented and intentional per `01.4-RESEARCH.md` §Finding 2 + §Pitfall 3 + Pattern 4: "v16.2.0 introduced unstable_retry prop... reset only re-renders with stale data; unstable_retry actually re-fetches and recovers." Three segment error.tsx files + global-error.tsx all use `unstable_retry`. Same user-facing CTA, better behavior. Treat as a refinement of D-D3, not a gap. |

---

## Out-of-Scope Drift Check

| Concern | Result |
|---------|--------|
| New schema changes | NONE. `git diff --stat main..HEAD -- backend/core/src/main/resources/db/` returns no output. Latest changeset still `007-add-audit-columns.yaml`. |
| New shadcn primitives installed | NONE. `git diff --stat main..HEAD -- apps/web/package.json` returns no output. Only the 5 promised primitives added (`PageShell, SectionCard, StatusAlert, EmptyState, LoadingState`). |
| Gmail API surface beyond OAuth scope | NONE. No new files matching `watch\|GmailApi` in diff. Confined to `users.watch` / message reads in Phase 2A as planned. |
| Root `package.json` engines field added | OUT OF SCOPE — confirmed by user as parallel-agent change, not Phase 01.4. |
| `REVIEW.md` untracked file | OUT OF SCOPE — confirmed by user as parallel-agent artifact. Surfaces non-blocking follow-ups (see Recommended Follow-ups). |
| Multi-account/workspace concept | NOT IMPLEMENTED (correctly deferred per D-B1/B2/B3). |

No drift detected.

---

## Required Artifacts (level-3 wiring check)

| Artifact | Expected | Status | Wired Evidence |
|----------|----------|--------|----------------|
| `GmailOAuthSuccessHandler.java` | Subject check + transactional upsert + onboarding advance | VERIFIED | Imported by `OAuth2LoginDispatchingSuccessHandler` line 33, dispatched on `google-gmail` registration line 48. |
| `GmailOAuthFailureHandler.java` | Mismatch revoke + denied redirect | VERIFIED | Imported by `OAuth2LoginDispatchingFailureHandler` line 36, called for both `GmailIdentityMismatchException` + `access_denied`. |
| `GmailIdentityMismatchException.java` | Typed exception extending OAuth2AuthenticationException | VERIFIED | Constructed at `GmailOAuthSuccessHandler:121,127` with captured access token; matched at `GmailOAuthFailureHandler:65` + dispatcher line 45. |
| `OAuth2LoginDispatchingSuccessHandler.java` | Per-registration success dispatch | VERIFIED | Wired in `SecurityConfig:29` via `.successHandler(dispatchSuccess)`. |
| `OAuth2LoginDispatchingFailureHandler.java` | Per-exception failure dispatch | VERIFIED | Wired in `SecurityConfig:30` via `.failureHandler(dispatchFailure)`. |
| `GoogleTokenRevocationClient.java` | Best-effort revoke POST | VERIFIED | Injected into `GmailOAuthFailureHandler:55`, called at line 76 (`revokeQuietly`). |
| `GmailConnectionService.upsert(...)` | Idempotent persistence | VERIFIED | New method lines 87-98; called from `GmailOAuthSuccessHandler:155`. Existing `currentStatus/disconnect/deleteForCurrentTenant` preserved verbatim. |
| `GmailScopeRequestResolver.java` (extended) | login_hint additionalParameter | VERIFIED | New lines 51-57 inject login_hint; existing `prompt=consent`, `access_type=offline`, `include_granted_scopes=true` preserved at lines 43-45. |
| `apps/web/app/globals.css` (tokens) | @theme inline + light + dark | VERIFIED | Lines 7-49 (theme block), 51-84 (`:root` light), 86-118 (`.dark`). |
| `apps/web/components/ui/{PageShell,SectionCard,StatusAlert,EmptyState,LoadingState}.tsx` | 5 primitives | VERIFIED | All 5 files exist; consumed across 12 surfaces (login, onboarding, settings, landing, docs index, docs detail, layouts, ReconnectPrompt, TemplateCard, 3 segment error.tsx, not-found.tsx). |
| `apps/web/app/global-error.tsx` | Root replacement, English-only | VERIFIED | 76 lines; renders own `<html>`/`<body>`; English inline copy; uses `unstable_retry` with full-reload safety net. |
| `apps/web/app/not-found.tsx` | PageShell + EmptyState | VERIFIED | 24 lines; RSC; uses `getTranslations('errors.notFound')`. |
| `apps/web/app/(public|auth|protected)/error.tsx` | Segment boundaries | VERIFIED | 3 client components, identical contract differing only in PageShell variant. All use StatusAlert + SectionCard + `unstable_retry`. |
| Onboarding mismatch alert wiring | Closed-enum reader | VERIFIED | `apps/web/app/(protected)/onboarding/page.tsx:47-48` maps `gmail_identity_mismatch` → `gmail.identity.mismatch`, `gmail_consent_denied` → `gmail.consent.denied`; renders StatusAlert variant=error from `useSearchParams`. Wave 0 test `__tests__/app/onboarding-error.test.tsx` passes. |
| `/settings` singleAccountNote | Footer note rendered | VERIFIED | `apps/web/app/(protected)/settings/page.tsx:66` renders `t('settings.gmailConnection.singleAccountNote')`. |
| 13 new i18n keys parity | vi + en bundles | VERIFIED | All 13 present in both bundles (manual node script confirmed). i18n:check passes. |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java` | 77 (per REVIEW.md finding #5) | `UnsupportedOperationException` forward-declaration in production code | INFO | Flagged by parallel-agent REVIEW.md but not in Phase 01.4 deliverable scope. Verified the file's `upsert` method (lines 87-98) does NOT throw `UnsupportedOperationException` — line 77 in REVIEW.md may refer to a different surface or an outdated read. Out of scope for this verification; track in next phase if applicable. |
| `apps/web/features/gmail/components/ReconnectPrompt.tsx` | 9 (JSDoc) | Mentions `border-amber-500 / bg-amber-50` | NONE | This is a documentation comment recording the *previous* literal that was migrated. Not an active class. |
| `apps/web/features/onboarding/components/TemplateCard.tsx` | 17 (JSDoc) | Mentions `text-stone-600 -> text-muted-foreground` | NONE | Same — migration documentation. |

No blockers. Three references in JSDoc comments warning against rendering exception fields are intentional (documenting the privacy contract).

---

## Requirements Coverage

| Requirement | Source | Status | Evidence |
|-------------|--------|--------|----------|
| AUTH-01 (Sign in with Google + Gmail incremental consent) | Phase 1 origin, Phase 01.4 tightens login_hint | SATISFIED | `GmailScopeRequestResolver` injects login_hint; flow preserves leg-1 + leg-2 shape. |
| AUTH-02 (One Gmail account per tenant, identity-bound) | Phase 1 origin, Phase 01.4 closes the persistence + identity gap | SATISFIED | Subject check + idempotent upsert + scope filter + envelope encryption all wired and tested. |

Phase 01.4 adds NO new product requirements per CONTEXT §canonical_refs ("tightens AUTH-01/AUTH-02 behavior; UI sweep is non-functional polish").

---

## State Files Reflect Completion

| File | Expectation | Status |
|------|-------------|--------|
| `.planning/STATE.md` | progress 100%, status verifying, current_phase 01.4 | CONFIRMED — frontmatter shows `status: verifying`, `progress.percent: 100`, `total_plans: 41 / completed_plans: 41`, last_activity 2026-04-27. |
| `.planning/ROADMAP.md` Phase 01.4 row | 6/6 plans marked completed | CONFIRMED — all 6 plans listed with `[x]` checkmarks and "_(completed 2026-04-27)_" or earlier dates. Phase row itself still `[ ]` ("verification pending") — appropriate; verification flips it to `[x]`. |

---

## Recommended Follow-ups (non-blocking)

These came from REVIEW.md (parallel-agent artifact) and from this verification scan. They are NOT Phase 01.4 gaps — they are observations for future cleanup phases.

1. **REVIEW.md §2 Finding 1** — TanStack Query missing `staleTime`. Refetch on every focus/remount; suggest `staleTime: 5 * 60_000`. (`apps/web/lib/query-client.tsx`)
2. **REVIEW.md §2 Finding 3** — `stripTrailingSlash()` duplicated 3× across `GoogleOAuthSuccessHandler`, `GmailOAuthSuccessHandler`, `GmailOAuthFailureHandler`. Extract to shared utility.
3. **REVIEW.md §2 Finding 4** — `GMAIL_REGISTRATION_ID = "google-gmail"` constant duplicated 4× + raw string in dispatcher. Promote to enum or shared `OAuthRegistrations` class.
4. **REVIEW.md §2 Finding 6** — `access_denied` gate evaluated twice (dispatcher line 49-50 AND target handler line 82-83). Decide one owner; the leak hurts the abstraction boundary.
5. **REVIEW.md §2 Finding 5** — verify whether the flagged `UnsupportedOperationException` at `GmailConnectionService:77` is actually present (this verification's read of the file shows line 77 is a JSDoc continuation; may be a stale REVIEW.md observation). Worth a quick re-check during the cleanup phase.
6. **a11y polish across the 5 new primitives** — verify focus-visible ring contrast against new tokens, screen-reader labeling on EmptyState/StatusAlert CTAs. Plan a sweep with screen-reader testing in a future polish phase.
7. **Deferred i18n native review** (already listed in human_verification[1]) — Vietnamese-fluent reviewer should sanity-check the 6 new gmail.* keys + 6 errors.* keys + singleAccountNote for tone.
8. **Token-aware ESLint rule** (CONTEXT D-Discretion option) — the discipline currently relies on grep gates + review. If review drifts, codify into ESLint via `no-restricted-syntax`. Optional.
9. **Backend test forced re-run took 2m 6s** — within VALIDATION.md budget (~120s) but right at the edge. Watch for slowdown as suite grows.

---

## Gap Summary

**No gaps.** All 5 ROADMAP success criteria verified by codebase + green automated gates. The single deviation (D-D3 `reset()` → `unstable_retry()`) is sanctioned by 01.4-RESEARCH.md §Finding 2 and is an upgrade — Next.js 16.2's `unstable_retry` re-fetches data while `reset()` only re-renders stale state. CTA UX is identical from the user's perspective; behavior is strictly better.

5 items remain that require live human verification (OAuth UX, native-speaker copy, visual sweep, reconnect flow, mismatch dismissal). VALIDATION.md §Manual-Only Verifications already enumerates 3 of these as "unavoidable manual"; the other 2 are this verification's additions for completeness.

---

_Verified: 2026-04-26T22:25:55Z_
_Verifier: Claude (gsd-verifier, Opus 4.7)_
_Branch: gsd/phase-01.4-gmail-identity-semantics-permission-ux-and-ui-consistency (HEAD: 7e63744)_
