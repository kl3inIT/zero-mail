---
phase: 01.4
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/api/src/test/java/com/zeromail/api/security/GmailOAuthSuccessHandlerIntegrationTest.java
  - backend/api/src/test/java/com/zeromail/api/security/GmailOAuthFailureHandlerIntegrationTest.java
  - backend/api/src/test/java/com/zeromail/api/security/GmailScopeRequestResolverTest.java
  - backend/api/src/test/java/com/zeromail/api/security/testfixtures/MockGoogleRevocationServer.java
  - backend/core/src/test/java/com/zeromail/core/gmail/service/GmailConnectionServiceUpsertTest.java
  - apps/web/__tests__/components/StatusAlert.test.tsx
  - apps/web/__tests__/components/EmptyState.test.tsx
  - apps/web/__tests__/components/PageShell.test.tsx
  - apps/web/__tests__/components/SectionCard.test.tsx
  - apps/web/__tests__/components/LoadingState.test.tsx
  - apps/web/__tests__/app/error.test.tsx
  - apps/web/__tests__/app/global-error.test.tsx
  - apps/web/__tests__/app/not-found.test.tsx
  - apps/web/__tests__/app/onboarding-error.test.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "Backend test scaffolds exist as RED-by-design failing tests covering identity-match, identity-mismatch, consent-denied, idempotent upsert, and login_hint behavior"
    - "Frontend test scaffolds exist for the 5 new primitives + 4 new boundary/route surfaces + onboarding mismatch alert"
    - "MockGoogleRevocationServer fixture exists for failure-handler revoke-endpoint assertions"
  artifacts:
    - path: "backend/api/src/test/java/com/zeromail/api/security/GmailOAuthSuccessHandlerIntegrationTest.java"
      provides: "Identity-match + idempotent upsert + onboarding advance integration tests"
    - path: "backend/api/src/test/java/com/zeromail/api/security/GmailOAuthFailureHandlerIntegrationTest.java"
      provides: "Identity-mismatch + consent-denied + revoke + redirect integration tests"
    - path: "backend/api/src/test/java/com/zeromail/api/security/testfixtures/MockGoogleRevocationServer.java"
      provides: "JDK HttpServer-based stub capturing POST /revoke tokens"
    - path: "backend/core/src/test/java/com/zeromail/core/gmail/service/GmailConnectionServiceUpsertTest.java"
      provides: "Single-row idempotency + AES-GCM envelope round-trip"
    - path: "apps/web/__tests__/components/StatusAlert.test.tsx"
      provides: "Variant rendering, i18n key resolution, CTA invocation"
    - path: "apps/web/__tests__/app/error.test.tsx"
      provides: "Error boundary fallback rendering + unstable_retry invocation"
  key_links:
    - from: "GmailOAuthSuccessHandlerIntegrationTest"
      to: "ApiPostgresTestBase + TestSessionSupport.minter"
      via: "extends + @Autowired + minter.mint(sub, email)"
      pattern: "extends ApiPostgresTestBase"
    - from: "GmailOAuthFailureHandlerIntegrationTest"
      to: "MockGoogleRevocationServer"
      via: "@BeforeAll start + @DynamicPropertySource overrides revoke base-url"
      pattern: "MockGoogleRevocationServer"
---

<objective>
Land all Wave 0 test scaffolds (backend + frontend) BEFORE any implementation in Wave 1. Tests are RED by design — they reference classes / files / props that do not exist yet, so Wave 1 implementations have a concrete acceptance contract to satisfy. This is the BLOCKING gate for the entire phase.

Purpose: Per the GSD Wave 0 discipline, every implementation task in Waves 1-2 has a corresponding `<automated>` command from VALIDATION.md that will turn GREEN as the implementation lands. Implementing tests after-the-fact is forbidden in this codebase.

Output: 5 backend test files (4 tests + 1 fixture), 9 frontend Vitest scaffolds, all checked in as RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-RESEARCH.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-PATTERNS.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-VALIDATION.md
@apps/web/AGENTS.md
@backend/api/src/test/java/com/zeromail/api/security/MultiTenantLeakIntegrationTest.java
@backend/api/src/test/java/com/zeromail/api/security/OAuthProvisioningIntegrationTest.java
@backend/api/src/test/java/com/zeromail/api/security/GoogleOAuthSuccessHandlerTest.java
@backend/api/src/test/java/com/zeromail/api/security/TestSessionSupport.java
@backend/api/src/test/java/com/zeromail/api/ApiPostgresTestBase.java
@apps/web/__tests__/components

<interfaces>
<!-- Contracts the executor must reference verbatim. -->
- Test base class:        com.zeromail.api.ApiPostgresTestBase  (extends in every backend integration test)
- Session minting:        TestSessionSupport.TestSessionMinter#mint(String googleSubject, String email) — returns session cookie
- Tenant ScopedValue:     com.zeromail.core.tenant.TenantContext.TENANT  (ScopedValue<String>)
- Local port:             @LocalServerPort int port  (from MultiTenantLeakIntegrationTest)
- Postgres profile:       @ActiveProfiles("test"), @Import(TestSessionSupport.class)
- New impl classes (do not exist yet — tests reference them as RED):
    com.zeromail.api.security.GmailOAuthSuccessHandler
    com.zeromail.api.security.GmailOAuthFailureHandler
    com.zeromail.api.security.GmailIdentityMismatchException
    com.zeromail.api.security.GoogleTokenRevocationClient
    com.zeromail.core.gmail.service.GmailConnectionService#upsert(UUID, String, String, byte[])
- Frontend new primitives (do not exist yet — tests reference them as RED):
    @/components/ui/PageShell      (variant: 'app'|'marketing'|'docs'|'auth')
    @/components/ui/SectionCard    (heading?, description?, footer?, children)
    @/components/ui/StatusAlert    (variant: 'info'|'success'|'warn'|'error'; titleKey, bodyKey?, cta?, params?)
    @/components/ui/EmptyState     (icon?, title, body, cta?)
    @/components/ui/LoadingState   (variant: 'card'|'list'|'page')
- New error / 404 boundary files (do not exist yet):
    apps/web/app/global-error.tsx
    apps/web/app/not-found.tsx
    apps/web/app/(public)/error.tsx
    apps/web/app/(auth)/error.tsx
    apps/web/app/(protected)/error.tsx
- next-intl 4.x: useTranslations() (client), getTranslations() (server)
- Vitest 4.x + @testing-library/react 16.3 (jsdom 29) — see apps/web/__tests__/components for existing examples
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend Wave 0 test scaffolds + MockGoogleRevocationServer fixture</name>
  <files>
    backend/api/src/test/java/com/zeromail/api/security/GmailOAuthSuccessHandlerIntegrationTest.java
    backend/api/src/test/java/com/zeromail/api/security/GmailOAuthFailureHandlerIntegrationTest.java
    backend/api/src/test/java/com/zeromail/api/security/GmailScopeRequestResolverTest.java
    backend/api/src/test/java/com/zeromail/api/security/testfixtures/MockGoogleRevocationServer.java
    backend/core/src/test/java/com/zeromail/core/gmail/service/GmailConnectionServiceUpsertTest.java
  </files>
  <read_first>
    - backend/api/src/test/java/com/zeromail/api/security/MultiTenantLeakIntegrationTest.java (full — harness pattern, lines 22-64 are the canonical seedTenant pattern)
    - backend/api/src/test/java/com/zeromail/api/security/OAuthProvisioningIntegrationTest.java (full — DI-driven assertion pattern, lines 13-37)
    - backend/api/src/test/java/com/zeromail/api/security/GoogleOAuthSuccessHandlerTest.java (full — Mockito + MockHttpServletRequest + TestingAuthenticationToken pattern, lines 19-42)
    - backend/api/src/test/java/com/zeromail/api/ApiPostgresTestBase.java (full — Postgres testcontainer + stubbed google-gmail registration)
    - backend/api/src/test/java/com/zeromail/api/security/TestSessionSupport.java (full — minter API)
    - backend/core/src/main/java/com/zeromail/core/gmail/service/GmailConnectionService.java (existing methods — currentStatus / disconnect / deleteForCurrentTenant)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java (constructor + setters available)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCipher.java (encrypt/decrypt envelope, AAD = tenantId)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-PATTERNS.md §"Backend Test Patterns" (analog excerpts)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-RESEARCH.md §"Wave 0 Gaps" + §"Code Examples"
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md §Decisions D-A1, D-A3, D-A4 (the contract under test)
  </read_first>
  <action>
Create FIVE backend test files. All tests are RED-by-design — they reference classes that do not exist yet (`GmailOAuthSuccessHandler`, `GmailOAuthFailureHandler`, `GmailIdentityMismatchException`, `GoogleTokenRevocationClient`, `GmailConnectionService.upsert(...)`). Compile errors are EXPECTED until Wave 1 lands.

**File 1 — `GmailOAuthSuccessHandlerIntegrationTest.java`:**
- Package `com.zeromail.api.security`. Annotations: `@ActiveProfiles("test")`, `@Import(TestSessionSupport.class)`. Extends `ApiPostgresTestBase`.
- `@LocalServerPort int port;` + `@Autowired UserRepository users;` + `@Autowired TenantRepository tenants;` + `@Autowired GmailConnectionRepository connections;` + `@Autowired GmailConnectionService connectionService;` + `@Autowired RefreshTokenCipher cipher;` + `@Autowired TestSessionSupport.TestSessionMinter minter;` + `@Autowired GmailOAuthSuccessHandler handler;` (does not exist yet — RED).
- Helper `seedUser(String label)` mirrors `MultiTenantLeakIntegrationTest.seedTenant` (lines 55-64): create tenantId UUID, save TenantEntity, ScopedValue.where(TENANT, tenantId.toString()).call(() -> users.save(new UserEntity(...))), return record `Seed(UUID tenantId, String googleSubject, String email)`.
- Test 1 `successPath_matchingSubject_upsertsConnectionAndAdvancesStep()`:
  - seed user with subject `S1`
  - Build mock `OAuth2AuthenticationToken` whose principal is an `OidcUser` mock returning subject = `S1` and email = `gmail@example.com` and `getAuthorizedClientRegistrationId() == "google-gmail"` (consult `GoogleOAuthSuccessHandlerTest` lines 33-37 for mock construction)
  - Stub `OAuth2AuthorizedClientService` to return an `OAuth2AuthorizedClient` with refresh token `"refresh-token-bytes"` and scopes `Set.of("openid", "profile", "email", "https://www.googleapis.com/auth/gmail.modify")`
  - `handler.onAuthenticationSuccess(req, res, token);`
  - Assert `connections.findByTenantId(seed.tenantId()).isPresent()`, `status == GmailConnectionStatus.CONNECTED`, `cipher.decrypt(row.getRefreshTokenEncrypted(), seed.tenantId().toString())` decodes back to `"refresh-token-bytes"`, `scopesGranted == "https://www.googleapis.com/auth/gmail.modify"` (gmail-prefix-only filter per CONTEXT.md Specifics; openid+profile+email filtered out)
  - Assert `users.findById(seed.userId()).get().getOnboardingStep() == OnboardingStep.GMAIL_CONNECTED`
- Test 2 `idempotentReGrant_updatesExistingRowNoDuplicate()`:
  - seed; invoke handler once; capture `row.getId()` and `row.getVersion()`
  - invoke handler again with new token bytes `"refresh-token-bytes-v2"`
  - assert `connections.findAll().size() == 1`, same row id, version incremented, decrypted token == `"refresh-token-bytes-v2"`
- Test 3 `mismatchedSubject_throwsGmailIdentityMismatchException_andWritesNoRow()`:
  - seed user with subject `S1`
  - build OidcUser mock returning subject = `"S2-different"`
  - `assertThatThrownBy(() -> handler.onAuthenticationSuccess(req, res, token)).isInstanceOf(GmailIdentityMismatchException.class)`
  - assert `connections.count() == 0`, `users.findById(seed.userId()).get().getOnboardingStep() == OnboardingStep.SIGNED_IN` (no advance on mismatch)

**File 2 — `GmailOAuthFailureHandlerIntegrationTest.java`:**
- Same harness. `@Autowired GmailOAuthFailureHandler handler;` (RED).
- `@DynamicPropertySource` static method overrides `google.revoke.base-url` to `"http://localhost:" + mockServer.port()` so the failure handler POSTs to MockGoogleRevocationServer instead of real Google.
- Test 1 `identityMismatch_redirectsToOnboardingWithErrorCode_andRevokesToken()`:
  - construct `MockHttpServletRequest` + `MockHttpServletResponse`
  - `handler.onAuthenticationFailure(req, res, new GmailIdentityMismatchException("token-to-revoke"))`
  - assert `res.getRedirectedUrl().endsWith("/onboarding?error=gmail_identity_mismatch")`
  - assert `mockServer.getReceivedTokens()` contains `"token-to-revoke"`
- Test 2 `consentDenied_redirectsToOnboardingWithErrorCode_noRevoke()`:
  - construct exception `new OAuth2AuthenticationException(new OAuth2Error("access_denied"))`
  - `handler.onAuthenticationFailure(req, res, ex)`
  - assert `res.getRedirectedUrl().endsWith("/onboarding?error=gmail_consent_denied")`
  - assert `mockServer.getReceivedTokens()` is empty
- Test 3 `revokeEndpoint5xx_doesNotBlockRedirect()`:
  - configure mockServer to return 500 on next POST
  - assert handler still redirects with `gmail_identity_mismatch`

**File 3 — `GmailScopeRequestResolverTest.java`:**
- POJO unit test (no Spring context). Construct resolver via `new GmailScopeRequestResolver(...)` mirroring its existing constructor.
- Test 1 `loginHintInjected_whenAuthenticatedSession()`:
  - Set `SecurityContextHolder` with an `OAuth2AuthenticationToken` carrying an `OidcUser` mock returning email `"alice@example.com"`
  - Build a base `OAuth2AuthorizationRequest` with attribute `registration_id = "google-gmail"`
  - Call `resolver.customize(request)` (or whatever public path the existing resolver exposes — check `GmailScopeRequestResolver.java`)
  - Assert returned request's `additionalParameters` contains key `"login_hint"` with value `"alice@example.com"`
  - Assert `prompt == "consent"`, `access_type == "offline"`, `include_granted_scopes == "true"` are still present (no regression)
- Test 2 `loginHintOmittedGracefully_whenAnonymousSession()`:
  - Clear SecurityContextHolder (`SecurityContextHolder.clearContext()`)
  - Call `resolver.customize(request)`
  - Assert returned request has NO `login_hint` key (key absent, not present-with-null)
  - No exception thrown

**File 4 — `testfixtures/MockGoogleRevocationServer.java`:**
- New package `com.zeromail.api.security.testfixtures`.
- Use JDK `com.sun.net.httpserver.HttpServer` (no WireMock dependency needed).
- Public API:
  - `void start()` — bind to ephemeral port (`new InetSocketAddress(0)`), register handler at `/revoke`, start server
  - `int port()` — return bound port
  - `void stop()` — graceful shutdown with 0s delay
  - `List<String> getReceivedTokens()` — returns synchronized list of token query params from each POST
  - `void setNextResponseStatus(int status)` — one-shot override for next request (default 200)
  - `void clearReceivedTokens()`
- Handler reads `?token=...` query string from URI and adds to receivedTokens; replies `setNextResponseStatus().getAndSet(200)` with empty body.
- Class JavaDoc: "Best-effort stub of https://oauth2.googleapis.com/revoke for failure-handler integration tests. Spec: https://developers.google.com/identity/protocols/oauth2/web-server#tokenrevoke"

**File 5 — `GmailConnectionServiceUpsertTest.java`:**
- Package `com.zeromail.core.gmail.service`. Extends a Postgres-backed test base (mirror `OnboardingStepPersistenceTest` pattern from Phase 1.2.1 P03 if `ApiPostgresTestBase` lives in api module — for core-side tests, look for `PostgresContainerTest` or equivalent in `backend/core/src/test/java/.../core/`).
- `@Autowired GmailConnectionService service;` `@Autowired GmailConnectionRepository repo;`
- Test 1 `upsert_insertsNewRowOnFirstCall()`:
  - tenantId = random UUID, ScopedValue.where(TENANT, tenantId.toString()).run(() -> service.upsert(tenantId, "alice@example.com", "https://www.googleapis.com/auth/gmail.modify", new byte[]{1,2,3}))
  - assert repo.findByTenantId(tenantId).isPresent(), status=CONNECTED, refreshTokenEncrypted bytes match {1,2,3}
- Test 2 `upsert_updatesExistingRowOnSecondCall_singleRow()`:
  - call upsert twice with different scopes/tokens
  - assert repo.count() == 1, second call's scopes win, second call's bytes win, version incremented
- Test 3 `upsert_resetsDisconnectedAtAndStatus()`:
  - first call upsert, then disconnect (existing method) which sets DISCONNECTED + disconnectedAt
  - call upsert again
  - assert status=CONNECTED, disconnectedAt is null (CONTEXT D-A4: re-grant resets DISCONNECTED state)

For all 5 files: use `assertThat` from AssertJ (matches existing test style); use Mockito `mock()` / `when()` / `verify()` per `GoogleOAuthSuccessHandlerTest` precedent.
  </action>
  <verify>
    <automated>./gradlew :backend:api:compileTestJava :backend:core:compileTestJava 2>&1 | tee /tmp/compile.log; if grep -E 'cannot find symbol.*Gmail(OAuth|IdentityMismatch|TokenRevocation|ConnectionService\.upsert)' /tmp/compile.log; then echo "EXPECTED RED — symbols not yet implemented; will GREEN in Wave 1"; exit 0; else echo "UNEXPECTED — symbols already exist or wrong reference"; exit 1; fi</automated>
  </verify>
  <done>
    All 5 test files exist on disk. `gradle compileTestJava` reports `cannot find symbol` errors specifically for `GmailOAuthSuccessHandler`, `GmailOAuthFailureHandler`, `GmailIdentityMismatchException`, `GoogleTokenRevocationClient`, and `GmailConnectionService.upsert` — proving the tests are wired against the future production classes (RED-by-design). MockGoogleRevocationServer compiles cleanly (it depends only on JDK + JUnit). After Wave 1 lands, these tests must be GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create frontend Wave 0 Vitest scaffolds for primitives, error boundaries, and onboarding mismatch alert</name>
  <files>
    apps/web/__tests__/components/StatusAlert.test.tsx
    apps/web/__tests__/components/EmptyState.test.tsx
    apps/web/__tests__/components/PageShell.test.tsx
    apps/web/__tests__/components/SectionCard.test.tsx
    apps/web/__tests__/components/LoadingState.test.tsx
    apps/web/__tests__/app/error.test.tsx
    apps/web/__tests__/app/global-error.test.tsx
    apps/web/__tests__/app/not-found.test.tsx
    apps/web/__tests__/app/onboarding-error.test.tsx
  </files>
  <read_first>
    - apps/web/__tests__/components (list contents — pattern from existing Vitest+RTL files)
    - apps/web/__tests__/setup.ts (test setup — providers / mocks already configured)
    - apps/web/components/ui/alert.tsx (the Alert primitive that StatusAlert composes — types + variants)
    - apps/web/components/ui/card.tsx (Card primitive that SectionCard / EmptyState compose)
    - apps/web/components/ui/skeleton.tsx (Skeleton primitive that LoadingState composes)
    - apps/web/messages/en.json (head 60 lines — confirm i18n key namespaces from Phase 1.1/1.3)
    - apps/web/i18n/routing.ts (next-intl routing config)
    - apps/web/AGENTS.md (mandatory read for any Next.js work)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-UI-SPEC.md §Component Inventory + §Copywriting Contract (i18n keys to assert against)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md §D-D2 + §D-D3 (error boundary discipline + privacy contract)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-RESEARCH.md §"Pattern 4" + §"Pattern 5" (error boundary + global-error code shapes; verify `unstable_retry` per Finding #2)
    - apps/web/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md (per AGENTS.md — required reading before writing error boundary tests; CONFIRM whether `unstable_retry` or `reset` is the correct prop name in installed v16.2.4)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-VALIDATION.md §"Wave 0 Requirements"
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-06-PLAN.md Task 1 step 6 (`handleRetry` implementation contract — `router.replace('/onboarding')` for URL hygiene + `window.location.href = '/tenant/connect-gmail'` for full-page nav to backend OAuth init; the onboarding-error test asserts both)

    **Before writing any test file that imports from `@/components/ui/StatusAlert` or any new primitive, invoke the `frontend-design` skill** with this UI-SPEC §Component Inventory section + the relevant analog from PATTERNS.md so the skill grounds the test expectations.
  </read_first>
  <action>
Create NINE Vitest test files. All RED-by-design — they import from paths that do not exist yet. Use the existing `apps/web/__tests__/components/` files as patterning reference for imports (`render`, `screen`, `userEvent`, `vi`, `describe`, `it`, `expect`, NextIntlClientProvider wrapper).

For tests that need next-intl, wrap with a minimal NextIntlClientProvider stub passing in only the keys the test asserts. Mirror what existing __tests__ do — if existing tests use a helper like `renderWithIntl`, reuse it; otherwise inline the provider.

**File 1 — `StatusAlert.test.tsx`:**
- import `{ StatusAlert } from '@/components/ui/StatusAlert'` (RED)
- Tests:
  - `renders title and body from i18n keys for variant=info` — pass titleKey="errors.boundary.title" bodyKey="errors.boundary.body"; assert text `"Something went wrong"` and `"Please try again..."` are in document
  - `renders error variant with destructive styling` — assert root element has class containing `border-destructive` (from cva variants)
  - `invokes cta.onClick when CTA button clicked` — pass `cta={{ labelKey: 'errors.boundary.reset', onClick: vi.fn() }}`; await userEvent.click(screen.getByRole('button')); expect onClick called once
  - `does not render body section when bodyKey is undefined`
  - `passes ICU params to translation` — pass titleKey="errors.boundary.title" + params={{name:'Alice'}}; assert (skeleton — adapt once primitive lands)

**File 2 — `EmptyState.test.tsx`:**
- import `{ EmptyState } from '@/components/ui/EmptyState'` (RED)
- Tests:
  - `renders title and body strings`
  - `renders icon when icon prop provided` — pass `icon={<svg data-testid="custom-icon"/>}` or pass lucide icon component per UI-SPEC discretion; assert testid present
  - `renders cta as a Link with buttonVariants when cta provided` — assert there is an anchor element with class containing button variant classes (per UI-SPEC §Interaction Contracts: `<Link className={buttonVariants()}>` not `<Button asChild>`)
  - `omits cta region when cta prop missing`

**File 3 — `PageShell.test.tsx`:**
- import `{ PageShell } from '@/components/ui/PageShell'` (RED)
- Tests:
  - `renders <main> element` (use `screen.getByRole('main')`)
  - `applies max-w-3xl for variant=app (default)`
  - `applies max-w-5xl for variant=marketing`
  - `applies max-w-md and centered flex for variant=auth`
  - `applies max-w-3xl for variant=docs`

**File 4 — `SectionCard.test.tsx`:**
- import `{ SectionCard } from '@/components/ui/SectionCard'` (RED)
- Tests:
  - `renders heading text when heading prop provided`
  - `renders description text when description prop provided`
  - `omits header region when no heading and no description`
  - `renders children inside body slot`
  - `renders footer region when footer prop provided`

**File 5 — `LoadingState.test.tsx`:**
- import `{ LoadingState } from '@/components/ui/LoadingState'` (RED)
- Tests:
  - `renders multiple skeleton placeholders for variant=card (default)` — assert at least 3 elements with `data-slot="skeleton"`
  - `renders longer placeholder list for variant=list`
  - `renders page-shaped header+body skeletons for variant=page`

**File 6 — `app/error.test.tsx`:**
- Mocks: NextIntlClientProvider with keys `errors.boundary.title`, `errors.boundary.body`, `errors.boundary.reset`
- Test imports the protected segment boundary: `import ProtectedError from '@/app/(protected)/error'` (file does not exist yet — RED)
- Tests:
  - `renders title and body from i18n keys`
  - `does NOT render error.message anywhere in DOM` — pass `error = new Error('SECRET-PII-LEAK-XXX')`; assert `screen.queryByText(/SECRET-PII-LEAK-XXX/)` is null (privacy contract — Threat T-error-leak)
  - `does NOT render error.stack` — set `error.stack = 'STACK-XXX'`; assert no node contains `STACK-XXX`
  - `invokes the unstable_retry prop when CTA clicked` — pass `unstable_retry={vi.fn()}` (Plan 05 locks this name for the four segment boundaries per RESEARCH §Pitfall 3). Assert click invokes the spy.
  - `console.error is called only when NODE_ENV !== 'production'` — vi.spyOn(console, 'error'); render with NODE_ENV='test'; expect spy called; reset; render with NODE_ENV='production'; expect spy NOT called

**File 7 — `app/global-error.test.tsx`:**
- import `import GlobalError from '@/app/global-error'` (RED)
- Tests:
  - `renders <html> and <body> elements` — render and assert `document.querySelector('html')` and `'body'` (or use the rendered output's root element check; jsdom renders `<html>` already so assert presence of own `<body>` child instead)
  - `renders English-only fallback copy` — assert text contains `"Something went wrong"` literally (no i18n)
  - `does NOT render error.message`
  - `invokes unstable_retry when reload button clicked, with full-reload fallback on throw` — pass `unstable_retry={vi.fn()}`; click button; assert spy invoked. Separately, pass `unstable_retry={vi.fn(() => { throw new Error('retry-broken'); })}`; spy on `window.location.reload`; click; assert reload was called (Option A fallback path per RESEARCH §Pitfall 3 + Plan 05 Task 1 File 1 contract)

**File 8 — `app/not-found.test.tsx`:**
- import `import NotFound from '@/app/not-found'` (RED)
- Note: NotFound is server-side async. Use Vitest's pattern for async server-component testing — call the component as a function and inspect returned JSX (or use a renderRSC helper if one exists in __tests__/setup.ts; otherwise call `await NotFound()` and pass the JSX into RTL's `render`)
- Tests:
  - `renders title from errors.notFound.title`
  - `renders body from errors.notFound.body`
  - `renders Back home link pointing to "/"`

**File 9 — `app/onboarding-error.test.tsx`:**
- This tests the mismatch alert wiring on `/onboarding`. Mock `next/navigation`'s `useSearchParams` and `useRouter` (the router mock returns an object with `replace: vi.fn()`). Mock useSearchParams to return params with `error=gmail_identity_mismatch` (and other variants).
- Mock the `window.location.href` setter via `Object.defineProperty(window, 'location', { value: { ...window.location, href: '', assign: vi.fn() }, writable: true })` in `beforeEach`, OR use a direct setter spy: `const hrefSetter = vi.fn(); Object.defineProperty(window.location, 'href', { set: hrefSetter, configurable: true })`. Pick whichever pattern the existing __tests__/setup.ts already favors; if none, use the setter-spy approach because it isolates the assertion to the navigation event we care about.
- import the existing onboarding page: `import OnboardingPage from '@/app/(protected)/onboarding/page'`
- Tests:
  - `renders StatusAlert variant=error when ?error=gmail_identity_mismatch` — mock useSearchParams returning `gmail_identity_mismatch`; assert `screen.getByText(/Gmail account doesn't match/i)` is present (uses key `gmail.identity.mismatch.title`)
  - `renders StatusAlert variant=error when ?error=gmail_consent_denied` — assert `screen.getByText(/Gmail access was declined/i)`
  - `does NOT render any alert when ?error=<unknown_value>` — closed-enum mapping (Threat T-error-param-tamper); assert no element with role="alert"
  - `does NOT render any alert when ?error param absent`
  - `clicking the retry CTA navigates to /tenant/connect-gmail AND cleans the URL via router.replace('/onboarding')` — render with `?error=gmail_identity_mismatch`; click the alert's CTA button (`screen.getByRole('button', { name: /Try again/i })`). Two assertions, both required (per Plan 06 Task 1 step 6 `handleRetry` contract):
    1. **Navigation assertion (primary, what the user observes):** `expect(hrefSetter).toHaveBeenCalledWith('/tenant/connect-gmail')` (or, if using the `Object.defineProperty(window, 'location', ...)` pattern, `expect(window.location.href).toBe('/tenant/connect-gmail')`).
    2. **URL hygiene assertion (separate, asserts cleanup):** `expect(routerReplaceMock).toHaveBeenCalledWith('/onboarding')`.
    Do NOT collapse these into a single ambiguous assertion. The two side effects are independent and both must fire — the test must fail if either is missing.

For each file: include a top-level `// Wave 0 RED scaffold — references implementation that lands in Wave 1` comment.

Add necessary directories: `mkdir -p apps/web/__tests__/app` if it does not exist.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run __tests__/components/StatusAlert.test.tsx __tests__/components/EmptyState.test.tsx __tests__/components/PageShell.test.tsx __tests__/components/SectionCard.test.tsx __tests__/components/LoadingState.test.tsx __tests__/app/error.test.tsx __tests__/app/global-error.test.tsx __tests__/app/not-found.test.tsx __tests__/app/onboarding-error.test.tsx 2>&1 | tee /tmp/vitest.log; if grep -qE "(Cannot find module|Failed to resolve import).*(StatusAlert|EmptyState|PageShell|SectionCard|LoadingState|global-error|not-found|\\(protected\\)/error)" /tmp/vitest.log; then echo "EXPECTED RED — modules not yet implemented; will GREEN in Waves 1-2"; exit 0; else echo "UNEXPECTED — modules already exist or wrong import path"; exit 1; fi</automated>
  </verify>
  <done>
    All 9 Vitest files exist. Vitest reports "Cannot find module" / "Failed to resolve import" errors for `@/components/ui/StatusAlert`, `EmptyState`, `PageShell`, `SectionCard`, `LoadingState`, and the new `app/global-error`, `app/not-found`, `app/(protected)/error` paths — proving each test is wired against the future production module (RED-by-design). After Plan 04 lands the primitives and Plan 05 lands the boundaries, these tests must be GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → fixture HTTP | MockGoogleRevocationServer accepts unauthenticated POSTs but only on a localhost ephemeral port bound during test startup |
| Test code → ApiPostgresTestBase | Postgres testcontainer is per-class; tenant isolation enforced even in tests via ScopedValue binding (FND-05 invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.4-W0-01 | Information Disclosure | Test code logging | mitigate | Test code MUST NOT print refresh tokens, Google subjects, or emails to stdout. Use `tenantId.toString()` (UUID — opaque) for test diagnostics only. |
| T-1.4-W0-02 | Tampering | MockGoogleRevocationServer | accept | Bound to localhost only on ephemeral port; lifecycle is per-test-class; cannot be reached from outside the test JVM |
</threat_model>

<verification>
- `./gradlew :backend:api:compileTestJava :backend:core:compileTestJava` produces compile errors specifically for the 5 missing impl symbols (RED-by-design contract)
- `pnpm --filter zeromail-web exec vitest run __tests__/{components,app}/*.test.tsx` reports "Cannot find module" specifically for the new primitive + boundary paths (RED-by-design contract)
- All 14 test files exist on disk under their declared paths
- No test references real Google OAuth secrets, real refresh tokens, or any non-opaque PII
</verification>

<success_criteria>
- 14 files created (5 backend, 9 frontend)
- Backend tests fail to compile for the EXACT impl-symbol reasons (proving they exercise the contract Wave 1 will fulfill)
- Frontend tests fail to import for the EXACT module-path reasons (proving they exercise the components Plans 04-06 will create)
- MockGoogleRevocationServer compiles standalone (depends only on JDK + JUnit)
- Plan can be marked complete; downstream Wave 1 plans now have concrete acceptance contracts via the `<automated>` commands in their VALIDATION.md rows
</success_criteria>

<output>
After completion, create `.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-01-SUMMARY.md`
</output>
</content>
</invoke>