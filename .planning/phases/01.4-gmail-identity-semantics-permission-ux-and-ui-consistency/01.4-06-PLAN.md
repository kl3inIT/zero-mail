---
phase: 01.4
plan: 06
type: execute
wave: 2
depends_on: ["01.4-01", "01.4-02", "01.4-04", "01.4-05"]
files_modified:
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(protected)/onboarding/page.tsx
  - apps/web/app/(protected)/settings/page.tsx
  - apps/web/app/(public)/page.tsx
  - apps/web/app/(public)/docs/page.tsx
  - apps/web/app/(public)/docs/[slug]/page.tsx
  - apps/web/app/(public)/docs/[slug]/loading.tsx
  - apps/web/features/gmail/components/ReconnectPrompt.tsx
  - apps/web/features/gmail/components/ConnectionHealthBadge.tsx
  - apps/web/features/account/components/DeleteAccountDialog.tsx
  - apps/web/features/onboarding/components/TemplateCard.tsx
  - apps/web/features/i18n/components/LanguageSwitcher.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "Onboarding page renders StatusAlert variant=error at top when ?error=gmail_identity_mismatch or ?error=gmail_consent_denied"
    - "Onboarding page renders no alert for unknown ?error= values (closed-enum mapping per Threat T-error-param-tamper)"
    - "Mismatch alert CTA re-triggers /tenant/connect-gmail flow"
    - "Settings Gmail card displays singleAccountNote footer using key settings.gmailConnection.singleAccountNote"
    - "All 12 surfaces use token-aware Tailwind classes; no raw color names survive (text-stone-, bg-white, text-gray-, border-red-, border-amber-, bg-amber-)"
    - "Login + onboarding + settings + landing + docs surfaces wrap content in PageShell with the correct variant per UI-SPEC Spacing"
    - "Phase-end gates green: pnpm exec tsc --noEmit, pnpm i18n:check, pnpm lint, pnpm vitest run"
  artifacts:
    - path: "apps/web/app/(protected)/onboarding/page.tsx"
      provides: "Mismatch alert wiring via useSearchParams + closed-enum errorCode mapping; PageShell + SectionCard refactor"
    - path: "apps/web/app/(protected)/settings/page.tsx"
      provides: "5 SectionCard sections; Gmail card singleAccountNote footer; danger zone uses border-destructive/30"
    - path: "apps/web/features/gmail/components/ReconnectPrompt.tsx"
      provides: "Refactored to consume StatusAlert variant=warn; raw color literals removed"
  key_links:
    - from: "(protected)/onboarding/page.tsx"
      to: "@/components/ui/StatusAlert + next/navigation useSearchParams"
      via: "client component reads error param, maps via closed enum, renders StatusAlert"
      pattern: "useSearchParams"
    - from: "(protected)/settings/page.tsx"
      to: "i18n key settings.gmailConnection.singleAccountNote"
      via: "useTranslations() inside Gmail SectionCard footer"
      pattern: "singleAccountNote"
---

<objective>
Land the final Wave 2 deliverables: (1) wire the Gmail mismatch / consent-denied alert on /onboarding per CONTEXT D-D1 + UI-SPEC Interaction Contracts, (2) refactor every current frontend surface to consume the Plan 04 primitives + token-aware classes (per CONTEXT D-C2 / D-C3 / D-C4), (3) add the singleAccountNote footer to /settings (D-D5), and (4) run the phase-closing gates (tsc / vitest / i18n:check / lint) to GREEN.

Purpose: Closes the user-visible deliverables of Phase 1.4 — without this plan the backend mismatch handling has no UI, the new primitives have no consumers, and the token discipline never reaches the surfaces it was built for. Required by Phase 1.4 success criteria #3 (mismatch UX), #4 (consistent token usage across surfaces), and #5 (error fallbacks pass i18n/lint checks).

Output: 12 modified frontend files + a final phase-gate verification. Wave 0 onboarding-error.test.tsx turns GREEN. Phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-RESEARCH.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-PATTERNS.md
@.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-UI-SPEC.md
@apps/web/AGENTS.md
@apps/web/components/ui/PageShell.tsx
@apps/web/components/ui/SectionCard.tsx
@apps/web/components/ui/StatusAlert.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/ui/LoadingState.tsx
@apps/web/components/ui/button.tsx
@apps/web/app/(auth)/login/page.tsx
@apps/web/app/(protected)/onboarding/page.tsx
@apps/web/app/(protected)/settings/page.tsx
@apps/web/app/(public)/page.tsx
@apps/web/app/(public)/docs/page.tsx
@apps/web/app/(public)/docs/[slug]/page.tsx
@apps/web/app/(public)/docs/[slug]/loading.tsx
@apps/web/features/gmail/components/ReconnectPrompt.tsx
@apps/web/features/gmail/components/ConnectionHealthBadge.tsx
@apps/web/features/account/components/DeleteAccountDialog.tsx
@apps/web/features/onboarding/components/TemplateCard.tsx
@apps/web/features/i18n/components/LanguageSwitcher.tsx
@apps/web/messages/en.json
@apps/web/messages/vi.json

<interfaces>
- next/navigation: useSearchParams (client), useRouter (client) — both client-only
- Plan 04 primitives at @/components/ui/{PageShell,SectionCard,StatusAlert,EmptyState,LoadingState}
- Plan 05 i18n keys: errors.boundary.*, errors.notFound.*, gmail.identity.mismatch.*, gmail.consent.denied.*, settings.gmailConnection.singleAccountNote
- Plan 02 backend: /tenant/connect-gmail endpoint receives the retry; backend redirects to ?error=gmail_identity_mismatch or ?error=gmail_consent_denied on failure
- Closed enum for ?error= values (Threat T-error-param-tamper):
    'gmail_identity_mismatch' -> i18n base key 'gmail.identity.mismatch'
    'gmail_consent_denied'    -> i18n base key 'gmail.consent.denied'
    (any other value -> render no alert)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Gmail mismatch alert on /onboarding + singleAccountNote on /settings (with token sweep on both files)</name>
  <files>
    apps/web/app/(protected)/onboarding/page.tsx
    apps/web/app/(protected)/settings/page.tsx
  </files>
  <read_first>
    - apps/web/app/(protected)/onboarding/page.tsx (full — current client component; understand existing useCurrentUser / step machine; identify where to add useSearchParams + StatusAlert)
    - apps/web/app/(protected)/settings/page.tsx (full — current client component; locate the Gmail card section to add the footer note; identify the border-red-200 line for the danger zone retoken)
    - apps/web/components/ui/StatusAlert.tsx (Plan 04 — interface: variant + titleKey + bodyKey + cta props)
    - apps/web/components/ui/PageShell.tsx (Plan 04 — variant=app for protected pages)
    - apps/web/components/ui/SectionCard.tsx (Plan 04 — heading + description + footer + children)
    - apps/web/messages/en.json + vi.json (Plan 05 — confirm new keys exist)
    - apps/web/features/gmail/api (or wherever the existing onboarding page calls /tenant/connect-gmail — find the helper to reuse)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md D-D1 + D-D5 + D-D6
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-RESEARCH.md "Frontend onboarding mismatch alert" code example + "Common Pitfalls 10" (useSearchParams must stay client)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-PATTERNS.md "/onboarding mismatch alert rendering"
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-UI-SPEC.md "Interaction Contracts" "URL hygiene after error dismissal"
    - apps/web/__tests__/app/onboarding-error.test.tsx (Wave 0 contract — closed enum, retry CTA, no alert for unknown values)

    **Before writing any UI code, invoke the `frontend-design` skill** with UI-SPEC Component Inventory + Color Status alert mapping + the existing onboarding/settings page sources + the StatusAlert primitive contract. Mandatory.
  </read_first>
  <action>
**File A — apps/web/app/(protected)/onboarding/page.tsx:**

The page is already a client component. Top-level changes:

1. Add imports at the top of the file (alongside existing imports):
   - `import { useSearchParams, useRouter } from "next/navigation";`
   - `import { StatusAlert } from "@/components/ui/StatusAlert";`
   - `import { PageShell } from "@/components/ui/PageShell";`
   - `import { SectionCard } from "@/components/ui/SectionCard";`

2. Inside the component body, BEFORE the existing JSX return, add the closed-enum mapping block. Use this exact code (literal):

```
const params = useSearchParams();
const router = useRouter();
const rawError = params.get("error");

// Closed-enum mapping per Threat T-error-param-tamper — unknown values render nothing.
const ERROR_KEY_MAP = {
  gmail_identity_mismatch: "gmail.identity.mismatch",
  gmail_consent_denied:    "gmail.consent.denied",
} as const;
type KnownError = keyof typeof ERROR_KEY_MAP;
const isKnownError = (v: string | null): v is KnownError =>
  v !== null && Object.prototype.hasOwnProperty.call(ERROR_KEY_MAP, v);
const baseKey = isKnownError(rawError) ? ERROR_KEY_MAP[rawError] : null;

const handleRetry = () => {
  router.replace("/onboarding"); // URL hygiene per UI-SPEC Interaction Contracts
  window.location.href = "/tenant/connect-gmail"; // CONTEXT D-D6: same flow for first connect + reconnect
};
```

3. Wrap the existing top-level rendering with `<PageShell variant="app">`. Replace the existing ad-hoc `<main className="mx-auto max-w-3xl p-6">` (line 51 per PATTERNS.md) with `<PageShell variant="app">`.

4. Render the alert as the FIRST child inside PageShell, ABOVE the existing step machine. The JSX is:

```
<PageShell variant="app">
  {baseKey && (
    <StatusAlert
      variant="error"
      titleKey={`${baseKey}.title`}
      bodyKey={`${baseKey}.body`}
      cta={{ labelKey: `${baseKey}.cta`, onClick: handleRetry }}
    />
  )}
  {/* existing step machine rendering — keep verbatim, just sweep raw colors */}
  ...
</PageShell>
```

5. Token sweep on this file (verify and replace):
   - `text-stone-600` (lines 52, 65 per PATTERNS.md) -> `text-muted-foreground`
   - any `text-stone-700` / `text-stone-900` -> `text-foreground` or `text-muted-foreground`
   - replace ad-hoc `<Card className="mt-4 p-6"><h2>...` patterns with `<SectionCard heading={t('...')}>` per UI-SPEC Component Inventory

6. Privacy: do NOT render `rawError` directly — only the i18n-key-resolved StatusAlert copy is shown. The closed enum mapping ensures arbitrary URL values cannot leak as text content (Threat T-error-param-tamper).

**File B — apps/web/app/(protected)/settings/page.tsx:**

1. Add imports:
   - `import { PageShell } from "@/components/ui/PageShell";`
   - `import { SectionCard } from "@/components/ui/SectionCard";`

2. Wrap the existing layout with `<PageShell variant="app">` (replaces `<main className="mx-auto max-w-4xl space-y-6 p-6">` per PATTERNS.md). NOTE: Settings has 5 sections per CONTEXT D-C3 (account / language / Gmail / privacy / danger). Per UI-SPEC Spacing, `app` variant uses `max-w-3xl`. If the existing settings page uses `max-w-4xl` and the executor judges `max-w-3xl` too narrow, prefer keeping `max-w-3xl` (per UI-SPEC LOCKED widths) — the design contract takes precedence over the historical width. Document in SUMMARY.md if intentionally diverging.

3. Replace each of the 5 `<Card className="p-6">` blocks with `<SectionCard heading={t('settings.<section>.heading')}>` (use existing i18n keys from messages bundle for each section's heading; check vi/en for the section names — Phase 1.3 P07 should have populated them).

4. Inside the Gmail SectionCard, add the singleAccountNote footer below the existing connection status / reconnect / disconnect actions. The JSX shape is:

```
<SectionCard
  heading={t('settings.gmailConnection.heading')}
  footer={<p className="text-sm text-muted-foreground">{t('settings.gmailConnection.singleAccountNote')}</p>}
>
  {/* existing connection status content */}
</SectionCard>
```

5. Token sweep on this file (verify and replace):
   - `border-red-200` (line 81 per PATTERNS.md, danger zone) -> `border-destructive/30`
   - any inline destructive styling -> use `border-destructive`, `text-destructive`, `bg-destructive/10` per UI-SPEC Color
   - any raw color literals (`text-stone-*`, `bg-white`, `text-gray-*`) -> `text-foreground` / `text-muted-foreground` / `bg-card` / `bg-background`

6. Per CONTEXT D-B4: do NOT add an "add another Gmail account" affordance, no disabled stub, no "coming soon" badge. The footer note is the entire single-account communication.

**Test verification:** After saving both files, the Wave 0 `onboarding-error.test.tsx` should now GREEN. The earlier `<unknown_value>` test case relies on the closed enum returning `null` baseKey and rendering nothing. The retry CTA test relies on `handleRetry` being wired (test may need to adapt to whether it asserts `router.replace` invocation or `window.location.href` set — match the assertion shape).
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run __tests__/app/onboarding-error.test.tsx 2>&1 | tail -20; pnpm exec tsc --noEmit 2>&1 | tail -10; grep -cE "useSearchParams|useRouter" "apps/web/app/(protected)/onboarding/page.tsx" | grep -qE "^[2-9]" && echo "OK navigation hooks imported"; grep -c "ERROR_KEY_MAP" "apps/web/app/(protected)/onboarding/page.tsx" | grep -qE "^[2-9]" && echo "OK closed enum present"; grep -c "singleAccountNote" "apps/web/app/(protected)/settings/page.tsx" | grep -q "^1$" && echo "OK singleAccountNote rendered"; if grep -E "(text-stone-|bg-white|text-gray-|border-red-|border-amber-|bg-amber-)" "apps/web/app/(protected)/onboarding/page.tsx" "apps/web/app/(protected)/settings/page.tsx"; then echo "FAIL: raw color name remains"; exit 1; else echo "OK token sweep complete"; fi</automated>
  </verify>
  <done>
    Onboarding page renders the closed-enum mismatch alert + retry CTA + URL hygiene `router.replace`. Settings page renders 5 SectionCards + Gmail card singleAccountNote footer + danger zone uses `border-destructive/30`. Both files contain zero raw color literals (grep gate). `onboarding-error.test.tsx` GREEN. `tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Token sweep across remaining surfaces (login, landing, docs, shared components) + final phase gates</name>
  <files>
    apps/web/app/(auth)/login/page.tsx
    apps/web/app/(public)/page.tsx
    apps/web/app/(public)/docs/page.tsx
    apps/web/app/(public)/docs/[slug]/page.tsx
    apps/web/app/(public)/docs/[slug]/loading.tsx
    apps/web/features/gmail/components/ReconnectPrompt.tsx
    apps/web/features/gmail/components/ConnectionHealthBadge.tsx
    apps/web/features/account/components/DeleteAccountDialog.tsx
    apps/web/features/onboarding/components/TemplateCard.tsx
    apps/web/features/i18n/components/LanguageSwitcher.tsx
  </files>
  <read_first>
    - Each touched file in full BEFORE editing (one Read call per file; do not re-read after editing) — the executor must see the current ad-hoc class strings to know what to replace
    - apps/web/components/ui/PageShell.tsx, SectionCard.tsx, StatusAlert.tsx, EmptyState.tsx (Plan 04 — interfaces to consume)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-PATTERNS.md "Frontend — modified surfaces" (line-by-line drift list — verify against current file content) + "Token-aware Tailwind class discipline"
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-UI-SPEC.md Color (60/30/10 + accent reserved-for list) + Typography (4 sizes / 2 weights) + Spacing (container widths) + Component Inventory (which file consumes which primitive)
    - .planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-CONTEXT.md D-C1 + D-C2 + D-C3 (full surface list + token-only mandate)

    **Before editing any of these files, invoke the `frontend-design` skill** with the per-file PATTERNS.md row + the UI-SPEC Color + Spacing references + the relevant Plan 04 primitive contract. Mandatory per the user's persistent feedback memory — applies to skeleton structure as well as polish.
  </read_first>
  <action>
This is the bulk token-sweep + primitive-adoption task. For each file, perform the SAME 3-step workflow:

1. **Read the file** (one Read call) to capture current state.
2. **Identify drift** — every raw color literal (`text-stone-*`, `bg-white`, `text-gray-*`, `border-red-*`, `border-amber-*`, `bg-amber-*`), every ad-hoc max-w container, every `<Card><h2>` pattern, every inline Alert with hand-crafted classes.
3. **Apply per-file directives below.** When in doubt, follow the analog from PATTERNS.md.

---

**File 1 — `(auth)/login/page.tsx`:**
- Replace `<main className="flex min-h-dvh items-center justify-center p-6">` with `<PageShell variant="auth">`
- Replace the inner `<Card className="w-full max-w-md p-8">` with `<SectionCard className="w-full max-w-md">` (or just `<SectionCard>` if PageShell variant=auth provides max-w-md — verify against PageShell impl from Plan 04)
- Replace `text-stone-700` (line 28 per PATTERNS) with `text-muted-foreground`
- Preserve existing `<Link>` / `<a>` and Google sign-in button verbatim — do not change auth flow

**File 2 — `(public)/page.tsx`:**
- Wrap with `<PageShell variant="marketing">` (max-w-5xl per UI-SPEC)
- H1 currently `text-2xl font-semibold` (line 50) — change to `text-3xl font-semibold tracking-tight` per UI-SPEC Typography "Display"
- Existing `<Link className={buttonVariants()}>` CTA preserved (Phase 1.3 P05 D-decision)
- Token sweep any other raw colors

**File 3 — `(public)/docs/page.tsx`:**
- Wrap with `<PageShell variant="docs">` (max-w-3xl)
- Replace inline empty-state div (lines 51-55 per PATTERNS) with `<EmptyState title={t('docs.empty.heading')} body={t('docs.empty.body')} />` (no CTA — docs index is the destination)
- Token sweep any raw colors

**File 4 — `(public)/docs/[slug]/page.tsx`:**
- Wrap with `<PageShell variant="docs">`
- Token sweep

**File 5 — `(public)/docs/[slug]/loading.tsx`:**
- Replace existing inline `<div className="mx-auto max-w-3xl px-4 py-8"><p className="text-muted-foreground text-sm">{t('loading')}</p></div>` with `<PageShell variant="docs"><LoadingState variant="page" /></PageShell>` per UI-SPEC Component Inventory + Plan 04 LoadingState contract

**File 6 — `features/gmail/components/ReconnectPrompt.tsx`:**
- This is the analog file for StatusAlert (per PATTERNS StatusAlert "existing in-repo precedent"). Refactor to:

```
'use client';
import { StatusAlert } from "@/components/ui/StatusAlert";

export function ReconnectPrompt({ onReconnect }: { onReconnect: () => void }) {
  return (
    <StatusAlert
      variant="warn"
      titleKey="connectionHealth.reconnectPrompt"
      cta={{ labelKey: "settings.gmailConnection.reconnectCta", onClick: onReconnect }}
    />
  );
}
```

- The line-11 `border-amber-500 bg-amber-50` literal is REPLACED by StatusAlert's variant=warn token mapping (UI-SPEC Color "Status alert color mapping" — warn uses neutral bg-muted by default unless executor adds --color-warning per Discretion).
- Verify the existing i18n key `connectionHealth.reconnectPrompt` exists; if it has both title and body sub-keys (e.g., `.title` and `.body`), pass both via `titleKey` + `bodyKey`. Otherwise omit `bodyKey`.

**File 7 — `features/gmail/components/ConnectionHealthBadge.tsx`:**
- Per PATTERNS + CONTEXT Discretion: stays Badge-shaped, just retoken color variants. Replace any raw color literals with destructive/muted/foreground tokens. Do NOT convert to StatusAlert (the badge form is intentional per CONTEXT Discretion).

**File 8 — `features/account/components/DeleteAccountDialog.tsx`:**
- Verify all classes are token-aware. The destructive confirm button likely already uses `bg-destructive` or `<Button variant="destructive">` — preserve. No shape change.

**File 9 — `features/onboarding/components/TemplateCard.tsx`:**
- Verify all classes are token-aware. No shape change.

**File 10 — `features/i18n/components/LanguageSwitcher.tsx`:**
- Verify all classes are token-aware. The "active locale" indicator may use `text-primary` per UI-SPEC Color "Accent reserved for ... active link state in LanguageSwitcher" — if so, verify the token is correct. No shape change.

**Discipline reminders:**
- DO NOT install new shadcn primitives (CONTEXT D-C4 "NO new shadcn primitives in Phase 1.4")
- DO NOT add a `--color-success` / `--color-warning` token to globals.css (CONTEXT Discretion / RESEARCH Finding #3 — defer unless review demands)
- Preserve `<Link className={buttonVariants()}>` for navigational CTAs (UI-SPEC Interaction Contracts — Phase 1.3 P05 D-decision REAFFIRMED)
- DO NOT modify `apps/web/proxy.ts` (Phase 1.3 D-E5 lock)
- DO NOT modify `apps/web/app/globals.css` tokens (RESEARCH Finding #3 — they exist and are sufficient)

**FINAL PHASE GATES (run after all files saved):**

After all 10 files are edited, run the Phase 1.4 closing verification suite:

1. `cd apps/web && pnpm exec tsc --noEmit` — must exit 0
2. `cd apps/web && pnpm exec vitest run` — must exit 0 (all 9 Wave 0 frontend scaffolds GREEN + no existing-test regressions)
3. `cd apps/web && pnpm run i18n:check` — must exit 0 (parity intact; STRICT mode preserved per Phase 1.3 P07)
4. `cd apps/web && pnpm run lint` — must exit 0
5. `./gradlew :backend:api:test :backend:core:test` — must exit 0 (all Wave 0 backend tests GREEN now that Plans 02 + 03 + this Plan have landed)
6. JetBrains check on every Java file touched in Plans 02 + 03: invoke `mcp__jetbrains__get_file_problems` on each. Substitute via `./gradlew clean check` if MCP unavailable (Phase 01.2.1 P02 documented gap).

Document the gate results in SUMMARY.md. If any gate fails, fix and re-run before declaring the phase complete.

**Manual deferrals (per VALIDATION.md Manual-Only Verifications):**
- Live Google OAuth `login_hint` chooser-UX walkthrough — defer to user with replay instructions (sign in as A, click Connect Gmail, observe chooser, switch to B, observe `?error=gmail_identity_mismatch`)
- Vietnamese copy review by native speaker — defer to user
- Visual sweep across light + dark mode — defer to user (dark mode toggle ships in Phase 5; light-only review in Phase 1.4)

These three are unavoidable manual gates. Document deferral with `replay_command` strings in SUMMARY.md.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&1 | tail -10; pnpm exec vitest run 2>&1 | tail -20; pnpm run i18n:check 2>&1 | tail -5; pnpm run lint 2>&1 | tail -10; cd ../.. && ./gradlew :backend:api:test :backend:core:test --rerun-tasks 2>&1 | tail -30; if find apps/web/app apps/web/features apps/web/components -name "*.tsx" -type f -print0 | xargs -0 grep -lE "(text-stone-|bg-white|text-gray-|border-red-|border-amber-|bg-amber-)" 2>/dev/null | grep -v __tests__ | head -5; then echo "WARN: raw color literals remain in some files (review list above)"; else echo "OK: token sweep complete across apps/web"; fi</automated>
  </verify>
  <done>
    All 10 frontend files refactored. Zero raw color literals across `apps/web/app`, `apps/web/features`, `apps/web/components` (excluding `__tests__/` which may have intentional fixtures). Phase gates: `tsc --noEmit` GREEN, `vitest run` GREEN (all 9 Wave 0 frontend tests + no regressions), `i18n:check` GREEN, `lint` GREEN, `gradle :backend:api:test :backend:core:test` GREEN (all 4 Wave 0 backend tests pass — `GmailOAuthSuccessHandlerIntegrationTest`, `GmailOAuthFailureHandlerIntegrationTest`, `GmailScopeRequestResolverTest`, `GmailConnectionServiceUpsertTest`). Manual gates (live OAuth UX, Vietnamese copy review, light+dark visual sweep) documented as deferred to user with replay commands. Phase 1.4 ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?error=` query param -> client component | Untrusted input from browser address bar; closed-enum mapping rejects unknown values |
| Onboarding retry CTA -> /tenant/connect-gmail | Same-origin navigation; backend (Plan 02) handles all OAuth callback validation |
| Settings singleAccountNote -> DOM | Static i18n string from curated bundle; no user input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.4-06-error-param-tamper | Tampering | Onboarding `?error=` reader | mitigate | Closed-enum `ERROR_KEY_MAP` with type-guard `isKnownError`; unknown values resolve to `null` baseKey and render no alert (verifiable via Wave 0 test "does NOT render any alert when ?error=<unknown_value>") |
| T-1.4-06-error-leak | Information Disclosure | Onboarding StatusAlert | mitigate | Alert renders i18n keys only; rawError query value is NEVER passed to JSX text content; only used for closed-enum lookup |
| T-1.4-06-xss-via-href | XSS | Onboarding retry handler | mitigate | `window.location.href = "/tenant/connect-gmail"` is a hard-coded same-origin path; no user input concatenated |
| T-1.4-06-singleaccount-fingerprint | Information Disclosure | Settings footer | accept | singleAccountNote is a static product statement; no user-specific data leaked |
</threat_model>

<verification>
- `pnpm exec vitest run __tests__/app/onboarding-error.test.tsx` GREEN (all 4 cases: known mismatch, known consent-denied, unknown value, missing param)
- `pnpm exec tsc --noEmit` GREEN
- `pnpm i18n:check` GREEN (parity preserved)
- `pnpm lint` GREEN
- `./gradlew :backend:api:test :backend:core:test` GREEN (all Wave 0 backend tests pass once Plans 02 + 03 + 06 all land)
- grep gate: zero raw color literals across `apps/web/app`, `apps/web/features`, `apps/web/components` (excluding `__tests__/`)
- Onboarding page contains the literal `ERROR_KEY_MAP` constant (closed enum proof)
- Settings page contains `singleAccountNote` translation key reference
- ReconnectPrompt no longer contains `border-amber-500` or `bg-amber-50`
</verification>

<success_criteria>
- 12 frontend files refactored
- Phase 1.4 success criterion #3 met: machine-readable error codes route to /onboarding with localized vi+en alert; subjects/tokens/stack-traces never exposed
- Phase 1.4 success criterion #4 met: 5 reusable primitives + Tailwind 4 token layer applied across login, onboarding, settings, landing, docs + shared components
- Phase 1.4 success criterion #5 met: Next.js error fallbacks (5 files from Plan 05) exist and use new primitives where provider context allows; pass i18n/type/lint checks
- All automated phase gates GREEN
- Manual gates documented for user with replay commands
</success_criteria>

<output>
After completion, create `.planning/phases/01.4-gmail-identity-semantics-permission-ux-and-ui-consistency/01.4-06-SUMMARY.md`
</output>
