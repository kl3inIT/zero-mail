---
phase: 08-bulk-unsubscribe-campaign
plan: 08
type: execute
wave: 7
depends_on: [08-04, 08-07]
files_modified:
  - backend/api/src/main/java/com/zeromail/api/controllers/cleanup/UnsubscribeCandidateController.java
  - backend/api/src/main/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignController.java
  - backend/api/src/main/java/com/zeromail/api/controllers/cleanup/CampaignStatusController.java
  - backend/api/src/main/java/com/zeromail/api/controllers/cleanup/SuppressionController.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/package-info.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/UnsubscribeCandidateResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/UnsubscribeCandidateListResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignPreviewRequest.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignPreviewResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/PerSenderPreviewResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignExecuteRequest.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignExecuteResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignStatusResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/PerSenderStateResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/SuppressionEntryResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/SuppressionListResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/cleanup/SuppressionAddRequest.java
  - apps/web/lib/api/openapi-types.ts
autonomous: true
requirements: [UNS-01, UNS-02, UNS-03, UNS-05, UNS-06, UNS-07]
must_haves:
  truths:
    - "4 thin controller dưới `/api/unsubscribe/*` + `/api/cleanup/suppression` — controllers extract TenantContext.currentTenantUuid() + delegate vào service từ Wave 4b"
    - "12 DTO record (request + response) với `from(projection)` static factory (CONVENTIONS §1)"
    - "@ExceptionHandler maps domain exception → HTTP code: CampaignCapExceededException → 400; CampaignRetryConflictException → 409; UndoWindowExpiredException → 410; CampaignNotFoundException → 404; SuppressedSenderException → 409"
    - "Spring `springdoc-openapi` regen `apps/web/lib/api/openapi-types.ts` — typed client cho Wave 5b frontend"
    - "Wave 0 tests `UnsubscribeCampaignControllerTest` + `CampaignStatusControllerTest` flip GREEN"
  artifacts:
    - path: backend/api/src/main/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignController.java
      provides: "5 endpoint: POST preview, POST execute, GET {jobId}, POST retry, POST undo (UNS-03..UNS-07)"
    - path: backend/api/src/main/java/com/zeromail/api/controllers/cleanup/UnsubscribeCandidateController.java
      provides: "GET /api/unsubscribe/candidates (UNS-01)"
    - path: backend/api/src/main/java/com/zeromail/api/controllers/cleanup/SuppressionController.java
      provides: "CRUD endpoint cho suppression list (UNS-02)"
    - path: apps/web/lib/api/openapi-types.ts
      provides: "Regen typed client — Wave 5b frontend consume các type schema"
  key_links:
    - from: backend/api/src/main/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignController.java
      to: backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignPreviewService.java
      via: "Controller inject service via constructor"
      pattern: "campaignPreviewService.preview"
---

<objective>
Wave 5a: 4 controller + 12 DTO + OpenAPI codegen. Controllers thin (CONVENTIONS §1) — extract TenantContext + delegate service + map exception → HTTP code + return DTO.from(projection).

Purpose: HTTP surface cho Phase 8 frontend. UNS-01 + UNS-02 + UNS-03 + UNS-05 + UNS-06 + UNS-07 controller-level acceptance. Wave 0 controller tests flip GREEN.
Output: 18 file (4 controller + 13 DTO + 1 typed client regen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-SPEC.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-PATTERNS.md
@backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageAuditController.java
@backend/api/src/main/java/com/zeromail/api/controllers/analytics/AnalyticsController.java
@backend/api/src/main/java/com/zeromail/api/dto/triage/AuditEntryResponse.java
@backend/api/src/main/java/com/zeromail/api/dto/triage/AuditListResponse.java
@backend/api/src/main/java/com/zeromail/api/dto/triage/UndoAuditResponse.java
@backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignPreviewService.java
@backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignExecuteService.java
@backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignStatusQueryService.java
@backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignRetryService.java
@backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignUndoService.java
@backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CandidateQueryService.java
@backend/core/src/main/java/com/zeromail/core/cleanup/usecases/SuppressionCrudService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: 13 DTO record (request + response + package-info)</name>
  <files>
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/package-info.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/UnsubscribeCandidateResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/UnsubscribeCandidateListResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignPreviewRequest.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignPreviewResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/PerSenderPreviewResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignExecuteRequest.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignExecuteResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignStatusResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/PerSenderStateResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/SuppressionEntryResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/SuppressionListResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/SuppressionAddRequest.java
  </files>
  <read_first>
    backend/api/src/main/java/com/zeromail/api/dto/triage/AuditEntryResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/triage/AuditListResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/triage/UndoAuditResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/triage/package-info.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/UnsubscribeCandidateProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/CampaignStatusProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/PerSenderAttemptProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/projection/SenderSuppressionProjection.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignPreviewService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignExecuteService.java,
    .planning/phases/08-bulk-unsubscribe-campaign/08-SPEC.md
  </read_first>
  <action>
    Tạo 13 file DTO. Mọi DTO là `record` (CONVENTIONS §3 + §1), với static factory `from(projection)`.

    1. **`package-info.java`**: Empty package marker per CONVENTIONS — analog `api/dto/triage/package-info.java`:
       ```java
       package com.zeromail.api.dto.cleanup;
       ```

    2. **`UnsubscribeCandidateResponse`** (record):
       ```java
       public record UnsubscribeCandidateResponse(
           String senderEmail,
           String senderDomain,
           long messageCount,
           Instant lastSeenAt,
           String unsubscribeMethod,  // ONE_CLICK | MAILTO | NONE
           boolean suppressed
       ) {
           public static UnsubscribeCandidateResponse from(UnsubscribeCandidateProjection projection) {
               return new UnsubscribeCandidateResponse(
                   projection.senderEmail(), projection.senderDomain(), projection.messageCount(),
                   projection.lastSeenAt(), projection.unsubscribeMethod().id(), projection.suppressed());
           }
       }
       ```

    3. **`UnsubscribeCandidateListResponse`** (wrapper):
       ```java
       public record UnsubscribeCandidateListResponse(List<UnsubscribeCandidateResponse> items) {
           public UnsubscribeCandidateListResponse { items = List.copyOf(items); }
           public static UnsubscribeCandidateListResponse from(List<UnsubscribeCandidateProjection> projections) {
               return new UnsubscribeCandidateListResponse(projections.stream().map(UnsubscribeCandidateResponse::from).toList());
           }
       }
       ```

    4. **`CampaignPreviewRequest`** (request record với bean validation):
       ```java
       public record CampaignPreviewRequest(
           @NotEmpty
           @Size(max = 25, message = "Sender list exceeds cap of 25")
           List<@NotBlank @Size(max = 320) String> senderEmails
       ) {}
       ```

    5. **`PerSenderPreviewResponse`** (record):
       ```java
       public record PerSenderPreviewResponse(
           String senderEmail,
           String senderDomain,
           String unsubscribeMethod,
           long historyMessageCount,
           boolean willArchive,
           String riskBadge  // SAFE | NO_HEADER_DISABLED | SUPPRESSED_BLOCKED
       ) {
           public static PerSenderPreviewResponse from(CampaignPreviewService.PerSenderPreview row) {
               return new PerSenderPreviewResponse(
                   row.senderEmail(), row.senderDomain(), row.unsubscribeMethod().id(),
                   row.messageCount(), row.willArchive(), row.riskBadge());
           }
       }
       ```

    6. **`CampaignPreviewResponse`** (wrapper):
       ```java
       public record CampaignPreviewResponse(List<PerSenderPreviewResponse> perSender, long totalHistoryCount) {
           public CampaignPreviewResponse { perSender = List.copyOf(perSender); }
           public static CampaignPreviewResponse from(CampaignPreviewService.CampaignPreviewResult result) {
               return new CampaignPreviewResponse(
                   result.perSender().stream().map(PerSenderPreviewResponse::from).toList(),
                   result.totalHistoryCount());
           }
       }
       ```

    7. **`CampaignExecuteRequest`** (same shape as preview):
       ```java
       public record CampaignExecuteRequest(
           @NotEmpty @Size(max = 25)
           List<@NotBlank @Size(max = 320) String> senderEmails
       ) {}
       ```

    8. **`CampaignExecuteResponse`**:
       ```java
       public record CampaignExecuteResponse(UUID campaignId, UUID jobId, String status) {
           public static CampaignExecuteResponse from(CampaignExecuteService.CampaignExecuteResult result) {
               return new CampaignExecuteResponse(result.campaignId(), result.jobId(), "QUEUED");
           }
       }
       ```

    9. **`PerSenderStateResponse`**:
       ```java
       public record PerSenderStateResponse(
           String senderEmail,
           String senderDomain,
           String unsubscribeMethod,
           String state,           // PENDING | RUNNING | OK | FAILED
           String failureReason,   // nullable
           int archivedMessageCount,
           Instant startedAt,
           Instant finishedAt
       ) {
           public static PerSenderStateResponse from(PerSenderAttemptProjection projection) {
               return new PerSenderStateResponse(
                   projection.senderEmail(), projection.senderDomain(),
                   projection.unsubscribeMethod().id(), projection.state().id(),
                   projection.failureReason(), projection.archivedMessageCount(),
                   projection.startedAt(), projection.finishedAt());
           }
       }
       ```

    10. **`CampaignStatusResponse`**:
        ```java
        public record CampaignStatusResponse(
            UUID campaignId,
            UUID jobId,
            String status,          // QUEUED | RUNNING | COMPLETED | FAILED
            int progressPct,
            Instant appliedAt,
            Instant revertedAt,
            int totalSenderCount,
            int totalHistoryMessageCount,
            List<PerSenderStateResponse> perSender,
            boolean undoAvailable    // computed: status=COMPLETED && now < appliedAt + 30d
        ) {
            public CampaignStatusResponse { perSender = List.copyOf(perSender); }
            public static CampaignStatusResponse from(CampaignStatusProjection projection, boolean undoAvailable, int progressPct) {
                return new CampaignStatusResponse(
                    projection.id(), projection.jobId(), projection.status().id(), progressPct,
                    projection.appliedAt(), projection.revertedAt(),
                    projection.totalSenderCount(), projection.totalHistoryMessageCount(),
                    projection.perSender().stream().map(PerSenderStateResponse::from).toList(),
                    undoAvailable);
            }
        }
        ```

    11. **`SuppressionEntryResponse`**:
        ```java
        public record SuppressionEntryResponse(
            UUID id,
            String senderEmail,    // nullable
            String senderDomain,   // nullable
            String reason,         // manual | replied | auto
            Instant createdAt
        ) {
            public static SuppressionEntryResponse from(SenderSuppressionProjection projection) {
                return new SuppressionEntryResponse(
                    projection.id(), projection.senderEmail(), projection.senderDomain(),
                    projection.reason().id(), projection.createdAt());
            }
        }
        ```

    12. **`SuppressionListResponse`** (wrapper):
        ```java
        public record SuppressionListResponse(List<SuppressionEntryResponse> items) {
            public SuppressionListResponse { items = List.copyOf(items); }
            public static SuppressionListResponse from(List<SenderSuppressionProjection> projections) {
                return new SuppressionListResponse(projections.stream().map(SuppressionEntryResponse::from).toList());
            }
        }
        ```

    13. **`SuppressionAddRequest`**:
        ```java
        public record SuppressionAddRequest(
            @NotBlank
            @Size(max = 320)
            String senderEmailOrDomain
        ) {}
        ```

    All DTOs records, Lombok-free. Imports: `java.time.Instant`, `java.util.List`, `java.util.UUID`, Jakarta validation `@NotEmpty`, `@NotBlank`, `@Size`. Springdoc OpenAPI sẽ pick up automatically.

    Naming: full identifier — `senderEmail`, `senderDomain`, `messageCount`, `lastSeenAt`, `historyMessageCount`, `archivedMessageCount`, `failureReason`, `progressPct`, `appliedAt`, `revertedAt`. KHÔNG `sndr/cnt/ts/applAt`.
  </action>
  <verify>
    <automated>./gradlew :backend:api:compileJava 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 13 file Java DTO trong `backend/api/src/main/java/com/zeromail/api/dto/cleanup/` tồn tại.
    - Mọi DTO file `public record` (CONVENTIONS §3 records-only cho DTOs).
    - 8 response DTO chứa `public static ... from(...)` static factory method.
    - Wrapper DTOs (`UnsubscribeCandidateListResponse`, `CampaignPreviewResponse`, `CampaignStatusResponse`, `SuppressionListResponse`) chứa compact constructor `items = List.copyOf(items);` HOẶC `perSender = List.copyOf(perSender);`.
    - 3 request DTO chứa Jakarta validation annotation `@NotEmpty`, `@NotBlank`, `@Size`.
    - `CampaignPreviewRequest` chứa `@Size(max = 25` (cap value).
    - `CampaignStatusResponse` chứa field `undoAvailable boolean` (computed flag cho frontend).
    - `./gradlew :backend:api:compileJava` exits 0.
    - `grep -E "\b(req|res|svc|cfg|ctx|sndr|cnt|ts)\b" backend/api/src/main/java/com/zeromail/api/dto/cleanup/*.java` trả 0 (no banned abbreviations).
  </acceptance_criteria>
  <done>
    13 DTO record lands. CONVENTIONS §1 + §3 compliant. Static factories ready cho controller layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: 4 thin controllers + @ExceptionHandler mappings</name>
  <files>
    backend/api/src/main/java/com/zeromail/api/controllers/cleanup/UnsubscribeCandidateController.java,
    backend/api/src/main/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignController.java,
    backend/api/src/main/java/com/zeromail/api/controllers/cleanup/CampaignStatusController.java,
    backend/api/src/main/java/com/zeromail/api/controllers/cleanup/SuppressionController.java
  </files>
  <read_first>
    backend/api/src/main/java/com/zeromail/api/controllers/analytics/AnalyticsController.java,
    backend/api/src/main/java/com/zeromail/api/controllers/triage/TriageAuditController.java,
    backend/api/src/main/java/com/zeromail/api/controllers/notifications/NotificationPreferencesController.java,
    backend/core/src/main/java/com/zeromail/core/shared/tenant/TenantContext.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignCapExceededException.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/UndoWindowExpiredException.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignRetryConflictException.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignNotFoundException.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignPreviewService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignExecuteService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignStatusQueryService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignRetryService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CampaignUndoService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/CandidateQueryService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/SuppressionCrudService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/SuppressionAutoAddService.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeCampaignPolicy.java
  </read_first>
  <action>
    Tạo 4 thin controller theo PATTERNS.md Group 3 + CONVENTIONS §1:

    1. **`UnsubscribeCandidateController`** (UNS-01) — analog `AnalyticsController`:
       ```
       @RestController
       @Tag(name = "cleanup")
       @RequestMapping("/api/unsubscribe")
       public class UnsubscribeCandidateController {
           private static final int DEFAULT_LIMIT = 25;
           private static final Duration DEFAULT_WINDOW = Duration.ofDays(30);

           private final CandidateQueryService candidateQueryService;

           public UnsubscribeCandidateController(CandidateQueryService candidateQueryService) { this.candidateQueryService = candidateQueryService; }

           @GetMapping("/candidates")
           public UnsubscribeCandidateListResponse listCandidates(
                   @RequestParam(value = "window", required = false) String rawWindow,
                   @RequestParam(value = "limit", required = false, defaultValue = "25") int limit) {
               UUID tenantId = TenantContext.currentTenantUuid();
               Duration window = resolveWindow(rawWindow);
               int effectiveLimit = Math.min(limit, UnsubscribeCampaignPolicy.MAX_SENDERS_PER_CAMPAIGN);
               List<UnsubscribeCandidateProjection> projections = candidateQueryService.findCandidates(tenantId, window, effectiveLimit);
               log.info("event=cleanup_candidates_requested tenantId={} window={} limit={} count={}", tenantId, window, effectiveLimit, projections.size());
               return UnsubscribeCandidateListResponse.from(projections);
           }

           private Duration resolveWindow(String raw) {
               if (raw == null || raw.isBlank()) return DEFAULT_WINDOW;
               return switch (raw) {
                   case "7d" -> Duration.ofDays(7);
                   case "30d" -> Duration.ofDays(30);
                   case "90d" -> Duration.ofDays(90);
                   default -> throw new IllegalArgumentException("Unsupported window: " + raw);
               };
           }
       }
       ```

    2. **`UnsubscribeCampaignController`** (UNS-03 + UNS-04 + UNS-06 + UNS-07) — 4 endpoint:
       ```
       @RestController
       @Tag(name = "cleanup")
       @RequestMapping("/api/unsubscribe/campaigns")
       public class UnsubscribeCampaignController {
           private final CampaignPreviewService campaignPreviewService;
           private final CampaignExecuteService campaignExecuteService;
           private final CampaignRetryService campaignRetryService;
           private final CampaignUndoService campaignUndoService;

           public UnsubscribeCampaignController(... constructor inject all) { ... }

           @PostMapping("/preview")
           public CampaignPreviewResponse preview(@Valid @RequestBody CampaignPreviewRequest request) {
               UUID tenantId = TenantContext.currentTenantUuid();
               CampaignPreviewService.CampaignPreviewResult result = campaignPreviewService.preview(tenantId, request.senderEmails());
               log.info("event=cleanup_campaign_preview_requested tenantId={} senderCount={}", tenantId, request.senderEmails().size());
               return CampaignPreviewResponse.from(result);
           }

           @PostMapping("/execute")
           @ResponseStatus(HttpStatus.CREATED)
           public CampaignExecuteResponse execute(@Valid @RequestBody CampaignExecuteRequest request) {
               UUID tenantId = TenantContext.currentTenantUuid();
               CampaignExecuteService.CampaignExecuteResult result = campaignExecuteService.execute(tenantId, request.senderEmails());
               log.info("event=cleanup_campaign_execute_requested tenantId={} campaignId={} jobId={}", tenantId, result.campaignId(), result.jobId());
               return CampaignExecuteResponse.from(result);
           }

           @PostMapping("/{jobId}/senders/{senderEmail}/retry")
           public ResponseEntity<Void> retrySender(@PathVariable UUID jobId, @PathVariable String senderEmail) {
               UUID tenantId = TenantContext.currentTenantUuid();
               campaignRetryService.retry(tenantId, jobId, senderEmail);
               log.info("event=cleanup_campaign_retry_requested tenantId={} jobId={}", tenantId, jobId);
               return ResponseEntity.accepted().build();
           }

           @PostMapping("/{jobId}/undo")
           public CampaignUndoService.CampaignUndoResult undo(@PathVariable UUID jobId) {
               UUID tenantId = TenantContext.currentTenantUuid();
               CampaignUndoService.CampaignUndoResult result = campaignUndoService.undo(tenantId, jobId);
               log.info("event=cleanup_campaign_undo_requested tenantId={} jobId={} restoredCount={}", tenantId, jobId, result.restoredCount());
               return result;
           }

           // @ExceptionHandler — controller-local
           @ExceptionHandler(CampaignCapExceededException.class)
           public ResponseEntity<ApiErrorResponse> handleCapExceeded(CampaignCapExceededException exception) {
               return ResponseEntity.badRequest().body(new ApiErrorResponse(exception.errorCode(), exception.getMessage()));
           }

           @ExceptionHandler(CampaignRetryConflictException.class)
           public ResponseEntity<ApiErrorResponse> handleRetryConflict(CampaignRetryConflictException exception) {
               return ResponseEntity.status(HttpStatus.CONFLICT).body(new ApiErrorResponse(exception.errorCode(), exception.getMessage()));
           }

           @ExceptionHandler(UndoWindowExpiredException.class)
           public ResponseEntity<ApiErrorResponse> handleUndoWindowExpired(UndoWindowExpiredException exception) {
               return ResponseEntity.status(HttpStatus.GONE).body(new ApiErrorResponse(exception.errorCode(), exception.getMessage()));
           }

           @ExceptionHandler(CampaignNotFoundException.class)
           public ResponseEntity<ApiErrorResponse> handleNotFound(CampaignNotFoundException exception) {
               return ResponseEntity.status(HttpStatus.NOT_FOUND).body(new ApiErrorResponse(exception.errorCode(), exception.getMessage()));
           }
       }
       ```

       `ApiErrorResponse` — verify existing `backend/api/src/main/java/com/zeromail/api/error/ApiErrorResponse.java` (đã có từ Phase 4) HOẶC tạo mới với shape `record ApiErrorResponse(String code, String message)`. **ASSUMPTION**: existing reused.

    3. **`CampaignStatusController`** (UNS-05) — split từ UnsubscribeCampaignController vì 2 file Wave 0 đã separate test:
       ```
       @RestController
       @Tag(name = "cleanup")
       @RequestMapping("/api/unsubscribe/campaigns")
       public class CampaignStatusController {
           private final CampaignStatusQueryService campaignStatusQueryService;
           private final Clock clock;

           public CampaignStatusController(CampaignStatusQueryService campaignStatusQueryService, Clock clock) { ... }

           @GetMapping("/{jobId}")
           public CampaignStatusResponse getStatus(@PathVariable UUID jobId) {
               UUID tenantId = TenantContext.currentTenantUuid();
               CampaignStatusProjection projection = campaignStatusQueryService.findByJobId(tenantId, jobId)
                   .orElseThrow(() -> new CampaignNotFoundException(jobId));

               int progressPct = computeProgressPct(projection.perSender());
               boolean undoAvailable = computeUndoAvailable(projection);
               log.info("event=cleanup_campaign_status_requested tenantId={} jobId={}", tenantId, jobId);
               return CampaignStatusResponse.from(projection, undoAvailable, progressPct);
           }

           private int computeProgressPct(List<PerSenderAttemptProjection> perSender) {
               if (perSender.isEmpty()) return 0;
               long terminal = perSender.stream()
                   .filter(p -> p.state() == UnsubscribeAttemptState.OK || p.state() == UnsubscribeAttemptState.FAILED)
                   .count();
               return (int)(terminal * 100 / perSender.size());
           }

           private boolean computeUndoAvailable(CampaignStatusProjection projection) {
               if (projection.status() != CampaignStatus.COMPLETED || projection.appliedAt() == null || projection.revertedAt() != null) return false;
               Instant undoableUntil = UnsubscribeCampaignPolicy.undoableUntil(projection.appliedAt());
               return clock.instant().isBefore(undoableUntil);
           }

           @ExceptionHandler(CampaignNotFoundException.class)
           public ResponseEntity<ApiErrorResponse> handleNotFound(CampaignNotFoundException exception) {
               return ResponseEntity.status(HttpStatus.NOT_FOUND).body(new ApiErrorResponse(exception.errorCode(), exception.getMessage()));
           }
       }
       ```

    4. **`SuppressionController`** (UNS-02) — analog `NotificationPreferencesController`:
       ```
       @RestController
       @Tag(name = "cleanup")
       @RequestMapping("/api/cleanup/suppression")
       public class SuppressionController {
           private final SuppressionCrudService suppressionCrudService;

           public SuppressionController(SuppressionCrudService suppressionCrudService) { this.suppressionCrudService = suppressionCrudService; }

           @GetMapping
           public SuppressionListResponse list() {
               UUID tenantId = TenantContext.currentTenantUuid();
               List<SenderSuppressionProjection> projections = suppressionCrudService.list(tenantId);
               log.info("event=cleanup_suppression_list_requested tenantId={} count={}", tenantId, projections.size());
               return SuppressionListResponse.from(projections);
           }

           @PostMapping
           @ResponseStatus(HttpStatus.CREATED)
           public SuppressionEntryResponse add(@Valid @RequestBody SuppressionAddRequest request) {
               UUID tenantId = TenantContext.currentTenantUuid();
               SenderSuppressionProjection added = suppressionCrudService.addManual(tenantId, request.senderEmailOrDomain());
               return SuppressionEntryResponse.from(added);
           }

           @DeleteMapping("/{suppressionId}")
           @ResponseStatus(HttpStatus.NO_CONTENT)
           public void remove(@PathVariable UUID suppressionId) {
               UUID tenantId = TenantContext.currentTenantUuid();
               suppressionCrudService.remove(tenantId, suppressionId);
               log.info("event=cleanup_suppression_remove_requested tenantId={} suppressionId={}", tenantId, suppressionId);
           }

           @ExceptionHandler(IllegalArgumentException.class)
           public ResponseEntity<ApiErrorResponse> handleDuplicate(IllegalArgumentException exception) {
               // Duplicate suppression → 409
               if ("Duplicate suppression entry".equals(exception.getMessage())) {
                   return ResponseEntity.status(HttpStatus.CONFLICT).body(new ApiErrorResponse("SUPPRESSION_DUPLICATE", exception.getMessage()));
               }
               return ResponseEntity.badRequest().body(new ApiErrorResponse("INVALID_INPUT", exception.getMessage()));
           }
       }
       ```

    Naming: `tenantId`, `jobId`, `senderEmail`, `suppressionId`, `progressPct`, `request`, `result`, `exception`, `projection`. KHÔNG `req/res/ex/e/svc/cfg`.

    Privacy: log line chỉ chứa UUID + counts, KHÔNG sender list bodies.
  </action>
  <verify>
    <automated>./gradlew :backend:api:test --tests "*UnsubscribeCampaignControllerTest*" --tests "*CampaignStatusControllerTest*"</automated>
  </verify>
  <acceptance_criteria>
    - 4 file controller trong `backend/api/src/main/java/com/zeromail/api/controllers/cleanup/` tồn tại.
    - `UnsubscribeCandidateController.java` chứa `@RequestMapping("/api/unsubscribe")` + `@GetMapping("/candidates")`.
    - `UnsubscribeCampaignController.java` chứa 4 mapping: `@PostMapping("/preview")`, `@PostMapping("/execute")`, `@PostMapping("/{jobId}/senders/{senderEmail}/retry")`, `@PostMapping("/{jobId}/undo")`.
    - `UnsubscribeCampaignController.java` chứa 4 `@ExceptionHandler` (CampaignCapExceededException, CampaignRetryConflictException, UndoWindowExpiredException, CampaignNotFoundException).
    - Exception → HTTP status mapping:
      - `CampaignCapExceededException` → `HttpStatus.BAD_REQUEST` (400).
      - `CampaignRetryConflictException` → `HttpStatus.CONFLICT` (409).
      - `UndoWindowExpiredException` → `HttpStatus.GONE` (410).
      - `CampaignNotFoundException` → `HttpStatus.NOT_FOUND` (404).
    - `CampaignStatusController.java` chứa `@GetMapping("/{jobId}")` + `computeProgressPct` + `computeUndoAvailable` methods.
    - `SuppressionController.java` chứa 3 mapping: `@GetMapping`, `@PostMapping`, `@DeleteMapping("/{suppressionId}")`.
    - Wave 0 test `./gradlew :backend:api:test --tests "*UnsubscribeCampaignControllerTest*"` exits 0:
      - `previewWith26Senders_returns400_codeCampaignTooManySenders`.
      - `previewWithOver2000History_returns400_codeCampaignTooManyMessages`.
      - `retryAfterSenderAlreadyOk_returns409`.
      - `undoAfter30Days_returns410_codeUndoWindowExpired`.
      - `executeWithValidInput_returns201AndJobId`.
    - Wave 0 test `./gradlew :backend:api:test --tests "*CampaignStatusControllerTest*"` exits 0.
    - Privacy grep: `grep -nE "log\..*request\.senderEmails[^.]" backend/api/src/main/java/com/zeromail/api/controllers/cleanup/*.java` chỉ match `request.senderEmails().size()` (count only).
  </acceptance_criteria>
  <done>
    4 controller lands. Exception mapping + thin controller pattern. Wave 0 controller tests flip GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: OpenAPI codegen regen (apps/web/lib/api/openapi-types.ts)</name>
  <files>
    apps/web/lib/api/openapi-types.ts
  </files>
  <read_first>
    apps/web/lib/api/openapi-types.ts,
    apps/web/package.json,
    backend/api/src/main/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignController.java,
    backend/api/src/main/java/com/zeromail/api/dto/cleanup/CampaignPreviewResponse.java
  </read_first>
  <action>
    Regen typed client. Steps:

    1. Start Spring `backend/api` (or use existing build task) để export OpenAPI spec.
    2. Run `pnpm --filter web run codegen:openapi` (verify task exists in `apps/web/package.json` scripts — analog từ Phase 4 / Phase 5C).
    3. **ALTERNATIVE** nếu codegen script chưa có: run `pnpm dlx openapi-typescript http://localhost:8080/v3/api-docs --output apps/web/lib/api/openapi-types.ts` (per RESEARCH.md verified version).
    4. Verify regenerated file chứa các type schema mới: `UnsubscribeCandidateListResponse`, `UnsubscribeCandidateResponse`, `CampaignPreviewRequest`, `CampaignPreviewResponse`, `PerSenderPreviewResponse`, `CampaignExecuteRequest`, `CampaignExecuteResponse`, `CampaignStatusResponse`, `PerSenderStateResponse`, `SuppressionListResponse`, `SuppressionEntryResponse`, `SuppressionAddRequest`.
    5. Verify regenerated paths chứa `/api/unsubscribe/candidates`, `/api/unsubscribe/campaigns/preview`, `/api/unsubscribe/campaigns/execute`, `/api/unsubscribe/campaigns/{jobId}`, `/api/unsubscribe/campaigns/{jobId}/senders/{senderEmail}/retry`, `/api/unsubscribe/campaigns/{jobId}/undo`, `/api/cleanup/suppression`.

    **If codegen tooling unavailable** (e.g., no Spring backend running trong CI):
    - Plan B: hand-extend `openapi-types.ts` với new types matching Java DTOs above. Verify type names exactly match (TypeScript components['schemas']['UnsubscribeCandidateResponse'] etc.).
    - Use `apps/web/lib/api/openapi-types.ts` existing structure as template.

    Document trong SUMMARY which path was taken (auto regen vs hand-extend).
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | grep -E "openapi-types|UnsubscribeCandidate|CampaignPreview" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/lib/api/openapi-types.ts` chứa identifier `UnsubscribeCandidateResponse` (grep ≥ 1).
    - File chứa identifier `CampaignPreviewRequest` + `CampaignPreviewResponse`.
    - File chứa identifier `CampaignStatusResponse` + `PerSenderStateResponse`.
    - File chứa identifier `SuppressionEntryResponse`.
    - File chứa path literal `/api/unsubscribe/candidates`.
    - File chứa path literal `/api/unsubscribe/campaigns/preview`.
    - File chứa path literal `/api/cleanup/suppression`.
    - `cd apps/web && pnpm tsc --noEmit` exits 0 (TypeScript compile pass — Wave 5b consumers can now import these types).
  </acceptance_criteria>
  <done>
    `openapi-types.ts` regen với 12+ schema types + 6+ path entries. Wave 5b frontend có thể consume typed client.
  </done>
</task>

</tasks>

<verification>
- 18 file lands (4 controller + 13 DTO + 1 typed client regen).
- Compile + Wave 0 controller test GREEN.
- Exception mapping correct: 400/404/409/410 cho 4 business exception.
- `openapi-types.ts` typed client cập nhật cho Wave 5b consumption.
- Privacy invariant — log lines chỉ counts + UUID.
</verification>

<success_criteria>
- [ ] 4 controller dưới `/api/unsubscribe/*` + `/api/cleanup/suppression` ships.
- [ ] 13 DTO record với `from(projection)` factory + List.copyOf compact constructors.
- [ ] @ExceptionHandler maps domain exception → HTTP code đúng SPEC + Wave 0 test.
- [ ] OpenAPI codegen cập nhật.
- [ ] Wave 0 controller tests `UnsubscribeCampaignControllerTest` + `CampaignStatusControllerTest` GREEN.
</success_criteria>

<output>
Tạo `.planning/phases/08-bulk-unsubscribe-campaign/08-08-SUMMARY.md` khi done.
</output>
