---
phase: 08-bulk-unsubscribe-campaign
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/core/src/test/java/com/zeromail/core/cleanup/application/CandidateQueryServiceTest.java
  - backend/core/src/test/java/com/zeromail/core/cleanup/application/SuppressionServiceTest.java
  - backend/core/src/test/java/com/zeromail/core/cleanup/application/CampaignUndoServiceTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignControllerTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/cleanup/CampaignStatusControllerTest.java
  - backend/core/src/test/java/com/zeromail/core/cleanup/http/UnsubscribeHttpClientTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/UnsubscribeHttpClientBoundaryTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/GmailWriteBoundaryTest.java
  - backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java
  - backend/core/src/test/java/com/zeromail/core/cleanup/CleanupPrivacySweepTest.java
  - backend/core/src/test/java/com/zeromail/core/cleanup/CleanupModuleVerificationTest.java
  - backend/core/src/test/java/com/zeromail/core/cleanup/UnsubscribeMailtoSenderRecipientGuardTest.java
  - backend/core/src/test/java/com/zeromail/core/triage/usecases/TriageGmailWriterLookupLabelIdTest.java
  - backend/core/src/test/java/com/zeromail/core/triage/persistence/TriageAuditWriterCleanupArchiveTest.java
  - backend/worker/src/test/java/com/zeromail/worker/cleanup/ProcessingJobWorkerThrottleDeferralTest.java
  - backend/worker/src/test/java/com/zeromail/worker/cleanup/UnsubscribeCampaignE2ETest.java
  - backend/worker/src/test/java/com/zeromail/worker/cleanup/UnsubscribeDomainThrottleTest.java
  - backend/worker/src/test/java/com/zeromail/worker/scheduling/ProcessingJobReaperBatchTest.java
  - backend/worker/src/test/java/com/zeromail/worker/scheduling/ProcessingJobPurgeBatchTest.java
  - apps/web/features/cleanup/unsubscribe-campaign/hooks/__tests__/useCampaignStatus.test.ts
  - apps/web/features/cleanup/suppression/hooks/__tests__/useSuppressionList.test.ts
  - apps/web/e2e/cleanup-unsubscribe-campaign.spec.ts
  - apps/web/e2e/cleanup-suppression.spec.ts
autonomous: true
requirements: [UNS-01, UNS-02, UNS-03, UNS-04, UNS-05, UNS-06, UNS-07, UNS-08, UNS-09]
must_haves:
  truths:
    - "Tất cả test trong Wave 0 ở trạng thái RED (fail vì class implementation chưa tồn tại) khi chạy `./gradlew :backend:core:test :backend:api:test :backend:worker:test`"
    - "Mọi UNS-01..UNS-09 đều có ít nhất 1 test stub map tới (xem 08-VALIDATION.md `Per-Task Verification Map`)"
    - "`TriageGmailWriteBoundaryTest.java` bị xóa (git mv → `GmailWriteBoundaryTest.java`)"
    - "Wave 1..5 build/sửa code đến khi mọi test stub GREEN"
  artifacts:
    - path: backend/core/src/test/java/com/zeromail/core/cleanup/application/CandidateQueryServiceTest.java
      provides: "UNS-01 RED stub — 3-sender fixture: 1 one-click, 1 mailto, 1 không header + 1 suppressed"
    - path: backend/core/src/test/java/com/zeromail/core/cleanup/application/SuppressionServiceTest.java
      provides: "UNS-02 CRUD + auto-add heuristic (reply ≥1/90d) RED stub"
    - path: backend/core/src/test/java/com/zeromail/core/cleanup/application/CampaignUndoServiceTest.java
      provides: "UNS-07a undo trong 30d RED stub với Clock injection"
    - path: backend/api/src/test/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignControllerTest.java
      provides: "UNS-03a/03b/06/07b — cap reject + retry 409 + undo 410 RED stub"
    - path: backend/api/src/test/java/com/zeromail/api/controllers/cleanup/CampaignStatusControllerTest.java
      provides: "UNS-05 backend status endpoint RED stub"
    - path: backend/core/src/test/java/com/zeromail/core/cleanup/http/UnsubscribeHttpClientTest.java
      provides: "UNS-04c + UNS-08c — WireMock fixture: 200/202/204→OK, 3xx/4xx/5xx/timeout→FAILED, reject http://"
    - path: backend/core/src/test/java/com/zeromail/core/arch/UnsubscribeHttpClientBoundaryTest.java
      provides: "UNS-08a — ArchUnit cấm HttpClient/RestClient ngoài core.cleanup.usecases.UnsubscribeHttpClient"
    - path: backend/core/src/test/java/com/zeromail/core/arch/GmailWriteBoundaryTest.java
      provides: "UNS-08b — Extend allow-list: {TriageGmailWriter, UnsubscribeMailtoSender}; rename khỏi TriageGmailWriteBoundaryTest"
    - path: backend/core/src/test/java/com/zeromail/core/cleanup/CleanupPrivacySweepTest.java
      provides: "UNS-09 — mirror TriagePrivacySweepTest; log sweep với SENTINEL token"
    - path: backend/core/src/test/java/com/zeromail/core/cleanup/CleanupModuleVerificationTest.java
      provides: "Spring Modulith verify allowedDependencies cho core.cleanup"
    - path: backend/core/src/test/java/com/zeromail/core/cleanup/UnsubscribeMailtoSenderRecipientGuardTest.java
      provides: "Unit guard: recipient parse từ URI phải match persisted list_unsubscribe_mailto"
    - path: backend/worker/src/test/java/com/zeromail/worker/cleanup/UnsubscribeCampaignE2ETest.java
      provides: "UNS-04a — SpringBootTest E2E worker pickup + per-sender atomic"
    - path: backend/worker/src/test/java/com/zeromail/worker/cleanup/UnsubscribeDomainThrottleTest.java
      provides: "UNS-04b — Throttle 1/domain/60s + 10/domain/h với Testcontainers Redis"
    - path: backend/worker/src/test/java/com/zeromail/worker/scheduling/ProcessingJobReaperBatchTest.java
      provides: "D-03 crash recovery — heartbeat stale > 5 min reset về QUEUED"
    - path: backend/worker/src/test/java/com/zeromail/worker/scheduling/ProcessingJobPurgeBatchTest.java
      provides: "D-25 retention purge — terminal processing_job rows older than 90d deleted, active/fresh rows retained"
    - path: apps/web/features/cleanup/unsubscribe-campaign/hooks/__tests__/useCampaignStatus.test.ts
      provides: "UNS-05 frontend — refetchInterval=2000 khi QUEUED/RUNNING, false khi terminal"
    - path: apps/web/features/cleanup/suppression/hooks/__tests__/useSuppressionList.test.ts
      provides: "UNS-02 frontend — optimistic update khi add/remove"
    - path: apps/web/e2e/cleanup-unsubscribe-campaign.spec.ts
      provides: "Golden path 9 step (UI-SPEC §Playwright e2e)"
    - path: apps/web/e2e/cleanup-suppression.spec.ts
      provides: "Suppression CRUD + auto-add visibility"
  key_links:
    - from: backend/core/src/test/java/com/zeromail/core/cleanup/CleanupPrivacySweepTest.java
      to: backend/core/src/test/java/com/zeromail/core/triage/TriagePrivacySweepTest.java
      via: "Mirror pattern — `SensitiveMarkerScrubFilter` + `ListAppender<ILoggingEvent>` + `FORBIDDEN_CONTENT_TOKENS` sentinel list"
      pattern: "extends PostgresContainerTest"
    - from: backend/core/src/test/java/com/zeromail/core/arch/GmailWriteBoundaryTest.java
      to: backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java
      via: "Rename + extend allow-list constant tới `List.of(\"...TriageGmailWriter\", \"...UnsubscribeMailtoSender\")` + add `\"send\"` vào `isGmailWriteCall` method matcher"
      pattern: "@AnalyzeClasses(packages = \"com.zeromail\""
---

<objective>
Wave 0: Tạo 21 test file mới + 1 rename (`TriageGmailWriteBoundaryTest` → `GmailWriteBoundaryTest`) ở trạng thái RED. (Adds 3 stubs per HIGH+MEDIUM iteration: `TriageGmailWriterLookupLabelIdTest` per H-2; `TriageAuditWriterCleanupArchiveTest` per H-3; `ProcessingJobWorkerThrottleDeferralTest` per M-2.) Mỗi test stub map tới UNS-01..UNS-09 theo 08-VALIDATION.md `Per-Task Verification Map`. Bắt buộc fail vì class implementation chưa tồn tại — đây là Nyquist gate cho mọi wave sau.

Purpose: Khóa contract test-first cho Phase 8. Wave 1..5 KHÔNG được merge tới khi test stub này GREEN.
Output: 21 file mới + 1 rename. `./gradlew check` BUILD FAILED ở 21 test class mới (expected RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@TESTING.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-SPEC.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-VALIDATION.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-PATTERNS.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-UI-SPEC.md

<interfaces>
<!-- Test stubs reference these symbols KHÔNG TỒN TẠI ở Wave 0 — đó là intent. Wave 1+ tạo. -->

Backend production class FQNs that tests reference (not yet existing — will be created in later waves):
- com.zeromail.core.cleanup.application.CandidateQueryService (Wave 2 / Plan 04)
- com.zeromail.core.cleanup.application.SuppressionCrudService (Wave 2 / Plan 04)
- com.zeromail.core.cleanup.application.SuppressionAutoAddService (Wave 2 / Plan 04)
- com.zeromail.core.cleanup.application.CampaignPreviewService (Wave 4b / Plan 07)
- com.zeromail.core.cleanup.application.CampaignExecuteService (Wave 4b / Plan 07)
- com.zeromail.core.cleanup.application.CampaignStatusQueryService (Wave 4b / Plan 07)
- com.zeromail.core.cleanup.application.CampaignRetryService (Wave 4b / Plan 07)
- com.zeromail.core.cleanup.application.CampaignUndoService (Wave 4b / Plan 07)
- com.zeromail.core.cleanup.usecases.UnsubscribeHttpClient (Wave 3 / Plan 05)
- com.zeromail.core.cleanup.usecases.UnsubscribeMailtoSender (Wave 3 / Plan 05)
- com.zeromail.core.cleanup.usecases.UnsubscribeDomainThrottle (Wave 4a / Plan 06)
- com.zeromail.api.controllers.cleanup.UnsubscribeCampaignController (Wave 5a / Plan 08)
- com.zeromail.api.controllers.cleanup.UnsubscribeCandidateController (Wave 5a / Plan 08)
- com.zeromail.worker.cleanup.ProcessingJobWorker (Wave 4a / Plan 06)
- com.zeromail.worker.cleanup.UnsubscribeCampaignHandler (Wave 4a / Plan 06)
- com.zeromail.worker.scheduling.ProcessingJobReaperBatch (Wave 4a / Plan 06)
- com.zeromail.worker.scheduling.ProcessingJobPurgeBatch (Wave 4a / Plan 06)

Existing classes that tests MUST reference / mirror (read these in <read_first>):
- com.zeromail.core.triage.TriagePrivacySweepTest (privacy sweep analog)
- com.zeromail.core.arch.TriageGmailWriteBoundaryTest (ArchUnit analog — RENAME target)
- com.zeromail.core.PostgresContainerTest (Testcontainers base class)
- com.zeromail.core.shared.tenant.TenantContext (ScopedValue for tenant binding)
- com.zeromail.api.support.JsonHelper (request body JSON)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Java test stubs — core module (8 files) + ArchUnit rename</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/cleanup/application/CandidateQueryServiceTest.java,
    backend/core/src/test/java/com/zeromail/core/cleanup/application/SuppressionServiceTest.java,
    backend/core/src/test/java/com/zeromail/core/cleanup/application/CampaignUndoServiceTest.java,
    backend/core/src/test/java/com/zeromail/core/cleanup/http/UnsubscribeHttpClientTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/UnsubscribeHttpClientBoundaryTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/GmailWriteBoundaryTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java,
    backend/core/src/test/java/com/zeromail/core/cleanup/CleanupPrivacySweepTest.java,
    backend/core/src/test/java/com/zeromail/core/cleanup/CleanupModuleVerificationTest.java,
    backend/core/src/test/java/com/zeromail/core/cleanup/UnsubscribeMailtoSenderRecipientGuardTest.java,
    backend/core/src/test/java/com/zeromail/core/triage/usecases/TriageGmailWriterLookupLabelIdTest.java,
    backend/core/src/test/java/com/zeromail/core/triage/persistence/TriageAuditWriterCleanupArchiveTest.java
  </files>
  <read_first>
    backend/core/src/test/java/com/zeromail/core/triage/TriagePrivacySweepTest.java,
    backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java,
    backend/core/src/test/java/com/zeromail/core/PostgresContainerTest.java,
    backend/core/src/test/java/com/zeromail/core/ApplicationModulesTest.java,
    backend/core/src/test/java/com/zeromail/core/triage/persistence/TriageAuditRepositoryTest.java,
    backend/core/build.gradle.kts,
    CLAUDE.md,
    CONVENTIONS.md,
    TESTING.md,
    .planning/phases/08-bulk-unsubscribe-campaign/08-SPEC.md,
    .planning/phases/08-bulk-unsubscribe-campaign/08-VALIDATION.md,
    .planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
  </read_first>
  <action>
    Tạo 10 file (gồm rename) theo VALIDATION.md `Wave 0 Requirements > Backend`. Quy tắc bắt buộc:
    1. **Rename `TriageGmailWriteBoundaryTest.java` → `GmailWriteBoundaryTest.java`**: dùng `git mv` để giữ history; đổi class name `TriageGmailWriteBoundaryTest` → `GmailWriteBoundaryTest`; thay private constant `TRIAGE_GMAIL_WRITER` thành public-package `ALLOWED_GMAIL_WRITERS = List.of("com.zeromail.core.triage.usecases.TriageGmailWriter", "com.zeromail.core.cleanup.usecases.UnsubscribeMailtoSender")`; mở rộng `resideInAPackage("..core.triage..")` thành `resideInAPackage("..core..")`; trong `isGmailWriteCall` thêm điều kiện `|| (normalizedOwnerName.endsWith(GMAIL_MESSAGES_OWNER) && methodName.equals("send"))`; cập nhật `@ArchTest` field name → `only_allowed_writers_call_gmail_write_apis`. Xóa file gốc `TriageGmailWriteBoundaryTest.java`.
    2. **`CandidateQueryServiceTest`** (D-21, UNS-01): extends `PostgresContainerTest`; `@MockitoBean Clock` not required (read-only query); `@Autowired JdbcTemplate` + `@Autowired CandidateQueryService` (sẽ RED vì class chưa tồn tại — đúng intent); 4 @Test methods: `returnsCandidatesWithOneClickAndMailto`, `excludesSenderWithoutListUnsubscribeHeader`, `excludesSenderInSuppressionList`, `excludesSuppressedDomain`. Fixture: insert 3 mail_message_observed row (one-click URL = 'https://provider.test/u/a1' + one-click flag=true; mailto-only with list_unsubscribe_mailto = 'mailto:unsub@b.test'; no-header NULL); insert 1 sender_suppression row cho sender D; assert `findCandidates(tenantId, ...)` trả 2 candidate có method = ONE_CLICK + MAILTO theo enum.
    3. **`SuppressionServiceTest`** (UNS-02): extends `PostgresContainerTest`; @Test methods cho CRUD: `addSenderEmail_isPersisted`, `addSenderDomain_isPersisted`, `add_rejectsBothNull`, `add_rejectsBothNonNull` (CHECK constraint), `remove_byId_softCheckRowAbsent`, `autoAddAfterUserRepliedOnce_within90d_appearsAsRepliedReason`. Reference `core.triage.usecases.TriageAuditSaga` cho audit-fed pattern. Mock `Clock.fixed(...)` cho 90d cutoff.
    4. **`CampaignUndoServiceTest`** (UNS-07a): extends `PostgresContainerTest`; @Test methods: `undoWithin30Days_restoresInboxAndRemovesLabel`, `undoSetsRevertedAtOnAllAuditRows`, `undoAfter30Days_throwsUndoWindowExpired`. `@MockitoBean Clock` để control time; `@MockitoBean TriageGmailWriter` để verify `restoreToInbox` + `removeLabel` called per archived message.
    5. **`UnsubscribeHttpClientTest`** (UNS-04c, UNS-08c): pure unit + WireMock fixture (use Spring `WireMockExtension` từ `wiremock-spring-boot` đã có trong build); @Test methods: `responds200_returnsOk`, `responds202_returnsOk`, `responds204_returnsOk`, `responds301_returnsFailedHttp3xxRedirect`, `responds410_returnsFailedHttp4xx410`, `responds500_returnsFailedHttp5xx500`, `connectTimeout_returnsFailedTimeout`, `rejectsHttpUrlNotHttps` (assertThrows IllegalArgumentException). Reference RESEARCH §"Error-mapping summary table".
    6. **`UnsubscribeHttpClientBoundaryTest`** (UNS-08a): pure ArchUnit, `@AnalyzeClasses(packages = "com.zeromail", importOptions = ImportOption.DoNotIncludeTests.class)`; rule `only_unsubscribe_http_client_creates_http_machinery` — classes in `..core.cleanup..` should not call `RestClient.builder()`, `RestClient.create()`, hay constructor của `java.net.http.HttpClient.Builder` / `java.net.http.HttpClient` ngoài class `com.zeromail.core.cleanup.usecases.UnsubscribeHttpClient`. `.because("UNS-08: HTTP unsubscribe traffic centralized behind UnsubscribeHttpClient.")` `.allowEmptyShould(true)`. Mirror skeleton từ `TriageGmailWriteBoundaryTest.java` line 22-60 (analog đã đọc trong read_first).
    7. **`CleanupPrivacySweepTest`** (UNS-09): mirror `TriagePrivacySweepTest` line 49-104. Class extends `PostgresContainerTest`; `@Import(MeterRegistryTestConfiguration.class)`; ListAppender + SensitiveMarkerScrubFilter; `FORBIDDEN_CONTENT_TOKENS` = `List.of(SENDER_DISPLAY_NAME_SENTINEL, EMAIL_SUBJECT_SENTINEL, EMAIL_BODY_SENTINEL, RAW_SENDER_EMAIL, RAW_UNSUBSCRIBE_URL_TOKEN, RAW_MAILTO_SUBJECT_TOKEN)`. Fixture: seed sender vào mail_message_observed với `list_unsubscribe_url='https://provider.test/unsub?token=URL_TOKEN_SENTINEL'` + `list_unsubscribe_mailto='mailto:unsub@provider.test?subject=MAILTO_SUBJECT_SENTINEL&body=unsub'`. @MockitoBean cho `UnsubscribeHttpClient` + `UnsubscribeMailtoSender`. @Test method `campaignExecution_doesNotLeakSensitiveTokensInLogs` — run `CampaignExecuteService.execute(...)` + `UnsubscribeCampaignHandler.handle(...)` fixture, capture logs, assert mọi `FORBIDDEN_CONTENT_TOKENS` token NOT in any log line.
    8. **`CleanupModuleVerificationTest`**: thin test sử dụng Spring Modulith `ApplicationModules.of(CoreApplication.class).verify()` — nhưng filter chỉ lấy module `cleanup`. Reference pattern từ `ApplicationModulesTest.java`. @Test `cleanupModuleRespectsAllowedDependencies` — assert module `cleanup` allowedDependencies match `{"gmail", "triage", "analytics", "tenant", "shared.privacy", "shared.persistence", "shared.lang"}` (D-17). Test sẽ RED vì `package-info.java` của `core.cleanup` chưa tồn tại — fine.
    9. **`UnsubscribeMailtoSenderRecipientGuardTest`**: pure unit. @Test methods: `parsesMailtoUriRecipient_correctly`, `rejectsUriIfSchemeNotMailto`, `rejectsIfParsedRecipientDoesNotMatchPersistedHeader`. Mock `TriageGmailWriter` không cần — class chỉ test guard logic của `UnsubscribeMailtoSender.sendUnsubscribeMailto(...)` ở mức exception path (D-06, D-23). Reference RESEARCH §"RFC 6068 — mailto: URI Parsing".
    10. **`TriageGmailWriterLookupLabelIdTest`** (H-2 — NEW from iteration): pure unit + Mockito mock of `GmailApiClientFactory`. @Test methods:
       - `returnsLabelIdWhenLabelExists` — mock `gmail.users().labels().list()` to return a list including label named "Zero Mail/Unsubscribed" with id "Label_42". Assert `lookupLabelId(tenantId, "Zero Mail/Unsubscribed")` returns `Optional.of("Label_42")`.
       - `returnsEmptyWhenLabelMissing` — mock returns a list NOT including the target label. Assert `lookupLabelId(...)` returns `Optional.empty()` (covers the "user manually deleted the label" branch in `CampaignUndoService`).
       - `propagatesIoExceptionFromGmailApi` — mock list call throws `IOException`. Assert `lookupLabelId(...)` propagates (or wraps in a domain exception — match the existing pattern of `applyLabel` for consistency).
    11. **`TriageAuditWriterCleanupArchiveTest`** (H-3 — NEW from iteration): extends `PostgresContainerTest`; `@Autowired TriageAuditWriter` + `@Autowired TriageAuditRepository`. @Test methods:
       - `recordCleanupArchive_persistsRowWithSourceCleanupCampaign` — call `writer.recordCleanupArchive(tenantId, "msg-1", attemptId, "Label_42", "boss@example.com")`; query DB; assert row exists with `source='CLEANUP_CAMPAIGN'`, `action_type='ARCHIVE'`, `external_ref=attemptId.toString()`, `gmail_change_token` contains `"Label_42"`, `sanitized_sender_email='boss@example.com'`.
       - `recordCleanupArchive_doesNotInterfereWithSourceTriageRows` — pre-seed a `source='TRIAGE'` row for the same sender; call `recordCleanupArchive` for the same sender; assert both rows coexist with distinct `source` values; assert the new partial index `idx_triage_audit_cleanup` is used (verify via `EXPLAIN` or by ensuring query plan picks the partial index when filtering by source — soft assertion).
       - `recordCleanupArchive_logsEventWithDomainOnly` — capture log via `ListAppender`; assert log line contains `event=triage_audit_cleanup_archive_recorded` and `senderDomain=example.com` and does NOT contain the full email `boss@example.com`.

    Naming convention: KHÔNG abbreviation (CLAUDE.md backend code style) — viết `gmailMessage`, `processingJob`, `senderEmail`, `unsubscribeAttempt`, `tenantId`. KHÔNG dùng `e/ex/cfg/req/res/svc`.

    Tất cả 8 file mới phải compile (import existing JPA / Mockito / AssertJ / ArchUnit / Spring Boot Test classes), nhưng FAIL khi chạy vì class production trong `core.cleanup.*` chưa tồn tại. Đó là intent RED.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileTestJava 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/core/src/test/java/com/zeromail/core/arch/TriageGmailWriteBoundaryTest.java` không tồn tại (đã rename).
    - File `backend/core/src/test/java/com/zeromail/core/arch/GmailWriteBoundaryTest.java` tồn tại, chứa identifier `ALLOWED_GMAIL_WRITERS` và literal string `"com.zeromail.core.cleanup.usecases.UnsubscribeMailtoSender"`.
    - File `backend/core/src/test/java/com/zeromail/core/arch/GmailWriteBoundaryTest.java` chứa `methodName.equals("send")` trong logic `isGmailWriteCall`.
    - `grep -c "@ArchTest" backend/core/src/test/java/com/zeromail/core/arch/UnsubscribeHttpClientBoundaryTest.java` ≥ 1.
    - `grep -c "extends PostgresContainerTest" backend/core/src/test/java/com/zeromail/core/cleanup/CleanupPrivacySweepTest.java` = 1.
    - `grep -c "FORBIDDEN_CONTENT_TOKENS\|SensitiveMarkerScrubFilter\|ListAppender" backend/core/src/test/java/com/zeromail/core/cleanup/CleanupPrivacySweepTest.java` ≥ 3.
    - `./gradlew :backend:core:compileTestJava` SUCCESS (test stubs compile được — Mockito/ArchUnit imports valid).
    - `./gradlew :backend:core:test --tests "*CandidateQueryServiceTest*"` exits ≠ 0 (RED — class production chưa tồn tại).
    - `./gradlew :backend:core:test --tests "*UnsubscribeHttpClientTest*"` exits ≠ 0 (RED).
    - `./gradlew :backend:core:test --tests "*CleanupPrivacySweepTest*"` exits ≠ 0 (RED).
    - `./gradlew :backend:core:test --tests "*UnsubscribeHttpClientBoundaryTest*"` exits ≠ 0 (RED — `UnsubscribeHttpClient` class chưa tồn tại, ArchUnit không thể match).
    - Mọi test file KHÔNG chứa abbreviation cấm: `grep -nE "\b(req|res|svc|cfg|ctx|msg|err|ex|e)\b" backend/core/src/test/java/com/zeromail/core/cleanup/**/*.java` trả 0 match cho parameter/field names (Java keywords `e` trong catch is OK chỉ khi exception name dài hơn — không cho phép `catch (Exception e)`).
    - File `backend/core/src/test/java/com/zeromail/core/triage/usecases/TriageGmailWriterLookupLabelIdTest.java` chứa identifier `lookupLabelId` và `Optional.empty()` literal (H-2 stub).
    - File `backend/core/src/test/java/com/zeromail/core/triage/persistence/TriageAuditWriterCleanupArchiveTest.java` chứa identifier `recordCleanupArchive` và literal `"CLEANUP_CAMPAIGN"` (H-3 stub).
    - `./gradlew :backend:core:test --tests "*TriageGmailWriterLookupLabelIdTest*"` exits ≠ 0 (RED — `lookupLabelId` not yet implemented; Plan 07 Task 3 + Plan 06 Task 3 ship it).
    - `./gradlew :backend:core:test --tests "*TriageAuditWriterCleanupArchiveTest*"` exits ≠ 0 (RED — `recordCleanupArchive` + changelog 046 + `source` column not yet shipped; Plan 02 Task 4 + Plan 03 Task 4 ship them).
  </acceptance_criteria>
  <done>
    8 file core test stubs mới được tạo + 1 rename. Tất cả compile được. ArchUnit + CleanupPrivacySweepTest fail RED vì code production chưa tồn tại. Naming hygiene pass (no banned abbreviations).
  </done>
</task>

<task type="auto">
  <name>Task 2: Backend Java test stubs — api + worker modules (6 files)</name>
  <files>
    backend/api/src/test/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignControllerTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/cleanup/CampaignStatusControllerTest.java,
    backend/worker/src/test/java/com/zeromail/worker/cleanup/UnsubscribeCampaignE2ETest.java,
    backend/worker/src/test/java/com/zeromail/worker/cleanup/UnsubscribeDomainThrottleTest.java,
    backend/worker/src/test/java/com/zeromail/worker/scheduling/ProcessingJobReaperBatchTest.java,
    backend/worker/src/test/java/com/zeromail/worker/scheduling/ProcessingJobPurgeBatchTest.java,
    backend/worker/src/test/java/com/zeromail/worker/cleanup/ProcessingJobWorkerThrottleDeferralTest.java
  </files>
  <read_first>
    backend/api/src/test/java/com/zeromail/api/controllers/triage/TriageAuditControllerTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/analytics/AnalyticsControllerTest.java,
    backend/api/build.gradle.kts,
    backend/worker/src/test/java/com/zeromail/worker/triage/TriagePendingReaperBatchTest.java,
    backend/worker/src/test/java/com/zeromail/worker/notification/DigestPendingReaperJobTest.java,
    backend/worker/build.gradle.kts,
    backend/worker/src/main/java/com/zeromail/worker/billing/ShedLockConfig.java,
    .planning/phases/08-bulk-unsubscribe-campaign/08-SPEC.md,
    .planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
  </read_first>
  <action>
    Tạo 6 file test stub theo VALIDATION.md `Wave 0 Requirements > Worker` + 2 `Backend api` còn lại. Quy tắc:
    1. **`UnsubscribeCampaignControllerTest`** (UNS-03a, UNS-03b, UNS-06, UNS-07b): `@WebMvcTest(UnsubscribeCampaignController.class)`; `@MockitoBean` cho `CampaignPreviewService` + `CampaignExecuteService` + `CampaignRetryService` + `CampaignUndoService`; @Test methods:
       - `previewWith26Senders_returns400_codeCampaignTooManySenders` — POST `/api/unsubscribe/campaigns/preview` body `{"senderEmails": [...26 emails...]}` → assert HTTP 400 + body JSON `error.code == "CAMPAIGN_TOO_MANY_SENDERS"`.
       - `previewWithOver2000History_returns400_codeCampaignTooManyMessages` — mock service throws `CampaignCapExceededException("CAMPAIGN_TOO_MANY_MESSAGES")` → assert 400 + code.
       - `retryAfterSenderAlreadyOk_returns409` — mock `CampaignRetryService.retry(...)` throws `CampaignRetryConflictException` → assert HTTP 409.
       - `undoAfter30Days_returns410_codeUndoWindowExpired` — mock `CampaignUndoService.undo(...)` throws `UndoWindowExpiredException` → assert HTTP 410 + code.
       - `executeWithValidInput_returns201AndJobId` — mock service returns `CampaignExecuteResult(UUID jobId)` → assert HTTP 201 + JSON `{"jobId": "..."}`.
    2. **`CampaignStatusControllerTest`** (UNS-05 backend half): `@WebMvcTest`; @Test methods:
       - `getStatus_returnsProgressPctAndPerSender` — mock `CampaignStatusQueryService.findStatus(tenantId, jobId)` returns projection with progressPct=40, perSender list of 5 items → assert response JSON shape.
       - `getStatus_jobIdNotFound_returns404` — mock returns Optional.empty → assert HTTP 404.
       - `getStatus_jobIdMalformed_returns400` — request với path `/api/unsubscribe/campaigns/not-a-uuid` → assert 400 (Spring `MethodArgumentTypeMismatchException` handler).
    3. **`UnsubscribeCampaignE2ETest`** (UNS-04a): `@SpringBootTest(webEnvironment=NONE)` + Testcontainers Postgres + Testcontainers Redis (chain `@Container` pattern); `@Autowired CampaignExecuteService` + `@Autowired ProcessingJobWorker` (RED — chưa tồn tại). @Test method `executeCampaign_workerPicksJob_perSenderAtomic`:
       - Insert 3 candidate row trong mail_message_observed + 3 sender không suppressed.
       - Call `CampaignExecuteService.execute(...)` → returns jobId.
       - Mock `UnsubscribeHttpClient.postOneClick` để: sender 1 + 2 returns `UnsubscribeResult.ok(200)`, sender 3 throws → returns `UnsubscribeResult.failed("NETWORK_ERROR")`.
       - Mock `TriageGmailWriter.applyLabel/archiveSkipInbox` để track invocations.
       - `Awaitility.await().atMost(30, SECONDS).until(...)` để worker xử lý job.
       - Assert: campaign.status == COMPLETED; sender 1+2 attempt state == OK + archive_count > 0; sender 3 attempt state == FAILED + archive_count == 0 (per-sender atomic).
    4. **`UnsubscribeDomainThrottleTest`** (UNS-04b): `@SpringBootTest` + Testcontainers Redis; `@Autowired UnsubscribeDomainThrottle`; @Test methods:
       - `firstCallInWindow_returnsAllowed` — call `throttle.acquire(tenantId, "example.com")` → true.
       - `secondCallWithin60s_returnsDenied` — back-to-back call → false (1/domain/60s).
       - `eleventhCallWithin1h_returnsDenied_evenIfSpaced` — loop 11 call với offset Clock.advance(120s) mỗi lần → call thứ 11 fail (10/domain/h).
       - `differentTenants_doNotShareBucket` — tenant A call exhausts; tenant B vẫn allowed (D-20 per-tenant scope).
       - Assert Redis key format match `throttle:unsubscribe:domain:{tenantId}:{domain}:60s` và `:1h` (D-20).
    5. **`ProcessingJobReaperBatchTest`** (D-03 crash recovery): `@SpringBootTest` + Testcontainers Postgres; @Test methods:
       - `staleRunningJob_isResetToQueued` — insert processing_job với status=RUNNING + heartbeat_at = NOW() - 10 min; call `reaperBatch.reap()` → assert row.status == QUEUED + attempts incremented.
       - `freshRunningJob_isNotReset` — heartbeat_at = NOW() - 30s → reap → row.status == RUNNING.
       - `reaperLogsEventWithCount` — capture log + assert `event=processing_job_reaper_reaped count={}` pattern.
    6. **`ProcessingJobPurgeBatchTest`** (D-25 retention): `@SpringBootTest` + Testcontainers Postgres; @Test methods:
       - `terminalJobOlderThan90Days_isDeleted` — insert COMPLETED + finished_at = NOW() - 91d; call `purgeBatch.purge()` → assert row deleted.
       - `freshTerminalJob_isRetained` — insert FAILED + finished_at = NOW() - 89d; purge → assert row still exists.
       - `nonTerminalOldJob_isRetained` — insert RUNNING + finished_at = NOW() - 120d and QUEUED + finished_at = NOW() - 120d; purge → assert both stay.
       - `purgeIsBoundedToOneThousandRowsPerBatch` — insert 1001 expired terminal jobs; call `purgeExpiredOnce(cutoff)` → assert selected/deleted count = 1000.
       - `purgeDoesNotDeleteCampaignAuditTables` — insert linked `unsubscribe_campaign`/`unsubscribe_attempt`; purge expired job → assert audit rows still exist.
       - `purgeLogsEventWithTotalDeleted` — capture log + assert `event=processing_job_purged totalDeleted={}` pattern.
    7. **`ProcessingJobWorkerThrottleDeferralTest`** (M-2 — NEW from iteration): `@SpringBootTest` + Testcontainers Postgres + Redis; `@Autowired ProcessingJobWorker` + `@MockitoBean UnsubscribeCampaignHandler`. @Test methods:
       - `handlerThrowsThrottleDeferred_jobStaysQueuedNotFailed` — mock `UnsubscribeCampaignHandler.handle(...)` to throw `ThrottleDeferredException(tenantId, "example.com", "PER_DOMAIN_60S_EXCEEDED")` AFTER updating the row to status='QUEUED' (simulating the throttle-deferral path in `executeSingleAttempt`). Trigger worker pickup. Assert: `processing_job.status == 'QUEUED'` (NOT 'FAILED') after the dispatch returns; `failure_reason IS NULL`; `event=processing_job_throttle_deferred` log line emitted.
       - `handlerThrowsRuntimeException_jobIsMarkedFailed` — mock handler throws generic `RuntimeException`. Assert: `processing_job.status == 'FAILED'` + `failure_reason == 'RuntimeException'` + `event=processing_job_handler_failed` log line emitted. (Regression guard — the M-2 fix must NOT break the existing generic catch.)
       - `handlerSucceeds_jobIsMarkedCompleted` — mock handler completes normally. Assert: `processing_job.status == 'COMPLETED'` + `finished_at IS NOT NULL`. (Regression guard for success path.)

    Naming convention enforced (no abbreviations). Test methods name verbose (full sentence-like English).

    File rồi sẽ FAIL RED do production class chưa tồn tại (`UnsubscribeCampaignController`, `ProcessingJobWorker`, `UnsubscribeDomainThrottle`, `ProcessingJobReaperBatch`, `ProcessingJobPurgeBatch` đều ở Wave 4a/5a). Compile-only target ở task này.
  </action>
  <verify>
    <automated>./gradlew :backend:api:compileTestJava :backend:worker:compileTestJava 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/api/src/test/java/com/zeromail/api/controllers/cleanup/UnsubscribeCampaignControllerTest.java` chứa literal `"CAMPAIGN_TOO_MANY_SENDERS"`, `"CAMPAIGN_TOO_MANY_MESSAGES"`, `"UNDO_WINDOW_EXPIRED"`.
    - File `UnsubscribeCampaignControllerTest.java` chứa annotation `@WebMvcTest(UnsubscribeCampaignController.class)`.
    - File `backend/worker/src/test/java/com/zeromail/worker/cleanup/UnsubscribeDomainThrottleTest.java` chứa Redis key pattern `throttle:unsubscribe:domain:` (D-20).
    - File `ProcessingJobReaperBatchTest.java` chứa identifier `heartbeat_at` và literal duration `5` (minutes).
    - File `ProcessingJobPurgeBatchTest.java` chứa `Duration.ofDays(90)`, `BATCH_LIMIT`, `COMPLETED`, `FAILED`, và `unsubscribe_campaign`.
    - `./gradlew :backend:api:compileTestJava :backend:worker:compileTestJava` SUCCESS.
    - `./gradlew :backend:api:test --tests "*UnsubscribeCampaignControllerTest*"` exits ≠ 0 (RED — `UnsubscribeCampaignController` class chưa tồn tại).
    - `./gradlew :backend:worker:test --tests "*UnsubscribeCampaignE2ETest*"` exits ≠ 0 (RED).
    - `./gradlew :backend:worker:test --tests "*ProcessingJobReaperBatchTest*"` exits ≠ 0 (RED).
    - `./gradlew :backend:worker:test --tests "*ProcessingJobPurgeBatchTest*"` exits ≠ 0 (RED).
    - `./gradlew :backend:worker:test --tests "*ProcessingJobWorkerThrottleDeferralTest*"` exits ≠ 0 (RED — `ProcessingJobWorker` + `ThrottleDeferredException` not yet implemented).
    - File `ProcessingJobWorkerThrottleDeferralTest.java` chứa literal `ThrottleDeferredException` import + literal `"PER_DOMAIN_60S_EXCEEDED"` reason string + literal `"event=processing_job_throttle_deferred"` log expectation.
    - `grep -E "\b(req|res|svc|cfg|ctx|msg)\b" backend/{api,worker}/src/test/java/com/zeromail/{api/controllers/cleanup,worker/{cleanup,scheduling}}/*.java` trả 0 match (no banned abbreviations).
  </acceptance_criteria>
  <done>
    6 file api+worker test stub mới tạo. Compile pass. Test execution RED expected (production classes Wave 4-5 chưa tồn tại).
  </done>
</task>

<task type="auto">
  <name>Task 3: Frontend Vitest hook stubs (2 files) + Playwright e2e specs (2 files)</name>
  <files>
    apps/web/features/cleanup/unsubscribe-campaign/hooks/__tests__/useCampaignStatus.test.ts,
    apps/web/features/cleanup/suppression/hooks/__tests__/useSuppressionList.test.ts,
    apps/web/e2e/cleanup-unsubscribe-campaign.spec.ts,
    apps/web/e2e/cleanup-suppression.spec.ts
  </files>
  <read_first>
    apps/web/features/analytics/hooks/__tests__/useAnalyticsSummary.test.ts,
    apps/web/features/notifications/hooks/__tests__/useNotificationPreferences.test.ts,
    apps/web/e2e/analytics.spec.ts,
    apps/web/e2e/needs-reply.spec.ts,
    apps/web/e2e/chrome-test-utils.ts,
    apps/web/vitest.config.ts,
    apps/web/playwright.config.ts,
    .planning/phases/08-bulk-unsubscribe-campaign/08-UI-SPEC.md
  </read_first>
  <action>
    Tạo 4 file frontend test stub. Quy tắc:
    1. **`useCampaignStatus.test.ts`** (UNS-05 frontend): Vitest spec — KHÔNG cần JSX (test pure hook). Render hook qua `@testing-library/react` `renderHook` + `QueryClientProvider`. Mock `fetchCampaignStatus` qua `vi.mock('@/features/cleanup/unsubscribe-campaign/api/unsubscribe-campaign-api')`. Test cases:
       - `pollsEvery2sWhenStatusIsQueued` — mock trả `{status: 'QUEUED'}` lần 1, `{status: 'RUNNING'}` lần 2, `{status: 'COMPLETED'}` lần 3. Use `vi.useFakeTimers()` + `await vi.advanceTimersByTimeAsync(2000)` → assert `fetchCampaignStatus` called 3 lần.
       - `stopsPollingWhenStatusBecomesCompleted` — sau COMPLETED, advance time 5s → `fetchCampaignStatus` NOT called again.
       - `stopsPollingWhenStatusBecomesFailed` — same với FAILED.
       - `usesCorrectQueryKey` — assert `queryClient.getQueryData(unsubscribeCampaignKeys.byId(jobId))` returns expected snapshot.

       Hook `useCampaignStatus` + module `unsubscribe-campaign-api.ts` + `query-keys.ts` đều chưa tồn tại (Wave 5b). Mock thông qua `vi.mock(...)` ở top of file để Vitest không complain ESM resolution — test fail vì import path không resolve. Đó là RED expected.
    2. **`useSuppressionList.test.ts`** (UNS-02 frontend): Similar pattern. Test cases:
       - `fetchesSuppressionList_returnsArray`.
       - `optimisticallyAddsEntryOnMutation` — `useAddSuppression` mutation pre-update + rollback on error.
       - `removesEntryFromCache_onSuccessfulRemove`.
    3. **`cleanup-unsubscribe-campaign.spec.ts`** (Playwright golden path UNS-05 9 steps): Mirror `apps/web/e2e/analytics.spec.ts` structure. Loop 2 viewport (desktop 1280×820, mobile 320×740). Use `chrome-test-utils.openAuthenticatedRoute` + `installChromeApiMock` để mock 5 endpoint: `GET /api/unsubscribe/candidates`, `POST /api/unsubscribe/campaigns/preview`, `POST /api/unsubscribe/campaigns/execute`, `GET /api/unsubscribe/campaigns/{jobId}`, `POST /api/unsubscribe/campaigns/{jobId}/undo`. 9 step:
       (1) `await openAuthenticatedRoute(page, '/cleanup/unsubscribe-campaign', state)`;
       (2) `expect(page.getByRole('row')).toHaveCount(3 + 1)` (3 fixture + header);
       (3) check 2 SAFE rows → assert counter `2 / 25 sender đã chọn`;
       (4) click `getByRole('button', { name: 'Xem trước campaign' })` → dialog mở;
       (5) `expect(page.getByText(/2 mail sẽ archive/)).toBeVisible()`;
       (6) click `getByRole('button', { name: 'Execute campaign' })` inside dialog → assert URL match `/cleanup/unsubscribe-campaign/<uuid>`;
       (7) `await expect(page.getByText('Hoàn tất')).toBeVisible({ timeout: 10_000 })` (polling);
       (8) `expect(page.getByRole('button', { name: 'Undo campaign' })).toBeVisible()`;
       (9) goto `/cleanup/suppression` → add row test deferred to spec 4.
    4. **`cleanup-suppression.spec.ts`** (suppression CRUD + auto-add visibility): 2 test:
       - `addManualSuppressionEntry_excludesSenderFromCandidates` — goto `/cleanup/suppression` → fill input `boss@example.com` → click "Thêm vào suppression" → row appears trong table với badge "Thủ công" → goto back `/cleanup/unsubscribe-campaign` → assert sender absent.
       - `autoAddedSenderShowsRepliedBadge` — mock API trả entry với `source='replied'` → assert badge `Đã reply` hiển thị.

    All test files reference identifiers (hook names, route paths, i18n keys) đã được lock trong UI-SPEC. Wave 5b sẽ ship implementation; Wave 0 chỉ stub.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/features/cleanup/unsubscribe-campaign/hooks/__tests__/useCampaignStatus.test.ts` chứa identifier `refetchInterval` và `unsubscribeCampaignKeys.byId`.
    - File `apps/web/features/cleanup/suppression/hooks/__tests__/useSuppressionList.test.ts` chứa identifier `useAddSuppression` và `suppressionKeys`.
    - File `apps/web/e2e/cleanup-unsubscribe-campaign.spec.ts` chứa các string sau (Playwright locator targets): `'Xem trước campaign'`, `'Execute campaign'`, `'Undo campaign'`, `'/cleanup/unsubscribe-campaign'`.
    - File `apps/web/e2e/cleanup-suppression.spec.ts` chứa string `'Thêm vào suppression'` và `'Thủ công'`.
    - Playwright spec uses `chrome-test-utils.openAuthenticatedRoute` + `installChromeApiMock` (grep ≥ 1 occurrence each).
    - `cd apps/web && pnpm tsc --noEmit` exits non-zero is OK (vì module paths chưa tồn tại) — BUT spec file syntax phải valid TypeScript (acceptance: `pnpm tsc --noEmit` chỉ raise unresolved module errors, KHÔNG syntax errors). Verify bằng cách grep stderr cho `Cannot find module '@/features/cleanup'` — đó là expected RED, KHÔNG phải syntax error.
    - `cd apps/web && pnpm exec playwright test --list cleanup-unsubscribe-campaign cleanup-suppression 2>&1` lists ≥ 4 tests (2 file × ≥ 2 viewports/cases).
  </acceptance_criteria>
  <done>
    4 file frontend stub mới tạo. TypeScript module-resolution errors expected (Wave 5b sẽ ship code). Playwright `--list` thấy specs.
  </done>
</task>

</tasks>

<verification>
- 21 file mới + 1 rename theo VALIDATION.md `Wave 0 Requirements` + iteration addendum (`TriageGmailWriterLookupLabelIdTest`, `TriageAuditWriterCleanupArchiveTest`, `ProcessingJobWorkerThrottleDeferralTest`).
- Tất cả test stub compile pass (Java + TypeScript).
- Mọi test stub thực thi RED khi chạy `./gradlew :backend:core:test :backend:api:test :backend:worker:test` — vì production class trong `core.cleanup.*` + `worker.cleanup.*` + `worker.scheduling.ProcessingJobReaperBatch` + `worker.scheduling.ProcessingJobPurgeBatch` chưa tồn tại.
- `TriageGmailWriteBoundaryTest.java` đã xóa; `GmailWriteBoundaryTest.java` đã thay thế với allow-list extend.
- Naming hygiene: 0 occurrence của banned abbreviation (`req/res/svc/cfg/ctx/msg/err/ex/e`) trong test file mới.
</verification>

<success_criteria>
- [ ] 21 file test stub + 1 rename theo VALIDATION.md mapping (mỗi UNS-XX có ≥ 1 stub) + 3 iteration stubs (H-2/H-3/M-2).
- [ ] `./gradlew :backend:core:compileTestJava :backend:api:compileTestJava :backend:worker:compileTestJava` SUCCESS (compile pass).
- [ ] Mọi `./gradlew :backend:*:test --tests "*Cleanup*|*Unsubscribe*|*ProcessingJob*"` exits ≠ 0 (RED expected).
- [ ] `apps/web/e2e/cleanup-*.spec.ts` listed bằng `playwright test --list`.
- [ ] Frontmatter `nyquist_compliant` trong 08-VALIDATION.md có thể flip `true` ở cuối Wave 0 (planner sẽ deferred — checker flip).
</success_criteria>

<output>
Tạo `.planning/phases/08-bulk-unsubscribe-campaign/08-01-SUMMARY.md` khi done.
</output>
