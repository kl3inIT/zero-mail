---
phase: 08-bulk-unsubscribe-campaign
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/cleanup/package-info.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeMethod.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeAttemptState.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/domain/CampaignStatus.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/domain/SuppressionReason.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeCampaignPolicy.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeCampaignEntity.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeCampaignRepository.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeAttemptEntity.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeAttemptRepository.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SenderSuppressionEntity.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SenderSuppressionRepository.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SuppressionReasonAttributeConverter.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/ProcessingJobEntity.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/persistence/ProcessingJobRepository.java
  - backend/core/src/main/java/com/zeromail/core/triage/domain/CleanupAuditSource.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignCapExceededException.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/exception/UndoWindowExpiredException.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignNotFoundException.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignRetryConflictException.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/exception/SuppressedSenderException.java
autonomous: true
requirements: [UNS-01, UNS-02, UNS-03, UNS-04, UNS-05, UNS-06, UNS-07, UNS-08]
must_haves:
  truths:
    - "Spring Modulith `core.cleanup` module declared với allowedDependencies = {gmail, triage, analytics, tenant, shared.privacy, shared.persistence, shared.lang}"
    - "4 enum implement `OrderedEnum`/`IdentifiedEnum` per CONVENTIONS §4 với fromId fail-loud (NoSuchElementException)"
    - "4 entity class (UnsubscribeCampaign / UnsubscribeAttempt / SenderSuppression / ProcessingJob) extend `AbstractTenantOwnedEntity` hoặc `AbstractEntity` đúng tenancy semantics"
    - "5 business exception class extend `RuntimeException` với constructor lấy domain context"
    - "Spring Modulith `CleanupModuleVerificationTest` Wave 0 flip GREEN"
    - "Repository hỗ trợ tối thiểu query cho campaign/attempt/suppression/job lookup by tenant + id"
  artifacts:
    - path: backend/core/src/main/java/com/zeromail/core/cleanup/package-info.java
      provides: "Spring Modulith @ApplicationModule khai báo (D-17)"
      contains: "@ApplicationModule"
    - path: backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeCampaignPolicy.java
      provides: "Constants: MAX_SENDERS_PER_CAMPAIGN=25, MAX_HISTORY_MESSAGES_PER_CAMPAIGN=2000, UNDO_WINDOW=Duration.ofDays(30), UNSUBSCRIBED_LABEL_NAME='Zero Mail/Unsubscribed'"
    - path: backend/core/src/main/java/com/zeromail/core/cleanup/persistence/ProcessingJobEntity.java
      provides: "JPA entity cho generic processing_job table (D-01)"
    - path: backend/core/src/main/java/com/zeromail/core/cleanup/persistence/ProcessingJobRepository.java
      provides: "Repository với `@Query(nativeQuery=true)` SKIP LOCKED claim method (D-02 — chỉ skeleton; Wave 4a sẽ wire vào worker)"
  key_links:
    - from: backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeAttemptEntity.java
      to: backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeCampaignEntity.java
      via: "@ManyToOne `@JoinColumn(name=\"campaign_id\")` FK link"
      pattern: "campaign_id"
    - from: backend/core/src/main/java/com/zeromail/core/cleanup/package-info.java
      to: backend/core/src/main/java/com/zeromail/core/analytics/package-info.java
      via: "Cleanup module depends on analytics (read-side candidate query feeder)"
      pattern: "allowedDependencies"
---

<objective>
Wave 1b: Khai báo Spring Modulith `core.cleanup` module + 4 enum (CONVENTIONS §4 `OrderedEnum`/`IdentifiedEnum` + `fromId` fail-loud) + 1 policy class (constants) + 4 entity class + 4 repository + 5 business exception class. Establish package skeleton + persistence layer cho Wave 2-5.

Purpose: Foundation cho mọi service / controller / worker / test Wave 2+. CleanupModuleVerificationTest (Wave 0) sẽ flip GREEN. UnsubscribeCampaign*EntityTest (nếu có Wave 0 stub không — confirm none required hiện tại) + Liquibase migration (Wave 1a) → JPA entity match column → repository CRUD work.
Output: 19 file Java mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-PATTERNS.md
@backend/core/src/main/java/com/zeromail/core/analytics/package-info.java
@backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java
@backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java
@backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInEntity.java
@backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInRepository.java
@backend/core/src/main/java/com/zeromail/core/onboarding/domain/OnboardingStep.java
@backend/core/src/main/java/com/zeromail/core/gmail/domain/GmailConnectionStatus.java
@backend/core/src/main/java/com/zeromail/core/triage/domain/TriageUndoPolicy.java
@backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoException.java
@backend/core/src/main/java/com/zeromail/core/shared/lang/OrderedEnum.java
@backend/core/src/main/java/com/zeromail/core/shared/lang/IdentifiedEnum.java
@backend/core/src/main/java/com/zeromail/core/shared/persistence/AbstractTenantOwnedEntity.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module declaration + 4 enums + policy class</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/cleanup/package-info.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeMethod.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeAttemptState.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/CampaignStatus.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/SuppressionReason.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeCampaignPolicy.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/analytics/package-info.java,
    backend/core/src/main/java/com/zeromail/core/onboarding/domain/OnboardingStep.java,
    backend/core/src/main/java/com/zeromail/core/gmail/domain/GmailConnectionStatus.java,
    backend/core/src/main/java/com/zeromail/core/shared/lang/OrderedEnum.java,
    backend/core/src/main/java/com/zeromail/core/shared/lang/IdentifiedEnum.java,
    backend/core/src/main/java/com/zeromail/core/triage/domain/TriageUndoPolicy.java,
    CONVENTIONS.md
  </read_first>
  <action>
    Tạo 6 file:
    1. **`package-info.java`** (D-17): copy structure từ `analytics/package-info.java`, đổi:
       ```
       @ApplicationModule(
           displayName = "Cleanup",
           allowedDependencies = {"gmail", "triage", "analytics", "tenant",
               "shared.privacy", "shared.persistence", "shared.lang"})
       package com.zeromail.core.cleanup;
       import org.springframework.modulith.ApplicationModule;
       ```
       (Bao gồm `triage` để dùng `TriageGmailWriter` + `TriageAuditWriter`; `analytics` cho future candidate query cross-ref.)
    2. **`UnsubscribeMethod.java`** (D-23 + CONVENTIONS §4): `enum UnsubscribeMethod implements IdentifiedEnum { ONE_CLICK("ONE_CLICK"), MAILTO("MAILTO"), NONE("NONE"); }` — unordered (different methods, no ordering semantic). Static `fromId(String id)` throws `NoSuchElementException` on unknown. Override `String id()`. Pattern exact theo `GmailConnectionStatus.java`.
    3. **`UnsubscribeAttemptState.java`**: `enum UnsubscribeAttemptState implements OrderedEnum { PENDING(10, "PENDING"), RUNNING(20, "RUNNING"), OK(30, "OK"), FAILED(40, "FAILED"); }` — ordered state machine (PENDING < RUNNING < OK/FAILED). `int weight()` + `String id()` + static `fromId`. Pattern theo `OnboardingStep.java`.
    4. **`CampaignStatus.java`**: `enum CampaignStatus implements OrderedEnum { QUEUED(10, "QUEUED"), RUNNING(20, "RUNNING"), COMPLETED(30, "COMPLETED"), FAILED(40, "FAILED"); }` — same pattern.
    5. **`SuppressionReason.java`**: `enum SuppressionReason implements IdentifiedEnum { MANUAL("manual"), REPLIED("replied"), AUTO("auto"); }` — match DB CHECK constraint `043-sender-suppression.yaml` (LOWERCASE values). Static `fromId` fail-loud.
    6. **`UnsubscribeCampaignPolicy.java`** (analog `TriageUndoPolicy.java`):
       - `public final class UnsubscribeCampaignPolicy` private constructor.
       - `public static final int MAX_SENDERS_PER_CAMPAIGN = 25;`
       - `public static final int MAX_HISTORY_MESSAGES_PER_CAMPAIGN = 2000;`
       - `public static final Duration UNDO_WINDOW = Duration.ofDays(30);`
       - `public static final String UNSUBSCRIBED_LABEL_NAME = "Zero Mail/Unsubscribed";`
       - `public static final Duration THROTTLE_PER_DOMAIN_WINDOW_60S = Duration.ofSeconds(60);` (D-20)
       - `public static final Duration THROTTLE_PER_DOMAIN_WINDOW_1H = Duration.ofHours(1);`
       - `public static final int THROTTLE_MAX_PER_DOMAIN_60S = 1;`
       - `public static final int THROTTLE_MAX_PER_DOMAIN_1H = 10;`
       - `public static Instant undoableUntil(Instant appliedAt) { return Objects.requireNonNull(appliedAt, "appliedAt").plus(UNDO_WINDOW); }`

    Naming: full domain identifier (no `cap` short for capacity etc.) — `MAX_SENDERS_PER_CAMPAIGN`, `unsubscribedLabelName`. Java keyword `e` cấm (no `catch (Exception e)`); dùng `catch (NoSuchElementException unknownIdentifier)` etc.

    KHÔNG dùng Lombok. Records cho DTOs (sau), classes cho entities (sau), enum implements interface (đây).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/core/src/main/java/com/zeromail/core/cleanup/package-info.java` chứa `@ApplicationModule(` và literal `"gmail"`, `"triage"`, `"analytics"`, `"tenant"`, `"shared.privacy"`, `"shared.persistence"`, `"shared.lang"` trong `allowedDependencies` array.
    - 4 enum file đều chứa `implements OrderedEnum` HOẶC `implements IdentifiedEnum` (CONVENTIONS §4).
    - Mỗi enum file chứa method `public static <EnumType> fromId(String id)` với `throws NoSuchElementException` semantics (line chứa `orElseThrow(() -> new NoSuchElementException`).
    - `UnsubscribeCampaignPolicy.java` chứa 4 constant literal: `25`, `2000`, `30` (days), `"Zero Mail/Unsubscribed"`.
    - `UnsubscribeCampaignPolicy.java` chứa Redis throttle constants: `THROTTLE_PER_DOMAIN_WINDOW_60S`, `THROTTLE_MAX_PER_DOMAIN_1H`.
    - `./gradlew :backend:core:compileJava` exits 0.
    - `./gradlew :backend:core:test --tests "*ApplicationModulesTest*"` exits 0 (Spring Modulith verify pass — KHÔNG fail vì allowedDependencies match).
    - `./gradlew :backend:core:test --tests "*CleanupModuleVerificationTest*"` exits 0 (Wave 0 stub flip GREEN — module declared).
    - `grep -E "\b(req|res|svc|cfg|ctx|msg|err|ex|conn|tx)\b" backend/core/src/main/java/com/zeromail/core/cleanup/domain/*.java backend/core/src/main/java/com/zeromail/core/cleanup/package-info.java` trả 0 match (no banned abbreviations).
  </acceptance_criteria>
  <done>
    Module declared. 4 enum + 1 policy class lands. Wave 0 `CleanupModuleVerificationTest` flips GREEN. Compile pass. Spring Modulith verify pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: 4 JPA entities + 4 repositories</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeCampaignEntity.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeCampaignRepository.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeAttemptEntity.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeAttemptRepository.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SenderSuppressionEntity.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SenderSuppressionRepository.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/ProcessingJobEntity.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/ProcessingJobRepository.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInEntity.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantSenderOptInRepository.java,
    backend/core/src/main/java/com/zeromail/core/shared/persistence/AbstractTenantOwnedEntity.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeMethod.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeAttemptState.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/CampaignStatus.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/SuppressionReason.java,
    backend/core/src/main/resources/db/changelog/changes/042-processing-job.yaml,
    backend/core/src/main/resources/db/changelog/changes/043-sender-suppression.yaml,
    backend/core/src/main/resources/db/changelog/changes/044-unsubscribe-campaign.yaml,
    backend/core/src/main/resources/db/changelog/changes/045-unsubscribe-attempt.yaml
  </read_first>
  <action>
    Tạo 4 entity + 4 repository. Quy tắc shared (CONVENTIONS §3 + analog `TriageAuditEntity.java`):
    - Class (KHÔNG record), `@Entity`, `@Table(name = "<table>")`.
    - Extend `AbstractTenantOwnedEntity` (Phase 1.2.1) — provides `tenantId` + `createdAt` + `updatedAt` + `id`.
    - `@AttributeOverride(name = "id", column = @Column(name = "id", nullable = false))` để map PK `id` (entity hierarchy default name `audit_id` không apply).
    - Field column annotations match Liquibase exact: `varchar`/`uuid`/`timestamptz`/`int`/`boolean` đúng nullable.
    - `protected NoArgsConstructor()` (Hibernate proxy) + explicit `public Constructor(...)` (Lombok-free CONVENTIONS §3).
    - Enum field: `@Enumerated(EnumType.STRING)` + `@Column(name = "<col>")` — values match Liquibase CHECK exactly.
    - Getter only (no setter — entity invariants enforced via explicit mutation method ví dụ `markRunning(Instant startedAt)`).
    - `@SuppressWarnings("JpaDataSourceORMInspection")` ở class level.

    1. **`UnsubscribeCampaignEntity`** (analog `TriageAuditEntity`):
       - Table `unsubscribe_campaign`.
       - Fields: `UUID id` (override), `UUID tenantId` (inherited), `UUID jobId`, `CampaignStatus status`, `Instant appliedAt`, `Instant revertedAt`, `int totalSenderCount`, `int totalHistoryMessageCount`, `Instant createdAt`.
       - Constructor `public UnsubscribeCampaignEntity(UUID tenantId, int totalSenderCount, int totalHistoryMessageCount)` — initialize id via parent (`gen_random_uuid` default), set `status = CampaignStatus.QUEUED`, `appliedAt = null`.
       - Method `markCompleted(Instant appliedAt)` — set status=COMPLETED + appliedAt; validate from-state ∈ {RUNNING}.
       - Method `markFailed()` — set status=FAILED.
       - Method `markReverted(Instant revertedAt)` — set revertedAt + assert appliedAt non-null.
       - Method `linkJob(UUID jobId)` — set jobId one-time (assert null before).
    2. **`UnsubscribeAttemptEntity`** (analog `TriageAuditEntity`):
       - Table `unsubscribe_attempt`.
       - Fields: `UUID id`, `UUID campaignId`, `String senderEmail`, `String senderDomain`, `UnsubscribeMethod unsubscribeMethod`, `UnsubscribeAttemptState state`, `String failureReason`, `int archivedMessageCount`, `Instant startedAt`, `Instant finishedAt`.
       - Constructor: `public UnsubscribeAttemptEntity(UUID campaignId, String senderEmail, String senderDomain, UnsubscribeMethod method)` — set state=PENDING, archivedMessageCount=0.
       - Methods: `markRunning(Instant startedAt)`, `markOk(int archivedMessageCount, Instant finishedAt)`, `markFailed(String failureReason, Instant finishedAt)`, `resetToPending()` — set state=PENDING + clear startedAt/finishedAt/failureReason; used by (a) throttle-deferred path in `UnsubscribeCampaignHandler` (Plan 06 Task 3) when worker re-queues the job, and (b) `CampaignRetryService` (Plan 07 Task 2) when retrying a FAILED attempt. Validate from-state ∈ {PENDING, RUNNING, FAILED} (NOT OK — OK is terminal; retry of OK throws CampaignRetryConflictException via Plan 07).
       - KHÔNG có `tenantId` cột riêng — tenancy join thông qua campaign (analog: `TriageAuditEntity` direct tenancy, `UnsubscribeAttempt` indirect via campaign FK). NOTE: do KHÔNG extend `AbstractTenantOwnedEntity`. Thay vào đó extend `AbstractEntity` (Phase 1.2.1 Plan 01) cho id + createdAt + updatedAt. Verify `AbstractEntity` tồn tại trong shared/persistence; nếu không, extend `AbstractAuditableEntity` skip tenancy.
    3. **`SenderSuppressionEntity`** (analog `TenantSenderOptInEntity`):
       - Table `sender_suppression`.
       - Fields: `UUID id`, `UUID tenantId`, `String senderEmail`, `String senderDomain`, `SuppressionReason reason`, `Instant createdAt`.
       - Constructor: `public SenderSuppressionEntity(UUID tenantId, String senderEmail, String senderDomain, SuppressionReason reason)` — assert exactly one of senderEmail / senderDomain non-null (XOR mirror DB CHECK).
       - Enum mapping: `@Convert(converter = SuppressionReasonAttributeConverter.class)` on the `reason` field (NOT `@Enumerated(EnumType.STRING)`, which would persist `name()` "MANUAL" and violate changelog 043's lowercase CHECK constraint `reason IN ('manual','replied','auto')`). The converter (separate file — see decision below) auto-applies via `@Converter(autoApply = true)` so the field declaration only needs `@Column(name = "reason", nullable = false, length = 16)`.

       **Decision (M-1 resolution):** Create `SuppressionReasonAttributeConverter` as a separate file at `backend/core/src/main/java/com/zeromail/core/cleanup/persistence/SuppressionReasonAttributeConverter.java`, matching the existing analog `core.triage.persistence.TriageActionAttributeConverter`. Reasoning: JPA `@Converter` requires a no-arg public class; keeping converters in their own file next to the entity is the established project pattern. Annotate with `@Converter(autoApply = true)` so all `SuppressionReason` fields on entities serialize via `id()` (lowercase `'manual'`/`'replied'`/`'auto'`) and deserialize via `SuppressionReason.fromId(String)`. This keeps changelog 043's lowercase CHECK constraint intact and avoids changing DB CHECK to uppercase.
    4. **`ProcessingJobEntity`** (analog `TriageAuditEntity`, generic):
       - Table `processing_job`.
       - Fields: `UUID id`, `UUID tenantId`, `String jobType`, `String payload` (raw JSONB as String — let JPA store JSON via Hibernate `@JdbcTypeCode(SqlTypes.JSON)`), `String status` (use String not enum — payload-agnostic dispatch; Wave 4 ProcessingJobWorker dispatch trên jobType string), `int attempts`, `Instant nextRunAt`, `Instant heartbeatAt`, `Instant createdAt`, `Instant startedAt`, `Instant finishedAt`, `String failureReason`, `Instant updatedAt`.
       - Constructor: `public ProcessingJobEntity(UUID tenantId, String jobType, String payload)`.
       - Method `markRunning(Instant startedAt)`, `markCompleted(Instant finishedAt)`, `markFailed(String reason, Instant finishedAt)`, `updateHeartbeat(Instant now)`, `incrementAttempts()`, `resetToQueued()`.

    Repositories:
    1. **`UnsubscribeCampaignRepository extends JpaRepository<UnsubscribeCampaignEntity, UUID>`**:
       - `Optional<UnsubscribeCampaignEntity> findByIdAndTenantId(UUID id, UUID tenantId)`.
       - `List<UnsubscribeCampaignEntity> findByTenantIdOrderByCreatedAtDesc(UUID tenantId, Pageable pageable)`.
    2. **`UnsubscribeAttemptRepository extends JpaRepository<UnsubscribeAttemptEntity, UUID>`**:
       - `List<UnsubscribeAttemptEntity> findByCampaignIdOrderBySenderEmailAsc(UUID campaignId)`.
       - `Optional<UnsubscribeAttemptEntity> findByCampaignIdAndSenderEmail(UUID campaignId, String senderEmail)`.
       - `long countByCampaignIdAndState(UUID campaignId, UnsubscribeAttemptState state)`.
    3. **`SenderSuppressionRepository extends JpaRepository<SenderSuppressionEntity, UUID>`**:
       - `List<SenderSuppressionEntity> findByTenantIdOrderByCreatedAtDesc(UUID tenantId)`.
       - `Optional<SenderSuppressionEntity> findByTenantIdAndSenderEmail(UUID tenantId, String senderEmail)`.
       - `Optional<SenderSuppressionEntity> findByTenantIdAndSenderDomain(UUID tenantId, String senderDomain)`.
       - `Optional<SenderSuppressionEntity> findByIdAndTenantId(UUID id, UUID tenantId)`.
    4. **`ProcessingJobRepository extends JpaRepository<ProcessingJobEntity, UUID>`**:
       - Method skeleton: `Optional<UUID> claimQueuedJob()` annotated với `@Query(nativeQuery = true, value = "SELECT id FROM processing_job WHERE status = 'QUEUED' AND next_run_at <= NOW() ORDER BY created_at LIMIT 1 FOR UPDATE SKIP LOCKED")` — Wave 4a sẽ wire vào `ProcessingJobWorker`. (Method KHÔNG mark `@Modifying` vì chỉ SELECT.)
       - `Optional<ProcessingJobEntity> findByIdAndTenantId(UUID id, UUID tenantId)`.
       - `int markStaleRunningJobsAsQueued(Instant cutoff)` (native UPDATE — wave 4a will use, declare skeleton here):
         ```java
         @Modifying
         @Query(nativeQuery = true, value = "UPDATE processing_job SET status='QUEUED', attempts = attempts + 1, heartbeat_at=NULL, updated_at=NOW(), next_run_at=NOW() WHERE status='RUNNING' AND heartbeat_at < :cutoff")
         int markStaleRunningJobsAsQueued(@Param("cutoff") Instant cutoff);
         ```

    Naming: Full domain identifiers — `unsubscribeAttempt`, `senderEmail`, `senderDomain`, `processingJob`, `failureReason`, `heartbeatAt`. Banned: `req/res/svc/ex/e/cfg/ctx/msg/repo/conn/tx`. Constructor parameter cùng tên với field (Java 25 record-ish but classes).
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava :backend:core:compileTestJava 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 8 file Java + 1 converter file tồn tại (9 total NEW production files trong Task 2). [Add `SuppressionReasonAttributeConverter.java` vào commit; nếu planner để files_modified missing nó là acceptable miễn được ship.]
    - 4 entity class extend `AbstractTenantOwnedEntity` HOẶC `AbstractEntity` đúng (UnsubscribeCampaign + SenderSuppression + ProcessingJob extend AbstractTenantOwnedEntity; UnsubscribeAttempt extend AbstractEntity vì tenancy indirect).
    - Mỗi entity class chứa `protected NoArgsConstructor()` + ≥ 1 `public` constructor explicit (no Lombok).
    - `@Enumerated(EnumType.STRING)` annotation hiện trên enum field.
    - `ProcessingJobRepository.java` chứa literal string `FOR UPDATE SKIP LOCKED` trong `@Query` (D-02 native SQL).
    - `ProcessingJobRepository.java` chứa method `markStaleRunningJobsAsQueued` với `@Modifying` annotation.
    - `./gradlew :backend:core:compileJava :backend:core:compileTestJava` exits 0.
    - `./gradlew :backend:core:test --tests "*ApplicationModulesTest*"` exits 0 (Modulith verify still pass).
    - Run khởi động: `./gradlew :backend:core:test --tests "*PostgresContainer*"` — bất kỳ test extends PostgresContainerTest pickle Liquibase apply + JPA metamodel boot không error.
    - `grep -E "\bnew \w+ (req|svc|cfg|ctx|msg|err|ex|e)\s*[=;]" backend/core/src/main/java/com/zeromail/core/cleanup/persistence/*.java` trả 0 (no banned local var names).
  </acceptance_criteria>
  <done>
    8 entity/repository class + 1 converter lands. JPA metamodel boots clean với Liquibase 041-045 schema. Compile pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: 5 business exception classes</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignCapExceededException.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/UndoWindowExpiredException.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignNotFoundException.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/CampaignRetryConflictException.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/exception/SuppressedSenderException.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/triage/exception/TriageUndoException.java,
    backend/core/src/main/java/com/zeromail/core/triage/exception/TriageAuditNotFoundException.java,
    backend/core/src/main/java/com/zeromail/core/triage/exception/TriageSafetyViolationException.java,
    CONVENTIONS.md
  </read_first>
  <action>
    Tạo 5 file exception. Mỗi exception extend `RuntimeException` (CONVENTIONS §1 — không checked exception trong service layer). Pattern theo `TriageUndoException.java`.

    1. **`CampaignCapExceededException`** (UNS-03a + UNS-03b):
       - Has enum field `Kind kind`: `Kind.TOO_MANY_SENDERS` hoặc `Kind.TOO_MANY_MESSAGES`.
       - Constructor `public CampaignCapExceededException(Kind kind, int actual, int cap)` — message format `"Campaign cap exceeded: kind=%s actual=%d cap=%d".formatted(...)` (KHÔNG log full sender email — privacy invariant).
       - Getter `kind()`, `actual()`, `cap()`.
       - `Kind` is nested public enum `{ TOO_MANY_SENDERS, TOO_MANY_MESSAGES }` — each maps to HTTP code string via `errorCode()` method returning `"CAMPAIGN_TOO_MANY_SENDERS"` hoặc `"CAMPAIGN_TOO_MANY_MESSAGES"`.

    2. **`UndoWindowExpiredException`** (UNS-07b):
       - Constructor `public UndoWindowExpiredException(UUID campaignId, Instant appliedAt, Duration windowDuration)`.
       - Message `"Undo window expired: campaignId=%s appliedAt=%s windowDuration=%s".formatted(campaignId, appliedAt, windowDuration)`.
       - errorCode = `"UNDO_WINDOW_EXPIRED"`.

    3. **`CampaignNotFoundException`**:
       - Constructor `public CampaignNotFoundException(UUID campaignId)`.
       - errorCode = `"CAMPAIGN_NOT_FOUND"`.

    4. **`CampaignRetryConflictException`** (UNS-06):
       - Constructor `public CampaignRetryConflictException(UUID campaignId, String senderEmail, UnsubscribeAttemptState currentState)`.
       - Message KHÔNG include senderEmail full — privacy. Format `"Retry conflict: campaignId=%s senderDomain=%s currentState=%s".formatted(campaignId, extractDomain(senderEmail), currentState.id())`. Inline helper `extractDomain` static method.
       - errorCode = `"RETRY_CONFLICT"`.

    5. **`SuppressedSenderException`**:
       - Constructor `public SuppressedSenderException(UUID tenantId, String senderEmail)`.
       - Message KHÔNG include senderEmail full — `"Suppressed sender: tenantId=%s senderDomain=%s"`.
       - errorCode = `"SENDER_SUPPRESSED"`.

    Naming: full identifier — `kind`, `actual`, `cap`, `campaignId`, `appliedAt`, `windowDuration`, `currentState`, `senderEmail`, `senderDomain`, `tenantId`. Banned: `ex/e/err/cfg`.

    Privacy invariant (UNS-09): exception message KHÔNG được include full sender email. Trong `extractDomain` helper: `senderEmail.substring(senderEmail.indexOf('@') + 1)`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 5 file exception trong `backend/core/src/main/java/com/zeromail/core/cleanup/exception/` tồn tại.
    - Mỗi exception class `extends RuntimeException`.
    - `CampaignCapExceededException.java` chứa enum `Kind` với 2 value `TOO_MANY_SENDERS` và `TOO_MANY_MESSAGES`.
    - `CampaignCapExceededException.java` chứa method `errorCode()` returning literal `"CAMPAIGN_TOO_MANY_SENDERS"` HOẶC `"CAMPAIGN_TOO_MANY_MESSAGES"`.
    - `UndoWindowExpiredException.java` chứa literal `"UNDO_WINDOW_EXPIRED"`.
    - `CampaignRetryConflictException.java` chứa literal `"RETRY_CONFLICT"` và method `extractDomain` HOẶC equivalent inline pattern (no full senderEmail in exception message).
    - Privacy grep: `grep -nE 'senderEmail.*"%s"' backend/core/src/main/java/com/zeromail/core/cleanup/exception/*.java` chỉ match cho domain extraction, KHÔNG cho raw senderEmail.format.
    - `./gradlew :backend:core:compileJava` exits 0.
    - `grep -E "\b(req|res|svc|cfg|ctx|msg|err|ex|e|conn|tx)\b\s*[)=,]" backend/core/src/main/java/com/zeromail/core/cleanup/exception/*.java` trả 0 (no banned param/local names).
  </acceptance_criteria>
  <done>
    5 business exception class lands. errorCode pattern match controller exception handler Wave 5a. Privacy invariant enforced trong message format.
  </done>
</task>


<task type="auto">
  <name>Task 4: Extend TriageAuditEntity with source field + TriageAuditWriter.recordCleanupArchive (H-3 Path A)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/triage/domain/CleanupAuditSource.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditRepository.java,
    backend/core/src/main/resources/db/changelog/changes/046-triage-audit-source.yaml,
    backend/core/src/main/java/com/zeromail/core/shared/lang/IdentifiedEnum.java,
    CONVENTIONS.md
  </read_first>
  <action>
    Per H-3 Path A resolution (RESEARCH.md Open Question §1) — extend existing `triage_audit` surface so cleanup-campaign audit rows are first-class. Changelog 046 (Plan 02 Task 4) added the DB column; this task wires the JPA + writer surface.

    1. **`CleanupAuditSource.java`** (new enum, placed in `core.triage.domain` because it sits on the triage-owned `TriageAuditEntity`, not in cleanup module — keeps Spring Modulith dependency direction clean: `cleanup` already depends on `triage`, so `triage` must NOT depend on `cleanup`):
       ```
       public enum CleanupAuditSource implements IdentifiedEnum {
           TRIAGE("TRIAGE"),
           CLEANUP_CAMPAIGN("CLEANUP_CAMPAIGN");
           // standard id() + fromId(String) fail-loud per CONVENTIONS §4
       }
       ```
       Matches changelog 046's CHECK constraint values exactly (uppercase, `name()` == `id()`). Default DB value `'TRIAGE'` corresponds to enum constant `TRIAGE`.

    2. **`TriageAuditEntity.java`** — ADD field:
       ```
       @Column(name = "source", nullable = false, length = 32)
       @Convert(converter = CleanupAuditSourceAttributeConverter.class)  // OR @Enumerated(STRING) since enum.name() == constraint value
       private CleanupAuditSource source;
       ```
       Since `CleanupAuditSource.name()` matches the DB CHECK values verbatim ("TRIAGE"/"CLEANUP_CAMPAIGN"), `@Enumerated(EnumType.STRING)` is sufficient — no converter needed. Use `@Enumerated(EnumType.STRING)`.
       - Add getter `public CleanupAuditSource getSource()`.
       - Add no-setter mutation: constructor variants take `CleanupAuditSource` so the field is set at insert time (default `TRIAGE` for triage-side writes; `CLEANUP_CAMPAIGN` for the new `recordCleanupArchive` path).
       - Existing constructors must default `this.source = CleanupAuditSource.TRIAGE` so all current triage writes continue producing `source=TRIAGE` rows without code change at triage call sites.

    3. **`TriageAuditWriter.java`** — ADD method:
       ```
       public void recordCleanupArchive(
               UUID tenantId,
               String gmailMessageId,
               UUID campaignAttemptId,
               String labelId,
               String senderEmail) {
           // Build TriageAuditEntity with:
           //   tenantId, gmailMessageId,
           //   actionType = "ARCHIVE",
           //   decision   = "APPLIED",
           //   decidedAt + appliedAt = clock.instant(),
           //   sanitizedSenderEmail = senderEmail,  // already sanitized by caller per existing convention
           //   source     = CleanupAuditSource.CLEANUP_CAMPAIGN,
           //   externalRef = campaignAttemptId.toString(),  // links audit row back to unsubscribe_attempt.id
           //   gmailChangeToken = "{\"appliedLabelIds\":[\"" + labelId + "\"]}"  // matches existing convention; labelId is the Gmail label ID returned by ensureLabelExists
           // Persist via JPA repository.
           // Log: event=triage_audit_cleanup_archive_recorded tenantId={} attemptId={} senderDomain={extractDomain(senderEmail)}
       }
       ```
       - Method is explicit (NO `...` ellipsis in real source — listed here only as planning shorthand for the field-population block).
       - Signature exact: `public void recordCleanupArchive(UUID tenantId, String gmailMessageId, UUID campaignAttemptId, String labelId, String senderEmail)` — `labelId` is the opaque Gmail label ID returned by `TriageGmailWriter.ensureLabelExists` (Plan 06 Task 3), stored verbatim in `gmail_change_token` JSONB to match existing triage convention.
       - Privacy: log line MUST use `extractDomain(senderEmail)` helper (existing in `core.shared.privacy` or inline), NEVER the full email.

    4. **`TriageAuditRepository.java`** — ADD query method for the undo path:
       ```
       @Query(nativeQuery = true, value =
           "SELECT gmail_message_id, gmail_change_token, external_ref " +
           "FROM triage_audit " +
           "WHERE tenant_id = :tenantId " +
           "  AND sanitized_sender_email = :senderEmail " +
           "  AND source = 'CLEANUP_CAMPAIGN' " +
           "  AND applied_at >= :undoWindowStart " +
           "  AND reverted_at IS NULL")
       List<Object[]> findCleanupArchiveRowsForUndo(
               @Param("tenantId") UUID tenantId,
               @Param("senderEmail") String senderEmail,
               @Param("undoWindowStart") Instant undoWindowStart);
       ```
       Uses the new partial index `idx_triage_audit_cleanup` from changelog 046.
       Plan 07 Task 3 `CampaignUndoService` invokes this method per sender to drive `restoreToInbox` + `removeLabel` calls.

    Naming hygiene: `tenantId`, `gmailMessageId`, `campaignAttemptId`, `labelId`, `senderEmail`, `undoWindowStart`. NO `req/res/svc/ex/e/cfg/ctx/msg/repo/conn/tx`.

    Spring Modulith: `triage` module does NOT gain a new dependency — `CleanupAuditSource` is in `core.triage.domain`, not `core.cleanup`. Cross-module call direction stays `cleanup → triage` per D-17.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava :backend:core:compileTestJava 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/core/src/main/java/com/zeromail/core/triage/domain/CleanupAuditSource.java` tồn tại; chứa `implements IdentifiedEnum` + 2 enum constants `TRIAGE` và `CLEANUP_CAMPAIGN` + `fromId` fail-loud (`orElseThrow(() -> new NoSuchElementException`).
    - `TriageAuditEntity.java` chứa field `private CleanupAuditSource source;` + `@Column(name = "source"` + `@Enumerated(EnumType.STRING)`.
    - `TriageAuditEntity.java` chứa getter `public CleanupAuditSource getSource()`.
    - `TriageAuditWriter.java` chứa method signature exact: `public void recordCleanupArchive(UUID tenantId, String gmailMessageId, UUID campaignAttemptId, String labelId, String senderEmail)` — grep returns exactly 1 match. (`labelId` is `String` — opaque Gmail label ID; aligned with `TriageGmailWriter.ensureLabelExists` return type per Plan 06 Task 3.)
    - `TriageAuditWriter.recordCleanupArchive` body chứa literal `CleanupAuditSource.CLEANUP_CAMPAIGN`.
    - `TriageAuditWriter.recordCleanupArchive` log line uses `extractDomain` HOẶC equivalent domain-only formatting — `grep -E "log\..*senderEmail[^.A-Za-z]" backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditWriter.java` returns only matches that pass through a domain helper.
    - `TriageAuditRepository.java` chứa SQL literal `"AND source = 'CLEANUP_CAMPAIGN'"` và `"AND applied_at >= :undoWindowStart"`.
    - `./gradlew :backend:core:compileJava :backend:core:compileTestJava` exits 0.
    - `./gradlew :backend:core:test --tests "*TriageAuditWriterCleanupArchiveTest*"` flips GREEN (Wave 0 stub from Plan 01 Task 1 — see H-3 update to Plan 01).
    - Existing triage tests still GREEN: `./gradlew :backend:core:test --tests "*TriageAudit*" --tests "*Triage*Audit*"` — no regressions because default `source=TRIAGE` preserves prior behavior at all existing call sites.
  </acceptance_criteria>
  <done>
    `CleanupAuditSource` enum + `TriageAuditEntity.source` field + `TriageAuditWriter.recordCleanupArchive(...)` + `TriageAuditRepository.findCleanupArchiveRowsForUndo(...)` all land. Wave 0 `TriageAuditWriterCleanupArchiveTest` GREEN. Existing triage audit code paths unchanged (default source=TRIAGE).
  </done>
</task>

</tasks>

<verification>
- 24+ file Java mới/modified (6 domain + 1 module info + 8 persistence + 1 converter + 5 exception + 1 new CleanupAuditSource enum + 3 modified triage files: TriageAuditEntity/Writer/Repository).
- `./gradlew :backend:core:compileJava :backend:core:compileTestJava` SUCCESS.
- `./gradlew :backend:core:test --tests "*ApplicationModulesTest*"` exits 0 (Modulith verify).
- `./gradlew :backend:core:test --tests "*CleanupModuleVerificationTest*"` exits 0 (Wave 0 stub flip GREEN).
- Liquibase migration boot + JPA metamodel match — no `SchemaManagementException`.
</verification>

<success_criteria>
- [ ] Spring Modulith `core.cleanup` declared với 7-item allowedDependencies (D-17).
- [ ] 4 enum + 1 policy class lands (CONVENTIONS §4 pattern strict).
- [ ] 4 entity (UnsubscribeCampaign / UnsubscribeAttempt / SenderSuppression / ProcessingJob) + 4 repository + 1 enum converter (`SuppressionReasonAttributeConverter`) ships.
- [ ] `UnsubscribeAttemptEntity.resetToPending()` method exists (M-2 sub-point — used by throttle deferral + retry).
- [ ] H-3 Path A artifacts: `CleanupAuditSource` enum + `TriageAuditEntity.source` field + `TriageAuditWriter.recordCleanupArchive(UUID, String, UUID, String, String)` (labelId is String — opaque Gmail label ID) + `TriageAuditRepository.findCleanupArchiveRowsForUndo(...)` all land.
- [ ] 5 business exception class ships với errorCode mapping cho controller exception handler.
- [ ] `CleanupModuleVerificationTest` (Wave 0) flips GREEN.
- [ ] Compile pass + JPA boots clean trên Postgres 17 Testcontainer.
</success_criteria>

<output>
Tạo `.planning/phases/08-bulk-unsubscribe-campaign/08-03-SUMMARY.md` khi done.
</output>
