---
phase: 08-bulk-unsubscribe-campaign
plan: 06
type: execute
wave: 5
depends_on: [08-03, 08-05]
files_modified:
  - backend/core/src/main/java/com/zeromail/core/cleanup/application/UnsubscribeDomainThrottle.java
  - backend/core/src/main/java/com/zeromail/core/cleanup/exception/ThrottleDeferredException.java
  - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java
  - backend/worker/src/main/java/com/zeromail/worker/cleanup/ProcessingJobWorker.java
  - backend/worker/src/main/java/com/zeromail/worker/cleanup/UnsubscribeCampaignHandler.java
  - backend/worker/src/main/java/com/zeromail/worker/scheduling/ProcessingJobReaperBatch.java
  - backend/worker/src/main/java/com/zeromail/worker/scheduling/ProcessingJobPurgeBatch.java
autonomous: true
requirements: [UNS-04]
must_haves:
  truths:
    - "`ProcessingJobWorker` virtual-thread continuous poll loop dùng SKIP LOCKED (D-02) — pickup `processing_job` row, dispatch theo job_type"
    - "`UnsubscribeCampaignHandler` xử lý per-sender atomic: unsubscribe (HTTP/mailto) → if OK + applyLabel + archive history; if FAILED → KHÔNG archive"
    - "`ProcessingJobReaperBatch` @Scheduled(fixedDelayString=60s) reset stale RUNNING (heartbeat > 5 min) về QUEUED + attempts++ (D-03)"
    - "`ProcessingJobPurgeBatch` @Scheduled(cron=`0 0 3 * * *`) daily 03:00 UTC purge processing_job sau 90d, batch ≤ 1000 (D-25)"
    - "`UnsubscribeDomainThrottle` Redis INCR + EXPIRE per-tenant per-domain (D-20): 1/60s + 10/h"
    - "Wave 0 tests `UnsubscribeCampaignE2ETest` + `UnsubscribeDomainThrottleTest` + `ProcessingJobReaperBatchTest` + `ProcessingJobPurgeBatchTest` flip GREEN"
  artifacts:
    - path: backend/core/src/main/java/com/zeromail/core/cleanup/application/UnsubscribeDomainThrottle.java
      provides: "Redis-backed per-tenant per-domain throttle (D-20) — Lettuce StringRedisTemplate"
    - path: backend/worker/src/main/java/com/zeromail/worker/cleanup/ProcessingJobWorker.java
      provides: "Virtual-thread SKIP LOCKED poll loop (D-02) — generic worker framework reusable cho SEED-009"
    - path: backend/worker/src/main/java/com/zeromail/worker/cleanup/UnsubscribeCampaignHandler.java
      provides: "Per-sender atomic execution: unsubscribe → label + archive history; FAILED → no archive"
    - path: backend/worker/src/main/java/com/zeromail/worker/scheduling/ProcessingJobReaperBatch.java
      provides: "Crash recovery (D-03) — stale RUNNING > 5 min → QUEUED + attempts++"
    - path: backend/worker/src/main/java/com/zeromail/worker/scheduling/ProcessingJobPurgeBatch.java
      provides: "Daily purge processing_job sau 90d (D-25); audit tables giữ vĩnh viễn"
  key_links:
    - from: backend/worker/src/main/java/com/zeromail/worker/cleanup/ProcessingJobWorker.java
      to: backend/worker/src/main/java/com/zeromail/worker/cleanup/UnsubscribeCampaignHandler.java
      via: "Worker dispatch theo job_type → UNSUBSCRIBE_CAMPAIGN routes to UnsubscribeCampaignHandler.handle(jobId)"
      pattern: "switch (jobType)"
    - from: backend/worker/src/main/java/com/zeromail/worker/cleanup/UnsubscribeCampaignHandler.java
      to: backend/core/src/main/java/com/zeromail/core/cleanup/usecases/UnsubscribeHttpClient.java
      via: "Per-sender call qua UnsubscribeHttpClient HOẶC UnsubscribeMailtoSender theo unsubscribe_method"

threat_model:
  trust_boundaries:
    - boundary: "Worker JVM → Postgres processing_job pickup"
      description: "SKIP LOCKED is intra-trust-zone (same tenant per row); race condition giữa workers handled bởi row-level lock."
    - boundary: "Worker → Redis throttle bucket"
      description: "Lettuce client untrusted-host scenario nếu Redis bị compromised → throttle bypass. Mitigation: Redis in-VPS only (CLAUDE.md self-host), no external exposure."

  threats:
    - id: T-08-06
      category: Denial of Service
      component: UnsubscribeDomainThrottle
      disposition: mitigate
      mitigation_plan: "D-20: per-tenant per-domain key prevents noisy-neighbor across tenants. Throttle exceeded → worker reschedules job (next_run_at += 60s) thay vì FAILED — không drop work."
    - id: T-08-07
      category: Tampering
      component: ProcessingJobWorker
      disposition: mitigate
      mitigation_plan: "SKIP LOCKED idempotent — row-level lock ngăn duplicate pickup. Heartbeat update mỗi step + reaper batch (D-03) phục hồi crash trong 5 phút."
    - id: T-08-08
      category: Information Disclosure
      component: All worker classes
      disposition: mitigate
      mitigation_plan: "Privacy log format CONVENTIONS §5: event=processing_job_picked tenantId={} jobId={} jobType={}; KHÔNG log payload (chứa campaignId only nhưng giữ habit)."
---

<objective>
Wave 4a: Build worker infrastructure. `ProcessingJobWorker` virtual-thread poll loop (D-02), `UnsubscribeCampaignHandler` per-sender atomic dispatch, `ProcessingJobReaperBatch` (D-03), `ProcessingJobPurgeBatch` (D-25), `UnsubscribeDomainThrottle` Redis (D-20).

Purpose: Async execution backbone cho Phase 8 + reusable SEED-009 framework. UNS-04 (execute job + throttle + per-sender atomic) contract. Wave 0 worker tests flip GREEN.
Output: 5 file Java mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-RESEARCH.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-PATTERNS.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
@backend/worker/src/main/java/com/zeromail/worker/billing/ShedLockConfig.java
@backend/worker/src/main/java/com/zeromail/worker/notification/DigestPendingReaperJob.java
@backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java
@backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java
@backend/core/src/main/java/com/zeromail/core/shared/lock/RedisDistributedLock.java
@backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java
@backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeCampaignPolicy.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: UnsubscribeDomainThrottle (Redis per-tenant per-domain, D-20)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/cleanup/application/UnsubscribeDomainThrottle.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/shared/lock/RedisDistributedLock.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeCampaignPolicy.java,
    .planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
  </read_first>
  <action>
    Tạo `UnsubscribeDomainThrottle` per D-20 (CONTEXT) + Wave 0 `UnsubscribeDomainThrottleTest` contract.

    Class outline:
    ```
    @Component
    public class UnsubscribeDomainThrottle {
        private static final Logger log = LoggerFactory.getLogger(UnsubscribeDomainThrottle.class);
        private static final String KEY_60S = "throttle:unsubscribe:domain:%s:%s:60s";
        private static final String KEY_1H = "throttle:unsubscribe:domain:%s:%s:1h";

        private final StringRedisTemplate stringRedisTemplate;
        private final Clock clock;

        public UnsubscribeDomainThrottle(StringRedisTemplate stringRedisTemplate, Clock clock) {
            this.stringRedisTemplate = stringRedisTemplate;
            this.clock = clock;
        }

        public boolean acquire(UUID tenantId, String senderDomain) {
            String key60s = KEY_60S.formatted(tenantId, senderDomain);
            String key1h = KEY_1H.formatted(tenantId, senderDomain);

            Long count60s = stringRedisTemplate.opsForValue().increment(key60s);
            if (count60s != null && count60s == 1L) {
                stringRedisTemplate.expire(key60s, UnsubscribeCampaignPolicy.THROTTLE_PER_DOMAIN_WINDOW_60S);
            }
            Long count1h = stringRedisTemplate.opsForValue().increment(key1h);
            if (count1h != null && count1h == 1L) {
                stringRedisTemplate.expire(key1h, UnsubscribeCampaignPolicy.THROTTLE_PER_DOMAIN_WINDOW_1H);
            }

            if (count60s != null && count60s > UnsubscribeCampaignPolicy.THROTTLE_MAX_PER_DOMAIN_60S) {
                log.info("event=cleanup_throttle_deferred tenantId={} senderDomain={} window=60s count={}", tenantId, senderDomain, count60s);
                rollback(key60s, key1h);
                return false;
            }
            if (count1h != null && count1h > UnsubscribeCampaignPolicy.THROTTLE_MAX_PER_DOMAIN_1H) {
                log.info("event=cleanup_throttle_deferred tenantId={} senderDomain={} window=1h count={}", tenantId, senderDomain, count1h);
                rollback(key60s, key1h);
                return false;
            }
            return true;
        }

        private void rollback(String key60s, String key1h) {
            stringRedisTemplate.opsForValue().decrement(key60s);
            stringRedisTemplate.opsForValue().decrement(key1h);
        }
    }
    ```

    Key format D-20 strict: `throttle:unsubscribe:domain:{tenantId}:{domain}:60s` và `:1h`. TTL: 60s + 3600s.

    Per-tenant scope (D-20): key chứa `{tenantId}` để tenant A không block tenant B.

    Rollback on deny: nếu acquire fail, decrement both keys để không double-count (consume budget cho call FAILED). Edge case: 2 worker race → có thể vài extra increment trước rollback; acceptable cho v1 (CONTEXT no Lua script requirement at this stage; defer to ops phase).

    Method `acquire` returns boolean. Worker reschedules job (`next_run_at += 60s`) nếu return false (handled trong UnsubscribeCampaignHandler Task 3).

    Naming: full identifier `tenantId`, `senderDomain`, `stringRedisTemplate`, `key60s`, `key1h`, `count60s`, `count1h`. KHÔNG `tpl/ctx/rt`.

    Privacy: log line chứa `senderDomain` only (đã domain-only, KHÔNG full email).

    **M-2 sibling artifact — `ThrottleDeferredException`** (placed in `backend/core/src/main/java/com/zeromail/core/cleanup/exception/ThrottleDeferredException.java`, NOT a private inner class — must be `public` so `ProcessingJobWorker.dispatchJob` in `backend/worker/...` can `catch (ThrottleDeferredException deferred)` explicitly per M-2 fix in Task 3):
    ```
    package com.zeromail.core.cleanup.exception;

    public final class ThrottleDeferredException extends RuntimeException {
        private final UUID tenantId;
        private final String senderDomain;
        private final String reason;

        public ThrottleDeferredException(UUID tenantId, String senderDomain, String reason) {
            super("Throttle deferred: tenantId=" + tenantId + " senderDomain=" + senderDomain + " reason=" + reason);
            this.tenantId = tenantId;
            this.senderDomain = senderDomain;
            this.reason = reason;
        }
        public UUID tenantId() { return tenantId; }
        public String senderDomain() { return senderDomain; }
        public String reason() { return reason; }
    }
    ```
    Reasons (canonical strings): `"PER_DOMAIN_60S_EXCEEDED"`, `"PER_DOMAIN_1H_EXCEEDED"`. Thrown by `UnsubscribeCampaignHandler.executeSingleAttempt` (Task 3) after `UnsubscribeDomainThrottle.acquire(...)` returns `false` and the handler has re-queued the `processing_job` row.
  </action>
  <verify>
    <automated>./gradlew :backend:worker:test --tests "*UnsubscribeDomainThrottleTest*"</automated>
  </verify>
  <acceptance_criteria>
    - File chứa `@Component` annotation.
    - File chứa exact literal `"throttle:unsubscribe:domain:%s:%s:60s"` và `"throttle:unsubscribe:domain:%s:%s:1h"` (D-20 key format).
    - File chứa `StringRedisTemplate` constructor injection.
    - File chứa `opsForValue().increment(...)` và `expire(...)` (Lettuce INCR + EXPIRE pattern).
    - File chứa `UnsubscribeCampaignPolicy.THROTTLE_MAX_PER_DOMAIN_60S` và `THROTTLE_MAX_PER_DOMAIN_1H` references.
    - File chứa rollback method (decrement on deny).
    - File chứa `event=cleanup_throttle_deferred` log line.
    - `./gradlew :backend:worker:test --tests "*UnsubscribeDomainThrottleTest*"` exits 0 — 4 test method từ Wave 0:
      - `firstCallInWindow_returnsAllowed`.
      - `secondCallWithin60s_returnsDenied`.
      - `eleventhCallWithin1h_returnsDenied_evenIfSpaced` (loop 11 với Clock.advance).
      - `differentTenants_doNotShareBucket` (D-20 per-tenant verify).
  </acceptance_criteria>
  <done>
    UnsubscribeDomainThrottle ships với D-20 per-tenant per-domain semantics. Wave 0 throttle test GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: ProcessingJobReaperBatch (D-03) + ProcessingJobPurgeBatch (D-25)</name>
  <files>
    backend/worker/src/main/java/com/zeromail/worker/scheduling/ProcessingJobReaperBatch.java,
    backend/worker/src/main/java/com/zeromail/worker/scheduling/ProcessingJobPurgeBatch.java
  </files>
  <read_first>
    backend/worker/src/main/java/com/zeromail/worker/notification/DigestPendingReaperJob.java,
    backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeJob.java,
    backend/worker/src/main/java/com/zeromail/worker/triage/TriageAuditPurgeBatch.java,
    backend/worker/src/main/java/com/zeromail/worker/billing/ShedLockConfig.java,
    .planning/phases/08-bulk-unsubscribe-campaign/08-PATTERNS.md
  </read_first>
  <action>
    Tạo 2 scheduled batch class trong `worker/scheduling/` package (D-18 placement — generic, KHÔNG trong `worker/cleanup/` vì reusable SEED-009).

    1. **`ProcessingJobReaperBatch`** (D-03 crash recovery, analog `DigestPendingReaperJob.java`):
       ```
       @Component
       public class ProcessingJobReaperBatch {
           static final String LOCK_NAME = "processingJobReaper";
           private static final Logger log = LoggerFactory.getLogger(ProcessingJobReaperBatch.class);
           private static final Duration STALE_HEARTBEAT_GRACE = Duration.ofMinutes(5);

           private final JdbcTemplate jdbcTemplate;
           private final Clock clock;

           public ProcessingJobReaperBatch(JdbcTemplate jdbcTemplate, Clock clock) {
               this.jdbcTemplate = jdbcTemplate;
               this.clock = clock;
           }

           @Scheduled(fixedDelayString = "PT60S")
           @SchedulerLock(name = LOCK_NAME, lockAtLeastFor = "PT30S", lockAtMostFor = "PT5M")
           public void scheduledReap() {
               LockAssert.assertLocked();
               reap();
           }

           public int reap() {
               Instant cutoff = clock.instant().minus(STALE_HEARTBEAT_GRACE);
               int reapedCount = jdbcTemplate.update(
                   "UPDATE processing_job SET status='QUEUED', attempts = attempts + 1, heartbeat_at=NULL, updated_at=NOW(), next_run_at=NOW() " +
                   "WHERE status='RUNNING' AND heartbeat_at < ?",
                   Timestamp.from(cutoff)
               );
               log.info("event=processing_job_reaper_reaped count={}", reapedCount);
               return reapedCount;
           }
       }
       ```

    2. **`ProcessingJobPurgeBatch`** (D-25 daily purge, analog `TriageAuditPurgeJob` + `TriageAuditPurgeBatch` split-pattern). Trong cùng 1 file (đơn giản hơn split — D-25 ghi 1 file `ProcessingJobPurgeBatch.java`; planner chọn single-file để giảm file count):
       ```
       @Component
       public class ProcessingJobPurgeBatch {
           static final String LOCK_NAME = "processingJobPurge";
           private static final Logger log = LoggerFactory.getLogger(ProcessingJobPurgeBatch.class);
           private static final Duration PROCESSING_JOB_RETENTION = Duration.ofDays(90);
           private static final int BATCH_LIMIT = 1_000;

           private final JdbcTemplate jdbcTemplate;
           private final Clock clock;

           public ProcessingJobPurgeBatch(JdbcTemplate jdbcTemplate, Clock clock) {
               this.jdbcTemplate = jdbcTemplate;
               this.clock = clock;
           }

           @Scheduled(cron = "0 0 3 * * *", zone = "UTC")
           @SchedulerLock(name = LOCK_NAME, lockAtLeastFor = "PT1M", lockAtMostFor = "PT30M")
           public void scheduledPurge() {
               LockAssert.assertLocked();
               purge();
           }

           public int purge() {
               Instant cutoff = clock.instant().minus(PROCESSING_JOB_RETENTION);
               int totalDeleted = 0;
               int selectedCount;
               do {
                   PurgeResult result = purgeExpiredOnce(cutoff);
                   selectedCount = result.selectedCount();
                   totalDeleted += result.deletedCount();
               } while (selectedCount == BATCH_LIMIT);
               log.info("event=processing_job_purged totalDeleted={}", totalDeleted);
               return totalDeleted;
           }

           @Transactional
           public PurgeResult purgeExpiredOnce(Instant cutoff) {
               // CTE delete pattern mirror TriageAuditPurgeBatch.purgeExpiredOnce
               return jdbcTemplate.queryForObject(
                   "WITH expired_jobs AS (" +
                   "  SELECT id FROM processing_job " +
                   "  WHERE finished_at < ? AND status IN ('COMPLETED','FAILED') " +
                   "  ORDER BY finished_at ASC, id ASC " +
                   "  LIMIT ? FOR UPDATE SKIP LOCKED" +
                   "), deleted_jobs AS (" +
                   "  DELETE FROM processing_job WHERE id IN (SELECT id FROM expired_jobs) RETURNING id" +
                   ") SELECT " +
                   "  (SELECT COUNT(*) FROM expired_jobs) AS selected_count, " +
                   "  (SELECT COUNT(*) FROM deleted_jobs) AS deleted_count",
                   (rs, rowNum) -> new PurgeResult(rs.getInt("selected_count"), rs.getInt("deleted_count")),
                   Timestamp.from(cutoff), BATCH_LIMIT
               );
           }

           public record PurgeResult(int selectedCount, int deletedCount) {}
       }
       ```

    D-25 invariant — KHÔNG purge `unsubscribe_campaign` HOẶC `unsubscribe_attempt` (audit retain mãi mãi). Comment trong file rõ ràng:
    ```
    // D-25: processing_job retention = 90 days (status COMPLETED|FAILED).
    // Audit trail unsubscribe_campaign + unsubscribe_attempt KEEP FOREVER for support + analytics retro.
    ```

    `ShedLock` `@SchedulerLock` requires `ShedLockConfig` (đã có trong project per `backend/worker/billing/ShedLockConfig.java`). Reaper + Purge dùng existing lock table (`shedlock`).

    Naming: `STALE_HEARTBEAT_GRACE`, `PROCESSING_JOB_RETENTION`, `BATCH_LIMIT`, `reapedCount`, `totalDeleted`, `selectedCount`, `cutoff`. KHÔNG `n/cnt/svc`.
  </action>
  <verify>
    <automated>./gradlew :backend:worker:test --tests "*ProcessingJobReaperBatchTest*" --tests "*ProcessingJobPurgeBatchTest*"</automated>
  </verify>
  <acceptance_criteria>
    - 2 file lands trong `backend/worker/src/main/java/com/zeromail/worker/scheduling/` (D-18 placement).
    - `ProcessingJobReaperBatch.java` chứa `@Scheduled(fixedDelayString = "PT60S")` (D-03).
    - File chứa `Duration.ofMinutes(5)` (D-03 stale heartbeat threshold).
    - File chứa SQL literal `"status='RUNNING' AND heartbeat_at <"` (reaper query D-03).
    - File chứa log line `event=processing_job_reaper_reaped`.
    - `ProcessingJobPurgeBatch.java` chứa `@Scheduled(cron = "0 0 3 * * *", zone = "UTC")` (D-25 — daily 03:00 UTC).
    - File chứa `Duration.ofDays(90)` (D-25 retention).
    - File chứa `BATCH_LIMIT = 1_000` (D-25).
    - File chứa SQL literal `"status IN ('COMPLETED','FAILED')"` (D-25 — only purge terminal).
    - File chứa SQL literal `FOR UPDATE SKIP LOCKED` (concurrent-safe).
    - File CHỨA comment line "Audit trail unsubscribe_campaign + unsubscribe_attempt KEEP FOREVER" (D-25 invariant comment).
    - `./gradlew :backend:worker:test --tests "*ProcessingJobReaperBatchTest*"` exits 0 — 3 test method từ Wave 0.
    - `./gradlew :backend:worker:test --tests "*ProcessingJobPurgeBatchTest*"` exits 0 — verifies terminal jobs older than 90d are purged, fresh/non-terminal jobs are retained, batch limit is 1000, and audit tables are not deleted.
    - Both classes có `@SchedulerLock(name = ...)` (ShedLock prevent multi-instance double-fire).
  </acceptance_criteria>
  <done>
    Reaper + Purge batch ships ở `worker/scheduling/`. D-03 + D-25 + D-18 placement correct. Wave 0 reaper + purge tests GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: ProcessingJobWorker + UnsubscribeCampaignHandler (D-01 + D-02 + D-04 + per-sender atomic)</name>
  <files>
    backend/worker/src/main/java/com/zeromail/worker/cleanup/ProcessingJobWorker.java,
    backend/worker/src/main/java/com/zeromail/worker/cleanup/UnsubscribeCampaignHandler.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/ProcessingJobRepository.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeCampaignRepository.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeAttemptRepository.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/persistence/UnsubscribeAttemptEntity.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/UnsubscribeHttpClient.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/UnsubscribeMailtoSender.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/usecases/UnsubscribeResult.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/application/UnsubscribeDomainThrottle.java,
    backend/core/src/main/java/com/zeromail/core/cleanup/domain/UnsubscribeCampaignPolicy.java,
    backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java,
    backend/core/src/main/java/com/zeromail/core/shared/tenant/TenantContext.java,
    .planning/phases/08-bulk-unsubscribe-campaign/08-RESEARCH.md
  </read_first>
  <action>
    Tạo 2 file:

    1. **`ProcessingJobWorker`** (D-02 — virtual-thread continuous-poll loop):
       Outline per RESEARCH §"Worker poll loop pseudocode":
       ```
       @Component
       public class ProcessingJobWorker {
           private static final Logger log = LoggerFactory.getLogger(ProcessingJobWorker.class);
           private static final Duration POLL_INTERVAL = Duration.ofSeconds(2);

           private final JdbcTemplate jdbcTemplate;
           private final TransactionTemplate transactionTemplate;
           private final UnsubscribeCampaignHandler unsubscribeCampaignHandler;
           private final Clock clock;
           private volatile boolean shouldRun = true;
           private Thread pollLoopThread;

           public ProcessingJobWorker(JdbcTemplate jdbcTemplate,
                                      PlatformTransactionManager transactionManager,
                                      UnsubscribeCampaignHandler unsubscribeCampaignHandler,
                                      Clock clock) {
               this.jdbcTemplate = jdbcTemplate;
               this.transactionTemplate = new TransactionTemplate(transactionManager);
               this.unsubscribeCampaignHandler = unsubscribeCampaignHandler;
               this.clock = clock;
           }

           @PostConstruct
           void startPolling() {
               this.pollLoopThread = Thread.ofVirtual().name("processing-job-worker").start(this::pollLoop);
           }

           @PreDestroy
           void stopPolling() throws InterruptedException {
               shouldRun = false;
               if (pollLoopThread != null) pollLoopThread.join(Duration.ofSeconds(5).toMillis());
           }

           private void pollLoop() {
               while (shouldRun) {
                   try {
                       Optional<ClaimedJob> claimed = transactionTemplate.execute(status -> claimNextJob());
                       if (claimed.isEmpty()) {
                           Thread.sleep(POLL_INTERVAL);
                           continue;
                       }
                       dispatchJob(claimed.get());
                   } catch (InterruptedException interrupted) {
                       Thread.currentThread().interrupt();
                       return;
                   } catch (RuntimeException unexpected) {
                       log.error("event=processing_job_worker_error", unexpected);
                       try { Thread.sleep(POLL_INTERVAL); } catch (InterruptedException ignored) { return; }
                   }
               }
           }

           private Optional<ClaimedJob> claimNextJob() {
               // SELECT ... FOR UPDATE SKIP LOCKED + UPDATE to RUNNING + heartbeat_at
               return jdbcTemplate.query(
                   "SELECT id, tenant_id, job_type, payload FROM processing_job " +
                   "WHERE status = 'QUEUED' AND next_run_at <= NOW() " +
                   "ORDER BY created_at FOR UPDATE SKIP LOCKED LIMIT 1",
                   resultSet -> {
                       if (!resultSet.next()) return Optional.<ClaimedJob>empty();
                       UUID id = (UUID) resultSet.getObject("id");
                       UUID tenantId = (UUID) resultSet.getObject("tenant_id");
                       String jobType = resultSet.getString("job_type");
                       String payload = resultSet.getString("payload");
                       jdbcTemplate.update(
                           "UPDATE processing_job SET status='RUNNING', started_at=NOW(), heartbeat_at=NOW(), updated_at=NOW() WHERE id = ?",
                           id);
                       log.info("event=processing_job_picked tenantId={} jobId={} jobType={}", tenantId, id, jobType);
                       return Optional.of(new ClaimedJob(id, tenantId, jobType, payload));
                   }
               );
           }

           private void dispatchJob(ClaimedJob claimed) {
               try {
                   ScopedValue.where(TenantContext.TENANT, claimed.tenantId().toString())
                              .run(() -> {
                                   switch (claimed.jobType()) {
                                       case "UNSUBSCRIBE_CAMPAIGN" -> unsubscribeCampaignHandler.handle(claimed.id(), claimed.tenantId(), claimed.payload());
                                       default -> throw new IllegalStateException("Unknown job_type: " + claimed.jobType());
                                   }
                              });
                   markCompleted(claimed.id());
               } catch (ThrottleDeferredException deferred) {
                   // M-2 fix: handler already re-queued the job via processing_job UPDATE (status='QUEUED', next_run_at = NOW() + 60s).
                   // DO NOT markFailed — that would overwrite the QUEUED reschedule the handler just performed.
                   log.info("event=processing_job_throttle_deferred tenantId={} jobId={} senderDomain={} reason={}",
                       deferred.tenantId(), claimed.id(), deferred.senderDomain(), deferred.reason());
                   // No markCompleted either — job stays QUEUED until next poll cycle.
               } catch (RuntimeException handlerFailure) {
                   log.error("event=processing_job_handler_failed jobId={}", claimed.id(), handlerFailure);
                   markFailed(claimed.id(), handlerFailure.getClass().getSimpleName());
               }
           }

           private void markCompleted(UUID jobId) {
               jdbcTemplate.update("UPDATE processing_job SET status='COMPLETED', finished_at=NOW(), updated_at=NOW() WHERE id = ?", jobId);
           }

           private void markFailed(UUID jobId, String failureReason) {
               jdbcTemplate.update("UPDATE processing_job SET status='FAILED', finished_at=NOW(), failure_reason=?, updated_at=NOW() WHERE id = ?", failureReason, jobId);
           }

           private record ClaimedJob(UUID id, UUID tenantId, String jobType, String payload) {}
       }
       ```

       Use `ScopedValue` binding to `TenantContext.TENANT` (Phase 1 — existing scoped value infra). Handler reads `TenantContext.currentTenantUuid()` inside dispatched logic.

    2. **`UnsubscribeCampaignHandler`** (UNS-04, per-sender atomic):
       Outline:
       ```
       @Component
       public class UnsubscribeCampaignHandler {
           private static final Logger log = LoggerFactory.getLogger(UnsubscribeCampaignHandler.class);

           private final UnsubscribeCampaignRepository unsubscribeCampaignRepository;
           private final UnsubscribeAttemptRepository unsubscribeAttemptRepository;
           private final UnsubscribeHttpClient unsubscribeHttpClient;
           private final UnsubscribeMailtoSender unsubscribeMailtoSender;
           private final UnsubscribeDomainThrottle unsubscribeDomainThrottle;
           private final TriageGmailWriter triageGmailWriter;
           private final TriageAuditWriter triageAuditWriter;
           private final JdbcTemplate jdbcTemplate;
           private final ObjectMapper objectMapper;
           private final Clock clock;

           // constructor inject all above

           public void handle(UUID jobId, UUID tenantId, String payloadJson) {
               UnsubscribeCampaignPayload payload = parsePayload(payloadJson);  // {"campaignId": uuid, "schemaVersion": 1}
               UnsubscribeCampaignEntity campaign = unsubscribeCampaignRepository.findByIdAndTenantId(payload.campaignId(), tenantId)
                   .orElseThrow(() -> new IllegalStateException("Campaign not found: " + payload.campaignId()));
               campaign.markRunning() ... // set status RUNNING in campaign aggregate

               List<UnsubscribeAttemptEntity> attempts = unsubscribeAttemptRepository.findByCampaignIdOrderBySenderEmailAsc(campaign.getId());
               for (UnsubscribeAttemptEntity attempt : attempts) {
                   if (attempt.getState() != UnsubscribeAttemptState.PENDING) continue;  // skip already-processed (retry path)
                   executeSingleAttempt(jobId, tenantId, attempt);
                   updateHeartbeat(jobId);
               }
               campaign.markCompleted(clock.instant());
               unsubscribeCampaignRepository.save(campaign);
               log.info("event=cleanup_campaign_completed tenantId={} campaignId={} jobId={}", tenantId, campaign.getId(), jobId);
           }

           private void executeSingleAttempt(UUID jobId, UUID tenantId, UnsubscribeAttemptEntity attempt) {
               attempt.markRunning(clock.instant());
               unsubscribeAttemptRepository.save(attempt);

               // Throttle check — if denied, reschedule and skip
               if (!unsubscribeDomainThrottle.acquire(tenantId, attempt.getSenderDomain())) {
                   // Reschedule entire job 60s later — keeps attempt as PENDING (revert)
                   attempt.resetToPending();  // helper method on entity
                   unsubscribeAttemptRepository.save(attempt);
                   jdbcTemplate.update("UPDATE processing_job SET status='QUEUED', next_run_at = NOW() + INTERVAL '60 seconds', updated_at=NOW() WHERE id = ?", jobId);
                   throw new ThrottleDeferredException(tenantId, attempt.getSenderDomain(), "PER_DOMAIN_60S_EXCEEDED");  // M-2: caught by worker explicitly → job stays QUEUED, no completion/failure mark
               }

               // 1. Unsubscribe step
               UnsubscribeResult result = switch (attempt.getUnsubscribeMethod()) {
                   case ONE_CLICK -> {
                       URI url = lookupPersistedHttpsUrl(tenantId, attempt.getSenderEmail());
                       yield unsubscribeHttpClient.postOneClick(tenantId, url);
                   }
                   case MAILTO -> {
                       String rawMailto = lookupPersistedMailtoValue(tenantId, attempt.getSenderEmail());
                       String parsedRecipient = UnsubscribeMailtoUriParser.parse(rawMailto).recipient();
                       yield unsubscribeMailtoSender.sendUnsubscribeMailto(tenantId, rawMailto, parsedRecipient);
                   }
                   case NONE -> UnsubscribeResult.failed("NO_HEADER");  // shouldn't happen (filtered at preview)
               };

               // 2. Per-sender atomic gate
               if (result instanceof UnsubscribeResult.Failed failed) {
                   attempt.markFailed(failed.failureReason(), clock.instant());
                   unsubscribeAttemptRepository.save(attempt);
                   log.info("event=cleanup_campaign_step_failed tenantId={} senderDomain={} failureReason={}", tenantId, attempt.getSenderDomain(), failed.failureReason());
                   return;
               }

               // 3. Apply label + archive history (only if unsubscribe OK)
               int archivedCount = applyLabelAndArchiveHistory(tenantId, attempt);
               attempt.markOk(archivedCount, clock.instant());
               unsubscribeAttemptRepository.save(attempt);
               log.info("event=cleanup_campaign_step_ok tenantId={} senderDomain={} archivedCount={}", tenantId, attempt.getSenderDomain(), archivedCount);
           }

           private URI lookupPersistedHttpsUrl(UUID tenantId, String senderEmail) {
               // SELECT distinct list_unsubscribe_url FROM mail_message_observed WHERE tenant_id=? AND sender_email=? AND list_unsubscribe_url IS NOT NULL LIMIT 1
               // D-11 provenance check: only URL persisted in mail_message_observed accepted
               String url = jdbcTemplate.queryForObject(
                   "SELECT list_unsubscribe_url FROM mail_message_observed WHERE tenant_id=? AND sender_email=? AND list_unsubscribe_url IS NOT NULL LIMIT 1",
                   String.class, tenantId, senderEmail);
               return URI.create(url);
           }

           private String lookupPersistedMailtoValue(UUID tenantId, String senderEmail) {
               return jdbcTemplate.queryForObject(
                   "SELECT list_unsubscribe_mailto FROM mail_message_observed WHERE tenant_id=? AND sender_email=? AND list_unsubscribe_mailto IS NOT NULL LIMIT 1",
                   String.class, tenantId, senderEmail);
           }

           private int applyLabelAndArchiveHistory(UUID tenantId, UnsubscribeAttemptEntity attempt) {
               // 1. Resolve or create label "Zero Mail/Unsubscribed" via NEW TriageGmailWriter.ensureLabelExists
               //    Signature: public String ensureLabelExists(UUID tenantId, String labelName) throws IOException
               //    Returns the Gmail-side label ID (opaque string), captured here so recordCleanupArchive can persist it.
               String labelId = triageGmailWriter.ensureLabelExists(tenantId, UnsubscribeCampaignPolicy.UNSUBSCRIBED_LABEL_NAME);
               String senderEmail = attempt.getSenderEmail();
               // 2. List history Gmail message IDs from this sender (use existing GmailHistoryListReadService or direct Gmail API)
               List<String> messageIds = listHistoryMessageIds(tenantId, senderEmail);  // helper — delegate to existing
               int count = 0;
               for (String gmailMessageId : messageIds) {
                   triageGmailWriter.applyLabel(tenantId, gmailMessageId, labelId);
                   triageGmailWriter.archiveSkipInbox(tenantId, gmailMessageId);
                   triageAuditWriter.recordCleanupArchive(tenantId, gmailMessageId, attempt.getId(), labelId, attempt.getSenderEmail());
                   count++;
               }
               return count;
           }

           private void updateHeartbeat(UUID jobId) {
               jdbcTemplate.update("UPDATE processing_job SET heartbeat_at = NOW(), updated_at = NOW() WHERE id = ?", jobId);
           }

           private UnsubscribeCampaignPayload parsePayload(String json) {
               try { return objectMapper.readValue(json, UnsubscribeCampaignPayload.class); }
               catch (IOException ioFailure) { throw new IllegalStateException("Malformed campaign payload", ioFailure); }
           }

           private record UnsubscribeCampaignPayload(UUID campaignId, int schemaVersion) {}  // D-19
           // ThrottleDeferredException is imported from core.cleanup.exception (see Task 2 of this plan, lines 173-191)
           // — do NOT redeclare as inner class. The handler throws the top-level 3-arg ctor; this catch binds to it.
       }
       ```

    **EXPLICIT METHOD CONTRACTS (H-2 + H-3 resolutions):**

    1. **`TriageGmailWriter.ensureLabelExists` — NEW method on existing class** (verified absent in current source — only `applyLabel`/`archiveSkipInbox`/`saveDraft`/`removeLabel`/`restoreToInbox` exist today):
       ```
       public String ensureLabelExists(UUID tenantId, String labelName) throws IOException
       ```
       Implementation: delegates to the existing private `resolveOrCreateLabelId(Gmail gmail, String labelName)` helper that `applyLabel` already uses internally — idempotent resolve-or-create against `gmail.users().labels().list()` + `labels().create()` if absent. Returns the **opaque Gmail-side label ID** as a `String` (matches Gmail API surface — e.g., `"Label_42"`). Throws `GmailLabelException` on Gmail API failure. Spring Modulith allowedDependencies (D-17) already includes `triage` from `cleanup`, so cross-module call is OK; no module change.

       **Type alignment with `recordCleanupArchive`**: `recordCleanupArchive` accepts `String labelId` (NOT `UUID`) — matches this return type and the existing `gmail_change_token` JSONB convention (`{"appliedLabelIds":["Label_42"]}`). Plan 03 Task 4 signature is the authoritative reference; this Plan 06 Task 3 call site passes the `String` returned by `ensureLabelExists` directly through.

    2. **`TriageAuditWriter.recordCleanupArchive` — NEW method** (defined in Plan 03 Task 4 per H-3 Path A). Signature (authoritative, see resolution above):
       ```
       public void recordCleanupArchive(
               UUID tenantId,
               String gmailMessageId,
               UUID campaignAttemptId,
               String labelId,
               String senderEmail)
       ```
       Inserts `triage_audit` row with `source=CleanupAuditSource.CLEANUP_CAMPAIGN` (changelog 046), `actionType='ARCHIVE'`, `decision='APPLIED'`, `externalRef=campaignAttemptId.toString()`, `gmailChangeToken='{"appliedLabelIds":["<labelId>"]}'`, `sanitizedSenderEmail=senderEmail`. Plan 07 Task 3 undo query reads `(gmail_message_id, sanitized_sender_email)` filtered by `source='CLEANUP_CAMPAIGN'`.

    3. **`listHistoryMessageIds(tenantId, senderEmail)` — local private helper** (not a cross-class method): query `mail_message_observed` for cleanup retro:
       ```sql
       SELECT gmail_message_id FROM mail_message_observed
       WHERE tenant_id = ? AND sender_email = ?
         AND ('INBOX' = ANY(label_ids) OR cardinality(label_ids) = 0)
       ```
       Returns the list of Gmail message IDs to apply-label-and-archive in step 3 of `executeSingleAttempt`. Simpler than Gmail API roundtrip (data already ingested by Phase 2A).

    Payload D-19: `{"campaignId": "uuid", "schemaVersion": 1}` exact format.

    Privacy: log lines KHÔNG chứa raw URL, mailto, full senderEmail. Chỉ `senderDomain` + `tenantId` + `campaignId` + `jobId` + counts.
  </action>
  <verify>
    <automated>./gradlew :backend:worker:test --tests "*UnsubscribeCampaignE2ETest*"</automated>
  </verify>
  <acceptance_criteria>
    - File `ProcessingJobWorker.java` chứa `Thread.ofVirtual()` (virtual-thread continuous poll D-02).
    - File chứa SQL literal `FOR UPDATE SKIP LOCKED LIMIT 1` (D-02 row-lock pickup).
    - File chứa `ScopedValue.where(TenantContext.TENANT` (TenantContext binding).
    - File chứa `switch (claimed.jobType())` HOẶC `switch (jobType)` (dispatch by job_type).
    - File chứa literal `"UNSUBSCRIBE_CAMPAIGN"` case branch.
    - File `UnsubscribeCampaignHandler.java` chứa `parsePayload` method với `UnsubscribeCampaignPayload(UUID campaignId, int schemaVersion)` record (D-19).
    - File chứa `unsubscribeDomainThrottle.acquire(tenantId, attempt.getSenderDomain())` invocation.
    - File chứa logic per-sender atomic — `if (result instanceof UnsubscribeResult.Failed)` followed by `return` (KHÔNG archive nếu fail).
    - File chứa SQL literal `"SELECT list_unsubscribe_url FROM mail_message_observed"` (D-11 provenance check — chỉ URL persisted accepted).
    - File chứa `UnsubscribeCampaignPolicy.UNSUBSCRIBED_LABEL_NAME` (literal "Zero Mail/Unsubscribed").
    - File chứa log line `event=cleanup_campaign_step_ok` và `event=cleanup_campaign_step_failed`.
    - Privacy: `grep -nE 'log\.(info|warn).*url=|senderEmail[^.]' backend/worker/src/main/java/com/zeromail/worker/cleanup/*.java` trả 0 (no raw URL/email in log).
    - `./gradlew :backend:worker:test --tests "*UnsubscribeCampaignE2ETest*"` exits 0 — Wave 0 E2E:
      - 3 sender fixture: 2 one-click OK + 1 mailto FAILED.
      - Assert campaign.status == COMPLETED.
      - Assert sender 1+2 attempt state == OK + archive_count > 0.
      - Assert sender 3 attempt state == FAILED + archive_count == 0 (per-sender atomic).
    - **H-2 explicit signature checks** — exact-string greps on the source file:
      - `grep -c "public String ensureLabelExists(UUID tenantId, String labelName)" backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java` = 1 (new method shipped per H-2; return type `String` = opaque Gmail label ID, matches Plan 03 Task 4 `recordCleanupArchive` `labelId String` parameter).
      - `grep -c "triageAuditWriter.recordCleanupArchive(tenantId, gmailMessageId, attempt.getId(), labelId, attempt.getSenderEmail())" backend/worker/src/main/java/com/zeromail/worker/cleanup/UnsubscribeCampaignHandler.java` = 1 (explicit call site per H-3).
    - **M-2 deferral check**:
      - `grep -c "catch (ThrottleDeferredException deferred)" backend/worker/src/main/java/com/zeromail/worker/cleanup/ProcessingJobWorker.java` = 1.
      - `grep -c "event=processing_job_throttle_deferred" backend/worker/src/main/java/com/zeromail/worker/cleanup/ProcessingJobWorker.java` = 1.
      - `./gradlew :backend:worker:test --tests "*ProcessingJobWorkerThrottleDeferralTest*"` exits 0 — Wave 0 stub asserts deferred path leaves `processing_job.status='QUEUED'` (NOT FAILED).
    - **ThrottleDeferredException class** lands as a top-level `public final class` in `core.cleanup.exception` (NOT a private inner class) — required because the worker module catches it.
  </acceptance_criteria>
  <done>
    ProcessingJobWorker + UnsubscribeCampaignHandler ships. UNS-04 per-sender atomic + D-19 payload + D-11 provenance check + privacy. Wave 0 E2E test GREEN.
  </done>
</task>

</tasks>

<verification>
- 7 file Java lands/modified: 5 new (UnsubscribeDomainThrottle, ProcessingJobWorker, UnsubscribeCampaignHandler, ProcessingJobReaperBatch, ProcessingJobPurgeBatch) + 1 new exception (ThrottleDeferredException) + 1 modified (TriageGmailWriter — adds ensureLabelExists per H-2).
- Wave 0 tests flip GREEN: `UnsubscribeCampaignE2ETest` + `UnsubscribeDomainThrottleTest` + `ProcessingJobReaperBatchTest` + `ProcessingJobPurgeBatchTest` + `ProcessingJobWorkerThrottleDeferralTest` (new — M-2).
- D-02 SKIP LOCKED poll loop + D-03 reaper + D-04 transactional + D-19 payload + D-20 throttle + D-25 purge all enforced.
- Privacy invariant — log line không chứa raw URL/email/mailto.
- Threats T-08-06..T-08-08 mitigated.
</verification>

<success_criteria>
- [ ] `UnsubscribeDomainThrottle` Redis INCR + EXPIRE per-tenant per-domain — Wave 0 test GREEN.
- [ ] `ProcessingJobReaperBatch` + `ProcessingJobPurgeBatch` trong `worker/scheduling/` (D-18 placement).
- [ ] `ProcessingJobWorker` virtual-thread + SKIP LOCKED + ScopedValue tenant binding.
- [ ] `UnsubscribeCampaignHandler` per-sender atomic + D-11 provenance check + reuse `TriageGmailWriter` (with NEW `ensureLabelExists` method per H-2).
- [ ] `ThrottleDeferredException` is a top-level `public final class` in `core.cleanup.exception`; `ProcessingJobWorker.dispatchJob` catches it explicitly BEFORE the generic RuntimeException catch (M-2 fix). Deferred path leaves `processing_job.status=QUEUED`.
- [ ] `triageAuditWriter.recordCleanupArchive(tenantId, gmailMessageId, attempt.getId(), labelId, attempt.getSenderEmail())` call site is explicit (no `...` ellipsis) per H-3.
- [ ] Audit trail KHÔNG bị purge (D-25 comment invariant).
</success_criteria>

<output>
Tạo `.planning/phases/08-bulk-unsubscribe-campaign/08-06-SUMMARY.md` khi done.
</output>
