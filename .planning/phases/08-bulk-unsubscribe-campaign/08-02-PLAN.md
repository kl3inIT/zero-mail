---
phase: 08-bulk-unsubscribe-campaign
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - backend/core/src/main/resources/db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml
  - backend/core/src/main/resources/db/changelog/changes/042-processing-job.yaml
  - backend/core/src/main/resources/db/changelog/changes/043-sender-suppression.yaml
  - backend/core/src/main/resources/db/changelog/changes/044-unsubscribe-campaign.yaml
  - backend/core/src/main/resources/db/changelog/changes/045-unsubscribe-attempt.yaml
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
autonomous: true
requirements: [UNS-01, UNS-02, UNS-04, UNS-07]
must_haves:
  truths:
    - "5 schema migration files (041..045) tồn tại trong `backend/core/src/main/resources/db/changelog/changes/`"
    - "Liquibase apply forward + rollback xanh trên Postgres 17 Testcontainer"
    - "`mail_message_observed` extend 3 column NULL/false (forward-only D-10)"
    - "Mọi CHECK constraint cho enum đã enforce (status / job_type / state / unsubscribe_method / reason)"
    - "Index `idx_processing_job_pickup` + partial index `idx_processing_job_running_heartbeat` exist cho SKIP LOCKED + reaper queries"
  artifacts:
    - path: backend/core/src/main/resources/db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml
      provides: "D-09 ADD COLUMN list_unsubscribe_url + list_unsubscribe_mailto + list_unsubscribe_one_click"
      contains: "list_unsubscribe_url"
    - path: backend/core/src/main/resources/db/changelog/changes/042-processing-job.yaml
      provides: "D-01 generic worker outbox table"
      contains: "ck_processing_job_status"
    - path: backend/core/src/main/resources/db/changelog/changes/043-sender-suppression.yaml
      provides: "D-09 sender_suppression với XOR CHECK constraint"
      contains: "ck_sender_suppression_one_target"
    - path: backend/core/src/main/resources/db/changelog/changes/044-unsubscribe-campaign.yaml
      provides: "Campaign aggregate root với FK→processing_job (D-19)"
      contains: "unsubscribe_campaign"
    - path: backend/core/src/main/resources/db/changelog/changes/045-unsubscribe-attempt.yaml
      provides: "Per-sender attempt rows với FK→unsubscribe_campaign delete CASCADE"
      contains: "unsubscribe_attempt"
  key_links:
    - from: backend/core/src/main/resources/db/changelog/changes/045-unsubscribe-attempt.yaml
      to: backend/core/src/main/resources/db/changelog/changes/044-unsubscribe-campaign.yaml
      via: "Foreign key campaign_id REFERENCES unsubscribe_campaign(id) ON DELETE CASCADE"
      pattern: "foreignKeyName: fk_unsubscribe_attempt_campaign"
    - from: backend/core/src/main/resources/db/changelog/changes/044-unsubscribe-campaign.yaml
      to: backend/core/src/main/resources/db/changelog/changes/042-processing-job.yaml
      via: "Foreign key job_id REFERENCES processing_job(id) — D-19"
      pattern: "foreignKeyName: fk_unsubscribe_campaign_job"
---

<objective>
Wave 1a: Tạo 5 Liquibase YAML changelog `041..045` (forward-only, đã verified next-free là 041 trong PATTERNS.md numbering correction). Đăng ký vào `db.changelog-master.yaml`. Mỗi changelog có `rollback:` block — `LiquibaseRollbackTest` (project-wide pattern) phải pass.

Purpose: Schema foundation cho cả phase. Wave 1b (entity) + Wave 2 (read service) + Wave 4 (worker) đều cần schema đã land. Forward-only backfill (D-10) — rows cũ giữ NULL.
Output: 5 file YAML + 1 file modify (`db.changelog-master.yaml`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-CONTEXT.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-PATTERNS.md
@.planning/phases/08-bulk-unsubscribe-campaign/08-RESEARCH.md
@backend/core/src/main/resources/db/changelog/changes/032-mail-message-observed-sender-email.yaml
@backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml
@backend/core/src/main/resources/db/changelog/changes/027-tenant-sender-opt-in.yaml
@backend/core/src/main/resources/db/changelog/changes/040-triage-audit-message-ref.yaml
@backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Changelog 041 — mail_message_observed extend (D-09 ADD COLUMN forward-only)</name>
  <files>
    backend/core/src/main/resources/db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  </files>
  <read_first>
    backend/core/src/main/resources/db/changelog/changes/032-mail-message-observed-sender-email.yaml,
    backend/core/src/main/resources/db/changelog/changes/040-triage-audit-message-ref.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  </read_first>
  <action>
    Tạo `041-mail-message-observed-list-unsubscribe.yaml`. Mirror exact structure của `032-mail-message-observed-sender-email.yaml` (analog đã đọc). Schema theo D-09:
    - `changeSet id: 041-mail-message-observed-list-unsubscribe`, `author: zeromail`.
    - `comment:` ghi rõ "Forward-only backfill (D-10). Rows ingested before Phase 8 keep NULL. Candidate query filters `(list_unsubscribe_url IS NOT NULL OR list_unsubscribe_mailto IS NOT NULL)`. URL invariant: any non-NULL value begins with `https://` (D-11 parse-time guard)."
    - `addColumn` trên table `mail_message_observed`:
      - `list_unsubscribe_url` type `varchar(2048)` nullable (default NULL).
      - `list_unsubscribe_mailto` type `varchar(512)` nullable (default NULL).
      - `list_unsubscribe_one_click` type `boolean` `defaultValueBoolean: false` `constraints.nullable: false`.
    - `rollback:` block với 3 `dropColumn` (reverse order).

    Đăng ký `db.changelog-master.yaml`: thêm dòng `- include: { file: db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml }` sau dòng include của `040-triage-audit-message-ref.yaml`.

    KHÔNG dùng `addCheckConstraint` cho HTTPS prefix — D-11 quyết định enforce ở app-layer parse-time (`GmailPreviewReadService` DROP non-HTTPS) thay vì DB-layer (DB CHECK quá strict cho legacy data).

    KHÔNG dùng `defaultValueComputed` cho mailto/URL — D-09 forward-only.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*LiquibaseRollbackTest*"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/core/src/main/resources/db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml` tồn tại.
    - File chứa exact identifier `list_unsubscribe_url`, `list_unsubscribe_mailto`, `list_unsubscribe_one_click`.
    - File chứa `varchar(2048)` (cho URL) và `varchar(512)` (cho mailto).
    - File chứa `defaultValueBoolean: false` cho column `list_unsubscribe_one_click`.
    - File chứa `rollback:` block với 3 `dropColumn` entry.
    - `db.changelog-master.yaml` chứa entry `041-mail-message-observed-list-unsubscribe.yaml` (single grep).
    - `./gradlew :backend:core:test --tests "*LiquibaseRollbackTest*"` exits 0 (apply + rollback xanh trên Postgres 17 Testcontainer).
    - Sau migrate, `psql -c "\d mail_message_observed"` show 3 cột mới (acceptance via Liquibase verification test).
  </acceptance_criteria>
  <done>
    Changelog 041 lands + registered + rollback test green. mail_message_observed có 3 cột mới NULL/false default. Wave 2 GmailPreviewReadService extraction sẵn sàng.
  </done>
</task>

<task type="auto">
  <name>Task 2: Changelog 042 — processing_job generic outbox (D-01 + D-19 + D-25)</name>
  <files>
    backend/core/src/main/resources/db/changelog/changes/042-processing-job.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  </files>
  <read_first>
    backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml,
    backend/core/src/main/resources/db/changelog/changes/041-mail-message-observed-list-unsubscribe.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml,
    .planning/phases/08-bulk-unsubscribe-campaign/08-RESEARCH.md
  </read_first>
  <action>
    Tạo `042-processing-job.yaml` per D-01 + RESEARCH §"Liquibase DDL sketch (041-processing-job.yaml)" (RESEARCH ghi nhầm tên file `041`, nhưng đây là `042` sau correction). Schema:
    - `changeSet id: 042-processing-job`, `author: zeromail`.
    - `comment:` ghi rõ "Generic Postgres-backed worker outbox (D-01) for user-triggered async jobs. Phase 8 ships UNSUBSCRIBE_CAMPAIGN; SEED-009 reuses with new job_type enum values. SKIP LOCKED pickup + heartbeat-based crash recovery via reaper batch (D-03)."
    - `createTable: processing_job`:
      - `id` uuid PK `defaultValueComputed: gen_random_uuid()` NOT NULL.
      - `tenant_id` uuid NOT NULL FK→`tenants(id)` `deleteCascade: true` `foreignKeyName: fk_processing_job_tenant`.
      - `job_type` varchar(64) NOT NULL.
      - `payload` jsonb NOT NULL.
      - `status` varchar(16) NOT NULL `defaultValue: 'QUEUED'`.
      - `attempts` int NOT NULL `defaultValueNumeric: 0`.
      - `next_run_at` timestamptz NOT NULL `defaultValueComputed: now()`.
      - `heartbeat_at` timestamptz nullable.
      - `created_at` timestamptz NOT NULL `defaultValueComputed: now()`.
      - `started_at` timestamptz nullable.
      - `finished_at` timestamptz nullable.
      - `failure_reason` varchar(255) nullable.
      - `updated_at` timestamptz NOT NULL `defaultValueComputed: now()`.
    - `sql:` ALTER TABLE ADD CONSTRAINT `ck_processing_job_status` CHECK `status IN ('QUEUED','RUNNING','COMPLETED','FAILED')`.
    - `sql:` ALTER TABLE ADD CONSTRAINT `ck_processing_job_job_type` CHECK `job_type IN ('UNSUBSCRIBE_CAMPAIGN')`. (Wave SEED-009 extend với migration mới.)
    - `createIndex` `idx_processing_job_pickup` trên `(status, next_run_at, created_at)` — supports `SELECT FOR UPDATE SKIP LOCKED` poll query (D-02).
    - `sql:` `CREATE INDEX idx_processing_job_running_heartbeat ON processing_job (heartbeat_at) WHERE status = 'RUNNING'` — partial index cho reaper scan (D-03).
    - `createIndex` `idx_processing_job_tenant_type` trên `(tenant_id, job_type)` — cho per-tenant filter.
    - `createIndex` `idx_processing_job_finished_at` trên `(finished_at)` `WHERE status IN ('COMPLETED','FAILED')` — partial index cho purge batch D-25.
    - `rollback:` `dropTable: processing_job`.

    Đăng ký `db.changelog-master.yaml` sau 041.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*LiquibaseRollbackTest*"</automated>
  </verify>
  <acceptance_criteria>
    - File `042-processing-job.yaml` chứa exact identifier: `processing_job`, `ck_processing_job_status`, `ck_processing_job_job_type`, `idx_processing_job_pickup`, `idx_processing_job_running_heartbeat`.
    - CHECK constraint enumerate đúng giá trị: `QUEUED`, `RUNNING`, `COMPLETED`, `FAILED` cho status; `UNSUBSCRIBE_CAMPAIGN` cho job_type.
    - File chứa string `gen_random_uuid` (defaultValueComputed cho id).
    - File chứa string `jsonb` (cho payload column — D-19 `{"campaignId": "uuid", "schemaVersion": 1}`).
    - `db.changelog-master.yaml` chứa entry `042-processing-job.yaml`.
    - `./gradlew :backend:core:test --tests "*LiquibaseRollbackTest*"` exits 0.
    - Postgres: `\d processing_job` shows 13 cột; `\di processing_job_*` shows ≥ 4 index (pickup, running_heartbeat, tenant_type, finished_at).
  </acceptance_criteria>
  <done>
    processing_job table + 4 index + 2 CHECK constraint lands. Forward + rollback xanh. Wave 1b ProcessingJobEntity + Wave 4a ProcessingJobWorker sẵn sàng.
  </done>
</task>

<task type="auto">
  <name>Task 3: Changelogs 043 + 044 + 045 — sender_suppression + unsubscribe_campaign + unsubscribe_attempt</name>
  <files>
    backend/core/src/main/resources/db/changelog/changes/043-sender-suppression.yaml,
    backend/core/src/main/resources/db/changelog/changes/044-unsubscribe-campaign.yaml,
    backend/core/src/main/resources/db/changelog/changes/045-unsubscribe-attempt.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  </files>
  <read_first>
    backend/core/src/main/resources/db/changelog/changes/025-triage-audit.yaml,
    backend/core/src/main/resources/db/changelog/changes/027-tenant-sender-opt-in.yaml,
    backend/core/src/main/resources/db/changelog/changes/042-processing-job.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  </read_first>
  <action>
    Tạo 3 changelog đồng thời (cùng task vì cùng schema feature group):
    1. **`043-sender-suppression.yaml`** (D-09 + analog `027-tenant-sender-opt-in.yaml`):
       - `createTable: sender_suppression`:
         - `id` uuid PK `defaultValueComputed: gen_random_uuid()` NOT NULL.
         - `tenant_id` uuid NOT NULL FK→`tenants(id)` `deleteCascade: true` `foreignKeyName: fk_sender_suppression_tenant`.
         - `sender_email` varchar(320) nullable.
         - `sender_domain` varchar(255) nullable.
         - `reason` varchar(16) NOT NULL.
         - `created_at` timestamptz NOT NULL `defaultValueComputed: now()`.
       - `sql:` ADD CONSTRAINT `ck_sender_suppression_one_target` CHECK `(sender_email IS NOT NULL) <> (sender_domain IS NOT NULL)` — XOR exactly-one-target.
       - `sql:` ADD CONSTRAINT `ck_sender_suppression_reason` CHECK `reason IN ('manual','replied','auto')` — match D-09 enum.
       - `sql:` `CREATE UNIQUE INDEX ux_sender_suppression_email ON sender_suppression (tenant_id, sender_email) WHERE sender_email IS NOT NULL` — partial unique cho email target.
       - `sql:` `CREATE UNIQUE INDEX ux_sender_suppression_domain ON sender_suppression (tenant_id, sender_domain) WHERE sender_domain IS NOT NULL` — partial unique cho domain target.
       - `rollback:` `dropTable: sender_suppression`.
    2. **`044-unsubscribe-campaign.yaml`** (analog `025-triage-audit.yaml`):
       - `createTable: unsubscribe_campaign`:
         - `id` uuid PK `defaultValueComputed: gen_random_uuid()` NOT NULL.
         - `tenant_id` uuid NOT NULL FK→`tenants(id)` `deleteCascade: true` `foreignKeyName: fk_unsubscribe_campaign_tenant`.
         - `job_id` uuid nullable FK→`processing_job(id)` `foreignKeyName: fk_unsubscribe_campaign_job` (nullable vì campaign tạo trước job — D-04 transactional INSERT order: campaign → attempts → job).
         - `status` varchar(16) NOT NULL `defaultValue: 'QUEUED'`.
         - `applied_at` timestamptz nullable (set khi campaign reaches COMPLETED).
         - `reverted_at` timestamptz nullable.
         - `total_sender_count` int NOT NULL.
         - `total_history_message_count` int NOT NULL.
         - `created_at` timestamptz NOT NULL `defaultValueComputed: now()`.
       - `sql:` ADD CONSTRAINT `ck_unsubscribe_campaign_status` CHECK `status IN ('QUEUED','RUNNING','COMPLETED','FAILED')`.
       - `createIndex` `idx_unsubscribe_campaign_tenant_created` trên `(tenant_id, created_at DESC)`.
       - `createIndex` `idx_unsubscribe_campaign_job` trên `(job_id)`.
       - `rollback:` `dropTable: unsubscribe_campaign`.
    3. **`045-unsubscribe-attempt.yaml`** (analog `025-triage-audit.yaml`):
       - `createTable: unsubscribe_attempt`:
         - `id` uuid PK `defaultValueComputed: gen_random_uuid()` NOT NULL.
         - `campaign_id` uuid NOT NULL FK→`unsubscribe_campaign(id)` `deleteCascade: true` `foreignKeyName: fk_unsubscribe_attempt_campaign`.
         - `sender_email` varchar(320) NOT NULL.
         - `sender_domain` varchar(255) NOT NULL.
         - `unsubscribe_method` varchar(16) NOT NULL.
         - `state` varchar(16) NOT NULL `defaultValue: 'PENDING'`.
         - `failure_reason` varchar(255) nullable.
         - `archived_message_count` int NOT NULL `defaultValueNumeric: 0`.
         - `started_at` timestamptz nullable.
         - `finished_at` timestamptz nullable.
       - `sql:` ADD CONSTRAINT `ck_unsubscribe_attempt_method` CHECK `unsubscribe_method IN ('ONE_CLICK','MAILTO')`.
       - `sql:` ADD CONSTRAINT `ck_unsubscribe_attempt_state` CHECK `state IN ('PENDING','RUNNING','OK','FAILED')`.
       - `createIndex` `idx_unsubscribe_attempt_campaign_state` trên `(campaign_id, state)`.
       - `createIndex` `idx_unsubscribe_attempt_campaign_domain` trên `(campaign_id, sender_domain)` — supports throttle bucket-by-domain queries.
       - `rollback:` `dropTable: unsubscribe_attempt`.

    Đăng ký cả 3 vào `db.changelog-master.yaml` (sau 042). KHÔNG dùng JSONB cho domain table (CLAUDE.md: JSONB chỉ cho rule matchers + processing_job payload).

    NOTE D-25: `unsubscribe_campaign` + `unsubscribe_attempt` KHÔNG có purge schedule — giữ vĩnh viễn cho support + analytics retro. Chỉ `processing_job` purge sau 90d.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*LiquibaseRollbackTest*"</automated>
  </verify>
  <acceptance_criteria>
    - 3 file 043, 044, 045 tồn tại trong `backend/core/src/main/resources/db/changelog/changes/`.
    - `043-sender-suppression.yaml` chứa cả 2 string `ck_sender_suppression_one_target` và `ux_sender_suppression_email`.
    - `043` CHECK XOR constraint: file chứa string `(sender_email IS NOT NULL) <> (sender_domain IS NOT NULL)` exact (XOR semantics).
    - `043` reason values: file chứa `'manual','replied','auto'` exact.
    - `044-unsubscribe-campaign.yaml` chứa `fk_unsubscribe_campaign_job` (FK→processing_job).
    - `044` chứa `ck_unsubscribe_campaign_status` với 4 status value.
    - `045-unsubscribe-attempt.yaml` chứa `fk_unsubscribe_attempt_campaign` với `deleteCascade: true`.
    - `045` chứa cả 2 CHECK: `ck_unsubscribe_attempt_method` (ONE_CLICK,MAILTO) và `ck_unsubscribe_attempt_state` (PENDING,RUNNING,OK,FAILED).
    - `045` chứa identifier `idx_unsubscribe_attempt_campaign_state` (composite index per PATTERNS Group 1).
    - `db.changelog-master.yaml` chứa cả 3 include entry (043, 044, 045).
    - `./gradlew :backend:core:test --tests "*LiquibaseRollbackTest*"` exits 0.
    - Postgres: `\d sender_suppression unsubscribe_campaign unsubscribe_attempt` show schema đúng; rollback drops all 3 tables clean.
  </acceptance_criteria>
  <done>
    3 changelog 043-045 land + registered + rollback xanh. Schema cho suppression + campaign + per-sender attempt sẵn sàng cho Wave 1b entity layer + Wave 4 worker.
  </done>
</task>

</tasks>

<verification>
- 5 file changelog 041..045 tồn tại trong `backend/core/src/main/resources/db/changelog/changes/`.
- `db.changelog-master.yaml` đăng ký đủ 5.
- `./gradlew :backend:core:test --tests "*LiquibaseRollbackTest*"` exits 0 (forward apply + rollback xanh trên Postgres 17 Testcontainer).
- Wave 0 test `CleanupModuleVerificationTest` chưa pass (vì code chưa) — Wave 1b sẽ flip.
</verification>

<success_criteria>
- [ ] 5 file YAML 041..045 land với schema đúng D-09 + D-19 + D-25.
- [ ] Mọi CHECK constraint + index per RESEARCH §"Liquibase DDL sketch" + D-25 partial index cho purge.
- [ ] `LiquibaseRollbackTest` xanh trên Postgres 17 Testcontainer.
- [ ] Sau `./gradlew :backend:core:bootRun` (hoặc test-time migrate), schema có đủ 4 table mới + 3 column extend.
</success_criteria>

<output>
Tạo `.planning/phases/08-bulk-unsubscribe-campaign/08-02-SUMMARY.md` khi done.
</output>
