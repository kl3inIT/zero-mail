---
phase: 10-gmail-mailbox-foundation-and-account-management
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/core/src/test/java/com/zeromail/core/gmail/support/OldSingleAccountFixture.java
  - backend/core/src/test/java/com/zeromail/core/gmail/migration/Migration119Test.java
  - backend/core/src/test/java/com/zeromail/core/gmail/persistence/DuplicateActiveEmailTest.java
  - backend/core/src/test/java/com/zeromail/core/gmail/persistence/SetPrimaryTransactionalTest.java
  - backend/core/src/test/java/com/zeromail/core/gmail/persistence/RefreshTokenCipherContinuityTest.java
  - backend/core/src/test/java/com/zeromail/core/gmail/gateway/GmailApiClientFactoryMailboxCacheTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/gmail/MailboxOwnershipSeamTest.java
  - backend/api/src/test/java/com/zeromail/api/security/OAuthIntentRoutingTest.java
  - backend/api/src/test/java/com/zeromail/api/security/IntentCarryingRepositoryTest.java
autonomous: true
requirements: [VER-01, WSP-02, GMA-03, GMA-06, WSP-05, WSP-06, GMA-05, GMA-07, AUD-04]
must_haves:
  truths:
    - "Every Wave 0 test file from VALIDATION.md exists and is collected by the Gradle test runner"
    - "An old-single-account gmail_connections fixture is reusable by migration + cipher-continuity tests"
    - "Each Wave 0 test references the future production symbol it validates, so it stays RED until Waves 1-4 land that symbol"
  artifacts:
    - path: "backend/core/src/test/java/com/zeromail/core/gmail/support/OldSingleAccountFixture.java"
      provides: "Pre-119 single-row gmail_connections seed with known ciphertext for migration + continuity tests"
    - path: "backend/core/src/test/java/com/zeromail/core/gmail/migration/Migration119Test.java"
      provides: "Testcontainers migration assertion (backfill + byte-identical tokens)"
    - path: "backend/api/src/test/java/com/zeromail/api/controllers/gmail/MailboxOwnershipSeamTest.java"
      provides: "404/409 fail-closed contract assertion via RestClient + @LocalServerPort"
  key_links:
    - from: "Migration119Test"
      to: "OldSingleAccountFixture"
      via: "seed-then-migrate-then-assert"
      pattern: "OldSingleAccountFixture"
    - from: "GmailApiClientFactoryMailboxCacheTest"
      to: "GmailApiClientFactory.buildClientForMailbox"
      via: "two-mailbox cache isolation"
      pattern: "buildClientForMailbox"
---

<objective>
Scaffold the Phase 10 Wave 0 validation spine: the 9 automated tests + 1 shared fixture named in `10-VALIDATION.md`. These tests reference production symbols that Waves 1-4 create (`changeset 119`, `is_primary`, `findByIdAndTenantId`, `buildClientForMailbox`, `MailboxRef`, `resolveOwnedConnectionOrThrow`, `IntentCarryingAuthorizationRequestRepository`, the duplicate-active constraint). They are RED-by-design: compile/import/assertion failures against not-yet-written symbols ARE the executable acceptance contract for the rest of the phase.

Purpose: Honor the Nyquist VALIDATION.md — every Phase 10 requirement maps to an automated check that exists before the implementation it guards.
Output: 8 test classes + 1 fixture (the ArchUnit `GmailClientLookupBoundaryTest` is owned by Plan 03 because it ships with the factory re-key it guards).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-VALIDATION.md
@.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-RESEARCH.md
@.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates RED references to (does NOT implement) the following Phase 10 symbols — they are produced in Waves 1-4:
- `changeset 119-gmail-connections-multi-mailbox` (Plan 02), columns `is_primary` boolean + `label`/`display_purpose` (Plan 02)
- `GmailConnectionRepository.findByIdAndTenantId(UUID id, UUID tenantId)` and the tenant-ordered list query (Plan 02)
- Partial unique indexes `uq_gmail_conn_active_email`, `uq_gmail_conn_primary` (Plan 02)
- `GmailApiClientFactory.buildClientForMailbox(MailboxRef)`, `MailboxRef(UUID tenantId, UUID gmailConnectionId)` (Plan 03)
- `GmailConnectionService.resolveOwnedConnectionOrThrow(UUID tenantId, UUID gmailConnectionId)` (CONNECTED-only, 409 on any non-CONNECTED), `resolveReconnectableConnectionOrThrow(...)` (permits non-connected reconnectable states), `setPrimary(...)`, `disconnect(MailboxRef)` (Plan 04)
- `MailboxNotOwnedException` (404), `MailboxDisconnectedException` (409), `DuplicateActiveMailboxException` (409) (Plan 04)
- `IntentCarryingAuthorizationRequestRepository`, `OAuthIntentSnapshot`, the `ZEROMAIL_OAUTH_PENDING_INTENT` session-snapshot constant, intent-stamping resolver (reads the pending session snapshot, NOT URL params — codex HIGH-A), success-handler branch with top-of-handler one-shot intent removal (codex HIGH-B) + callback tenant check, failure-handler intent cleanup (Plan 05)
- New endpoints `/api/gmail/mailboxes`, `/api/gmail/mailboxes/{gmailConnectionId}/...` (Plan 06)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Old-single-account fixture + Testcontainers migration/persistence test scaffolds</name>
  <files>backend/core/src/test/java/com/zeromail/core/gmail/support/OldSingleAccountFixture.java, backend/core/src/test/java/com/zeromail/core/gmail/migration/Migration119Test.java, backend/core/src/test/java/com/zeromail/core/gmail/persistence/DuplicateActiveEmailTest.java, backend/core/src/test/java/com/zeromail/core/gmail/persistence/SetPrimaryTransactionalTest.java, backend/core/src/test/java/com/zeromail/core/gmail/persistence/RefreshTokenCipherContinuityTest.java</files>
  <read_first>
    - backend/core/src/test/java/com/zeromail/core/support/PostgresContainerTest.java (Testcontainers Postgres base — the migration/repo test superclass)
    - backend/core/src/test/java/com/zeromail/core/gmail/persistence/crypto/RefreshTokenCipherTest.java (AAD usage analog for continuity test)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java (the columns the fixture must seed: google_email, status, refresh_token_encrypted, scopes_granted, connected_at)
    - backend/core/src/main/resources/db/changelog/changes/003-create-gmail-connections.yaml (the pre-119 schema shape the fixture targets)
    - .planning/phases/10-gmail-mailbox-foundation-and-account-management/10-VALIDATION.md (§ Wave 0 Requirements — the authoritative test list)
  </read_first>
  <action>
    Create `OldSingleAccountFixture` in `backend/core/src/test/.../gmail/support/` exposing a static helper that INSERTs ONE pre-119-shape `gmail_connections` row (single tenant, `status='CONNECTED'`, a known AES-GCM ciphertext produced via `RefreshTokenCipher.encrypt(knownPlaintext, tenantId.toString())` so AAD continuity is assertable). Expose the tenantId, the known plaintext token, and the produced ciphertext as fields/accessors.
    Create `Migration119Test` extending `PostgresContainerTest`: seed via the fixture BEFORE migration is applied through changeset 119, then assert (a) exactly one row has `is_primary = true` for the tenant, (b) `refresh_token_encrypted` bytes are byte-identical pre/post migration, (c) the row's `status`/`scopes_granted` unchanged. Cite VER-01/WSP-02/D-11.
    Create `DuplicateActiveEmailTest` extending `PostgresContainerTest`: insert two `CONNECTED` rows for one tenant with the same `lower(google_email)` and assert the second INSERT fails with a Postgres 23505 on constraint `uq_gmail_conn_active_email` (GMA-06). Also assert two DIFFERENT active emails for one tenant coexist (GMA-01 multi-mailbox coexistence).
    Create `SetPrimaryTransactionalTest` extending `PostgresContainerTest`: assert the `uq_gmail_conn_primary` partial unique forbids two `is_primary=true` rows for one tenant, and reference the future `GmailConnectionService.setPrimary(...)` clear-old-set-new behavior (GMA-03).
    Create `RefreshTokenCipherContinuityTest` extending `PostgresContainerTest`: seed via fixture, apply migration 119, then `RefreshTokenCipher.decrypt(row.refresh_token_encrypted, tenantId.toString())` must equal the known plaintext (D-11 AAD unchanged).
    All four reference symbols that do not yet exist (changeset 119 indexes/columns, `setPrimary`) — leave those references unresolved so the tests are RED until Plan 02/04 land. Do NOT stub the production symbols here. Privacy: never assert on raw `google_email` in log output; the email value in fixtures is test data only.
  </action>
  <verify>
    <automated>cd backend && ./gradlew :backend:core:compileTestJava 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist at the declared paths under `backend/core/src/test/.../gmail/`.
    - `Migration119Test`, `RefreshTokenCipherContinuityTest` both consume `OldSingleAccountFixture` (grep finds `OldSingleAccountFixture` in both).
    - The four test classes reference the future symbols `is_primary`, `uq_gmail_conn_active_email`, `uq_gmail_conn_primary`, `setPrimary` (grep confirms) — they are RED, not green.
    - No production class under `backend/core/src/main` is modified by this task.
  </acceptance_criteria>
  <done>The migration/persistence Wave 0 scaffolds compile-fail or assertion-fail only against not-yet-written Plan 02/04 symbols; the fixture is shared by the migration + continuity tests.</done>
</task>

<task type="auto">
  <name>Task 2: Factory cache-isolation unit test scaffold</name>
  <files>backend/core/src/test/java/com/zeromail/core/gmail/gateway/GmailApiClientFactoryMailboxCacheTest.java</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java (the cache field at lines 45-48, `buildClientForConnection` lines 108-133, `refreshAccessToken` line 137 — what the mailbox cache test mocks)
    - backend/core/src/main/java/com/zeromail/core/gmail/persistence/GmailConnectionEntity.java (constructor + getters the test builds two connections with)
    - .planning/phases/10-gmail-mailbox-foundation-and-account-management/10-RESEARCH.md (§ Token Cache Re-Keying — the two-mailbox isolation invariant, lines 310-316)
  </read_first>
  <action>
    Create `GmailApiClientFactoryMailboxCacheTest` in `backend/core/src/test/.../gmail/gateway/`. Build two `MailboxRef` values for ONE tenant but two distinct `gmailConnectionId`s (mailbox A, mailbox B), back them with two `GmailConnectionEntity` rows carrying distinct ciphertext, and a mocked refresh path (`Mockito` spy/mock on the HTTP refresh seam — mirror the existing factory test approach in the repo if one exists, else mock `refreshAccessToken`). Assert: requesting `buildClientForMailbox(refA)` then `buildClientForMailbox(refB)` triggers a cache MISS on B's key (B's refresh runs) and B never receives A's cached access token. Reference `MailboxRef` and `buildClientForMailbox` (Plan 03 symbols) so the test is RED.
    Privacy: assert behavior on token identity/cache-key, never log token bytes.
  </action>
  <verify>
    <automated>cd backend && ./gradlew :backend:core:compileTestJava 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `GmailApiClientFactoryMailboxCacheTest` exists and references `MailboxRef` + `buildClientForMailbox` (grep confirms).
    - The test encodes the "B never receives A's cached token" assertion (the D-10 cross-mailbox bleed invariant).
    - RED against Plan 03 symbols; no production main-source change.
  </acceptance_criteria>
  <done>The token-cache isolation invariant (D-10, the central phase threat) has a failing automated guard before the re-key lands.</done>
</task>

<task type="auto">
  <name>Task 3: API-tier OAuth + ownership Wave 0 scaffolds (RestClient + @LocalServerPort)</name>
  <files>backend/api/src/test/java/com/zeromail/api/controllers/gmail/MailboxOwnershipSeamTest.java, backend/api/src/test/java/com/zeromail/api/security/OAuthIntentRoutingTest.java, backend/api/src/test/java/com/zeromail/api/security/IntentCarryingRepositoryTest.java</files>
  <read_first>
    - backend/api/src/test/java/com/zeromail/api/controllers/rules/RulesControllerIntegrationTest.java (RestClient + @LocalServerPort HTTP test pattern — filters/ScopedValue bind, NOT MockMvc)
    - backend/api/src/main/java/com/zeromail/api/security/GoogleOAuthSuccessHandler.java (the three-intent branch target the routing test asserts: provision / add / reconnect)
    - backend/api/src/main/java/com/zeromail/api/security/GoogleAuthorizationRequestResolver.java (resolver the intent test stamps attributes through)
    - backend/api/src/main/java/com/zeromail/api/config/GlobalExceptionHandler.java (404/409 ProblemDetail shape the ownership test asserts on — `code`, `status`)
    - .planning/phases/10-gmail-mailbox-foundation-and-account-management/10-RESEARCH.md (§ OAuth Callback Survival — IntentCarrying shim behavior + Redis-dirty setAttribute, lines 117-206)
    - .claude/skills (spring-security-testing, spring-mvc-testing — Boot 4 / Security 7 test-API specifics: @MockitoBean not @MockBean)
  </read_first>
  <action>
    Create `MailboxOwnershipSeamTest` in `backend/api/src/test/.../controllers/gmail/` using `RestClient + @LocalServerPort` with an authenticated test session. Assert the full per-endpoint state matrix (Plan 04), all RED against future symbols:
    - a mailbox-scoped endpoint `/api/gmail/mailboxes/{gmailConnectionId}/...` returns 404 (ProblemDetail) for a `gmailConnectionId` not owned by the current tenant or missing;
    - set-primary returns 409 for a DISCONNECTED owned mailbox AND 409 for a PENDING / NOT_CONNECTED owned mailbox (REVIEW FIX codex HIGH #6 — `resolveOwnedConnectionOrThrow` must fail closed on ANY non-CONNECTED status, not only DISCONNECTED);
    - the reconnect trigger endpoint `/api/gmail/mailboxes/{gmailConnectionId}/reconnect` is REACHABLE (302, NOT 409) for a DISCONNECTED owned mailbox (REVIEW FIX codex HIGH #2 — reconnect of a disconnected mailbox must work) and 404 for a not-owned/missing id.
    Reference the future endpoint paths + `resolveOwnedConnectionOrThrow` / `resolveReconnectableConnectionOrThrow` semantics (Plan 04/06) so it is RED. Assert on HTTP status + ProblemDetail `code`, never on raw email.
    Create `OAuthIntentRoutingTest` in `backend/api/src/test/.../security/` as the test-profile SecurityConfig slice (folded WR-06): exercise the three OAuth intents (first_login / add_mailbox / reconnect_mailbox) routing through success/failure handlers. Use a test-profile SecurityConfig and a mocked `OAuth2AuthorizedClientService`/authentication; assert first-login routes to `provisionBundledOAuth`, add routes to a new-row INSERT branch (no re-provision), reconnect routes to the targeted-row update branch. ADD these RED cases for the new invariants:
    - URL-param intent carries NO branch authority (codex HIGH-A / D-01): a request whose URL carries `?intent=add_mailbox`/`?intent=reconnect_mailbox&targetMailboxId=...` BUT has NO `ZEROMAIL_OAUTH_PENDING_INTENT` session snapshot stamps NO intent attributes (the resolver ignores the URL param and falls closed to first-login); conversely a valid `ZEROMAIL_OAUTH_PENDING_INTENT` session snapshot (no matching URL param) DOES drive the branch and the resolver clears the pending key one-shot. Drive via `GoogleAuthorizationRequestResolver` reading `ZEROMAIL_OAUTH_PENDING_INTENT`. Also assert the Plan 06 `ConnectMailboxController` add/reconnect redirect Location contains `/oauth2/authorization/google?reconnect=true` and does NOT contain `intent=` or `targetMailboxId=`.
    - unknown/forged pending-intent value with an authenticated session falls closed to first-login (no add/reconnect branch taken), and an authenticated request with no pending snapshot does NOT reach the add/reconnect branch via any URL param — codex HIGH-A;
    - callback-time tenant mismatch: when the live session tenant != `OAuthIntentSnapshot.initiatingTenantId`, the handler does NOT write and fails closed (asserts `event=oauth_intent_tenant_mismatch` / no INSERT) — codex HIGH #9;
    - stale-intent-after-failure: a FAILED auth callback clears `ZEROMAIL_OAUTH_INTENT`, and a subsequent callback does NOT reuse the stale add/reconnect snapshot — codex HIGH #7 (drive via the `LoginRedirectAuthenticationFailureHandler` path);
    - stale-intent-after-success-handler-early-failure (codex HIGH-B): the success handler is REACHED with a `ZEROMAIL_OAUTH_INTENT` snapshot present, then an EARLY validation short-circuit fires (the granted-scope check throws, OR the null-refresh-token first-login branch does `sendRedirect; return`) WITHOUT routing through `LoginRedirectAuthenticationFailureHandler`; assert that after this early exit the `ZEROMAIL_OAUTH_INTENT` session attribute is GONE (the top-of-handler one-shot removal ran) and a subsequent callback does not reuse the stale add/reconnect snapshot.
    Reference the future success-handler branch + `OAuthIntentSnapshot` + the `ZEROMAIL_OAUTH_PENDING_INTENT` constant + the failure-handler cleanup (Plan 05/06) so it is RED. Cite GMA-07/WR-06.
    Create `IntentCarryingRepositoryTest` in `backend/api/src/test/.../security/`: assert the future `IntentCarryingAuthorizationRequestRepository.removeAuthorizationRequest` copies the intent snapshot into the `HttpSession` via a FRESH-value `setAttribute` (Redis-dirty fix, RESEARCH Pitfall 2 / #7327) so the success handler reads it back. Reference `IntentCarryingAuthorizationRequestRepository` + `OAuthIntentSnapshot` (Plan 05) so it is RED.
    Honor the project skills' Boot 4 / Security 7 APIs: `@MockitoBean` (not `@MockBean`).
  </action>
  <verify>
    <automated>cd backend && ./gradlew :backend:api:compileTestJava 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist at the declared API test paths.
    - `MailboxOwnershipSeamTest` asserts 404 (not-owned/missing), 409 on set-primary for DISCONNECTED AND PENDING/NOT_CONNECTED (codex HIGH #6), and reconnect REACHABLE (302, not 409) on a DISCONNECTED mailbox (codex HIGH #2); uses RestClient + `@LocalServerPort` (grep confirms `@LocalServerPort`, not `MockMvc`).
    - `OAuthIntentRoutingTest` references all three intent branch targets, is a test-profile SecurityConfig slice, AND adds: URL-param-intent-carries-no-authority + redirect-has-no-intent-params (HIGH-A), pending-snapshot-drives-branch + resolver-clears-pending-key (HIGH-A), tenant-mismatch-fails-closed (#9), stale-intent-cleared-after-failure (#7), AND stale-intent-cleared-after-success-handler-early-failure (HIGH-B: success handler reached, scope/null-refresh check short-circuits → `ZEROMAIL_OAUTH_INTENT` is gone).
    - `IntentCarryingRepositoryTest` references `IntentCarryingAuthorizationRequestRepository` + the fresh-`setAttribute` dirty behavior.
    - Tests use `@MockitoBean` not `@MockBean`; RED against Plan 05/06 symbols; no production main-source change.
  </acceptance_criteria>
  <done>Ownership fail-closed (404/409 incl. PENDING/NOT_CONNECTED), reconnect-of-disconnected reachability, three-intent routing (WR-06), server-side-snapshot-only intent authority (no URL branch authority — HIGH-A), callback tenant-identity check, stale-intent cleanup on BOTH failure-handler and success-handler-early-exit paths (HIGH-B), and the intent-shim session survival each have a failing automated guard before their implementation lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → not-yet-written production symbols | RED scaffolds reference symbols that do not exist; the risk is accidentally satisfying them with stubs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | Wave 0 scaffolds | mitigate | Tests MUST NOT add production stubs (no `backend/*/src/main` change); acceptance criteria assert main-source untouched, so the RED contract is real |
| T-10-01-02 | Information Disclosure | test fixtures / log assertions | mitigate | Fixtures use test-only emails; no test asserts on raw `google_email` in logs; cipher continuity test asserts plaintext equality in-memory, never logs it |
| T-10-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH § Package Legitimacy Audit: not applicable, zero new deps) |
</threat_model>

<verification>
- `./gradlew :backend:core:compileTestJava :backend:api:compileTestJava` — scaffolds compile only where they reference existing symbols; the RED references to Plan 02-06 symbols are the intended failure surface (document which symbols are unresolved in the SUMMARY).
- After Plans 02-06 land, the same files turn green with no edits to the assertions.
</verification>

<success_criteria>
- 8 test classes + 1 fixture exist at declared paths (the 9th Wave 0 item, `GmailClientLookupBoundaryTest`, is owned by Plan 03).
- Each test references the specific future symbol it guards (per the VALIDATION.md per-requirement map).
- No production source under `backend/*/src/main` is modified.
- VALIDATION.md `wave_0_complete` can be flipped true once Plan 03's arch test also lands.
</success_criteria>

<output>
Create `.planning/phases/10-gmail-mailbox-foundation-and-account-management/10-01-SUMMARY.md` when done, listing each scaffold + the exact future symbol each one is RED against.
</output>
