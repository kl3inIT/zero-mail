---
phase: 01.2-domain-owned-persistence-restructuring
plan: 04
subsystem: persistence
tags: [spring-modulith, java-25, refactor, package-rename, onboarding, jpa, entity-scan, cl-2, pitfall-5]

# Dependency graph
requires:
  - phase: 01.2-domain-owned-persistence-restructuring
    plan: 03
    provides: "AccountService at core.account.service (1 repo only); AccountDeletionController transitional bridge with OnboardingSelectionRepository injection; account/package-info forward-deferred onboarding edge; 3-element @EntityScan array; OnboardingService still at core.account/ (imports already updated)"
  - phase: 01.2-domain-owned-persistence-restructuring
    plan: 01
    provides: "Locked CL-3 form `\"shared.privacy\"`"
  - phase: 01-foundation-safety-infrastructure
    provides: "OnboardingSelectionEntity/Repository, OnboardingStep enum, OnboardingService, UserEntity (post-Plan-03 location), OnboardingController, AccountDeletionController, OnboardingStateMachineTest, AccountDeletionE2ETest, ApplicationModulesTest"
provides:
  - "OnboardingSelectionEntity + OnboardingSelectionRepository at com.zeromail.core.onboarding.persistence"
  - "OnboardingStep enum at com.zeromail.core.onboarding.model (canonical domain location per D-A2 + D-A5)"
  - "OnboardingService at com.zeromail.core.onboarding.service (relocated out of core.account/)"
  - "OnboardingService.deleteSelectionsForCurrentTenant(UUID) — single-domain delete per CL-2"
  - "core.onboarding/package-info.java with @ApplicationModule(allowedDependencies = {\"tenant\", \"account\", \"shared.privacy\"}) — D-D4 onboarding -> account edge"
  - "core.account/package-info.java amended to add \"onboarding\" (forward-deferred from Plan 03 closed in same commit as the new module)"
  - "AccountDeletionController transitional bridge: 1 of 3 cross-domain repos (Onboarding) replaced by service call; Gmail + Tenant remain for Plan 05"
  - "Widened transitional 4-element @EntityScan + @EnableJpaRepositories arrays in Application.java + CoreTestApplication.java"
  - "OnboardingStepEnumPersistenceTest — Wave 0 gap closure for T-01.2-E (Pitfall 5)"
affects: [01.2-05, 01.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CL-2 single-domain delete pattern continued: OnboardingService.deleteSelectionsForCurrentTenant mirrors AccountService.deleteCurrentUser shape; repository.deleteAll(repository.findByTenantId(...)) inside @Transactional"
    - "Transitional bridge controller idiom (Plan 03 -> Plan 06 progressive): each plan replaces ONE direct cross-domain repo call with the per-domain service. Plan 04 swaps onboardingRepo for OnboardingService."
    - "Pitfall 1 closure protocol applied identically to Plans 02 + 03: every stale FQN reference (UserEntity import + 2 test files this plan) updated alongside the git mv"
    - "Wave 0 gap-closure pattern: net-new pure-JVM unit test (OnboardingStepEnumPersistenceTest) defends against a silent-failure mode that compile-time signals cannot catch (Pitfall 5 — enum-name persistence drift)"

key-files:
  created:
    - "backend/core/src/main/java/com/zeromail/core/onboarding/persistence/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/onboarding/persistence/lowlevel/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/onboarding/model/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/onboarding/service/package-info.java"
    - "backend/core/src/main/java/com/zeromail/core/onboarding/package-info.java"
    - "backend/core/src/test/java/com/zeromail/core/onboarding/OnboardingStepEnumPersistenceTest.java"
  modified:
    - "backend/core/src/main/java/com/zeromail/core/onboarding/persistence/OnboardingSelectionEntity.java (relocated; package decl updated)"
    - "backend/core/src/main/java/com/zeromail/core/onboarding/persistence/OnboardingSelectionRepository.java (relocated; package decl updated)"
    - "backend/core/src/main/java/com/zeromail/core/onboarding/model/OnboardingStep.java (relocated; package decl updated)"
    - "backend/core/src/main/java/com/zeromail/core/onboarding/service/OnboardingService.java (relocated; package + imports updated; new deleteSelectionsForCurrentTenant method added)"
    - "backend/core/src/main/java/com/zeromail/core/account/persistence/UserEntity.java (OnboardingStep import flipped from core.persistence to core.onboarding.model)"
    - "backend/core/src/main/java/com/zeromail/core/account/package-info.java (allowedDependencies amended to {\"tenant\", \"onboarding\", \"shared.privacy\"})"
    - "backend/api/src/main/java/com/zeromail/api/Application.java (@EntityScan + @EnableJpaRepositories widened to 4 packages)"
    - "backend/core/src/test/java/com/zeromail/core/support/CoreTestApplication.java (same widening)"
    - "backend/api/src/main/java/com/zeromail/api/controllers/OnboardingController.java (OnboardingService import flipped)"
    - "backend/api/src/main/java/com/zeromail/api/controllers/AccountDeletionController.java (OnboardingSelectionRepository -> OnboardingService swap; comment updated)"
    - "backend/api/src/test/java/com/zeromail/api/OnboardingStateMachineTest.java (OnboardingStep import flipped)"
    - "backend/api/src/test/java/com/zeromail/api/AccountDeletionE2ETest.java (OnboardingSelectionEntity/Repository imports flipped)"
  deleted:
    - "backend/core/src/main/java/com/zeromail/core/persistence/OnboardingSelectionEntity.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/persistence/OnboardingSelectionRepository.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/persistence/OnboardingStep.java (moved via git mv)"
    - "backend/core/src/main/java/com/zeromail/core/account/OnboardingService.java (moved via git mv to core.onboarding.service/)"

key-decisions:
  - "Bidirectional account <-> onboarding edges declared atomically in Task 2's commit. core.onboarding/package-info.java created with allowedDependencies = {\"tenant\", \"account\", \"shared.privacy\"} in the same commit that amends core.account/package-info.java to add \"onboarding\" (forward-deferral from Plan 03 closed). Modulith verifies module existence eagerly — both edges land together so ApplicationModulesTest.verify() stays green at the task boundary."
  - "Pitfall 1 sweep folded into Task 1 commit (same protocol as Plan 03 d7fbe69). Moving OnboardingStep out of core.persistence broke UserEntity's import — fixing it in Task 1 keeps :backend:core:compileJava green at the task boundary. Plan text suggested the flip happens in Task 2; Rule 3 (blocking-issue auto-fix) folded it earlier."
  - "Test import sweep folded into Task 2 commit. Removing core.persistence.OnboardingSelectionEntity/Repository/Step broke OnboardingStateMachineTest + AccountDeletionE2ETest's imports — fixing them in Task 2 keeps :backend:api:compileTestJava green at the task boundary."
  - "AccountDeletionController bridge progresses one step toward final form (CL-2 + Plan 03's transitional bridge plan). OnboardingSelectionRepository injection dropped; OnboardingService injected; onboardingRepo.deleteAll(...) replaced with onboardingService.deleteSelectionsForCurrentTenant(tenantId). Comment updated to reflect Plan 04 done; Plan 05 still owes the gmail + tenant swaps."
  - "OnboardingStep stays at core.onboarding.model (D-A5 — onboarding domain owns the lifecycle definition even though it's persisted as a column on users). UserEntity (in core.account.persistence) imports it cross-module; this is the concrete reason for the account -> onboarding allowedDependency edge."

patterns-established:
  - "Atomic bidirectional Modulith edge declaration: when introducing a new module that an existing module already depends on, declare BOTH edges in the same commit as the new package-info.java. Forward-deferred allowedDependencies (Plan 03's pattern) close out in the same commit that creates the target module. Modulith refuses dangling edges; this protocol prevents intermediate red commits."
  - "Wave 0 enum-stability test pattern: when relocating a @Enumerated(EnumType.STRING) enum, ship a pure-JVM test that asserts each constant's name() literal matches its persisted DB string. Defends against a silent-failure mode (constant rename) that compile-time signals miss because @Enumerated only resolves the enum class, not the constant identifiers."

requirements-completed: ["N/A — structural refactor, no Phase 1 requirement IDs apply"]

# Metrics
duration: ~6min
completed: 2026-04-26
---

# Phase 01.2 Plan 04: Move onboarding domain to per-tier sub-packages + add CL-2 delete + close Pitfall 5

**The onboarding domain now lives at `com.zeromail.core.onboarding.{persistence,model,service}.*`; OnboardingService gains the single-domain `deleteSelectionsForCurrentTenant(UUID)` per CL-2; AccountDeletionController's transitional bridge swaps the OnboardingSelectionRepository call for the service call; UserEntity's `OnboardingStep` import flipped to the canonical domain location; the new `OnboardingStepEnumPersistenceTest` defends against silent enum-name drift (Pitfall 5).**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-04-26T09:11Z
- **Completed:** 2026-04-26T09:17Z
- **Tasks:** 3 (all completed)
- **Files modified:** 18 (4 source relocations + 5 new package-info markers + 1 net-new test + 8 reference updates including 2 test imports)
- **Commits:** 3 (one per task — clean linear history, no fix-up commits)

## Accomplishments

- `OnboardingSelectionEntity` + `OnboardingSelectionRepository` moved to `com.zeromail.core.onboarding.persistence.*` (rename history preserved via `git mv`).
- `OnboardingStep` enum moved to `com.zeromail.core.onboarding.model.*` — D-A2 + D-A5 honoured (onboarding domain owns the lifecycle definition).
- `OnboardingService` relocated out of `core.account/` into `core.onboarding.service.*`.
- **`OnboardingService.deleteSelectionsForCurrentTenant(UUID)`** added per CL-2 — single-domain delete (`onboarding.deleteAll(onboarding.findByTenantId(tenantId))` inside `@Transactional`). Mirrors the shape of `AccountService.deleteCurrentUser` from Plan 03.
- **UserEntity `OnboardingStep` import flipped** from `core.persistence.OnboardingStep` to `core.onboarding.model.OnboardingStep`. `@Enumerated(EnumType.STRING)` semantics keep the persisted `users.onboarding_step` column byte-identical.
- **`core.onboarding/package-info.java`** created with `@ApplicationModule(displayName = "Onboarding", allowedDependencies = {"tenant", "account", "shared.privacy"})`. The `account` edge is real — `OnboardingService` reads `UserEntity` to advance the step.
- **`core.account/package-info.java`** amended to add `"onboarding"` (forward-deferred from Plan 03 — see Plan 03 SUMMARY notes for the deferral rationale). Both `account <-> onboarding` edges are now declared.
- **AccountDeletionController transitional bridge progressed one step.** OnboardingSelectionRepository injection dropped; `OnboardingService` injected. The cascade body now reads:
  ```java
  onboardingService.deleteSelectionsForCurrentTenant(tenantId);  // was: onboardingRepo.deleteAll(onboardingRepo.findByTenantId(tenantId))
  gmailRepo.findByTenantId(tenantId).ifPresent(gmailRepo::delete);  // Plan 05 swaps to GmailConnectionService
  accountService.deleteCurrentUser(tenantId);
  tenantRepo.findById(tenantId).ifPresent(tenantRepo::delete);  // Plan 05 swaps to TenantService
  ```
- **Transitional 4-element `@EntityScan` + `@EnableJpaRepositories` arrays** in both `Application.java` and `CoreTestApplication.java`: `{core.persistence, core.tenant.persistence, core.account.persistence, core.onboarding.persistence}`. Plan 05 collapses to single root `"com.zeromail.core"` after gmail entities also move.
- **`OnboardingStepEnumPersistenceTest`** added at `backend/core/src/test/java/com/zeromail/core/onboarding/` — pure-JVM unit test asserting each enum constant's `name()` matches its stable persisted string. Closes Wave 0 gap row T-01.2-E (Pitfall 5 — enum-name persistence drift).
- All gates green: `OnboardingStateMachineTest`, `AccountDeletionE2ETest`, `ApplicationModulesTest`, `OnboardingStepEnumPersistenceTest`. Phase 1.1 contract suite still green: `ProblemDetailContractTest`, `ApiErrorSchemaTest`, `LocaleResolutionTest`.
- Zero stale `com.zeromail.core.persistence.Onboarding*` or `com.zeromail.core.account.OnboardingService` references in `backend/`.

## Task Commits

Each task committed atomically — every commit compiles cleanly:

1. **Task 1: move onboarding entity/repo/enum/service to per-tier sub-packages + fold UserEntity import flip** — `cb8166b` (refactor)
2. **Task 2: add onboarding ApplicationModule + amend account allowedDependencies + bridge AccountDeletionController to OnboardingService** — `2f25214` (refactor)
3. **Task 3: add OnboardingStepEnumPersistenceTest** — `30d8ae7` (test)

## Files Created/Modified

**Created (5 package-info markers + 1 net-new test):**
- `backend/core/src/main/java/com/zeromail/core/onboarding/persistence/package-info.java` — intra-domain marker, plain JavaDoc
- `backend/core/src/main/java/com/zeromail/core/onboarding/persistence/lowlevel/package-info.java` — proactive native-SQL allow-list marker (per D-D3); empty in Phase 1.2
- `backend/core/src/main/java/com/zeromail/core/onboarding/model/package-info.java` — domain enums; explicitly NOT wire DTOs
- `backend/core/src/main/java/com/zeromail/core/onboarding/service/package-info.java` — concrete @Service classes only (D-B3)
- `backend/core/src/main/java/com/zeromail/core/onboarding/package-info.java` — `@ApplicationModule(allowedDependencies = {"tenant", "account", "shared.privacy"})`
- `backend/core/src/test/java/com/zeromail/core/onboarding/OnboardingStepEnumPersistenceTest.java` — Wave 0 gap closure (T-01.2-E)

**Relocated via git mv (rename history preserved, package decls updated):**
- `core/persistence/OnboardingSelectionEntity.java` → `core/onboarding/persistence/OnboardingSelectionEntity.java`
- `core/persistence/OnboardingSelectionRepository.java` → `core/onboarding/persistence/OnboardingSelectionRepository.java`
- `core/persistence/OnboardingStep.java` → `core/onboarding/model/OnboardingStep.java`
- `core/account/OnboardingService.java` → `core/onboarding/service/OnboardingService.java` (+ `deleteSelectionsForCurrentTenant(UUID)` added)

**Modified (Modulith + entity-scan widening):**
- `backend/core/src/main/java/com/zeromail/core/account/package-info.java` — `allowedDependencies` updated from `{"tenant", "shared.privacy"}` to `{"tenant", "onboarding", "shared.privacy"}`
- `backend/api/src/main/java/com/zeromail/api/Application.java` — `@EntityScan` + `@EnableJpaRepositories` widened from 3 to 4 packages
- `backend/core/src/test/java/com/zeromail/core/support/CoreTestApplication.java` — identical widening

**Modified (consumer import flips):**
- `backend/core/src/main/java/com/zeromail/core/account/persistence/UserEntity.java` — `OnboardingStep` import flipped to `core.onboarding.model.OnboardingStep`
- `backend/api/src/main/java/com/zeromail/api/controllers/OnboardingController.java` — `OnboardingService` import flipped to `core.onboarding.service.OnboardingService`
- `backend/api/src/main/java/com/zeromail/api/controllers/AccountDeletionController.java` — TRANSITIONAL bridge: OnboardingSelectionRepository injection swapped for OnboardingService; comment updated
- `backend/api/src/test/java/com/zeromail/api/OnboardingStateMachineTest.java` — `OnboardingStep` import flipped
- `backend/api/src/test/java/com/zeromail/api/AccountDeletionE2ETest.java` — `OnboardingSelectionEntity` + `OnboardingSelectionRepository` imports flipped

## Decisions Made

- **Bidirectional `account <-> onboarding` edges declared atomically in Task 2's commit.** Plan 03's forward-deferral protocol closed cleanly here: the `account -> onboarding` allowedDependency edge was added to `core.account/package-info.java` in the SAME commit (`2f25214`) that creates `core.onboarding/package-info.java`. This avoids any intermediate red state where Modulith's eager module-existence check would fail.
- **Pitfall 1 sweep folded into Task 1 commit (Rule 3 - Blocking).** Plan 04's Task 1 verify is `:backend:core:compileJava`, but moving `OnboardingStep` out of `core.persistence` immediately breaks `UserEntity`'s import. Fixing it in Task 1 keeps the per-task `compileJava` invariant intact. Same protocol as Plan 03 commit `d7fbe69`.
- **Test import sweep folded into Task 2 commit (Rule 3 - Blocking).** Plan 04's Task 2 verify runs three integration tests, but `OnboardingStateMachineTest` + `AccountDeletionE2ETest` were still importing `core.persistence.Onboarding*`. Updating them in Task 2's commit keeps `:backend:api:compileTestJava` green at the task boundary so the verify command can actually run.
- **AccountDeletionController bridge: 1 of 3 cross-domain repos replaced.** Per the plan's `<output>` note: Plan 05 must replace `GmailConnectionRepository` with `GmailConnectionService.deleteForCurrentTenant` AND `TenantRepository` with `TenantService.deleteCurrentTenant`. The controller's class-level Javadoc was updated to mark Plan 04 done while listing Plan 05's remaining work.
- **OnboardingService `selectTemplate` + `complete` bodies preserved verbatim.** Only the package decl, imports, and one new method changed. The state-machine semantics + `CurrentUserNotFoundException` throwing behavior carry over byte-identical.
- **No `OnboardingStep` import added to OnboardingService** — it's now in `core.onboarding.model` (sibling to `core.onboarding.service`), so the import is present and explicit (as plan specified).
- **OnboardingStepEnumPersistenceTest is pure-JVM, not Postgres-backed.** `name()` semantics are JLS-defined; no container needed. Runs in milliseconds. The test gates against the silent-failure mode of an enum-rename masquerading as a "harmless" refactor — exactly the Pitfall 5 surface the threat register flagged as `mitigate`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Pitfall 1 sweep — UserEntity OnboardingStep import flipped in Task 1 commit (planned for Task 2)**
- **Found during:** Task 1 verify gate (`./gradlew :backend:core:compileJava` failed with "cannot find symbol: class OnboardingStep" 5 times in `UserEntity.java` after the enum's `git mv`).
- **Issue:** Plan 04 Task 1 listed only the relocated files + new package-info markers in `<files>`. But moving `OnboardingStep` out of `com.zeromail.core.persistence` immediately breaks `UserEntity`'s `import com.zeromail.core.persistence.OnboardingStep;`. The plan explicitly stated "compile succeeds — UserEntity import-flip in Task 2 is the next step", but reality contradicts this — `:backend:core:compileJava` cannot pass without UserEntity's import flip.
- **Fix:** Updated `UserEntity.java` to `import com.zeromail.core.onboarding.model.OnboardingStep;` in the same commit as the source moves. Same Pitfall 1 closure pattern Plan 03 used (commit `d7fbe69`).
- **Files modified:** `backend/core/src/main/java/com/zeromail/core/account/persistence/UserEntity.java`
- **Verification:** Bundled into Task 1's commit `cb8166b`; subsequent `:backend:core:compileJava` green.
- **Committed in:** `cb8166b`

**2. [Rule 3 - Blocking] Test import sweep folded into Task 2 commit**
- **Found during:** Task 2 verify gate preflight (`:backend:api:compileTestJava` failed with 8 errors across `OnboardingStateMachineTest.java` and `AccountDeletionE2ETest.java`).
- **Issue:** Removing `core.persistence.OnboardingStep` + `core.persistence.OnboardingSelectionEntity` + `core.persistence.OnboardingSelectionRepository` broke 2 test files that imported them. The plan's `<files>` list for Task 2 didn't include these tests, but the plan's verify command DOES run them — so the test imports MUST be updated in Task 2 or the verify command fails.
- **Fix:** Updated both test files' imports to point at the new `core.onboarding.{model,persistence}` paths. Bundled into Task 2's commit so the verify command had compilable tests to run.
- **Files modified:** `backend/api/src/test/java/com/zeromail/api/OnboardingStateMachineTest.java`, `backend/api/src/test/java/com/zeromail/api/AccountDeletionE2ETest.java`
- **Verification:** Bundled into Task 2's commit `2f25214`; subsequent `:backend:api:test --tests "OnboardingStateMachineTest" --tests "AccountDeletionE2ETest" --tests "ApplicationModulesTest"` green.
- **Committed in:** `2f25214`

**3. [Rule 1 - Comment Drift] AccountDeletionController class-level Javadoc updated**
- **Found during:** Task 2 implementation (cosmetic — the existing comment said "Plan 04 replaces the OnboardingSelectionRepository call with OnboardingService" in present-future tense; after the swap landed it became inaccurate).
- **Issue:** Stale comment that no longer matched the code below it (the swap had already happened in this same commit).
- **Fix:** Changed wording to "Plan 04 replaced the OnboardingSelectionRepository call with OnboardingService (done)". Plan 05's TODO line preserved.
- **Files modified:** `backend/api/src/main/java/com/zeromail/api/controllers/AccountDeletionController.java`
- **Committed in:** `2f25214`

---

**Total deviations:** 3 auto-fixed (2 Rule 3 — same blocking-issue closure protocol as Plans 02 + 03; 1 Rule 1 — minor comment drift fix to keep Javadoc honest)
**Impact on plan:** Net-positive — kept every commit compile-green, avoided fix-up commits, kept Task 3 a clean 1-file metadata commit. AccountDeletionE2ETest stays green throughout the bridge progression.

## Issues Encountered

- **CRLF line-ending warnings on Windows** for every staged file. Cosmetic; `.gitattributes` handles normalization.
- **No real issues.** Plan 04 was a clean execution: 3 commits, each compile-green after the necessary Rule 3 folds, all gates green on first run, no probes, no surprises. The CL-2 single-domain delete shape mirrors Plan 03's exactly; the Modulith bidirectional-edge pattern lands cleanly.

## Verification Gate Results

| Gate | Command | Result |
|------|---------|--------|
| Task 1 compile | `./gradlew :backend:core:compileJava` | PASS |
| Task 2 cross-tier compile | `./gradlew :backend:core:compileJava :backend:api:compileJava :backend:api:compileTestJava :backend:core:compileTestJava` | PASS |
| Task 2 — OnboardingStateMachineTest | `./gradlew :backend:api:test --tests "OnboardingStateMachineTest"` | PASS |
| Task 2 — AccountDeletionE2ETest | `./gradlew :backend:api:test --tests "AccountDeletionE2ETest"` | PASS |
| Task 2 — ApplicationModulesTest | `./gradlew :backend:api:test --tests "ApplicationModulesTest"` | PASS |
| Task 3 — OnboardingStepEnumPersistenceTest | `./gradlew :backend:core:test --tests "OnboardingStepEnumPersistenceTest"` | PASS |
| Phase 1.1 contract suite | `./gradlew :backend:api:test --tests "ProblemDetailContractTest" --tests "ApiErrorSchemaTest" --tests "LocaleResolutionTest"` | PASS |

## Stale-reference sweep

```
$ grep -rn 'com\.zeromail\.core\.persistence\.(OnboardingSelection|OnboardingStep)' backend/
(no matches)

$ grep -rn 'com\.zeromail\.core\.account\.OnboardingService' backend/
(no matches)
```

Zero stale references. Other `com.zeromail.core.persistence.*` references (Gmail*) intentionally remain — that domain relocates in Plan 05.

## Self-Check: PASSED

All claimed files exist at the asserted paths; all claimed commit hashes resolve in `git log`.

- `backend/core/src/main/java/com/zeromail/core/onboarding/persistence/OnboardingSelectionEntity.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/onboarding/persistence/OnboardingSelectionRepository.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/onboarding/persistence/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/onboarding/persistence/lowlevel/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/onboarding/model/OnboardingStep.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/onboarding/model/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/onboarding/service/OnboardingService.java` — FOUND (with `deleteSelectionsForCurrentTenant(UUID)`)
- `backend/core/src/main/java/com/zeromail/core/onboarding/service/package-info.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/onboarding/package-info.java` — FOUND with `allowedDependencies = {"tenant", "account", "shared.privacy"}`
- `backend/core/src/main/java/com/zeromail/core/account/package-info.java` — FOUND with `{"tenant", "onboarding", "shared.privacy"}`
- `backend/core/src/test/java/com/zeromail/core/onboarding/OnboardingStepEnumPersistenceTest.java` — FOUND
- `backend/core/src/main/java/com/zeromail/core/persistence/OnboardingSelectionEntity.java` — DELETED
- `backend/core/src/main/java/com/zeromail/core/persistence/OnboardingSelectionRepository.java` — DELETED
- `backend/core/src/main/java/com/zeromail/core/persistence/OnboardingStep.java` — DELETED
- `backend/core/src/main/java/com/zeromail/core/account/OnboardingService.java` — DELETED
- Commits `cb8166b`, `2f25214`, `30d8ae7` — all resolve in `git log`.

## Notes for Plan 05 Executor

Plan 05 will move the gmail domain (`GmailConnectionEntity` + `GmailConnectionRepository` + `GmailConnectionStatus`) and the `RefreshTokenCipher`/`RefreshTokenCryptoConfig` into `core.gmail.{persistence,model,service}.*`, plus rename `TenantConnectionService`/`TenantConnectionView` to `GmailConnectionService`/`GmailConnectionView`. Specific follow-ups from this plan:

1. **Replace the GmailConnectionRepository call in `AccountDeletionController`.** Add `GmailConnectionService.deleteForCurrentTenant(UUID)` to the new gmail service and swap the controller's `gmailRepo.findByTenantId(tenantId).ifPresent(gmailRepo::delete);` line with `gmailConnectionService.deleteForCurrentTenant(tenantId);`. Drop the `GmailConnectionRepository gmailRepo` field + constructor argument.

2. **Replace the TenantRepository call in `AccountDeletionController`.** Add `TenantService.deleteCurrentTenant(UUID)` (or extend the existing tenant service if present) and swap the controller's `tenantRepo.findById(tenantId).ifPresent(tenantRepo::delete);` line with `tenantService.deleteCurrentTenant(tenantId);`. Drop the `TenantRepository tenantRepo` field + constructor argument.

3. **Entity-scan collapse to single root.** Plan 05 should be the last entity move — after gmail entities relocate, every entity lives under `com.zeromail.core.<domain>.persistence`. Per the planner's discretion (D-A1 + Plan 05 brief), the `@EntityScan` + `@EnableJpaRepositories` arrays in BOTH `Application.java` AND `CoreTestApplication.java` can collapse to single root `"com.zeromail.core"` (the legacy `core.persistence` package will be empty after the gmail move).

4. **Modulith allowedDependencies for gmail.** Per D-D4: `gmail -> {tenant, shared.privacy}`. Note the absence of `account` — gmail does NOT depend on account directly. If Plan 05 finds an unexpected gmail-to-account read path, surface as a deviation rather than silently expanding the edge set.

5. **Keep `RefreshTokenCipher` consumer wiring intact.** It moves into `core.gmail.persistence.crypto.*` (per D-C2 — single consumer = gmail) but its API surface and AES-GCM behavior must remain byte-identical. Phase 1's encrypt/decrypt round-trip tests should stay green.

No blockers, no concerns. The transitional bridge in `AccountDeletionController` now has 1 service call + 2 direct repo calls remaining; Plan 05 reduces this to 0 + 0, then Plan 06 finalizes the orchestration shape.

---

*Phase: 01.2-domain-owned-persistence-restructuring*
*Plan: 04*
*Completed: 2026-04-26*
