---
phase: 01.3
plan: 08
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md
autonomous: false
requirements: []
success_criterion_refs: [1, 2, 3, 4, 5, 6, 7]
must_haves:
  truths:
    - "All 5 Wave 0 test files exit 0 (FULL GREEN)."
    - "tsc --noEmit, vitest, i18n:check, Playwright e2e all pass."
    - "proxy.ts as unknown as cast STILL compiles after the full refactor."
    - "Husky pre-commit hook fires on a deliberate git commit attempt with a broken i18n key."
    - "Tailwind 4 class sorting via prettier-plugin-tailwindcss applies on a staged TSX file."
    - "Visual smoke of /, /docs, /docs/getting-started passes manually."
    - "All 7 ROADMAP success criteria literally satisfied."
    - "VALIDATION.md frontmatter flipped to nyquist_compliant: true and wave_0_complete: true."
  artifacts:
    - path: ".planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md"
      provides: "Updated frontmatter + verification map populated with green status"
  key_links:
    - from: "Phase 1.3 closure"
      to: "/gsd-verify-work"
      via: "All success criteria satisfied; ready for verifier subagent"
      pattern: "/gsd-verify-work 01.3"
---

<objective>
Wave 6 — Final verification + cleanup. Run the full automated suite (tsc, vitest, i18n,
Playwright); execute the manual smokes (Husky hook fires, Tailwind class sort applies,
visual landing/docs render); literally verify each of the 7 ROADMAP success criteria;
flip VALIDATION.md frontmatter `nyquist_compliant: true` + `wave_0_complete: true`.

Purpose: Single source-of-truth verification gate. Plan 08 fails closed if anything from
Plans 01-07 regressed or if the proxy.ts deviation note (D-E5) was silently broken.

Output: Phase 1.3 closure-ready; VALIDATION.md updated; ROADMAP entry for Phase 1.3
unchecked items can be ticked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-CONTEXT.md
@.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md
@apps/web/proxy.ts
@apps/web/__tests__/architecture/route-groups.test.ts
@apps/web/__tests__/architecture/feature-folders.test.ts
@apps/web/__tests__/api/isomorphic-shape.test.ts
@apps/web/__tests__/docs/mdx-pipeline.test.ts
@apps/web/__tests__/workspace/workspace-cleanup.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite + literal-verify all 7 ROADMAP success criteria</name>
  <files>.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md</files>
  <read_first>
    .planning/ROADMAP.md (lines 89-101 — Phase 1.3 7 success criteria),
    .planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-VALIDATION.md (current frontmatter + verification map),
    apps/web/proxy.ts (full — confirm `as unknown as` cast still present)
  </read_first>
  <action>
**Step A — Run full automated suite** from repo root:

```bash
pnpm --filter zeromail-web exec tsc --noEmit
pnpm --filter zeromail-web test -- --run
pnpm --filter zeromail-web run i18n:check
pnpm --filter zeromail-web run test:e2e
```

All four commands must exit 0. If any fail, surface as a deviation; do NOT proceed.

**Step B — Run each Wave 0 test file individually** to confirm GREEN:

```bash
pnpm --filter zeromail-web test apps/web/__tests__/architecture/route-groups.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/architecture/feature-folders.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/api/isomorphic-shape.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/docs/mdx-pipeline.test.ts -- --run
pnpm --filter zeromail-web test apps/web/__tests__/workspace/workspace-cleanup.test.ts -- --run
```

All five must exit 0.

**Step C — Confirm proxy.ts cast preservation** (D-E5 invariant):

```bash
grep -c "as unknown as" apps/web/proxy.ts
```

Must return >= 2 (the cast appears in 2 spots: the handleI18n input and output bridges).
If 0, the cast was silently rewritten — surface as a critical deviation per D-E5.

**Step D — Literal-check each ROADMAP success criterion**. For each, confirm the
required artifact:

1. `apps/web/app` is organized with route groups for public, auth, protected — check:
   `ls -d apps/web/app/\(public\) apps/web/app/\(auth\) apps/web/app/\(protected\)`.
   Clean URLs preserved: `/, /login, /onboarding, /settings, /docs, /docs/[slug]` —
   check via `apps/web/app/(public)/page.tsx`, `apps/web/app/(auth)/login/page.tsx`,
   `apps/web/app/(protected)/onboarding/page.tsx`, `apps/web/app/(protected)/settings/page.tsx`,
   `apps/web/app/(public)/docs/page.tsx`, `apps/web/app/(public)/docs/[slug]/page.tsx`.
2. Feature folders with api/components/hooks subfolders — check `apps/web/features/<name>/{api,components,hooks}/`
   for {auth, account, onboarding, gmail, i18n}.
3. Shared UI primitives in components/ui — check `apps/web/components/ui/` exists with shadcn
   primitives. Shared infra in lib/ — check `apps/web/lib/api/{client,errors,schema.d}.ts` + `apps/web/lib/query-client.tsx` exist unchanged.
4. Duplicate workspace artifacts removed — check `apps/web/pnpm-lock.yaml` and
   `apps/web/pnpm-workspace.yaml` absent; root `pnpm-workspace.yaml` has
   `ignoredBuiltDependencies`.
5. Prettier, Husky, lint-staged configured — check `.husky/pre-commit`, `.prettierrc.json`,
   `.prettierignore` exist; root `package.json` has `prepare:"husky"` + `lint-staged` config.
6. lib/api singleton stays at lib/api/client.ts; per-feature endpoints in features/<name>/api/ —
   check `apps/web/features/account/api/me.ts` exists; `lib/api/client.ts` still exports
   `api`. Run `pnpm --filter zeromail-web run generate:api` and confirm
   `apps/web/lib/api/schema.d.ts` regenerates without errors (typed OpenAPI codegen still
   wires).
7. Public landing + multi-page docs scaffolding — check `apps/web/app/(public)/page.tsx`,
   `apps/web/app/(public)/docs/page.tsx`, `apps/web/app/(public)/docs/[slug]/page.tsx`,
   and at least 4 files in `apps/web/content/docs/*.mdx`.

**Step E — Update `01.3-VALIDATION.md`**:
- Flip frontmatter `nyquist_compliant: false` -> `true`.
- Flip frontmatter `wave_0_complete: false` -> `true`.
- Update the `## Validation Sign-Off` section: tick all 6 boxes.
- Update `**Approval:**` line to `approved YYYY-MM-DD by /gsd-execute-phase 01.3 Plan 08`.
- Populate the per-task verification map table with one row per Wave 0 test file (status
  `green`).

**Step F — Verify generate:api round-trip** (ROADMAP #6 — typed OpenAPI generation must
not regress):

```bash
pnpm --filter zeromail-web run generate:api
git status apps/web/lib/api/schema.d.ts
```

If `schema.d.ts` changed compared to its committed version, that's expected only if the
backend OpenAPI surface changed since last regen — not a Phase 1.3 concern. Document the
diff size in SUMMARY.md.
  </action>
  <verify>
    <automated>pnpm --filter zeromail-web exec tsc --noEmit && pnpm --filter zeromail-web test -- --run && pnpm --filter zeromail-web run i18n:check && pnpm --filter zeromail-web run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - All 5 Wave 0 test files exit 0 individually.
    - Full suite (`pnpm --filter zeromail-web test -- --run`) exits 0.
    - `pnpm --filter zeromail-web exec tsc --noEmit` exits 0.
    - `pnpm --filter zeromail-web run i18n:check` exits 0.
    - `pnpm --filter zeromail-web run test:e2e` exits 0 (Playwright LanguageSwitcher smoke green).
    - `apps/web/proxy.ts` contains literal `as unknown as` (cast preserved per D-E5).
    - All 7 ROADMAP success-criteria artifacts verified present (per Step D enumeration).
    - `apps/web/lib/api/schema.d.ts` exists; `pnpm generate:api` round-trip succeeds without errors.
    - `01.3-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`.
    - `01.3-VALIDATION.md` Validation Sign-Off section has all 6 boxes ticked.
  </acceptance_criteria>
  <done>Full automated suite green; ROADMAP criteria verified; VALIDATION.md updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke — Husky hook fires + Tailwind class sort applies + visual landing/docs render</name>
  <what-built>
    Phase 1.3 architecture refactor complete. Three manual checks per
    01.3-VALIDATION.md §"Manual-Only Verifications" — these depend on real git/dev-server
    interaction and cannot be deterministically scripted.
  </what-built>
  <how-to-verify>
    **Check A — Husky pre-commit hook fires on bad i18n** (ROADMAP #5):
    1. From repo root: `pnpm install` (idempotent).
    2. Open `apps/web/messages/vi.json` and DELETE one key that exists in en.json (e.g., remove `landing.continueSetupCta`).
    3. `git add apps/web/messages/vi.json`
    4. `git commit -m "test: deliberate i18n parity drift"`
    5. EXPECTED: commit BLOCKED with `pnpm i18n:check` failure output. The hook output should mention the missing key.
    6. Restore the key. `git add apps/web/messages/vi.json && git commit --amend --no-edit` (or new commit). EXPECTED: commit SUCCEEDS.

    **Check B — Tailwind class sort applies via prettier-plugin-tailwindcss** (ROADMAP #5):
    1. Open any TSX file in `apps/web/app/(public)/`. Add a deliberately misordered className: `<div className="p-4 m-2 flex">test</div>`.
    2. `git add` that file.
    3. `git commit -m "test: tailwind class sort"`
    4. EXPECTED: lint-staged runs Prettier; the className gets rewritten (e.g., `m-2 flex p-4`).
    5. The commit succeeds; verify the rewritten file: `git show HEAD -- <path>` shows sorted classes.
    6. Revert the test edit.

    **Check C — Visual smoke of landing + docs** (ROADMAP #7):
    1. From repo root: `pnpm --filter zeromail-web dev`.
    2. Open http://localhost:3000/ — confirm landing renders with header (Zero Mail logo + Docs link + Sign in link + LanguageSwitcher), heading "Zero Mail", tagline (Vietnamese by default), and "Đăng nhập" CTA button linking to /login.
    3. Click LanguageSwitcher -> English. Page swaps; tagline becomes English; CTA reads "Sign in".
    4. Open http://localhost:3000/docs — confirm list of 2 docs (Getting Started, Privacy in current locale) sorted by order.
    5. Click "Getting Started". Confirm MDX article renders with h1 + body + "Back to docs" link.
    6. Switch language while on /docs/getting-started — confirm content swaps to other locale.
    7. Open http://localhost:3000/docs/non-existent — confirm 404 not crash.
    8. Open http://localhost:3000/docs/../etc/passwd (URL-encoded if needed) — confirm 404 (slug regex blocks).

    Type "approved" if all 3 checks pass; otherwise describe the failure for triage.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer -> running app | Manual visual confirmation depends on dev server boot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.3.08-01 | R (Repudiation) | Phase declared complete with regressed automated check | mitigate | Task 1 acceptance criterion enumerates each automated check; if any exits non-zero the plan fails closed. |
| T-1.3.08-02 | T (Tampering) | Pre-commit hook bypassed via --no-verify | accept | --no-verify is a documented escape hatch for emergencies. Plans never instruct executors to use it; CI re-runs the same checks server-side as a backstop (Phase 1.1 P07 contract). |
| T-1.3.08-03 | T (Tampering) | proxy.ts cast silently rewritten across 8 plans | mitigate | Task 1 Step C grep enforces `as unknown as` literal count >= 2. |
</threat_model>

<verification>
- All 5 Wave 0 tests GREEN.
- All 4 automated suites (tsc, vitest, i18n, Playwright) GREEN.
- 3 manual checks (Husky, Tailwind sort, visual) PASS via human checkpoint.
- proxy.ts cast preserved.
- VALIDATION.md frontmatter flipped.
</verification>

<success_criteria>
Phase 1.3 closes when:
- All 7 ROADMAP success criteria verified literally.
- Automated suite green.
- Manual checkpoints approved.
- VALIDATION.md frontmatter `nyquist_compliant: true` + `wave_0_complete: true`.
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-08-SUMMARY.md` with the verification matrix (each ROADMAP criterion + which artifact + which command).
2. The orchestrator will tick the Phase 1.3 entry in ROADMAP.md and update STATE.md.
3. Recommend `/gsd-verify-work 01.3` for cross-AI review before proceeding to Phase 2A.
</output>