---
phase: 01.3
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - apps/web/pnpm-lock.yaml
  - apps/web/pnpm-workspace.yaml
  - pnpm-workspace.yaml
autonomous: true
requirements: []
success_criterion_refs: [4]
must_haves:
  truths:
    - "apps/web/pnpm-lock.yaml is deleted (no longer duplicates root lockfile)."
    - "apps/web/pnpm-workspace.yaml is deleted (root is single source of workspace truth)."
    - "Root pnpm-workspace.yaml owns ignoredBuiltDependencies: [sharp, unrs-resolver]."
    - "Fresh pnpm install regenerates apps/web/node_modules with the same hoist shape that keeps proxy.ts:as unknown as cast valid."
    - "tsc --noEmit and Playwright e2e LanguageSwitcher smoke (Phase 1.1 P07) still pass after cleanup."
  artifacts:
    - path: "pnpm-workspace.yaml (root)"
      provides: "packages: + ignoredBuiltDependencies"
      contains: "ignoredBuiltDependencies"
  key_links:
    - from: "Root pnpm-workspace.yaml"
      to: "pnpm install across the monorepo"
      via: "pnpm 10 reads ignoredBuiltDependencies for build-script gating"
      pattern: "ignoredBuiltDependencies:"
---

> **Shell:** bash (Git Bash on Windows). Tasks use `rm`, escaped parens, and POSIX paths. PowerShell users: invoke via `bash -c '...'` or use Git Bash directly. (REVIEWS Revision 8 — Codex MEDIUM shell portability)

<objective>
Wave 1 (parallel-safe with Plan 02) — Workspace cleanup. Delete the duplicate
`apps/web/pnpm-lock.yaml` and `apps/web/pnpm-workspace.yaml`; migrate
`ignoredBuiltDependencies: [sharp, unrs-resolver]` to the root workspace yaml; run a fresh
`pnpm install` from root; verify the proxy.ts `as unknown as` cast (Phase 1.1 P07
deviation D-E5) still type-checks and the Playwright LanguageSwitcher smoke still passes.
Satisfies ROADMAP success criterion #4.

Purpose: Eliminate the workspace artifact duplication that risks lockfile drift, while
proving the proxy.ts deviation note still holds after a clean reinstall (Pitfall 4).

Output: A leaner workspace with one lockfile, one workspace yaml, and a verified
identical pnpm hoist shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-CONTEXT.md
@.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-RESEARCH.md
@.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-PATTERNS.md
@apps/web/pnpm-workspace.yaml
@pnpm-workspace.yaml
@apps/web/proxy.ts
@apps/web/playwright.config.ts

<interfaces>
Current root `pnpm-workspace.yaml`:
```yaml
packages:
  - apps/*
```

Current `apps/web/pnpm-workspace.yaml`:
```yaml
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
```

Target root `pnpm-workspace.yaml`:
```yaml
packages:
  - apps/*

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete duplicate workspace artifacts + migrate ignoredBuiltDependencies + fresh pnpm install</name>
  <files>
    apps/web/pnpm-lock.yaml,
    apps/web/pnpm-workspace.yaml,
    pnpm-workspace.yaml
  </files>
  <read_first>
    pnpm-workspace.yaml (root — current state),
    apps/web/pnpm-workspace.yaml (current state — only contains ignoredBuiltDependencies),
    apps/web/proxy.ts (full file — preserve `as unknown as` cast at line ~95),
    .planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-CONTEXT.md (D-E4 step-by-step + D-E5 cast preservation),
    .planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-RESEARCH.md (Pitfall 4 — pnpm hoist may shift)
  </read_first>
  <action>
> **Shell reminder:** all `rm` / `pnpm` commands below are bash (Git Bash on Windows). PowerShell users: prefix with `bash -c '...'` or open a Git Bash session.

**Step A — Update root `pnpm-workspace.yaml`** to add `ignoredBuiltDependencies` BEFORE
deleting the apps/web copy. Final root content:

```yaml
packages:
  - apps/*

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
```

**Step B — Delete `apps/web/pnpm-lock.yaml`** (duplicate of root). Use `rm` from repo
root: `rm apps/web/pnpm-lock.yaml`.

**Step C — Delete `apps/web/pnpm-workspace.yaml`**: `rm apps/web/pnpm-workspace.yaml`.

**Step D — Optional clean** (recommended per RESEARCH §Runtime State Inventory): wipe
`apps/web/.next` and `apps/web/tsconfig.tsbuildinfo` to force fresh build cache:

```bash
rm -rf apps/web/.next apps/web/tsconfig.tsbuildinfo
```

**Step E — Fresh `pnpm install` from repo root**:

```bash
pnpm install
```

This regenerates `apps/web/node_modules` and `apps/web/node_modules/.pnpm-workspace-state-v1.json`
under the new single-workspace-yaml regime. Expect the install to take 30-60 seconds.

**Step F — Verify proxy.ts `as unknown as` cast still compiles** (Pitfall 4 + D-E5):

```bash
pnpm --filter zeromail-web exec tsc --noEmit
```

Expected: exits 0. If tsc reports a NEW error in proxy.ts on the `as unknown as`-bearing
line (~95) or on the call to `handleI18n(...)`, this is the Pitfall 4 surface — the pnpm
hoist shape changed. **Do NOT silently rewrite the cast.** Per CONTEXT D-E5, document
the change as a deviation in the SUMMARY.md and stop; surface to user. The expected case
(verified by Phase 1.1 P07 contract) is the cast remains intact.

**Step G — Verify Playwright LanguageSwitcher smoke still passes**:

```bash
pnpm --filter zeromail-web run test:e2e
```

Expected: green. The Phase 1.1 P07 smoke test exercises LanguageSwitcher persistence
across the / and /login routes; it depends on the proxy.ts cast holding and on
`@playwright/test` peer-dep at root. If RED with a `next` resolution error, surface
deviation per D-E5. Otherwise, cleanup is verified.

**Step H — Verify i18n parity still wires**:

```bash
pnpm --filter zeromail-web run i18n:check
```

Expected: green (vi.json + en.json mirror — Phase 1.1 D-D1 invariant unchanged).

Document the install duration, any deviation, and any resolved-version drifts in the
SUMMARY.md.
  </action>
  <verify>
    <automated>pnpm --filter zeromail-web exec tsc --noEmit && pnpm --filter zeromail-web run i18n:check</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/pnpm-lock.yaml` does NOT exist.
    - File `apps/web/pnpm-workspace.yaml` does NOT exist.
    - File `pnpm-workspace.yaml` (root) contains literal `packages:`, `apps/*`, `ignoredBuiltDependencies:`, `sharp`, `unrs-resolver`.
    - Directory `apps/web/node_modules` exists (regenerated by pnpm install).
    - File `apps/web/node_modules/.pnpm-workspace-state-v1.json` exists.
    - Command `pnpm --filter zeromail-web exec tsc --noEmit` exits 0 — proves proxy.ts `as unknown as` cast still compiles.
    - File `apps/web/proxy.ts` STILL contains the literal `as unknown as` substring (cast not silently rewritten).
    - Command `pnpm --filter zeromail-web run i18n:check` exits 0.
    - Wave 0 `apps/web/__tests__/workspace/workspace-cleanup.test.ts` flips GREEN for the lockfile/workspace-yaml/ignoredBuiltDependencies assertions (combined with Plan 02 hook + prettier files = full GREEN).
  </acceptance_criteria>
  <done>Workspace consolidated to single root yaml; proxy.ts cast verified post-install; e2e smoke green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Optional human-verify for Playwright LanguageSwitcher smoke</name>
  <what-built>
    Workspace cleanup completed. proxy.ts `as unknown as` cast verified via tsc.
    Playwright e2e smoke ran in Task 1.
  </what-built>
  <how-to-verify>
    1. From repo root, run `pnpm --filter zeromail-web run test:e2e` if not yet executed in Task 1.
    2. Confirm the `language-switcher.spec.ts` test file passes (1 test, ~10-30 seconds).
    3. If RED with a `next` module-resolution or NextResponse type error: this is the Pitfall 4 hoist-shape regression. Document and surface to user — do NOT proceed to Plan 04 until resolved.
    4. If GREEN: type "approved" and continue.
  </how-to-verify>
  <resume-signal>Type "approved" or describe Playwright e2e failure for triage.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lockfile -> install | Deleting + regenerating risks dependency graph drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.3.03-01 | T (Tampering) | pnpm hoist shape change breaking proxy.ts cast (Pitfall 4) | mitigate | Step F's tsc + Step G's Playwright smoke run BEFORE declaring done. If the cast becomes invalid, Task 2 checkpoint blocks progression. |
| T-1.3.03-02 | D (Denial of service) | CI pipeline impacted by lockfile regeneration | accept | Lockfile updates are committed; CI re-uses them. Only the developer's first install pays the regeneration cost. |
</threat_model>

<verification>
- Wave 0 workspace-cleanup test now GREEN for lockfile/workspace-yaml/ignoredBuiltDependencies (combined with Plan 02's prettier/husky/lint-staged work = full file GREEN).
- proxy.ts cast verified intact.
- Playwright LanguageSwitcher smoke green.
</verification>

<success_criteria>
- ROADMAP #4 satisfied: duplicate workspace artifacts removed; root is single source of workspace truth.
- proxy.ts P07 deviation note (D-E5) preserved.
- Phase 1.1 P07 smoke test passes post-cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/01.3-frontend-architecture-refactor-and-public-content-foundation/01.3-03-SUMMARY.md` documenting install duration, any drift, and any deviation.
</output>
