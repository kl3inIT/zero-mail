---
phase: 01.3
slug: frontend-architecture-refactor-and-public-content-foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-26
---

# Phase 01.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.x (unit) + Playwright 1.59 (smoke e2e) |
| **Config file** | `apps/web/vitest.config.ts` + `apps/web/playwright.config.ts` |
| **Quick run command** | `pnpm --filter zeromail-web test -- --run` |
| **Full suite command** | `pnpm --filter zeromail-web test -- --run && pnpm --filter zeromail-web run i18n:check && pnpm --filter zeromail-web exec tsc --noEmit && pnpm --filter zeromail-web run test:e2e` |
| **Estimated runtime** | ~30 seconds quick / ~2 minutes full |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter zeromail-web test -- --run`
- **After every plan wave:** Run the full suite command above
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

> Populated by gsd-planner during plan creation. Each task in a PLAN.md gets a row mapping it to its verification command. Wave 0 plan introduces new test files; downstream tasks reference them via `read_first` and produce code that turns those tests green.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _TBD_ | _TBD_ | 0 | _Phase 1.3 architecture-only — no new product REQ-IDs_ | — | _N/A_ | unit | `pnpm --filter zeromail-web test -- --run` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Per `01.3-RESEARCH.md` `## Validation Architecture` (5 test files + 1 manual smoke procedure covering all 7 ROADMAP success criteria):

- [ ] `apps/web/__tests__/architecture/route-groups.test.ts` — verifies `app/(public)`, `app/(auth)`, `app/(protected)` directory structure, that `app/[locale]/` mirror is deleted, that clean URLs (`/`, `/login`, `/onboarding`, `/settings`, `/docs`, `/docs/[slug]`) resolve.
- [ ] `apps/web/__tests__/architecture/feature-folders.test.ts` — verifies `apps/web/features/{auth,account,onboarding,gmail,i18n}/{api,components,hooks}/` exist; verifies no `index.ts` barrels in `features/<name>/`; verifies all 5 relocated components are at their new owner paths.
- [ ] `apps/web/__tests__/api/isomorphic-shape.test.ts` — verifies `features/account/api/me.ts` exports `getCurrentUser({ headers, signal, fetcher })` and that calling with `{ headers: { cookie } }` produces a fetch that includes the Cookie header. Verifies `proxy.ts` and `app/layout.tsx` import this single function (no duplicate raw `fetch('/me')` call sites remain).
- [ ] `apps/web/__tests__/docs/mdx-pipeline.test.ts` — verifies `apps/web/content/docs/*.{vi,en}.mdx` files exist with valid gray-matter frontmatter `{title, slug, order, locale}`; verifies `(public)/docs/page.tsx` lists by current locale + order; verifies `(public)/docs/[slug]/page.tsx` renders the matching MDX file via `next-mdx-remote/rsc` and 404s on locale mismatch or missing slug.
- [ ] `tools/scripts/verify-workspace-cleanup.test.ts` (or root vitest equivalent) — verifies `apps/web/pnpm-lock.yaml` is absent; `apps/web/pnpm-workspace.yaml` is absent; root `pnpm-workspace.yaml` contains `ignoredBuiltDependencies: [sharp, unrs-resolver]`.
- [ ] _(manual smoke)_ Tooling smoke procedure documented in `01.3-RESEARCH.md` — `pnpm install` from root → `pnpm dev` boots → `pnpm test:e2e` LanguageSwitcher persistence smoke (Phase 1.1 P07) still green → pre-commit hook fires on staged file → i18n parity script catches a deliberately-broken key.
- [ ] Vitest test infrastructure additions (if needed): no new framework — vitest 4.1.x already configured; Playwright already configured at root with `testDir: apps/web/__tests__/e2e`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Husky pre-commit hook fires on git commit | ROADMAP success criterion #5 | Git hook execution requires actual `git commit` invocation; not deterministic in vitest unit context. | (1) `pnpm install` from root. (2) Stage a deliberately-malformed `apps/web/messages/vi.json` (drop a key present in en.json). (3) `git commit -m "test: bad i18n parity"`. (4) Expect commit blocked with `pnpm i18n:check` error message. (5) Restore key, commit succeeds. |
| `apps/web/proxy.ts:as unknown as` cast still compiles after fresh pnpm install | Phase 1.1 P07 deviation note (D-E5) | Depends on pnpm hoist shape — must verify after workspace cleanup regenerates node_modules. | After workspace cleanup task: `pnpm install` from root → `pnpm --filter zeromail-web exec tsc --noEmit` → expect zero errors. If cast becomes wrong, surface as deviation per D-E5 — do NOT silently rewrite. |
| Visual smoke of landing + docs scaffold | ROADMAP success criterion #7 | Light skeleton has placeholder copy without final styling — visual confirmation is manual at this stage (final design deferred). | `pnpm dev` from root → open `http://localhost:3000/` → confirm landing renders with header/main/footer slots and CTA "Đăng nhập / Sign in" linking to `/login`. Open `/docs` → see list of 2-3 sample docs filtered by current locale. Open `/docs/getting-started` → see MDX content rendered. Switch language via LanguageSwitcher → docs list and content swap to other locale. |
| Tailwind 4 class sorting via prettier-plugin-tailwindcss applies on staged files | ROADMAP success criterion #5 | Prettier-Tailwind sort behavior is best confirmed visually in a real edit cycle. | Stage a TSX file with deliberately misordered Tailwind classes (`<div className="p-4 m-2 flex">`); commit; expect lint-staged to rewrite to canonical order (`<div className="m-2 flex p-4">`); commit succeeds with sorted classes. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — populated by gsd-planner after plans land.
