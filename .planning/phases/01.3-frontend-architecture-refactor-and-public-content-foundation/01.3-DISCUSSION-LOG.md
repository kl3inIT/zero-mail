# Phase 1.3: Frontend Architecture Refactor and Public Content Foundation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `01.3-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-26
**Phase:** 01.3-frontend-architecture-refactor-and-public-content-foundation
**Areas discussed:** Feature folder decomposition, API + hooks contract (server vs client), `[locale]/` mirror + route group edges, Docs/MDX architecture + landing scope, Tooling depth + workspace cleanup

---

## Feature folder decomposition

### Q1 — Chiến lược chia features/<name>/ ?

| Option | Description | Selected |
|--------|-------------|----------|
| Hybrid: backend-mirror + i18n cắt riêng (Recommended) | features/ = {auth, account, onboarding, gmail, i18n}. Mirror 3 backend domains user-facing + auth + i18n. tenant infra → lib/. | ✓ |
| Pure backend mirror | features/ = {account, onboarding, gmail, tenant}. 1-1 với backend Phase 1.2. | |
| Route-aligned | features/ = {auth, onboarding, settings}. Theo 3 surface user thấy. | |
| Claude's discretion | Default = Hybrid. | |

**User's choice:** Hybrid: backend-mirror + i18n cắt riêng (Recommended)
**Notes:** Pattern locks for Phase 2A→5 — each future phase carves its own feature.

### Q2 — Shared chrome (header / sidebar / global pause toggle / connection status / language switcher) của (protected) layout sống ở đâu?

| Option | Description | Selected |
|--------|-------------|----------|
| app/(protected)/_components/ (Recommended) | Co-locate với route group; underscore prefix → not routed; compose from features/*. | ✓ |
| components/layout/ | Tách ra root components/. | |
| features/shell/ | Tạo feature shell riêng. | |

**User's choice:** app/(protected)/_components/ (Recommended)

### Q3 — Map 5 components hiện có sang đâu?

| Option | Description | Selected |
|--------|-------------|----------|
| Theo đúng feature owner (Recommended) | LanguageSwitcher→i18n, ConnectionHealthBadge+ReconnectPrompt→gmail, DeleteAccountDialog→account, TemplateCard→onboarding. | ✓ |
| Giữ components/ root, chỉ thay đổi pages | Keep flat, only move routes. | |
| Hybrid | Cross-feature → features/i18n/, internal → respective feature, edges case-by-case. | |
| Claude's discretion | Default = đúng feature owner. | |

**User's choice:** Theo đúng feature owner (Recommended)

### Q4 — Tên folder top-level + barrel-export convention?

| Option | Description | Selected |
|--------|-------------|----------|
| features/ + no barrel (deep imports) (Recommended) | features/<name>/{api,components,hooks}/ với deep imports. | ✓ |
| features/ + barrel index.ts | index.ts re-export per feature. | |
| modules/ instead of features/ | Tên khác. | |
| Co-located với app/ | app/(protected)/<name>/_lib/, bỏ features/ hoàn toàn. | |

**User's choice:** features/ + no barrel (deep imports) (Recommended)

---

## API + hooks contract (server vs client)

### Q1 — Mỗi endpoint trong features/<name>/api/<endpoint>.ts có shape nào?

| Option | Description | Selected |
|--------|-------------|----------|
| Isomorphic single function (Recommended) | 1 function với { fetcher?, signal?, headers? } cho cả RSC và CSR. | ✓ |
| Server+Client siblings | 2 functions per endpoint. | |
| Client-only, server fetch ở lớp trên | features/<name>/api/ chỉ CSR; server fetch inline. | |
| Claude's discretion | Default = Isomorphic. | |

**User's choice:** Isomorphic single function (Recommended)

### Q2 — Base openapi-fetch client đặt ở đâu, mỗi feature tự tạo instance hay reuse singleton?

| Option | Description | Selected |
|--------|-------------|----------|
| lib/api/client.ts singleton (Recommended) | Giữ nguyên; mỗi feature import api từ đó. | ✓ |
| Server + client variants ở lib/api/ | client.ts (CSR) + server-client.ts (RSC). | |
| Per-feature client factory | Mỗi features/<name>/api/_client.ts tự factory. | |

**User's choice:** lib/api/client.ts singleton (Recommended)
**Notes:** Aligns with ROADMAP success criterion #6 verbatim ("base openapi-fetch client stays in lib/api").

### Q3 — TanStack Query key factory pattern?

| Option | Description | Selected |
|--------|-------------|----------|
| Per-feature keys.ts (Recommended) | features/<name>/api/keys.ts. Feature-local invalidation. | ✓ |
| Central lib/api/query-keys.ts | Tất cả keys 1 file. | |
| No factory, ad-hoc strings | useQuery({ queryKey: ['account', 'me'] }). | |
| Claude's discretion | Default = per-feature keys.ts. | |

**User's choice:** Per-feature keys.ts (Recommended)

### Q4 — Server-side /me reconciliation đang duplicate trong proxy.ts + layout.tsx — di chuyển vào features/account/api/ ?

| Option | Description | Selected |
|--------|-------------|----------|
| Move vào features/account/api/me.ts (Recommended) | Single getCurrentUser({ cookieHeader, signal }). proxy.ts + layout.tsx đều import. | ✓ |
| Giữ proxy.ts/layout.tsx tự fetch, di chuyển minh bạch | Move helper to lib/locale/server-locale.ts. | |
| Giữ nguyên (status quo) | No change. | |

**User's choice:** Move vào features/account/api/me.ts (Recommended)

---

## [locale]/ mirror + route group edges

### Q1 — [locale]/ mirror tree (app/[locale]/{login,onboarding,settings}) — xử sao?

| Option | Description | Selected |
|--------|-------------|----------|
| Drop hoàn toàn (Recommended) | Xóa app/[locale]/ tree. | ✓ |
| Refactor: localePrefix → 'as-needed' và dùng [locale]/ thật | SEO-friendly URLs. Breaks Phase 1.1 D-B1. | |
| Giữ nguyên (no-op mirror) | Status quo dead code. | |

**User's choice:** Drop hoàn toàn (Recommended)
**Notes:** SEO-localized URLs deferred — would require flipping localePrefix.

### Q2 — /login thuộc route group nào?

| Option | Description | Selected |
|--------|-------------|----------|
| (auth) (Recommended) | app/(auth)/login/page.tsx, minimal centered-card chrome. | ✓ |
| (public) | app/(public)/login/page.tsx, share landing chrome. | |
| Claude's discretion | Default = (auth). | |

**User's choice:** (auth) (Recommended)

### Q3 — / (root) post-refactor behavior?

| Option | Description | Selected |
|--------|-------------|----------|
| Always show public landing (Recommended) | (public)/page.tsx luôn render landing; auth user thấy CTA pointing to next-step. | ✓ |
| Auth-aware redirect | Unauth → landing; auth → /onboarding hoặc /settings. | |
| Giữ redirect('/login') hiện tại | No change. Vi phạm ROADMAP criterion #7. | |

**User's choice:** Always show public landing (Recommended)

### Q4 — Layouts cho 3 route groups — chia thế nào?

| Option | Description | Selected |
|--------|-------------|----------|
| Mỗi group có layout.tsx riêng + shared brand component (Recommended) | (public)/layout.tsx, (auth)/layout.tsx, (protected)/layout.tsx; LanguageSwitcher single source from features/i18n/. | ✓ |
| Chỉ (protected)/layout.tsx, còn lại dùng root | Conditional in root layout. | |
| Per-group layout + 1 shared root khung | Default Next pattern with shared root. | |
| Claude's discretion | Default = mỗi group layout riêng. | |

**User's choice:** Mỗi group có layout.tsx riêng + shared brand component (Recommended)

---

## Docs/MDX architecture + landing scope

### Q1 — MDX library cho docs?

| Option | Description | Selected |
|--------|-------------|----------|
| next-mdx-remote/rsc (Recommended) | Lightweight, RSC-native, no design lock-in. | ✓ |
| Fumadocs | Full docs framework (sidebar/TOC/search OOB). | |
| @next/mdx (Next built-in MDX) | Each .mdx as page route. Vi phạm content/ outside app/. | |
| Claude's discretion | Default = next-mdx-remote/rsc. | |

**User's choice:** next-mdx-remote/rsc (Recommended)
**Notes:** Verify Next 16.2.4 compat via Context7 at install time; fallback = @mdx-js/mdx + @mdx-js/react direct usage.

### Q2 — Vị trí content/ ?

| Option | Description | Selected |
|--------|-------------|----------|
| apps/web/content/docs/ (Recommended) | Co-located with app, outside app/. | ✓ |
| Repo-root content/docs/ | Outside apps/web. Cần config. | |
| apps/web/app/(public)/docs/_content/ | Co-locate route. Vi phạm criterion #7. | |
| Claude's discretion | Default = apps/web/content/docs/. | |

**User's choice:** apps/web/content/docs/ (Recommended)

### Q3 — 'Scaffolding without final design' — cụ thể mức độ nào?

| Option | Description | Selected |
|--------|-------------|----------|
| Light skeleton (Recommended) | Layout slots + placeholder copy + 2-3 sample MDX để prove pipeline. | ✓ |
| Minimum (Hello world) | <h1>Coming soon</h1> + 1 sample MDX. | |
| Full skeleton | Auto-generated sidebar/TOC/breadcrumbs. Vi phạm "no final design". | |
| Claude's discretion | Default = Light skeleton. | |

**User's choice:** Light skeleton (Recommended)

### Q4 — Frontmatter parser + listing convention?

| Option | Description | Selected |
|--------|-------------|----------|
| gray-matter + frontmatter có title/slug/order/locale (Recommended) | Battle-tested; aligns with Phase 1.1 vi/en. | ✓ |
| next-mdx-remote/rsc built-in vfile | Library tự parse. | |
| No frontmatter — slug = filename, order = abc sort | Zero parse. | |
| Claude's discretion | Default = gray-matter. | |

**User's choice:** gray-matter + frontmatter có title/slug/order/locale (Recommended)

---

## Tooling depth + workspace cleanup

### Q1 — Pre-commit hook (Husky+lint-staged) chạy gì trên staged files?

| Option | Description | Selected |
|--------|-------------|----------|
| Tier B: ESLint + Prettier + i18n parity (Recommended) | < 5s; balance fast feedback + Phase 1.1 contract. | ✓ |
| Tier A: ESLint + Prettier only | i18n drift lọt qua local. | |
| Tier C: + tsc + vitest --run staged | 10-30s; --no-verify magnet; known anti-pattern. | |
| Claude's discretion | Default = Tier B. | |

**User's choice:** Tier B: ESLint + Prettier + i18n parity (Recommended)

### Q2 — Husky + lint-staged đặt ở đâu (monorepo root vs apps/web)?

| Option | Description | Selected |
|--------|-------------|----------|
| Root (Recommended) | Husky 9+ monorepo guidance. | ✓ |
| apps/web/ | Local. Vi phạm Husky 9+. | |
| Claude's discretion | Default = root. | |

**User's choice:** Root (Recommended)

### Q3 — Prettier config — single root hay per-package?

| Option | Description | Selected |
|--------|-------------|----------|
| Single root .prettierrc + .prettierignore (Recommended) | 1 source of truth + prettier-plugin-tailwindcss. | ✓ |
| Per-package configs | Drift; copy-paste. | |
| Claude's discretion | Default = single root. | |

**User's choice:** Single root .prettierrc + .prettierignore (Recommended)

### Q4 — Workspace cleanup file list — confirm?

| Option | Description | Selected |
|--------|-------------|----------|
| Xóa + reconsolidate (Recommended) | Delete apps/web/pnpm-lock.yaml + apps/web/pnpm-workspace.yaml; migrate ignoredBuiltDependencies to root pnpm-workspace.yaml; pnpm install + smoke test. | ✓ |
| Xóa duplicate lock, giữ apps/web/pnpm-workspace.yaml | Safer but keeps anomaly. | |
| Inventory lại kỹ hơn trước khi quyết | Run pnpm doctor first. | |
| Claude's discretion | Default = xóa + reconsolidate. | |

**User's choice:** Xóa + reconsolidate (Recommended)
**Notes:** Must verify proxy.ts `as unknown as` peer-permutation cast still compiles after fresh pnpm install.

---

## Claude's Discretion

Areas the planner / executor have flexibility on within the locked decisions:

- Exact `next-mdx-remote/rsc` version (Context7-verify Next 16.2.4 compat).
- Husky 9 install command shape.
- Whether to introduce narrower path aliases (`@/features/*`, `@/lib/*`) or keep `@/*`.
- Whether `app/(protected)/_components/ProtectedHeader.tsx` ships in Phase 1.3 or is created lazily.
- Sample MDX file slugs (defaults: getting-started, privacy, troubleshooting).
- `lib/locale/` vs inline locale helpers.
- Test file relocation strategy (default: feature-internal tests in `features/<name>/__tests__/`; e2e stays at `apps/web/__tests__/e2e/`).
- frontend-design skill invocation timing (must run before any landing/docs UI authoring).

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section. Highlights:

- SEO-localized URLs (`/vi/docs` vs `/en/docs`).
- Fumadocs / full docs framework.
- Auto-generated docs sidebar / TOC / search.
- Tier C pre-commit.
- Per-feature openapi-fetch client factory.
- Auth-aware redirect on `/`.
- MDX rendering on client.
- Refactoring `proxy.ts:as unknown as` cast.
