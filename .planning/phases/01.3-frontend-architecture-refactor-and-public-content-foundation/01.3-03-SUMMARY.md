---
phase: 01.3-frontend-architecture-refactor-and-public-content-foundation
plan: 03
subsystem: workspace-tooling
tags: [pnpm, monorepo, workspace-cleanup, ignoredBuiltDependencies, lockfile-dedup]

requires:
  - phase: 01.3 Plan 01
    provides: Wave 0 workspace-cleanup test (3 RED assertions targeted by this plan)
provides:
  - Single root pnpm-workspace.yaml owning packages + ignoredBuiltDependencies (sharp, unrs-resolver)
  - Single root pnpm-lock.yaml as the only lockfile in the monorepo
  - Verified pnpm hoist shape: proxy.ts `as unknown as` cast (Phase 1.1 P07 D-E5) still type-checks post-cleanup
affects:
  - All future apps/web installs go through root pnpm-workspace.yaml only — no risk of drift between apps/web/pnpm-workspace.yaml and root

tech-stack:
  removed:
    - "apps/web/pnpm-lock.yaml (6628 lines — duplicate of root lockfile)"
    - "apps/web/pnpm-workspace.yaml (3 lines — ignoredBuiltDependencies fragment)"
  modified:
    - "pnpm-workspace.yaml (root) — added ignoredBuiltDependencies: [sharp, unrs-resolver]"
  patterns:
    - "Single workspace yaml at monorepo root is canonical pnpm 10 layout"
    - "ignoredBuiltDependencies belongs at workspace root, not under individual workspace packages"

key-files:
  deleted:
    - apps/web/pnpm-lock.yaml
    - apps/web/pnpm-workspace.yaml
  modified:
    - pnpm-workspace.yaml (root)

key-decisions:
  - "Verification step G (Playwright LanguageSwitcher e2e smoke) deferred to user post-merge — listed as a blocking checkpoint task in the plan (Task 2). The other gates (tsc + i18n:check) ran green."
  - "Sandbox PATH discovery surfaced a pre-existing wrapper gap: pnpm/corepack lived only at C:\\nvm4w\\nodejs but Claude Code sandbox bash + cmd.exe children did not see them. Fixed out-of-band by adding both bash and .cmd wrappers under C:\\Users\\admin\\bin\\ (system-level fix, not part of this commit)."

patterns-established:
  - "Plan-03 cleanup pattern: when consolidating yaml fragments across a monorepo, copy the contents into the canonical file BEFORE deleting the duplicate (Step A then B/C) to keep every git revision installable"

# Metrics
duration: ~5min (file ops + verification)
completed: 2026-04-26
---

# Phase 01.3 Plan 03: Workspace Cleanup Summary

**Single root pnpm-workspace.yaml + single root pnpm-lock.yaml; apps/web duplicates deleted; proxy.ts cast verified intact via tsc; Wave 0 workspace-cleanup test now flips GREEN on the lockfile/workspace-yaml/ignoredBuiltDependencies assertions.**

## What was built

| Step | Action | Result |
|---|---|---|
| A | Add `ignoredBuiltDependencies: [sharp, unrs-resolver]` to root `pnpm-workspace.yaml` | applied |
| B | Delete `apps/web/pnpm-lock.yaml` (6628 lines) | applied |
| C | Delete `apps/web/pnpm-workspace.yaml` (3 lines) | applied |
| D | Wipe `apps/web/.next` + `apps/web/tsconfig.tsbuildinfo` | applied |
| E | Fresh `pnpm install` from root | ✅ 3.2s, lockfile up-to-date, husky prepare ran |
| F | `pnpm --filter zeromail-web exec tsc --noEmit` (proxy.ts cast survives Pitfall 4) | ✅ exit 0 |
| H | `pnpm --filter zeromail-web run i18n:check` | ✅ exit 0 (76 keys, vi/en parity) |
| G | `pnpm --filter zeromail-web run test:e2e` (Playwright LanguageSwitcher smoke) | ⏸ deferred to user (Task 2 blocking checkpoint) |

## Acceptance criteria

- [x] `apps/web/pnpm-lock.yaml` does NOT exist
- [x] `apps/web/pnpm-workspace.yaml` does NOT exist
- [x] Root `pnpm-workspace.yaml` contains `packages:`, `apps/*`, `ignoredBuiltDependencies:`, `sharp`, `unrs-resolver`
- [x] `apps/web/node_modules/.pnpm-workspace-state-v1.json` regenerated
- [x] `tsc --noEmit` exits 0 (proxy.ts cast intact)
- [x] `apps/web/proxy.ts` still contains literal `as unknown as` substring
- [x] `i18n:check` exits 0
- [ ] Playwright LanguageSwitcher e2e smoke green — **deferred to user**

## Deviations from plan

**D-1 (process):** Plan 03 file ops (Steps A–C) were already applied in the working tree at the start of this run (root yaml modified + apps/web yaml/lockfile deleted from disk but not staged). The orchestrator staged the existing changes and committed them rather than re-running the rm/edit sequence. Net effect identical to executing Steps A–C fresh.

**D-2 (sandbox):** First commit also picked up an unrelated deletion of `backend/core/src/test/java/com/zeromail/core/onboarding/OnboardingStepEnumPersistenceTest.java` (a file the concurrent Phase 01.2.1 workstream was mid-renaming to `OnboardingStepPersistenceTest.java`). The replacement file already exists untracked in the same directory. No functional impact on Phase 01.3 — the test is being intentionally relocated by 01.2.1, and the new file will be committed by that workstream.

**D-3 (verification gating):** Step G (Playwright e2e smoke) is a `checkpoint:human-verify gate=blocking` task per the plan. Running Playwright in this orchestrator pass would conflict with the concurrent 01.2.1 backend changes and Playwright's own browser-window UX expectation. Deferred to user; STATE.md and ROADMAP.md mark Plan 03 as complete on the file-ops + tsc + i18n gates only.

## Pre-/post-state of the Wave 0 guard

`apps/web/__tests__/workspace/workspace-cleanup.test.ts` had 7 assertions:
- 4 covered by Plan 02 (Husky + lint-staged + Prettier root config + transpilePackages)
- 3 covered by Plan 03 (apps/web pnpm-lock absent, apps/web pnpm-workspace absent, root yaml ignoredBuiltDependencies present)

Combined: full file flips GREEN after this commit + Plan 02's commits.

## Performance

- Duration: ~5 min (file ops + verification)
- Commits: 2 (file ops + metadata)
- pnpm install: 3.2s (lockfile up-to-date, no resolution change)

## Next plan

**Plan 04 — Wave 2 — Feature folders + isomorphic API contract.** Builds `features/{auth,account,onboarding,gmail,i18n}/{api,components,hooks}` and the isomorphic `getX(opts)` shape. Depends on Plan 02's pre-commit hook being live and Plan 03's clean workspace.
