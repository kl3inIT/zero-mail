---
status: diagnosed
phase: 01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r
source:
  - 01.5-01-SUMMARY.md
  - 01.5-02-SUMMARY.md
  - 01.5-03-SUMMARY.md
  - 01.5-04-SUMMARY.md
  - 01.5-05-SUMMARY.md
  - 01.5-06-SUMMARY.md
  - 01.5-07-SUMMARY.md
  - 01.5-08-SUMMARY.md
started: "2026-04-28T00:44:00+07:00"
updated: "2026-04-28T01:06:00+07:00"
---

## Current Test

[testing complete - 1 blocked item outstanding]

## Tests

### 1. Public Landing Page
expected: Open the public landing page while signed out. The page should load without console/runtime errors, show the Zero Mail value proposition immediately, provide a clear sign-in CTA, and keep the polished Phase 01.5 visual hierarchy without wrapper-component regressions.
result: pass

### 2. Login Error Rendering
expected: Open `/login?error=consent_denied` and `/login?error=gmail_scope_required`. Each known error should render a localized destructive alert. Open `/login?error=anything_else`; the unknown value should not render an alert or unsafe text.
result: issue
reported: |
  Console Error: MISSING_MESSAGE: Could not resolve `auth.login.error.consent_denied.title` in messages for locale `vi`.
severity: blocker

### 3. Bundled Google OAuth First Login
expected: Start Google sign-in from the login page with a clean session. The app should request Google identity plus Gmail modify consent in one Google flow, then return to the app without a second `google-gmail` OAuth leg.
result: pass

### 4. Post-Login Onboarding State
expected: After successful bundled login, the user should not see a `SIGNED_IN` intermediate step. The app should treat Gmail as connected and show the current onboarding/template selection experience or the next valid authenticated destination.
result: pass

### 5. Repeated Same-Account Login
expected: Sign out or restart the browser session, then sign in again with the same Google account. The existing tenant/user/Gmail connection should be reused cleanly; no duplicate-account error, partial onboarding reset, or broken Gmail state should appear.
result: pass

### 6. Settings Gmail Reconnect Flow
expected: In Settings, when Gmail is disconnected or needs reconnect, the reconnect action should navigate by GET to `/tenant/connect-gmail`, then redirect to `/oauth2/authorization/google?reconnect=true` and show Google consent. It should not fail with a 403 CSRF error or 405 method error.
result: blocked
blocked_by: other
reason: "cant setting because cant select template"

### 7. Authenticated Layout `/me` Dedupe
expected: Load an authenticated page that uses the shared layout. Within one page render, `/me` should be fetched once for the same cookie/session rather than duplicated by multiple server components. Cross-request freshness should remain intact after refresh.
result: pass

### 8. Required Secret Fail-Fast
expected: Start `backend/api` without `REFRESH_TOKEN_KEY_BASE64`. Startup should fail with a clear required-secret message and should not attempt any `sm://` Google Secret Manager fallback. Start it again with the env var set; startup should proceed normally.
result: pass

### 9. Onboarding Template Selection
expected: On the authenticated onboarding template selection screen, selecting a template should call the backend successfully and advance/save the selected template without a 403 authorization or CSRF failure.
result: issue
reported: |
  `:8080/onboarding/select-template` returned 403; user cannot select a template.
severity: blocker

## Summary

total: 9
passed: 6
issues: 2
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "Known login error codes render localized destructive alerts without runtime errors; unknown error values render no alert or unsafe text."
  status: failed
  reason: "User reported: MISSING_MESSAGE could not resolve auth.login.error.consent_denied.title for locale vi at app/(auth)/login/page.tsx:58."
  severity: blocker
  test: 2
  root_cause: "LoginPage calls getTranslations('auth.login') and then resolves error.${code}.title, producing auth.login.error.* keys. The message bundles currently store the same copy at auth.error.*. The unit test mock hides this because it returns error.* for any namespace."
  artifacts:
    - path: "apps/web/app/(auth)/login/page.tsx"
      issue: "Uses tLogin as tError and resolves error.${code} under the auth.login namespace."
    - path: "apps/web/i18n/messages/vi.json"
      issue: "Contains auth.error.consent_denied.*, not auth.login.error.consent_denied.*."
    - path: "apps/web/i18n/messages/en.json"
      issue: "Contains auth.error.consent_denied.*, not auth.login.error.consent_denied.*."
    - path: "apps/web/__tests__/pages/login-error-rendering.test.tsx"
      issue: "Mock translator is namespace-insensitive and does not catch the real next-intl namespace mismatch."
  missing:
    - "Either change LoginPage to use getTranslations('auth.error') with ${code}.title/body, or move/alias message keys under auth.login.error in both vi/en."
    - "Update login-error-rendering.test.tsx so the mocked translator respects namespace selection and catches missing nested keys."
- truth: "Selecting an onboarding template succeeds for an authenticated user and advances/saves the selected template."
  status: failed
  reason: "User reported: :8080/onboarding/select-template returned 403; cannot select template."
  severity: blocker
  test: 9
  root_cause: "SecurityConfig uses CookieCsrfTokenRepository.withHttpOnlyFalse() manually, but Spring Security 7's SPA CSRF docs recommend csrf.spa(), which includes cookie token repository plus the request handler needed for raw X-XSRF-TOKEN headers. Current tests run under @Profile('test') with TestSessionSupport disabling CSRF, so the production filter-chain failure is not covered."
  artifacts:
    - path: "apps/web/app/(protected)/onboarding/page.tsx"
      issue: "Template selection submits to /onboarding/select-template but receives 403."
    - path: "apps/web/features/onboarding/api/selectTemplate.ts"
      issue: "Sends X-XSRF-TOKEN only if an XSRF-TOKEN cookie exists and can be resolved by Spring Security."
    - path: "apps/web/lib/api/client.ts"
      issue: "xsrfHeader reads the raw XSRF-TOKEN cookie into X-XSRF-TOKEN, matching SPA conventions."
    - path: "backend/api/src/main/java/com/zeromail/api/security/SecurityConfig.java"
      issue: "Uses CookieCsrfTokenRepository.withHttpOnlyFalse() directly instead of Spring Security 7 csrf.spa() or an equivalent SPA CsrfTokenRequestHandler setup."
    - path: "backend/api/src/test/java/com/zeromail/api/security/TestSessionSupport.java"
      issue: "Test profile disables CSRF, so integration tests do not exercise this production 403 path."
  missing:
    - "Switch SecurityConfig to Spring Security 7 SPA CSRF support, preserving OAuth callback CSRF ignores."
    - "Add a production-security integration/slice test that proves /onboarding/select-template succeeds with a valid XSRF token and fails without one."
