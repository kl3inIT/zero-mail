---
phase: 01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r
secured_at: 2026-04-27
asvs_level: 1
block_on: high
threats_total: 47
threats_closed: 47
threats_open: 0
accepted_risks_count: 13
status: secured
---

# Phase 01.5 Security Audit

Phase: **01.5 — inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r**
Plans audited: 8/8 (5 original + 3 gap-closure 06/07/08)
ASVS level: 1 — block_on: high

This audit verifies every threat declared in the Phase 01.5 PLAN.md threat models against the implemented code. Each `mitigate` threat was verified by reading the cited file and confirming the declared mitigation pattern is present at the right location. `accept` threats were re-checked to ensure the disposition still stands (no implementation regression that would invalidate the acceptance).

## Threat Verification Summary

**Closed:** 47/47 (34 mitigated + 13 accepted)
**Open:** 0
**Unregistered flags:** 1 informational (worker `application.yml` still carries `sm://`, out of scope for Plan 08)

## Threat Register — Mitigations

| Threat ID | Severity | Category | Disposition | Evidence |
|-----------|----------|----------|-------------|----------|
| T-01.5-01-02 | HIGH | Information Disclosure | mitigate | `GoogleOAuthSuccessHandler.java` only logs `event=oauth_no_refresh_token_first_login`, `event=oauth_settings_basic_missing tenantId={uuid}`, `event=oauth_provisioning_complete tenantId={uuid}`. Refresh-token bytes pass straight to `cipher.encrypt(...)` (`OAuthProvisioningService.java:118-120, 144-146`); no logging of token bytes anywhere on the path. |
| T-01.5-01-03 | HIGH | Information Disclosure | mitigate | `LoginRedirectAuthenticationFailureHandler.java:61, 73` logs only `event=login_consent_denied` / `event=login_gmail_scope_missing`. No `oae.getError().getDescription()` interpolation, no email, no Google subject, no tenantId (none exists at failure time). |
| T-01.5-01-04 | HIGH | Elevation of Privilege | mitigate | `GoogleOAuthSuccessHandler.java:113-129`: granted-scope check runs BEFORE any DB write; `OAuth2AuthenticationException("gmail_scope_required")` short-circuits provisioning. |
| T-01.5-01-04b | HIGH | Atomicity / Elevation | mitigate | `OAuthProvisioningService.java:50-63, 138-152`: single `bundledTx = TransactionTemplate(PROPAGATION_REQUIRED)`. Tenant + user + cipher.encrypt + connections.upsert + advanceTo all run inside ONE `bundledTx.executeWithoutResult(...)` call. Any failure rolls back all three. |
| T-01.5-01-05 | MEDIUM | Repudiation | mitigate | `GoogleOAuthSuccessHandler.java:120-124, 139-141` and `LoginRedirectAuthenticationFailureHandler.java:68-72`: both pre-throw and best-effort `removeAuthorizedClient("google", ...)` cleanups. Liquibase 008 (`backend/core/src/main/resources/db/changelog/changes/008-cleanup-gmail-registration-data.yaml`) deletes orphaned `client_registration_id='google-gmail'` rows. |
| T-01.5-01-06 | LOW | Tampering / XSS | mitigate (Plan 02) | See T-01.5-02-01 below. |
| T-01.5-01-10 | MEDIUM | Spoofing / Elevation | mitigate | `GoogleOAuthSuccessHandler.java:131-147`: explicit null check on `client.getRefreshToken()`. First-login null → `OAuth2AuthenticationException("consent_denied")` thrown BEFORE provisioning call. Reconnect null falls through to `OAuthProvisioningService` which preserves existing envelope (`OAuthProvisioningService.java:110-114`). |
| T-01.5-01-11 | LOW | Information Disclosure | mitigate | `GoogleOAuthSuccessHandler.java:149-153, 170-173`: `event=oauth_settings_basic_missing` warning emitted with tenantId after provisioning resolves. Provisioning still succeeds (settings.basic is forward-looking). |
| T-01.5-02-01 | HIGH | Tampering / XSS | mitigate | `apps/web/app/(auth)/login/page.tsx:33-38, 56`: `KNOWN_ERROR_CODES = [...] as const` + `isKnownError` typeguard. Unknown values render no Alert. RSC searchParams awaited. |
| T-01.5-02-02 | MEDIUM | Information Disclosure | mitigate | i18n bundles (`apps/web/i18n/messages/{vi,en}.json`) carry only generic copy ("Zero Mail needs Gmail access ..."); no scope strings, no provider error message echo. |
| T-01.5-02-03 | MEDIUM | Tampering | mitigate | `pnpm i18n:check` STRICT pre-commit gate (Husky + lint-staged) verified active in repo conventions; bundle parity enforced. |
| T-01.5-02-04 | LOW | Tampering | mitigate | grep for `errors.gmail.identity|errors.gmail.consent|gmail.identity.mismatch|gmail.consent.denied` across `apps/web/` returned zero matches; ESLint+tsc would also fail on unresolved imports. |
| T-01.5-02-09 | MEDIUM | Tampering | mitigate | INFO-9: pre-delete grep guard verified empty across `apps/web/` (no orphan consumers of deleted i18n keys). |
| T-01.5-02-10 | MEDIUM | Tampering | mitigate | MED-4: Plan 02 ran wave 2 with `depends_on: [01.5-01]`. Plan 01 backend collapse landed before Plan 02 frontend deletes — codebase never in a state where frontend missing what backend still produced. Verified by phase summary commit ordering. |
| T-01.5-03-01 | HIGH | Information Disclosure | mitigate | `apps/web/features/account/api/me.ts:40, 64-72`: `cache: 'no-store'` preserved on the underlying fetch. `cache()` is per-render-pass only — verified by RESEARCH §D citing Next.js 16.2.4 docs. |
| T-01.5-03-02 | HIGH | Tampering | mitigate | `apps/web/features/account/api/me.ts:1`: `import { cache } from 'react'`. Grep confirms zero `next/cache` imports anywhere in `me.ts`. |
| T-01.5-03-03 | HIGH | Spoofing | mitigate | `apps/web/features/account/api/me.ts:64-72`: `cache(async (cookieHeader: string \| undefined) => ...)` — primitive string arg, value-equality cache key. Different users have different cookie strings → no shared cache entry. Regression guard test: `apps/web/__tests__/features/account/me-cache-dedupe.test.ts:76-86`. |
| T-01.5-03-03b | HIGH | Spoofing | mitigate | Same as T-01.5-03-03; the WR-03 fix specifically lands the primitive-string contract. |
| T-01.5-03-04 | MEDIUM | Information Disclosure | mitigate | `apps/web/lib/api/client.ts:38` retains `as unknown as LooseClient` cast (Phase 01.3 D-E5 lock). Plan-acceptance grep tolerates ≥1 occurrence at the locked site. |
| T-01.5-04-01 | LOW | Tampering | mitigate | `apps/web/app/(auth)/login/page.tsx` uses semantic tokens (`text-foreground`, `text-muted-foreground`, `buttonVariants`) — zero raw color literals (`bg-blue-600`, `text-red-500`, etc.) introduced. |
| T-01.5-04-02 | MEDIUM | Scope creep | mitigate | Polish constrained to spacing/typography hierarchy; no logo/palette/typography-pairing/hero changes (verified by manual review checkpoint). |
| T-01.5-04-03 | LOW | Information Disclosure | mitigate | Polish microcopy is generic ("Zero Mail needs Gmail access ..."); no scope strings or internal error echo. |
| T-01.5-04-04 | MEDIUM | Tampering | mitigate | `pnpm i18n:check` pre-commit gate active. |
| T-01.5-04-05 | LOW | Tampering | mitigate | `TemplateCard` uses `ring-primary` (D-D3) — confirmed via 01.5-04-SUMMARY surface scan. |
| T-01.5-05-01 | MEDIUM | Tampering | mitigate | Convention examples in `CLAUDE.md` reference live files (`TenantStatusController.java`, `MeResponse.java`, `UserEntity.java`, `OnboardingStep.java`, `GoogleOAuthSuccessHandler.java`); each path resolves and contains the cited pattern. |
| T-01.5-05-02 | LOW | Information Disclosure | mitigate | All convention examples use UUIDs and opaque event names; anti-pattern blocks intentionally show forbidden patterns without real PII. |
| T-01.5-05-03 | MEDIUM | Spoofing | mitigate | All four convention blocks (thin controllers, records-vs-entities, OrderedEnum, privacy logging) reference grep-verified patterns in current codebase. |
| T-01.5-05-04 | LOW | Tampering | mitigate | STATE.md blocker bullets preserved + new entries appended without duplication. |
| T-01.5-05-05 | MEDIUM | Repudiation | mitigate | Each new blocker entry includes name + owning phase + verification protocol. |
| T-01.5-06-01 | (gap) | Tampering | mitigate | `OAuthProvisioningService.java:153-164`: `DataIntegrityViolationException` race-loser branch is read-only — calls `users.findByGoogleSubject(...)` and returns winner's tenantId/userId. NO second transaction, NO writes after race. |
| T-01.5-07-02 | (gap) | Tampering | mitigate | `GoogleOAuthSuccessHandler.java:120-121, 139-141`: pre-throw `removeAuthorizedClient("google", token.getName())` ensures no partial AuthorizedClient lingers. `token.getName()` used (not `req.getUserPrincipal()` which is null pre-commit to SecurityContext). |
| T-01.5-07-03 | (gap) | Information Disclosure | mitigate | `GoogleOAuthSuccessHandler.java:122-124, 141`: `catch (Exception ignored)` on the cleanup — never logs principal name (privacy contract D-E1). |
| T-01.5-08-01 | (gap) | Information Disclosure | mitigate | `backend/api/build.gradle.kts` contains zero `spring-cloud-gcp` dependencies. Repo-wide grep over `**/*.{kts,toml,yml,yaml,java}` for `spring-cloud-gcp` returns no matches. |
| T-01.5-08-02 | (gap) | Denial of Service | mitigate | `backend/api/src/main/resources/application.yml:73`: `${REFRESH_TOKEN_KEY_BASE64:?REFRESH_TOKEN_KEY_BASE64 must be supplied via deployment secret source ...}` — `:?` fail-fast pattern with clear error message. |
| T-01.5-08-03 | (gap) | Tampering | mitigate | `backend/api/src/main/resources/application.yml`: zero `sm://` references; placeholder removed entirely. (Note: `backend/worker/src/main/resources/application.yml:10` still has `sm://` — out of scope for Plan 08; see Unregistered Flag below.) |

## Accepted Risks

The following threats are accepted (not mitigated by code). Each was re-verified to ensure no implementation regression invalidated the acceptance.

| Threat ID | Severity | Category | Rationale |
|-----------|----------|----------|-----------|
| T-01.5-01-01 | LOW | Tampering | `?reconnect=true` worst-case is `prompt=consent` on first login — extra friction, no persistence side effect. `GoogleAuthorizationRequestResolver.java:67` uses strict `"true"` equality. |
| T-01.5-01-07 | LOW | Spoofing | Spring Security 7 default OAuth2 state validation preserved verbatim. `SecurityConfig.java` change is handler-only. |
| T-01.5-01-08 | LOW | Denial of Service | Liquibase 008/009 against pre-launch dev DB only; idempotent + 0-row no-op on clean DB. |
| T-01.5-01-09 | LOW | Information Disclosure | Spring's default failure response uses redirect URL, error description internal log only. |
| T-01.5-02-05 | LOW | Tampering | D-C4 deferred ESLint raw-color-class ban; manual review + frontend-design skill discipline. |
| T-01.5-02-06 | LOW | Tampering | `unstable_retry` API drift documented (STATE.md line 151); future Next.js upgrade phase will adapt. |
| T-01.5-02-07 | LOW | Information Disclosure | Standard `getTranslations()` (RSC) / `useTranslations()` (client) split — no cross-boundary leak. |
| T-01.5-02-08 | LOW | Spoofing | Dark-mode toggle deferred (Phase 5); tokens are dark-ready but no `data-theme` flip wired. |
| T-01.5-03-05 | LOW | Denial of Service | Standard React `cache()` rejection semantics — rejections re-throw per render pass; next render gets fresh attempt. |
| T-01.5-04-06 | LOW | Spoofing | Stale dev-server port — documented hazard (STATE.md lines 133, 141). |
| T-01.5-05-06 | (LOW) | (HIGH-6 GCP-baseline conflict) | Closed by Plan 08 — see T-01.5-08-01..03 mitigations. Disposition stands. |
| T-01.5-06-02 | (gap) | Denial of Service | Postgres unique constraint is the fence; race-loser fast-returns winner's state. |
| T-01.5-07-01 | (gap) | Elevation of Privilege | `ConnectGmailController.java:18` is `@GetMapping("/tenant/connect-gmail")` behind `authenticated()` filter (`SecurityConfig.java:25-27` — `.anyRequest().authenticated()`). Unauthenticated users get 302 to login. Actual mutation (refresh-token rotation) happens on OAuth callback which has CSRF state. |

## Unregistered Flags (informational)

| Flag | Location | Disposition |
|------|----------|-------------|
| `sm://` placeholder still present in worker config | `backend/worker/src/main/resources/application.yml:10` | **Out of scope for Plan 08** — Plan 08 declared scope as `backend/api/src/main/resources/application.yml` only. Same threat class as T-01.5-08-03; recommend a follow-up cleanup phase to apply the same `:?` fail-fast pattern to the worker application.yml. Not a blocker — the worker is not part of Phase 01.5's scope and the threat is documented for the next sweep. |

## Audit Trail

- Threat sources read: `01.5-01-PLAN.md` through `01.5-08-PLAN.md` (8 plans, ~47 threats).
- Implementation files re-verified against declared mitigations:
  - `backend/api/src/main/resources/application.yml` (REFRESH_TOKEN_KEY_BASE64 fail-fast, no `sm://`, single `google` registration with bundled scopes)
  - `backend/api/src/main/java/com/zeromail/api/security/{SecurityConfig, GoogleOAuthSuccessHandler, LoginRedirectAuthenticationFailureHandler, GoogleAuthorizationRequestResolver, OAuthScopes}.java`
  - `backend/api/src/main/java/com/zeromail/api/controllers/ConnectGmailController.java` (now `@GetMapping`)
  - `backend/api/build.gradle.kts` (no `spring-cloud-gcp`)
  - `backend/core/src/main/java/com/zeromail/core/account/service/OAuthProvisioningService.java` (single-tx PROPAGATION_REQUIRED, race-loser read-only)
  - `backend/core/src/main/java/com/zeromail/core/onboarding/model/OnboardingStep.java` (SIGNED_IN dropped)
  - `backend/core/src/main/resources/db/changelog/changes/{008-cleanup-gmail-registration-data, 009-drop-signed-in-onboarding-step}.yaml`
  - `apps/web/app/(auth)/login/page.tsx` (KNOWN_ERROR_CODES + isKnownError typeguard)
  - `apps/web/features/account/api/me.ts` (`cache from 'react'`, primitive cookie key)
  - `apps/web/proxy.ts` + `apps/web/lib/api/client.ts` (`as unknown as` lock preserved)
  - `apps/web/__tests__/features/account/me-cache-dedupe.test.ts` (regression guard)
  - `apps/web/i18n/messages/{vi,en}.json` (orphan keys removed — verified by grep returning zero matches)
- Repo-wide grep `spring-cloud-gcp|spring.cloud.gcp` over `**/*.{kts,toml,yml,yaml,java}`: zero matches.
- Repo-wide grep `sm://` over `**/*.{kts,toml,yml,yaml,java,properties}`: 1 match in `backend/worker/src/main/resources/application.yml` (out of scope, see Unregistered Flag).
- Repo-wide grep `errors.gmail.(identity|consent)|gmail.identity.mismatch|gmail.consent.denied` over `apps/web/`: zero matches.
- Cross-checked against existing audit reports: `01.5-REVIEW.md` (4 BLOCKERs CR-01..04 closed by gap plans 06/07/08), `01.5-REVIEW-FIX.md` (5 WARNINGs WR-01,03,04,05,07 closed; WR-06 deferred), `01.5-VERIFICATION.md` (5/5 must-haves passed).
- Privacy contract spot-check: every log statement on the OAuth path emits opaque `event=...` names; none interpolate email, Google subject, refresh-token bytes, or OAuth error descriptions. Tenant context is the only stable identifier and is logged as a UUID.

## Status

**SECURED** — All 47 declared threats are closed (34 mitigated + 13 accepted). One informational unregistered flag (worker `sm://`) is documented for follow-up but does not block Phase 01.5. Phase ships clean.
