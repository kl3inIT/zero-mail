---
phase: "01.5"
plan: 9
subsystem: uat-gap-closure
tags: [uat, i18n, csrf, spring-security-7, next-intl]
gap_closure: true
key_files:
  created:
    - backend/api/src/test/java/com/zeromail/api/security/OnboardingCsrfIntegrationTest.java
  modified:
    - apps/web/app/(auth)/login/page.tsx
    - apps/web/__tests__/pages/login-error-rendering.test.tsx
    - backend/api/src/main/java/com/zeromail/api/security/SecurityConfig.java
    - .planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-09-PLAN.md
provides:
  - "Login error messages resolve from auth.error.* instead of auth.login.error.*"
  - "Namespace-sensitive login-error test mock catches future next-intl namespace drift"
  - "Spring Security 7 SPA CSRF support via csrf.spa()"
  - "Real security-chain onboarding CSRF regression test"
metrics:
  duration: "24min"
  completed: "2026-04-28T09:55:00+07:00"
  tasks_completed: 2
  files_changed: 5
code_commit: 4893144
---

# Phase 01.5 Plan 09: UAT Gap Closure Summary

## Result

Closed both blocker gaps found by `01.5-UAT.md`.

## Changes

- Fixed `/login?error=consent_denied` and `/login?error=gmail_scope_required` by resolving dynamic error copy from `auth.error.*`, matching the real `vi`/`en` message bundles.
- Updated `login-error-rendering.test.tsx` so the mocked `getTranslations(namespace)` is namespace-sensitive and catches this class of mismatch.
- Replaced manual `CookieCsrfTokenRepository.withHttpOnlyFalse()` wiring with Spring Security 7 `csrf.spa()` while preserving OAuth callback CSRF ignores.
- Added `OnboardingCsrfIntegrationTest`, using the real security filter chain, to prove:
  - `/onboarding/select-template` rejects POST without CSRF.
  - `/onboarding/select-template` accepts SPA `XSRF-TOKEN` cookie + `X-XSRF-TOKEN` header and persists the selection.

## Verification

- `pnpm --dir apps/web run i18n:check` passed.
- `pnpm --dir apps/web exec vitest run __tests__/pages/login-error-rendering.test.tsx` passed.
- `pnpm --dir apps/web exec tsc --noEmit` passed.
- `pnpm --dir apps/web run lint` passed.
- `./gradlew.bat :backend:api:test --tests com.zeromail.api.security.OnboardingCsrfIntegrationTest --warning-mode all` passed.
- `./gradlew.bat :backend:api:check --warning-mode all` passed.
- JetBrains project build passed.
- JetBrains file-problem checks on changed Java/TS files returned no problems.

## Notes

- Spring Security 7 documentation confirms `csrf.spa()` creates SPA CSRF defaults with cookie-based token storage and a request handler that resolves the raw header token.
- `01.5-UAT.md` still records the original user-reported failures for auditability. Manual retest should confirm login error rendering, onboarding template selection, and then the previously blocked Settings reconnect flow.

