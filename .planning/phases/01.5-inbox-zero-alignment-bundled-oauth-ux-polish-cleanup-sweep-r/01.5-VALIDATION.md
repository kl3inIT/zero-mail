---
phase: 01.5
slug: inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-27
---

# Phase 01.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (backend)** | JUnit 5 + Spring Boot Test + Testcontainers (Postgres) — `PostgresContainerTest` harness |
| **Framework (frontend)** | Vitest + Testing Library + jsdom |
| **Config file (backend)** | `backend/build.gradle.kts` test task; `PostgresContainerTest` base class |
| **Config file (frontend)** | `apps/web/vitest.config.ts` |
| **Quick run command (backend)** | `./gradlew :backend:api:test --tests '*OAuth*'` |
| **Quick run command (frontend)** | `pnpm --filter web test --run` |
| **Full suite command** | `./gradlew test && pnpm --filter web test --run && pnpm --filter web tsc --noEmit && pnpm --filter web lint && pnpm --filter web i18n:check` |
| **Estimated runtime** | ~120 seconds (backend ~80s, frontend ~30s, i18n+tsc+lint ~10s) |

---

## Sampling Rate

- **After every task commit:** Run quick command relevant to slice (backend → backend gradle test; frontend → vitest)
- **After every plan wave:** Run full suite command (gradle test + vitest + tsc + lint + i18n:check)
- **Before `/gsd-verify-work`:** Full suite must be green; manual `pnpm dev` walkthrough for polish surfaces
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

> Filled by /gsd-plan-phase planner per plan task. Each row maps a plan task to an automated check.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 01.5-01 | 1 | AUTH-01/AUTH-02 (pivot) | T-01.5-01-04 | Wave 0 RED-by-design — bundled-scope config + Liquibase 008/009 + 3 RED test scaffolds | Wave 0 scaffold | `grep -c "google-gmail" backend/api/src/main/resources/application.yml` (=0) + file existence checks (008/009 yaml + 3 test classes) | ✅ pre-staged | ⬜ pending |
| 01-T2 | 01.5-01 | 1 | AUTH-01/AUTH-02 (pivot) | T-01.5-01-02, T-01.5-01-04, T-01.5-01-05 | Bundled OAuth implementation — atomic provisioning + scope short-circuit + SIGNED_IN drop + 6-file delete | Integration test | `./gradlew :backend:api:test --tests '*GoogleAuthorizationRequestResolverTest' --tests '*LoginRedirectAuthenticationFailureHandlerTest' --tests '*BundledGoogleOAuthIntegrationTest'` | ✅ written | ⬜ pending |
| 01-T3 | 01.5-01 | 1 | AUTH-01/AUTH-02 (pivot) | All §T-01.5-01-* | Full backend gate — modulith + ArchUnit + MultiTenantLeak intact | Full check | `./gradlew clean check` | ✅ existing | ⬜ pending |
| 02-T1 | 01.5-02 | 1 | AUTH-01/AUTH-02 (pivot) | T-01.5-02-01, T-01.5-02-02 | Wave 0 RED-by-design — Alert warning variant + globals.css tokens + login-error-rendering vitest scaffold (4 cases including tamper-guard) | Wave 0 scaffold | `grep -c "warning:" apps/web/components/ui/alert.tsx` (>=1) + `grep -c "\-\-warning" apps/web/app/globals.css` (>=4) + `ls apps/web/__tests__/pages/login-error-rendering.test.tsx` + `pnpm tsc --noEmit` | ✅ pre-staged | ⬜ pending |
| 02-T2 | 01.5-02 | 1 | AUTH-01/AUTH-02 (pivot) | T-01.5-02-01, T-01.5-02-03, T-01.5-02-04 | Atomic deflation — 5 primitives deleted + ~16 consumers refactored + RSC login + i18n vi+en parity + TemplateCard `ring-primary` per D-D3 | Vitest + tsc + lint + i18n | `cd apps/web && pnpm tsc --noEmit && pnpm lint && pnpm i18n:check && pnpm test --run` + surface-scan grep returns 0 + `grep -c "ring-primary" apps/web/features/onboarding/components/TemplateCard.tsx` (>=1) | ✅ written (RED-by-design GREENs after this task) | ⬜ pending |
| 03-T1 | 01.5-03 | 2 | AUTH-01/AUTH-02 (pivot) | T-01.5-03-01, T-01.5-03-02 | Wave 0 RED-by-design — me-cache-dedupe vitest (3 cases: identical-opts dedupe, different-opts no-dedupe, no-store preserved) | Wave 0 scaffold | `ls apps/web/__tests__/features/account/me-cache-dedupe.test.ts` + `pnpm tsc --noEmit` | ✅ pre-staged | ⬜ pending |
| 03-T2 | 01.5-03 | 2 | AUTH-01/AUTH-02 (pivot) | T-01.5-03-02, T-01.5-03-04 | React `cache()` wrap — single-line export change with proxy.ts cast preserved | Vitest | `cd apps/web && pnpm test --run --testPathPattern me-cache-dedupe` + full suite + `grep -c "as unknown as" apps/web/proxy.ts` (>=2) | ✅ written (RED-by-design GREENs after this task) | ⬜ pending |
| 04-T1 | 01.5-04 | 2 | AUTH-01/AUTH-02 (pivot) | T-01.5-04-01, T-01.5-04-02 | Polish landing + login via frontend-design skill — raw shadcn + token-aware preserved | Vitest + tsc + lint + i18n | `cd apps/web && pnpm tsc --noEmit && pnpm lint && pnpm i18n:check && pnpm test --run` + grep guards (no deleted wrappers, no raw color literals) | ⬜ N/A (asserts post-edit invariants) | ⬜ pending |
| 04-T2 | 01.5-04 | 2 | AUTH-01/AUTH-02 (pivot) | T-01.5-04-01, T-01.5-04-04, T-01.5-04-05 | Polish onboarding + settings + ReconnectPrompt — TemplateCard `ring-primary` preserved | Vitest + tsc + lint + i18n | Same gate set + `grep -c "ring-primary" apps/web/features/onboarding/components/TemplateCard.tsx` (>=1) + `grep -c 'variant=\"warning\"' apps/web/features/gmail/components/ReconnectPrompt.tsx` (>=1) | ⬜ N/A (asserts post-edit invariants) | ⬜ pending |
| 04-T3 | 01.5-04 | 2 | D-D1 | T-01.5-04-02 | Manual `pnpm dev` walkthrough — visual quality verification across 5 polish surfaces | Manual | User runs `pnpm dev`, walks 5 surfaces, types "approved" | ⬜ N/A (manual gate) | ⬜ pending |
| 05-* | 01.5-05 | 3 | AUTH-01/AUTH-02 (pivot) | — | Cleanup sweep — privacy convention promotion + REVIEW.md doc tasks | Doc-only | (deferred to Plan 05 — no automated check beyond standard tsc/lint pass) | ⬜ N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] No new test framework installation needed — JUnit 5 + Vitest already in tree.
- [x] `BundledGoogleOAuthIntegrationTest` (or equivalent) class file under `backend/api/src/test/java/com/zeromail/api/security/` — exercises both happy path (full scope grant → user/tenant/Gmail row written) and partial-grant short-circuit (no DB write, redirect URL). **Owned by Plan 01 Task 1.**
- [x] `GmailScopeRequestResolverTest` — verify `prompt=consent` toggle via runtime signal (query param per RESEARCH.md §A). **Owned by Plan 01 Task 1 as `GoogleAuthorizationRequestResolverTest` (renamed per resolver rename in Task 2).**
- [x] Vitest test for `app/(auth)/login/page.tsx` `searchParams` error rendering — covers `consent_denied`, `gmail_scope_required`, no-error. **Owned by Plan 02 Task 1 as `apps/web/__tests__/pages/login-error-rendering.test.tsx` (4 cases including tamper-guard).**
- [x] Vitest test for `getCurrentUser` `cache()` dedupe — within one request invocation, only one underlying fetch. **Owned by Plan 03 Task 1 as `apps/web/__tests__/features/account/me-cache-dedupe.test.ts` (3 cases: identical-opts dedupe, different-opts no-dedupe, no-store preserved).**
- [x] `LoginRedirectAuthenticationFailureHandlerTest` — added during Plan 01 Task 1 to cover failure-handler branch coverage (access_denied → consent_denied; gmail_scope_required → removeAuthorizedClient + redirect; default → super).

*Reuse `PostgresContainerTest` base class + `MultiTenantLeakIntegrationTest` ScopedValue/Spring Security wiring per Phase 01.2.1 P03.*

*All Wave 0 scaffolds are RED-by-design at write-time and will GREEN after the corresponding implementation task in the same plan. `wave_0_complete` flag flips to `true` only after `/gsd-execute-phase` runs Wave 0 tasks live.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `frontend-design` skill output applied to landing | D-D1 | Visual quality is design judgment, not assertable | `pnpm dev` → load `/` → confirm visual hierarchy/spacing distinct from Claude-default; compare screenshot to skill output recommendations |
| `frontend-design` skill output applied to login | D-D1 | Same | `pnpm dev` → load `/login` (with and without `?error=...`); verify Card + error Alert composition |
| `frontend-design` skill output applied to onboarding | D-D1 | Same | `pnpm dev` → walk through provisioned account at `/onboarding` |
| `frontend-design` skill output applied to settings | D-D1 | Same | `pnpm dev` → load `/settings`; verify single-account note + sections |
| `frontend-design` skill output applied to ReconnectPrompt | D-D1 | Same | Trigger DISCONNECTED state via dev-DB UPDATE; verify `<Alert variant="warning">` rendering |
| Bundled OAuth happy path (real Google account) | AUTH-01 (pivot) | Requires real consent screen | Sign in with disposable Google account in dev profile; verify single consent screen, lands on `/onboarding` template selector, `users.onboarding_step='GMAIL_CONNECTED'` |
| Bundled OAuth deny path (real Google account) | AUTH-01 (pivot) | Requires real consent screen | Click "Cancel" on consent screen; verify redirect to `/login?error=consent_denied`, no DB writes |
| Reconnect flow (`/tenant/connect-gmail` → `prompt=consent`) | AUTH-02 (pivot) | Requires real Google session | After successful login, manually `DELETE FROM gmail_connections...`; click ReconnectPrompt; verify `prompt=consent` shown |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (Plan 04 manual checkpoint and Plan 05 doc-only tasks excepted by design)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (each plan's tasks alternate scaffold/implementation with gates)
- [x] Wave 0 covers all MISSING references (login-error-rendering test, me-cache-dedupe test, BundledGoogleOAuthIntegrationTest, GoogleAuthorizationRequestResolverTest, LoginRedirectAuthenticationFailureHandlerTest)
- [x] No watch-mode flags (`pnpm test --run` and `./gradlew test` are one-shot)
- [x] Feedback latency < 120s (full suite ~120s per Test Infrastructure table)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending (flips to "approved" after `/gsd-execute-phase` Wave 0 runs and all RED scaffolds GREEN as expected)
