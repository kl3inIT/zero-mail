---
phase: 01.5
plan: 04
type: execute
wave: 3
depends_on: [01.5-02]
files_modified:
  - apps/web/app/(public)/page.tsx
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(protected)/onboarding/page.tsx
  - apps/web/app/(protected)/settings/page.tsx
  - apps/web/features/gmail/components/ReconnectPrompt.tsx
autonomous: false
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "Pivot of AUTH-01/AUTH-02 — no new requirement IDs added in this phase"
    - "Plan 04 is the SOLE place where the frontend-design skill is mandated (MED-5 review fix). Plan 02 (deflation) is structural and does NOT invoke the skill; Plan 04 (polish) IS the visual-design pass and MUST invoke the skill before each surface refactor (D-D1 + feedback_frontend_design_skill memory)"
    - "frontend-design skill is invoked BEFORE refactoring JSX on each of the 5 polish surfaces (per-surface invocation, not batched)"
    - "Polish lifts visual quality from Claude-default to professional/distinctive within existing token taxonomy — NO final brand identity (logo, palette finalize, typography pairings, hero illustrations stay Phase 5)"
    - "All polish edits use raw shadcn primitives + token-aware classNames — zero re-introduction of deflated wrappers, zero raw color literals"
    - "tsc + lint + i18n:check + vitest GREEN at plan boundary"
    - "Manual pnpm dev walkthrough on each surface confirms visible quality lift; reviewer can articulate the design intent change per surface"
  artifacts:
    - path: "apps/web/app/(public)/page.tsx"
      provides: "Polished landing surface with frontend-design-skill output applied"
    - path: "apps/web/app/(auth)/login/page.tsx"
      provides: "Polished login surface — Card composition + error Alert layout refined"
    - path: "apps/web/app/(protected)/onboarding/page.tsx"
      provides: "Polished onboarding template-selection flow"
    - path: "apps/web/app/(protected)/settings/page.tsx"
      provides: "Polished settings — section rhythm + footer single-account-note + danger-zone visual hierarchy"
    - path: "apps/web/features/gmail/components/ReconnectPrompt.tsx"
      provides: "Polished warning Alert composition"
  key_links:
    - from: "executor agent"
      to: "frontend-design skill"
      via: "per-surface skill invocation BEFORE writing/refactoring JSX (D-D1 + MED-5 review fix — Plan 04 is the SOLE skill-invocation site)"
      pattern: "(skill invocation logged in commit message or summary)"
    - from: "polish surfaces"
      to: "shadcn primitives + token taxonomy"
      via: "raw composition only — no wrapper re-introduction"
      pattern: "PageShell|SectionCard|StatusAlert|EmptyState|LoadingState"
---

<objective>
Apply the `frontend-design` skill across the five polish surfaces locked by D-D1: landing (`app/(public)/page.tsx`), login (`app/(auth)/login/page.tsx`), onboarding (`app/(protected)/onboarding/page.tsx`), settings (`app/(protected)/settings/page.tsx`), and `features/gmail/components/ReconnectPrompt.tsx`. The executor MUST invoke the `frontend-design` skill BEFORE authoring or refactoring JSX on each surface (per `feedback_frontend_design_skill` memory + D-D1 lock). Apply the skill output (visual hierarchy, spacing rhythm, layout cues, microcopy refinement) on top of the deflated raw-shadcn baseline shipped in Plan 02.

**MED-5 review fix scope clarification:** Plan 04 is now the SOLE place in Phase 01.5 where the `frontend-design` skill is invoked. Plan 02 (deflation) explicitly does NOT invoke the skill — it's a structural 1:1 swap. The CONTEXT D-D1 mandate ("MUST invoke before authoring/refactoring JSX") and the locked feedback memory `feedback_frontend_design_skill` are honored HERE, in Plan 04, where visual-design decisions ARE the deliverable.

Purpose: Lift visual quality from the post-deflation Claude-default look to "professional, distinctive" within the existing token taxonomy. Phase 5 owns final brand identity (logo, brand-color palette finalize, typography pairings, hero illustrations) — this plan does NOT touch those.

Output: Five surfaces polished and merged into main; manual `pnpm dev` walkthrough checkpoint confirms visible quality lift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-CONTEXT.md
@.planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-RESEARCH.md
@.planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-PATTERNS.md
@.planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-VALIDATION.md
@.planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-REVIEWS.md
@CLAUDE.md
@apps/web/AGENTS.md
@apps/web/app/(public)/page.tsx
@apps/web/app/(auth)/login/page.tsx
@apps/web/app/(protected)/onboarding/page.tsx
@apps/web/app/(protected)/settings/page.tsx
@apps/web/features/gmail/components/ReconnectPrompt.tsx
@apps/web/components/ui/alert.tsx
@apps/web/components/ui/card.tsx
@apps/web/components/ui/button.tsx
@apps/web/app/globals.css
@apps/web/i18n/messages/vi.json
@apps/web/i18n/messages/en.json

<interfaces>
<!-- Tokens + primitives available for polish (no new installs allowed). -->

Tokens (apps/web/app/globals.css):
- Base: --background, --foreground, --card, --card-foreground, --popover, --popover-foreground
- Brand-leaning: --primary, --primary-foreground, --secondary, --secondary-foreground, --accent, --accent-foreground
- Status: --destructive, --destructive-foreground, --warning, --warning-foreground (D-C3 added)
- Structural: --muted, --muted-foreground, --border, --input, --ring
- Charts/sidebar: --chart-{1..5}, --sidebar-*

shadcn primitives in tree (apps/web/components/ui/):
- alert (default | destructive | warning), badge, button + buttonVariants, card (Card/Header/Title/Description/Content/Footer), dialog, input, separator, skeleton

Out-of-scope additions (from CONTEXT Out-of-scope):
- No new shadcn primitives (Toast, Sheet, Tabs, DropdownMenu, Popover, Command-Palette deferred)
- No final brand color flips (palette finalize is Phase 5)
- No logo, hero illustrations, custom 404 art
- No dark-mode toggle UI (tokens stay dark-ready)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish landing + login surfaces with frontend-design skill</name>
  <read_first>
    - apps/web/app/(public)/page.tsx (current deflated shape post-Plan 02)
    - apps/web/app/(auth)/login/page.tsx (current deflated shape post-Plan 02 — RSC searchParams + Card + Alert)
    - apps/web/app/globals.css (token inventory)
    - apps/web/components/ui/{alert,card,button}.tsx (primitive surface)
    - apps/web/i18n/messages/{vi,en}.json (microcopy currently in tree — auth.login.* + landing.* namespaces)
    - .planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-CONTEXT.md (D-D1 + Specifics §"Polish ≠ brand finalize")
    - .planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-REVIEWS.md (MED-5 — Plan 04 is the SOLE skill-invocation site)
    - apps/web/AGENTS.md (Next.js 16 doc-first directive)
  </read_first>
  <action>
    **MANDATORY (D-D1 + MED-5 review fix — Plan 04 is the SOLE skill-invocation site in Phase 01.5):** invoke the `frontend-design` skill BEFORE writing any JSX on EITHER surface. Pass the skill: (a) a screenshot or description of the current deflated landing+login surfaces; (b) the token inventory above; (c) the constraint list (no new primitives, no brand finalize, raw shadcn + token-aware className only).

    Apply the skill's output across:

    **Landing (`apps/web/app/(public)/page.tsx`):**
    - Refine visual hierarchy: hero heading scale, body copy line-length, primary CTA prominence, secondary navigation/links spacing.
    - Apply spacing rhythm: consistent vertical scale (e.g., space-y-6 / space-y-8 / space-y-12) tuned to surface density.
    - Improve layout cues: container widths, breakpoints, whitespace breathing.
    - Refine microcopy if the skill flags Claude-default phrasing; surface those edits as i18n bundle changes (vi+en parity required — `pnpm i18n:check` strict). If microcopy changes, update both `apps/web/i18n/messages/vi.json` AND `en.json` in the same commit.
    - **Out of scope:** logo, hero illustration, brand color palette flip. If the skill suggests these, defer them to Phase 5 and document in SUMMARY.

    **Login (`apps/web/app/(auth)/login/page.tsx`):**
    - Refine the auth Card composition: heading/sub-heading rhythm, Google sign-in button visual weight, safety/policy microcopy treatment, LanguageSwitcher placement.
    - Refine the error Alert layout when present: space between Alert and Card, visual hierarchy that doesn't dominate the auth form.
    - Preserve closed-enum tamper-guard from Plan 02 (`KNOWN_ERROR_CODES` + `isKnownError`).
    - Preserve RSC `searchParams: Promise<{error?: string}>` signature.

    **Constraints (mandatory across both surfaces):**
    - Raw shadcn primitives only — zero re-introduction of `PageShell`/`SectionCard`/`StatusAlert`/`EmptyState`/`LoadingState`.
    - Token-aware classNames only — zero raw color literals (`text-stone-700`, `bg-white`, `text-gray-500`, `border-amber-500`, etc.). Use `text-foreground`, `bg-background`, `text-muted-foreground`, `border-border`, etc.
    - No new shadcn primitive installs.
    - Privacy contract preserved (any new microcopy emits no scope strings, token state, etc.).

    **Run gates after each surface:**
    - `cd apps/web && pnpm tsc --noEmit`
    - `cd apps/web && pnpm lint`
    - `cd apps/web && pnpm i18n:check` (strict)
    - `cd apps/web && pnpm test --run` (login-error-rendering and any other vitest must stay GREEN)

    **Commit message convention:** `feat(01.5-04): polish landing + login surfaces via frontend-design skill`. Document in commit body: "frontend-design skill output applied" + 1-line summary of the visual changes.
  </action>
  <verify>
    <automated>
      grep -rn 'PageShell|SectionCard|StatusAlert|EmptyState|LoadingState' 'apps/web/app/(public)/page.tsx' 'apps/web/app/(auth)/login/page.tsx'
      # Expected: 0 matches (deflation preserved)

      grep -rn 'border-amber|bg-amber|text-stone|bg-stone|text-gray|bg-gray|border-blue|bg-blue' 'apps/web/app/(public)/page.tsx' 'apps/web/app/(auth)/login/page.tsx'
      # Expected: 0 matches (token-aware discipline)

      cd apps/web && pnpm tsc --noEmit
      # Expected: exits 0

      cd apps/web && pnpm lint
      # Expected: exits 0

      cd apps/web && pnpm i18n:check
      # Expected: exits 0

      cd apps/web && pnpm test --run
      # Expected: all tests pass
    </automated>
  </verify>
  <acceptance_criteria>
    - Both surfaces use raw shadcn primitives only — zero references to deleted wrappers (grep returns 0)
    - Both surfaces use token-aware classNames — zero raw color literals (grep returns 0 for the listed patterns)
    - `apps/web/app/(auth)/login/page.tsx` still contains `searchParams: Promise` (RSC signature preserved)
    - `apps/web/app/(auth)/login/page.tsx` still contains `KNOWN_ERROR_CODES` and `isKnownError` (tamper-guard preserved)
    - If microcopy changed, both `apps/web/i18n/messages/vi.json` and `en.json` were updated in same commit (i18n:check strict)
    - `pnpm tsc --noEmit`, `pnpm lint`, `pnpm i18n:check`, `pnpm test --run` all GREEN
    - Commit message references frontend-design skill invocation
  </acceptance_criteria>
  <done>Landing + login surfaces polished, frontend-design skill output applied per D-D1 (Plan 04 is the SOLE skill-invocation site per MED-5), all gates GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Polish onboarding + settings + ReconnectPrompt with frontend-design skill</name>
  <read_first>
    - apps/web/app/(protected)/onboarding/page.tsx (current deflated shape post-Plan 02)
    - apps/web/app/(protected)/settings/page.tsx (current deflated shape post-Plan 02 — 5 Card chains)
    - apps/web/features/gmail/components/ReconnectPrompt.tsx (current Alert variant=warning shape post-Plan 02)
    - apps/web/features/onboarding/components/TemplateCard.tsx (consumed by onboarding)
    - apps/web/i18n/messages/{vi,en}.json (auth.onboarding.* + settings.* + connectionHealth.* namespaces)
    - .planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-CONTEXT.md (D-D1)
    - .planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-REVIEWS.md (MED-5)
  </read_first>
  <action>
    **MANDATORY (D-D1 + MED-5):** invoke the `frontend-design` skill BEFORE writing any JSX on EACH of the three surfaces (skill invocation per surface, NOT once for all three — surface contexts differ enough that batching the skill call dilutes its output).

    **Onboarding (`apps/web/app/(protected)/onboarding/page.tsx`):**
    - Polish the template-selection step (the only step shown post-collapse since SIGNED_IN was dropped in Plan 01).
    - Refine: heading hierarchy, TemplateCard grid spacing, "Continue" CTA prominence, in-progress state, empty/loading transitions.
    - Preserve `cn(buttonVariants(...))` plain-button pattern (STATE.md line 153 vitest @base-ui boundary lock).
    - Preserve `TemplateCard.tsx:25` `ring-primary` (Plan 02 sets this per D-D3 — do NOT touch; brand swap belongs to Phase 5).

    **Settings (`apps/web/app/(protected)/settings/page.tsx`):**
    - Polish the 5 Card sections (account, language, gmailConnection, privacy, dangerZone).
    - Refine: section-heading rhythm, CardFooter `singleAccountNote` treatment, `<Separator />` rhythm, danger-zone visual distinction (consider `<Alert variant="destructive">` or token-aware Card border-destructive — pick whichever the skill recommends; preserve raw shadcn discipline).
    - Preserve max-width container, single-account-note copy from Phase 01.4 D-D5.

    **ReconnectPrompt (`apps/web/features/gmail/components/ReconnectPrompt.tsx`):**
    - Polish the `<Alert variant="warning">` composition. Refine the icon (still inline SVG per STATE.md line 149) + AlertTitle rhythm + AlertAction button placement.
    - Preserve `useTranslations` (client component) + plain `<button className={cn(buttonVariants(...))}>` (STATE.md line 153).
    - The reconnect button still posts to `/tenant/connect-gmail` via `<form method="post">` (Plan 01 INFO-8 verb lock).

    **Constraints (mandatory across all three surfaces):**
    - Raw shadcn only.
    - Token-aware classNames only.
    - No new primitives.
    - i18n vi+en parity if microcopy changes.

    **Run gates after each surface.** Same gates as Task 1.

    **Commit message:** `feat(01.5-04): polish onboarding + settings + ReconnectPrompt via frontend-design skill`.
  </action>
  <verify>
    <automated>
      grep -rn 'PageShell|SectionCard|StatusAlert|EmptyState|LoadingState' 'apps/web/app/(protected)/onboarding/page.tsx' 'apps/web/app/(protected)/settings/page.tsx' apps/web/features/gmail/components/ReconnectPrompt.tsx
      # Expected: 0 matches

      grep -rn 'border-amber|bg-amber|text-stone|bg-stone|text-gray|bg-gray|border-blue|bg-blue' 'apps/web/app/(protected)/onboarding/page.tsx' 'apps/web/app/(protected)/settings/page.tsx' apps/web/features/gmail/components/ReconnectPrompt.tsx
      # Expected: 0 matches

      grep -c "ring-primary" apps/web/features/onboarding/components/TemplateCard.tsx
      # Expected: >= 1 (D-D3 — Plan 02 set this; polish must not regress)

      grep -cE "ring-ring|ring-blue-600" apps/web/features/onboarding/components/TemplateCard.tsx
      # Expected: 0 (D-D3 mandates ring-primary)

      grep -c "variant=\"warning\"" apps/web/features/gmail/components/ReconnectPrompt.tsx
      # Expected: >= 1 (preserved from Plan 02)

      cd apps/web && pnpm tsc --noEmit
      cd apps/web && pnpm lint
      cd apps/web && pnpm i18n:check
      cd apps/web && pnpm test --run
      # All expected: exits 0 / GREEN
    </automated>
  </verify>
  <acceptance_criteria>
    - All three surfaces use raw shadcn primitives only (grep returns 0 for deleted wrappers)
    - Token-aware classNames only (grep returns 0 for raw color literals)
    - `TemplateCard.tsx` still uses `ring-primary` (per D-D3; Plan 02 set this — polish must not regress)
    - `ReconnectPrompt.tsx` still uses `<Alert variant="warning">` (preserved from Plan 02)
    - `pnpm tsc --noEmit`, `pnpm lint`, `pnpm i18n:check`, `pnpm test --run` all GREEN
    - Commit message references frontend-design skill invocation per surface
  </acceptance_criteria>
  <done>Onboarding + settings + ReconnectPrompt polished, frontend-design skill output applied per surface, all gates GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual pnpm dev walkthrough — visual quality verification across 5 polish surfaces</name>
  <what-built>Five surfaces polished using the `frontend-design` skill across Tasks 1-2 within the existing token taxonomy. Visual quality is design judgment, not assertable — VALIDATION.md lists this as a manual gate.</what-built>
  <how-to-verify>
    Spin up dev server and walk through each surface:

    1. Start dev: `cd apps/web && pnpm dev`. (If port 3000 is held by stale `next dev`, kill the PID and retry — STATE.md lines 133, 141 documented this hazard.)
    2. **Landing**: navigate to http://localhost:3000/. Confirm:
       - Visual hierarchy is distinct from Claude-default (clear heading scale, body copy rhythm).
       - Primary CTA prominence reads correctly.
       - Spacing feels intentional, not arbitrary.
    3. **Login**: navigate to http://localhost:3000/login. Confirm:
       - Card composition (heading + body + Google button + safety microcopy) reads with intentional rhythm.
       - Then navigate to http://localhost:3000/login?error=consent_denied. Confirm:
         - `<Alert variant="destructive">` renders above the Card with refined spacing.
         - Title/body i18n strings render in the active locale (vi default; toggle via LanguageSwitcher to test en).
       - Repeat with http://localhost:3000/login?error=gmail_scope_required.
       - Verify http://localhost:3000/login?error=javascript:alert(1) renders no Alert (closed-enum guard).
    4. **Onboarding**: sign in (or set a dev-DB session manually). Navigate to http://localhost:3000/onboarding. Confirm:
       - Template-selection step renders directly (no SIGNED_IN intermediate step).
       - TemplateCard grid spacing + selection ring (`ring-primary` per D-D3) reads cleanly.
       - "Continue" CTA prominence is appropriate.
    5. **Settings**: navigate to http://localhost:3000/settings. Confirm:
       - 5 sections (account, language, gmailConnection, privacy, dangerZone) each render with intentional heading/body rhythm.
       - Single-account-note CardFooter reads as supplementary, not primary.
       - Danger-zone (delete account) is visually distinct without being alarming on default state.
    6. **ReconnectPrompt**: trigger DISCONNECTED state via dev-DB UPDATE on `gmail_connections` (set `status='DISCONNECTED'`). Reload onboarding/settings to surface ReconnectPrompt. Confirm:
       - `<Alert variant="warning">` renders with `--warning` token color (amber-ish in light mode, dark-amber in dark via `.dark` selector).
       - "Reconnect Gmail" button renders correctly with `buttonVariants({ variant: 'outline', size: 'sm' })` and posts to `/tenant/connect-gmail` (POST verb per Plan 01 INFO-8).
    7. **Locale toggle**: use LanguageSwitcher to flip vi ↔ en on each surface. Confirm:
       - All polish microcopy renders correctly in both bundles.
       - No untranslated keys leak (no raw key paths visible).

    For each surface, the reviewer should be able to articulate one design intent change ("the heading scale was tightened so body copy reads first" / "the danger zone uses border-destructive token instead of solid background to keep it visually subordinate to active sections" / etc.). If a surface still looks Claude-default to the reviewer, the polish task did not land — return to Tasks 1-2 with skill output corrections.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Plan 04 polish complete, OR describe specific issues per surface (e.g., "login error Alert is too dominant, reduce visual weight"; "settings danger-zone needs more separation from privacy section").</resume-signal>
  <action>Manual verification — see <how-to-verify> above. The executor pauses for the user to run pnpm dev and walk through each polish surface. No code changes happen in this task; user approval gates plan completion.</action>
  <verify>
    <manual>User runs through the 5-surface walkthrough described in <how-to-verify> and types "approved" or specific issue feedback into chat.</manual>
  </verify>
  <done>User typed "approved" after walking through landing, login (3 error-param variants), onboarding, settings, and ReconnectPrompt across both vi and en locales. Reviewer articulated one design-intent change per surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Designer (frontend-design skill output) → executor implementation | Skill recommendations are design suggestions; executor must respect token taxonomy + raw shadcn discipline |
| User (manual reviewer) → polish acceptance | Subjective design judgment gate (no automated equivalent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-01.5-04-01 | Tampering | frontend-design skill output suggesting raw color literals or new primitives | LOW | mitigate | Plan constraints make skill output recommendations subordinate to token-aware + raw-shadcn rules. Acceptance criterion grep enforces zero raw color literals and zero deleted-wrapper imports. |
| T-01.5-04-02 | Scope creep | Polish drifting into Phase 5 brand finalize (logo, palette finalize, typography pairings, hero illustrations) | MEDIUM | mitigate | CONTEXT Out-of-scope explicitly forbids these. Manual checkpoint reviewer rejects any polish that introduces brand-locked elements. |
| T-01.5-04-03 | Information Disclosure | Polish microcopy leaking scope strings, token state, internal errors | LOW | mitigate | Privacy contract enforced verbatim. Any microcopy change must read as user-facing generic phrasing. |
| T-01.5-04-04 | Tampering | i18n bundle drift introduced by microcopy edits | MEDIUM | mitigate | `pnpm i18n:check` STRICT pre-commit gate blocks vi/en parity violations. |
| T-01.5-04-05 | Tampering | TemplateCard ring flipped from `ring-primary` (D-D3) back to `ring-ring` or a raw literal mid-polish | LOW | mitigate | Acceptance criterion grep confirms `ring-primary` present and `ring-ring` / `ring-blue-600` absent. D-D3 locks the brand-leaning token so Phase 5 brand swap propagates. |
| T-01.5-04-06 | Spoofing | Stale dev-server port 3000 holding mid-polish builds | LOW | accept | Documented hazard (STATE.md lines 133, 141). Manual checkpoint instructions include kill-and-retry guidance. |
</threat_model>

<verification>
- All five surfaces use raw shadcn primitives + token-aware classNames only.
- `frontend-design` skill invoked per surface (surfaceable in commit messages or Plan 04 SUMMARY) — Plan 04 is the SOLE skill-invocation site per MED-5 review fix.
- All automated gates GREEN: tsc, lint, i18n:check, vitest.
- Manual checkpoint approved: reviewer confirms visual quality lift on each of the 5 surfaces.
- No new shadcn primitives installed.
- No final brand identity introduced.
</verification>

<success_criteria>
- D-D1 implemented: full `frontend-design` skill pass across the 5 surfaces, scoped per MED-5 review fix to Plan 04 only.
- ROADMAP Phase 1.5 success criterion 4 (polish lift on top of deflated baseline) closed.
- ROADMAP Phase 1.5 success criterion 5 (REVIEW carryover — surface polish) closed in part (Plan 05 closes the docs/blockers parts).
- Manual checkpoint approved by user.
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-inbox-zero-alignment-bundled-oauth-ux-polish-cleanup-sweep-r/01.5-04-SUMMARY.md` documenting:
- Per-surface design intent changes (the reviewer should have articulated these during the manual checkpoint).
- Microcopy changes (if any) with vi+en parity confirmation.
- Confirmation that no new primitives installed and no brand finalize introduced.
- Confirmation that frontend-design skill was invoked PER SURFACE in Plan 04 (and NOT in Plan 02 — MED-5 scope rule).
- Threat-model dispositions executed.
- Manual checkpoint approval marker.
</output>
</content>
</invoke>