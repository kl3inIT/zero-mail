---
phase: "01.5"
plan: "05"
subsystem: "docs"
tags: [conventions, documentation, state-management, deferred-ceremonies]
dependency_graph:
  requires: [01.5-01, 01.5-02, 01.5-03, 01.5-04]
  provides: [CLAUDE.md-conventions, STATE.md-blockers]
  affects: [all-future-plans]
tech_stack:
  added: []
  patterns:
    - "Thin controllers + service-owned @Transactional"
    - "Records for DTOs, classes for entities, Lombok-free"
    - "OrderedEnum/IdentifiedEnum + fromId fail-loud state machines"
    - "Privacy logging: event= opaque name + tenantId UUID"
key_files:
  created: []
  modified:
    - CLAUDE.md
    - .planning/STATE.md
decisions:
  - "Convention examples reference stable file paths + real code excerpts verified via Read before pasting (T-01.5-05-01 mitigation)"
  - "No 5th convention added — exactly 4 conventions per D-D4 all have verified pattern-coverage in current codebase"
  - "Pub/Sub OIDC ceremony (a) keeps GCP-specific language — Pub/Sub IS GCP-only (correct, not HIGH-6 violation)"
  - "Ceremonies (b) and (c) use generic deployment-secret language per HIGH-6 fix — Docker secrets / systemd credentials / locked-down env files"
metrics:
  duration: "12min"
  completed: "2026-04-27"
  tasks_completed: 2
  files_modified: 2
---

# Phase 01.5 Plan 05: CLAUDE.md Conventions + STATE.md Deferred Ceremonies Summary

Documentation-only plan: populated CLAUDE.md `## Conventions` with 4 verified conventions from REVIEW.md §4, and appended 3 deferred verification ceremonies to STATE.md blockers section with HIGH-6 generic deployment-secret language.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Populate CLAUDE.md ## Conventions with 4 conventions | 200b349 | CLAUDE.md |
| 2 | Append 3 deferred verification ceremonies to STATE.md | c862aca | .planning/STATE.md |

## Convention Example File Paths (verified live)

All example paths verified via `Read` tool before writing:

| Convention | Example File | Verified |
|-----------|--------------|---------|
| 1. Thin controllers | `backend/api/src/main/java/com/zeromail/api/controllers/TenantStatusController.java` | Yes — `toResponse(GmailConnectionView)` pattern confirmed |
| 2. Records for DTOs | `backend/api/src/main/java/com/zeromail/api/dto/account/MeResponse.java` | Yes — `record MeResponse(...)` confirmed |
| 2. Classes for entities | `backend/core/src/main/java/com/zeromail/core/account/persistence/UserEntity.java` | Yes — `class UserEntity extends AbstractTenantOwnedEntity` confirmed |
| 3. OrderedEnum + fromId | `backend/core/src/main/java/com/zeromail/core/onboarding/model/OnboardingStep.java` | Yes — `implements OrderedEnum`, `fromId(String)` with `NoSuchElementException` confirmed |
| 4. Privacy logging | `backend/api/src/main/java/com/zeromail/api/security/GoogleOAuthSuccessHandler.java` | Yes — `log.info("event=oauth_provisioning_complete tenantId={}", result.tenantId())` confirmed |

## Convention Candidate Evaluation

REVIEW.md §4 surfaced exactly 4 conventions. All 4 have verified pattern-coverage in the current codebase — no candidate was rejected or downgraded to "emerging".

## STATE.md Blocker Entries Added (verbatim)

**(a) Pub/Sub OIDC verification ceremony** (Phase 2A push-receiver):

> Verification protocol: receive a synthetic Google Pub/Sub push; assert (a) JWT signature validates against Google's JWKS, (b) `aud` claim matches the configured public push endpoint URL on the VPS, (c) `email` claim matches the configured Pub/Sub service-account principal. Note: Pub/Sub itself remains GCP-only (Gmail push delivery is a Google service), but Spring receives the push as a plain HTTPS POST on the VPS — no `spring-cloud-gcp` Pub/Sub starter needed for receiving.

**(b) Refresh-token key rotation drill** (Phase 2C or dedicated security-ceremony phase):

> Verification protocol: deploy v2 key alongside v1 in the deployment secret source (current VPS baseline: Docker secrets / systemd credentials / locked-down env files; future production options may include GCP Secret Manager, AWS Secrets Manager, or HashiCorp Vault); verify multi-version decrypt path reads `key_version` byte from envelope and selects correct key; rotate v1 → v2 + re-encrypt all rows; verify v1 envelopes still decrypt during overlap window. Per CLAUDE.md TL;DR ("No GCP hosting baseline; do not add spring-cloud-gcp starters by default"), the drill must be deployment-source-agnostic.

**(c) Production cookie `secure: true` profile override + `REFRESH_TOKEN_KEY_BASE64` deployment secret resolution** (Phase 6 launch hardening):

> Verification protocol: assert `application-prod.yml` overrides `server.servlet.session.cookie.secure: true`; assert `REFRESH_TOKEN_KEY_BASE64` resolves successfully from the configured deployment secret source in prod profile (Docker secret / systemd credential / env file mounted via the VPS deployment pipeline; possible future production options: GCP Secret Manager, AWS Secrets Manager, HashiCorp Vault); assert app fails-fast at boot if the secret is missing (no fallback to plain env-var in prod). Per CLAUDE.md TL;DR, no GCP-specific resolution is required by default.

**HIGH-6 compliance:** Ceremonies (b) and (c) use generic "deployment secret source" language. `spring-cloud-gcp-secretmanager` and `sm://` URI patterns do NOT appear in the new bullets. GCP Secret Manager is mentioned only as one possible future production option alongside AWS Secrets Manager and HashiCorp Vault. Ceremony (a) keeps GCP-specific language — Pub/Sub is GCP-only by nature and this is correct.

## No Code Changes

This plan is documentation-only per CONTEXT Specifics ("CLAUDE.md Conventions promotion is documentation-only"). No Java, TypeScript, or configuration files were modified.

## Threat Model Dispositions Executed

| Threat ID | Description | Disposition |
|-----------|-------------|-------------|
| T-01.5-05-01 | Stale example excerpts | Mitigated — all file paths verified via Read before pasting |
| T-01.5-05-02 | PII in example excerpts | Mitigated — examples use UUIDs and opaque event names only |
| T-01.5-05-03 | Claimed pattern not in codebase | Mitigated — all 4 convention examples read from live files |
| T-01.5-05-04 | Duplicate blocker bullets | Mitigated — existing 3 bullets preserved verbatim; 3 new added |
| T-01.5-05-05 | Vague blocker entries | Mitigated — each entry: name + owning phase + verification protocol |
| **T-01.5-05-06** | **GCP baseline lock conflict in STATE.md** | **Mitigated — HIGH-6 fix applied: generic deployment-secret language for ceremonies (b) + (c)** |

## Deviations from Plan

None — plan executed exactly as written. Parallel-agent edits to CLAUDE.md and AGENTS.md were stashed before Task 1, then restored via `git stash pop` after commits.

## Self-Check: PASSED

- CLAUDE.md ## Conventions section: 4 sub-headings confirmed (`grep -c` returned >= 1 each)
- Placeholder "Conventions not yet established." removed: `grep -c` returned 0
- STATE.md new bullets: all 3 ceremony names confirmed present
- `spring-cloud-gcp-secretmanager` / `sm://` absent from new bullets: grep returned 0
- Existing STATE.md blockers preserved: all 3 original bullets confirmed present
- Commits 200b349 and c862aca confirmed in git log
