---
phase: 09-user-settings-ui-on-curated-catalog
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  - backend/core/src/main/resources/db/changelog/changes/094-assistant-settings-phase9-columns.yaml
  - backend/core/src/main/resources/db/changelog/changes/095-assistant-knowledge-snippet-unique-title.yaml
  - backend/core/src/main/resources/db/changelog/changes/096-safety-net-pattern-kind-and-audit-badge.yaml
  - backend/core/src/main/resources/db/changelog/changes/097-user-byok-key-table.yaml
  - backend/core/src/main/java/com/zeromail/core/chat/persistence/AssistantSettingsEntity.java
  - backend/core/src/main/java/com/zeromail/core/chat/persistence/AssistantKnowledgeMemoryEntity.java
  - backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationEntity.java
  - backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokKeyEntity.java
  - backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokKeyRepository.java
  - backend/core/src/test/java/com/zeromail/core/chat/settings/SettingsVoiceServiceWordBoundsTest.java
  - backend/core/src/test/java/com/zeromail/core/chat/settings/AssistantSettingsTonePresetCheckTest.java
  - backend/core/src/test/java/com/zeromail/core/chat/sanitize/PersonalizationSanitizerSingleCallSiteTest.java
  - backend/core/src/test/java/com/zeromail/core/chat/knowledge/AssistantKnowledgeMemoryUniqueTitleTest.java
  - backend/core/src/test/java/com/zeromail/core/chat/knowledge/KnowledgeSnippetSingleWriteSiteTest.java
  - backend/core/src/test/java/com/zeromail/core/chat/knowledge/AssistantKnowledgeAppendCallSiteTest.java
  - backend/core/src/test/java/com/zeromail/core/voice/VoiceGenerationFromSentLeakTest.java
  - backend/core/src/test/java/com/zeromail/core/voice/VoiceGenerationRateLimitTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/ByokResolutionIntegrationTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/ByokSaveResetsStateTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/UserByokKeySingleRowPerTenantTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/ByokTestConnectionRateLimitTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/ProviderConnectionTesterSingleBindingTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/UserByokTestConnectionSentinelLeakTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/cost/AiCostQueryService7DayTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/redaction/SensitiveDataRedactionToggleTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/settings/SettingsVoiceControllerTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/settings/SettingsVoiceLanguageValidationTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/settings/KnowledgeSnippetControllerTenantIsolationTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/triage/SenderSafetyNetDeleteAuthorityTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/triage/SenderSafetyNetDomainPatternTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokActivateGateModelMissingTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokActivateGateNotTestedTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokSaveProviderAllowListTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokSaveBaseUrlValidationTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokResponseNeverEchoesPlaintextTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokTestConnectionEnumOnlyTest.java
  - backend/api/src/test/java/com/zeromail/api/config/SpringAiObservationDisabledTest.java
  - backend/worker/src/test/java/com/zeromail/worker/draft/DraftAutoToggleIntegrationTest.java
  - backend/worker/src/test/java/com/zeromail/worker/draft/DraftConfidenceThresholdTest.java
  - backend/worker/src/test/java/com/zeromail/worker/draft/DraftSignatureIntegrationTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageSafetyNetDomainMatchTest.java
  - backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditSafetyNetBadgeTest.java
  - apps/web/e2e/ai-settings.spec.ts
autonomous: true
requirements:
  - SET-VOICE-01
  - SET-VOICE-02
  - SET-VOICE-03
  - SET-VOICE-04
  - SET-VOICE-05
  - SET-VOICE-06
  - SET-VOICE-07
  - SET-BEHV-01
  - SET-BEHV-02
  - SET-BEHV-04
  - SET-SAFE-01
  - SET-SAFE-04
  - SET-AI-01
  - SET-AI-02
  - SET-AI-03
  - SET-AI-04

must_haves:
  truths:
    - "Liquibase 094..097 changesets exist and are referenced from db.changelog-master.yaml"
    - "Backend boots with all four changesets applied without error against Testcontainers Postgres"
    - "AssistantSettingsEntity exposes JPA fields for email_signature, tone_preset, auto_draft_replies, draft_confidence, sensitive_data_protection"
    - "AssistantKnowledgeMemoryEntity has UNIQUE(tenant_id, title) enforced at the DB level"
    - "user_byok_key table exists with exactly one row per tenant (PK on tenant_id) and CHECK constraints for provider + last_test_result"
    - "Legacy tenant_byok_credentials table is left intact (no rename, no drop) per REVIEWS.md HIGH-2; archive-rename deferred to a v1.3 follow-up phase"
    - "Every ❌ Wave-0 stub from 09-VALIDATION.md exists as a compilable test class with at least one @Disabled or stub @Test annotation, allowing later waves to fill in assertions"
    - "Wave-0 stub list includes BOTH KnowledgeSnippetSingleWriteSiteTest (repo-write-site rule) AND AssistantKnowledgeAppendCallSiteTest (append-callers rule) so Plan 09-02 Task 2 can fill in two distinct ArchUnit invariants"
  artifacts:
    - path: "backend/core/src/main/resources/db/changelog/changes/094-assistant-settings-phase9-columns.yaml"
      provides: "5 new columns on assistant_settings (email_signature, tone_preset, auto_draft_replies, draft_confidence, sensitive_data_protection) per D-09/D-07"
    - path: "backend/core/src/main/resources/db/changelog/changes/095-assistant-knowledge-snippet-unique-title.yaml"
      provides: "UNIQUE(tenant_id, title) constraint on assistant_knowledge_snippet per D-05"
    - path: "backend/core/src/main/resources/db/changelog/changes/096-safety-net-pattern-kind-and-audit-badge.yaml"
      provides: "pattern_kind + created_by_user columns on tenant_protected_sender_observation, plus blocked_by_safety_net_pattern on triage_audit per SET-SAFE-01/04"
    - path: "backend/core/src/main/resources/db/changelog/changes/097-user-byok-key-table.yaml"
      provides: "New user_byok_key table per D-17, with forward-migration of any legacy tenant_byok_credentials row. **REPLAN 2026-05-26 per REVIEWS.md HIGH-2:** legacy table is LEFT INTACT (no rename, no archive, no drop) for the duration of Phase 9 so plans 09-02 / 09-03 / 09-05 cannot boot against stale JPA mappings while 09-04 is still in flight. The legacy table's archive-rename + final DROP becomes a follow-up v1.3 phase that runs only after 09-04 has removed every reference to `tenant_byok_credentials` and a verification window has elapsed."
    - path: "backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokKeyEntity.java"
      provides: "JPA entity for user_byok_key (single-row-per-tenant) per D-17"
    - path: "backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokKeyRepository.java"
      provides: "Spring Data JPA repository with findByTenantId for ByokProviderResolver / UserByokService in Wave 1"
  key_links:
    - from: "db.changelog-master.yaml"
      to: "changes/094..097-*.yaml"
      via: "include entries appended in master order"
      pattern: "include:.*094-assistant-settings-phase9-columns|095-assistant-knowledge-snippet-unique-title|096-safety-net-pattern-kind-and-audit-badge|097-user-byok-key-table"
    - from: "AssistantSettingsEntity"
      to: "assistant_settings columns"
      via: "@Column mappings matching the 5 new fields"
      pattern: "(emailSignature|tonePreset|autoDraftReplies|draftConfidence|sensitiveDataProtection)"
    - from: "UserByokKeyEntity"
      to: "user_byok_key columns"
      via: "@Entity + @Id tenantId mapping with CHECK-aligned enum strings"
      pattern: "@Table\\(name = \"user_byok_key\"\\)"
---

<objective>
Lay the Phase 9 foundation: 4 Liquibase changesets (assistant_settings columns, knowledge UNIQUE, safety-net + audit-badge columns, new user_byok_key table with legacy archive-rename migration), JPA entity scaffolding for those schema changes, and all 34 ❌ Wave-0 test stubs (33 backend + 1 Playwright) from `09-VALIDATION.md` so subsequent waves have a compilable, in-place test surface to fill in.

Purpose: Wave 0 must land BEFORE any service / controller code so Wave 1 plans can run in parallel against settled DB shape and pre-existing test class names.
Output: Liquibase changesets + entity scaffolding + 34 Wave-0 stub files (33 backend tests + 1 Playwright spec).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-SPEC.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-CONTEXT.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-RESEARCH.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-VALIDATION.md
@CLAUDE.md
@CONVENTIONS.md
@TESTING.md

<interfaces>
<!-- Source-of-truth artifacts the executor MUST inspect once (no re-reads) -->

From `backend/core/src/main/resources/db/changelog/db.changelog-master.yaml`:
- Highest existing changeset integer is `093-billing-package-presentation-fields.yaml` per RESEARCH § Pitfall 7. Append 094..097 in master ordering.

From `backend/core/src/main/java/com/zeromail/core/chat/persistence/AssistantSettingsEntity.java`:
- Existing JPA fields include `personalInstructions`, `writingStyle`, `providerId`, `chatModelId`, `triageModelId`, `draftModelId`, `aiOutputLanguage`.
- Extend with 5 new `@Column` mappings: `emailSignature` (VARCHAR(500)), `tonePreset` (VARCHAR(16), nullable, CHECK enum PROFESSIONAL/FRIENDLY/CASUAL/FORMAL/CUSTOM), `autoDraftReplies` (boolean NOT NULL DEFAULT true), `draftConfidence` (VARCHAR(8) NOT NULL DEFAULT 'MEDIUM' CHECK LOW/MEDIUM/HIGH), `sensitiveDataProtection` (boolean NOT NULL DEFAULT true).

From `backend/core/src/main/java/com/zeromail/core/chat/persistence/AssistantKnowledgeMemoryEntity.java`:
- Existing fields: `tenantId`, `title VARCHAR(120)`, `content TEXT`.
- Add `updatedAt TIMESTAMPTZ` mapping (DB default `now()` already added by 095); confirm `@PreUpdate` or `@LastModifiedDate` touch path per RESEARCH § Open Question 5 (read `AbstractTenantOwnedEntity` if entity extends it; otherwise add `@PreUpdate` lifecycle method).
- Liquibase 095 adds `UNIQUE(tenant_id, title)` + ensures `updated_at` column is NOT NULL DEFAULT NOW() (verify column exists from changelog 046 per RESEARCH summary; if absent, this changeset must `addColumn` it).

From `backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationEntity.java`:
- Add `patternKind` (VARCHAR(8) NOT NULL DEFAULT 'EMAIL' CHECK IN EMAIL/DOMAIN) and `createdByUser` (boolean NOT NULL DEFAULT false) mappings — used by Wave 1 SenderSafetyNet plan.

From triage audit row entity (locate via grep `triage_audit` table mapping):
- Add `blockedBySafetyNetPattern` (VARCHAR(320), nullable) mapping for SET-SAFE-04 badge.

New `user_byok_key` JPA entity (per D-17 + RESEARCH § Recommended Project Structure):
- `@Entity @Table(name = "user_byok_key")` with `@Id` on `tenantId UUID`.
- Columns: `provider VARCHAR(16)`, `baseUrl VARCHAR(255)`, `apiKeyCiphertext byte[]`, `apiKeyIv byte[]`, `modelId VARCHAR(64) NULL`, `active boolean NOT NULL DEFAULT false`, `lastTestResult VARCHAR(16) NULL`, `lastTestedAt OffsetDateTime NULL`, `createdAt OffsetDateTime`, `updatedAt OffsetDateTime`.
- Read `RefreshTokenCipher` source signature first: if `encrypt(...)` returns `byte[] envelope` with IV inlined, DROP `apiKeyIv` column from both the changeset AND the entity (single blob). Otherwise keep two columns. Assumption A7 — VERIFY in Task 1.

From `backend/core/src/main/java/com/zeromail/core/llm/gateway/admin/MasterKeyTestResult.java` (canonical enum per D-14 + Assumption A8):
- Values: `OK`, `INVALID_KEY`, `RATE_LIMITED`, `NETWORK_ERROR`, `TIMEOUT`. Use these strings in `ck_user_byok_key_last_test_result` CHECK constraint; do NOT invent `UNSUPPORTED`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema mutation | Liquibase changesets executed by app DB user; must not break existing v1.0/v1.1 invariants |
| Legacy → new BYOK row migration | Reads `tenant_byok_credentials.encrypted_key` and writes to `user_byok_key.api_key_ciphertext` — same AES-GCM ciphertext bytes copied unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | Liquibase changeset numbering | mitigate | Append 094..097 strictly after 093 per RESEARCH § Pitfall 7; verify in db.changelog-master.yaml include order |
| T-09-01-02 | Information disclosure | Legacy ciphertext → new table forward-migrate | mitigate | Copy `encrypted_key` bytes unchanged (same cipher version); do NOT log raw bytes; rollback path drops new table only; legacy table is RENAMED (not dropped) so rollback can restore it |
| T-09-01-03 | Denial of service | Long-running ALTER TABLE on assistant_settings | accept | Solo-tenant deployment — ALTERs add nullable / default columns only; runtime <1s |
| T-09-01-04 | Repudiation | user_byok_key state changes lack created_at/updated_at | mitigate | Changeset 097 adds `created_at` + `updated_at` TIMESTAMPTZ DEFAULT now() NOT NULL |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Wave 0 — pure SQL + Java scaffolding |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify RefreshTokenCipher envelope shape + write Liquibase 094..097 changesets + register in master</name>
  <files>
    backend/core/src/main/resources/db/changelog/changes/094-assistant-settings-phase9-columns.yaml,
    backend/core/src/main/resources/db/changelog/changes/095-assistant-knowledge-snippet-unique-title.yaml,
    backend/core/src/main/resources/db/changelog/changes/096-safety-net-pattern-kind-and-audit-badge.yaml,
    backend/core/src/main/resources/db/changelog/changes/097-user-byok-key-table.yaml,
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml
  </files>
  <read_first>
    backend/core/src/main/resources/db/changelog/db.changelog-master.yaml,
    backend/core/src/main/resources/db/changelog/changes/093-billing-package-presentation-fields.yaml,
    backend/core/src/main/resources/db/changelog/changes/046-assistant-knowledge-snippet-updated-at.yaml (or whatever existing 046 changeset added the column per RESEARCH summary),
    backend/core/src/main/java/com/zeromail/core/crypto/RefreshTokenCipher.java (verify Assumption A7 envelope shape),
    backend/core/src/main/java/com/zeromail/core/llm/gateway/admin/MasterKeyTestResult.java (confirm 5 enum values),
    .planning/phases/09-user-settings-ui-on-curated-catalog/09-RESEARCH.md (sections Code Examples §094 + §097, Pitfall 7)
  </read_first>
  <behavior>
    - 094 adds 5 columns to `assistant_settings` with the exact column types + CHECK constraints in RESEARCH § Code Examples Changeset 094 (NO `ai_provider_mode` column — D-17 retired it)
    - 095 ensures `UNIQUE(tenant_id, title)` and `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` on `assistant_knowledge_snippet` (per D-05); if `updated_at` was already added by changelog 046, the new changeset only adds the unique constraint
    - 096 adds `pattern_kind VARCHAR(8) NOT NULL DEFAULT 'EMAIL' CHECK IN (EMAIL, DOMAIN)` and `created_by_user BOOLEAN NOT NULL DEFAULT FALSE` to `tenant_protected_sender_observation`; backfills `pattern_kind='EMAIL'` for existing rows; adds `blocked_by_safety_net_pattern VARCHAR(320) NULL` to the triage audit row table (locate exact table name `triage_audit` per SPEC)
    - 097 creates `user_byok_key` with columns + CHECK constraints exactly as in RESEARCH § Code Examples Changeset 097 AND forward-migrates any legacy `tenant_byok_credentials` row. **REPLAN 2026-05-26 per REVIEWS.md HIGH-2 (Codex + OpenCode joint concern):** the legacy `tenant_byok_credentials` table is LEFT INTACT — no `RENAME TABLE` and no archive-rename in this changeset. Rationale: plans 09-02 / 09-03 / 09-05 may run in parallel with 09-04; if 09-01 renamed the table before 09-04 finished removing the legacy resolver/entity/service, the app would boot with JPA mappings pointing at a non-existent table. The archive-rename + final DROP becomes a follow-up v1.3 phase that fires only after 09-04 has removed every reference to `tenant_byok_credentials`. If RefreshTokenCipher returns single-blob envelopes (verified in read_first), DROP `api_key_iv` column from both this changeset AND the entity in Task 2; otherwise keep it.
    - Each changeset has a working `rollback:` block. Rollback for 097 drops the new `user_byok_key` table only — the legacy table is untouched by this changelog so no inverse rename is needed.
    - `db.changelog-master.yaml` includes the 4 new files in numerical order after 093
  </behavior>
  <action>
    Read RefreshTokenCipher signature first to settle the single-blob-vs-two-column question (per Assumption A7). Then author the four Liquibase YAML changesets following Liquibase project convention (YAML; no SQL-only files per CLAUDE.md). Use closed `CHECK` constraints for tone_preset (PROFESSIONAL/FRIENDLY/CASUAL/FORMAL/CUSTOM per D-09), draft_confidence (LOW/MEDIUM/HIGH per D-07), pattern_kind (EMAIL/DOMAIN), user_byok_key.provider (OPENAI/ANTHROPIC/GOOGLE/DEEPSEEK only — server-side defense for D-17 allow-list), and user_byok_key.last_test_result (OK/INVALID_KEY/RATE_LIMITED/NETWORK_ERROR/TIMEOUT per Assumption A8).

    **REPLAN 2026-05-26 (REVIEWS.md HIGH-2):** the 097 changeset executes the forward-migration `INSERT … SELECT … FROM tenant_byok_credentials ON CONFLICT DO NOTHING` and STOPS THERE. Do NOT issue any `RENAME TABLE`, `DROP TABLE`, or archive-rename of `tenant_byok_credentials` in this changeset. Document the deferral in a YAML comment inside changeset 097: "Legacy table `tenant_byok_credentials` is intentionally left intact in this changelog. Plans 09-02 / 09-03 / 09-05 may boot before 09-04 finishes removing the legacy resolver/entity/service — renaming the table here would break JPA mappings during that window. The archive-rename + final DROP is owned by a follow-up v1.3 phase that fires only after 09-04 has merged AND a verification window has elapsed AND `mcp__jetbrains__search_in_files_by_text \"tenant_byok_credentials\"` returns zero hits across `backend/` (excluding this changelog)." Rollback for 097 drops the new `user_byok_key` table only.

    **Liquibase preconditions (REVIEWS.md MEDIUM — Codex Liquibase 095):** add a `preConditions: onFail: HALT` block to changeset 095 with a `sqlCheck` asserting `SELECT count(*) - count(DISTINCT (tenant_id, title)) FROM assistant_knowledge_snippet = 0` BEFORE creating the unique constraint. If duplicate `(tenant_id, title)` rows already exist, fail with a clear migration message instructing the operator to deduplicate manually (the data-loss risk of automatic dedup is too high). Add `tableExists` / `columnExists` preconditions to all four changesets so reruns are idempotent.

    Register all four files in db.changelog-master.yaml.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests "*LiquibaseStartupTest*" --tests "*ChangelogValidationTest*"</automated>
  </verify>
  <acceptance_criteria>
    - All four changeset YAML files exist with valid Liquibase 5.0.2 syntax (boots without parser errors)
    - `db.changelog-master.yaml` lists 094..097 in order, after 093
    - Booting backend test profile against fresh Testcontainers Postgres applies all four cleanly and rollback for each is non-empty
    - `assistant_settings` has the 5 new columns; `assistant_knowledge_snippet` has `UNIQUE(tenant_id, title)`; `tenant_protected_sender_observation` has `pattern_kind` + `created_by_user`; triage audit row table has `blocked_by_safety_net_pattern`; `user_byok_key` table exists with PK on `tenant_id`
    - Legacy `tenant_byok_credentials` table is LEFT INTACT (no rename, no archive, no drop) per REVIEWS.md HIGH-2; the archive-rename + final DROP is deferred to a v1.3 follow-up phase
    - Changeset 095 has a `sqlCheck` precondition that fails the migration with a clear message if duplicate `(tenant_id, title)` rows exist (per REVIEWS.md MEDIUM)
    - No `ai_provider_mode` column added to `assistant_settings`; no `CallSite.CHAT` enum change to `ck_llm_call_audit_call_site`
  </acceptance_criteria>
  <done>Changesets applied cleanly in CI Testcontainers run; no Liquibase parser warnings; `mcp__postgres__list_objects` (when run against a freshly migrated DB) lists the 5 new assistant_settings columns + the new user_byok_key table + the archived legacy table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JPA entity scaffolding (AssistantSettings extension, AssistantKnowledgeMemory updated_at, TenantProtectedSenderObservation extension, TriageAudit badge column, new UserByokKeyEntity + repo)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/chat/persistence/AssistantSettingsEntity.java,
    backend/core/src/main/java/com/zeromail/core/chat/persistence/AssistantKnowledgeMemoryEntity.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationEntity.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java,
    backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokKeyEntity.java,
    backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokKeyRepository.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/chat/persistence/AssistantSettingsEntity.java,
    backend/core/src/main/java/com/zeromail/core/chat/persistence/AssistantKnowledgeMemoryEntity.java,
    backend/core/src/main/java/com/zeromail/core/shared/persistence/AbstractTenantOwnedEntity.java (verify whether updatedAt + @LastModifiedDate is wired — RESEARCH § Open Question 5),
    backend/core/src/main/java/com/zeromail/core/triage/persistence/TenantProtectedSenderObservationEntity.java,
    backend/core/src/main/java/com/zeromail/core/triage/persistence/ (locate triage audit row entity by grep `triage_audit` Hibernate @Table mapping),
    CONVENTIONS.md (records-for-DTOs, classes-for-entities, no-Lombok, enum state machine via OrderedEnum/IdentifiedEnum, no opaque abbreviations per project Backend Code Style)
  </read_first>
  <behavior>
    - `AssistantSettingsEntity` gains 5 new `@Column` mappings matching changeset 094 column names; setter methods follow the existing builder/setter style used for `writingStyle` etc.
    - `AssistantKnowledgeMemoryEntity` has `updatedAt OffsetDateTime` mapped + Hibernate `@PreUpdate` (or `@LastModifiedDate` if AbstractTenantOwnedEntity has auditing wired) so service-layer touches are unnecessary
    - `TenantProtectedSenderObservationEntity` gains `patternKind String` (with a fail-loud enum/string mapping that throws on unknown values per CONVENTIONS §4) and `createdByUser boolean` mappings
    - Triage audit row entity gains `blockedBySafetyNetPattern String` (nullable, max 320 chars)
    - New `UserByokKeyEntity`: `@Entity @Table(name = "user_byok_key")` with `@Id UUID tenantId`; fields exactly mirror the DB columns. `provider` and `lastTestResult` stay as `String` columns with fail-loud `valueOf` helpers (NO `ordinal()`-based storage per CONVENTIONS §4). Lombok-free; explicit constructors / accessors.
    - `UserByokKeyRepository extends JpaRepository<UserByokKeyEntity, UUID>` with `Optional<UserByokKeyEntity> findByTenantId(UUID tenantId)` — used by Wave 1 plans `09-04` (BYOK) and indirectly through resolver
  </behavior>
  <action>
    Extend the three existing entities with new `@Column`-mapped fields in the same style they already use (verify each via `mcp__jetbrains__get_file_text_by_path` first). Use explicit, domain-revealing names per CLAUDE.md backend code style (no `req`, `res`, `cfg`, `e`, etc.). For `patternKind` / `provider` / `lastTestResult` / `tonePreset` / `draftConfidence` columns, expose them as `String` properties; add static `fromId(String)` helpers that throw `NoSuchElementException` on unknown values per CONVENTIONS §4 (no `ordinal()`). Create `UserByokKeyEntity` + `UserByokKeyRepository` under the new package `com.zeromail.core.llm.byok` (Modulith boundary acceptable — D-13 lets settings/voice/knowledge sit in `core.chat.settings` while BYOK can sit in `core.llm.byok`). If RefreshTokenCipher uses single-blob envelopes per Task 1's verification, the entity has only `apiKeyCiphertext byte[]` (no `apiKeyIv`). Do NOT write services or controllers here — Wave 1 plans own those.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileJava :backend:core:compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - All five entity files compile; `mcp__jetbrains__get_file_problems` on each returns no errors
    - `UserByokKeyRepository.findByTenantId(UUID)` resolves and returns `Optional<UserByokKeyEntity>`
    - No `ordinal()` references in any enum-backed column accessor (grep `\.ordinal\(\)` in the touched files returns zero matches)
    - No Lombok annotations introduced (grep `@(Data|Builder|Getter|Setter|RequiredArgsConstructor)` in touched files = 0)
    - All new fields use domain-revealing names per CLAUDE.md (no `req`/`res`/`cfg`/etc.)
  </acceptance_criteria>
  <done>Backend core compiles cleanly with the new entity fields and the new BYOK entity + repo; no enum-via-ordinal storage; no Lombok introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold all 34 Wave-0 stub files (33 backend tests + 1 Playwright e2e shell) so Wave 1+2 plans can fill in assertions without creating files</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/chat/settings/SettingsVoiceServiceWordBoundsTest.java,
    backend/core/src/test/java/com/zeromail/core/chat/settings/AssistantSettingsTonePresetCheckTest.java,
    backend/core/src/test/java/com/zeromail/core/chat/sanitize/PersonalizationSanitizerSingleCallSiteTest.java,
    backend/core/src/test/java/com/zeromail/core/chat/knowledge/AssistantKnowledgeMemoryUniqueTitleTest.java,
    backend/core/src/test/java/com/zeromail/core/chat/knowledge/KnowledgeSnippetSingleWriteSiteTest.java,
    backend/core/src/test/java/com/zeromail/core/chat/knowledge/AssistantKnowledgeAppendCallSiteTest.java,
    backend/core/src/test/java/com/zeromail/core/voice/VoiceGenerationFromSentLeakTest.java,
    backend/core/src/test/java/com/zeromail/core/voice/VoiceGenerationRateLimitTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/ByokResolutionIntegrationTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/ByokSaveResetsStateTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/UserByokKeySingleRowPerTenantTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/ByokTestConnectionRateLimitTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/ProviderConnectionTesterSingleBindingTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/UserByokTestConnectionSentinelLeakTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/cost/AiCostQueryService7DayTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/redaction/SensitiveDataRedactionToggleTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/settings/SettingsVoiceControllerTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/settings/SettingsVoiceLanguageValidationTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/settings/KnowledgeSnippetControllerTenantIsolationTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/triage/SenderSafetyNetDeleteAuthorityTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/triage/SenderSafetyNetDomainPatternTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokActivateGateModelMissingTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokActivateGateNotTestedTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokSaveProviderAllowListTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokSaveBaseUrlValidationTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokResponseNeverEchoesPlaintextTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokTestConnectionEnumOnlyTest.java,
    backend/api/src/test/java/com/zeromail/api/config/SpringAiObservationDisabledTest.java,
    backend/worker/src/test/java/com/zeromail/worker/draft/DraftAutoToggleIntegrationTest.java,
    backend/worker/src/test/java/com/zeromail/worker/draft/DraftConfidenceThresholdTest.java,
    backend/worker/src/test/java/com/zeromail/worker/draft/DraftSignatureIntegrationTest.java,
    backend/worker/src/test/java/com/zeromail/worker/triage/TriageSafetyNetDomainMatchTest.java,
    backend/worker/src/test/java/com/zeromail/worker/triage/TriageAuditSafetyNetBadgeTest.java,
    apps/web/e2e/ai-settings.spec.ts
  </files>
  <read_first>
    .planning/phases/09-user-settings-ui-on-curated-catalog/09-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements),
    TESTING.md (Spring Boot 4 slice ladder + Testcontainers + mocked ChatModel rule),
    backend/api/src/test/java/com/zeromail/api/controllers/llm/ (existing controller test for reference base class — likely extends `ApiPostgresTestBase`),
    backend/worker/src/test/java/ (locate worker base test for triage/draft slices),
    apps/web/e2e/ (existing Playwright spec for reference shape)
  </read_first>
  <behavior>
    - Each stub file compiles and is picked up by Gradle (correct package, correct test class name matching the `--tests` filter in 09-VALIDATION.md)
    - Each stub contains either one `@Test` method annotated `@Disabled("Wave 1: implement after <service> lands — see 09-VALIDATION.md row <REQ>")` OR a placeholder test that asserts `true` (to prove discovery), with a TODO comment naming the Wave 1 plan that owns it
    - Backend stubs extend the appropriate Spring Boot test base (`PostgresContainerTest`, `ApiPostgresTestBase`, `@DataJpaTest`, `@WebMvcTest`, `@SpringBootTest` — per TESTING.md slice ladder for each row in the validation map)
    - `AssistantKnowledgeAppendCallSiteTest` stub exists in `core.chat.knowledge` package alongside `KnowledgeSnippetSingleWriteSiteTest`; its `@Disabled` message reads "Plan 09-02 Task 2 owns the append-callers ArchUnit invariant"
    - `SpringAiObservationDisabledTest` stub exists in `backend/api/src/test/java/com/zeromail/api/config/`; its `@Disabled` message reads "Plan 09-05 Task 1 owns the Spring AI observation property-binding invariant"
    - `apps/web/e2e/ai-settings.spec.ts` exists with a `test.skip('Phase 9 golden path: voice + behavior + knowledge + safety-net + BYOK', ...)` placeholder importing existing Playwright fixtures
    - After this task, running the full backend suite reports the stub classes as `skipped` (not `errored`), confirming discovery
  </behavior>
  <action>
    Generate exactly the 33 backend stub files + 1 Playwright stub listed above (Wave 0 Requirements in 09-VALIDATION.md — includes `AssistantKnowledgeAppendCallSiteTest` per BLOCKER #1 and `SpringAiObservationDisabledTest` per WARNING #6). For each backend stub: pick the slice annotation per TESTING.md (`@DataJpaTest` for entity tests, `@WebMvcTest` for MVC slices, `@SpringBootTest` for integration tests touching the worker, ArchUnit test pattern for single-binding/single-call-site tests). Include `@Disabled` with a message naming the Wave 1 plan that owns it (e.g. `@Disabled("Plan 09-04 owns BYOK implementation")`). Test class names MUST match the `--tests "<ClassName>"` filters from 09-VALIDATION.md verbatim. No assertion bodies — just structure. After all 34 stubs land (33 backend + 1 Playwright), flip `nyquist_compliant: true` in `09-VALIDATION.md` frontmatter and update `wave_0_complete: true`.
  </action>
  <verify>
    <automated>./gradlew :backend:core:compileTestJava :backend:api:compileTestJava :backend:worker:compileTestJava && ./gradlew :backend:core:test :backend:api:test :backend:worker:test --tests "*Settings*" --tests "*Knowledge*" --tests "*VoiceGeneration*" --tests "*Byok*" --tests "*SafetyNet*" --tests "*Draft*" --tests "*Triage*" -PdryRun=false</automated>
  </verify>
  <acceptance_criteria>
    - All 33 backend stubs compile (TestJava compile task succeeds), including `KnowledgeSnippetSingleWriteSiteTest`, `AssistantKnowledgeAppendCallSiteTest`, and `SpringAiObservationDisabledTest`
    - All 33 backend stubs are discovered by Gradle when filtered by the `--tests` patterns in 09-VALIDATION.md (Gradle reports them as Skipped, not Missing)
    - `apps/web/e2e/ai-settings.spec.ts` exists and `pnpm --filter web playwright test --list ai-settings.spec.ts` lists it
    - `09-VALIDATION.md` frontmatter updated: `nyquist_compliant: true`, `wave_0_complete: true`
    - Each stub references the Wave-1 plan that owns it via `@Disabled` message — no orphan stubs
    - Total scaffolded file count: 34 (33 backend tests + 1 Playwright spec) — matches 09-VALIDATION.md Validation Sign-Off bullet
  </acceptance_criteria>
  <done>Every ❌ Wave-0 row in 09-VALIDATION.md now has a compilable, discoverable stub. The Per-Task Verification Map's `File Exists` column flips ❌→✅ for all rows.</done>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:test :backend:api:test :backend:worker:test --tests "*LiquibaseStartupTest*" --tests "*Settings*" --tests "*Knowledge*" --tests "*VoiceGeneration*" --tests "*Byok*" --tests "*SafetyNet*" --tests "*Draft*" --tests "*Triage*"` runs without compile errors; all Phase 9 tests show as Skipped (not Errored)
- `mcp__postgres__list_objects` against the fresh Testcontainers DB lists 5 new columns on `assistant_settings`, the new `user_byok_key` table, and the renamed `tenant_byok_credentials_archived_2026_05_26` archive table
- `09-VALIDATION.md` frontmatter shows `nyquist_compliant: true` and `wave_0_complete: true`
- No Lombok / no `ordinal()` storage / no opaque abbreviations in any touched Java file (project-wide grep gates per CLAUDE.md + CONVENTIONS.md)
</verification>

<success_criteria>
- 4 Liquibase changesets (094..097) committed and registered in master changelog; Testcontainers boot applies them cleanly
- 5 JPA entity changes (3 extensions + 1 new entity + 1 repository) compiled and Lombok-free
- 33 backend test stubs + 1 Playwright stub exist and are discoverable (34 total files)
- Legacy `tenant_byok_credentials` table LEFT INTACT per REVIEWS.md HIGH-2 (no rename in this changelog); single forward-migrated row in `user_byok_key` (or zero rows if no legacy data); follow-up v1.3 phase owns the archive-rename and final DROP
- `09-VALIDATION.md` flipped to `nyquist_compliant: true`
</success_criteria>

<output>
Create `.planning/phases/09-user-settings-ui-on-curated-catalog/09-01-SUMMARY.md` when done. Include: Liquibase changeset summary, RefreshTokenCipher envelope-shape decision (single-blob vs two-column), legacy-row migration result, confirmation that legacy `tenant_byok_credentials` table is LEFT INTACT (no rename in this phase per REVIEWS.md HIGH-2), 095 sqlCheck precondition result (any pre-existing duplicate `(tenant_id, title)` rows surfaced), full stub file count (34: 33 backend + 1 Playwright), Wave-1 plans that own each stub.
</output>
