---
phase: 09-user-settings-ui-on-curated-catalog
plan: 04
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ProviderConnectionTester.java
  - backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ConnectionTestResult.java
  - backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ModelsProbeClient.java
  - backend/core/src/main/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminService.java
  - backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokService.java
  - backend/core/src/main/java/com/zeromail/core/llm/byok/BaseUrlValidator.java
  - backend/core/src/main/java/com/zeromail/core/llm/byok/ProviderAllowList.java
  - backend/core/src/main/java/com/zeromail/core/llm/byok/ByokProviderResolver.java
  - backend/core/src/main/java/com/zeromail/core/llm/byok/ByokRateLimiter.java
  - backend/api/src/main/java/com/zeromail/api/controllers/llm/ByokController.java
  - backend/core/src/main/java/com/zeromail/core/llm/gateway/LlmGatewayImpl.java
  - backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/SpringAiChatModelFactory.java
  - backend/core/src/main/java/com/zeromail/core/llm/cost/AiCostQueryService.java
  - backend/api/src/main/java/com/zeromail/api/controllers/byok/UserByokController.java
  - backend/api/src/main/java/com/zeromail/api/controllers/settings/SettingsAiCostController.java
  - backend/api/src/main/java/com/zeromail/api/dto/byok/ByokResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/byok/ByokSaveRequest.java
  - backend/api/src/main/java/com/zeromail/api/dto/byok/ByokActivateRequest.java
  - backend/api/src/main/java/com/zeromail/api/dto/byok/ByokModelRequest.java
  - backend/api/src/main/java/com/zeromail/api/dto/byok/ByokTestConnectionRequest.java
  - backend/api/src/main/java/com/zeromail/api/dto/byok/ByokTestConnectionResponse.java
  - backend/api/src/main/java/com/zeromail/api/dto/settings/AiCostResponse.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/ByokResolutionIntegrationTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/ByokSaveResetsStateTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/UserByokKeySingleRowPerTenantTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/ByokTestConnectionRateLimitTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/ProviderConnectionTesterSingleBindingTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/byok/UserByokTestConnectionSentinelLeakTest.java
  - backend/core/src/test/java/com/zeromail/core/llm/cost/AiCostQueryService7DayTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokActivateGateModelMissingTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokActivateGateNotTestedTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokSaveProviderAllowListTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokSaveBaseUrlValidationTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokResponseNeverEchoesPlaintextTest.java
  - backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokTestConnectionEnumOnlyTest.java
autonomous: true
requirements:
  - SET-AI-01
  - SET-AI-02
  - SET-AI-03
  - SET-AI-04

must_haves:
  truths:
    - "ProviderConnectionTester is extracted in core.llm.gateway.springai; both admin MasterKeyAdminService MKEY-03 AND UserByokController.testConnection reach the SAME probeConnection method (ArchUnit green)"
    - "POST /api/byok stores AES-GCM-encrypted key via RefreshTokenCipher; response never echoes plaintext (regex assertion: no `sk-[A-Za-z0-9]{20,}` substring)"
    - "Saving a row resets active=false, last_test_result=NULL, last_tested_at=NULL, model_id=NULL (per REVIEWS.md HIGH-4 — locked lifecycle is Save → Test stored row → Pick model → Activate; no inline-pre-save Test path can be used for activation)"
    - "POST /api/byok/test-connection accepts ONLY stored-row form (request body has no inline payload fields per REVIEWS.md HIGH-4); 404 code=ai.byok.no_row when no stored row exists — caller must Save first"
    - "PUT /api/byok/active {active:true} returns 400 code=ai.byok.no_model_picked when model_id IS NULL OR last_test_result <> 'OK'"
    - "POST /api/byok/test-connection returns {result: OK, models: [...]} (capped 100) on success for ALL FOUR allow-listed providers (OPENAI, ANTHROPIC, GOOGLE, DEEPSEEK); {result: <enum>} (no models key) otherwise; provider error bodies never appear in response. On success the returned models[] is PERSISTED into a new `last_test_models_json` column on the stored row so a subsequent PUT /api/byok/model can server-side-validate membership (per REVIEWS.md MEDIUM — Codex setModel membership). The cached list TTL = until the next Save or successful Test (whichever comes first)."
    - "Anthropic /v1/models probe path uses x-api-key + anthropic-version: 2023-06-01 headers and parses {data: [{id, ...}]} response per https://platform.claude.com/docs/en/api/models-list — verified live in ByokTestConnectionEnumOnlyTest#anthropicProvider_returnsOkWithModels"
    - "Per-tenant 10 tests/hour rate limit applied to stored-row testConnection path (no inline path exists per HIGH-4)"
    - "Server-side provider allow-list rejects OPENROUTER and ROUTER_9R with code=ai.byok.provider_not_allowed (defense in depth)"
    - "Base URL validator rejects non-https URLs (with code=ai.byok.base_url_not_https) except http://localhost* in dev profile"
    - "BaseUrlValidator SSRF-hardens the URL per REVIEWS.md HIGH-3: resolves host to IP at request time (defeats DNS rebinding via re-resolution before the probe call), rejects RFC1918 private ranges (10/8, 172.16/12, 192.168/16), loopback (127.0.0.0/8 plus aliases like `localhost.localdomain` outside dev profile), link-local (169.254/16, fe80::/10), unspecified (0.0.0.0, ::), reserved/CGNAT (100.64/10), multicast (224/4), and IPv6 ULA (fc00::/7). Non-default ports (anything other than 443 outside dev) require an explicit `byok.base-url.allowed-extra-ports` config entry or are rejected with code=ai.byok.base_url_port_not_allowed. Rejection codes: ai.byok.base_url_not_https / ai.byok.base_url_host_private / ai.byok.base_url_host_unresolvable / ai.byok.base_url_port_not_allowed"
    - "ByokController (the LEGACY @RestController at /api/llm/byok) is REWRITTEN in Task 2 as a deprecated 410-Gone shim per REVIEWS.md HIGH-2 — every endpoint returns 410 with body {code: 'ai.byok.moved', message: 'Use /api/byok'} and Location: /api/byok header; the class is DELETED in plan 09-06 alongside ByokForm.tsx. This prevents the cross-plan compile gap between 09-04 and 09-06."
    - "Exactly one user_byok_key row per tenant — saving a different provider replaces the existing row"
    - "ByokProviderResolver returns Optional<ResolvedLlmProviderCredential> when (active && model_id != null && last_test_result == 'OK'); empty otherwise → caller falls back to admin-catalog platform default"
    - "Chat / triage / draft / voice-generate pipelines call {base_url} with model_id when BYOK is active and tested (integration test stubs URL and asserts request body model field)"
    - "GET /api/settings/ai/cost?window=7d returns exactly {usd: number} tenant-wide SUM with no per-feature keys"
    - "Legacy TenantByokProviderCredentialResolver, TenantByokCredentialsEntity, ByokService are removed in this plan; ByokController is REWRITTEN as a deprecated 410-Gone shim in Task 2 and DELETED in plan 09-06; ByokForm.tsx is deleted in plan 09-06. Replaced by ByokProviderResolver as the single resolver bean."
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ProviderConnectionTester.java"
      provides: "Shared connection tester used by admin MKEY-03 + user BYOK; per-provider auth/header strategy table that selects Authorization-Bearer for OPENAI/DEEPSEEK, x-api-key + anthropic-version for ANTHROPIC, and provider-native auth for GOOGLE; sentinel-leak-clean ConnectionTestResult"
    - path: "backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokService.java"
      provides: "save / load / testConnection (stored row only per HIGH-4) / setModel (with server-side membership check against last_test_models_json) / activate / delete — with rate limiter"
    - path: "backend/core/src/main/java/com/zeromail/core/llm/byok/ByokProviderResolver.java"
      provides: "Per-call BYOK vs platform-default resolution (Pattern 2 in RESEARCH)"
    - path: "backend/api/src/main/java/com/zeromail/api/controllers/byok/UserByokController.java"
      provides: "GET /api/byok, POST /api/byok, PUT /api/byok/active, PUT /api/byok/model, DELETE /api/byok, POST /api/byok/test-connection"
    - path: "backend/core/src/main/java/com/zeromail/core/llm/cost/AiCostQueryService.java"
      provides: "Tenant-wide 7-day cost SUM (Spring Data JDBC over llm_call_audit)"
    - path: "backend/api/src/main/java/com/zeromail/api/controllers/settings/SettingsAiCostController.java"
      provides: "GET /api/settings/ai/cost?window=7d → {usd: number}"
  key_links:
    - from: "MasterKeyAdminService"
      to: "ProviderConnectionTester.probeConnection"
      via: "refactored delegate call"
      pattern: "providerConnectionTester\\.probeConnection"
    - from: "UserByokController.testConnection"
      to: "ProviderConnectionTester.probeConnection"
      via: "after rate-limit check"
      pattern: "providerConnectionTester\\.probeConnection"
    - from: "LlmGatewayImpl"
      to: "ByokProviderResolver.resolve"
      via: "injection replaces TenantByokProviderCredentialResolver bean"
      pattern: "byokProviderResolver\\.resolve"
    - from: "SpringAiChatModelFactory"
      to: "ByokProviderResolver.resolve"
      via: "injection"
      pattern: "byokProviderResolver"
---

<objective>
Deliver the entire D-17 BYOK + cost surface: extract `ProviderConnectionTester` shared with admin MKEY-03 (with per-provider header/auth strategy covering Anthropic's `x-api-key` + `anthropic-version` contract — per planner-checker BLOCKER #2 fix), build new `user_byok_key` service + resolver + controller, replace legacy `TenantByokProviderCredentialResolver` as the single resolver bean injected into `LlmGatewayImpl` + `SpringAiChatModelFactory`, ship the tenant-wide cost SUM endpoint, and delete legacy `ByokController` + `ByokForm.tsx` paths.

Purpose: this plan is the architectural heart of Phase 9 — wrong wiring here breaks chat/triage/draft for the live user. Privacy invariants (no plaintext key in responses, no provider error body leak) and the activation gate (`active=true` requires model + OK test) are non-negotiable. The Anthropic probe path is also non-negotiable because SET-AI-04 acceptance requires `{result: OK, models: [...]}` to work for ALL FOUR allow-listed providers.

Output: 1 extracted tester (with per-provider header strategy) + 1 admin refactor + 5 BYOK service classes + 1 cost service + 2 controllers + 8 DTO records + 14 ❌ Wave-0 stubs flipped green.

**Plan size note (per planner-checker WARNING #3):** this plan has 3 tasks across ~34 files. The plan is kept single (not split into 09-04a/09-04b) — the tasks are tightly coupled (Task 1 extracts the tester that Task 2 injects; Task 2 builds the service that Task 3 wraps). HOWEVER, executors MUST commit after each task (see `<execution_discipline>` block) so context windows do not exceed 50% during execution. If Task 3 begins above 35% context usage, STOP and resume in a fresh window — the `09-04-SUMMARY.md` carry-forward will preserve state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_discipline>
This plan is large (3 tasks, ~34 files). To avoid context-window degradation:

- **After Task 1:** STOP. Commit. Run the Task-1 verify command. The next task is a natural commit boundary — Task 1 ships the shared tester + supporting helpers; Task 2 starts a new logical chunk (service + resolver + legacy removal).
- **After Task 2:** STOP. Commit. Run the Task-2 verify command. Task 3 is a controller + DTO layer that depends on Task 2 but is independently testable.
- **Before Task 3:** Check current context usage. If above 35%, STOP and resume in a fresh window. Read `09-04-SUMMARY.md` (or git log if SUMMARY isn't yet written) to recover state.

The per-task commits also keep the checker's review surface small if a later iteration revisits any single task.
</execution_discipline>

<context>
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-SPEC.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-CONTEXT.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-RESEARCH.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-VALIDATION.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-01-SUMMARY.md
@.planning/phases/08-admin-console-operator-tooling/08-CONTEXT.md
@CLAUDE.md
@CONVENTIONS.md

<interfaces>
From `backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ModelsProbeClient.java`:
- Existing low-level HTTP client. Methods (verify exact signatures): `MasterKeyTestResult probeConnection(LlmProvider provider, KeyFormat keyFormat, String baseUrl, byte[] plaintextKey)` and `List<RawModel> fetchModelCatalog(LlmProvider provider, KeyFormat keyFormat, String baseUrl, byte[] plaintextKey) throws ProbeFailedException`. Returns canonical enum from `MasterKeyTestResult` per Assumption A8 — values `OK / INVALID_KEY / RATE_LIMITED / NETWORK_ERROR / TIMEOUT`.
- The existing `ModelsProbeClient` likely uses OpenAI-style `Authorization: Bearer` for ALL providers (admin MKEY-03 was implemented before BYOK shipped Anthropic + Google). Phase 9 must teach it the per-provider header convention OR (preferred — see Anthropic probe contract below) introduce a small strategy mapper inside `ProviderConnectionTester` that selects the right transport.

From existing `MasterKeyAdminService` (admin MKEY-03):
- Currently calls `ModelsProbeClient.probeConnection(...)` directly + `fetchModelCatalog(...)`. Phase 9 refactors this to delegate to `ProviderConnectionTester.probeConnection(...)`. `MasterKeySentinelLeakTest` ARCH-11 must remain green — the new tester preserves the sentinel-leak scrub by returning only the enum + model IDs.

From `backend/core/src/main/java/com/zeromail/core/crypto/RefreshTokenCipher.java` (verify in 09-01 envelope shape):
- `encrypt(byte[] plaintext, String tenantContextStr) -> byte[] envelope` and `decrypt(byte[] envelope, String tenantContextStr) -> byte[] plaintext`. AES-GCM. Per Assumption A7, single-blob envelope (verify in 09-01-SUMMARY).

From existing `LlmGatewayImpl` + `SpringAiChatModelFactory`:
- Both inject `TenantByokProviderCredentialResolver`. Phase 9 swaps the bean implementation type to `ByokProviderResolver` (same return type `Optional<ResolvedLlmProviderCredential>`) — either rename the interface or change the bean used. Simpler: delete `TenantByokProviderCredentialResolver` outright and rename references to `ByokProviderResolver`.

From `SpringAiProviderChatClientFactory.openAiCompatibleModel(credential, model, ...)`:
- Already accepts `credential.baseUrl()` per Pattern 2. No starter reconfiguration needed — per-call base URL is the runtime contract.

From `LlmProvider` enum (verify):
- Values include OPENAI, ANTHROPIC, GOOGLE (or GOOGLE_GENAI), DEEPSEEK, OPENROUTER, ROUTER_9R. Allow-list for user BYOK = {OPENAI, ANTHROPIC, GOOGLE, DEEPSEEK}. Reject the rest at `UserByokService.save(...)`.

From Phase 8 `MasterKeyRateLimiter` (per RESEARCH § Open Question 6):
- Likely admin-bound. Extract to `core.shared.ratelimit` or write a thin per-tenant variant `ByokRateLimiter` keyed by `byok.test_connection:{tenantId}` for 10/h and `voice.generate:{tenantId}` for 3/h (Plan 09-05 uses the latter). Redis Lettuce primitive.

From `llm_call_audit` schema:
- Has `tenant_id`, `total_cost_usd NUMERIC`, `created_at TIMESTAMPTZ`. Cost SUM is straight `SELECT COALESCE(SUM(total_cost_usd), 0) FROM llm_call_audit WHERE tenant_id = ? AND created_at >= now() - interval '7 days'`. Existing index on `(tenant_id, created_at)` per RESEARCH § Spend Aggregate.

Provider canonical base URLs (RESEARCH § AiProviderSection field-by-field):
- OPENAI: `https://api.openai.com/v1`
- ANTHROPIC: `https://api.anthropic.com/v1`
- GOOGLE: `https://generativelanguage.googleapis.com/v1beta`
- DEEPSEEK: `https://api.deepseek.com/v1`

**Anthropic Models API contract (verified via https://platform.claude.com/docs/en/api/models-list during planner-checker iteration 1):**
- Method: `GET /v1/models`
- Auth header: `X-Api-Key: <key>` (NOT `Authorization: Bearer ...`)
- Required header: `anthropic-version: 2023-06-01`
- Optional query params: `after_id`, `before_id`, `limit` (default 20, max 1000)
- Response shape: `{data: [{id: string, display_name: string, max_input_tokens: number, max_tokens: number, type: "model", capabilities: {...}, created_at: string}, ...], first_id, has_more, last_id}`
- Sample curl: `curl https://api.anthropic.com/v1/models -H 'anthropic-version: 2023-06-01' -H "X-Api-Key: $ANTHROPIC_API_KEY"`
- Model ID extraction: `response.data[*].id` → these are the strings that go into `ConnectionTestResult.models()`. Apply the same chat-completion-capability filter as OpenAI (exclude embedding/whisper/tts/dall-e/text-embedding/aqa substrings) — Anthropic IDs like `claude-opus-4-7`, `claude-haiku-4-1` are chat-capable; if Anthropic ever ships a non-chat model it likely won't match the filter regex anyway.
</interfaces>

<resolved_questions>
- **O-1 (Anthropic /v1/models) — RESOLVED in this revision:** the Anthropic Models List endpoint IS reachable with `x-api-key` + `anthropic-version: 2023-06-01` (verified live during planner-checker iteration 1 against https://platform.claude.com/docs/en/api/models-list). The previous "defer to llm-eval" stance was insufficient because SET-AI-04 acceptance requires the test-connection endpoint to return `{result: OK, models: [...]}` for ALL FOUR allow-listed providers. Task 1 below implements the Anthropic-specific probe path inside `ProviderConnectionTester` (per-provider header strategy) and `ByokTestConnectionEnumOnlyTest` gains a parameterized `anthropicProvider_returnsOkWithModels` case alongside the existing OpenAI / Google / DeepSeek cases. No `@Tag("llm-eval")` deferral — the test uses a mocked `ModelsProbeClient` (or stubbed HTTP server) so it runs in `./gradlew test`.
- **O-3 (Google base URL override):** unchanged. If Spring AI's google-genai adapter does NOT support per-call `baseUrl` override, reject non-default base URL for `GOOGLE` provider at `UserByokService.save(...)` with `code=ai.byok.base_url_not_supported_for_provider`. Verify Client builder via Context7 `/google/genai-sdk-java` query for "base url override" in Task 2; if unsupported, add the rejection and a regression test. If supported, no rejection needed.
</resolved_questions>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/byok | User submits provider + base URL + plaintext API key |
| storage at rest | AES-GCM via RefreshTokenCipher; plaintext never persisted |
| /api/byok response | Server MUST NOT echo plaintext key (only `lastFourChars`) |
| provider HTTP probe | Provider error bodies (401, 429, 5xx) MUST NOT leak through; only enum + sanitized model IDs |
| chat/triage/draft → BYOK | Per-call decryption + base URL handoff; plaintext bytes scrubbed after handoff |
| admin MKEY ↔ user BYOK | Shared `ProviderConnectionTester` must keep ARCH-11 (MasterKeySentinelLeakTest) green |
| Anthropic-specific probe transport | `x-api-key` + `anthropic-version: 2023-06-01` headers on the probe HTTP call; sanitization rules MUST apply identically to Anthropic 401/429/5xx error bodies (no special-case leak) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information disclosure | Plaintext key in response | mitigate | Response DTO has no `apiKey` field; only `lastFourChars`. Snapshot test asserts response payload regex `sk-[A-Za-z0-9]{20,}` finds zero matches |
| T-09-04-02 | Information disclosure | Plaintext key in logs / exception messages | mitigate | Service wraps decrypt + provider HTTP call; catches every exception and replaces message with sanitized version; structured log only `event=byok.save tenantId={} provider={} lastFourChars={}` |
| T-09-04-03 | Information disclosure | Provider error body bleeds through test-connection | mitigate | `ProviderConnectionTester` returns ONLY enum + model IDs; never includes provider error string. `UserByokTestConnectionSentinelLeakTest` seeds provider stub with sentinel `SENTINEL_LEAK_X9Z2` and asserts no leak in response payload — for BOTH OpenAI/DeepSeek/Google (Authorization-Bearer providers) AND Anthropic (x-api-key provider) so the new per-provider strategy doesn't introduce a leak class |
| T-09-04-04 | Elevation of privilege | Activate BYOK without testing | mitigate | `PUT /api/byok/active {active:true}` validates `model_id IS NOT NULL && last_test_result = 'OK'`; rejects with 400 `code=ai.byok.no_model_picked`. FE switch is also disabled (defense in depth) — owned by 09-06 |
| T-09-04-05 | Tampering | Save with disallowed provider | mitigate | Server-side `ProviderAllowList` rejects OPENROUTER / ROUTER_9R with `code=ai.byok.provider_not_allowed` |
| T-09-04-06 | Tampering | Non-https base URL (MitM) | mitigate | `BaseUrlValidator` requires `https://` prefix or `http://localhost` (dev profile only); rejects others with `code=ai.byok.base_url_not_https` |
| T-09-04-06b | Server-side request forgery | User-controlled `baseUrl` causes backend to probe internal services (Redis admin port, metadata endpoints, internal DBs) via private IPs / DNS rebinding / link-local | mitigate | Per REVIEWS.md HIGH-3: `BaseUrlValidator` performs DNS resolution of the host at request time (NOT just at save time — defeats DNS-rebinding where the host A-record flips between save and probe), rejects RFC1918 (10/8, 172.16/12, 192.168/16), loopback (127.0.0.0/8 outside dev profile), link-local (169.254/16, fe80::/10), unspecified, CGNAT (100.64/10), multicast, IPv6 ULA (fc00::/7); rejects non-443 ports outside an explicit allow-list. Probe code path re-resolves the host immediately before the RestClient call and aborts if the resolved IP changes vs the validation-time resolution — closes the TOCTOU window |
| T-09-04-07 | Denial of service | Brute-force key guess via test endpoint | mitigate | Per-tenant 10 tests/hour Redis rate-limit applied at the top of `UserByokService.testConnection` (no inline-payload path per HIGH-4 — caller must Save the key first, and Save requires the SSRF-hardened BaseUrlValidator to pass) |
| T-09-04-08 | Repudiation | Activate/save without audit | accept | User-owned setting; no compliance requirement for v1.2 audit beyond structured app log |
| T-09-04-09 | Spoofing | Tenant A activates Tenant B's row | mitigate | Repository queries always `WHERE tenant_id = ?` from `TenantContext.currentOrThrow()` |
| T-09-04-10 | Tampering | Resolver returns stale credential after toggle | mitigate | Resolver uses `@Transactional(readOnly = true)` per call; no caching layer in Phase 9 |
| T-09-04-11 | Information disclosure | Decrypted plaintext key sits in heap after use | mitigate | `ByokProviderResolver.toResolvedCredential` uses try/finally with `Arrays.fill(plaintextKey, (byte) 0)` after handoff (Pattern 2 in RESEARCH) |
| T-09-04-12 | Tampering | Anthropic probe uses wrong auth header → silent degrade for Anthropic BYOK users (SET-AI-04 violation) | mitigate | Per-provider header strategy inside `ProviderConnectionTester`: ANTHROPIC → `x-api-key` + `anthropic-version: 2023-06-01`; OPENAI/DEEPSEEK → `Authorization: Bearer`; GOOGLE → provider-native (Google API key as query param or x-goog-api-key header per the google-genai SDK convention). `ByokTestConnectionEnumOnlyTest#anthropicProvider_returnsOkWithModels` asserts the OK + models[] path |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract ProviderConnectionTester (per-provider header strategy incl. Anthropic x-api-key per BLOCKER #2) + refactor admin MKEY-03 + ProviderAllowList + SSRF-hardened BaseUrlValidator + ByokRateLimiter (REVIEWS.md HIGH-1: ArchUnit single-binding + sentinel-leak tests moved to Tasks 2 + 3 where their referenced classes exist)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ProviderConnectionTester.java,
    backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ConnectionTestResult.java,
    backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ModelsProbeClient.java,
    backend/core/src/main/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminService.java,
    backend/core/src/main/java/com/zeromail/core/llm/byok/BaseUrlValidator.java,
    backend/core/src/main/java/com/zeromail/core/llm/byok/HostResolver.java,
    backend/core/src/main/java/com/zeromail/core/llm/byok/ProviderAllowList.java,
    backend/core/src/main/java/com/zeromail/core/llm/byok/ByokRateLimiter.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/BaseUrlValidatorSsrfTest.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/ModelsProbeClient.java,
    backend/core/src/main/java/com/zeromail/core/admin/mkey/usecases/MasterKeyAdminService.java (locate the two call sites for probeConnection + fetchModelCatalog and the existing sentinel-leak scrub),
    backend/core/src/test/java/com/zeromail/core/admin/mkey/MasterKeySentinelLeakTest.java (ARCH-11 — must remain green),
    backend/core/src/main/java/com/zeromail/core/admin/mkey/usecases/MasterKeyRateLimiter.java (if exists — extract or mirror per RESEARCH § Open Question 6),
    Context7 query: `/spring-projects/spring-ai` for "RestClient base URL header override" + `/anthropic/anthropic-sdk-java` for "/v1/models list with x-api-key" (cross-check against the verified contract in <interfaces> block above)
  </read_first>
  <behavior>
    - `ConnectionTestResult` is a Java record: `ConnectionTestResult(MasterKeyTestResult result, List<String> models)` — models empty list when not OK.
    - `ProviderConnectionTester.probeConnection(LlmProvider provider, String baseUrl, byte[] plaintextKey) -> ConnectionTestResult`:
      - **Per-provider header strategy (NEW per BLOCKER #2):** select the auth/header transport based on `provider`:
        - `OPENAI`, `DEEPSEEK` → `Authorization: Bearer <key>`, list at `GET {baseUrl}/models`, parse `{data: [{id}]}`.
        - `ANTHROPIC` → `X-Api-Key: <key>` + `anthropic-version: 2023-06-01`, list at `GET {baseUrl}/models`, parse `{data: [{id, display_name, ...}]}`. Pagination params NOT needed for chat-capable list under default `limit=20` (Anthropic returns active families).
        - `GOOGLE` → provider-native (Google API key as `x-goog-api-key` header per the google-genai SDK convention; if `ModelsProbeClient` already implements this path for admin MKEY, reuse it).
        - This strategy lives INSIDE `ProviderConnectionTester` (not exposed to MasterKeyAdminService callers); existing `ModelsProbeClient.probeConnection/fetchModelCatalog` should either grow a `provider` arg if it doesn't already have one (verify — the existing signature carries `LlmProvider`), or `ProviderConnectionTester` constructs a per-provider `RestClient` instance with the right headers.
      - Compute `KeyFormat keyFormat = LlmProvider.defaultKeyFormat(provider)`.
      - `MasterKeyTestResult result = modelsProbeClient.probeConnection(...)` (now using the per-provider transport).
      - If result != OK → return `ConnectionTestResult(result, List.of())`.
      - Otherwise call `modelsProbeClient.fetchModelCatalog(...)`, filter to chat-completion-capable IDs (exclude `embedding`, `whisper`, `tts`, `dall-e`, `text-embedding`, `aqa` substrings per RESEARCH Pattern 3), cap at 100, return.
      - On `ProbeFailedException` → return `ConnectionTestResult(exception.reason(), List.of())`.
    - `MasterKeyAdminService.testConnection(...)` and `MasterKeyAdminService.testKey(...)` refactored to delegate to `providerConnectionTester.probeConnection(...)` — no behavior change visible to admin clients. The per-provider strategy ALSO benefits admin probes (admin probing an Anthropic master key now works correctly).
    - `BaseUrlValidator.validate(String baseUrl) -> ValidatedTarget` (per REVIEWS.md HIGH-3 SSRF hardening) — throws layered codes depending on which gate fails:
      - Length ≤ 255, valid URI parse, scheme is `https` (or `http` only when `spring.profiles.active=dev` AND host is exactly `localhost` / `127.0.0.1`) → otherwise `code=ai.byok.base_url_not_https`.
      - Host is NOT a literal IP in private/loopback/link-local/unspecified/CGNAT/multicast/IPv6-ULA range. Reject with `code=ai.byok.base_url_host_private`.
      - Host resolves via `HostResolver.resolve(host)` (uses `InetAddress.getAllByName` and rejects ANY resolved address that falls in any blocked range — covers `localhost.evil.tld`-style aliases pointing to 127.0.0.1, and any AAAA record landing on `::1`/`fc00::/7`/`fe80::/10`). Unresolvable → `code=ai.byok.base_url_host_unresolvable`.
      - Port: 443 always allowed; other ports only when listed in `byok.base-url.allowed-extra-ports` config (default empty in prod, `8080, 11434` in dev). Reject with `code=ai.byok.base_url_port_not_allowed`.
      - Returns `ValidatedTarget(URI uri, InetAddress resolvedAddress)` — `UserByokService.testConnection` re-resolves the host immediately before the RestClient call (in Task 2) and aborts if the resolved IP differs (TOCTOU/DNS-rebinding guard).
    - `HostResolver` is a small `@Component` wrapping `InetAddress.getAllByName` for testability (allows the SSRF test to stub responses).
    - `BaseUrlValidatorSsrfTest` (NEW per REVIEWS.md HIGH-3): parameterized over hostile inputs — `http://10.0.0.1`, `https://192.168.1.1`, `https://169.254.169.254` (AWS metadata), `https://127.0.0.1`, `https://localhost.evil.tld` (stub HostResolver → resolves to 127.0.0.1), `https://[::1]`, `https://[fc00::1]`, `https://api.openai.com:8443` outside allow-list. Each case asserts the correct `code=...` is raised. Positive cases: `https://api.openai.com/v1`, `https://api.anthropic.com/v1`, `https://generativelanguage.googleapis.com/v1beta`, `https://api.deepseek.com/v1` all pass; `http://localhost:11434` passes only when profile=dev.
    - `ProviderAllowList.validateForByok(String provider)` throws `ProviderNotAllowedException` (mapped to 400 `code=ai.byok.provider_not_allowed`) when provider ∉ {OPENAI, ANTHROPIC, GOOGLE, DEEPSEEK}.
    - `ByokRateLimiter` uses Redis Lettuce: `requireAllowance(UUID tenantId, String key, int max, Duration window)` increments a counter; throws `RateLimitExceededException` (mapped to HTTP 429 with `code=ai.byok.test_connection.rate_limited` or `code=voice.generate.rate_limited` based on the key). Sliding-window or fixed-window per RESEARCH § Open Question 6 — pick fixed window for simplicity.
    - **REVIEWS.md HIGH-1 RESEQUENCING:** the Wave-0 stubs `ProviderConnectionTesterSingleBindingTest` and `UserByokTestConnectionSentinelLeakTest` are NOT filled in this task — those tests reference `UserByokService` (lands in Task 2) and `UserByokController` (lands in Task 3) respectively. They are filled in their landing-class tasks: single-binding in Task 2, sentinel-leak in Task 3. This task fills only `BaseUrlValidatorSsrfTest` (depends on nothing beyond `BaseUrlValidator` + `HostResolver` written here). `MasterKeySentinelLeakTest` (ARCH-11 — from Phase 8) must remain GREEN after the MasterKeyAdminService refactor — verify by running `./gradlew :backend:core:test --tests MasterKeySentinelLeakTest`.
  </behavior>
  <action>
    Create `ProviderConnectionTester` as `@Component` in `core.llm.gateway.springai`. Move (don't duplicate) the model-catalog filter logic out of MasterKeyAdminService into the tester. Implement the per-provider header strategy: read `ModelsProbeClient`'s current signature first — if it already accepts `LlmProvider` and dispatches headers internally, just verify the Anthropic branch exists; otherwise pull header construction into `ProviderConnectionTester` itself by holding a `Map<LlmProvider, ProviderTransport>` strategy table or by composing a per-call `RestClient.Builder` that sets headers based on provider.

    **Anthropic-specific implementation contract (per BLOCKER #2 + verified contract in <interfaces>):**
    - URL: `GET {baseUrl}/models` (where `baseUrl` is `https://api.anthropic.com/v1` by default).
    - Headers: `X-Api-Key: <plaintextKey>` AND `anthropic-version: 2023-06-01`. Do NOT also send `Authorization: Bearer` — Anthropic ignores it but cleaner to omit.
    - Response parsing: deserialize `{data: [{id, ...}], has_more, ...}`; collect `data[].id` strings; apply the chat-completion-capability filter (exclude embedding/whisper/tts/dall-e/text-embedding/aqa substrings) — Anthropic IDs are claude-*; cap at 100.
    - Error handling: 401 → `INVALID_KEY`; 429 → `RATE_LIMITED`; timeout / connect failure → `NETWORK_ERROR`; 5xx → `NETWORK_ERROR`; sanitize all error bodies (do NOT include them in the result).

    Refactor MasterKeyAdminService to delegate. Verify `MasterKeySentinelLeakTest` stays green via `./gradlew :backend:core:test --tests MasterKeySentinelLeakTest`. Write `BaseUrlValidator`, `ProviderAllowList`, `ByokRateLimiter` as small `@Component`s. The rate limiter uses the existing `RedisTemplate` (or Lettuce equivalent) — read `backend/core/src/main/java/com/zeromail/core/shared/redis/` for the existing pattern. ArchUnit test for single-binding uses `noClasses().that().resideOutsideOfPackages("..mkey..", "..byok..").should().callMethodWhere(target(name("probeConnection")).and(declaredIn(ProviderConnectionTester.class)))`. The sentinel-leak test boots `@WebMvcTest(UserByokController.class)` with a mocked `UserByokService` that returns a `ConnectionTestResult` from a stub `ProviderConnectionTester` that has been seeded with a provider 401 body containing `SENTINEL_LEAK_X9Z2`; assert the controller response body does not contain that substring.

    **After completing this task: STOP. Commit. Run the verify command. Resume Task 2 in either the same window (if context < 35%) or a fresh window (read `09-04-SUMMARY.md` partial notes if you already started one).**
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests BaseUrlValidatorSsrfTest --tests MasterKeySentinelLeakTest</automated>
  </verify>
  <acceptance_criteria>
    - `ProviderConnectionTester.probeConnection` exists; `MasterKeyAdminService` delegates to it; `MasterKeySentinelLeakTest` (ARCH-11) is still GREEN
    - Per-provider header strategy verified: OPENAI/DEEPSEEK use `Authorization: Bearer`; ANTHROPIC uses `X-Api-Key` + `anthropic-version: 2023-06-01`; GOOGLE uses native auth (whichever ModelsProbeClient currently does or extend it)
    - `BaseUrlValidator` SSRF-hardened per REVIEWS.md HIGH-3 — `BaseUrlValidatorSsrfTest` covers all hostile cases (private IPs, loopback aliases, link-local, AWS metadata 169.254.169.254, IPv6 loopback, IPv6 ULA, non-default ports, scheme rejection) and positive cases (4 canonical provider URLs + dev localhost)
    - `ProviderAllowList` rejects OPENROUTER and ROUTER_9R; accepts OPENAI/ANTHROPIC/GOOGLE/DEEPSEEK
    - `ByokRateLimiter` 11th invocation in an hour throws `RateLimitExceededException` (mapped HTTP 429 by controllers in next tasks)
    - **`ProviderConnectionTesterSingleBindingTest` and `UserByokTestConnectionSentinelLeakTest` are NOT filled in this task** — they land in Tasks 2 and 3 respectively per REVIEWS.md HIGH-1 resequencing (this task's behavior block explains the move)
  </acceptance_criteria>
  <done>Shared tester extracted with per-provider header strategy; admin MKEY refactor green; allow-list + URL validator + rate limiter ready to inject in Task 2. **Commit + verify before starting Task 2.**</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserByokService (locked Save→Test→Pick→Activate lifecycle per HIGH-4) + ByokProviderResolver + replace TenantByokProviderCredentialResolver + REWRITE legacy ByokController as 410-Gone shim per HIGH-2 + ProviderConnectionTesterSingleBindingTest (single-binding test moved here from Task 1 per HIGH-1)</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokService.java,
    backend/core/src/main/java/com/zeromail/core/llm/byok/ByokProviderResolver.java,
    backend/core/src/main/java/com/zeromail/core/llm/gateway/LlmGatewayImpl.java,
    backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/SpringAiChatModelFactory.java,
    backend/api/src/main/java/com/zeromail/api/controllers/llm/ByokController.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/ProviderConnectionTesterSingleBindingTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/ByokResolutionIntegrationTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/ByokSaveResetsStateTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/UserByokKeySingleRowPerTenantTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/ByokTestConnectionRateLimitTest.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/llm/gateway/LlmGatewayImpl.java (current injection of TenantByokProviderCredentialResolver),
    backend/core/src/main/java/com/zeromail/core/llm/gateway/springai/SpringAiChatModelFactory.java (current injection + per-call resolver invocation),
    backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokKeyEntity.java (from 09-01),
    backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokKeyRepository.java (from 09-01),
    backend/core/src/main/java/com/zeromail/core/llm/gateway/TenantByokProviderCredentialResolver.java (legacy — to be deleted),
    backend/core/src/main/java/com/zeromail/core/crypto/RefreshTokenCipher.java
  </read_first>
  <behavior>
    - **REVIEWS.md HIGH-4 — Locked BYOK Lifecycle.** The user-visible state machine is exactly: `Save → Test stored row → Pick model → Activate`. There is NO inline-pre-save Test path. POST /api/byok/test-connection accepts ONLY the stored-row form (no inline payload fields in the request body). Rationale: an inline pre-save Test that returned `models[]` could not legitimately survive Save because Save resets `last_test_result=NULL` — so the user would always have to Test again after Save, contradicting the UI order. Locking the contract removes the contradiction.
    - `UserByokService` exposes:
      - `save(UUID tenantId, ByokSaveCommand command)` → validates provider allow-list + base URL via SSRF-hardened `BaseUrlValidator.validate(...)` (Task 1) → encrypts key via RefreshTokenCipher → upserts the single row with `active=false, last_test_result=NULL, last_tested_at=NULL, last_test_models_json=NULL, model_id=NULL` (resets ALL gating state per SET-AI-02 acceptance — `modelId` is intentionally NULLed even if the previous row had one because the new key may not support the previously-picked model).
      - `load(UUID tenantId) -> Optional<ByokRowSummary>`.
      - `testConnection(UUID tenantId)` → load stored row → 404 `code=ai.byok.no_row` if absent → rate-limit (`byok.test_connection`, 10/h per tenant) → decrypt key → re-resolve host via `HostResolver` (TOCTOU/DNS-rebinding guard from Task 1) → `providerConnectionTester.probeConnection(...)` → persist `last_test_result + last_tested_at` always; on OK ALSO persist `last_test_models_json` (the returned model list serialized to JSONB; capped at 100 IDs) → return `ConnectionTestResult`. Scrub plaintext bytes via `Arrays.fill` in finally.
      - `setModel(UUID tenantId, String modelId)` → require `last_test_result='OK'` AND `modelId` is present in the persisted `last_test_models_json` list (server-side membership check — answers REVIEWS.md MEDIUM Codex setModel concern); reject otherwise with 400 `code=ai.byok.model_not_in_last_test`.
      - `activate(UUID tenantId, boolean active)` → if active=true and (modelId null OR last_test_result != 'OK') throw `ByokActivationGateException` (400 `code=ai.byok.no_model_picked`); else flip `active` flag.
      - `delete(UUID tenantId)` → delete row.
    - `ByokProviderResolver.resolve(UUID tenantId) -> Optional<ResolvedLlmProviderCredential>`: load row; gate: `row.active && row.modelId != null && "OK".equals(row.lastTestResult)`; if gated → decrypt key, build `ResolvedLlmProviderCredential` per Pattern 2; finally scrub plaintext via `Arrays.fill(plaintextKey, (byte) 0)`.
    - `LlmGatewayImpl` + `SpringAiChatModelFactory` swap their injected resolver type from `TenantByokProviderCredentialResolver` to `ByokProviderResolver` (same `Optional<ResolvedLlmProviderCredential>` shape — should be a rename + bean type change). Legacy `TenantByokProviderCredentialResolver` class is DELETED.
    - **REVIEWS.md HIGH-2 — Legacy `ByokController` 410-Gone shim.** REWRITE `backend/api/src/main/java/com/zeromail/api/controllers/llm/ByokController.java` (do NOT delete the file in this plan — the file is deleted in plan 09-06 alongside the FE `ByokForm.tsx`). Every existing endpoint under `@RequestMapping("/api/llm/byok")` is replaced with a `@Deprecated` handler returning HTTP 410 Gone, body `{code: "ai.byok.moved", message: "Use /api/byok instead"}`, and `Location: /api/byok` header. The shim has no dependencies on the legacy `ByokService` / `TenantByokCredentialsEntity` so those classes can still be deleted in this task without breaking the controller. Rationale: between 09-04 merge and 09-06 merge, `backend/api` must still compile — without the shim, deleting the controller here would break the build until 09-06 lands the new `/api/byok` controller.
    - **`ProviderConnectionTesterSingleBindingTest` (moved here from Task 1 per REVIEWS.md HIGH-1):** ArchUnit asserts that only `MasterKeyAdminService` (Phase 8) AND `UserByokService` (this task) may call `ProviderConnectionTester.probeConnection`. `noClasses().that().resideOutsideOfPackages("..mkey..", "..byok..").should().callMethodWhere(target(name("probeConnection")).and(declaredIn(ProviderConnectionTester.class)))`. Now that `UserByokService` exists in this task, the test can finally compile and pass — it would have failed compilation in Task 1.
    - Wave-0 stubs:
      - `ByokResolutionIntegrationTest`: integration test stubs an HTTP server at the BYOK base URL; seeds a `user_byok_key` row with `active=true, modelId='gpt-4o-mini', lastTestResult='OK'`; invokes `LlmGatewayImpl.chat(...)`; asserts the stubbed server received a request whose body's `model` field equals `gpt-4o-mini` AND request URL is the saved `base_url`. Then flips `active=false` and asserts the next call goes to the admin-catalog platform default URL.
      - `ByokSaveResetsStateTest` (`@DataJpaTest` — per REVIEWS.md MEDIUM Codex testing-fundamentals: use `TestEntityManager` + `flush()` + `clear()` before read-back assertions to avoid Hibernate L1-cache false positives): save a row with `active=true, lastTestResult='OK', lastTestedAt=now, modelId='gpt-4o', lastTestModelsJson='[...]'`; then call `UserByokService.save(...)` with a new provider; flush + clear; reload the row and assert `active=false, lastTestResult=NULL, lastTestedAt=NULL, modelId=NULL, lastTestModelsJson=NULL` — the FULL reset per HIGH-4.
      - `UserByokKeySingleRowPerTenantTest` (`@DataJpaTest` with TestEntityManager flush+clear discipline): save twice for the same tenant; assert exactly 1 row exists with the second provider's values.
      - `ByokTestConnectionRateLimitTest`: invoke `UserByokService.testConnection` 10 times → all succeed; 11th throws `RateLimitExceededException`. **No inline-payload variant per HIGH-4 — testConnection has only the stored-row form.**
  </behavior>
  <action>
    Lay down `UserByokService` (~280 lines — covers 5 mutation methods + testConnection (stored-row only) + state-reset logic + rate-limit hook + TOCTOU re-resolve before probe). Use `@Transactional` per method (read-only where appropriate). The save path: validate (SSRF-hardened) → encrypt → repo.save (upsert via JPA `merge` since PK is tenantId — or use `findByTenantId` + new entity construction when absent) → reset modelId + lastTestResult + lastTestedAt + lastTestModelsJson to NULL. `ByokProviderResolver` is simple per Pattern 2 in RESEARCH. Swap the resolver bean type in `LlmGatewayImpl` + `SpringAiChatModelFactory`. Delete the legacy `TenantByokProviderCredentialResolver` class + legacy `TenantByokCredentialsEntity` / `ByokService` (NOT `ByokController` — it stays as a shim per HIGH-2). REWRITE `ByokController` as the 410-Gone shim described in `<behavior>`. The legacy `ByokForm.tsx` is deleted in 09-06.

    Fill the 4 Wave-0 stubs PLUS the resequenced `ProviderConnectionTesterSingleBindingTest`. Per RESEARCH Open Question 3 (Google base URL): if Spring AI google-genai adapter does NOT accept per-call baseUrl override, add a check in `UserByokService.save(...)` that rejects non-default base URL for `GOOGLE` provider with `code=ai.byok.base_url_not_supported_for_provider`; document the decision in 09-04-SUMMARY.

    **After completing this task: STOP. Commit. Run the verify command. Resume Task 3 in either the same window (if context < 35%) or a fresh window.**
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests ProviderConnectionTesterSingleBindingTest --tests ByokResolutionIntegrationTest --tests ByokSaveResetsStateTest --tests UserByokKeySingleRowPerTenantTest --tests ByokTestConnectionRateLimitTest && ./gradlew :backend:api:compileJava</automated>
  </verify>
  <acceptance_criteria>
    - Saving a row resets ALL gating state to default: `active=false, lastTestResult=NULL, lastTestedAt=NULL, modelId=NULL, lastTestModelsJson=NULL` (assertion green via TestEntityManager flush+clear)
    - Saving twice yields exactly 1 row per tenant (PK on tenant_id enforces single-row)
    - Rate limit 10/h enforced on `testConnection` (stored-row only — no inline path per HIGH-4)
    - `ByokResolutionIntegrationTest`: with active+tested+model, chat call hits saved `base_url` + uses saved `model_id`; with active=false, chat call hits admin-catalog default URL
    - `ProviderConnectionTesterSingleBindingTest` (resequenced from Task 1) is GREEN — `UserByokService` and `MasterKeyAdminService` are the only callers
    - Legacy `TenantByokProviderCredentialResolver`, `TenantByokCredentialsEntity`, `ByokService` are gone; verify by `mcp__jetbrains__search_in_files_by_text "TenantByokProviderCredentialResolver"` returns zero hits outside test fixtures
    - `ByokController` exists but every endpoint returns 410 Gone with body `{code: "ai.byok.moved", ...}` and `Location: /api/byok` header — `backend/api:compileJava` succeeds (no broken legacy dependencies)
    - Plaintext byte arrays scrubbed via `Arrays.fill` after handoff
  </acceptance_criteria>
  <done>BYOK resolver + service + rate limiter live; legacy resolver removed; ByokController rewritten as 410-Gone shim; chat/triage/draft pipelines call the right URL/model depending on active flag; locked Save→Test→Pick→Activate lifecycle enforced. **Commit + verify before starting Task 3.**</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UserByokController + SettingsAiCostController + AiCostQueryService + DTOs + 7 controller stubs (including new Anthropic case in ByokTestConnectionEnumOnlyTest per BLOCKER #2) + UserByokTestConnectionSentinelLeakTest moved here per REVIEWS.md HIGH-1 (controller exists in this task)</name>
  <files>
    backend/api/src/main/java/com/zeromail/api/controllers/byok/UserByokController.java,
    backend/api/src/main/java/com/zeromail/api/controllers/settings/SettingsAiCostController.java,
    backend/core/src/main/java/com/zeromail/core/llm/cost/AiCostQueryService.java,
    backend/api/src/main/java/com/zeromail/api/dto/byok/ByokResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/byok/ByokSaveRequest.java,
    backend/api/src/main/java/com/zeromail/api/dto/byok/ByokActivateRequest.java,
    backend/api/src/main/java/com/zeromail/api/dto/byok/ByokModelRequest.java,
    backend/api/src/main/java/com/zeromail/api/dto/byok/ByokTestConnectionResponse.java,
    backend/api/src/main/java/com/zeromail/api/dto/settings/AiCostResponse.java,
    backend/core/src/test/java/com/zeromail/core/llm/cost/AiCostQueryService7DayTest.java,
    backend/core/src/test/java/com/zeromail/core/llm/byok/UserByokTestConnectionSentinelLeakTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokActivateGateModelMissingTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokActivateGateNotTestedTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokSaveProviderAllowListTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokSaveBaseUrlValidationTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokResponseNeverEchoesPlaintextTest.java,
    backend/api/src/test/java/com/zeromail/api/controllers/byok/ByokTestConnectionEnumOnlyTest.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/llm/byok/UserByokService.java (from Task 2),
    backend/core/src/main/java/com/zeromail/core/llm/audit/ (locate llm_call_audit entity + repository — likely Spring Data JDBC),
    backend/api/src/main/java/com/zeromail/api/controllers/admin/AdminMasterKeyController.java (existing controller for MKEY — mirror its error-mapping shape for consistency),
    apps/web/AGENTS.md (DTO emit rules so openapi-typescript generates accurate required/nullable)
  </read_first>
  <behavior>
    - `ByokSaveRequest`: `provider @Pattern("^(OPENAI|ANTHROPIC|GOOGLE|DEEPSEEK)$") String`, `baseUrl @Size(min=8,max=255) String`, `apiKey @Size(min=8,max=256) String`, `modelId @Size(max=64) String (nullable)`. `@Schema(requiredProperties = {"provider","baseUrl","apiKey"})` + `@Schema(allowableValues = {...})` on provider.
    - `ByokResponse`: `provider`, `baseUrl`, `lastFourChars`, `modelId` (nullable), `active`, `lastTestResult` (nullable enum), `lastTestedAt` (nullable). NEVER `apiKey`. `@JsonInclude(JsonInclude.Include.NON_NULL)` for nullable fields.
    - `ByokActivateRequest`: `active @NotNull Boolean`.
    - `ByokModelRequest`: `modelId @NotBlank @Size(max=64) String`.
    - **REVIEWS.md HIGH-4 — NO `ByokTestConnectionRequest` DTO.** POST /api/byok/test-connection has an EMPTY body — the request shape is `POST /api/byok/test-connection` with no fields. The handler signature is `public ResponseEntity<ByokTestConnectionResponse> testConnection() { ... }`. This locks the contract: the user MUST Save first, then Test the stored row. There is no inline-pre-save Test path.
    - `ByokTestConnectionResponse`: `result` (enum string), `models` (nullable List<String>, only on OK). `@JsonInclude(NON_NULL)`.
    - `AiCostResponse`: single field `usd: BigDecimal` (or `double`). `@Schema(requiredProperties = {"usd"})`.
    - `UserByokController` at `/api/byok`:
      - `GET /` → 200 + ByokResponse OR 404 if no row
      - `POST /` → 201 + ByokResponse (after save reset — modelId, lastTestResult, lastTestedAt, lastTestModelsJson ALL null per HIGH-4)
      - `PUT /active` → 200 + ByokResponse on toggle; 400 `code=ai.byok.no_model_picked` on activation gate failure
      - `PUT /model` → 200 + ByokResponse after model selection; 400 `code=ai.byok.model_not_in_last_test` when modelId not in persisted last_test_models_json
      - `DELETE /` → 204
      - `POST /test-connection` (empty body per HIGH-4) → 200 + ByokTestConnectionResponse (persists last_test_result + last_tested_at + last_test_models_json on stored-row path); 404 `code=ai.byok.no_row` when no stored row; 429 `code=ai.byok.test_connection.rate_limited` on rate limit
    - `SettingsAiCostController` at `GET /api/settings/ai/cost?window=7d` → 200 + AiCostResponse. Currently only `window=7d` supported; other values → 400 `code=ai.cost.window_invalid`.
    - `AiCostQueryService.totalUsdLast7Days(UUID tenantId) -> BigDecimal`: Spring Data JDBC SQL `SELECT COALESCE(SUM(total_cost_usd), 0) FROM llm_call_audit WHERE tenant_id = ? AND created_at >= now() - interval '7 days'`. Use existing index. Returns BigDecimal scaled to 2 decimal places.
    - Wave-0 stubs:
      - **`UserByokTestConnectionSentinelLeakTest` (moved here from Task 1 per REVIEWS.md HIGH-1 — controller now exists in this task):** boot `@WebMvcTest(UserByokController.class)` with `@MockitoBean UserByokService` returning a `ConnectionTestResult` from a stub `ProviderConnectionTester` seeded with a provider 401 body containing `SENTINEL_LEAK_X9Z2`. Call POST `/api/byok/test-connection`; assert the serialized response body does NOT contain the substring `SENTINEL_LEAK_X9Z2`. Run for BOTH `Authorization: Bearer` providers (OPENAI / DEEPSEEK) AND Anthropic's `x-api-key` provider so the per-provider transport strategy can't introduce a leak class. Per REVIEWS.md MEDIUM (Codex Spring Security): include `@WithMockUser` + `with(csrf())` (or whatever the project's MVC test convention is — read `apps/api`'s existing POST tests to match) so the secured POST endpoint doesn't 403 the test before the assertion runs.
      - `AiCostQueryService7DayTest` (`@DataJpaTest` / `@JdbcTest` with TestEntityManager flush+clear discipline): seed 3 audit rows over the past 5 days + 2 rows older than 7 days; assert SUM == sum of in-window rows (older rows excluded); zero-row tenant returns `BigDecimal.ZERO`; response shape contains exactly one key `usd` (no per-feature keys). Add `@Transactional(timeout = 5)` to `AiCostQueryService.totalUsdLast7Days` per REVIEWS.md MEDIUM (OpenCode cost endpoint fragility) so a busy tenant cannot hang a worker thread.
      - `ByokActivateGateModelMissingTest`: seed row with `modelId=NULL, lastTestResult='OK'`; PUT /active {active:true} → 400 `code=ai.byok.no_model_picked`.
      - `ByokActivateGateNotTestedTest`: seed row with `modelId='gpt-4o', lastTestResult=NULL`; PUT /active {active:true} → 400 `code=ai.byok.no_model_picked`.
      - `ByokSaveProviderAllowListTest`: POST with `provider='OPENROUTER'` → 400 `code=ai.byok.provider_not_allowed`.
      - `ByokSaveBaseUrlValidationTest`: POST with `baseUrl='http://attacker.com'` → 400 `code=ai.byok.base_url_not_https`; POST with `baseUrl='https://api.openai.com/v1'` → 201.
      - `ByokResponseNeverEchoesPlaintextTest`: POST a key with prefix `sk-` and 40 chars total; serialize response JSON; regex-assert `sk-[A-Za-z0-9]{20,}` returns zero matches in response payload. Repeat for GET /api/byok and PUT /active responses.
      - `ByokSaveSsrfRejectionTest` (NEW per REVIEWS.md HIGH-3): POST `/api/byok` with `baseUrl=https://169.254.169.254` (AWS metadata endpoint), `https://10.0.0.5`, `https://[::1]`, and `https://localhost.evil.tld` (with a stubbed `HostResolver` returning `127.0.0.1`) — each returns 400 with the appropriate SSRF code (`ai.byok.base_url_host_private` / `ai.byok.base_url_host_unresolvable`). Positive case: `https://api.openai.com/v1` → 201.
      - `ByokTestConnectionEnumOnlyTest` — **EXPANDED per BLOCKER #2**: parameterized across all 4 allow-listed providers (OPENAI, ANTHROPIC, GOOGLE, DEEPSEEK). Each test uses `POST /api/byok/test-connection` with an EMPTY BODY (per HIGH-4) — the seeded `user_byok_key` row in the test fixture supplies the provider context. For each provider:
        - **Case A (`<provider>Provider_returnsOkWithModels`)**: stub `ProviderConnectionTester` to return `(OK, [model_id_1, model_id_2, ..., model_id_5])` → response = `{result:'OK', models:[...]}`; for ANTHROPIC, the stub also asserts the underlying HTTP probe used `X-Api-Key` + `anthropic-version: 2023-06-01` headers (verified by capturing the headers the stub received).
        - **Case B (`<provider>Provider_returnsInvalidKeyWithoutLeak`)**: stub returns `INVALID_KEY` with a raw 401 body containing `sk-leaked-AB12CD34` → response is exactly `{result:'INVALID_KEY'}` (NO `models` key, NO leaked substring).
        - **Case C (`<provider>Provider_modelListCappedAt100`)**: stub returns 150 model IDs → response models length == 100.
        - **Anthropic-specific assertion:** the Case A test for ANTHROPIC additionally asserts that the response JSON shape (`{result, models}`) is structurally identical to the OpenAI Case A response — proving the per-provider transport produces a normalized output and the controller doesn't leak Anthropic-specific shape (e.g. `display_name`, `capabilities`, `max_input_tokens`) into the response.
  </behavior>
  <action>
    Build the controllers thin (CONVENTIONS §1) — they map DTO ↔ service commands + return ResponseEntity with proper status codes. Use Spring `@RestControllerAdvice` (or existing project error handler) to translate `ByokActivationGateException`, `BaseUrlInvalidException`, `ProviderNotAllowedException`, `RateLimitExceededException`, `KnowledgeSnippetNotFoundException`, etc. into HTTP responses with the documented `code=...` strings. DTO records emit accurate OpenAPI; `apps/web/lib/api/schema.d.ts` is regenerated in plan 09-06. AiCostQueryService uses Spring Data JDBC `JdbcClient` (matches existing project pattern per RESEARCH § Standard Stack). Fill all 7 Wave-0 stubs with the expanded Anthropic-coverage per BLOCKER #2.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests AiCostQueryService7DayTest --tests UserByokTestConnectionSentinelLeakTest && ./gradlew :backend:api:test --tests ByokActivateGateModelMissingTest --tests ByokActivateGateNotTestedTest --tests ByokSaveProviderAllowListTest --tests ByokSaveBaseUrlValidationTest --tests ByokSaveSsrfRejectionTest --tests ByokResponseNeverEchoesPlaintextTest --tests ByokTestConnectionEnumOnlyTest</automated>
  </verify>
  <acceptance_criteria>
    - All 6 BYOK endpoints respond per the spec acceptance criteria (save reset, activate gate, allow-list, SSRF-hardened base URL validation, model membership check, plaintext-never-echo, enum-only response)
    - `GET /api/settings/ai/cost?window=7d` returns exactly `{usd: <decimal>}` (no per-feature keys); query has 5s transactional timeout per REVIEWS.md MEDIUM
    - POST `/api/byok/test-connection` has an EMPTY request body per HIGH-4; 404 `code=ai.byok.no_row` when no stored row; 429 `code=ai.byok.test_connection.rate_limited` on rate limit
    - `UserByokTestConnectionSentinelLeakTest` (resequenced from Task 1) is GREEN — provider 401 body sentinel never appears in response across all 4 providers
    - `ByokSaveSsrfRejectionTest` (new) GREEN — 4 hostile baseUrl cases each rejected with the right code
    - 8 Wave-0 stubs in this task are GREEN
    - `ByokTestConnectionEnumOnlyTest` runs all 4 providers (OPENAI, ANTHROPIC, GOOGLE, DEEPSEEK) through Cases A/B/C (12 parameterized cases total); the Anthropic Case A specifically asserts the probe used `X-Api-Key` + `anthropic-version: 2023-06-01` headers per BLOCKER #2 fix
  </acceptance_criteria>
  <done>Full BYOK + cost surface live; 7 Wave-0 stubs green (including Anthropic-specific cases); ready for FE consumption after 09-06 codegen step.</done>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:test :backend:api:test --tests "*Byok*" --tests "*ProviderConnectionTester*" --tests "*AiCost*" --tests "*MasterKey*"` all green (including ARCH-11 MasterKeySentinelLeakTest)
- ArchUnit single-binding test confirms only `MasterKeyAdminService` + `UserByokService` invoke `ProviderConnectionTester.probeConnection`
- Legacy `TenantByokProviderCredentialResolver`, `TenantByokCredentialsEntity`, `ByokService` are removed (no compile references); `ByokController` REWRITTEN as 410-Gone shim per HIGH-2 (file deleted in 09-06 alongside ByokForm.tsx)
- Plaintext-leak snapshot: response payloads from POST/PUT/GET /api/byok contain zero `sk-[A-Za-z0-9]{20,}` matches
- Sentinel-leak: provider 401 body with `SENTINEL_LEAK_X9Z2` never appears in test-connection response (verified for all 4 providers including Anthropic per BLOCKER #2)
- `ByokTestConnectionEnumOnlyTest` Anthropic case is GREEN — SET-AI-04 acceptance holds for ALL 4 allow-listed providers
- No new packages installed
</verification>

<success_criteria>
- 4 requirement IDs satisfied: SET-AI-01, SET-AI-02, SET-AI-03, SET-AI-04 (with full Anthropic coverage per BLOCKER #2)
- ProviderConnectionTester extracted and shared with admin MKEY-03 (single binding) with per-provider header strategy
- ByokProviderResolver replaces legacy resolver as single resolver bean
- 15 Wave-0 stubs flipped GREEN (was 14 — added `BaseUrlValidatorSsrfTest` + `ByokSaveSsrfRejectionTest` per HIGH-3; net delta +1 because `UserByokTestConnectionSentinelLeakTest` was already counted)
- Cost endpoint returns single tenant-wide value (no per-feature breakdown)
- Per-task commit discipline kept context usage below 50% during execution
</success_criteria>

<output>
Create `.planning/phases/09-user-settings-ui-on-curated-catalog/09-04-SUMMARY.md`. Document the ProviderConnectionTester extraction, per-provider header strategy details (especially the Anthropic `x-api-key` + `anthropic-version: 2023-06-01` contract verified against https://platform.claude.com/docs/en/api/models-list during planner-checker iteration 1), legacy-resolver removal, Google base-URL O-3 decision (rejected or accepted), plaintext-leak audit results, any rate-limiter primitive choices, and confirmation that per-task commit discipline was followed.
</output>
