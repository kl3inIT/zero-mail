---
phase: 09-user-settings-ui-on-curated-catalog
plan: 05
type: execute
wave: 1
depends_on:
  - 09-01
  - 09-04
files_modified:
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationService.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationCommand.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationResult.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/GmailSentMessagesReader.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/QuotedReplyStripper.java
  - backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationPrompt.java
  - backend/core/src/main/java/com/zeromail/core/llm/byok/ByokRateLimiter.java
  - backend/api/src/main/java/com/zeromail/api/controllers/settings/VoiceGenerateController.java
  - backend/api/src/main/java/com/zeromail/api/dto/settings/GenerateFromSentRequest.java
  - backend/api/src/main/java/com/zeromail/api/dto/settings/GenerateFromSentResponse.java
  - backend/api/src/main/java/com/zeromail/api/config/SpringAiObservationProperties.java
  - backend/api/src/main/resources/application.yml
  - backend/worker/src/main/resources/application.yml
  - backend/api/src/test/java/com/zeromail/api/config/SpringAiObservationDisabledTest.java
  - backend/core/src/test/java/com/zeromail/core/voice/VoiceGenerationFromSentLeakTest.java
  - backend/core/src/test/java/com/zeromail/core/voice/VoiceGenerationRateLimitTest.java
autonomous: true
requirements:
  - SET-VOICE-07

must_haves:
  truths:
    - "POST /api/settings/voice/generate-from-sent (request: {sampleSize: number}, default 20, max 50) returns {generatedStyle: string} ≤ 500 words"
    - "Raw email bodies, LLM prompt, and LLM completion exchange NEVER persist to DB, log files, or audit rows (privacy invariant D-11)"
    - "Sentinel-seed integration test asserts zero leaks of seeded sentinel content across DB rows, log lines, and audit entries after the generate path runs"
    - "Rate limit 3 generations/hour/tenant — 4th call/hour returns 429 with code=voice.generate.rate_limited"
    - "Aggregate prompt cap (per REVIEWS.md HIGH-3): the joined Sent-sample text fed to the LLM is bounded to MAX_AGGREGATE_PROMPT_CHARS=60_000 (60k characters ≈ 15k tokens — well under any provider's context window and well above the 200k-char worst-case Codex flagged). Per-sample body cap stays at 4000 chars; sampleSize cap stays at 50, but GmailSentMessagesReader truncates the joined corpus at 60k regardless of sampleSize × per-sample size. The cap is enforced AFTER quoted-reply stripping."
    - "Quoted-reply stripping (per REVIEWS.md HIGH-3): GmailSentMessagesReader strips quoted inbound content from each sample BEFORE truncation. Detection rules: drop lines starting with `>` (RFC 3676 quote prefix); drop lines after the first occurrence of any of `^On .* wrote:$`, `^From: .*$`, `^-----Original Message-----`, `^________________________________` (Outlook quote separator), and the Vietnamese variants `^Vào .* đã viết:$`, `^Người gửi: .*$`. The sentinel-leak test in Task 2 adds a case where the seeded quoted-reply portion contains LEAK_SENTINEL_QUOTED — the prompt fed to the mocked ChatModel must NOT contain that sentinel."
    - "Zero sent messages returns HTTP 200 + {generatedStyle: ''} (empty-state UI semantics)"
    - "Spring AI prompt/completion capture (observation hooks) is DISABLED in both backend/api and backend/worker application.yml; property-binding test asserts BOTH (a) the bound POJO fields evaluate false AND (b) Environment.containsProperty(...) returns true for the exact property keys (per planner-checker WARNING #6 — guards against silently binding to a non-existent Spring AI knob)"
    - "An end-to-end observation snapshot integration test issues a real ChatModel call against a stub model and asserts the captured observation contains NO prompt/completion text fields — even if the property keys are wrong, this catches the regression at the behavior layer"
    - "The user must explicitly click Save (PUT /api/settings/voice) to persist the generated style — endpoint itself NEVER writes to assistant_settings"
  artifacts:
    - path: "backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationService.java"
      provides: "Strict-in-memory generate path — calls GmailSentMessagesReader + LlmGateway; never persists"
    - path: "backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/GmailSentMessagesReader.java"
      provides: "Wraps GmailApiClientFactory; lists + reads in:sent messages; returns method-local SentMessageSummary list (no DB write)"
    - path: "backend/api/src/main/java/com/zeromail/api/controllers/settings/VoiceGenerateController.java"
      provides: "POST /api/settings/voice/generate-from-sent endpoint"
    - path: "backend/api/src/main/java/com/zeromail/api/config/SpringAiObservationProperties.java"
      provides: "Property-binding test target asserting prompt/completion capture is OFF + the property keys actually exist in the resolved Environment"
  key_links:
    - from: "VoiceGenerationService"
      to: "LlmGateway.chat"
      via: "PREVIEW CallSite (no audit prompt/completion persistence)"
      pattern: "CallSite\\.PREVIEW"
    - from: "VoiceGenerateController"
      to: "ByokRateLimiter.requireAllowance"
      via: "rate limit check before service invocation"
      pattern: "byokRateLimiter\\.requireAllowance.*voice\\.generate"
    - from: "application.yml"
      to: "spring.ai prompt/completion observation"
      via: "explicit `false` setting"
      pattern: "(log-prompt|include-prompt|include-completion).*false"
---

<objective>
Ship SET-VOICE-07 "Generate writing style from recent sent emails" under the LOCKED D-11 privacy invariant: raw email bodies + LLM prompts + LLM completions are in-memory-only — no DB write, no log line, no audit row. The endpoint returns a generated style guide that the user reviews + edits + explicitly saves via the existing `PUT /api/settings/voice` from plan 09-02.

Purpose: highest-risk privacy surface in Phase 9. Sentinel-seed integration test must prove no leak across DB + log + audit before this plan is signed off. Per planner-checker WARNING #6: the Spring AI observation property keys are still `[ASSUMED]` in RESEARCH at planning time — the binding test must guard against silently binding to a non-existent key by ALSO checking `Environment.containsProperty(...)` and by running a behavior-layer observation snapshot test.

Output: 1 service + 1 Gmail reader + 1 prompt template + 1 controller + 2 DTO records + Spring AI observation hardening (yml + bound POJO + property-binding test + observation snapshot test) + 2 ❌ Wave-0 stubs flipped green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-SPEC.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-CONTEXT.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-RESEARCH.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-VALIDATION.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-01-SUMMARY.md
@CLAUDE.md
@CONVENTIONS.md

<interfaces>
From `backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java`:
- Existing factory provides a per-tenant authenticated Gmail client. Methods (verify): `Gmail clientForTenant(UUID tenantId)` — handles token refresh + RefreshTokenCipher decrypt. Use this; do not write custom OAuth flow.

From Gmail API:
- `users.messages.list` with query `in:sent` paginated; `users.messages.get(id, format=FULL or METADATA)` for body extraction. For SET-VOICE-07 we read body content method-locally and discard after passing to the LLM — `format=FULL` then base64-decode the body parts.

From `LlmGateway.chat(LlmChatRequest, CallSite)`:
- Existing gateway. `CallSite.PREVIEW` enum value already exists per RESEARCH Pattern 6 — used in chat-tool preview path which does NOT persist prompt/completion to `llm_call_audit`. Use this same CallSite to ensure no audit row records prompt/completion text.

From `ByokRateLimiter` (built in plan 09-04 Task 1):
- `requireAllowance(UUID tenantId, String key, int max, Duration window)` throws `RateLimitExceededException` (HTTP 429). Use key `voice.generate` with `max=3, window=1h`.

Spring AI M7 observation config (per RESEARCH Pitfall 1 — property keys `[ASSUMED]` at planning time):
- Property keys MUST be verified via Context7 `/spring-projects/spring-ai` query for "observation include-prompt" (and "include-completion") BEFORE editing yml. The most likely keys per Spring AI M7 docs are:
  - `spring.ai.chat.client.observations.include-prompt: false`
  - `spring.ai.chat.client.observations.include-completion: false`
  but the actual M7 names may be `spring.ai.chat.observations.log-prompt`, `spring.ai.chat.client.observations.log-prompt-content`, or similar. The executor confirms via Context7 then locks the yml + the `@ConfigurationProperties` prefix in `SpringAiObservationProperties`.
- The property-binding test MUST use BOTH:
  - **Mechanism A — bound POJO check:** `@SpringBootTest` boot the api profile, autowire `SpringAiObservationProperties`, assert `includePrompt() == false && includeCompletion() == false`.
  - **Mechanism B — Environment key existence check (per planner-checker WARNING #6):** autowire `org.springframework.core.env.Environment`, assert `environment.containsProperty("<verified.property.key.for.include-prompt>") == true` AND `environment.containsProperty("<verified.property.key.for.include-completion>") == true`. This catches the failure mode where the bound POJO has the right field names but Spring AI's actual property keys are different — Mechanism A would pass with both flags `false` (their default), but Spring AI would still be capturing prompt/completion because nobody's reading our properties.
  - **Mechanism C — observation snapshot integration test (per planner-checker WARNING #6):** issue a real `ChatModel.call(...)` against a STUB ChatModel (per TESTING.md — no real LLM in CI) wired through the same Spring AI auto-config the api would use; capture the emitted observation via a test `ObservationRegistry` with a `TestObservationRegistryAssert` (Micrometer test helper) or equivalent; assert the captured observation has NO `gen_ai.prompt` / `gen_ai.completion` / `prompt.text` / `completion.text` keyValue entries. This is the strongest gate — even if both property-name guesses are wrong, the behavior gate catches the regression.

PREVIEW CallSite + `llm_call_audit`:
- Verify the existing `CallSite.PREVIEW` either (a) skips audit insertion entirely, OR (b) writes audit rows WITHOUT prompt/completion text. If (b), the sentinel-leak test will catch any prompt/completion bytes; if (a), confirm by grep `CallSite.PREVIEW` in the audit pipeline. **The 09-05-SUMMARY MUST document which of (a)/(b) is true — it affects future PREVIEW callers.**

Style-extraction prompt:
- A short system prompt instructing the LLM to extract style (tone, formality, sentence length, common phrases) from N sample emails and produce a ≤ 500-word style guide. Outcome-first prompting per CLAUDE.md rule. Pass samples as anonymized snippets (e.g. `[SAMPLE 1] ... [SAMPLE N]`) — the LLM produces ONE summary string.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail API → backend | Per-tenant access to user's Sent folder; tokens via existing OAuth refresh cipher |
| backend memory → LLM | Raw email bodies → prompt → completion; all bytes in-process |
| backend → DB / log / audit | MUST NOT persist prompts, completions, or bodies (D-11) |
| /api/settings/voice/generate-from-sent → client | Response carries the user-reviewable style guide string only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information disclosure | Spring AI observation captures prompt/completion to spans/logs | mitigate | Explicit `false` in both api + worker application.yml; property-binding test asserts FALSE at boot via Mechanism A (POJO check) + Mechanism B (Environment.containsProperty for verified key names) + Mechanism C (observation snapshot integration test). Mechanism B + C together catch the silent-no-op failure mode flagged by planner-checker WARNING #6 |
| T-09-05-02 | Information disclosure | llm_call_audit row persists prompt/completion via PREVIEW CallSite | mitigate | Verify PREVIEW CallSite skips prompt/completion columns OR doesn't write a row; sentinel-leak test confirms; CallSite.PREVIEW behavior documented in 09-05-SUMMARY |
| T-09-05-03 | Information disclosure | Service logs "User Sent body length=N" with body excerpt | mitigate | Per CONVENTIONS §5: ONLY structured fields `event=voice.generate.completed tenantId={} sampleSize={} resultWordCount={}` — never any body excerpt; sentinel-leak test asserts |
| T-09-05-04 | Information disclosure | Exception messages leak body content | mitigate | Catch every checked exception inside service; wrap with sanitized error event before re-throwing/returning |
| T-09-05-05 | Tampering | Prompt-injection from user's own past sent emails | accept | The user is the source of their own emails; output is user-reviewed before save; PersonalizationSanitizer applies on Save path (handled by 09-02) |
| T-09-05-06 | Denial of service | Spam-trigger LLM calls | mitigate | Rate limit 3/h via `ByokRateLimiter` keyed on `voice.generate` |
| T-09-05-07 | Information disclosure | Generated style auto-saved containing email fragments | mitigate | Endpoint NEVER persists — caller (FE) must explicitly call `PUT /api/settings/voice` after user review; integration test asserts endpoint writes zero rows to assistant_settings |
| T-09-05-08 | Repudiation | No audit of generate trigger | accept | App-log structured event suffices; user is doing work on their own setting |
| T-09-05-09 | Information disclosure | Spring AI observation property keys silently wrong → privacy regression invisible | mitigate | Mechanism B (Environment.containsProperty) + Mechanism C (observation snapshot integration test) per planner-checker WARNING #6; if property names change between RESEARCH and execution, the executor defers to a follow-up task in this same plan rather than silently binding to a non-existent key |
| T-09-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spring AI observation hardening + property-binding test (Mechanisms A/B/C per planner-checker WARNING #6) + GmailSentMessagesReader (no DB write)</name>
  <files>
    backend/api/src/main/resources/application.yml,
    backend/worker/src/main/resources/application.yml,
    backend/api/src/main/java/com/zeromail/api/config/SpringAiObservationProperties.java,
    backend/api/src/test/java/com/zeromail/api/config/SpringAiObservationDisabledTest.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/GmailSentMessagesReader.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/QuotedReplyStripper.java,
    backend/core/src/test/java/com/zeromail/core/chat/usecases/settings/QuotedReplyStripperTest.java,
    backend/core/src/test/java/com/zeromail/core/chat/usecases/settings/GmailSentMessagesReaderAggregateCapTest.java
  </files>
  <read_first>
    backend/api/src/main/resources/application.yml (current spring.ai block — verify whether observation properties are already set),
    backend/worker/src/main/resources/application.yml,
    backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java (clientForTenant method signature),
    backend/core/src/main/java/com/zeromail/core/gmail/ (search for existing Gmail message reading helpers — likely an entry point that loads inbox messages; mirror its shape but DO NOT persist),
    **Context7 — MANDATORY before yml edit (per planner-checker WARNING #6 + REVIEWS.md MEDIUM Codex Spring-AI rename note):** `/spring-projects/spring-ai` resolve-library-id, then query-docs for both candidate name pairs:
    - Older naming: `spring.ai.chat.client.observations.include-prompt` / `include-completion`.
    - **Newer naming (per REVIEWS.md MEDIUM):** Spring AI 2.0 snapshot docs renamed `include-prompt` / `include-completion` → `log-prompt` / `log-completion`; chat-client scope keys may now read `spring.ai.chat.client.observations.log-prompt` / `log-completion`.
    Verify the EXACT property keys + the prefix used by `@ConfigurationProperties` for the matching POJO. If Context7 returns no result, run `ctx7 library spring-ai "observation log-prompt"` as fallback. If BOTH return nothing definitive, the executor pulls the relevant ChatClient observation source from the locally checked out Spring AI M7 source (or `mvn dependency:sources` for the spring-ai-* artifacts in `~/.gradle/caches/...`) and reads the `@ConfigurationProperties`-annotated class for ground truth.
  </read_first>
  <behavior>
    - Verify the Spring AI M7 property keys via Context7 (see read_first). The candidate set is `spring.ai.chat.client.observations.include-prompt` / `include-completion`, but the executor LOCKS the exact keys based on M7 docs/source — do NOT lock the planning guess without verification.
    - Add the verified observation-disable properties (both prompt + completion) with `false` values to BOTH `backend/api/src/main/resources/application.yml` AND `backend/worker/src/main/resources/application.yml` per Convention §9.
    - `SpringAiObservationProperties` is an `@ConfigurationProperties(<verified.prefix>)` record with `includePrompt(boolean)` + `includeCompletion(boolean)` accessors. The prefix matches the verified key prefix from Context7. Used by the property-binding test.
    - `SpringAiObservationDisabledTest` (`@SpringBootTest`):
      - **Mechanism A:** autowire `SpringAiObservationProperties`; assert `includePrompt() == false && includeCompletion() == false`.
      - **Mechanism B (NEW per WARNING #6):** autowire `org.springframework.core.env.Environment`; assert `environment.containsProperty("<verified.include-prompt.key>") == true` AND `environment.containsProperty("<verified.include-completion.key>") == true`. This guards against the executor naming the POJO fields correctly but pointing them at the wrong prefix.
      - **Mechanism C (NEW per WARNING #6 — observation snapshot integration assertion):** wire a Micrometer `TestObservationRegistry` (or equivalent) into the test ApplicationContext, override the `ChatModel` bean with a stub returning a fixed completion, invoke `chatClient.prompt("hello").call().content()` (or the M7 equivalent), then assert the captured observation has zero keyValue entries whose key matches `gen_ai.prompt*` / `gen_ai.completion*` / `prompt.text` / `completion.text` (or whichever Spring AI M7 KeyValues names emitted by the `ChatClientObservationConvention` — the executor reads the M7 convention class to get the exact keyValue names and pins them as a regex assertion).
      - If Mechanism C cannot be wired in a focused unit test (e.g. the relevant convention isn't easily extractable), at minimum capture the application logs during the stub call and assert no log line contains `gen_ai.prompt` / `gen_ai.completion` substrings.
    - `QuotedReplyStripper.strip(String body) -> String` — pure function applying the 6 quote-detection rules in `<must_haves>`. Unit-tested by `QuotedReplyStripperTest` with fixtures covering: pure `>` quoting (drop quoted block), Gmail "On … wrote:" separator (drop everything below), Outlook `________________________________` separator (drop below), Vietnamese "Vào … đã viết:" separator (drop below), mixed inline + quoted (preserve inline above-quote, strip below), no-quote case (return verbatim), edge: single-line body equal to a quote separator (return empty), edge: sentinel `LEAK_SENTINEL_QUOTED` placed inside a quoted block — stripper output MUST NOT contain it (this fixture is also used by the sentinel-leak test in Task 2 per REVIEWS.md HIGH-3).
    - `GmailSentMessagesReaderAggregateCapTest` (`@SpringBootTest` Testcontainers + Gmail-API stub): seed 50 messages each with a 4000-char body (200_000 chars total); call `readRecentSent(tenantId, 50)`; assert returned `samples` joined-length ≤ 60_000; assert the structured log `event=voice.generate.aggregate_cap_applied originalSampleCount=50 cappedSampleCount={N≤15}` was emitted exactly once.
    - `GmailSentMessagesReader.readRecentSent(UUID tenantId, int sampleSize) -> List<SentMessageSummary>` (record `SentMessageSummary(String fromAddress, String toAddress, String subject, String bodyPlaintext)`):
      - Bounds-check `sampleSize` to `Math.min(sampleSize, 50)`.
      - Use `GmailApiClientFactory.clientForTenant(tenantId)`.
      - List messages with `q="in:sent"`, max results = sampleSize.
      - For each message id, fetch full payload, extract `text/plain` part (or fall back to `text/html` → plain via simple strip), decode base64.
      - **Quoted-reply strip (per REVIEWS.md HIGH-3):** apply `QuotedReplyStripper.strip(body)` BEFORE truncation. Strip rules: drop lines matching `^>` (RFC 3676 quote prefix); drop everything after the first occurrence of any of `^On .* wrote:$`, `^From: .*$`, `^-----Original Message-----`, `^________________________________`, `^Vào .* đã viết:$`, `^Người gửi: .*$` (case-sensitive, multiline). The user's own outbound content sits ABOVE the quote separator in a reply, so dropping below it preserves voice while excluding inbound third-party content.
      - Truncate body to 4000 chars per sample AFTER quoted-reply strip.
      - **Aggregate prompt cap (per REVIEWS.md HIGH-3):** before returning, compute `total = sum(s.bodyPlaintext().length() for s in samples)`. If `total > 60_000`, repeatedly drop the last sample until `total ≤ 60_000`. This caps the joined-corpus size that `VoiceGenerationPrompt.assemble(samples)` produces, defeating the Codex-flagged 200k-char worst case. Log structured event `event=voice.generate.aggregate_cap_applied tenantId={} originalSampleCount={} cappedSampleCount={} aggregateChars={}` — no body content.
      - Return method-local list — NO repository writes, NO log of body content.
      - Empty Sent folder → return empty list (controller maps to `{generatedStyle: ""}`).
      - Errors (auth, transient HTTP) wrapped in `GmailSentReadException` with sanitized message.
  </behavior>
  <action>
    **STEP 1 (MANDATORY before any yml edit):** verify the Spring AI observation property keys via Context7 `/spring-projects/spring-ai` query for "observation include-prompt M7" (and "include-completion"). Lock the exact keys + their `@ConfigurationProperties` prefix. If Context7 is ambiguous or returns nothing, pull the M7 sources (Gradle dependency-sources) and read `ChatClientObservationConvention` / the observation-properties class directly. Record the verified keys in 09-05-SUMMARY.

    **STEP 2:** add the disable properties to both subproject yml files (Convention §9). Create `SpringAiObservationProperties` record with the matching `@ConfigurationProperties` prefix.

    **STEP 3:** write `SpringAiObservationDisabledTest` covering Mechanisms A/B/C as described in `<behavior>`. The test class name MUST match `*SpringAiObservation*` so the `--tests "*SpringAiObservation*"` Gradle filter in 09-VALIDATION.md picks it up.

    **STEP 4:** write `GmailSentMessagesReader` with explicit body-handling — keep every body variable method-local and avoid any `@Transactional` annotation (this class does NOT touch the DB). Use existing `GmailApiClientFactory.clientForTenant(...)`.

    **Fallback policy (per WARNING #6 — silent-key-bind regression):** if Context7 + sources still leave the property keys ambiguous, DO NOT guess and bind to a candidate key. Instead, in the same plan, add a follow-up task that pins the keys against a live Spring AI M7 boot + logs the actual `ChatClientObservation` keyValues emitted. The observation snapshot test (Mechanism C) becomes the primary gate while the property name is in flux.
  </action>
  <verify>
    <automated>./gradlew :backend:api:test --tests "*SpringAiObservation*" && ./gradlew :backend:core:test --tests "*GmailSentMessagesReader*" --tests QuotedReplyStripperTest --tests GmailSentMessagesReaderAggregateCapTest</automated>
  </verify>
  <acceptance_criteria>
    - Both application.yml files have the VERIFIED Spring AI observation properties set to `false` (key names confirmed via Context7 or M7 sources — NOT planning-time guesses; verifier handles both `include-prompt` / `include-completion` AND `log-prompt` / `log-completion` candidates per REVIEWS.md MEDIUM)
    - `SpringAiObservationDisabledTest` runs all three mechanisms and is GREEN: (A) POJO false, (B) Environment.containsProperty true for the verified keys, (C) observation snapshot has no prompt/completion keyValues
    - `GmailSentMessagesReader` returns list of summaries without writing any DB row (verified by `@SpringBootTest` Testcontainers DB row count before/after)
    - `QuotedReplyStripperTest` covers all 8 fixtures from `<behavior>` including the `LEAK_SENTINEL_QUOTED` scenario (per REVIEWS.md HIGH-3)
    - `GmailSentMessagesReaderAggregateCapTest` (per REVIEWS.md HIGH-3) proves joined-corpus length ≤ 60_000 chars and the structured cap-applied log line was emitted
    - Empty Sent folder returns empty list (not exception)
    - 09-05-SUMMARY documents the exact verified property keys + the `@ConfigurationProperties` prefix used + the chosen MAX_AGGREGATE_PROMPT_CHARS value + sample quoted-reply-strip output for one fixture
  </acceptance_criteria>
  <done>Spring AI observation hardening committed in both subproject yml files with VERIFIED keys (not assumed); three-mechanism property-binding test green; Gmail reader uses no DB writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VoiceGenerationService + style-extraction prompt + controller + DTOs + sentinel-leak integration test + rate-limit test</name>
  <files>
    backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationService.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationCommand.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationResult.java,
    backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/VoiceGenerationPrompt.java,
    backend/api/src/main/java/com/zeromail/api/controllers/settings/VoiceGenerateController.java,
    backend/api/src/main/java/com/zeromail/api/dto/settings/GenerateFromSentRequest.java,
    backend/api/src/main/java/com/zeromail/api/dto/settings/GenerateFromSentResponse.java,
    backend/core/src/test/java/com/zeromail/core/voice/VoiceGenerationFromSentLeakTest.java,
    backend/core/src/test/java/com/zeromail/core/voice/VoiceGenerationRateLimitTest.java
  </files>
  <read_first>
    backend/core/src/main/java/com/zeromail/core/llm/gateway/LlmGateway.java (chat method signature; CallSite enum and whether PREVIEW writes audit prompt/completion),
    backend/core/src/main/java/com/zeromail/core/llm/audit/ (verify PREVIEW CallSite path — assume PREVIEW skips prompt/completion columns; otherwise mock the audit repo to assert no insert),
    backend/core/src/main/java/com/zeromail/core/llm/byok/ByokRateLimiter.java (from plan 09-04 Task 1),
    backend/core/src/main/java/com/zeromail/core/chat/usecases/settings/GmailSentMessagesReader.java (from Task 1)
  </read_first>
  <behavior>
    - `VoiceGenerationService.generate(UUID tenantId, int sampleSize) -> VoiceGenerationResult`:
      - `@Transactional(propagation = NOT_SUPPORTED)` — explicitly no DB transaction.
      - Rate-limit: `byokRateLimiter.requireAllowance(tenantId, "voice.generate", 3, Duration.ofHours(1))` — throws on 4th call/hour.
      - Load samples via `gmailSentMessagesReader.readRecentSent(...)`.
      - If empty → return `VoiceGenerationResult.empty()` (generatedStyle="").
      - Build prompt via `VoiceGenerationPrompt.assemble(samples)` (outcome-first per CLAUDE.md).
      - Call `LlmGateway.chat(LlmChatRequest.forVoiceGenerate(tenantId, prompt), CallSite.PREVIEW)`.
      - Extract content, truncate to 500 words via `truncateToWords(content, 500)`.
      - Return result; log structured `event=voice.generate.completed tenantId={} sampleSize={} resultWordCount={}` — NO sample bodies, NO LLM prompt/completion in log.
      - On LLM error → log `event=voice.generate.failed tenantId={} cause={}` with sanitized cause; throw `VoiceGenerationFailedException` (mapped to HTTP 502 with `code=voice.generate.failed`).
    - `VoiceGenerationPrompt`: holds the style-extraction system prompt template (outcome-first per CLAUDE.md). Example shape: "You are a writing-style analyst. Given N email samples authored by the user, produce a ≤ 500-word style guide covering tone, formality, sentence length, common phrasings, and sign-off patterns. Output only the style guide; no preamble." (Final wording at executor's discretion — must produce a deterministic style-guide string and NEVER inline raw sample text in the output.)
    - `GenerateFromSentRequest`: `sampleSize @Min(1) @Max(50) int` (default 20 if absent).
    - `GenerateFromSentResponse`: `generatedStyle String` (always present, may be empty).
    - `VoiceGenerateController` `@PostMapping("/api/settings/voice/generate-from-sent")`: parse request → call service → return response. 429 on rate-limit failure via existing exception mapping with `code=voice.generate.rate_limited`; 502 on LLM failure with `code=voice.generate.failed`.
    - Wave-0 stubs:
      - `VoiceGenerationFromSentLeakTest` (`@SpringBootTest` + Testcontainers + mocked ChatModel per TESTING.md):
        - Seed Gmail-API stub with samples containing THREE sentinels (per REVIEWS.md HIGH-3):
          - **Body sentinel** `LEAK_SENTINEL_AB12CD34_VOICE_BODY` placed in the user's own outbound text (above any quote separator).
          - **Quoted-reply sentinel** `LEAK_SENTINEL_QUOTED_INBOUND` placed BELOW an `^On .* wrote:$` separator — simulates inbound content the user is replying to. The `QuotedReplyStripper` from Task 1 MUST strip this before the prompt is built.
          - **Completion sentinel** `LEAK_SENTINEL_XY99ZZ_COMPLETION` returned by the mocked ChatModel.
        - Capture the actual prompt string passed to the mocked `ChatModel.call(...)` via an ArgumentCaptor.
        - Invoke `VoiceGenerationService.generate(tenantId, 5)`.
        - Assert:
          - The captured prompt CONTAINS the body sentinel (proves the user's voice WAS fed to the LLM) — this is OK because the prompt is in-memory only.
          - The captured prompt DOES NOT contain the quoted-reply sentinel (proves `QuotedReplyStripper` ran before prompt assembly).
          - Zero rows in `llm_call_audit` contain ANY of the three sentinels (or PREVIEW row contains no prompt/completion columns — depends on the STEP 0 verification above).
          - Zero rows in `assistant_settings` updated (endpoint does not auto-save).
          - Zero rows in `assistant_knowledge_snippet`.
          - Capture in-memory log output via a Logback `ListAppender<ILoggingEvent>` and grep for any of the three sentinels → assert zero matches.
        - Assert returned `generatedStyle` MAY contain the completion sentinel (that's fine — user sees it; they decide whether to Save).
      - `VoiceGenerationRateLimitTest`: invoke generate 3 times → all succeed; 4th throws `RateLimitExceededException` (HTTP 429 `code=voice.generate.rate_limited`).
  </behavior>
  <action>
    **STEP 0 (MANDATORY — execute BEFORE any other code in this task per REVIEWS.md HIGH-3 + OpenCode HIGH-1):** verify `CallSite.PREVIEW` audit-row semantics in the existing codebase. Run `mcp__jetbrains__search_in_files_by_text "CallSite.PREVIEW"` and `mcp__jetbrains__search_in_files_by_regex "case PREVIEW.*->.*"` to find the audit-insert branching. Read the audit-pipeline class that consumes `CallSite` and answer in writing inside `09-05-SUMMARY.md` (or as a comment block in `VoiceGenerationFromSentLeakTest`):
       - **(a)** Does `CallSite.PREVIEW` SKIP audit-row insertion entirely?
       - **(b)** Does it INSERT a row but with `prompt`/`completion` columns NULL?
       - **(c)** Does it INSERT a row WITH `prompt`/`completion` columns populated? **If (c), STOP coding and fix the PREVIEW path first** — D-11 forbids prompt/completion persistence in the email-content pipeline, and SET-VOICE-07 is email-content. Submit a follow-up sub-plan that adds the PREVIEW-skips-prompt branch before this task can proceed. The sentinel-leak test below adapts to (a) or (b) but cannot rescue (c).
       Document the answer + the file path that proves it in the summary. Future PREVIEW callers reuse the conclusion.

    Build the service with explicit `NOT_SUPPORTED` propagation so no transaction is opened. Every variable holding sample-body bytes is method-local; explicit `samples = null` at end of method if paranoid. Log lines structured per CONVENTIONS §5. Use `CallSite.PREVIEW` per RESEARCH Pattern 6. Build the controller thin. Fill the 2 Wave-0 stubs. The sentinel-leak test is the critical privacy gate — wire a Logback `ListAppender<ILoggingEvent>` to capture log output during the test, plus query `llm_call_audit` + `assistant_settings` + `assistant_knowledge_snippet` directly via `JdbcTemplate` to assert sentinel absence. **REVIEWS.md HIGH-3 quoted-reply case:** the sentinel-leak test ALSO seeds a sample where the quoted-reply portion contains a third sentinel `LEAK_SENTINEL_QUOTED_INBOUND`; assert (1) the prompt fed to the mocked ChatModel does NOT contain that sentinel (proves `QuotedReplyStripper` ran), (2) no DB row contains it, (3) no log line contains it.
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests VoiceGenerationFromSentLeakTest --tests VoiceGenerationRateLimitTest</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/settings/voice/generate-from-sent` returns `{generatedStyle: <≤500-word string>}` on success
    - Empty Sent folder returns `{generatedStyle: ""}`
    - 4th call/hour returns 429 `code=voice.generate.rate_limited`
    - LLM failure returns 502 `code=voice.generate.failed`
    - Sentinel-leak test asserts ZERO leaks of either body sentinel or completion sentinel across DB + logs + audit
    - The endpoint NEVER writes to `assistant_settings` (caller must invoke `PUT /api/settings/voice` separately)
  </acceptance_criteria>
  <done>SET-VOICE-07 endpoint live; sentinel-leak privacy invariant verified; rate limit enforced; 2 Wave-0 stubs green.</done>
  <summary_requirement>
    Per planner-checker INFO #9: the `09-05-SUMMARY.md` produced from this plan MUST document the `CallSite.PREVIEW` audit-row semantics observed during execution. Specifically, document:
    - WHAT a `CallSite.PREVIEW` row writes to `llm_call_audit` (expected: model + tenant_id + sample_size + created_at, but NEVER `prompt` / `completion` / body bytes per D-11).
    - WHETHER `CallSite.PREVIEW` writes a row at all (option a) or skips audit insertion entirely (option b) — settle which based on the verification done in this plan's `<read_first>`.
    - If a row IS written, the column-by-column contents (with body/prompt/completion confirmed empty/NULL).
    This semantics note is consumed by future PREVIEW callers (e.g. v1.3 features) so they don't have to re-derive what the call site does. Non-blocking now; binding reminder so the summary isn't written without it.
  </summary_requirement>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:test --tests "*VoiceGeneration*" --tests "*SpringAiObservation*"` all green
- Sentinel-leak test asserts no body / prompt / completion bytes appear in DB rows, app logs, or audit table
- Spring AI observation properties are `false` in both subproject yml files with VERIFIED key names (Mechanism B passes)
- Observation snapshot integration test (Mechanism C) confirms zero `gen_ai.prompt*` / `gen_ai.completion*` keyValues emitted
- Endpoint does NOT update `assistant_settings` (caller saves via PUT /api/settings/voice from plan 09-02)
- No new packages installed
</verification>

<success_criteria>
- 1 requirement ID satisfied: SET-VOICE-07
- D-11 privacy invariant verified via three-sentinel integration test (body + quoted-reply + completion) + three-mechanism observation property gate (per planner-checker WARNING #6 + REVIEWS.md HIGH-3)
- Aggregate prompt cap (60_000 chars) and quoted-reply stripping in `GmailSentMessagesReader` per REVIEWS.md HIGH-3
- 2 ❌ Wave-0 stubs flipped GREEN; 2 new unit tests added (`QuotedReplyStripperTest`, `GmailSentMessagesReaderAggregateCapTest`)
- `CallSite.PREVIEW` audit-row semantics PRE-VERIFIED as STEP 0 of Task 2 and documented in 09-05-SUMMARY (per planner-checker INFO #9 + REVIEWS.md HIGH-3 + OpenCode HIGH-1)
</success_criteria>

<output>
Create `.planning/phases/09-user-settings-ui-on-curated-catalog/09-05-SUMMARY.md`. Document:
- The VERIFIED Spring AI observation property keys (verified via Context7 / M7 sources during execution, NOT the planning-time guess)
- The `@ConfigurationProperties` prefix actually used
- The three-mechanism property-binding test result (Mechanisms A/B/C all passing)
- The sentinel-leak test result summary
- **The `CallSite.PREVIEW` audit-row semantics — what it writes, what it does NOT write, and whether it writes a row at all** (per `<summary_requirement>` in Task 2 / planner-checker INFO #9)
- Any deviation from the locked prompt structure
</output>
