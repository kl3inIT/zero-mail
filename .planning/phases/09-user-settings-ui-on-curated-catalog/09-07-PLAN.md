---
phase: 09-user-settings-ui-on-curated-catalog
plan: 07
type: execute
wave: 3
depends_on:
  - 09-06
files_modified:
  - apps/web/e2e/ai-settings.spec.ts
  - backend/core/src/test/java/com/zeromail/core/architecture/Phase9ArchitectureTest.java
  - .planning/phases/09-user-settings-ui-on-curated-catalog/09-VALIDATION.md
autonomous: false
requirements:
  - SET-VOICE-01
  - SET-VOICE-02
  - SET-VOICE-03
  - SET-VOICE-04
  - SET-VOICE-05
  - SET-VOICE-06
  - SET-VOICE-07
  - SET-BEHV-01
  - SET-BEHV-02
  - SET-BEHV-03
  - SET-BEHV-04
  - SET-BEHV-05
  - SET-SAFE-01
  - SET-SAFE-04
  - SET-AI-01
  - SET-AI-02
  - SET-AI-03
  - SET-AI-04

must_haves:
  truths:
    - "Playwright e2e ai-settings.spec.ts is implemented (not skipped) and covers the golden path: edit voice (Dialog) → save → reload → persist; toggle behavior switches → reload → persist; add+edit+delete knowledge snippet; add+delete safety-net sender (EMAIL + DOMAIN forms); BYOK Save → Test → pick Model → Activate → reload → persist"
    - "Phase9ArchitectureTest aggregates all phase-level ArchUnit invariants: single PersonalizationSanitizer call site (chat-tool + REST), single AssistantKnowledgeService.append call site (chat-tool + REST), ProviderConnectionTester single binding (admin MKEY + user BYOK), and a ban on any class outside core.chat.byok / core.llm.byok / core.llm.gateway from referencing user_byok_key"
    - "Repo grep gate confirms: no plaintext API key strings in any FE component file; no `toast.success/error` in feature hooks; no hardcoded color hex in Phase 9 components; no ByokForm references anywhere"
    - "User checkpoint: visual sweep through /ai page against 09-PROTOTYPE.html; live SET-VOICE-07 against real Gmail account; live BYOK round-trip with a test OpenAI key"
    - "09-VALIDATION.md frontmatter status flipped to `passed` and all Per-Task rows show ✅ green"
  artifacts:
    - path: "apps/web/e2e/ai-settings.spec.ts"
      provides: "Playwright golden-path coverage for Phase 9 /ai page"
    - path: "backend/core/src/test/java/com/zeromail/core/architecture/Phase9ArchitectureTest.java"
      provides: "ArchUnit aggregate ensuring no Phase 9 invariants drift"
  key_links:
    - from: "Playwright spec"
      to: "/ai route"
      via: "page.goto('/ai') + DOM interactions"
      pattern: "page\\.goto\\(['\"]/ai['\"]\\)"
---

<objective>
Close out Phase 9 with end-to-end coverage and aggregate ArchUnit hardening, plus the manual UX verification per project UX rule.

Purpose: ensure no FE/BE drift between this phase and v1.3, and validate the live UX matches 09-PROTOTYPE.html.
Output: implemented Playwright spec + aggregate ArchUnit test + manual checkpoint signed off + 09-VALIDATION.md flipped to passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-SPEC.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-VALIDATION.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-UI-SPEC.md
@.planning/phases/09-user-settings-ui-on-curated-catalog/09-PROTOTYPE.html
@CLAUDE.md
@apps/web/AGENTS.md
@TESTING.md

<interfaces>
From `apps/web/e2e/` existing Playwright fixtures:
- Likely a `loginAsUser` fixture using the bundled OAuth flow. For the test, either reuse an existing seeded user OR use Playwright's `storageState` snapshot — locate the pattern in an existing spec like `chat.spec.ts` or `rules.spec.ts`.

From Phase 9 ArchUnit single-call-site tests (already green from earlier plans):
- `PersonalizationSanitizerSingleCallSiteTest` (plan 09-02)
- `KnowledgeSnippetSingleWriteSiteTest` (plan 09-02)
- `ProviderConnectionTesterSingleBindingTest` (plan 09-04)
- `Phase9ArchitectureTest` (this plan) re-asserts these in a single aggregate test for future regression discoverability + adds a NEW rule: `user_byok_key` table/entity references confined to `core.chat.byok`, `core.llm.byok`, `core.llm.gateway`.

Manual verification from 09-VALIDATION.md § Manual-Only Verifications:
1. Visual brand-token compliance + IZ-aligned spacing/typography on live `/ai`.
2. BYOK round-trip against a real provider (test OpenAI key against `https://api.openai.com/v1` — sets active=true, sends a chat message, confirms completion went through BYOK URL via app log).
3. SET-VOICE-07 "Generate from recent sent emails" with a real Gmail account.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live `/ai` page | Real user session; e2e simulates this via Playwright |
| live provider call | Real OpenAI key in BYOK round-trip (manual checkpoint only — NOT in CI) |
| live Gmail body access | Real Sent folder in SET-VOICE-07 manual run (NOT in CI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Information disclosure | Playwright spec logs DOM snapshots containing key fragments | mitigate | Test uses a fake key (`sk-test-PLAYWRIGHT-DUMMY-KEY-32CHARSLONG`); assertion checks the masked display (`sk-****ngth`), never compares against full key |
| T-09-07-02 | Repudiation | Manual checkpoint result not recorded | mitigate | User-facing checkpoint block records visual sweep result + live BYOK result + live Voice-generate result in 09-VALIDATION.md / 09-07-SUMMARY.md |
| T-09-07-03 | Tampering | Future commits drift sanitizer / knowledge / tester invariants | mitigate | `Phase9ArchitectureTest` aggregates all three single-binding rules + adds user_byok_key access rule |
| T-09-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement apps/web/e2e/ai-settings.spec.ts (golden path covering all 5 sections + Knowledge CRUD + BYOK state machine)</name>
  <files>
    apps/web/e2e/ai-settings.spec.ts
  </files>
  <read_first>
    apps/web/e2e/ (locate an existing spec like chat.spec.ts or rules.spec.ts — mirror its fixture / login setup),
    apps/web/playwright.config.ts (verify projects + storageState handling),
    apps/web/features/ai/components/AiConfigPage.tsx (selectors — prefer accessible roles + text over CSS classes),
    .planning/phases/09-user-settings-ui-on-curated-catalog/09-PROTOTYPE.html (visual contract reference for assertions)
  </read_first>
  <behavior>
    - Spec uses the existing logged-in storageState fixture.
    - `test('flat-section golden path', ...)` step-by-step:
      1. `page.goto('/ai')`; assert all 5 section headers visible (`Giọng văn của bạn`, `Hành vi trợ lý`, `Cập nhật`, `Lưới an toàn`, `Nhà cung cấp AI` — or EN variants based on locale fixture).
      2. Voice: click Edit on `Phong cách viết` SettingCard → fill 250-word textarea → click Save → assert success toast → reload → reopen Dialog → assert text persisted.
      3. Voice: open `Về tôi` Dialog → enter 1500 chars → Save → reload → persist; verify no XML fence / injection sentinel rendered.
      4. Behavior: toggle `Tự động soạn nháp trả lời` Switch off → assert optimistic state + success toast → reload → persists off.
      5. Behavior: open Draft confidence Dialog → pick HIGH → Save → reload → persists.
      6. Knowledge: click `+ Add` → fill title + content → Save → assert new row visible at top of Table; click Edit on that row → modify content → Save → assert update; click Delete → confirm in ConfirmDialog → assert row removed.
      7. Safety net: type `vip@acme.com` in the add input → Enter → assert row appears with `Email` Badge + `Bạn` Badge; type `@evilcorp.com` → Enter → assert row appears with `Domain` Badge.
      8. Safety net: click Delete on the `vip@acme.com` row → ConfirmDialog → confirm → row removed.
      9. AI Provider: pick `OpenAI` from Provider Select → assert Base URL auto-filled with `https://api.openai.com/v1` → enter dummy key `sk-test-PLAYWRIGHT-DUMMY-KEY-32CHARSLONG` → click Save → assert Status pill cleared, Active switch disabled with tooltip.
      10. AI Provider: assert Active Switch shows disabled state + tooltip on hover (use `expect(switch).toBeDisabled()` and tooltip text matches the localized message). NOTE: Test connection is NOT clicked in e2e (would make a real provider HTTP call); manual checkpoint covers that.
      11. AI Provider: reload → BYOK row persists with masked key display `sk-****`+last 4 chars; Active still off; no plaintext key in any DOM node.
      12. Cost footer: assert text matches regex `\$\d+\.\d{2}`.
      13. Assert page DOM contains zero substrings matching `sk-[A-Za-z0-9]{20,}` (plaintext-key leak guard at the DOM level).
  </behavior>
  <action>
    Implement the spec following the existing Playwright fixture pattern in `apps/web/e2e/`. Use `page.getByRole('button', { name: 'Lưu' })` or `page.getByRole('switch', { name: 'Tự động soạn nháp trả lời' })`-style accessible selectors per Playwright best practices. Use `await expect(...).toBeVisible()` / `toBeDisabled()` / `toHaveText(...)` assertions. After every reload, use `expect.poll(...)` to wait for the queries to refetch. Do NOT click Test connection in e2e (it would hit a real provider). Use a dummy 40+char key starting with `sk-` so the masked-display assertion is meaningful. The spec is the implementation of the Playwright stub from plan 09-01 Task 3 — remove the `test.skip(...)` from the file.
  </action>
  <verify>
    <automated>pnpm --filter web e2e -- ai-settings.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec runs and passes against the dev backend + apps/web
    - All 13 steps assert observable behavior (no `console.log`-only assertions)
    - DOM-level plaintext-leak guard at end: zero `sk-[A-Za-z0-9]{20,}` substring matches
    - Test takes under 60 seconds (per 09-VALIDATION.md feedback-latency target for quick run)
  </acceptance_criteria>
  <done>Playwright golden-path spec green; covers all 18 requirements at the e2e layer (subject to manual BYOK + voice-generate live checkpoints).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase9ArchitectureTest aggregate ArchUnit + grep-gate audits + 09-VALIDATION.md flip</name>
  <files>
    backend/core/src/test/java/com/zeromail/core/architecture/Phase9ArchitectureTest.java,
    .planning/phases/09-user-settings-ui-on-curated-catalog/09-VALIDATION.md
  </files>
  <read_first>
    backend/core/src/test/java/com/zeromail/core/chat/sanitize/PersonalizationSanitizerSingleCallSiteTest.java (from plan 09-02),
    backend/core/src/test/java/com/zeromail/core/chat/knowledge/KnowledgeSnippetSingleWriteSiteTest.java (from plan 09-02),
    backend/core/src/test/java/com/zeromail/core/llm/byok/ProviderConnectionTesterSingleBindingTest.java (from plan 09-04),
    .planning/phases/09-user-settings-ui-on-curated-catalog/09-VALIDATION.md (Per-Task Verification Map status column)
  </read_first>
  <behavior>
    - `Phase9ArchitectureTest` aggregates the existing single-binding rules + adds:
      - Classes residing outside `..core.chat.byok..`, `..core.llm.byok..`, `..core.llm.gateway..` MUST NOT reference `UserByokKeyEntity` or `UserByokKeyRepository`.
      - The class `ByokForm` MUST NOT exist anywhere under `apps/web` (this is a documentation rule — enforced via a separate FE grep gate, not ArchUnit; record in 09-VALIDATION).
      - `core.chat.usecases.settings` classes MUST NOT directly reference `Gmail` API client classes — they must go through `GmailSentMessagesReader`.
    - FE grep gates (recorded in 09-VALIDATION.md under "Validation Sign-Off"):
      - `grep -r "ByokForm" apps/web` returns zero matches outside historical generated/test data
      - `grep -rE "toast\\.(success|error)" apps/web/features/ai apps/web/features/knowledge` returns zero matches
      - `grep -rE "bg-\\[#" apps/web/features/ai apps/web/features/knowledge` returns zero matches
    - `09-VALIDATION.md` frontmatter updated: `status: passed`; every row in the Per-Task Verification Map flipped to ✅ green (or ⚠️ flaky with a note); `Validation Sign-Off` checkboxes all checked.
  </behavior>
  <action>
    Write `Phase9ArchitectureTest` as a single `@ArchTest` class containing 4-5 rules. Run the FE grep gates locally (use `git grep` or `mcp__jetbrains__search_in_files_by_regex` against the apps/web workspace). Update `09-VALIDATION.md` frontmatter + table status column. If any gate fails, fix in the relevant plan's already-merged code (e.g. orphan `toast.success` slipped past 09-06 — fix and re-run).
  </action>
  <verify>
    <automated>./gradlew :backend:core:test --tests Phase9ArchitectureTest</automated>
  </verify>
  <acceptance_criteria>
    - `Phase9ArchitectureTest` green (4-5 rules)
    - 3 FE grep gates clean
    - `09-VALIDATION.md` frontmatter: `status: passed`, all rows ✅, all Sign-Off boxes checked
  </acceptance_criteria>
  <done>Aggregate architecture invariants in place; FE grep gates audited; validation strategy signed off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UX checkpoint — visual sweep + live BYOK + live SET-VOICE-07</name>
  <files>.planning/phases/09-user-settings-ui-on-curated-catalog/09-07-SUMMARY.md</files>
  <action>Pause for the developer to perform the live manual verification steps below. After developer signals "approved", record the three results (visual sweep, live BYOK, live SET-VOICE-07) into 09-07-SUMMARY.md and flip phase status in .planning/STATE.md to complete. If the developer reports issues, surface them as a new gap-closure plan via /gsd:plan-phase 9 --gaps.</action>
  <what-built>
    Phase 9 has shipped: 5-section `/ai` page with SettingCard+Dialog edit pattern, Knowledge Table+Dialog, Safety Net DOMAIN support, single BYOK card with Active switch, cost footer, and Generate-from-sent endpoint with privacy-strict in-memory invariant. CI covers everything except real-provider HTTP and real-Gmail body access (per 09-VALIDATION.md § Manual-Only).
  </what-built>
  <how-to-verify>
    1. Start dev environment (`./gradlew :backend:api:bootRun` + `pnpm --filter web dev`); open `/ai` in browser.
    2. **Visual sweep:** compare the rendered page against `.planning/phases/09-user-settings-ui-on-curated-catalog/09-PROTOTYPE.html` side-by-side. Check spacing (page container max-w-3xl, section gap space-y-8, SettingCard padding p-6), typography (page title text-3xl, section header text-xl), colors (no hex hardcodes in DevTools computed styles), 60/30/10 split honored, locked tokens consumed.
    3. **Live SET-VOICE-07:** sign in with a real Gmail account containing several Sent messages; open `/ai` → `Phong cách viết` Dialog → click `Tạo từ email đã gửi gần đây`. Expect: loading state for 5-30 seconds → textarea populates with a non-empty style guide ≤ 500 words. Verify by tailing the app log: NO log line contains email body content, prompt content, or completion content. Open Postgres via mcp__postgres__execute_sql and run `SELECT prompt, completion FROM llm_call_audit ORDER BY created_at DESC LIMIT 5;` — assert no Phase 9 row has populated `prompt`/`completion`. Click Save → assert writing_style column updated with the textarea content.
    4. **Live BYOK round-trip:** in `Nhà cung cấp AI` section, pick `OpenAI`, enter a test key (any throw-away `sk-...` from your OpenAI account), click `Kiểm tra kết nối`. Expect status pill = OK + Model Select populated with model IDs. Pick `gpt-4o-mini`. Toggle Active switch on. Send a chat message via the existing chat surface; confirm in the backend log that the chat completion request hit `api.openai.com/v1/chat/completions` with `model=gpt-4o-mini`. Toggle Active off → next chat call falls back to admin-catalog platform default.
    5. **Provider switch behavior:** change Provider to `Anthropic` → Base URL auto-fills to `https://api.anthropic.com/v1`; Active switch resets to off; status pill clears.
    6. Verify no plaintext API key visible in any reload of `/ai` (DevTools → Elements / Network response panes).
  </how-to-verify>
  <verify>
    <human-check>Developer confirms visual sweep matches 09-PROTOTYPE.html, live SET-VOICE-07 produces a style guide with no log/DB leak of bodies or LLM prompts/completions, and live BYOK round-trip routes the chat request through the user's BYOK URL with the picked model_id.</human-check>
  </verify>
  <acceptance_criteria>
    - Visual sweep: no token regressions; no hardcoded hex visible in DevTools computed styles
    - Live SET-VOICE-07: style guide populated; zero leaks observed in logs or llm_call_audit prompt/completion columns
    - Live BYOK: chat completion request hits the user's base URL with the picked model_id when active=true; falls back to platform default when active=false
    - Developer responds "approved" via resume-signal; results recorded in 09-07-SUMMARY.md
  </acceptance_criteria>
  <done>Three manual checks signed off; 09-07-SUMMARY.md updated with each result; phase status flipped to complete in STATE.md.</done>
  <resume-signal>Type "approved" or describe issues. If approved, write the visual sweep result + live BYOK result + live SET-VOICE-07 result into `09-07-SUMMARY.md` and flip the phase status in `.planning/STATE.md` to complete.</resume-signal>
</task>

</tasks>

<verification>
- `./gradlew :backend:core:test --tests Phase9ArchitectureTest` green
- `pnpm --filter web e2e -- ai-settings.spec.ts` green
- FE grep gates green (no ByokForm refs / no toast.success in feature hooks / no hex literals)
- Manual checkpoint signed off in 09-07-SUMMARY.md
- `09-VALIDATION.md` frontmatter `status: passed`
</verification>

<success_criteria>
- Playwright golden path implemented and green
- Phase9ArchitectureTest aggregate live; future drift detectable
- Manual UX + live SET-VOICE-07 + live BYOK checkpoints signed off
- Phase 9 fully shipped; ready for ROADMAP completion update
</success_criteria>

<output>
Create `.planning/phases/09-user-settings-ui-on-curated-catalog/09-07-SUMMARY.md`. Include the three manual-check results (visual sweep, BYOK live, voice-generate live), aggregate ArchUnit results, FE grep-gate audit, and final phase completion notes.
</output>
