---
phase: 9
slug: user-settings-ui-on-curated-catalog
status: draft
shadcn_initialized: true
preset: base-nova (neutral + purple-shifted brand tokens, lucide icons)
created: 2026-05-26
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for the User Settings UI on Curated Catalog. Generated by gsd-ui-researcher; pre-populated from 09-SPEC.md + 09-CONTEXT.md + project tokens in `apps/web/app/globals.css`. Verified by gsd-ui-checker.

**Scope:** the single `/ai/page.tsx` route restructured into five flat `<SectionHeader>` groups: `Your voice`, `Behavior`, `Updates`, `Safety net`, `AI Provider`. Inbox Zero pattern. No `<Tabs>`. No query-param state. SettingCard + Dialog edit pattern for multi-field settings; inline `<Switch>` for short toggles; Knowledge as `<Table>` with `+ Add` Dialog.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (initialized — `apps/web/components.json` present) |
| Preset | `base-nova` style, `baseColor: neutral`, CSS variables on (purple-shifted brand via locked tokens in `app/globals.css`) |
| Component library | radix-ui under shadcn |
| Icon library | lucide-react (locked in `components.json`) |
| Font | `--font-sans` (ui-sans-serif / system stack) for UI; `--font-serif` reserved for marketing-only `.zm-proto` surfaces (NOT used on `/ai`) |

Tokens consumed (NEVER hardcoded hex):
- Surface: `bg-background`, `bg-card`, `bg-popover`, `bg-muted`
- Text: `text-foreground`, `text-muted-foreground`
- Accent: `bg-primary`, `text-primary-foreground`, `bg-accent`, `text-accent-foreground`, `bg-primary/10`
- Border/Ring: `border-border`, `ring-ring`
- Semantic: `text-destructive`, `bg-destructive/10`, `text-warning`
- Helpers (Phase 1.6 supplemental): `bg-green-soft text-green`, `bg-amber-soft text-amber`, `bg-blue-soft text-blue` for status pills (test-connection result, last-7d cost trend, audit badge)

---

## Spacing Scale

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon ↔ label gap inside buttons, badge inner padding |
| sm | 8px | Inline form-control inner gaps, switch ↔ label gap |
| md | 16px | Default vertical rhythm inside a SettingCard, dialog body field gap |
| lg | 24px | SettingCard inner padding (`p-6`), dialog padding |
| xl | 32px | Section gap between SectionHeader groups (`space-y-8`) |
| 2xl | 48px | Page outer padding top/bottom on `/ai` (`py-12`) |
| 3xl | 64px | Reserved — not used on this page |

Page container: `max-w-3xl mx-auto px-4 sm:px-6` (matches Inbox Zero settings width — single column, not full-bleed dashboard).

Exceptions: none. Touch targets default to shadcn `Button` 36px and `Switch` 20×24; both already meet WCAG ≥ 24×24 minimum on the row layout (the surrounding `SettingCard` provides ≥ 44px hit area).

---

## Typography

| Role | Size | Weight | Line Height | Tailwind |
|------|------|--------|-------------|----------|
| Page title | 28px | 600 | 1.2 | `text-3xl font-semibold tracking-tight` |
| Section header | 20px | 600 | 1.3 | `text-xl font-semibold tracking-tight` |
| Body / SettingCard title | 16px | 500 | 1.5 | `text-base font-medium` |
| Helper / description / table cell | 14px | 400 | 1.5 | `text-sm text-muted-foreground` |

Total: **4 sizes, 2 weights (400 regular, 500/600 medium-or-semibold)**. Page does NOT use display sizes — that scale is reserved for marketing surfaces.

Heading line-heights: 1.2 (page title) / 1.3 (section header). Body line-height: 1.5. Letter-spacing: `tracking-tight` (-0.01em) on headings only; body uses normal.

---

## Color

60/30/10 split for the `/ai` page surface:

| Role | Value (token) | Hex (light) | Usage |
|------|---------------|-------------|-------|
| Dominant (60%) | `bg-background` | `#FFFFFF` | Page background, dialog background |
| Secondary (30%) | `bg-card`, `bg-muted`, `bg-sidebar` | `#FFFFFF` card, `#F4F2F9` muted, `#F8F7FB` sidebar | SettingCard surfaces, table header row, dialog header strip |
| Accent (10%) | `bg-primary`, `text-primary-foreground`, `bg-primary/10`, `text-accent-foreground` | `#867AEB` primary | See reserved list below |
| Destructive | `text-destructive`, `bg-destructive/10` | `#B0413E` | Delete buttons, error messages, audit badge "Blocked by safety net" (warning hue from `--amber-soft` / `--warning`) |

**Accent (10%) reserved for** (explicit list — never "all interactive"):
1. Primary CTA inside each SettingCard Dialog: `Save` button.
2. The active state of a single segmented control (e.g. the "Use platform default ↔ Use my key" toggle uses primary on the active half).
3. The focus ring on inputs / switches (`ring-ring`).
4. The "Test connection" success pill (`OK` → `bg-green-soft text-green`; not primary accent — it has its own semantic color).
5. The currently selected option in a `<Select>` dropdown (shadcn default).
6. The page-level `SectionHeader` left bar accent line (4px wide `bg-primary` strip on the title's left edge to anchor the section visually).

NOT accent: card borders (use `border-border`), table row hover (use `bg-muted`), SettingCard description text (use `text-muted-foreground`), inline `<Switch>` track-on color (uses shadcn default `bg-primary` — counted under #2 since it is a single semantic element).

Semantic colors (additive, not the 10% slice):
- Status pill `OK` (test-connection): `bg-green-soft text-green`
- Status pill `INVALID_KEY` / `RATE_LIMITED` / `NETWORK_ERROR` / `TIMEOUT`: `bg-red-soft text-red`
- Audit-row "Blocked by safety net" badge: `bg-amber-soft text-amber` (matches existing v1.0 audit warning treatment)
- Last-7d cost helper text: `text-muted-foreground` (no chromatic emphasis — it is reference data, not status)

Dark-mode parity: all tokens above resolve via `.dark` block in `globals.css` — no per-component dark overrides needed.

---

## Copywriting Contract

Bilingual (Vietnamese-first; English secondary). Strings live in `apps/web/features/ai/messages.ts` (new) — next-intl bundles `vi.json` + `en.json` MUST contain every key below. Spec table shows VI first, EN below in italics. Technical terms (e.g. `BYOK`, `OpenAI`, `LOW/MEDIUM/HIGH`) stay English in both bundles.

### Page chrome

| Element | VI | EN |
|---------|-----|-----|
| Page title | "Cài đặt AI" | "AI settings" |
| Page subtitle | "Tinh chỉnh giọng văn, hành vi, và nhà cung cấp AI." | "Tune your voice, behavior, and AI provider." |

### Section headers (`<SectionHeader>` titles + helper line)

| Section | VI title / helper | EN title / helper |
|---------|--------------------|--------------------|
| Your voice | "Giọng văn của bạn" / "Cách AI viết thay bạn" | "Your voice" / "How the AI writes for you" |
| Behavior | "Hành vi trợ lý" / "Khi nào AI hành động tự động" | "Behavior" / "When the AI acts automatically" |
| Updates | "Cập nhật" / "Tóm tắt hằng ngày và chế độ shadow" | "Updates" / "Daily digest and shadow mode" |
| Safety net | "Lưới an toàn" / "Người gửi mà AI không bao giờ tự xử lý" | "Safety net" / "Senders the AI never auto-actions" |
| AI Provider | "Nhà cung cấp AI" | "AI Provider" |

### Primary CTAs

| Context | VI | EN |
|---------|-----|-----|
| SettingCard Edit button (multi-field) | "Sửa" | "Edit" |
| SettingCard Set button (empty value) | "Đặt giá trị" | "Set" |
| Dialog primary save | "Lưu" | "Save" |
| Dialog secondary close | "Hủy" | "Cancel" |
| Knowledge `+ Add` button | "+ Thêm đoạn kiến thức" | "+ Add snippet" |
| Safety net `+ Add` button | "+ Thêm người gửi" | "+ Add sender" |
| Writing-style generate button (SET-VOICE-07) | "Tạo từ email đã gửi gần đây" | "Generate from recent sent emails" |
| AI Provider test-connection | "Kiểm tra kết nối" | "Test connection" |

### Empty states

| Section | VI heading / body | EN heading / body |
|---------|--------------------|--------------------|
| Knowledge (no snippets) | "Chưa có đoạn kiến thức nào" / "Thêm thông tin về bạn hoặc khách hàng để AI dùng khi soạn thư. Bấm + Thêm đoạn kiến thức để bắt đầu." | "No snippets yet" / "Add facts about you or customers for the AI to use when drafting. Click + Add snippet to start." |
| Safety net (no entries) | "Chưa có người gửi nào" / "Thêm email (vd. ceo@acme.com) hoặc domain (vd. @acme.com) để AI luôn để bạn xử lý tay." | "No senders yet" / "Add an email (e.g. ceo@acme.com) or a domain (e.g. @acme.com) so the AI always leaves these to you." |
| AI Provider — no BYOK key (per provider row) | "Chưa cài key cá nhân — đang dùng key nền tảng" / Subline: "Bạn vẫn dùng được {provider}; AI sẽ tính chi phí qua tài khoản Zero Mail." | "No personal key — using platform default" / Subline: "You can still use {provider}; cost runs through your Zero Mail account." |
| Writing-style generate — 0 sent messages | "Không tìm thấy email đã gửi" / "Hộp thư Đã gửi trống nên không tạo được mẫu giọng văn. Hãy viết thử vài email rồi quay lại sau." | "No sent emails found" / "Your Sent folder is empty so no style sample could be generated. Send a few emails and come back." |

### Error states (toast surface — via QueryClient `meta.errorMessage`; field-level via `useLocalizedFieldError`)

| Error code | VI | EN |
|------------|-----|-----|
| `voice.writing_style.too_short` | "Mô tả giọng văn cần ít nhất 200 từ." | "Writing style needs at least 200 words." |
| `voice.writing_style.too_long` | "Mô tả giọng văn không được vượt 500 từ." | "Writing style cannot exceed 500 words." |
| `voice.personal_instructions.too_long` | "Hướng dẫn cá nhân không được vượt 2000 ký tự." | "Personal instructions cannot exceed 2000 characters." |
| `voice.tone_preset.invalid` | "Tone không hợp lệ." | "Invalid tone preset." |
| `knowledge.title.duplicate` | "Đã có đoạn kiến thức với tiêu đề này." | "A snippet with this title already exists." |
| `safety_net.observation_not_deletable` | "Không thể xóa người gửi do hệ thống tự thêm." | "Cannot delete a system-observed sender." |
| `ai.byok.provider_not_allowed` | "Nhà cung cấp này không hỗ trợ BYOK." | "BYOK is not supported for this provider." |
| `ai.model.not_in_catalog` | "Model không có trong danh mục đã duyệt." | "Model is not in the curated catalog." |
| `ai.test_connection.rate_limited` | "Bạn đã kiểm tra quá nhiều lần. Thử lại sau 1 giờ." | "Too many test attempts. Try again in 1 hour." |
| `voice.generate.rate_limited` | "Đã đạt giới hạn 3 lần/giờ. Thử lại sau." | "Reached the 3/hour limit. Try again later." |
| Generic mutation failure | "Không lưu được. Thử lại nhé." | "Couldn't save. Please try again." |

### Success toasts (via `meta.successMessage`)

| Action | VI | EN |
|--------|-----|-----|
| Voice saved | "Đã lưu giọng văn" | "Voice saved" |
| Behavior saved | "Đã lưu hành vi" | "Behavior saved" |
| Snippet added | "Đã thêm đoạn kiến thức" | "Snippet added" |
| Snippet updated | "Đã cập nhật đoạn kiến thức" | "Snippet updated" |
| Snippet deleted | "Đã xóa đoạn kiến thức" | "Snippet deleted" |
| Safety-net sender added | "Đã thêm người gửi vào lưới an toàn" | "Sender added to safety net" |
| Safety-net sender removed | "Đã gỡ người gửi" | "Sender removed" |
| BYOK key saved | "Đã lưu key (không hiển thị lại)" | "Key saved (will not be shown again)" |
| BYOK test OK | "Key hoạt động bình thường" | "Key works" |
| AI provider preference saved | "Đã lưu lựa chọn AI" | "AI preference saved" |
| Writing style generated | "Đã tạo bản nháp — xem lại trước khi lưu" | "Draft generated — review before saving" |

### Destructive actions + confirmation

All destructive confirms use a shadcn `Dialog` (NOT `<AlertDialog>` — project has not standardized on alert-dialog yet; reuse `Dialog` with a destructive primary button to keep the primitive set tight).

| Action | Trigger location | Confirmation body (VI) | Confirmation body (EN) | Primary button copy |
|--------|------------------|-------------------------|------------------------|--------------------|
| Delete knowledge snippet | Row Delete icon in `Your voice → Knowledge` Table | "Xóa đoạn kiến thức {title}? Hành động này không thể hoàn tác." | "Delete snippet {title}? This cannot be undone." | "Xóa" / "Delete" (destructive variant) |
| Remove safety-net sender | Row Delete icon in `Safety net` Table | "Gỡ {pattern} khỏi lưới an toàn? AI sẽ lại tự xử lý thư từ địa chỉ này." | "Remove {pattern} from the safety net? The AI will resume auto-actioning email from this sender." | "Gỡ" / "Remove" (destructive variant) |
| Replace BYOK key (overwrite existing) | Pencil icon next to masked key in BYOK row | "Thay key {provider}? Key cũ sẽ bị ghi đè và không khôi phục được." | "Replace {provider} key? The previous key will be overwritten and cannot be recovered." | "Thay key" / "Replace key" (destructive variant) |

Single-step confirm (one click in the Dialog) — Phase 9 does NOT use the "confirm twice + reason" admin-side pattern from Phase 8. These are user-owned items, not tenant-wide state.

---

## Layout & Component Inventory

### Page skeleton

```
/ai/page.tsx
└── <AiConfigPage>           // existing shell, refactored
    ├── <h1>Cài đặt AI</h1>  + subtitle
    ├── <YourVoiceSection>
    ├── <BehaviorSection>
    ├── <UpdatesSection>
    ├── <SafetyNetSection>
    └── <AiProviderSection>
```

Sections are visually separated by `<Separator className="my-8" />` (24px gap above/below from `space-y-8` + 32px separator margin = ~56px section break).

### Reusable composed components (Phase 9 owns these)

**Important: review against the "raw shadcn first" gate** (rule of three + 3+ stacked primitives + new prop API value). Each composed component below justifies its own existence:

1. **`<SectionHeader title icon? helperText? rightSlot?>`** — used 5× (one per section); composes `lucide` icon + `text-xl font-semibold` title + `text-sm text-muted-foreground` helper + optional right-aligned action slot. Justified: 5 callsites > rule-of-three; new prop API (icon + helper + rightSlot) beats raw `<div className="flex">` shape repeated 5×.
2. **`<SettingCard title description? icon? rightSlot? children?>`** — used ≥ 12× across sections (writing style, personal instructions, signature, tone, AI output language, generate-style trigger lives inside writing style card, draft confidence, plus the inline-switch cards). Composes `Card + CardHeader + CardTitle + CardDescription + CardContent` from shadcn `card.tsx` with a consistent right-aligned action area. Justified: 12 callsites; the rightSlot prop turns "title + description + Edit button OR title + description + Switch" into one component instead of two duplicated patterns.
3. **`<ConfirmDialog title description trigger confirmLabel onConfirm variant?>`** — used 3× (delete snippet, remove sender, replace BYOK). Composes `Dialog` + destructive `Button`. Justified: 3 callsites = rule-of-three; the destructive-variant + async-onConfirm + auto-close behavior is a real prop API improvement over inlining `Dialog` three times.

Raw shadcn used directly (NOT wrapped):
- `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent` (used inside `<SettingCard>` only — outside features that need a generic card import shadcn directly)
- `Dialog`, `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter` — used directly for each per-setting edit Dialog. NO `EditDialog` wrapper — every edit dialog has a different form body, so a wrapper would add no value.
- `Button`, `Input`, `Textarea`, `Switch`, `Select` (with `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`), `RadioGroup`, `RadioGroupItem`, `Table` (with `TableHeader`, `TableRow`, `TableHead`, `TableBody`, `TableCell`), `Badge`, `Separator`, `Label`, `Tooltip`.

### Per-section component tree

#### Your voice (`<YourVoiceSection>`)
- `<SettingCard title="Phong cách viết">` → "Sửa" button opens `<WritingStyleDialog>` containing:
  - `<Textarea>` (200–500 word counter, live count in helper text)
  - `<Button variant="outline">` "Tạo từ email đã gửi gần đây" (SET-VOICE-07) — opens loading state, populates the textarea on success; never auto-saves
  - Footer: `<Button variant="secondary">Hủy</Button>` + `<Button>Lưu</Button>`
- `<SettingCard title="Về tôi (hướng dẫn cá nhân)">` → opens `<PersonalInstructionsDialog>` with `<Textarea>` + 2000-char counter
- `<SettingCard title="Chữ ký email">` → opens `<EmailSignatureDialog>` with `<Textarea>` + 500-char counter
- `<SettingCard title="Tone giọng văn">` → opens `<TonePresetDialog>` with `<Select>` (professional/friendly/casual/formal/custom)
- `<SettingCard title="Ngôn ngữ AI viết">` → opens `<AiOutputLanguageDialog>` with `<RadioGroup>` (Tiếng Việt mặc định / English)
- `<SettingCard title="Kho kiến thức" rightSlot={+ Add Button}>` → `<Table>` (Tiêu đề | Cập nhật | sửa-icon | xóa-icon) ordered by `updatedAt DESC`; clicking + Add or row Edit opens `<KnowledgeDialog>` with `<Input>` title + `<Textarea>` content; deletion uses `<ConfirmDialog>`

#### Behavior (`<BehaviorSection>`)
- `<SettingCard title="Tự động soạn nháp trả lời">` with inline `<Switch>` (no Dialog — short toggle, D-03)
- `<SettingCard title="Ngưỡng tự tin nháp">` → opens `<DraftConfidenceDialog>` with `<Select>` LOW / MEDIUM / HIGH + a one-line explanation per option
- `<SettingCard title="Bảo vệ dữ liệu nhạy cảm">` with inline `<Switch>` (default ON; tooltip explains it strips PII from outbound LLM prompts)

#### Updates (`<UpdatesSection>`)
- `<SettingCard title="Tóm tắt hằng ngày">` with inline `<Switch>` (bound to v1.0 ANL-03 endpoint)
- `<SettingCard title="Chế độ shadow (chỉ ghi log)">` with inline `<Switch>` (bound to v1.0 TRG-07)

#### Safety net (`<SafetyNetSection>`)
- `<SettingCard title="Người gửi được bảo vệ" rightSlot={inline-add-input + add-button}>` → `<Table>` (Mẫu | Loại | Thêm bởi | Ngày | xóa-icon)
  - Loại column shows `<Badge>` reading "Email" or "Domain"
  - Thêm bởi column shows `<Badge variant="outline">` "Bạn" for `created_by_user=TRUE` or "Hệ thống" for observation-created
  - Delete icon disabled (with tooltip) for `created_by_user=FALSE` rows
- Below the table: inline single-row helper showing "Mẹo: dùng `@acme.com` để bảo vệ toàn bộ domain."
- The `Auto-send rules` toggle that already exists on `/ai` lives as the LAST card in this section: `<SettingCard title="Tự động gửi theo rule">` with inline `<Switch>` — moved into this section because it is a safety-net-adjacent global gate. (Phase 9 does not change the toggle's backend behavior — it only rehouses the UI.)

#### AI Provider (`<AiProviderSection>`)
Three feature rows (Chat / Triage / Draft), each rendered as a `<SettingCard>` with the following body layout (NO Dialog — these are persistent control surfaces, not edit-mode):

```
┌─────────────────────────────────────────────────────────────────┐
│ Chat                                                            │
│ Phục vụ chat với trợ lý                                         │
│                                                                 │
│ [○ Use platform default  ●  Use my key]   ← segmented switch    │
│                                                                 │
│ Provider [Select ▼]   Model [Select ▼]                          │
│                                                                 │
│ Đang dùng: OpenAI / gpt-4o-mini · $0.42 trong 7 ngày qua        │
└─────────────────────────────────────────────────────────────────┘
```

Below the three feature rows: a single `<SettingCard title="Key cá nhân (BYOK)">` containing the migrated `<ByokForm>` with one row per allowed provider (OpenAI / Anthropic / Google / DeepSeek). Each row:
- Provider label + logo
- If no key saved: `<Input type="password">` + `Lưu` button
- If key saved: masked `sk-****abc1` text + `<Button variant="outline">Kiểm tra kết nối</Button>` + pencil-icon trigger for `<ConfirmDialog>` (replace key)
- Result pill after test-connection: `<Badge>` with semantic token (`bg-green-soft` for OK, `bg-red-soft` for INVALID_KEY/etc.)
- OpenRouter and 9Router NEVER appear here (frontend filter + backend allow-list).

### Loading + error states (per component family)

| Component | Loading | Error |
|-----------|---------|-------|
| Initial page load | `<LoadingState variant="cards" count={2} />` (existing component, wrapped by Suspense) | Next's `error.tsx` at route group level (existing pattern) |
| SettingCard inside section | Skeleton `<Spinner>` inside the card body while the section query resolves | Toast via QueryCache.onError (only on background refetch — initial fetch error owned by error.tsx) |
| Dialog form submission | `<Button disabled>` with `<Spinner size="sm">` left of label | Inline `<p className="text-sm text-destructive">` under the offending field (via `useLocalizedFieldError`) + toast |
| Generate-from-sent button | `<Button disabled>` with `<Spinner>` + button label changes to "Đang tạo…" / "Generating…" | Toast `meta.errorMessage="voice.generate.rate_limited"` or generic |
| Test-connection | Button label changes to "Đang kiểm tra…" / "Testing…" with `<Spinner>` | Pill renders the enum result; no toast (the pill IS the result UI) |
| Table empty | Empty-state copy block above the Table (NOT a row) — uses copy table above | n/a |

---

## Interaction Contracts

| Interaction | Behavior |
|-------------|---------|
| Click SettingCard Edit | Opens a controlled Dialog with current value pre-filled; ESC closes; click outside closes; unsaved state is discarded silently (no "are you sure?" mid-dialog — too noisy for setting cards) |
| Save inside Dialog | Disable Save while pending; on success → close Dialog + show success toast (meta) + invalidate the section TanStack Query; on validation error → keep Dialog open + inline field error |
| Toggle inline Switch | Optimistic update (TanStack `onMutate` rollback on error); on error → revert + error toast |
| Add knowledge snippet | `+ Add` opens empty `<KnowledgeDialog>`; Save closes Dialog + appends row; new row appears at top (ordered `updatedAt DESC`) |
| Edit knowledge row | Row Edit icon opens `<KnowledgeDialog>` prefilled; same Save behavior |
| Delete knowledge row | Row Delete icon opens `<ConfirmDialog>`; Confirm closes both + removes row |
| Add safety-net sender | Inline input above the Table (no Dialog — single field); Enter or click + Add submits; new row appears |
| Remove safety-net sender | Row Delete icon opens `<ConfirmDialog>`; observation-created rows have the icon disabled with `<Tooltip>` |
| Toggle "Use platform default ↔ Use my key" per feature | Segmented `<RadioGroup>` (visually styled as a 2-way pill); flipping to "Use my key" disables the Provider/Model selects when no BYOK exists for that provider AND shows an inline "Lưu key bên dưới để dùng tính năng này" / "Save your key below to use this option" |
| Save BYOK key | Plaintext key sent in POST body once; response NEVER echoes plaintext; on success → input replaced with masked display + Test connection button appears |
| Test BYOK connection | POST `/api/settings/ai/test-connection`; button shows spinner; on response → render pill with enum value (OK = green, others = red) and persist the result on the row for 10s before fading |
| Generate-from-sent (SET-VOICE-07) | Button inside writing-style Dialog; on click → button enters loading; on success → textarea replaced with LLM output (user can still edit); user MUST click Save explicitly to persist |
| Section anchor | Each `<SectionHeader>` has an `id` matching the section slug so deep-links from docs (`/ai#safety-net`) scroll-anchor cleanly |

Keyboard:
- Tab order follows DOM order; Dialog traps focus (shadcn default)
- `Cmd/Ctrl+S` inside any Dialog triggers Save (custom handler wired in dialog form)
- Switches respond to Space
- Delete icon focus state visible via `ring-ring` (no custom focus outlines)

---

## Accessibility

- Every `<Switch>` has an associated `<Label htmlFor>` (shadcn-aware) reading the SettingCard title
- Tables have `<caption className="sr-only">` describing the table purpose
- Pill status (`OK`, `INVALID_KEY`, etc.) NEVER conveys meaning by color alone — pill text always carries the enum value
- Word-count and char-count helpers under textareas use `aria-live="polite"` so screen readers announce the remaining budget
- The destructive `ConfirmDialog` uses `role="alertdialog"` (set via shadcn primitive props)
- Contrast: all text-on-surface combinations resolve to ≥ 4.5:1 via the locked palette (verified during Phase 1.6 visual remediation)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `card`, `dialog`, `button`, `input`, `textarea`, `switch`, `select`, `radio-group`, `table`, `badge`, `separator`, `label`, `tooltip`, `spinner`, `sonner` | not required (all already in `apps/web/components/ui/`) |

No third-party registries declared for Phase 9. No new shadcn primitives need installation — every primitive is already present (confirmed by listing `apps/web/components/ui/`).

---

## Internationalization

- Bundle file: `apps/web/features/ai/messages.ts` (new) re-exports VN+EN dictionaries that map to keys in `apps/web/messages/vi.json` and `apps/web/messages/en.json` (existing next-intl bundle).
- VI is the default; EN is the secondary. Both bundles MUST contain every key listed in the Copywriting Contract above — missing keys will cause `useTranslations` to throw at runtime (next-intl strict mode).
- AI output language picker on `Your voice` is INDEPENDENT of UI language — picking EN here does NOT switch the UI.

---

## Out-of-Scope (for this phase)

These are explicitly excluded from the visual contract:
- Visual brand refresh — deferred to v1.3 `VISUAL-REFRESH-*`. Phase 9 consumes the locked purple-shifted tokens as-is.
- New chart components for the cost helper — text-only "$X.XX trong 7 ngày" is sufficient. Sparkline / bar chart is a v1.3 want.
- Per-entry `protect` vs `escalate` mode UI on safety net — backend column does not exist in v1.2 (SET-SAFE-03 deferred).
- Paste-import textarea for safety net — SET-SAFE-02 deferred.
- Re-skinning the legacy `/settings` page — phase explicitly leaves it untouched except for ByokForm removal.
- Re-skinning rule examples / personas inside `RuleComposer` — that is Phase 08.1's surface.

---

## Verification

Before declaring `/ai` done in executor phase, the following MUST be verified per project UX rule:

1. Playwright spec under `apps/web/e2e/ai-settings.spec.ts` covers the golden path: edit writing style → Save → reload → value persists; toggle auto-draft → reload → value persists; add + edit + delete knowledge snippet; add + delete safety-net sender; pick BYOK provider+model + test connection (enum-only response).
2. ESLint passes (no `bg-[#xxxxxx]` violations).
3. `pnpm --filter web run generate:api` regenerates `schema.d.ts` after backend DTO additions; the regen is committed.
4. Playwright MCP visual sweep through each section + a representative Dialog screenshot taken for the auditor.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (5 sections × empty/error/success copy declared bilingually; CTAs verb+noun; destructive confirms named)
- [ ] Dimension 2 Visuals: PASS (component inventory complete, layout tree per section, raw-shadcn-first gate justified for 3 wrappers)
- [ ] Dimension 3 Color: PASS (60/30/10 declared, accent reserved-for list explicit, no hardcoded hex, semantic colors separated from accent)
- [ ] Dimension 4 Typography: PASS (4 sizes, 2 weights, body 16/1.5, heading 20/1.3 + 28/1.2)
- [ ] Dimension 5 Spacing: PASS (8-point scale, no exceptions, page container width locked)
- [ ] Dimension 6 Registry Safety: PASS (shadcn official only, no third-party blocks)

**Approval:** pending
