---
phase: 06-polish-casa-verified-launch
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - loadtest/compose.loadtest.yml
  - loadtest/scripts/golden-path.js
  - loadtest/README.md
  - loadtest/.gitignore
  - backend/api/build.gradle.kts
  - .gitignore
autonomous: true
requirements:
  - SPEC-06-R2
tags: [k6, docker-compose, gradle, loadtest, invariants, postgres, redis]

must_haves:
  truths:
    - "`docker compose -f loadtest/compose.loadtest.yml up -d --wait` starts a healthy stack (postgres-17 + redis-7 + zeromail-api:loadtest + zeromail-worker:loadtest) on the GHA runner, with `REFRESH_TOKEN_KEY_BASE64` injected into BOTH api and worker containers (D-15)."
    - "`k6 run loadtest/scripts/golden-path.js` against `API_BASE_URL=http://localhost:8080` drives 500 iterations/minute of synthetic Pub/Sub envelopes for ~10 minutes, with `http_req_failed < 1%` and synthetic tenants named exactly `loadtest-tenant-1` ... `loadtest-tenant-50` (D-05)."
    - "`./gradlew :backend:api:loadtestVerify` runs three invariant assertions against the running compose stack and exits 0 only if all three pass: (a) zero rows in `triage_audit` whose `tenant_id::text` is not `loadtest-tenant-%`; (b) zero ledger drift — `SUM(amount_credits)` per loadtest tenant is 0 across `credit_ledger_entry`; (c) zero log lines in `loadtest/run/run.log` matching the regex `email_body|prompt|completion|raw_html` (SPEC AC #1, #9 — privacy invariant validated on prod-config logs)."
    - "On success, `loadtestVerify` writes a result template at `.planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md` recording the three PASS lines and the ISO timestamp."
    - "OCI images are built via `./gradlew :backend:api:bootBuildImage --imageName=zeromail-api:loadtest` and `./gradlew :backend:worker:bootBuildImage --imageName=zeromail-worker:loadtest` — NO Dockerfile is added to the repo (resolves OQ-1)."
  artifacts:
    - path: "loadtest/compose.loadtest.yml"
      provides: "Ephemeral docker-compose stack: postgres-17 + redis-7 + api + worker. Tear down with `docker compose down -v` (D-02 + D-05)."
      contains: "ZEROMAIL_LOADTEST_ENABLED"
    - path: "loadtest/scripts/golden-path.js"
      provides: "k6 constant-arrival-rate scenario: 500 iters/min × 10m across 50 tenants posting synthetic Pub/Sub envelopes to /internal/pubsub/gmail."
      contains: "constant-arrival-rate"
    - path: "loadtest/README.md"
      provides: "Operator runbook: prerequisites, env vars, run command, teardown, where invariant assertions live."
    - path: "loadtest/.gitignore"
      provides: "Excludes `run/` runtime artifacts (run.log, summary.json) from commits — only `06-LOAD-TEST-RESULT.md` is committed evidence."
    - path: "backend/api/build.gradle.kts"
      provides: "New `loadtestVerify` Gradle task running the three invariant assertions via JDBC + filesystem regex."
      contains: "loadtestVerify"
  key_links:
    - from: "loadtest/scripts/golden-path.js"
      to: "backend/api/src/main/java/com/zeromail/api/controllers/gmail/GmailPubSubController.java"
      via: "HTTP POST /internal/pubsub/gmail with synthetic envelope; Authorization: Bearer loadtest-stub-token; profile=loadtest accepts any non-empty token via Plan 06-01's LoadtestPubsubVerifierConfig"
      pattern: "internal/pubsub/gmail"
    - from: "loadtest/compose.loadtest.yml"
      to: "backend/api/src/main/resources/application-loadtest.yml"
      via: "SPRING_PROFILES_ACTIVE=loadtest + ZEROMAIL_LOADTEST_ENABLED=true environment block (D-03 belt-and-suspenders)"
      pattern: "SPRING_PROFILES_ACTIVE"
    - from: "backend/api/build.gradle.kts :loadtestVerify"
      to: "postgres-loadtest published port 15433"
      via: "JDBC `jdbc:postgresql://localhost:15433/zeromail`"
      pattern: "15433"
---

<objective>
Land the 50-tenant × ~10 msg/min docker-compose load harness plus the Gradle task that asserts three invariants against the running stack. This is the SPEC AC #2 + #9 gate — the test that proves under sustained load there is no cross-tenant audit leak, no ledger drift, and no privacy log-bleed on prod-config logs.

Purpose: D-02 mandates ephemeral docker-compose on the GHA runner because Testcontainers loads the test logback config and would false-green invariant (c). The k6 + compose + Gradle JDBC task pairing is the only configuration that loads prod `logback-spring.xml` and exercises real backend code at sustained rate. D-04 fixes the exact three invariant queries. D-15 mandates `REFRESH_TOKEN_KEY_BASE64` injection into both containers to validate the fail-fast property at boot. OQ-1 resolves to `bootBuildImage` — Spring Boot's built-in paketobuildpacks task, no Dockerfile committed.

Output: 4 new files under `loadtest/`, 1 modified Gradle file with one new task, 1 modified root `.gitignore`. Result evidence file (`06-LOAD-TEST-RESULT.md`) is owned by Plan 06-05; Plan 06-02 only generates it at task runtime via the Gradle task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@.planning/phases/06-polish-casa-verified-launch/06-SPEC.md
@.planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md
@.planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md
@.planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md
@.planning/phases/06-polish-casa-verified-launch/06-01-PLAN.md
@backend/api/build.gradle.kts
@backend/api/src/main/resources/application.yml

<interfaces>
<!-- Existing types/contracts the load harness consumes. -->

Production env-var fail-fast contract (`backend/api/src/main/resources/application.yml`):
- `REFRESH_TOKEN_KEY_BASE64:?refresh-token-key required` (line 102)
- `GOOGLE_OAUTH_CLIENT_ID:?` + `GOOGLE_OAUTH_CLIENT_SECRET:?` (lines 27-28)
- `PUBSUB_PUSH_AUDIENCE_URL:?` + `PUBSUB_SA_PRINCIPAL_EMAIL:?` (lines 93-94)
- All `:?` properties must be supplied by `loadtest/compose.loadtest.yml` `environment:` blocks or boot fails (which is exactly the D-15 verification property).

Worker fail-fast contract (`backend/worker/src/main/resources/application.yml:63`):
- `REFRESH_TOKEN_KEY_BASE64:?` — must receive the SAME base64 value as api (AES-GCM key parity per D-15).

Production controller seam:
- `POST /internal/pubsub/gmail` accepts `PubSubPushEnvelope` JSON (see `backend/api/src/main/java/com/zeromail/api/dto/gmail/PubSubPushEnvelope.java` for record shape).
- `PubSubOidcAuthFilter.shouldNotFilter(...)` filters `/internal/pubsub/*` paths only; the loadtest profile's verifier (from Plan 06-01) accepts any non-empty Bearer.

Table schema (verified from PATTERNS §`loadtestVerify`):
- `triage_audit.tenant_id` (UUID or text — cast to text in the SQL `tenant_id::text NOT LIKE 'loadtest-tenant-%'`)
- `credit_ledger_entry.tenant_id` + `credit_ledger_entry.amount_credits` — invariant (b) groups by tenant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author `loadtest/scripts/golden-path.js` — k6 constant-arrival-rate scenario at 500 iter/min × 10m</name>
  <files>loadtest/scripts/golden-path.js</files>
  <read_first>
    - .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §1.1 (verbatim k6 script template — 92-166 includes the full skeleton, `SharedArray` of 50 tenants, `constant-arrival-rate` executor, `handleSummary` hook)
    - .planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md lines 228-257 (envelope shape + Authorization header requirements; matches `PubSubPushEnvelope` record at `backend/api/src/main/java/com/zeromail/api/dto/gmail/PubSubPushEnvelope.java`)
    - backend/api/src/main/java/com/zeromail/api/dto/gmail/PubSubPushEnvelope.java (record shape — `message: { data, messageId, publishTime }` + `subscription`)
    - .planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md D-05 (synthetic tenant naming `loadtest-tenant-<1..50>` — the invariant SQL queries filter on this prefix)
  </read_first>
  <action>
    Create `loadtest/scripts/golden-path.js` as a single-file k6 script. Use research §1.1 verbatim. Required properties:

    - Imports: `http from 'k6/http'`, `check from 'k6'`, `SharedArray from 'k6/data'`, `uuidv4` from `https://jslib.k6.io/k6-utils/1.4.0/index.js`, `encoding from 'k6/encoding'`.
    - `SharedArray('tenants')` returns exactly `Array.from({ length: 50 }, (_, i) => \`loadtest-tenant-${i + 1}\`)` — tenant naming is load-bearing for invariant (a).
    - `options.scenarios.pubsub_push`: `executor: 'constant-arrival-rate'`, `duration: '10m'`, `rate: 500`, `timeUnit: '1m'`, `preAllocatedVUs: 50`, `maxVUs: 200`, `tags: { kind: 'pubsub_push' }`.
    - `options.thresholds`: `http_req_failed: [{ threshold: 'rate<0.01', abortOnFail: true }]`, `'http_req_duration{kind:pubsub_push}': ['p(99)<2000']`.
    - `options.discardResponseBodies: true` (memory hygiene on GHA runner).
    - Default function: pick random tenant from `SharedArray`; build inner `data` as `encoding.b64encode(JSON.stringify({ emailAddress: \`${tenant}@loadtest.invalid\`, historyId }))`; wrap in envelope `{ message: { data, messageId: uuidv4(), publishTime: new Date().toISOString() }, subscription: 'projects/loadtest/subscriptions/loadtest-sub' }`; POST to `\`${__ENV.API_BASE_URL}/internal/pubsub/gmail\`` with headers `Content-Type: application/json` + `Authorization: Bearer loadtest-stub-token`.
    - `check(response, { 'push accepted': (r) => r.status >= 200 && r.status < 300 })`.
    - `handleSummary(data)` writes `loadtest/run/summary.json` (machine-readable artifact for the Gradle task to embed) + stdout `iterations` count.

    File header comment block: `// Load test for Phase 6 launch invariants. See .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §1.1.\n// Run with: API_BASE_URL=http://localhost:8080 k6 run loadtest/scripts/golden-path.js`

    Do NOT use `vu.idInTest` to map tenants; the random pick per iteration is correct (research §1.1 explicitly chose this). Do NOT add a second scenario.
  </action>
  <acceptance_criteria>
    - File exists at `loadtest/scripts/golden-path.js`.
    - `grep -c "executor: 'constant-arrival-rate'" loadtest/scripts/golden-path.js` returns ≥ 1 (presence-only, matching the Node verifier).
    - `grep -c "rate: 500" loadtest/scripts/golden-path.js` returns ≥ 1.
    - `grep -c "timeUnit: '1m'" loadtest/scripts/golden-path.js` returns ≥ 1.
    - `grep -c "preAllocatedVUs: 50" loadtest/scripts/golden-path.js` returns ≥ 1.
    - `grep -c "maxVUs: 200" loadtest/scripts/golden-path.js` returns ≥ 1.
    - `grep -c "loadtest-tenant-" loadtest/scripts/golden-path.js` returns ≥ 1 (in the SharedArray initializer).
    - `grep -c "Authorization: 'Bearer loadtest-stub-token'\\|Authorization.*Bearer loadtest-stub-token" loadtest/scripts/golden-path.js` returns ≥ 1.
    - `grep -c "loadtest/run/summary.json" loadtest/scripts/golden-path.js` returns ≥ 1.
    - Local smoke (manual, not in CI): `k6 run --duration 30s --vus 1 loadtest/scripts/golden-path.js` against a running api on port 8080 in `loadtest` profile completes without script errors (HTTP failures expected without compose stack — script-shape validation only).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('loadtest/scripts/golden-path.js','utf8');for(const p of [/executor: 'constant-arrival-rate'/, /rate: 500/, /timeUnit: '1m'/, /preAllocatedVUs: 50/, /maxVUs: 200/, /loadtest-tenant-/, /Bearer loadtest-stub-token/, /loadtest\/run\/summary.json/]){if(!p.test(s)){console.error('MISSING:',p);process.exit(1)}}console.log('ok')"</automated>
  </verify>
  <done>k6 script lands with all locked-in constants verifiable by grep.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author `loadtest/compose.loadtest.yml` — postgres-17 + redis-7 + api + worker, healthcheck-gated, with REFRESH_TOKEN_KEY_BASE64 to both containers (D-15)</name>
  <files>loadtest/compose.loadtest.yml</files>
  <read_first>
    - .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §1.3 (full compose skeleton lines 200-297 — env-var enumeration, healthchecks, depends_on chain api→worker)
    - .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §1.7 (REFRESH_TOKEN_KEY_BASE64 injection via runner shell env)
    - .planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md lines 187-225 (compose v2 dialect + healthcheck pattern from repo-root docker-compose.yml — same dialect, NEW ports)
    - backend/api/src/main/resources/application.yml lines 1-160 (every `:?` fail-fast env var; the compose env block must supply all of them — read the whole file)
    - backend/worker/src/main/resources/application.yml lines 1-100 (worker `:?` env vars including line 63 `REFRESH_TOKEN_KEY_BASE64`)
    - .planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md D-15 (REFRESH_TOKEN_KEY_BASE64 must be injected into BOTH api and worker — same value)
    - docker-compose.yml (repo root — dev stack, for port collision avoidance: dev postgres is 15432, loadtest must NOT collide)
  </read_first>
  <action>
    Create `loadtest/compose.loadtest.yml` as docker-compose v2 file (`name: zeromail-loadtest`). Use research §1.3 verbatim as the template.

    Required services + properties:
    1. `postgres-loadtest` (image `postgres:17.6`):
       - env: `POSTGRES_DB: zeromail`, `POSTGRES_USER: zeromail`, `POSTGRES_PASSWORD: zeromail`.
       - port: `"15433:5432"` (NOT 15432 — avoid collision with `docker-compose.yml` dev postgres).
       - healthcheck: `["CMD-SHELL", "pg_isready -U zeromail -d zeromail"]`, `interval: 2s`, `timeout: 3s`, `retries: 30`, `start_period: 5s`.
    2. `redis-loadtest` (image `redis:7.2`):
       - healthcheck: `["CMD", "redis-cli", "ping"]`, `interval: 2s`, `timeout: 3s`, `retries: 30`.
    3. `api` (image `zeromail-api:loadtest` — built locally via `bootBuildImage`, see Task 5):
       - `depends_on`: both postgres-loadtest + redis-loadtest with `condition: service_healthy`.
       - env block MUST include:
         - `SPRING_PROFILES_ACTIVE: loadtest`
         - `ZEROMAIL_LOADTEST_ENABLED: "true"` (D-03 second guard, per Plan 06-01 Task 4 `application-loadtest.yml` activation property)
         - `DB_URL: jdbc:postgresql://postgres-loadtest:5432/zeromail`
         - `DB_USER: zeromail`, `DB_PASSWORD: zeromail`
         - `REDIS_HOST: redis-loadtest`, `REDIS_PORT: "6379"`
         - `REFRESH_TOKEN_KEY_BASE64: ${REFRESH_TOKEN_KEY_BASE64}` (compose interpolation from runner shell env; D-15 fail-fast validation)
         - `PUBSUB_PUSH_AUDIENCE_URL: http://api:8080/internal/pubsub/gmail`
         - `PUBSUB_SA_PRINCIPAL_EMAIL: loadtest@invalid`
         - Dummy fail-fast values for every other `:?` env var in `application.yml` — read the file to enumerate the full list and supply `loadtest` placeholders. At minimum: `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `ZEROMAIL_LLM_PLATFORM_API_KEY`, `SEPAY_WEBHOOK_API_KEY`, `ZEROMAIL_BILLING_BANK_CODE`, `ZEROMAIL_BILLING_BANK_NAME`, `ZEROMAIL_BILLING_ACCOUNT_NUMBER` ("0000"), `ZEROMAIL_BILLING_ACCOUNT_NAME`.
       - `ports: ["8080:8080"]`.
       - healthcheck: `["CMD", "curl", "-fsS", "http://localhost:8080/actuator/health/readiness"]`, `interval: 5s`, `timeout: 3s`, `retries: 60`, `start_period: 30s` (JVM cold-start budget — `application.yml` already exposes probes).
    4. `worker` (image `zeromail-worker:loadtest`):
       - `depends_on`: postgres-loadtest + redis-loadtest with `condition: service_healthy`, AND `api: condition: service_healthy` (pitfall §7.1 — only api runs Liquibase; worker waits until schema exists).
       - env block: same `SPRING_PROFILES_ACTIVE: loadtest`, same DB + Redis connection, same `REFRESH_TOKEN_KEY_BASE64: ${REFRESH_TOKEN_KEY_BASE64}` (D-15 parity — SAME value as api), plus worker-specific `:?` env vars (read `backend/worker/src/main/resources/application.yml` and enumerate; expected: `GOOGLE_PUBSUB_TOPIC_NAME`, `SEPAY_WEBHOOK_API_KEY`, `RESEND_API_KEY`, `ZEROMAIL_LLM_PLATFORM_API_KEY`, the 4 billing-bank values).

    YAML formatting: 2-space indent, services nested under top-level `services:`. Add a top-of-file comment block: `# Phase 6 50-tenant load harness. See .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §1.3.\n# Run: REFRESH_TOKEN_KEY_BASE64=$(openssl rand -base64 32) docker compose -f loadtest/compose.loadtest.yml up -d --wait\n# Tear down: docker compose -f loadtest/compose.loadtest.yml down -v`.

    Do NOT add a `version:` key (compose v2 omits it). Do NOT mount any host volumes — `down -v` must wipe everything (D-05 zero-contamination requirement). Do NOT enable any LLM-platform networking (`ZEROMAIL_LLM_PLATFORM_API_KEY: loadtest` is a placeholder that satisfies `:?` only; api `application-loadtest.yml` from Plan 06-01 sets `spring.ai.model.chat: none`).

    Document the fail-fast smoke in `loadtest/README.md`: add a new H2 section `## Fail-Fast Property (D-15)` describing how to reproduce the negative-control (unset `REFRESH_TOKEN_KEY_BASE64` in the host shell, run `docker compose -f loadtest/compose.loadtest.yml up --wait`, expect a non-zero exit within 60s because the worker container's `:?` operator at `backend/worker/src/main/resources/application.yml:63` aborts boot).
  </action>
  <acceptance_criteria>
    - File exists at `loadtest/compose.loadtest.yml`.
    - `grep -c "name: zeromail-loadtest" loadtest/compose.loadtest.yml` returns 1.
    - Four services present: `grep -E '^  (postgres-loadtest|redis-loadtest|api|worker):' loadtest/compose.loadtest.yml | wc -l` returns 4.
    - Postgres port mapping is `"15433:5432"` — `grep -c '"15433:5432"' loadtest/compose.loadtest.yml` returns 1 (no port collision with dev compose).
    - `REFRESH_TOKEN_KEY_BASE64: \${REFRESH_TOKEN_KEY_BASE64}` appears in BOTH the `api` and `worker` environment blocks — `grep -c 'REFRESH_TOKEN_KEY_BASE64: \${REFRESH_TOKEN_KEY_BASE64}' loadtest/compose.loadtest.yml` returns 2.
    - `ZEROMAIL_LOADTEST_ENABLED: "true"` appears in the api environment block — `grep -c 'ZEROMAIL_LOADTEST_ENABLED' loadtest/compose.loadtest.yml` returns ≥ 1.
    - `SPRING_PROFILES_ACTIVE: loadtest` appears in both api and worker — `grep -c 'SPRING_PROFILES_ACTIVE: loadtest' loadtest/compose.loadtest.yml` returns 2.
    - Worker `depends_on: api: condition: service_healthy` is present — `grep -A2 'worker:' loadtest/compose.loadtest.yml | grep -B0 -A 30 'depends_on' | grep -c 'api:'` returns ≥ 1.
    - Compose schema validity: `docker compose -f loadtest/compose.loadtest.yml config` exits 0.
    - Negative-control smoke (D-15 fail-fast property): with REFRESH_TOKEN_KEY_BASE64 unset in the host shell, `docker compose -f loadtest/compose.loadtest.yml up --wait` exits non-zero within 60s because the worker container's `:?` operator at `backend/worker/src/main/resources/application.yml:63` aborts boot. Document this smoke in `loadtest/README.md` under "## Fail-Fast Property (D-15)".
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f loadtest/compose.loadtest.yml config > /dev/null</automated>
  </verify>
  <done>Compose file lands with all four services, healthcheck-gated, fail-fast env vars present, valid compose schema.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire OCI image builds via `bootBuildImage` (resolves OQ-1) — no Dockerfile committed</name>
  <files>backend/api/build.gradle.kts</files>
  <read_first>
    - backend/api/build.gradle.kts (entire file — Kotlin DSL style; the existing `openApi { customBootRun { ... } }` block at lines 31-75 is the analog for adding new Gradle tasks/configuration)
    - backend/worker/build.gradle.kts (for symmetry — same `bootBuildImage` configuration applies, but this task only edits backend/api; worker is configured in Task 4 OR by simply invoking the Gradle plugin's defaults at the command line, planner discretion)
    - .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §1.4 (OQ-1 resolution — `bootBuildImage` is the recommended path; uses paketobuildpacks; no Dockerfile)
    - .planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md lines 262-285 (Gradle Kotlin DSL task pattern in this file)
  </read_first>
  <action>
    Per OQ-1 resolution (recommended path: `bootBuildImage` — Spring Boot's built-in paketobuildpacks task, lowest churn, respects "no new application code"): add a configuration block in `backend/api/build.gradle.kts` that pins the loadtest image name:

    ```kotlin
    tasks.named<org.springframework.boot.gradle.tasks.bundling.BootBuildImage>("bootBuildImage") {
        imageName.set("zeromail-api:loadtest")
        // CLAUDE.md prescribes eclipse-temurin:25-jre-noble; let the Paketo Java buildpack pick its JDK 25 image automatically.
        // Do NOT override the builder unless the buildpack default does not match JDK 25.
    }
    ```

    Place this block AFTER the existing `openApi { customBootRun { ... } }` block (lines 31-75); use blank-line separation; do NOT touch any other configuration. The same task name resolves the same way for `:backend:worker:bootBuildImage` — if `backend/worker/build.gradle.kts` does NOT already pin an `imageName`, add the equivalent block to it as well (with `imageName.set("zeromail-worker:loadtest")`); if a different pin already exists, leave it and document the deviation in the SUMMARY.

    Enterprise readability: `imageName` is the canonical Spring Boot Gradle plugin property name — keep it. No locals needed.

    Do NOT commit a `Dockerfile`. Do NOT add a `paketo-buildpacks/*` builder override unless `bootBuildImage` fails its first run; if so, fall back to `eclipse-temurin:25-jre-noble` per CLAUDE.md and note the deviation.
  </action>
  <acceptance_criteria>
    - `backend/api/build.gradle.kts` contains `tasks.named<org.springframework.boot.gradle.tasks.bundling.BootBuildImage>("bootBuildImage")` configuration block.
    - The block sets `imageName.set("zeromail-api:loadtest")` — `grep -c 'imageName.set."zeromail-api:loadtest"' backend/api/build.gradle.kts` returns 1.
    - No `Dockerfile` file exists in the repo — `find . -name 'Dockerfile*' -not -path './node_modules/*' -not -path './.gradle/*' | wc -l` returns 0.
    - `./gradlew --no-daemon :backend:api:tasks --group="build"` lists `bootBuildImage` (Spring Boot plugin contract — sanity check that the task is still registered after the config block).
    - Image build smoke (manual, non-CI; documented in `loadtest/README.md` Task 5): `./gradlew --no-daemon :backend:api:bootBuildImage` produces a local OCI image `zeromail-api:loadtest`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew --no-daemon :backend:api:tasks --group="build" | grep -q bootBuildImage</automated>
  </verify>
  <done>`bootBuildImage` pinned to the `zeromail-api:loadtest` name; no Dockerfile committed; task discoverable via `./gradlew :backend:api:tasks`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add `:backend:api:loadtestVerify` Gradle task — three invariant assertions + result-file emission</name>
  <files>backend/api/build.gradle.kts</files>
  <read_first>
    - backend/api/build.gradle.kts (post Task 3 — the file the new task lives in; mirror the existing `openApi { customBootRun }` Kotlin DSL style; new task is `tasks.register("loadtestVerify") { ... }`)
    - .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §1.6 (verbatim Kotlin DSL skeleton lines 333-388 — the three invariant queries, JDBC connection, regex scan, result-file writer)
    - .planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md D-04 (exact invariant definitions)
    - backend/core/src/main/java/com/zeromail/core/triage/persistence/TriageAuditEntity.java (verify table name `triage_audit` and `tenant_id` column type — research §1.6 confirms this)
    - backend/core/src/main/java/com/zeromail/core/billing/persistence/CreditLedgerEntryEntity.java (verify table `credit_ledger_entry` + column `amount_credits`)
    - .planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md lines 262-285 (enterprise-readability for Kotlin DSL: `connection` not `conn`, `jdbcUrl` not `url`)
    - .planning/phases/06-polish-casa-verified-launch/06-VALIDATION.md (the three dimensions invariant (a)/(b)/(c) map to SPEC AC #1, #2, #9)
  </read_first>
  <action>
    In `backend/api/build.gradle.kts`, after the `bootBuildImage` block from Task 3, register a new task `loadtestVerify` with `group = "verification"` and `description = "Runs invariant checks against the loadtest compose Postgres + run.log"`. Use research §1.6 skeleton verbatim with enterprise-readable Kotlin locals.

    `doLast {}` body contains FOUR ordered steps:

    Step 1 — open JDBC connection:
    ```kotlin
    val jdbcUrl = System.getenv("LOADTEST_DB_URL") ?: "jdbc:postgresql://localhost:15433/zeromail"
    val username = System.getenv("LOADTEST_DB_USER") ?: "zeromail"
    val password = System.getenv("LOADTEST_DB_PASSWORD") ?: "zeromail"
    java.sql.DriverManager.getConnection(jdbcUrl, username, password).use { connection ->
       /* steps 2 + 3 */
    }
    ```

    Step 2 — Invariant (a) cross-tenant audit:
    ```kotlin
    val crossTenantCount = connection.prepareStatement(
        "SELECT COUNT(*) FROM triage_audit WHERE tenant_id::text NOT LIKE 'loadtest-tenant-%'"
    ).executeQuery().let { resultSet -> resultSet.next(); resultSet.getInt(1) }
    require(crossTenantCount == 0) { "Invariant (a) FAIL: $crossTenantCount rows from non-loadtest tenant" }
    ```

    Step 3 — Invariant (b) per-tenant ledger reconciliation:
    ```kotlin
    val drift = connection.prepareStatement("""
        SELECT tenant_id::text, SUM(amount_credits) AS net
        FROM credit_ledger_entry
        WHERE tenant_id::text LIKE 'loadtest-tenant-%'
        GROUP BY tenant_id
        HAVING SUM(amount_credits) <> 0
    """).executeQuery().let { resultSet ->
        val driftRows = mutableListOf<String>()
        while (resultSet.next()) driftRows.add("${resultSet.getString(1)}=${resultSet.getInt(2)}")
        driftRows
    }
    require(drift.isEmpty()) { "Invariant (b) FAIL: ledger drift ${drift.joinToString()}" }
    ```

    Step 4 — Invariant (c) regex log scan (OUTSIDE the JDBC `use {}` block):
    ```kotlin
    val logFile = rootProject.file("loadtest/run/run.log")
    require(logFile.exists()) { "Invariant (c) PRE-FAIL: ${logFile.absolutePath} missing — capture container logs before running this task" }
    val bleedPattern = Regex("email_body|prompt|completion|raw_html")
    val violations = logFile.useLines { lines -> lines.filter { bleedPattern.containsMatchIn(it) }.toList() }
    require(violations.isEmpty()) { "Invariant (c) FAIL: ${violations.size} log-bleed lines: ${violations.take(3)}" }
    ```

    Step 5 — emit result evidence:
    ```kotlin
    rootProject.file(".planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md").writeText("""
    # Load-Test Result — ${java.time.Instant.now()}

    - Invariant (a) cross-tenant audit: PASS (0 leaked rows)
    - Invariant (b) per-tenant ledger drift: PASS (50 tenants, all net=0)
    - Invariant (c) log-bleed regex: PASS (0 violating lines)

    *Generated by `:backend:api:loadtestVerify`. Source: loadtest/run/run.log + jdbc=$jdbcUrl.*
    """.trimIndent())
    ```

    Enterprise readability (CLAUDE.md): `connection`, `jdbcUrl`, `username`, `password`, `resultSet`, `driftRows`, `bleedPattern`, `logFile`, `violations`. FORBIDDEN: `conn`, `url`, `user`, `pass`, `rs`, `row`, `re`, `f`, `vios`. NO single-letter locals except `_` for explicitly ignored params (none here).

    Do NOT introduce HikariCP — `DriverManager` is sufficient (research §1.6 explicit). Do NOT couple the task to the `bootRun` lifecycle — it's a pure JDBC + filesystem operation. Do NOT use `kotlin.io.path` style for `loadtest/run/run.log`; `rootProject.file(...)` is the Gradle Kotlin DSL idiom.
  </action>
  <acceptance_criteria>
    - `backend/api/build.gradle.kts` contains `tasks.register("loadtestVerify")` — `grep -c 'tasks.register..loadtestVerify' backend/api/build.gradle.kts` returns 1.
    - All three invariant queries are present: `grep -c 'triage_audit' backend/api/build.gradle.kts` returns ≥ 1, `grep -c 'credit_ledger_entry' backend/api/build.gradle.kts` returns ≥ 1, `grep -c 'email_body|prompt|completion|raw_html' backend/api/build.gradle.kts` returns 1.
    - Result file path is exactly `.planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md` — `grep -c '06-LOAD-TEST-RESULT.md' backend/api/build.gradle.kts` returns ≥ 1.
    - Default JDBC URL points at port 15433 — `grep -c 'jdbc:postgresql://localhost:15433/zeromail' backend/api/build.gradle.kts` returns 1.
    - Enterprise-readable locals: forbidden tokens not present as bare identifiers — `grep -E '\b(conn|rs|tx|cfg|req|res)\b' backend/api/build.gradle.kts | grep -v -E 'connection|response|request|configuration' | wc -l` returns 0 (allow longer identifiers that happen to contain those substrings).
    - Task discoverable: `./gradlew :backend:api:tasks --group="verification" | grep -q loadtestVerify` returns 0.
    - Functional gate (full integration; SPEC AC #2): with the compose stack running + k6 having completed + `loadtest/run/run.log` captured, `./gradlew :backend:api:loadtestVerify` exits 0 and writes `06-LOAD-TEST-RESULT.md` with the three PASS lines. This functional run is invoked by Plan 06-04's `release.yml` job; this plan only verifies discoverability + signature.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew --no-daemon :backend:api:tasks --group="verification" | grep -q loadtestVerify</automated>
  </verify>
  <done>`loadtestVerify` Gradle task registered with all three invariant queries embedded; discoverable; ready for Plan 06-04 to wire into `release.yml`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Author `loadtest/README.md` + `loadtest/.gitignore` + update root `.gitignore`</name>
  <files>loadtest/README.md, loadtest/.gitignore, .gitignore</files>
  <read_first>
    - .gitignore (entire file — top-of-tree style; observe existing patterns like `.gradle/`, `build/`, `apps/web/.next/`)
    - .planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md lines 17-21 (loadtest/README.md analog: existing Playwright spec headers + research §1 — partial match; the README is operator-facing runbook)
    - .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §1.3 + §1.5 + §1.7 (the run commands the README documents)
    - .planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md D-05 (zero-contamination teardown property — `docker compose down -v`)
  </read_first>
  <action>
    File 1 — `loadtest/README.md`:
    Sections in this exact order (use markdown H2):
    - `## Purpose` — one paragraph: "Validates SPEC AC #2 + #9 — 50 concurrent tenants × ~10 msg/min for 10 minutes without cross-tenant audit leak, ledger drift, or privacy log-bleed. See `.planning/phases/06-polish-casa-verified-launch/06-SPEC.md`."
    - `## Prerequisites` — `k6 ≥ 1.7.0`, Docker Compose v2 (`docker compose ...`), Java 25 + Gradle 9, an unprivileged Postgres+Redis runner.
    - `## Run` — three blocks, copy-pasteable:
      1. Build images: `./gradlew --no-daemon :backend:api:bootBuildImage --imageName=zeromail-api:loadtest` then same for `:backend:worker:bootBuildImage`.
      2. Generate ephemeral key + bring up stack: `export REFRESH_TOKEN_KEY_BASE64=$(openssl rand -base64 32); docker compose -f loadtest/compose.loadtest.yml up -d --wait`.
      3. Drive load: `API_BASE_URL=http://localhost:8080 k6 run loadtest/scripts/golden-path.js`.
      4. Capture logs (BEFORE teardown): `docker compose -f loadtest/compose.loadtest.yml logs --no-color --no-log-prefix api worker > loadtest/run/run.log`.
      5. Run invariant verifier: `./gradlew --no-daemon :backend:api:loadtestVerify`.
    - `## Teardown` — `docker compose -f loadtest/compose.loadtest.yml down -v` (`-v` is required per D-05 to wipe volumes).
    - `## Fail-Fast Property (D-15)` — short paragraph: with `REFRESH_TOKEN_KEY_BASE64` UNSET in the host shell, `docker compose -f loadtest/compose.loadtest.yml up --wait` MUST exit non-zero within 60s. Worker boot aborts at `backend/worker/src/main/resources/application.yml:63` (`:?` operator on `REFRESH_TOKEN_KEY_BASE64`). This is the D-15 negative-control verification.
    - `## Result file` — `:backend:api:loadtestVerify` writes `.planning/phases/06-polish-casa-verified-launch/06-LOAD-TEST-RESULT.md`. THIS file is committed. `loadtest/run/*` is gitignored.
    - `## Invariants asserted` — bullet list quoting D-04 exactly:
      - (a) `triage_audit.tenant_id::text NOT LIKE 'loadtest-tenant-%' COUNT = 0`
      - (b) per-tenant `SUM(amount_credits) = 0` for every `loadtest-tenant-%` in `credit_ledger_entry`
      - (c) regex `email_body|prompt|completion|raw_html` on `loadtest/run/run.log` returns zero matches
    - `## Decision references` — bullet list linking to `.planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md` D-01 through D-05 + D-15.

    File 2 — `loadtest/.gitignore`:
    ```
    # Runtime artifacts produced by k6 + docker logs capture; committed evidence is .planning/phases/06-.../06-LOAD-TEST-RESULT.md only.
    run/
    *.log
    ```

    File 3 — root `.gitignore` (append, do not rewrite):
    Append at end:
    ```
    # Phase 6 load harness
    loadtest/run/
    ```
    Do NOT remove existing entries. Do NOT reorder.
  </action>
  <acceptance_criteria>
    - `loadtest/README.md` exists with sections `## Purpose`, `## Prerequisites`, `## Run`, `## Teardown`, `## Fail-Fast Property (D-15)`, `## Result file`, `## Invariants asserted`, `## Decision references` — `grep -c '^## ' loadtest/README.md` returns ≥ 8.
    - README documents the D-15 fail-fast smoke: `grep -c '^## Fail-Fast Property (D-15)' loadtest/README.md` returns 1.
    - README mentions all five run-time commands: `grep -c 'bootBuildImage\|docker compose .* up -d --wait\|k6 run loadtest/scripts/golden-path.js\|docker compose .* logs\|loadtestVerify' loadtest/README.md` returns ≥ 5.
    - README cites `-v` teardown: `grep -c 'down -v' loadtest/README.md` returns ≥ 1.
    - `loadtest/.gitignore` exists with `run/` entry: `grep -c '^run/' loadtest/.gitignore` returns 1.
    - Root `.gitignore` has `loadtest/run/` appended: `grep -c '^loadtest/run/' .gitignore` returns 1.
    - Pre-existing `.gitignore` entries preserved: `git diff .gitignore` shows ONLY additions (no deletions).
  </acceptance_criteria>
  <verify>
    <automated>test -f loadtest/README.md && test -f loadtest/.gitignore && grep -q '^loadtest/run/' .gitignore && grep -q 'down -v' loadtest/README.md</automated>
  </verify>
  <done>Operator runbook + .gitignore additions land; runtime artifacts excluded; committed evidence is exactly the result file from Task 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| k6 → /internal/pubsub/gmail | Synthetic Pub/Sub envelopes carry stub Bearer token; the loadtest profile's `TokenVerifier` (Plan 06-01) accepts any non-empty token. |
| compose stack ↔ host | Ports 15433 (postgres) + 8080 (api) bind to localhost on the runner; not exposed publicly. |
| `loadtest/run/run.log` ↔ committed artifacts | Run log is captured for invariant (c) scan, then discarded via `.gitignore`; only `06-LOAD-TEST-RESULT.md` is committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.02-01 | Information Disclosure | `loadtest/run/run.log` accidentally committed (real-looking synthetic data) | mitigate | Two `.gitignore` rules (root + `loadtest/.gitignore`) + the SPEC privacy constraint ("synthetic content only" — `loadtest-tenant-N@loadtest.invalid` is synthetic by construction). |
| T-06.02-02 | Tampering | Workshop env-var with real Google OAuth/SePay creds leaking via `${VAR}` interpolation | accept | Compose env block uses literal `loadtest` placeholders for all OAuth/SePay/LLM keys; only `REFRESH_TOKEN_KEY_BASE64` is sourced from runner shell env (generated fresh per run with `openssl rand`). |
| T-06.02-03 | Spoofing | Loadtest stub `TokenVerifier` accepts any token; a malicious actor pointing the api at the loadtest profile in prod could bypass OIDC | mitigate | Three guards from Plan 06-01: `@Profile("loadtest")` string + `@ConditionalOnProperty("zeromail.loadtest.enabled")` + ArchUnit `production_does_not_reference_loadtest` + YAML scan rule. |
| T-06.02-04 | Denial of Service | k6 at 500 req/min × 10m saturates the api container | accept | Test target — invariants are read on quiescent state after the run; the k6 thresholds (`rate<0.01` http_req_failed, `p(99)<2000`) abort the test if the system breaks instead of false-greening invariants. |
</threat_model>

<verification>
- `node` golden-path script validator from Task 1 passes.
- `docker compose -f loadtest/compose.loadtest.yml config` exits 0.
- `./gradlew --no-daemon :backend:api:tasks --group="verification"` lists `loadtestVerify`.
- `./gradlew --no-daemon :backend:api:tasks --group="build"` lists `bootBuildImage`.
- No Dockerfile committed: `find . -name 'Dockerfile*' -not -path './node_modules/*' -not -path './.gradle/*' -not -path './build/*' | wc -l` returns 0.
- Full functional run (executed by Plan 06-04's `release.yml`, not by this plan's CI): compose up → k6 run → log capture → `loadtestVerify` exits 0 and writes `06-LOAD-TEST-RESULT.md` with three PASS lines.
</verification>

<success_criteria>
- All five tasks land in separate commits.
- Compose stack boots end-to-end on a local Linux Docker host or GHA runner with `docker compose up -d --wait`.
- `:backend:api:loadtestVerify` is discoverable + parseable.
- `06-LOAD-TEST-RESULT.md` is generated (not pre-authored) and exits 0 when all three invariants pass.
- Plan 06-04 can call this Gradle task from the `release.yml` `loadtest` job without further configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-casa-verified-launch/06-02-SUMMARY.md` documenting:
- OQ-1 resolution (`bootBuildImage` succeeded with paketobuildpacks default OR fell back to `eclipse-temurin:25-jre-noble` builder — note which).
- Exact list of `:?` env vars enumerated from both `application.yml` files (so future load-harness edits don't miss one).
- Whether the local `k6 run --duration 30s` smoke test was performed and its outcome.
- Any deviation from research §1.3 / §1.6 (e.g., extra env vars discovered).
</output>
