---
phase: 06-polish-casa-verified-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/src/main/java/com/zeromail/api/security/PubSubSecurityConfig.java
  - backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java
  - backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubGmailApiClientFactory.java
  - backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubPubsubVerifierConfig.java
  - backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubResetController.java
  - backend/api/src/main/java/com/zeromail/api/e2estub/SeedMessageRequest.java
  - backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubAuthConfig.java
  - backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubChatModel.java
  - backend/api/src/main/java/com/zeromail/api/loadtest/LoadtestPubsubVerifierConfig.java
  - backend/api/src/main/resources/application-e2e-stub.yml
  - backend/api/src/main/resources/application-loadtest.yml
  - backend/api/src/test/java/com/zeromail/api/arch/LaunchProfileArchUnitTest.java
autonomous: true
requirements:
  - SPEC-06-R1
  - SPEC-06-R2
tags: [spring-profile, archunit, test-infra, java-25, spring-boot-4]

must_haves:
  truths:
    - "Under `spring.profiles.active=e2e-stub` AND `zeromail.e2e-stub.enabled=true`, an `E2eStubGmailApiClientFactory` subclass of `GmailApiClientFactory` is registered as `@Primary` so every existing production consumer (`GmailDeliveryProcessingService`, `GmailPreviewReadService`, `GmailConnectionService`, `DraftReplySourceLoader`, `ToneContextBuilder`, `TriageGmailWriter`, `SenderSafetyNetService`, `GmailWatchScheduler`) receives the stub WITHOUT source edits — codex HIGH-1 closure via opencode-c2 option-b subclass strategy. Alternative `GmailClient` interface path is documented in the plan; the executor picks ONE and justifies in the SUMMARY."
    - "The stub factory's `buildGmailClient(...)` returns a `com.google.api.services.gmail.Gmail` subclass whose `users().messages().get(...)`, `users().messages().modify(...)`, `users().messages().list(...)`, `users().drafts().create(...)`, `users().drafts().get(...)`, `users().drafts().delete(...)`, `users().labels().list(...)`, and `users().watch(...)` operations short-circuit `execute()` to read/write an in-memory `ConcurrentHashMap<String, SeededMessage>` + a `ConcurrentHashMap<String, SeededDraft>` — NO outbound HTTP. This closes opencode cycle-2 NEW MEDIUM (data-flow gap)."
    - "Under `spring.profiles.active=e2e-stub`, `E2eStubAuthConfig` contributes a `SecurityFilterChain @Order(2)` mirroring `TestSessionSupport` (header-based auth, `X-Test-Subject` + `X-Test-Email`, populates `SecurityContextHolder` + `TenantContext.TENANT`); a new endpoint `POST /api/test/e2e-stub/seed-session` creates or finds a tenant/user pair and returns `{ tenantId, googleSubject, email, sessionCookieValue }`. This closes codex HIGH-3 (real OAuth session needed) and opencode cycle-2 LOW (tenant-ID coordination)."
    - "Under `spring.profiles.active=e2e-stub`, `E2eStubChatModel` is registered as `@Primary` Spring AI `ChatModel` returning a fixed `ChatResponse` whose generation text is `\"Hello tester — generated by E2eStubChatModel\"`. This lets the golden-path spec request an AI draft and confirm the draft was saved in the stub Gmail (codex HIGH-4 SPEC AC#1 compliance)."
    - "Under `spring.profiles.active=loadtest` AND `zeromail.loadtest.enabled=true`, `LoadtestPubsubVerifierConfig` supplies a `@Primary` `TokenVerifier` that accepts any non-empty Bearer token; no auth filter is added (loadtest only exercises `/internal/pubsub/gmail`)."
    - "POST /api/test/e2e-stub/reset returns 204 and clears the in-memory stub Gmail message store + draft store + any auth-bound session marker; the endpoint is only registered when `zeromail.e2e-stub.enabled=true`."
    - "POST /api/test/e2e-stub/seed-message accepts the canonical `SeedMessageRequest(tenantId, messageId, threadId, from, subject, body)` Java 25 record and stores the message into the stub factory's in-memory map; this is the SAME shape Plan 06-03 posts (HIGH-2 reconciliation preserved)."
    - "GET /api/test/e2e-stub/drafts?messageId={id} returns the saved draft body for the given Gmail message id, or 404 if none exists; this is what the golden-path spec uses to assert SPEC AC#1 'draft saved in stub Gmail' (codex HIGH-4 closure)."
    - "Production `application.yml` (and `application-prod.yml` if present, in both api + worker) contains neither the literal `e2e-stub` nor `loadtest` nor `zeromail.e2e-stub.enabled: true` nor `zeromail.loadtest.enabled: true`."
    - "ArchUnit suite fails the build if any class outside `..api.e2estub..` depends on a class inside `..api.e2estub..`, and likewise for `..api.loadtest..` (SPEC AC #8 invariant preserved)."
    - "No class under `backend/api/src/main/java/com/zeromail/api/{e2estub,loadtest}/` imports a test-scope artifact (no `org.mockito.*`, no `com.google.api.client.testing.http.MockHttpTransport`, no `google-http-client-testing` types) — HIGH-1 classpath safety."
    - "TokenVerifier subclass uses the correct package `com.google.auth.oauth2.TokenVerifier` (Google Auth Library) as imported by the prod filter at `backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java:4` — NOT `com.google.api.client.googleapis.auth.oauth2.TokenVerifier`."
    - "The base64 placeholder for `REFRESH_TOKEN_KEY_BASE64` in `application-e2e-stub.yml` is exactly `AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=` (32 × `A`), matching `backend/api/build.gradle.kts:57` + `ApiPostgresTestBase.java:68`; decodes to exactly 32 bytes so `Cipher.init()` does not throw `InvalidKeyException` (opencode cycle-2 LOW closure)."
  artifacts:
    - path: "backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubGmailApiClientFactory.java"
      provides: "@Primary subclass of GmailApiClientFactory under e2e-stub profile; returns an offline Gmail subclass backed by in-memory seededMessages + seededDrafts maps"
      contains: "@Profile(\"e2e-stub\")"
    - path: "backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubPubsubVerifierConfig.java"
      provides: "@Primary TokenVerifier replacement under e2e-stub profile (com.google.auth.oauth2.TokenVerifier subclass)"
      contains: "@Profile(\"e2e-stub\")"
    - path: "backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubAuthConfig.java"
      provides: "Header-based SecurityFilterChain mirroring TestSessionSupport, lives under src/main/java so e2e-stub Playwright spec can authenticate against a running backend"
      contains: "SecurityFilterChain"
    - path: "backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubChatModel.java"
      provides: "@Primary Spring AI ChatModel returning a canned ChatResponse — supports SPEC AC#1 draft-saved assertion"
      contains: "ChatModel"
    - path: "backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubResetController.java"
      provides: "POST /api/test/e2e-stub/reset, POST /api/test/e2e-stub/seed-message, POST /api/test/e2e-stub/seed-session, GET /api/test/e2e-stub/drafts"
      contains: "@ConditionalOnProperty"
    - path: "backend/api/src/main/java/com/zeromail/api/e2estub/SeedMessageRequest.java"
      provides: "Canonical Java 25 record body for POST /api/test/e2e-stub/seed-message (consumed by Plan 06-03 Playwright spec)"
      contains: "record SeedMessageRequest"
    - path: "backend/api/src/main/java/com/zeromail/api/loadtest/LoadtestPubsubVerifierConfig.java"
      provides: "@Primary TokenVerifier replacement under loadtest profile"
      contains: "@Profile(\"loadtest\")"
    - path: "backend/api/src/main/resources/application-e2e-stub.yml"
      provides: "e2e-stub profile activation property + 32-byte AES key placeholder"
      contains: "zeromail.e2e-stub.enabled: true"
    - path: "backend/api/src/main/resources/application-loadtest.yml"
      provides: "loadtest profile activation property"
      contains: "zeromail.loadtest.enabled: true"
    - path: "backend/api/src/test/java/com/zeromail/api/arch/LaunchProfileArchUnitTest.java"
      provides: "ArchUnit guards for D-03 + D-07 profiles + glob-scan rule for all production application*.yml"
      contains: "DoNotIncludeTests"
  key_links:
    - from: "backend/api/src/main/java/com/zeromail/api/security/PubSubSecurityConfig.java"
      to: "backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java"
      via: "constructor injection of com.google.auth.oauth2.TokenVerifier @Bean"
      pattern: "TokenVerifier tokenVerifier"
    - from: "backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubGmailApiClientFactory.java"
      to: "backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java"
      via: "Java extends GmailApiClientFactory + @Primary override of buildGmailClient(String[, Duration])"
      pattern: "extends GmailApiClientFactory"
    - from: "backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubAuthConfig.java"
      to: "backend/api/src/test/java/com/zeromail/api/security/TestSessionSupport.java"
      via: "mirror pattern under src/main/java (NOT @Import — copy the SecurityFilterChain + header-based auth filter shape)"
      pattern: "X-Test-Subject"
---

<objective>
Land the test-only Spring infrastructure that both the Playwright golden-path spec (Plan 06-03) and the docker-compose load test (Plan 06-02) require. After cycle-2 codex review, the surface expanded to also cover: (a) actual interception of the production Gmail consumer call sites (codex HIGH-1 + opencode-c2 MEDIUM — subclass `GmailApiClientFactory` and the returned `Gmail` object); (b) a real Spring Session under the e2e-stub profile (codex HIGH-3) so the golden-path spec can authenticate; (c) an `E2eStubChatModel` (codex HIGH-4) so the spec can assert SPEC AC#1 "draft saved in stub Gmail" instead of the cycle-1 downgrade to "draft queued".

Purpose: D-06/D-07/D-03 require profile-gated bean swaps with belt-and-suspenders guards (`@Profile` + `@ConditionalOnProperty` + ArchUnit). Cycle-1's interface-based approach did NOT swap production usage because all consumers inject `GmailApiClientFactory` concretely. This revision replaces the interface extraction with a subclass override + an intercepting `Gmail` subclass; documents the alternative path (refactor 8 consumers to a new interface) as executor-time judgement.

Output: 8 new files + 2 modified files, all under `backend/api`; ArchUnit suite green; existing `./gradlew :backend:api:check` green; no behavior change in prod profile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONVENTIONS.md
@TESTING.md
@.planning/phases/06-polish-casa-verified-launch/06-SPEC.md
@.planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md
@.planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md
@.planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md
@.planning/phases/06-polish-casa-verified-launch/06-REVIEWS.md

<interfaces>
<!-- Production seams. KEEP THESE COMPATIBLE — production consumers must work unchanged. -->

`GmailApiClientFactory` is at `backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java:35`. It is a concrete `@Component public class` with constructor signature:
```
public GmailApiClientFactory(
    @Value("${spring.security.oauth2.client.registration.google.client-id}") String clientId,
    @Value("${spring.security.oauth2.client.registration.google.client-secret}") String clientSecret,
    ZeroMailCoreProperties properties,
    GmailConnectionRepository gmailConnectionRepository,
    RefreshTokenCipher refreshTokenCipher)
```
The two methods to override are `public Gmail buildGmailClient(String accessToken) throws IOException` (line 60) and `public Gmail buildGmailClient(String accessToken, Duration requestTimeout) throws IOException` (line 64). DO NOT override `buildClientForTenant(UUID, ...)` — that delegates to `buildGmailClient(...)` after token refresh and the refresh path itself is fine to keep (it calls `gmailConnectionRepository` + `refreshTokenCipher` which behave correctly in tests).

Production consumers that inject `GmailApiClientFactory` CONCRETELY (verified by grep — these must keep compiling unchanged):
- `backend/core/.../gmail/usecases/GmailDeliveryProcessingService.java`
- `backend/core/.../gmail/usecases/GmailPreviewReadService.java`
- `backend/core/.../gmail/usecases/GmailConnectionService.java`
- `backend/core/.../draft/usecases/DraftReplySourceLoader.java`
- `backend/core/.../draft/usecases/ToneContextBuilder.java`
- `backend/core/.../triage/usecases/TriageGmailWriter.java`
- `backend/core/.../triage/usecases/SenderSafetyNetService.java`
- `backend/worker/.../GmailWatchScheduler.java`

`PubSubOidcAuthFilter` at `backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java:1-32` imports `com.google.auth.oauth2.TokenVerifier` (NOT the API-client variant). Current constructor (line 23) is:
```
public PubSubOidcAuthFilter(String audience, String serviceAccountEmail, String certificatesUrl)
```
The internal `TokenVerifier.newBuilder()...build()` happens at lines 26-31. Refactor target — extract the verifier as a `@Bean`:
```
// PubSubSecurityConfig: @Bean TokenVerifier pubsubOidcTokenVerifier(...)
public PubSubOidcAuthFilter(String serviceAccountEmail, TokenVerifier tokenVerifier)
```

`SecurityConfig` at `backend/api/src/main/java/com/zeromail/api/security/SecurityConfig.java:16` is `@Profile("!test")` — the negation excludes ONLY the literal profile `test`, so it IS active under `e2e-stub` and `loadtest`. This means `/api/**` requires OAuth2 authentication under `e2e-stub` UNLESS another `SecurityFilterChain` with a higher order intercepts first. That is exactly what `E2eStubAuthConfig` provides (mirror of `TestSessionSupport`).

`TestSessionSupport` reference (NOT to import — it is `@TestConfiguration`, test-classpath only):
- `backend/api/src/test/java/com/zeromail/api/security/TestSessionSupport.java:33-130` shows the full pattern.
- Header names: `X-Test-Subject` + `X-Test-Email`.
- Filter sets `SecurityContextHolder` AuthN token AND populates `TenantContext.TENANT` ScopedValue keyed by `users.findByGoogleSubject(subject).tenantId`.
- Custom `@Bean @Order(2) SecurityFilterChain testSecurityChain(HttpSecurity http, OncePerRequestFilter testAuthFilter)` excludes `/internal/pubsub/**` and inserts the auth filter before `UsernamePasswordAuthenticationFilter`.

HIGH-2 canonical seed-message contract (Plan 06-03 Task 2 posts this; the controller deserializes; the stub's seed helper consumes):
```java
package com.zeromail.api.e2estub;
public record SeedMessageRequest(
    String tenantId,       // synthetic tenant id — for the e2e-stub, this is a UUID string from seed-session
    String messageId,
    String threadId,
    String from,
    String subject,
    String body
) {}
```
The Playwright spec posts a JSON object with exactly these 6 keys. The controller deserializes into the record via Jackson 3 canonical-constructor binding.

Existing 32-byte AES placeholder in the repo (use the SAME literal so the placeholder shape is consistent across openapi-emit + test-base + e2e-stub yml):
- `backend/api/build.gradle.kts:57` — `AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=`
- `backend/api/src/test/java/com/zeromail/api/support/ApiPostgresTestBase.java:68` — same string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract `TokenVerifier` into a @Bean in `PubSubSecurityConfig` and refactor `PubSubOidcAuthFilter` to receive it (resolves OQ-2; preserves prod behaviour)</name>
  <files>backend/api/src/main/java/com/zeromail/api/security/PubSubSecurityConfig.java, backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java</files>
  <read_first>
    - backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java (entire file — current constructor at lines 23-32 + the filter body that calls `tokenVerifier.verify(...)`; confirm import is `com.google.auth.oauth2.TokenVerifier`)
    - backend/api/src/main/java/com/zeromail/api/security/PubSubSecurityConfig.java (entire file — existing @Configuration; this plan adds a `@Bean TokenVerifier` method here)
    - backend/api/src/main/resources/application.yml (find the property keys feeding push-audience-url, sa-principal-email, certificates URL — DO NOT invent new ones)
    - .planning/phases/06-polish-casa-verified-launch/06-RESEARCH.md §2.1 + §7.9
    - .planning/phases/06-polish-casa-verified-launch/06-REVIEWS.md "Inspection 2" (TokenVerifier package fix)
    - CONVENTIONS.md (variable naming: `tokenVerifier`, `audience`, `serviceAccountEmail`, `certificatesUrl`)
  </read_first>
  <action>
    Per OQ-2 resolution (extract TokenVerifier as a @Bean):

    1. In `PubSubSecurityConfig`, add a new `@Bean public com.google.auth.oauth2.TokenVerifier pubsubOidcTokenVerifier(@Value(...) String audience, @Value(...) String certificatesUrl)` method that returns `com.google.auth.oauth2.TokenVerifier.newBuilder().setAudience(audience).setIssuer("https://accounts.google.com").setCertificatesLocation(certificatesUrl).build()`. Verify the exact `@Value` keys match the ones currently used by `PubSubOidcAuthFilter`'s constructor caller (likely `zero-mail.api.gmail.pubsub.push-audience-url` and `zero-mail.api.gmail.pubsub.oidc-certificates-url`).

    2. Change `PubSubOidcAuthFilter`'s constructor from `(String audience, String serviceAccountEmail, String certificatesUrl)` to `(String serviceAccountEmail, com.google.auth.oauth2.TokenVerifier tokenVerifier)`. Remove the `TokenVerifier.newBuilder()` call inside the constructor — store the injected `tokenVerifier` directly. Keep all `event=pubsub_oidc_*` log lines verbatim (privacy logging format is locked).

    3. Update the place that constructs `new PubSubOidcAuthFilter(...)` to inject the `TokenVerifier` @Bean.

    Variable names: `tokenVerifier`, `audience`, `serviceAccountEmail`, `certificatesUrl`. NO `tv` / `ver` / `cfg`.

    HIGH-1 classpath safety: the refactor stays inside `src/main/java`. Production-side wiring uses `com.google.auth.oauth2.TokenVerifier` (compile classpath via `google-auth-library`). Do NOT add Mockito or `google-http-client-testing` as `implementation` deps.

    Functional contract preserved: same audience, same issuer, same certificates location, same expected-email check, same 401 path on missing/invalid token. The point of the refactor is to make `TokenVerifier` swappable as a `@Bean`.
  </action>
  <acceptance_criteria>
    - `PubSubOidcAuthFilter` constructor signature is `(String serviceAccountEmail, com.google.auth.oauth2.TokenVerifier tokenVerifier)` — `grep -n 'public PubSubOidcAuthFilter' backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java` shows the new signature.
    - `PubSubSecurityConfig` contains a `@Bean` of type `com.google.auth.oauth2.TokenVerifier`.
    - `PubSubOidcAuthFilter` no longer references `TokenVerifier.newBuilder` directly — `grep -c 'TokenVerifier.newBuilder' backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java` returns 0.
    - All existing privacy log events remain verbatim — `grep -c 'event=pubsub_oidc_' backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java` returns ≥ 3.
    - `grep -RnE 'org\.mockito|MockHttpTransport|google-http-client-testing' backend/api/src/main/java/com/zeromail/api/security/` returns 0 hits.
    - `./gradlew :backend:api:check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew --no-daemon :backend:api:check</automated>
  </verify>
  <done>`TokenVerifier` is a @Bean; filter receives it via constructor; no production behavior change; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add e2e-stub package — Gmail interception (subclass strategy), Pub/Sub verifier, ChatModel, auth filter, reset controller, SeedMessageRequest, profile yml</name>
  <files>backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubGmailApiClientFactory.java, backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubPubsubVerifierConfig.java, backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubAuthConfig.java, backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubChatModel.java, backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubResetController.java, backend/api/src/main/java/com/zeromail/api/e2estub/SeedMessageRequest.java, backend/api/src/main/resources/application-e2e-stub.yml</files>
  <read_first>
    - backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailApiClientFactory.java (entire file — class signature line 35, constructor 44-58, the TWO `buildGmailClient(...)` overloads at 60 + 64; note that `buildClientForTenant` delegates to `buildGmailClient`, so overriding the two `buildGmailClient` methods covers everything)
    - backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java lines 50-260 (the heaviest consumer — see exactly which `gmail.users().X().Y().execute()` chains the executor's `Gmail` subclass MUST handle: `messages.modify(addLabel)`, `messages.modify(archive/removeInbox)`, `drafts.create`, `drafts.delete`, `labels.list`, `messages.get` for tone context)
    - backend/core/src/main/java/com/zeromail/core/gmail/usecases/GmailDeliveryProcessingService.java lines 80-170 (the other heavy consumer — `messages.get`, `history.list`)
    - backend/api/src/test/java/com/zeromail/api/security/TestSessionSupport.java (entire file — the EXACT pattern E2eStubAuthConfig mirrors under src/main/java; copy structure but do NOT import — TestSessionSupport is test-classpath only)
    - backend/api/src/main/java/com/zeromail/api/security/SecurityConfig.java (entire file — confirm `@Profile("!test")` is on the class; E2eStubAuthConfig's SecurityFilterChain must use `@Order(2)` (lower than SecurityConfig's `@Order(3)`) so it intercepts FIRST under e2e-stub)
    - backend/api/src/main/resources/application.yml (read entire file to enumerate every `:?` fail-fast key — the e2e-stub yml MUST provide a literal placeholder for every one)
    - backend/api/build.gradle.kts:57 (the `AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=` 32-byte placeholder reuse)
    - .planning/phases/06-polish-casa-verified-launch/06-REVIEWS.md (Inspections 1, 2, 3, 4, 11 — the cycle-2 findings being closed here)
    - .planning/phases/06-polish-casa-verified-launch/06-PATTERNS.md (existing patterns)
    - Spring AI 2.0.0-M6 ChatModel javadoc (resolve via Context7: `org.springframework.ai.chat.model.ChatModel` — implement `call(Prompt) -> ChatResponse` and `stream(Prompt) -> Flux<ChatResponse>`)
  </read_first>
  <action>
    Per D-07: package `com.zeromail.api.e2estub` under `src/main/java/`. Seven files. Each file follows enterprise-readability rules.

    **File 0 — `SeedMessageRequest.java`** (HIGH-2 canonical shape — UNCHANGED from cycle 1):
    Java 25 record with six fields `tenantId, messageId, threadId, from, subject, body`. Jackson 3 deserializes records via canonical constructor. No annotations needed.

    **File 1 — `E2eStubGmailApiClientFactory.java`** (codex HIGH-1 + opencode-c2 MEDIUM closure):

    Annotations stacked: `@Component`, `@Profile("e2e-stub")`, `@Primary`, `@ConditionalOnProperty(name = "zeromail.e2e-stub.enabled", havingValue = "true")`. `public class E2eStubGmailApiClientFactory extends GmailApiClientFactory`.

    Constructor: delegate to the parent constructor with the same five `@Value` / repository parameters (`clientId`, `clientSecret`, `ZeroMailCoreProperties properties`, `GmailConnectionRepository`, `RefreshTokenCipher`) — copy the signature from `GmailApiClientFactory.java:44-58` verbatim and call `super(...)`. The parent constructor stores these in private final fields; the stub doesn't use them but `super(...)` must be invocable. (If the parent's fields ARE used by the OAuth refresh path, the stub still inherits that path; the executor decides whether to override `buildClientForTenant` to skip refresh — recommend YES, override to skip refresh and just return `buildGmailClient(null, null)` because the stub Gmail subclass doesn't validate tokens.)

    State (in the stub subclass):
    ```java
    private final java.util.concurrent.ConcurrentHashMap<String, SeededMessage> seededMessages =
        new java.util.concurrent.ConcurrentHashMap<>();
    private final java.util.concurrent.ConcurrentHashMap<String, SeededDraft> seededDrafts =
        new java.util.concurrent.ConcurrentHashMap<>();
    public record SeededMessage(String tenantId, String messageId, String threadId, String from, String subject, String body) {}
    public record SeededDraft(String draftId, String messageId, String body) {}
    ```

    Public helpers:
    - `public void seedMessage(SeedMessageRequest request)` — `seededMessages.put(request.messageId(), new SeededMessage(request.tenantId(), request.messageId(), request.threadId(), request.from(), request.subject(), request.body()));`
    - `public void reset()` — `seededMessages.clear(); seededDrafts.clear();`
    - `public SeededDraft findDraft(String messageId)` — `return seededDrafts.values().stream().filter(d -> d.messageId().equals(messageId)).findFirst().orElse(null);`

    Override `buildGmailClient(String accessToken)` and `buildGmailClient(String accessToken, Duration requestTimeout)`. Both return an instance of an `OfflineGmail extends com.google.api.services.gmail.Gmail` subclass (nested inside the factory). The `OfflineGmail` uses a fail-loud `HttpTransport` (hand-rolled anonymous `HttpTransport` whose `buildRequest` throws `IOException("e2e-stub: outbound Gmail HTTP forbidden")`) — but every actual API call is intercepted before reaching transport via method override.

    **CRITICAL — executor-time judgement option:** The surface of `Gmail.users().*` chained calls that production code makes (see `TriageGmailWriter.java` 50-260) is large. The executor has TWO options:

    **Option A (subclass `Gmail`):** Override `Gmail.users()` to return a custom `Users` subclass; override `Users.messages()` to return a custom `Messages`; override `Messages.get(userId, messageId)` to return a `Get` request whose `execute()` reads from `seededMessages`. Do the same for `drafts.create`, `drafts.get`, `drafts.delete`, `labels.list`, `messages.modify`, `history.list`, `users.watch`. This requires writing ~10 small subclasses; each is 5-15 lines. The `execute()` short-circuit returns a fresh API model object (`Message`, `Draft`, `Label`, `ListLabelsResponse`, etc.) populated from the in-memory maps.

    **Option B (interface refactor):** Add a new `com.zeromail.core.gmail.gateway.GmailClient` interface with the leaf operations production code actually performs (`getMessage`, `listMessages`, `modifyLabels`, `archiveMessage`, `createDraft`, `getDraft`, `deleteDraft`, `listLabels`, `getHistory`, `watch`). Refactor all 8 consumer classes to inject and call this interface instead of constructing `Gmail` via the factory. The e2e-stub becomes a plain `@Primary GmailClient` bean reading the in-memory map directly. This is more invasive (touches 8 production classes) but cleaner — no Google API SDK subclassing.

    The executor MUST pick one and document the choice + rationale in the SUMMARY. The acceptance criteria below are written in terms of OBSERVABLE BEHAVIOUR (the golden-path spec must pass), not source shape, so either option satisfies them.

    **File 2 — `E2eStubPubsubVerifierConfig.java`** (HIGH-2 cycle-1 + HIGH-2 cycle-2 package fix):

    `@Configuration @Profile("e2e-stub") @ConditionalOnProperty(name = "zeromail.e2e-stub.enabled", havingValue = "true")`.

    One `@Bean @Primary public com.google.auth.oauth2.TokenVerifier e2eStubTokenVerifier(@Value("${zero-mail.api.gmail.pubsub.sa-principal-email}") String serviceAccountEmail)`.

    Implementation: subclass `com.google.auth.oauth2.TokenVerifier` (NOT the `com.google.api.client.googleapis.auth.oauth2.TokenVerifier` — that was the cycle-1 mistake). Seed the parent via `TokenVerifier.newBuilder().setAudience("e2e-stub").setIssuer("https://accounts.google.com").build()` then override `public com.google.api.client.json.webtoken.JsonWebSignature verify(String idToken) throws TokenVerifier.VerificationException`. Return a synthetic `JsonWebSignature` with `payload.email = serviceAccountEmail` so the prod filter's `expectedEmail.equalsIgnoreCase(verifiedEmail)` check passes; or return `null` when `idToken` is null/blank to match the prod filter's null-payload 401 path.

    NO Mockito. NO `google-http-client-testing`. NO `com.google.api.client.googleapis.auth.oauth2.TokenVerifier`.

    **File 3 — `E2eStubAuthConfig.java`** (codex HIGH-3 closure):

    `@Configuration @Profile("e2e-stub") @ConditionalOnProperty(name = "zeromail.e2e-stub.enabled", havingValue = "true")`.

    Mirror `TestSessionSupport`'s structure (read it, do NOT import — it is test-classpath). Produce:
    - One `@Bean public OncePerRequestFilter e2eStubAuthFilter(UserRepository users)` — reads `X-Test-Subject` + `X-Test-Email` headers, sets `SecurityContextHolder.getContext().setAuthentication(...)`, populates `ScopedValue.where(TenantContext.TENANT, tenantId.toString()).run(...)`. If headers absent, chain through (let SecurityConfig deny the request — that is fine for tests). If user not found, return 401.
    - One `@Bean @Order(2) SecurityFilterChain e2eStubSecurityChain(HttpSecurity http, OncePerRequestFilter e2eStubAuthFilter)` — securityMatcher excludes `/internal/pubsub/**`, requires authentication for everything else, disables csrf for `/api/test/e2e-stub/**` and `/login/oauth2/code/**`, inserts `e2eStubAuthFilter` before `UsernamePasswordAuthenticationFilter`. Crucially, this `@Order(2)` chain matches BEFORE the prod `SecurityConfig` `@Order(3)` chain → under e2e-stub, the prod OAuth2 login flow is bypassed for header-authenticated requests.

    The prod `SecurityConfig` remains `@Profile("!test")` — it stays active under e2e-stub but is ordered AFTER the stub chain, so it only handles requests that the stub chain `securityMatcher` did not match (i.e., `/internal/pubsub/**` for the prod Pub/Sub filter).

    NO Mockito. NO test-scope imports.

    **File 4 — `E2eStubChatModel.java`** (codex HIGH-4 closure):

    `@Component @Profile("e2e-stub") @Primary @ConditionalOnProperty(name = "zeromail.e2e-stub.enabled", havingValue = "true")`.

    `implements org.springframework.ai.chat.model.ChatModel`. Implementations:
    - `public org.springframework.ai.chat.model.ChatResponse call(org.springframework.ai.chat.prompt.Prompt prompt)` — return a `ChatResponse` with a single `Generation` whose text is the literal `"Hello tester — generated by E2eStubChatModel"`. The `Generation` constructor in Spring AI M6 is `new Generation(new AssistantMessage(text))` (verify against Context7 for the M6 exact API; M5+ requires `AssistantMessage` wrapping).
    - `public reactor.core.publisher.Flux<org.springframework.ai.chat.model.ChatResponse> stream(org.springframework.ai.chat.prompt.Prompt prompt)` — `return reactor.core.publisher.Flux.just(call(prompt));`

    No outbound HTTP; deterministic; no chat memory; respects the Privacy invariant (no real prompts/completions stored — the canned text is synthetic).

    **File 5 — `E2eStubResetController.java`**:

    `@RestController @Profile("e2e-stub") @ConditionalOnProperty(name = "zeromail.e2e-stub.enabled", havingValue = "true")`.

    Constructor-inject `E2eStubGmailApiClientFactory e2eStubGmailFactory`, `TenantRepository tenants`, `UserRepository users` (or whichever repository the seed-session endpoint uses to create+find tenants).

    Endpoints:
    1. `POST /api/test/e2e-stub/reset` → 204; calls `e2eStubGmailFactory.reset()`.
    2. `POST /api/test/e2e-stub/seed-message` → 204; body `SeedMessageRequest`; calls `e2eStubGmailFactory.seedMessage(request)`.
    3. `POST /api/test/e2e-stub/seed-session` → 200 with body `{ tenantId: UUID, googleSubject: String, email: String, sessionCookieName: String }`. Implementation: either (a) idempotently create a `Tenant` + `User` row for a deterministic subject `"e2e-stub-user-1"` + email `"e2e-stub-user-1@e2e-stub.invalid"`, OR (b) find the existing one. Return the freshly-resolved `tenantId` so the Playwright spec posts subsequent `seed-message` requests with the SAME tenantId.
    4. `GET /api/test/e2e-stub/drafts?messageId={id}` → 200 with `{ draftId, messageId, body }`, or 404 if `e2eStubGmailFactory.findDraft(messageId)` returns null.

    Privacy logging: `log.info("event=e2e_stub_reset")`, `log.info("event=e2e_stub_seed_message")`, `log.info("event=e2e_stub_seed_session tenantId={}", tenantId)`, `log.info("event=e2e_stub_drafts_query")`. NEVER log message body, subject, draft body, or email content.

    Variable names: `e2eStubGmailFactory`, `request`, `tenantId`. NO `factory` / `r` / `tid`.

    **File 6 — `application-e2e-stub.yml`**:

    HIGH-4 self-sufficiency: literal placeholder for every `:?` env var in `application.yml`. Use the existing 32-byte base64 key. Key contents:

    ```yaml
    # Activated by SPRING_PROFILES_ACTIVE=e2e-stub. Used by Playwright launch-golden-path.spec.ts only.
    # HIGH-4: self-sufficient — no host-side env vars required for bootRun. Every override below is a literal placeholder.
    zeromail:
      e2e-stub:
        enabled: true
    zero-mail:
      crypto:
        refresh-token-key-base64: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=  # 32 × A, decodes to 32 bytes for AES-256
      api:
        gmail:
          pubsub:
            push-audience-url: https://e2e-stub.invalid/internal/pubsub/gmail
            sa-principal-email: e2e-stub-sa@e2e-stub.invalid
            oidc-certificates-url: https://www.googleapis.com/oauth2/v3/certs
      billing:
        sepay:
          webhook-api-key: e2e-stub-sepay-key
        payment-account:
          bank-code: e2e-stub-bank
          bank-name: e2e-stub
          account-number: "0000"
          account-name: e2e-stub-account
          qr-payload: ""
        vnd-per-credit: 1000
        max-pending-intents-per-tenant: 5
        intent-expiry: PT24H
      llm:
        platform:
          api-key: e2e-stub-llm-key
    spring:
      security:
        oauth2:
          client:
            registration:
              google:
                client-id: e2e-stub-client-id
                client-secret: e2e-stub-client-secret
      ai:
        openai:
          api-key: e2e-stub-openai-key
        anthropic:
          api-key: e2e-stub-anthropic-key
    ```

    Rules:
    - Every value is a LITERAL string. NO `:?` operator anywhere in this file.
    - The base64 key is EXACTLY `AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=` (32 × `A` — matches `build.gradle.kts:57` + `ApiPostgresTestBase.java:68`). Decodes to exactly 32 bytes, satisfies AES-256 `Cipher.init()`.
    - DO NOT set `spring.ai.model.chat: none` — the `E2eStubChatModel` is `@Primary`, so the default ChatModel autoconfig (whichever provider Spring AI picks first) gets overridden. Setting `chat: none` would prevent ChatModel autoconfig from running, which on M6 may cause `E2eStubChatModel` registration to fail (depends on whether the `@Primary` requires a baseline bean to override). The executor verifies on boot — if `E2eStubChatModel` is registered correctly without a baseline, remove the `openai.api-key` line; otherwise keep it for autoconfig.
    - If the executor finds additional `:?` keys in `application.yml` not enumerated above, ADD a placeholder line. The yml is correct iff `grep -c '\${.*:?.*}' backend/api/src/main/resources/application-e2e-stub.yml` returns 0.
  </action>
  <acceptance_criteria>
    - `backend/api/src/main/java/com/zeromail/api/e2estub/` contains exactly six Java files: `E2eStubGmailApiClientFactory.java`, `E2eStubPubsubVerifierConfig.java`, `E2eStubAuthConfig.java`, `E2eStubChatModel.java`, `E2eStubResetController.java`, `SeedMessageRequest.java`.
    - `E2eStubGmailApiClientFactory` declaration includes `extends GmailApiClientFactory` AND all four guard annotations — `grep -c 'extends GmailApiClientFactory\|@Primary\|@Profile..e2e-stub..\|@ConditionalOnProperty' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubGmailApiClientFactory.java` returns ≥ 4.
    - HIGH-2 — `SeedMessageRequest.java` declares 6 canonical fields: `grep -cE '\b(tenantId|messageId|threadId|from|subject|body)\b' backend/api/src/main/java/com/zeromail/api/e2estub/SeedMessageRequest.java` returns ≥ 6.
    - `E2eStubPubsubVerifierConfig` uses the correct package: `grep -c 'com.google.auth.oauth2.TokenVerifier' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubPubsubVerifierConfig.java` returns ≥ 1; `grep -c 'com.google.api.client.googleapis.auth.oauth2.TokenVerifier' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubPubsubVerifierConfig.java` returns 0.
    - `E2eStubAuthConfig` exposes a `SecurityFilterChain @Order(2)` AND a header-based `OncePerRequestFilter`: `grep -c '@Order(2)' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubAuthConfig.java` returns ≥ 1; `grep -c 'X-Test-Subject\|X-Test-Email' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubAuthConfig.java` returns ≥ 2.
    - `E2eStubChatModel` implements Spring AI `ChatModel`: `grep -c 'implements .*ChatModel\|org.springframework.ai.chat.model.ChatModel' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubChatModel.java` returns ≥ 1; the canned text appears verbatim: `grep -Fc 'Hello tester — generated by E2eStubChatModel' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubChatModel.java` returns ≥ 1.
    - `E2eStubResetController` exposes the FOUR endpoints: `grep -c '/api/test/e2e-stub/' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubResetController.java` returns ≥ 4; the literal paths `reset`, `seed-message`, `seed-session`, `drafts` all appear.
    - HIGH-1 — no test-scope import: `grep -RnE 'org\.mockito|MockHttpTransport|google-http-client-testing' backend/api/src/main/java/com/zeromail/api/e2estub/` returns 0.
    - HIGH-1/opencode-c2 MEDIUM — Gmail interception covers the production call sites. EITHER (a) `E2eStubGmailApiClientFactory.java` contains nested subclasses for `Gmail.Users`, `Gmail.Users.Messages`, `Gmail.Users.Drafts`, `Gmail.Users.Labels` (subclass option) — `grep -c 'extends Gmail\b\|extends Users\b\|extends Messages\b\|extends Drafts\b' backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubGmailApiClientFactory.java` returns ≥ 4 — OR (b) a `GmailClient` interface exists at `backend/core/src/main/java/com/zeromail/core/gmail/gateway/GmailClient.java` AND every consumer in the inspection list now injects it (`grep -c 'GmailClient' backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java` ≥ 2, AND `grep -c 'GmailApiClientFactory ' backend/core/src/main/java/com/zeromail/core/triage/usecases/TriageGmailWriter.java` is 0). The SUMMARY documents the option chosen.
    - `application-e2e-stub.yml` contains the 32-byte placeholder: `grep -Fq 'refresh-token-key-base64: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=' backend/api/src/main/resources/application-e2e-stub.yml` exits 0.
    - `application-e2e-stub.yml` self-sufficient: `grep -c '\${.*:?.*}' backend/api/src/main/resources/application-e2e-stub.yml` returns 0.
    - `./gradlew :backend:api:compileJava` exits 0.
    - `./gradlew :backend:api:bootRun --args='--spring.profiles.active=e2e-stub --zeromail.e2e-stub.enabled=true'` starts to "Started ZeroMailApiApplication" on a clean shell with NO host-side env vars exported.
    - Functional smoke (deferred to Plan 06-03's spec — recorded here for traceability): with backend up under e2e-stub, `curl -X POST -H 'Content-Type: application/json' http://localhost:8080/api/test/e2e-stub/seed-session` returns 200 with a JSON body containing `tenantId`.
    - No source file in `backend/api/src/main/resources/` OR `backend/worker/src/main/resources/` (excluding `application-e2e-stub.yml` itself) contains the literal `e2e-stub`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew --no-daemon :backend:api:check</automated>
  </verify>
  <done>The six stub Java files + the yml exist; Spring context boots cleanly under `e2e-stub`; Gmail consumer call sites are intercepted (executor chose subclass OR interface option, documented in SUMMARY); auth filter authenticates header-tagged requests; ChatModel returns the canned text; `SeedMessageRequest` shape is shared between controller, factory, and Plan 06-03 spec; no test-scope class is reachable from `src/main/java`; production yml files clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add `loadtest` package — `LoadtestPubsubVerifierConfig` + `application-loadtest.yml`</name>
  <files>backend/api/src/main/java/com/zeromail/api/loadtest/LoadtestPubsubVerifierConfig.java, backend/api/src/main/resources/application-loadtest.yml</files>
  <read_first>
    - backend/api/src/main/java/com/zeromail/api/e2estub/E2eStubPubsubVerifierConfig.java (post Task 2 — sibling pattern; same shape, different profile string)
    - backend/api/src/main/java/com/zeromail/api/security/PubSubOidcAuthFilter.java:4 (confirm TokenVerifier import path)
    - .planning/phases/06-polish-casa-verified-launch/06-CONTEXT.md D-03
  </read_first>
  <action>
    Per D-03: profile `loadtest`, activation property `zeromail.loadtest.enabled`. Sibling-of-Task-2 implementation.

    File 1 — `LoadtestPubsubVerifierConfig.java`:
    - `@Configuration @Profile("loadtest") @ConditionalOnProperty(name = "zeromail.loadtest.enabled", havingValue = "true")`.
    - One `@Bean @Primary public com.google.auth.oauth2.TokenVerifier loadtestTokenVerifier(@Value("${zero-mail.api.gmail.pubsub.sa-principal-email}") String serviceAccountEmail)` — copy/paste the implementation body from `E2eStubPubsubVerifierConfig.e2eStubTokenVerifier(...)`, change only the bean name + the surrounding annotations.
    - HIGH-1: NO Mockito, NO `MockHttpTransport`, NO `google-http-client-testing`.
    - Privacy logging: if any log line is added, use `event=loadtest_pubsub_verify_stub`.
    - Variable names: `tokenVerifier`, `serviceAccountEmail`.

    File 2 — `application-loadtest.yml`:
    - `zeromail.loadtest.enabled: true`.
    - `spring.ai.model.chat: none` — loadtest is purely Pub/Sub ingestion; no ChatModel is wired and we WANT autoconfig to skip it. (Unlike e2e-stub which needs ChatModel for the AI-draft step.)
    - Header comment: `# Activated by SPRING_PROFILES_ACTIVE=loadtest. Used by loadtest/compose.loadtest.yml only. See D-02/D-03.`
    - Most loadtest env values come from `loadtest/compose.loadtest.yml` `environment:` block — this yml only sets the activation property + the chat-disabled line.
  </action>
  <acceptance_criteria>
    - `backend/api/src/main/java/com/zeromail/api/loadtest/` contains exactly `LoadtestPubsubVerifierConfig.java`.
    - All three annotations present: `grep -c '@Configuration\|@Profile("loadtest")\|@ConditionalOnProperty' backend/api/src/main/java/com/zeromail/api/loadtest/LoadtestPubsubVerifierConfig.java` returns ≥ 3.
    - One `@Bean @Primary com.google.auth.oauth2.TokenVerifier`: `grep -c 'com.google.auth.oauth2.TokenVerifier' backend/api/src/main/java/com/zeromail/api/loadtest/LoadtestPubsubVerifierConfig.java` returns ≥ 1.
    - HIGH-1 — no test-scope import: `grep -RnE 'org\.mockito|MockHttpTransport|google-http-client-testing' backend/api/src/main/java/com/zeromail/api/loadtest/` returns 0.
    - `application-loadtest.yml` contains literal `zeromail.loadtest.enabled: true` AND `chat: none`.
    - Production yml clean: `grep -nR 'loadtest' backend/api/src/main/resources/ backend/worker/src/main/resources/ | grep -v 'application-loadtest.yml' | wc -l` returns 0.
    - `./gradlew :backend:api:compileJava` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew --no-daemon :backend:api:compileJava</automated>
  </verify>
  <done>`LoadtestPubsubVerifierConfig` + `application-loadtest.yml` land with both belt-and-suspenders guards; production yml clean; no test-scope import.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add `LaunchProfileArchUnitTest` — two ArchUnit rules + one YAML-scan JUnit test that globs ALL `application*.yml` (D-03/D-07 third belt)</name>
  <files>backend/api/src/test/java/com/zeromail/api/arch/LaunchProfileArchUnitTest.java</files>
  <read_first>
    - backend/api/src/test/java/com/zeromail/api/arch/I18nArchUnitTest.java (entire file — rule shape, `@AnalyzeClasses` with `ImportOption.DoNotIncludeTests.class`, multiple `@ArchTest` fields)
    - backend/core/src/test/java/com/zeromail/core/draft/DraftPathArchUnitTest.java (entire file — predicate shape)
    - backend/api/src/main/java/com/zeromail/api/e2estub/ (post Task 2)
    - backend/api/src/main/java/com/zeromail/api/loadtest/ (post Task 3)
    - backend/api/src/main/resources/application.yml
    - backend/worker/src/main/resources/application.yml
  </read_first>
  <action>
    Same as cycle-1 plan — UNCHANGED. Three checks in one class: Rule A (e2e-stub isolation), Rule B (loadtest isolation), Rule C (glob `application*.yml` in api + worker, fail if any contains `e2e-stub` or `loadtest` or activates the test profiles).

    Class header: `@AnalyzeClasses(packages = "com.zeromail", importOptions = ImportOption.DoNotIncludeTests.class)`.

    Rule A:
    ```
    @ArchTest
    static final ArchRule production_does_not_reference_e2estub =
        noClasses().that().resideOutsideOfPackage("..api.e2estub..")
            .should().dependOnClassesThat().resideInAPackage("..api.e2estub..")
            .because("D-07: e2e-stub beans are test infrastructure; production code must not reach them.");
    ```

    Rule B (similar shape for `..api.loadtest..`).

    Rule C: plain JUnit `@Test` — glob both `backend/api/src/main/resources/` and `backend/worker/src/main/resources/` for `application*.yml` files (excluding `application-e2e-stub.yml` + `application-loadtest.yml` themselves); for each, fail if content contains `e2e-stub`, `loadtest`, or matches `zeromail\.(loadtest|e2e-stub)\.enabled\s*:\s*true`.

    Enterprise readability: `content`, `file`, `scanned`. Use `java.nio.charset.StandardCharsets.UTF_8`.
  </action>
  <acceptance_criteria>
    - File exists at `backend/api/src/test/java/com/zeromail/api/arch/LaunchProfileArchUnitTest.java`.
    - Class annotated `@AnalyzeClasses(packages = "com.zeromail", importOptions = ImportOption.DoNotIncludeTests.class)`.
    - Two `@ArchTest` rules: `production_does_not_reference_e2estub`, `production_does_not_reference_loadtest`.
    - One `@Test` method `production_ymls_do_not_activate_test_profiles` that globs both `api` + `worker` resources.
    - All three rules pass on the current codebase: `./gradlew :backend:api:test --tests 'com.zeromail.api.arch.LaunchProfileArchUnitTest'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew --no-daemon :backend:api:test --tests "com.zeromail.api.arch.LaunchProfileArchUnitTest"</automated>
  </verify>
  <done>Three guards land in one test file; Rule C globs every `application*.yml` and names each scanned path on failure; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod ↔ test-only beans | Stubs in `e2estub/` and `loadtest/` packages must never load in production; guards are `@Profile` + `@ConditionalOnProperty` + ArchUnit + YAML scan. |
| outbound Gmail call ↔ in-memory fake | `E2eStubGmailApiClientFactory` overrides `buildGmailClient(...)` to return a `Gmail` whose API calls read/write in-memory maps; the underlying `HttpTransport` is fail-loud if ever reached. |
| Pub/Sub OIDC verify ↔ accept-anything stub | Production OIDC verifier is swapped for a `com.google.auth.oauth2.TokenVerifier` subclass that returns a synthetic `JsonWebSignature`; guarded by profile + property. |
| Real user session ↔ e2e-stub header auth | The `E2eStubAuthConfig` `SecurityFilterChain @Order(2)` intercepts authenticated traffic under e2e-stub via `X-Test-Subject` + `X-Test-Email`; the prod OAuth2 chain (`@Order(3)`) handles only unmatched paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.01-01 | Elevation of Privilege | `E2eStubPubsubVerifierConfig` / `LoadtestPubsubVerifierConfig` / `E2eStubAuthConfig` | mitigate | Belt-and-suspenders: `@Profile` string + `@ConditionalOnProperty` + ArchUnit Rule A/B + YAML-scan Rule C. Three independent guards must all be defeated. |
| T-06.01-02 | Tampering | Production `application*.yml` accidentally gains `loadtest` or `e2e-stub` activation | mitigate | Rule C globs every `application*.yml` under api + worker main resources at build time. |
| T-06.01-03 | Information Disclosure | `E2eStubResetController` accessible in prod | mitigate | Controller gated by `@Profile("e2e-stub")` + `@ConditionalOnProperty`. |
| T-06.01-04 | Spoofing | Header-based auth filter (`X-Test-Subject`) loads in production and accepts unauthenticated requests | mitigate | `E2eStubAuthConfig` is `@Profile("e2e-stub") @ConditionalOnProperty` — both guards must be true for the SecurityFilterChain bean to register. ArchUnit Rule A blocks any production class from referencing it. YAML scan Rule C prevents `e2e-stub` from being activated in production yml. |
| T-06.01-05 | Tampering | Build breaks because a test-scope class is imported from `src/main/java` | mitigate | HIGH-1: every stub uses only compile-classpath APIs; grep gate enforces this on every Plan 06-01 commit. |
| T-06.01-06 | Information Disclosure | `E2eStubChatModel` accidentally invoked in prod, returning a hardcoded reply | mitigate | `@Profile("e2e-stub")` gate + `@ConditionalOnProperty` + ArchUnit Rule A. The model is `@Primary` only when its enclosing config is loaded — which requires the e2e-stub profile to be active. |
</threat_model>

<verification>
- `./gradlew --no-daemon :backend:api:check` exits 0.
- `./gradlew :backend:api:bootRun --args='--spring.profiles.active=e2e-stub --zeromail.e2e-stub.enabled=true'` boots cleanly (kill after `/actuator/health/readiness` returns 200).
- `./gradlew :backend:api:bootRun --args='--spring.profiles.active=loadtest --zeromail.loadtest.enabled=true'` boots cleanly.
- `grep -nR 'e2e-stub\|loadtest' backend/api/src/main/resources/ | grep -v 'application-e2e-stub.yml\|application-loadtest.yml' | wc -l` returns 0.
- `grep -RnE 'org\.mockito|MockHttpTransport|google-http-client-testing' backend/api/src/main/java/com/zeromail/api/e2estub/ backend/api/src/main/java/com/zeromail/api/loadtest/` returns 0 (HIGH-1).
- The new ArchUnit test class is executed as part of `./gradlew :backend:api:test`.
- Functional smoke: `curl -fsS http://localhost:8080/api/test/e2e-stub/seed-session -X POST` returns 200 + JSON body containing `tenantId`.
- Functional smoke: with a seeded session + seed-message, then a synthetic Pub/Sub push, the existing triage code path executes through the stub `Gmail` without throwing `IOException` (the offline `HttpTransport` is never invoked).
</verification>

<success_criteria>
- All four tasks complete and committed.
- `./gradlew --no-daemon :backend:core:check :backend:api:check` green.
- Two new packages (`com.zeromail.api.e2estub`, `com.zeromail.api.loadtest`); `SeedMessageRequest` record; six e2e-stub Java files; one loadtest Java file; two yml files; one ArchUnit test class; the TokenVerifier @Bean refactor on the prod side.
- No production yml file references either test profile.
- No test-scope artifact reachable from `src/main/java`.
- Plans 06-02 (loadtest) and 06-03 (Playwright golden-path) can consume these stubs without further refactoring. The `SeedMessageRequest` record is the shared contract.
- The executor's choice between Task 2 option A (Gmail subclass) and option B (GmailClient interface refactor) is documented in the SUMMARY with rationale.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-casa-verified-launch/06-01-SUMMARY.md` documenting:
- OQ-2 resolution: extracted `TokenVerifier` as a @Bean with correct package `com.google.auth.oauth2.TokenVerifier`; quote the line-count of the filter refactor.
- Codex HIGH-1 + opencode-c2 MEDIUM resolution: which option (A subclass-Gmail or B interface-refactor) was picked and why. If option A, list the `Gmail.users().*` chain methods that were overridden (e.g. `messages.get`, `messages.modify`, `drafts.create`, `drafts.get`, `drafts.delete`, `labels.list`, `history.list`, `users.watch`). If option B, list the consumer classes refactored to inject `GmailClient`.
- Codex HIGH-2 resolution: confirm the `com.google.auth.oauth2.TokenVerifier` subclass compiles and behaves correctly under integration test.
- Codex HIGH-3 resolution: the `E2eStubAuthConfig` `SecurityFilterChain` boots without conflicting with the prod `SecurityConfig`; document the exact `@Order` numbers used.
- Codex HIGH-4 resolution: confirm `E2eStubChatModel` is registered as `@Primary` and the canned text appears in the e2e-stub Gmail draft store after a draft request.
- HIGH-2 (cycle-1) preserved: confirm the canonical `SeedMessageRequest(tenantId, messageId, threadId, from, subject, body)` record signature is the single source of truth.
- Opencode-c2 LOW resolution: confirm the 32-byte base64 placeholder is exactly `AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=`.
- MED-5 (cycle-1) resolution: list the application yml paths Rule C globbed.
</output>